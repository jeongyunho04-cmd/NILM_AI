# 인수인계 — 학습 PC, 2026-08-25

`HANDOFF_2026-08-24b.md` 다음 세션의 기록이다. 상세 근거는
`MODEL_TRAINING_DESIGN.md` **12.62 ~ 12.88** 절에 있다. 커밋 33개.

---

## 0. 한 줄 요약

**계측기 버그를 하나 찾아 고쳤고 SMPS 맞바꿈을 15 -> 8 로 줄였다. 그리고 미니PC 를
직접 겨냥한 아홉 가지 시도가 전부 닫히면서, 그 실패들이 하나의 진단으로 수렴했다 —
식별가능성 문제다.**

- **짝수차 고조파는 계측 인공물이다** (레인지 전환 오프셋 단차). 사용자가 펌웨어를
  분석해 기제를 짚었고 데이터로 검증했다 — 12.72
- 그것을 배제하고 홀수차 크기 지터를 걸어 **전이 귀속 21/41 -> 29/41,
  SMPS 맞바꿈 15 -> 8** — 12.78
- 새 데이터(펌웨어 수정본 5개 + test_8 / test_9)를 반영 — 12.77~12.78
- **미니PC 는 저항 부하에 안 묻힌다. SMPS 끼리 혼동한다** — 12.81
- **아홉 개 시도가 다 닫혔고 12.87.3 이 이유를 하나로 설명한다** — 12.88

> ⚠ **이번 세션은 철회가 많다.** 6.1 절 1번 규칙을 **세 번 어겼다.**
> 아래 2절을 먼저 읽을 것.

---

## 1. 지금 상태

### 운영점 후보 (아직 교체 안 함)

```
results/adapt_ze1.pt  +  results/cnn_ze1.pt
python -m src.run_gate_check --ckpt results/adapt_ze1.pt     --ckpt-smps results/cnn_ze1.pt --stride 6
```

```
조합                 유령W   잔차W  미니PC 프로젝터 충전기  핫플   오븐   전이귀속
adapt_ze1+cnn_ze1    8.67  11.13  0.763  0.898  0.969  0.978  0.986  29/41·맞바꿈8
adapt_mag1+cnn_mag1  2.33  12.08  0.658  0.895  0.964  0.979  0.954  29/41·맞바꿈6
```

`mag1` 은 크기 채널 8개를 더한 팔이다. **미니PC 가 나빠져 채택하지 않았으나
유령과 맞바꿈은 더 낫다.** n=1 이고 운영 조합의 실행 간 폭은 **아직 안 쟀다.**

> ⚠ **이전 표와 직접 비교 금지.** 파일 6->8개, F1 평균 대상 3->4개, 세그먼트 풀과
> 합성 홀드아웃이 전부 새것이다. **전이 귀속만 같은 41개라 비교된다.**

### 저장소 상태 — 중요

- `inputs.ZERO_EVEN_HARMONICS = True`, `FINE_CHANNELS = 58`
- **기존 체크포인트(cnn_ov1, adapt_ph1, cnn_even, cnn_oddmag)는 가드에 막혀
  못 돈다.** `ZERO_EVEN_HARMONICS = False` 로 되돌리면 다시 돈다 — 채널을
  지우지 않고 0 으로 만든 이유가 이것이다 (12.77.1)
- 캐시: `train60_ze1`(18G, 현행) `train60_oddmag` `train60_mix` 등.
  옛것(`train60_v17` `_nohp2` `_ov` `_pf` `_ph`)은 지워도 된다
- **CPU 오버클럭 해제 뒤 크래시가 멈췄다** (그 전 3회 중 2회가 캐시 빌드 중). n=1

---

## 2. 철회된 것 — 먼저 읽을 것

이번 세션에 **결론 여섯 개를 철회했다.** 다시 그 길로 가지 말 것.

| 절 | 철회한 주장 | 실제 |
|---|---|---|
| 12.62.4 | 합성은 그 쌍을 고조파 지문으로 풀지 않는다 | 지터 σ 가 k=2 에서 0.11 인데 격차가 12.8σ 였다. **재지 못한 것을 반증으로 적었다** (12.65.4) |
| 12.65.3 | 그 쌍의 판별자는 ch35 = I2/I1 | 절제 그룹에 짝수 하나와 홀수 셋을 묶어 오인. ch35 단독 절제는 −0.0025 (12.73.1) |
| 12.71.4 | 모델이 쓰는 판별자는 계측 인공물이다 | 모델은 짝수차를 거의 안 썼다 (12.73) |
| 12.79.3 | 미니PC 는 저항 부하에 묻힌다 (66배 압축) | 압축은 실재하나 원인이 아니다. **저항만 있으면 재현율 99%** (12.81.2) |
| 12.81.5 | 비율 채널 지터로 위상으로 옮긴다 | 옮겨갈 곳이 없다. 복합에서 위상 d′ 0.4 (12.82) |
| 12.86 초안 | 미니PC 전력이 프로젝터에 얹힌다 | 교락된 비교였다. 같은 조건에서는 차이 없음 (12.86.2) |

**살아남은 것**: 12.72 의 계측 발견 전부.

---

## 3. 이번 세션이 확정한 것

### 3.1 짝수차 고조파는 계측 인공물이다 (12.72)

**사용자가 펌웨어(Core/Src/nilm_acq.c)를 분석해 기제를 짚었다.**
두 증폭 경로(LOW/HIGH)의 DC 오프셋이 개별 보정되지 않아 레인지 전환마다 단차가
생기고, 그 1/h 스펙트럼에 RC 보상 `s_rc_gain[h] ∝ h` 가 곱해져 **모든 차수에서
평평한 바닥**이 된다.

데이터 검증:

```
짝수차와의 순위상관   |I1| −0.36 │ 첨두 −0.16 │ **파고율 +0.855** │ **저전류 체류 +0.827**
SMPS 무리(파고율 3.8~5.2) 0.007~0.053  vs  선형 무리(~1.0) 0.0007~0.0018
헤어드라이어 r(|I2|,|I1|) = −0.993   (순저항인데 결정론적 관계)
같은 기기의 녹화 사이에서 1.3~1.8배 흔들린다
```

**펌웨어 수정은 절반만 먹었다:**

```
laptop_charger  짝수차 0.0551 -> 0.0253 (2.2배 감소), 홀수차 불변 ✓
hotplate        r(|I2|,|I1|) −0.990 -> −0.996 (불변)
```

큰 단차는 절반이 됐으나 **영점 교차 기제는 그대로다.** 이득 불일치가 남은
후보다. **소프트웨어 배제를 유지해야 한다.**

### 3.2 짝수차 배제를 정식화했다 (12.77)

`build_fine` 한 곳에서 15채널(짝수 Re/Im + ch35)을 0 으로 만들고, 체크포인트에
`zero_even_harmonics` 를 기록해 로더가 검사한다. **학습과 추론이 짝을 이뤄야
안전하다** — 지터 없이 학습한 모델의 짝수차를 추론에서만 끄면 충전기가
0.937 -> 0.868 로 무너진다 (12.74.2).

### 3.3 미니PC 는 SMPS 끼리 혼동한다 (12.81)

```
미니PC 재현율      저부하 <200W    고부하 >600W
경쟁 SMPS 없음      98.2%          99.2%     <- 부하와 무관
경쟁 SMPS 있음      66.6%          30.2%
```

**프로젝터·충전기가 없으면 저항 부하가 3kW 여도 99% 맞힌다.**

### 3.4 그리고 배분이 미결정이다 (12.87.3) — 이번 세션의 핵심 진단

```
총합은 맞는다        L_cons 가 강제한다
고조파도 맞는다      L_harm 이 강제한다 — 그런데 SMPS 3종을 못 가른다
개별 배분은 미결정   프로젝터 +8W, 미니PC 0W 여도 손실이 안 는다
```

복합에서 세 기기를 가르는 **순시 특징의 최고 d′ 가 0.65** 다 (12.84.4).
**관측 한 순간으로는 원리적으로 안 갈린다.**

---

## 4. 소진 목록 — 다시 하지 말 것 (12.88.3)

| # | 시도 | 결과 | 절 |
|---|---|---|---|
| 1 | 비율 채널 제거 | 순손실 (프로젝터 −0.15, 충전기 −0.10) | 12.80.3 |
| 2 | 홀수차 크기 채널 추가 | 미니PC 0.763 -> 0.658. ch0~29 와 중복 | 12.80.1, 12.84.3 |
| 3 | 위상 강화 (지터로 유도) | 복합 d′ 0.4 라 못 넘는다 | 12.82 |
| 4 | 위상 재인코딩 (3차 기준 등) | 현행 φ_h − h·φ1 이 최선 | 12.83.3 |
| 5 | 전이 차분 특징 | 미니PC 규모에서 오염이 신호를 넘는다 | 12.83.2 |
| 6 | 지속성 후처리 (게이트 HMM) | +0.01. 방출이 전이 사전을 100배 압도 | 12.85.2 |
| 7 | 잔차 축 (총전력 보존) | d′ 0.24 | 12.86.1 |
| 8 | 물리 전력 범위 사전 | 고조파가 나빠진다 | 12.87.2 |
| 9 | 전이 ΔP 앵커 | 앵커가 2.4배 흩어지고 고조파도 나빠진다 | 12.88.1 |

**새 후보가 나오면 3.4 의 진단(배분 미결정)과 맞는지부터 볼 것.**

---

## 5. 다음에 할 것 (우선순위 순)

### 5.1 SMPS 3종 동시 학습 창 늘리기 — 1순위

**아홉 개 중 유일하게 안 시도한 축이다.** 12.78 이 새 녹화로 맞바꿈을 15 -> 8 로
줄인 전례가 있다. `run_recipe_mix_probe` 로 후보를 먼저 재고(학습 전 10분),
전체 동시성이 아니라 **SMPS 3종 조합 비율**을 겨냥할 것 — 12.68 이 전체
동시성을 올렸을 때는 실패했다.

판정 기준: **전이 귀속 29/41·맞바꿈 8 을 넘을 것.** 미니PC F1 은 부차 지표.

### 5.2 지표 재정의

미니PC 단독 F1 대신 **전이 귀속**으로 판정한다. 3.3 의 재정의와 맞고,
12.88 이 단독 개선의 길이 막혔음을 보였다.

### 5.3 운영점 교체 결정

`adapt_ze1+cnn_ze1` 이 전이 귀속에서 앞선다(29 vs 21). 다만 **운영 조합의
실행 간 폭을 한 번도 안 쟀다** — 시드만 바꾼 재실행 1회면 된다. 그 폭 안이면
교체 근거가 약하다.

### 5.4 run_live 기본값

지금 `results/adapt_ph1.pt` / `cnn_ov1.pt` 인데 **둘 다 가드에 막혀 못 돈다.**
운영점을 정하면 함께 고칠 것.

### 5.5 다음 측정 때 넣을 것

1. **펌웨어 2차 수정 후 재검증** — `MEASUREMENT_CHECKLIST.md` A-2/A-3.
   특히 A-3(헤어드라이어 r)이 이번에 불통과였다
2. **SMPS 3종이 함께 켜진 실측 녹화** — 그 조합이 나오는 파일이 적다
3. 프로젝터 소등 후 5분 이상 연속 (기존 항목, 미해결)
4. 핫플 활성화 5개 이상 (지금 4개)

---

## 6. 도구 — 이번에 추가된 것

```
run_fingerprint_spread_probe   지문 산포 (녹화 내부 / 녹화 간 / 실측 복합)
run_score_holdout              저장된 모델을 임의 홀드아웃에 채점
run_pair_ablation              쌍 판별의 채널 의존. --keep-only 로 충분성도
run_recipe_mix_probe           레시피 믹스 후보의 동시성·2차 배경 사전 측정
run_hmm_probe                  게이트에 지속성 사전, p_stay 훑기
run_gate_check --ckpt-smps     운영 조합 채점 (12.52 표를 재현한다)
run_gate_check --zero-even / --zero-ch      추론 시 채널 0
run_build_traincache --recipe-mix / --dither-even-amp
run_build_holdout --recipe-mix / --ablate-pedestal / --level-scramble
run_transition_probe --zero-even
```

### 고쳐진 것

- `build_fine` 이 짝수차 15채널을 0 으로 (12.77). 체크포인트 기록 + 로더 가드
- `L_harm` 에 `--harm-odd-only` (1·2단계 모두). **2단계가 실측에서 도니 그쪽이 더 중요**
- `file_registry` 에 `*_fixed` 녹화 5개 등록
- `real_events.json` 에 test_8(사람 라벨) / test_9(신호 유추) 추가
- **설계 문서 6.1 절 신설** — 이번 세션이 어긴 측정 규칙 12개

---

## 7. 실행 메모

- **CPU 오버클럭 해제 후 45분 파이프라인이 처음 완주했다.** 그 전 3회 크래시 중
  2회가 캐시 빌드 중(8워커 CPU 100%, 18GB 연속 쓰기)이었다. n=1
- **태그 충돌 주의** — `cnn_v2.pt` 가 8월 21일자로 이미 있었다. 새 태그 전에
  `ls results/*<tag>*` 로 확인할 것
- 배경 작업의 대기 조건을 문자열 grep 으로 걸 때 **그 문자열이 실제로 찍히는지**
  확인할 것. 요약 표가 태그를 잘라 40분을 헛기다린 적이 있다
- 무거운 작업 전에 커밋. 크래시로 캐시 20분을 잃었으나 코드는 무사했다
