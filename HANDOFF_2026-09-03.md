# 인수인계 2026-09-03 — 라벨을 다시 만들었다. 그리고 **드라이기 결함의 원인을 찾았다**

앞 문서: `HANDOFF_2026-09-02.md` (에어컨 유령 = 문턱 아래 누설, 스켈치+흡수).
이 문서는 그것과 **겹치지 않는다** — 이번 세션은 라벨·자료 무결성·장소 전이를 다뤘다.

설계 문서 **12.155 ~ 12.155.6**, 측정 규칙 **47·48**.

---

## 0. 한 줄

기존 실측 라벨의 `delta_p_w` 는 **그 기기 몫이 아니라 총전력 계단**이었다 (충전기
꺼짐에 `+1002.6W`). 12파일 238사건을 다시 만들었고, 그 과정에서 **장소 B 재적응
모델이 없는 오븐의 유령을 92% 지운다**는 것과 **드라이기 결함의 원인이 짝수차를
통째로 버린 것**임을 확정했다.

그리고 **12.114 의 반파 채널을 다시 시험해 미결을 닫았다** — 채널은 듣는다
(드라이기 강 101 -> 917W, F1 +0.126) 그런데 **포트를 판다** (F1 −0.212).
`--zero-channels` 라는 조인 대조를 만들어 12.114 를 무효로 만들었던 재학습 잡음을
구조적으로 없앴다.

---

## 1. 용어 — "집" 이라고 쓰지 말 것

이전 절들(12.148~12.151)에서 8파일을 잰 곳을 "집" 이라고 적었는데 근거가 없다.

```
장소 A   기존 test 파일들 + test_14        V_src,1 222.7   |Z₁| 2.04Ω
장소 B   test_15~18 + *_new 단독녹화       V_src,1 227.6   |Z₁| 0.87Ω
장소 C   test.csv, 그리고 에어컨·선풍기·
         드라이기1,2·충전기4 단독녹화        V_src,1 234.7   |Z₁| (계단 부족)
```

⚠ **단독녹화의 장소가 섞여 있다.** 에어컨·선풍기·드라이기1/2 는 장소 C 다.
`V_ref` 로 구별할 수 있다 (`run_switch_sig` 가 녹화별 `v_rms` 를 찍는다).

---

## 2. 라벨 — 무엇이 틀렸고 무엇을 고쳤나

### 2-1. 기존 라벨의 `delta_p_w` 는 총전력 계단이었다

```
기존 라벨 (test_5)                       정밀화 (총 / 기기몫)
168.5 laptop_charger off  ΔP +1002.6W    -57.4 / **-63.6**
321.0 minipc         off  ΔP +1007.2W      5.6 / **-6.8**
404.0 beam_projector on   ΔP  +360.8W    435.0 / **+42.8**
```

오븐 히터 듀티(±1,100W)가 그대로 들어갔다. test_5 에서만 17건 중 5건이 동작과
부호가 반대다.

### 2-2. 독립 검증 — 그 시각에 그 기기다운 계단이 있는가

```
출처      n    계단있음      물리적합     부호맞음
기존     75   56 (75%)    44 (59%)   52 (69%)
정밀화   72   72 (100%)   71 (99%)   72 (100%)
```

`test_8` 만 기존이 멀쩡한데 **그 파일에는 저항 부하가 없다.** 오븐·핫플이 있는
파일에서만 무너진 것이 원인을 확증한다.

### 2-3. 자료 무결성부터 깨져 있었다 (12.155.1)

```
test_14.csv     녹화 **두 개**가 이어 붙음 (seq 925 -> 0, host_time +362.8초)
                안 갈랐으면 타임라인이 통째로 453.5초 어긋난다
seq 시작        test_4 는 1511 부터. t=(seq-seq_lo)*0.5 를 안 쓰면 755초 어긋남
패킷 순서       원본 CSV 전 파일 3,226곳 뒤바뀜 (hotplate_1 210회, 최대 7.5초)
                npz 는 멀쩡하지만 **원본 CSV 를 직접 읽는 코드가 정렬을 안 했다**
```

`src/preprocessing/raw_csv.py` 로 정본 순서를 강제했고 5곳에 적용했다
(`harmonic_offset`, `run_fit_impedance`, `run_norton_probe`,
`run_resistive_vh_probe`, `deploy/check_site.py`).

효과: `harmonic_offset` 의 vrms 정렬 상관이 test.2 0.797 -> 0.972, test3 0.878 ->
0.986. **배분 오차는 6.7W 그대로**라 12.148/12.151 판정은 유효하다.
`norton_coef.npz` 는 정렬본으로 다시 적합했다 (옛 것은 `norton_coef_presort.npz`).

### 2-4. 사람 기록의 오류 3건 — 각각 두 개의 독립 근거

```
test_16  seq  726  "미니피씨 꺼짐"  신호 +11W u₃0.92  = **켜짐**
                   교차: 타임라인에 미니PC 켜짐 기록이 없다
test_18  seq 1254  "충전기 켜짐"    신호 -63W u₃0.87  = **꺼짐**
                   교차: 883 꺼짐 -> 1015 켜짐 -> 1254 켜짐 (켜짐 연속 두 번)
test_7   seq  385  "빔프 꺼짐"      **-43W 계단이 없다** -> 시각 미상, uncertain
                   교차: 프로젝터 꺼짐은 단독녹화에서 한 블록에 -43W 로 끝난다
```

### 2-5. 자의 핵심 — **h1 정규화는 중첩에서 깨진다** (규칙 47)

```
439.30s  P=558  |I₃|=0.304
439.40s  P=114  |I₃|=0.410   <- 핫플 듀티 off (P 사건)
439.50s  P=158  |I₃|=0.599   <- 충전기 on   (h3 사건, +0.25A)
```

이 계단은 `ΔP = −400W`(핫플)인데 `ΔI₃ = +0.24A`(충전기)다. `u₃ = ΔI₃/ΔI₁` 이
**분모와 분자가 다른 기기 것**이라 0.13 이라는 무의미한 값이 된다.

**저항은 h3 에 `|u₃| 0.004~0.030` 밖에 안 내므로 h3 축에는 오염원이 없다.**
SMPS·에어컨은 `ΔI_h/ΔI₃` 로 부류를 재고 `|ΔI₃|/u₃ × V` 로 기기 몫 전력을 되돌린다.
방향도 `|I₃|` 증감이 정한다 — 총전력 부호는 핫플 잡음(+5.6W)에 끌려간다.

이 하나로 test_5 가 15/17 -> **17/17** 이 됐다.

### 2-6. 산출

```
processed_data/real_events_refined.json    12파일 238사건
_label_provenance: human_switching_log_signal_refined
**원본 real_events.json 을 대체하지 않았다** — 비교하고 사람이 바꿔 끼울 것
```

구간의 독립 검증(구간이 말하는 SMPS 의 h3 합 vs 실제 `|I₃|` 상관): 기존 파일
0.87~0.94 (기존 라벨과 동률 이상), **라벨이 없던 신규 5파일 0.83~0.98.**

---

## 3. 장소 B 재적응 모델 — 12.150.2 의 처방이 실측으로 확인됐다

사용자가 `power_monitor` 저장소에서 장소 B 의 `Z` 로 재적응한 모델
(`adapt_zi_newsite.pt`)을 만들어 왔다. `results/` 로 복사해 두었다.

```
모델                   장소   F1     잔차W    유령W
Z·I (장소A) s0/s1/s2    A   0.837~0.846  9.1~11.4  4.1~4.6
Z·I (장소B 재적응)       A   0.841    7.05    3.64
Z·I (장소A) s0/s1/s2    B   0.735~0.749  93~100  72~77
Z·I (장소B 재적응)       B   0.685  105.88  **22.53**
```

### 유령의 정체는 **오븐** — 장소 B 에 없는 기기

```
모델                 오븐 유령   나머지    합
Z·I (장소A) s0         54.4     22.2   76.6
Z·I (장소B 재적응)    **4.1**    18.4   22.5
```

**92% 지운다.** 다른 집 라벨이 없어 못 쟀던 것이 이제 확인됐다.

### ⚠ 공짜가 아니다 — **포트를 같이 죽인다**

```
장소 B 기기별 F1     zi_s0   newsite      차
electiric_kettle    0.935    0.629   **-0.305**   <- F1 손실 전부가 이것
laptop_charger      0.862    0.909    +0.046
hotplate            0.433    0.449    +0.016
beam_projector      0.872    0.873    +0.001
```

장소 B 에서 포트(1377W)와 오븐 유령(~1140W)이 같은 저항 전력대라 하나를 누르면
다른 하나가 같이 눌린다. **드라이기 문제와 같은 뿌리다 — 저항끼리 구별이 안 된다.**

---

## 4. 드라이기 결함의 원인 (12.155.5) — **짝수차를 통째로 버린 대가**

> **사용자 관찰** — *"드라이기 켰는데 핫플, 포트, 오븐으로 잘못 읽거나. 약풍일 때
> 특히 심하고 강풍일 때도 결함이 상당히 많아."*

### 4-1. 잔차는 포트·드라이기가 켜진 창에서만 난다

```
파일       포트 ON   드라이기 ON   둘 다 OFF
test_15    410.0     307.5        12.9
test_18    242.9     246.0         8.4
test_14     94.3      86.6        10.2   <- 장소 A 인데도 같다
```

**장소 효과가 아니라 기기 효과다.** (잔차는 라벨과 무관한 양이라 라벨 탓도 아니다.)

### 4-2. 기전은 게이트 헤지 (12.9.13)

```
파일      P>600W  포트   드라이기  핫플   오븐   σ합   p_raw합  예측합  P_obs
test_5      445   0.00   0.00   0.73  0.80  1.54   3220   1307   1312
test_18     673   0.23   0.29   0.65  0.16  1.33   3083   1067   1208
```

드라이기가 **없는** test_5 는 게이트가 단호하고 잔차가 5W. 있으면 저항 넷이 전부
0.16~0.70 으로 어정쩡해지고 `σ·p_raw` 의 곱이 모자란다. `p_raw` 합은 어느 파일이나
3,100~3,240W 로 같다 — **용량이 아니라 결정의 문제다.**

### 4-3. 원인 — 반파 정류 신호를 입력에서 지웠다

드라이기 **약풍은 반파 정류**다 (12.109.2). 짝수차가 해석적으로
`|I_2k|/|I₁| = (4/π)/(4k²−1)` 이고 **h8 까지 실측과 10~20% 안에서 맞는다**:

```
차수    이론      드라이기 약   핫플     오븐    포트    충전기 66W
h2    0.4244     0.4142    0.0038  0.0018  0.0004   0.0294
h4    0.0849     0.1063    0.0083  0.0018  0.0005   0.0289
h6    0.0364     0.0321    0.0086  0.0018  0.0008   0.0282
h2/h4    5.0      **3.9**     0.5     1.0     0.8      1.0
```

**인공물은 모든 차수에서 평평하고(12.72) 반파는 h2 에서 h8 로 20배 감쇠한다.**
그런데 `ZERO_EVEN_HARMONICS`(12.77)가 통째로 0 으로 만든다. 약풍 465~518W 는
핫플 465~501W 와 전력도 홀수차도 같아서, **가려낼 유일한 특징을 못 본다.**

강풍(879~1010W)은 짝수차가 0.001 이라 모양으로도 안 갈리고 전력이 핫플과 오븐
사이에 낀다. **약풍은 특징을 지워서, 강풍은 특징이 없어서** 어렵다.

### 4-4. 처방 — 채널을 되살리지 말고 **모양 채널**을 붙인다  (-> 결과는 5절)

```
지우지 않는다   인덱스가 밀리면 fine[:, :38] 로 도는 옛 체크포인트가 전부 무효
되살리지 않는다 원시 Re/Im 14채널은 인공물이 맞다. 12.74 가 켰다가 충전기
                F1 0.937 -> 0.868 로 무너뜨렸다
붙인다          ch58 — 반파 정류 전류 (A)

gate = (2.5 < |I₂|/|I₄| < 9.0) & (|I₂|/|I₁| > 0.02)
ch58 = arcsinh(|I₂| · 3π/4 · gate · CURRENT_SCALE)
```

**크기가 아니라 비로 관문을 건다** — 인공물의 크기 변동(12.72.4: 같은 기기 녹화
사이 1.3~1.8배)에 면역이고, 충전기가 66W 에서 바닥의 4배가 돼도 평평해서 통과
못 한다. 통과한 값은 물리량이다.

검증 (`build_fine` 출력 p95): 드라이기 4.45 / 4.39, 핫플 0.017, 포트 0.014,
미니PC 0.178, 오븐·충전기·프로젝터 0.000. 통과한 `I_hw` 가 2.00~2.13A 로 약풍의
실제 전류 2.15A 를 복원한다.

---

## 5. 반파 채널 재시험 (12.155.6) — **12.114 의 미결을 닫았다**

### 5-1. ⚠ 오늘 같은 함정에 두 번 빠졌다 — 문서를 먼저 읽을 것

```
12.114.2  "비율 형태는 복합에서 죽는다"       -> 나는 |I₂|/|I₄| 관문을 다시 만들었다
12.114.6  "뒤에 붙여 59 로 두면 안 된다"      -> 나는 ch58 에 붙였다
          (12.80 의 반증된 8채널이 같이 되살아나 단일 변수가 깨진다)
```

그 실험(`hw58`/`hw59`)의 결과: 드라이기 강 귀속 919 -> **418W**, F1 0.906 -> **0.715**,
유령 28 -> **168W**. **비율 관문 형태는 반증됐다.**

### 5-2. 조인 대조 — 새 플래그 `--zero-channels`

12.114 가 판정을 못 한 이유는 대조가 `cnn_ovh`(다른 캐시·다른 실행)여서
**채널이 0 인 파일이 발화하는 파일보다 더 많이 변한** 것이었다. 채널 수를 바꾸면
첫 층 모양이 달라져 초기화 난수까지 바뀐다.

```
처방   --fine-channels 51
대조   --fine-channels 51 --zero-channels 50
```

구조·파라미터 수·초기화·자료 순서가 전부 같고 **ch50 의 값만 없어진다.**
1·2단계와 홀드아웃 평가 경로 전부에 적용했다 (`run_train_cnn`, `run_adapt`).
**절제는 자료에서 하고 구조에서 하지 않는다** — 규칙 48 로 올렸다.

### 5-3. 결과

```
장소A = 8파일(test_5~8·11~14) 평균, 장소B = test_15~18 평균, soft 판정

                       hwA(대조)   hwB(반파)      차
드라이기 강 귀속        101~180W  **917~921W**   참값 ~945W   ✅
드라이기 약 귀속        476~486W    479~483W    참값 ~490W   (동률)
드라이기 F1 (장소B)       0.776       0.902     +0.126      ✅
잔차 (장소A / B)      13.50/21.35  6.87/9.86   −6.6/−11.5  ✅
유령 (장소B)            101.72       80.40     −21.3       ✅
프로젝터 오차             −3.7        −0.3      개선        ✅
────────────────────────────────────────────────────────────
F1 (장소B)               0.814       0.779     −0.035      ❌
포트 F1 (장소B)           0.979       0.767     −0.212      ❌
합성 홀드아웃 MAE          3.44        4.19     +0.75       ❌
```

**뜻밖의 것** — 채널은 강풍에서 0.2% 만 발화하는데 고쳐지는 것은 **강풍**이다.
`test_18` 의 강풍 구간(677~1081초)은 약풍이 근처에 없는데도 100 -> 917W 다.
학습 중에 약풍 창이 드라이기를 모호함 없이 지목해 주니 모델이 드라이기의 다른
특징을 핫플·오븐과 섞지 않고 배운다. **값어치가 발화하는 창이 아니라 학습을
정리해 준 데 있다.**

### 5-4. 판정 — 조건부 채택 후보. 지금은 채택하지 않는다

사는 것: 드라이기(강 +817W, F1 +0.126), 잔차, 유령, 프로젝터.
파는 것: 포트 F1(−0.212), 합성 MAE(+0.75), 장소 B 총 F1(−0.035).

포트 손실이 12.155.4 의 장소 B 재적응(포트 0.935 -> 0.629)과 **같은 자리**다.
저항 4종에서 하나를 떼어 내면 남은 셋의 모호함이 몰린다 (규칙 29).

### 5-5. ⚠ 코드 상태

```
src/model/inputs.py   FINE_CHANNELS = **51**, HALFWAVE_CH = 50
                      (patches/12.114_halfwave_channel.patch 적용 상태)
  => 58채널 캐시(train60_ovh 등)를 **못 읽는다.** 12.115.8 이 겪은 그 문제다
  => 운영점 계보를 돌리려면: git checkout src/model/inputs.py

cache/train60_hw2   51채널 30만창, recipe smps_hi_fix (운영점과 동일)
cache/train60_hw    59채널 — 내 ch58 실험용. **8월 27일 51채널 것을 덮어썼다**
results/cnn_hwA,B / adapt_hwA,B    조인 대조 쌍 (A=대조, B=반파)
results/cnn_hw58,59 / adapt_hw58,59   ch58 비율관문 실험 (반증됨)
```

---

## 6. 남은 것

### [1] 포트 ↔ 오븐 구별 — **가장 값진 다음 수**

3절(재적응 포트 −0.305), 4절(헤지), 5절(반파 채널 포트 −0.212)이 **전부 같은 뿌리**다.
저항 4종에서 하나를 떼어 낼 때마다 남은 셋의 모호함이 몰린다. 근거가 셋 있다:

```
전력      포트 1377 vs 오븐 1141W — 20% 차이라 원리적으로 갈린다
듀티      오븐은 써모스탯으로 계속 껐다 켜지고 포트는 한 번 끓고 끝난다
제어보드   오븐은 히터가 꺼진 동안 14W SMPS 부하가 남는다 (|u₃| 0.06).
          포트에는 그런 상태가 없다.  <- 가장 직접적
```

### [2] 타임라인 없는 5파일

`test.2`, `test3`, `test_4`(전부 `ai_inferred_signal_checked`), `test_9`
(`signal_inferred`), `test_10`(라벨 없음). 신호만으로 사건을 내면 오븐·핫플 듀티가
그대로 들어와 수가 폭증한다 (test_4: 283건 vs 기존 2건). **사용자 스위칭과 듀티를
가르는 근거가 신호에 없다.**

⚠ `test_10` 의 타임라인은 **교체 전(레인지 버그) 파일의 것**이라 사용자가 지웠다.
   지금 `test_10.csv` 는 4분짜리 다른 녹화다.

### [3] `test_17` 핫플 2건 미검출 (seq 296, 770)

장소 B 의 다이얼 설정이 참조 준위에 없는 듯하다. 장소 B 핫플은 230~240W 로 도는데
`hotplate_4_new` 에는 500/63W 만 있다.

### [4] `L` 이 식별이 안 된다

```
장소 A 8파일 455µH   장소 A test_14 단독 177µH   장소 B 6파일 6µH
```

`V_src` 는 0.1% 안에서 재현되는데 `L` 은 안 된다. **모른다고 적어야 한다.**
`|Z₁|` 계단법(R² 0.994~0.996)만 쓸 만하다.

### [5] 중첩 분해 (미시도)

계단 벡터 `ΔI_h` 를 기기 지문들로 **분해**(작은 NNLS)하면 두 사건이 겹친 자리를
정면으로 풀 수 있다. 지금은 h3 정규화로 우회했다.

---

## 7. 새 도구

```
src/preprocessing/raw_csv.py       원본 CSV 를 정본 순서로 (세션 분할 포함)
src/run_check_raw_integrity.py     무결성 점검 (세션·손실·불완전패킷·시간역행)
src/run_preprocess_eval.py         복합 파일만 전처리. --session 지원. **풀 불변**
src/labeling/timeline_parser.py    사람 타임라인 파서 (200항목 전부 파싱)
src/run_switch_sig.py              전이 지문 사전 (27녹화 800전이)
src/run_refine_labels.py           검출(2Hz->60Hz) + 항목별 국소 배정
src/run_write_labels.py            real_events 형식으로 변환
src/run_dryer_probe.py             반파 채널 판정 (판정 기준 내장)
run_train_cnn/run_adapt --zero-channels   채널 하나의 조인 대조 (12.155.6)
src/run_plot_rescore.py            재채점 그림
src/run_subsample_cache.py         replay 캐시 솎기 (12.151.3)
src/run_plot_h1v.py                12.151 그림
```

## 8. 산출물

```
processed_data/real_events_refined.json   정밀화 라벨 12파일 238사건
processed_data/composite_eval/test_14*.npz, test_15~18.npz
results/switch_sig.json                   전이 지문
results/refined_all.json, refined_1113.json
results/raw_integrity.json, seq_time_map.json
results/z_site2.npz (장소 B), z_home_t14.npz
results/pc_ref.json, gc_ref.json          정밀화 라벨 채점
results/pc_orig.json, gc_orig.json        기존 라벨 채점 (대조)
results/adapt_zi_newsite.pt               사용자가 만든 장소 B 재적응 모델 (복사본)
results/plots/rescore.png, h1v.png
cache/train60_hw2                         51채널 30만창 smps_hi_fix (12.155.6 용)
cache/train60_hw                          59채널 (⚠ 8월 27일 것을 덮어썼다)
results/cnn_hwA,B.pt / adapt_hwA,B.pt     반파 조인 대조 쌍
results/pc_hw2.json, gc_hw2.json          그 쌍의 채점
```
