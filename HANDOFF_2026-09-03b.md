# 인수인계 2026-09-03b — 반파 채널을 지켰다. 대가는 없앴다

앞 문서: `HANDOFF_2026-09-03.md` (라벨 재작성, 드라이기 결함의 원인, 반파 채널 재시험).
설계 문서 **12.156 ~ 12.156.8**, 측정 규칙 **49**.

---

## 0. 한 줄

**사용자가 반파 채널(ch50) 유지를 결정했고, 그것을 지키면서 대가를 없앴다.**
그리고 **12.156.9 가 발견한 시드 다중해까지 사라졌다.**

3시드 중앙, 장소 B, 후처리 off:

```
                      드라이기    포트    핫플   미니PC  프로젝터  전체F1   잔차   유령W
hwA 반파X 처방X        0.600   0.956  0.599  0.208  0.927  0.773  29.57  217.9
hwB 반파만            0.624   0.490  0.600  0.209  0.922  0.717  13.04  180.1  <- 포트 재앙
hwK +L_res           0.627   0.982  0.603  0.274  0.931  0.793  15.68  102.7
**hwO +L_swap 2.0**  **0.942 0.994** 0.584  0.267 **0.941 0.851 14.66   6.6**
```

**시드 폭이 무너졌다** — 드라이기 0.326 -> **0.001**, 포트 0.157 -> **0.001**,
유령 271 -> **2W**. 12.155.6 이 "seed 0 의 운" 으로 얻었던 것을 이제 **처방으로
세 시드 전부에서** 낸다.

세 절이 순서대로 하나씩 벗겨 낸 것이다:
```
12.156  포트↔오븐은 L_harm 에서 전력 말고는 축퇴다  ->  L_res (V²/R 을 손실로)
12.157  σ 를 곱해 거는 항은 포화된 게이트에 안 닿는다  ->  규칙 51
12.158  그러니 로짓에 직접 건다 (resistive_match 를 감독으로)  ->  L_swap
```

**남은 것은 핫플 하나다** (0.603 -> 0.584). 6-8 절에 적었다.

---

# ★ 다음 수 — `gt_plugged` 를 "동작 중" 으로 재정의한다 (12.163.5)

**이것부터 하면 된다. 근거는 12.163 에 다 있다.**

```python
# src/synthesis/synthesizer.py  4. 대기 레이어
지금   gt_plugged[app][:] = 1                     # 꽂혀 있으면 항상 1
바꿔   gt_plugged[oven]  = activation 구간에만 1   # 세션 중에만 팬/조명이 돈다
```

**왜.** 오븐은 "안 켜진" 상태가 **둘**이다 — 미사용 `OFF_STANDBY` 0.4W 와
세션 중 히터 off 인 `FAN_LIGHT` **15.0W**. 손실의 `idle = σ(plugged)·(1−σ(on))`
이 구조적으로 FAN_LIGHT 자리인데, `gt_plugged` 가 항상 1 이라 **두 상태를 가를
신호가 없다.** 그래서 모델이 평균값 5~7W 로 수렴한다 (참값 15W).

12.163 에서 `standby_sig` 만 FAN_LIGHT 으로 바꿔 봤고 **반증됐다** —
오븐 standby 5.37 -> 6.89W 로 목표(12~16W)에 한참 못 미쳤다. 2단계 상수만으로는
안 닿는다는 것이 확인됐으므로 **1단계부터** 가야 한다.

`is_on` 은 안 건드리므로 `L_res`/`L_swap` 과 충돌하지 않는다 (오븐을 3상태로
만들면 그 항들이 FAN_LIGHT 창에 1,143W 를 강요한다 — 그래서 이 길을 고른다).

**비용 약 55분**
```
캐시 재생성   16분   python -m src.run_build_traincache ... (train60_hw2 는 300k창/958s)
1단계 학습    27분   run_train_cnn --fine-channels 51
2단계 적응     6분   3시드
채점           5분
```

**판정 기준** — `results/_criteria_hwX.md` 의 ①②를 그대로 쓰되 대조는 hwO 다.
```
① 오븐 standby (오븐 세션 중 히터 off & 조용한 창)   5.37W -> 12~16W   ← 인과
② 없는 기기 standby (장소 A 의 에어컨 1.80 / 선풍기 0.74) 줄어야
③ 지켜야: 드라이기 0.942 · 포트 0.994 · 전체 F1 0.851 · 유령 6.56W (장소 B)
          장소 A 전체 F1 0.813
```

⚠ **위험**: `gt_plugged` 가 0 이 되는 창에서는 `plugged` BCE 의 정답이 바뀐다.
   오븐 외 기기는 안 건드릴 것. 그리고 `y_standby` 는 이미 맞으므로 손대지 말 것.

---

## 1. 무엇이 틀렸다고 알려져 있었나

12.155.6 의 표는 **운영 조합(`+pp+rm0.02`)** 으로 잰 것이었다. `resistive_match`
후처리가 손실을 절반 고친 뒤의 숫자다. 끄고 재면:

```
장소 B, soft            hwA(대조)   hwB(반파)      차
포트 F1  후처리 켬        0.979      0.767     −0.212
포트 F1  **후처리 끔**    0.969      **0.403**  **−0.565**
드라이기 F1 후처리 끔      0.546      0.944     +0.398
```

후처리가 포트를 0.403 -> 0.767 로 되돌리면서 드라이기를 0.944 -> 0.902 로
깎고 있었다. **처방이 모델이면 후처리를 끄고 재라** (`power_ref` 독스트링이
이미 적은 규율인데 12.155.6 이 안 지켰다).

---

## 2. 원인 — 드라이기가 아니라 **포트가 없는 기기에게 먹힌다**

```
파일       잃은 창   포트 ΔP    오븐 ΔP    그 창의 반파 발화율
test_16     211    −1209.4   **+1155.7**      0.0%
test_17     186    −1211.9   **+1100.6**      0.0%
test_18     117    −1170.2   **+1040.9**      0.0%
```

**장소 B 에 오븐은 없다.** 그리고 그 창에서 반파 채널은 한 번도 발화하지 않는다.
채널이 직접 한 일이 아니라 원래 있던 축퇴를 건드린 것이다.

축퇴의 크기를 쟀다 — 1377W 포트와 1141W 오븐의 `L_harm` 판별 기여:

```
차수    h1      h2      h3      h4      h5      h6      h7      h8    합
기여  17.381  0.395  0.175  0.040  0.158  0.041  0.068  0.009  18.267
비중  97.6%   ─────────────── 모양 전부 합쳐 4.9% ───────────────
```

**h1 은 모양이 아니라 전력이다.** 두 기기는 크기를 빼면 손실 안에서 구별이
안 된다. 12.155.4 의 장소 B 재적응이 포트를 0.935 -> 0.629 로 떨어뜨린 것도
**같은 자리**다 — 세 번째 증거다.

⚠ 순시 고조파로는 못 가른다. 통전 중 `|I3|` 이 오븐 12.4/18.9/20.0mA 와 포트
29.0/17.3mA 로 **겹친다** — 녹화마다 다른 전압 왜곡 `V3` 가 판별 신호보다 크게
변한다. **모양으로 고치는 길은 없다.**

---

## 3. 처방 B (채택 후보) — `L_res`

`resistive_match`(12.112) 가 후처리에서 푸는 것을 학습으로 옮긴다.

```
G_예측 = Σ_{포트,오븐} σ(on_k)/R_k  +  (나머지 예측)/V²
G_관측 = (P_관측 − 대기 − 계측) / V²
L_res  = |G_예측 − G_관측| · V²        w_res = 0.1 (= w_cons. 튜닝 상수 없음)
```

포트 35.8Ω 과 오븐 40.6Ω 은 222V 에서 **163W** 벌어진다. 잃은 창에서 16조합
최적을 세면 포트가 93/88/93% 이고 **오븐은 상위 4위에 한 번도 없다.**
정보는 깨끗한데 모델에게 준 적이 없었다.

⚠ **포트·오븐에만 건다.** 핫플은 장소 B 에서 230~240W 로 돌고(참조 460W),
드라이기는 강 54.3Ω / 약 108.6Ω 라 상태마다 다르다 (규칙 14).

---

## 4. 판정 — ⚠ **시드 하나로 낸 것이라 절반을 철회했다**

기준은 결과보다 먼저 적었다: `results/_criteria_hwC.md` (규칙 22).
조인 대조 (규칙 48): `cnn_hwB.pt` 하나에서 적응만 갈랐다. 후처리 끔.

**처음에 seed 0 하나로 냈고, 사용자가 그것을 의심해서 시드를 늘렸다.**
아래는 시드 3개 중앙값이다 (드라이기는 6개).

```
장소 B (3시드 중앙)      hwA(대조)  hwB(반파)  **hwD(반파+L_res)**   판정
① 포트 F1               0.956     0.490     **0.911**        ✅ 세 시드 전부 개선
② 오븐 유령 W            217.9     180.1     **81.9**         ✅
⑥ 미니PC F1             0.208     0.417     **0.290~0.580**   ✅ 시드 전부 hwB 이상
   전체 F1              0.773     0.717     **0.786**         ✅
③ 드라이기 F1            0.600     0.624     **0.634**        ⚠ 개선 +0.010 뿐
④ 드라이기 강 귀속        ─         ─         ─               ❌ **철회** (seed 0 값)
⑦ 합성 홀드아웃 MAE       ─         6.57      7.28            ⚠ 판정 불가 (SD 1.06~1.57)
⑨ 잃은 창의 포트 회복      ─         ─      83~92% (seed 0)   ✅ 인과는 확인됨
```

`L_res` 의 포트 회복은 **세 시드 모두에서 양수**다 (+0.579 / +0.421 / +0.081).
같은 시드 안에서 짝지어 비교한 값이라 해가 갈리는 것과 처방의 효과가 갈린다.

## 5. ⚠ 사용자의 팬/조명 안은 물리가 맞았는데 반증됐다 — 규칙 49

오븐은 `OFF_STANDBY / FAN_LIGHT / HEATING` 세 상태인데 라벨이 FAN_LIGHT 을
`is_on=0, target_power_w=0` 으로 적는다. `standby_profile` 이 **0.40W/6.44mA**
인데 실제 FAN_LIGHT 은 **14.2W/64.3mA** — 41배·11배 과소이고, 오븐이 존재하는
시간의 52~73% 를 차지하는 상태다. 포트에는 그런 상태가 아예 없다 (격리에서
3~60W 표본 **0개**).

물리는 전부 맞았다:
```
HEATING 의 |I3| 중 팬/조명 몫       58% / 40% / 27%   (녹화 3개)
HEATING − FAN_LIGHT 의 |u3|        0.0010/0.0022/0.0028   <- 순수 니크롬
등가저항  전체 40.6Ω  ->  히터만 41.2Ω
상수 재현성  P 14.48/14.20/14.21W, |I1| 64.5/63.9/64.3mA   폭/중앙 **0.010**
복합에서도 보인다   test_11 히터 off 구간 16.9W / 86.9mA (격리 16.5W / 73.8mA)
```

그런데 `σ(plugged)·companion_sig` 로 붙이자 **정확히 반대로 갔다**:

```
σ(plugged_oven) 중앙 (장소 B — 오븐이 없으니 내려가야 한다)
파일        hwB     hwC(동반)   hwE(둘 다)
test_15    0.114     0.408      0.934
test_16    0.282     0.895      0.984
test_18    0.075     0.648      0.941
```

`plugged_logit` 은 2단계에서 **아무 손실도 감독하지 않는다.** 거기에 14.2W 를
주는 항을 붙이자 모델이 그것을 설명 안 되는 전력의 주머니로 썼다 — `L_cons` 가
그 경로를 즉시 찾는다. 포트는 절반만 움직이고(0.558), `L_res` 와 같이 쓰면
오히려 방해한다(hwE 0.544).

> **규칙 49 — 감독받지 않는 머리에 전력 경로를 붙이지 마라.** 물리가 맞아도
> 손실이 그 머리를 붙잡지 않으면 새 항은 제약이 아니라 자유도가 된다.

`--companion` 은 **대조로만** 쓸 것. 코드는 남겨 두었다.

---

## 6. ⚠ 시드 — 사용자의 의심에서 나온 가장 큰 발견 (12.156.9, 규칙 50)

사용자: *"제대로 돌린 거 맞아? 시드가 좀 바뀌었다고 F1 이 0.3 넘게 바뀐다고
믿을 수가 없는데."*

### 6-1. 먼저 실행이 정상인지 확인했다
`adapt_hwB` 는 이전 세션 것이고 그 뒤 손실 코드를 고쳤으므로 같은 시드로 다시
돌렸다. **소수점 끝까지 일치한다** (합성 MAE 8.198774142471425).

### 6-2. 잡음이 아니었다 — 다중해다
```
hwB (반파) 드라이기 F1 (장소B)
seed      0       1       2       3       4       5
       **0.944**  0.624   0.618   0.619   0.620   0.626
recall **0.997**  0.505   0.499   0.500   0.501   0.507
```
다섯이 0.618~0.626 에 모이고 하나만 튄다. 잡음이면 이렇게 안 모인다.

### 6-3. 강/약으로 가르니 답이 나왔다
```
                 약풍(반파) recall  게이트중앙  |  강풍(전파) recall  게이트중앙
hwB  seed 0         1.000        1.000    |     **0.995**      1.000
hwB  seed 1         0.997        1.000    |       0.405      **0.001**
hwB  seed 3         0.997        1.000    |       0.399      **0.001**
hwA  seed 0         0.980        1.000    |       0.316        0.000
hwA  seed 1         0.914        0.999    |       0.392        0.000
```
게이트 중앙이 **1.000 vs 0.001** 이다 — 문턱의 흔들림이 아니라 확신을 갖고
반대로 간다. 그리고 갈리는 것은 **강풍뿐**이다.

> **반파 채널이 실제로 사는 것은 약풍이다** (전 시드 0.997~1.000, 대조 0.914~0.980).
> **12.155.6 의 "강풍이 고쳐진다" 는 철회한다.**

### 6-4. 강풍 전력은 없는 기기로 간다 — 사용자가 보고한 증상 그대로
장소 B 드라이기 강풍 1,605창, 평균 귀속 (W):
```
기기                 hwB s0    hwB s1    hwD s1
드라이기 (참값 ~945)   825.5     276.9     288.1
오븐 (장소B에 없다)     314.6   **784.3**   391.7
포트                  52.9     130.8   **506.4**
합                  1351.8    1335.3    1321.5    <- 셋 다 같다
```
**크기는 맞는데 신원을 못 정한다.**

### 6-5. 기준 ⑦(합성 MAE)은 애초에 판정 불가였다
```
hwB  8.20 / 6.57 / 5.64   중앙 6.57  SD 1.06
hwD  9.50 / 7.28 / 5.67   중앙 7.28  SD 1.57
```
내가 "위반"이라고 적은 +1.30 이 SD 보다 작다. **그리고 처음에 적은 원인
("합성이 V²/R 을 안 따른다")도 측정 오류였다** — 창 전체의 V 중앙을 썼는데
타깃 시점 V 로 재면 0.979~1.003 이다. 12.156.7 에 정정을 적었다.

### 6-6. (A) 의 결론 — `RESISTIVE_OHM` 은 재적합할 필요가 없다
세 자가 전부 일치한다:
```
자 ① 격리 (통전 중 V·P 를 같은 표본에서)   비 0.992~1.007, 폭/중앙 0.002~0.012
자 ② 합성 홀드아웃 (타깃시점 V)             0.979 ~ 1.003
자 ③ 실측 복합 (계단 **직후** V)
     드라이기 강 0.997(n=27)  **약(반파) 0.984(n=7, 폭/중앙 0.008)**
     포트 1.041(n=25)  핫플 1.058(n=10)  오븐 1.109(n=5, 폭 0.119 — 못 정함)
```
⚠ 덤 — **반파로 가른 약풍의 R 이 다섯 기기 중 가장 안정적**이다(폭/중앙 0.008).
반파 채널의 분류가 옳다는 독립 증거다.

---

## 6-7. 다음 수 — **`L_res` 를 드라이기까지 확장한다**

강풍이 오븐·포트 사이에서 미끄러지는 것이 남은 문제다. `L_res` 에서 드라이기를
뺀 이유는 상태별 저항(강 54.3Ω / 약 108.6Ω) 때문인데, **반파 채널이 바로 그
상태를 알려준다**:

```
R_dryer(창) = 108.6Ω  if 반파 관문 통과  else  54.3Ω
```

6-6 의 자 ③ 이 이것을 지지한다 — 반파로 가르면 강 0.997, 약 0.984 로 둘 다 맞다.
**즉 반파 채널의 값어치가 입력이 아니라 `L_res` 의 스위치일 수 있다.** 안 해 봤다.

## 6-8. `L_swap` — 로짓 감독 (12.158). **채택 후보**

### 왜 이 형태인가
12.157 이 `L_res` 를 드라이기·핫플까지 넓혔는데 **드라이기가 0.001 도 안 움직였다.**
원인은 기울기다 — `L_res` 는 `σ(on)` 을 곱해 걸리는데 강풍 게이트가 0.0002 라
`dσ/dlogit = σ(1−σ)` 로 5,000배 죽는다. 같은 항이 포트(σ중앙 0.0344)에서는
Δ +0.563 이었다. **효과가 게이트로 완전히 예측된다** (규칙 51).

### 무엇을 했나
`σ` 를 안 거치고 로짓에 BCE 를 건다. 목표는 `resistive_match`(12.112) 처럼
컨덕턴스 조합을 세서 정한다 — **라벨이 필요 없다.**

두 번 막혔고 둘 다 잰 뒤에 풀었다:
```
① 개수 고정이 막았다 — 드라이기 강풍 정답 ON 창의 **44%가 저항이 하나도 안
   켜진 창**이라 `best==cur` 로 감독에서 빠진다. `--swap-slack 1` 로 풀었다.
   (12.112 의 "개수는 안 바꾼다" 는 **후처리의** 규율이다. 거기선 겨냥이
    맞바꿈이었고, 여기선 꺼진 것을 켜는 것이다.)
② 힘이 약했다 — `w_swap` 0.2 -> **2.0** 에서 게이트가 0.0002 -> **0.9994** 로
   뒤집힌다. `L_harm`(w=4.0)이 전 창에 걸리는데 `L_swap` 은 감독 창이 2~5%뿐이라
   0.2 로는 경쟁이 안 됐다. 10.0 은 2.0 보다 약간 나쁘다.
```
조준은 처음부터 정확했다 — tol 통과 창 417개 중 **412개**에서 `best` 가
드라이기를 켜라고 했다. 양과 힘만 모자랐다.

### 운영점 후보
```
--w-res 0.1 --res-apps electiric_kettle,oven,hair_dryer,hotplate
--w-swap 2.0 --swap-slack 1
```

### ⚠ 안 한 것
```
- 운영 조합(후처리 켬)으로 안 쟀다. `resistive_match` 후처리와 겹치므로
  **둘이 싸울 수 있다.** 후처리를 빼야 할 수도 있다 — 재 볼 것.
- 장소 A 를 안 쟀다. 저항 4종을 못 박은 효과가 오븐이 **있는** 곳에서 다를 수 있다.
- 합성 홀드아웃을 안 봤다.
- `swap_slack` 2 이상, `swap_tol` 0.05 를 안 훑었다.
- 1단계(cnn)는 안 건드렸다. 전부 2단계 적응만이다.
```

## 7. 남은 것 (앞 문서에서 이어짐)

12.155 의 [2] 타임라인 없는 5파일, [3] `test_17` 핫플 2건 미검출, [4] `L` 미식별,
[5] 중첩 분해는 **그대로 남아 있다.** [1] 포트↔오븐 구별은 이 문서가 닫았다.

새로 생긴 것:
```
- 기준 ⑦ (6절). 이것이 다음 수다.
- 장소 A 핫플 F1 0.699 -> 0.638 (−0.055). `L_res` 가 포트·오븐을 못 박으니
  남은 저항의 배분이 바뀐 것. 장소 A 유령도 5.5 -> 15.0W 로 늘었다 (절대량은 작다).
- 복합에서 오븐 히터 계단이 ~790W 인데 격리 HEATING−FAN_LIGHT 는 1120~1140W 다.
  **안 맞는다.** 다단 히터이거나 온도 설정 차이일 수 있다. 안 쟀다.
```

---

## 8. 산출물

```
results/adapt_hwC.pt / hwD.pt / hwE.pt      조인 대조 3개 (C=동반, D=L_res, E=둘)
results/gc_hw3_raw.json                     후처리 **끈** 채점 (판정의 본체)
results/gc_hw3.json                         운영 조합 채점
results/pc_hw3.json                         배분 오차
results/_criteria_hwC.md                    판정 기준 (결과보다 먼저 적었다)
results/companion_oven.npz                  팬/조명 상수 (반증됐지만 값은 맞다)
results/plots/halfwave_cost.png             원인 그림
results/plots/hw3_verdict.png               판정 그림
src/model/companion.py                      동반 부하 상수 (⚠ 반증됨)
```

⚠ **아직 커밋 안 했다.** `src/model/inputs.py` 의 반파 채널(FINE_CHANNELS=51)도
여전히 미커밋 상태다 — 사용자가 유지를 결정했으니 커밋해야 한다.
12.155 의 도구들(`raw_csv.py`, `timeline_parser.py`, `run_refine_labels.py` 등)도
미커밋이다.

---

## 9. 이번 세션 전체 지도 (12.156 ~ 12.163)

```
12.156  포트↔오븐은 L_harm 에서 전력 말고는 축퇴다 (판별의 97.6%가 h1)
        -> L_res (V²/R 을 손실로).  포트 0.490 -> 0.982
        ⚠ 동반 부하 항(사용자의 팬/조명 안)은 **반증** — 규칙 49
12.157  L_res 를 드라이기·핫플까지 넓혔는데 안 들었다
        -> 원인: σ 를 곱해 거는 항은 포화된 게이트에 안 닿는다.  규칙 51
           효과가 처방 전 게이트로 **완전히 예측**된다 (σ중앙 0.0344/0.0033/0.0001)
12.158  그래서 로짓에 직접 건다 — L_swap (resistive_match 를 BCE 감독으로)
        드라이기 0.624 -> 0.942, 포트 0.994, 유령 180 -> 6.6W
        **시드 다중해가 사라졌다** (드라이기 폭 0.326 -> 0.001)
12.159  미니PC 는 test_14~18 에서 IDLE 전용이라 참값 9.90W 를 안다 (사용자 정보)
        -> SMPS 배분 진단: 프로젝터 +35%, 미니PC −77%
           네 처방 중 w_pref(프로젝터와 함께)만 들었다
12.160  harm_offset(ZI 보정)은 장소 전이가 안 되고 L_swap 과 역할이 겹친다
        2×2: L_swap 없으면 끌 때 무너지고, 있으면 장소 A 에서 좋아진다.  규칙 52
12.161  장소 A/B 는 어려움이 정반대 (A=SMPS위주+오븐, B=저항위주+오븐없음)
        포트가 A 에서 나쁜 것은 오븐 축퇴(포트 창의 64.9%), 미니PC 가 B 에서
        나쁜 것은 자료량(5.7배)
12.162  저항 부하 F1 의 절반은 **라벨이 스위치를, 모델이 통전을** 재서다
        물리 마스크로 오븐 0.530 -> 0.867, 포트 0.769 -> 0.887
12.163  오븐 FAN_LIGHT — 합성은 맞았고 standby_sig 만 10배 어긋나 있었다
        고쳐 봤으나 **반증** (5.37 -> 6.89W). 다음 수는 gt_plugged 재정의
```

### 채택 후보 (운영점 아님 — 계보가 다르다)
```
--init results/cnn_hwB.pt --cache cache/train60_hw2 --harm-weight inv_h2
--w-cons 0.1 --w-harm 4.0 --w-hedge 0.2 --harm-offset results/norton_coef.npz
--w-res 0.1 --res-apps electiric_kettle,oven,hair_dryer,hotplate
--w-swap 2.0 --swap-slack 1                                    = adapt_hwO
```
장소 B (3시드 중앙, 후처리 off): 드라이기 0.942 · 포트 0.994 · 전체 F1 0.851 ·
잔차 14.66 · 유령 6.56W.  장소 A: 전체 F1 0.813 · 유령 4.29W.

⚠ **운영점(`adapt_zi_s0`) 계보에서는 아무것도 안 쟀다.** 그 모델은 ZI 보정 위에
세운 것이라 12.160 의 결론이 거기서도 성립하는지 모른다. 운영점 교체를 말하려면
그것부터 재야 한다.

### 안 한 것 (우선순위 순)
```
1. gt_plugged 재정의 (위 ★)
2. 운영점 계보에서 L_res/L_swap/보정 끄기를 다시 재기
3. w_pref 0.1 (프로젝터+미니PC) 를 3시드로 확정 — seed 1 에서 미니PC
   p_raw 0.64 -> 6.79, 프로젝터 45.5W(참 46.9) 였다
4. 물리 마스크를 채점에 넣을지 결정 (12.162.4 — 용도가 정한다)
5. 미니PC 자료량 가설의 직접 검증 (장소 A 창을 803개로 줄여 적응)
```
