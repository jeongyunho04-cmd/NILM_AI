# 전압 꼬리 back-fill 설계 (h17~h31) — 2026-09-09

2Hz 녹화는 전압 고조파를 **h15 까지만** 준다. 그런데 SMPS 전류의 h11~h15 를 지배하는 것은
**h17 위의 전압**이다. 원시 스냅샷 22개가 네 세션을 다 덮으므로, 그 꼬리를 세션마다 하나씩
뽑아 텍스처에 붙인다. **펌웨어 변경 없이** 지금 자료를 고치는 길이다.

---

## 1. 왜 필요한가 — 실측

`sim_wave` 에 전압을 h15 로 자르고 넣은 것과 전체를 넣은 것의 복소 상대오차
(충전기 원시 24구간, `|sim−meas|/|meas|` 중앙값).

```
                  자리 D                        자리 E
                h9    h11   h13   h15    |   h9    h11   h13   h15
절단 (지금)     0.142 0.320 0.510 0.751   |  0.122 0.172 0.318 0.517
자기 꼬리(상한) 0.069 0.103 0.112 0.108   |  0.045 0.067 0.111 0.133
```

그리고 이것이 **자리마다 전력 의존을 다르게** 만든다 — 저P/고P 모양비 h13:

```
              측정   전체전압   h15절단
자리 D        1.59    1.66      0.72     <- 절단하면 부호가 뒤집힌다
자리 E        1.00    1.13      0.66
vh17+ 총량    D 0.43% · E 0.17% (2.6배)
```

`s(p)` 곡선을 **회로모델**(h15 까지만 본다)에서 만들었으므로 이 차이를 못 만들고,
20W/61W h13 모양비가 1.61 로 나온다 — D1 실측 3.86, E1 실측 0.94 **어느 쪽도 아니다**.
그 결과가 합성 충전기 15~30W 고차의 실측 대비 0.59~0.71 배다.

## 2. 몇 차수까지 — h17~h23 최소, h17~h31 권장

```
전압 차수        D h11  D h13  D h15  | E h11  E h13  E h15  | 추가 float
h15 절단        0.329  0.499  0.802   | 0.201  0.353  0.573   |  +0
+h17            0.244  0.389  0.616   | 0.190  0.355  0.581   |  +2
+h17,19         0.142  0.358  0.768   | 0.139  0.255  0.405   |  +4
+h17~21         0.130  0.298  0.623   | 0.105  0.176  0.295   |  +6
+h17~23         0.093  0.115  0.189   | 0.081  0.111  0.109   |  +8   <- 최소
+h17~31         0.072  0.059  0.112   | 0.048  0.082  0.124   | +16   <- 포화
전체 h128       0.088  0.062  0.124   | 0.032  0.091  0.136   | +64
```

⚠ **부분 집합은 금물.** `+h17,19` 와 `+h17~21` 은 D h15 에서 절단보다 **나쁘다**
(전력 의존 2.51·3.08, 측정 1.15). h17~23 이 처음으로 전 구간 개선인 집합이다.

⚠ 제안이 말한 "자리 E 는 손해 없음" 은 **틀렸다.** E 도 h13 0.353 -> 0.111 (3.2배),
h15 0.573 -> 0.109 (5.3배) 좋아진다. 이건 D 전용이 아니라 어디서나 듣는다.

### 2.1 **홀수만.** 짝수는 정확히 0 이다

```
전압 꼬리 크기 (V1 대비)      홀수 h17~31   짝수 h16~30   짝수/홀수
자리 D                       0.5557%      0.0311%      5.6%
자리 E                       0.1701%      0.0127%      7.5%

하류 (복소 상대오차)          D h13  D h15  | E h13  E h15
h15 절단                     0.510  0.751  | 0.318  0.517
홀수만 h17~31                0.105  0.122  | 0.093  0.117
**짝수만 h16~30**            0.510  0.751  | 0.318  0.517   <- 절단과 셋째 자리까지 동일
홀수+짝수                    0.105  0.122  | 0.093  0.117   <- 홀수만과 동일
전체 h128                    0.069  0.103  | 0.111  0.133
```
계통 전압은 반파 대칭(`v(t+T/2) = −v(t)`)이라 짝수차가 원리적으로 없다. npz 규약에도
이미 적혀 있다 ("단일 입력 ADC 데이터라면 전압에 짝수 아티팩트가 없으니 대칭화 불필요").
**홀수 8개가 전체 h128 과 같다** — 포화 지점이다.

펌웨어 요구:  최소 h17~h23 (홀수 4개, +8 float) · 권장 h17~h31 (홀수 8개, +16 float).
제안의 "h32까지 짝수 포함 +32" 는 절반이 낭비다.

### 2.2 누적 — 120초는 **되고, 필요하다**. 문제는 시간이 아니라 방법이다

원시 22파일, 40주기(0.67초) 안에서 잰 것:
```
차수    결맞음도   1주기 SNR
h15     0.9985      12.9      <- 기준
h17     0.9833       3.4      <- 한 주기로는 못 쓴다
h19     0.9938       6.3
h21     0.9730       2.8      <- 한 주기로는 못 쓴다
h23     0.9982      11.9
h25~31  0.990~0.994  4.6~7.0
```
꼬리의 결맞음은 h15 와 같은데 **1주기 SNR 이 1/4** 다. 누적이 필수다.

120초 동안 위상이 도는지는 2Hz 녹화의 h7~h15 로 쟀다 (꼬리는 연속 자료가 없다):
```
결맞음도      1s     10s     60s    120s    300s
h7         1.000   1.000   0.997   0.996   0.990
h11        1.000   0.988   0.944   0.874   0.918
h13        1.000   0.998   0.989   0.987   0.984
h15        1.000   0.999   0.998   0.993   0.983
누적 이득 (120s)  h15 84.3 · h11 74.2   (이론 최대 sqrt(7200)=84.9 의 87~99%)
```

**절대 안 되는 것은 시간이 아니라 누적 방법이다.**
```
O  주기마다 z_h = V_h · exp(−j·h·arg V_1) / |V_1| 를 만들어 **복소로** 누적 -> /N
   (= 지금 npz 의 vhdeg 규약 그대로. 펌웨어가 h1~h15 에 이미 쓰는 것)
X  크기만 평균 -> 위상을 잃는다. 4절: 총량만 맞추고 위상 무작위면 **절단보다 나쁘다**
X  기본파 위상으로 되돌리지 않고 생 빈을 누적 -> 주기 시작 흔들림이 h 배로 돌아 상쇄
```
### 2.3 누적 길이는 **60초 = 모델 창**으로 (120초 아님)

```
              1주기 SNR   60초(3600주기)   120초(7200주기)
h21 (최악)       2.8          168             238
h17              3.4          204             289
결맞음도 h11               **0.944**          0.874
결맞음도 h15                 0.998            0.993
```
① 60초에서 이미 잡음이 **표류의 1/100** 이다 (잡음 0.006 대 세션 안 표류 0.29~0.59).
   더 모아도 얻을 것이 없다.
② 120초는 오히려 결맞음이 나빠진다 — 60->120 의 실제 이득이 h11 에서 1.31배로
   이론값 sqrt(2)=1.41 에 못 미친다. 표류가 먹기 시작하는 구간이다.
③ **창과 맞아야 한다.** 모델 창이 3600주기 = 60.0초다.
   · 냉시동: 첫 예측이 t=60초인데 꼬리도 60초면 같은 순간에 준비된다. 120초면 없다
   · 정합성: 120초 평균은 창 밖 60초가 섞인다. 꼬리가 분 단위로 도니 그게 오차다
   · 50초가 아니라 **60초** — 창과 정확히 같아야 "이 창의 꼬리" 라는 뜻이 분명하다

대역폭: 60초에 16 float = 0.27 float/초. 창마다 보내도 무시할 수준이다.

우리 back-fill 은 어차피 **세션 단위 꼬리 하나**를 붙이므로(4절) 평균값이 순간값보다
쓰임에 맞다 — 다만 그건 지금 자료의 사정이고, 펌웨어는 창 단위로 주는 것이 옳다.

## 3. 꼬리는 얼마나 안정한가 — **초 단위로만**

```
파일 안 (10주기 토막, 0.17초 간격)   0.02 ~ 0.13   <- 잡음 바닥
세션 안, 파일 사이 (분 단위)          0.29 ~ 0.59
세션 사이                          0.56 ~ 1.27
```

세션 D1 안의 산포(0.592)가 D1<->D2 사이(0.556)와 **같다.** 즉 꼬리는 세션 고정이 아니다.
⚠ 제안이 말한 "같은 세션 스냅샷쌍 0.07~0.11" 은 우리 측정에서는 **파일 안** 값이다.

**총량은 다르다 — 그것은 자리의 성질이다.**
```
D1 0.429%   D2 0.427%   E1 0.165%   E2 0.169%
```

## 4. 그래서 무엇을 붙이나 — **자리(또는 세션)별 복소 중앙 꼬리**

갈래를 하류 오차로 재 봤다 (충전기 원시 24구간).

```
꼬리 갈래              D h11  D h13  D h15  | E h11  E h13  E h15
절단 (지금)            0.320  0.510  0.751  | 0.172  0.318  0.517
**자리 중앙 꼬리**      0.121  0.210  0.301  | 0.050  0.052  0.082
자기 꼬리 (상한)        0.103  0.112  0.108  | 0.067  0.111  0.133
남의 세션 꼬리          0.307  0.605  0.958  | 0.270  0.559  0.867
총량만 + 무작위 위상     0.520  0.908  1.258  | 0.155  0.286  0.487
```

  · 자리 E 는 **사실상 해결**된다 (0.318 -> 0.052, 상한보다도 낮다)
  · 자리 D 는 가용분의 **59%** 를 회수한다 (0.510 -> 0.210, 상한 0.112)
  · ⚠ **위상이 핵심**이다. 총량만 맞추고 위상을 무작위로 주면 **절단보다 나쁘다**
  · ⚠ **자리를 틀리면 절단보다 나쁘다**. 귀속이 정확해야 한다

**결정: 세션별 복소 중앙 꼬리.** 자리별보다 세션별이 낫다 (D1/D2 는 총량이 같고 조성이
다르다, 거리 0.556). 세션 안 표본 추출은 선택지로 남긴다 (D 에서 0.210 -> 상한 0.112 쪽으로).

## 5. 자료 — 원시 22파일의 세션 귀속

`vrms + vh3 + vh5 + vh7` 최근접으로 붙였다 (⚠ `abs(rfft[1])/(N/2)/sqrt(2)` 가 RMS 다 —
√2 를 빼먹으면 전부 E 로 무너진다).

```
D1  n=9   laptop_charger_1~4 · minipc_1~5        h17+ 0.429%
D2  n=2   beam_projector_3,4                     h17+ 0.427%
E1  n=6   beam_projector_1,2 · laptop_charger_5~8 h17+ 0.165%
E2  n=5   minipc_6~10                            h17+ 0.169%
```
네 세션이 다 덮인다. **D2 는 파일이 2개뿐**이라 가장 약하다 — D1 과 총량이 같으니
D 자리 중앙으로 대체하는 것도 선택지다.

## 5.1 기존 텍스처와 **같은 꼴**로 간다 — 다른 것 둘

```
                 h1~h15 텍스처                  h17~h31 꼬리
출처             2Hz npz                        **원시 CSV** -> 별도 빌더
알갱이           0.5초 블록마다 (231개)           **파일당 1개 · 세션당 1개** (4개)
귀속             file_registry                  같음 (vrms+vh3+vh5+vh7 최근접)
개방 벗기기       V_open = V_term + Z(h)·I        **같은 식**
짝수차           없음                            없음 (0 효과 확인)
```

세션 하나에 꼬리 하나가 붙고, 그 세션의 텍스처 231개가 **전부 같은 꼬리**를 쓴다.

**개방 벗기기는 꼬리에도 필요하다.** 원시 스냅샷은 그 자신의 부하가 물린 상태다.
강하 `|Z_h·I_h|` 를 꼬리 `|V_h|` 로 나눈 값:
```
        h17    h19    h21    h23    h25    h27    h29    h31
자리 D  0.069  0.031  0.119  0.022  0.133  0.147  0.098  0.104
자리 E  0.226  0.214  0.289  0.081  0.094  0.056  0.065  0.061
```
2~29% 다 — 무시할 크기가 아니고, 원시에 전류가 있으니 공짜로 벗길 수 있다.
`rel`/`rel_open` 과 똑같이 `tail`/`tail_open` 두 벌을 두고 `source_tail()` 이 개방을 준다.
Z 를 모르는 세션(E2)은 기존 폴백대로 단자 꼬리를 쓴다.

⚠ `DEEMBED_L_H = 225e-6` 은 **측정값이 아니다** (주석: "꼬리 자료로는 정해지지 않아
생성기 `x_grid_range` 중앙을 쓴다"). h15 에서는 `|jwL|` 0.57Ω 대 R 0.42~1.15Ω 로 R 이
지배했지만 **h31 에서는 2.63Ω 으로 L 이 지배한다.** 벗기는 양이 꼬리의 10% 안팎이라
L 이 2배 틀려도 꼬리의 ±3% 지만, 기록해 둔다.

## 6. 코드 변경 — 좁다

출력 전류는 **h15 그대로**다. 바뀌는 것은 **입력 전압의 길이**뿐이다.

```
① src/synthesis/vtexture.py
     TAIL_ORDERS = (17,19,21,23,25,27,29,31)
     Texture 에 `tail` / `tail_open` (8,) complex, V_h/V_1, h배 위상 관례
     Texture.source_tail() -> 개방 있으면 그것 (source_rel() 과 같은 규약)
     VoltageTextureLibrary 가 `processed_data/vtail.npz` 를 읽어 세션마다 붙인다
     Texture.rel_full() -> (32,) 스펙트럼 (h0..h31, 짝수 0)

② circuit_model/circuit12.py  wave_from_harmonics
     X[1:16] = ...  ->  가변 길이 (len(V) 만큼)
     ⚠ sim_harmonics 의 반환은 nh=15 로 그대로 둔다

③ src/synthesis/fcm.py  DeviceModel12.current
     X = to_spectrum(V); V15 = X[1:H+1]   ->  꼬리까지 넘긴다

④ src/synthesis/coupling.py
     SmpsCircuit.current / texture_delta / coupling_delta 가 긴 rel 을 받는다
     ⚠ 고정점(_solve_vterm)은 h1~h15 그대로. 꼬리는 **강하 없이 통과**시킨다
       (h17+ 전류가 mA 라 Z·I 가 1~2mV = 기본파의 0.001%. 무시해도 된다)
     캐시 키는 env_id/rec_id 라 그대로 — 꼬리가 세션에 매이므로 충돌 없음

⑤ src/run_build_vtail.py  (신규)
     원시 22파일 -> 세션 귀속 -> 세션별 복소 중앙 꼬리 -> processed_data/vtail.npz
```

**모델 입력 채널은 안 건드린다.** 이번 back-fill 은 **생성기 전용**이다 —
npz 에 h17+ 가 없으므로 실측 창에는 줄 수 없고, 주면 학습/추론이 어긋난다.
(모델이 그 축을 보게 하려면 펌웨어가 h17~h23 을 보내야 한다. 그건 별건이다.)

## 7. 검정 — 순서대로

```
① 단위 시험   꼬리 None 이면 지금과 **비트 단위로 같다** (되돌림 없음 확인)
② 원시 24구간 위 4절 표를 재현한다 (자리 중앙 꼬리 D h13 0.510 -> 0.210)
③ run_mixval12  [B] 경로. 생성기 변경의 정본 판정기다
④ s(p) 재생성   run_build_sp_curves 를 꼬리 켠 회로모델로 다시 -> 20W/61W h13 이
                1.61 에서 실측(D 3.86 / E 0.94) 쪽으로 갈라져야 한다
⑤ 캐시 + 1단계 + 2단계 3시드  <- 여기가 HPC 가 필요한 지점
```

⚠ ④ 가 이 설계의 **진짜 값**이다. 꼬리를 넣으면 회로모델이 자리별 전력 의존을 만들 수
있게 되고, 그러면 s(p) 곡선이 자리마다 갈린다. 지금 충전기 문제(합성 고차 0.59~0.71배,
2단계에서 충전기가 64.5W 를 먹는 것)의 뿌리가 거기다.

## 8. 위험

```
· D2 가 파일 2개뿐이다 -> 세션 중앙이 약하다. D 자리 중앙으로 대체 가능
· 꼬리가 분 단위로 돌므로 세션 중앙은 **평균**이지 그 창의 값이 아니다
  (자리 D 잔여 0.210 대 상한 0.112 의 정체가 이것이다)
· h1~h15 는 2Hz 녹화, 꼬리는 원시 스냅샷 — **서로 다른 시각**의 것을 붙인다.
  같은 세션이라는 것 말고는 보장이 없다
· 전 캐시·체크포인트가 무효가 된다 (생성기가 바뀌므로). 전면 재훈련 필요
· 새 장소에는 **전이되지 않는다.** 그 장소의 꼬리를 모르기 때문이다 —
  일반화하려면 펌웨어가 h17~h23 을 보내는 수밖에 없다
```
