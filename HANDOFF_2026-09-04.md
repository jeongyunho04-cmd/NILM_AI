# 인수인계 2026-09-04 — 운영점을 `adapt_ac_s0` 로 교체했다. 그리고 **두 문제의 원인이 하나로 모였다**

앞 문서: `HANDOFF_2026-09-03b.md`.
설계 **12.164 ~ 12.180**, 측정 규칙 **53 ~ 71**.

---

## 0. 한 줄

**장소 B 의 전압 고조파(vh9~vh13 2~4배, vh15 1/7)가 같은 SMPS 의 h9~h15 전류
위상을 100° 넘게 돌려 놓는다. 합성은 격리 녹화 장소(= 장소 A)의 환경만 알고,
모델의 미니PC `p_raw` 머리와 드라이기 게이트는 정확히 그 차수를 읽는다.**
미니PC 과소예측(12.179)과 에어컨 유령·시드 다중해(12.180)가 **둘 다** 여기서 나왔다.
실측 입력을 장소 전달비 `T_B` 로 나누면 1단계가 미니PC p_raw 3.0 -> 10.5W, 드라이기
강풍 0.12 -> 1.000, 유령 창의 에어컨 0.44 -> 0.00 을 낸다. 합성에 `T_B` 의 **위상만**
곱하면 합성에서도 똑같이 무너진다.

---

# ★ 지금 자리 — 운영점 선택은 **교환**이고 사용자 판단이다 (12.183.3). 다음 수는 (라)

(나) 60°/0.5 h3 부터 (12.182) 와 (나′) h5 부터 (12.183) 를 둘 다 실행했다.
```
                   장소A F1   장소B F1   s0 유령(test_15)   시드 일치   합성 미니PC
adapt_ac_s0 (현)   0.930     0.915     33.75W 에어컨      ✗           0.950
adapt_ph5_s*       0.903     0.903     1.1W               ✓ 셋 같음    0.895
```
**유령·시드는 장소 보정 없이 풀린다** (위상 지터가 h5 이상만 덮어도 세 시드가 388~414s 에서
드라 1.000 / AC 0.000). **대가는 장소 A −0.03** 이고 h3 를 지켜도 안 돌아온다 — 12.36 의 최상위
SMPS 판별자 `|I9|/|I3|` 가 h9 에 살고, 장소 B 가 돌리는 것도 h9 다 (T9 0.71/+66°). **판별자와
장소 취약 특징이 같은 차수에 산다.** 미니PC 신원(h11~h15, 기기별 장소 의존)은 어느 판에서도
안 생겼다 — 충전기를 따라가는 흡수기 (정밀도 0.41~0.64).

**기준 파일로는 둘 다 채택 아님.** 시연 장소가 B 형이고 "없는 에어컨 558W" 가 더 나쁜 결함이면
`adapt_ph5_s0` 이 낫다 — 리허설에서 보고 정할 것. 세 `.pt` 다 남아 있다.

**다음 수 (라) — GPU 없이 시작한다**: 장소 불변 판별자를 찾는다. 12.36 의 탐욕 전진 선택을
"장소 B 에서도 같은 값인가" 를 조건으로 다시 돌린다 (후보: h3·h5 크기/위상 — T3 0.97/+12°,
T5 0.89/+20°; PF; k = h1 위상; s(p) 의 전력 눈금). 살아남는 것이 있으면 그것만 입력에 남기거나
그 밖을 흔든다. 없으면 SMPS 신원은 장소별 30초 녹화(기기별 T, 12.181.4)로 가야 한다.

(다) 지터를 낮추는 재시도(h5 부터 30°/0.25)는 수확 체감이 예상돼 뒤로 미룬다.

---

# (나)/(나′) 레시피 — 실행 완료, 기록용 (12.182, 12.183)

```
캐시   python -m src.run_build_traincache --out cache/train60_ph5 --windows 300000 --recipe-mix smps_hi_fix \
         --dither-amp 0.5 --dither-phase-deg 60 --dither-min-order 5 --sp-curves      (23분)  [ph 는 min-order 없이]
1단계   ... --cache cache/train60_ph5 --holdout processed_data/holdout60_ac ... --tag cnn_ph5   (30분)
2단계   adapt_ac 레시피, --init results/cnn_ph5.pt --cache cache/train60_ph5, 3시드               (7분)
```
`--dither-min-order` 는 `DataAugmentor(harmonic_dither_min_order=)` 로 들어가고 meta.json 에 남는다.
홀드아웃은 지터를 안 걸므로 holdout60_ac 를 그대로 쓴다 (④ 가 같은 자).

# (나) 원래 계획 — 기록용 (12.179.7 의 (나), 12.181.5)

⚠ (가) 장소 전달비 입력 보정은 **실행했고 채택 불가** (12.181). 유령·시드는 사라졌지만
(세 시드가 388~414초에서 드라 1.000 / AC 0.000), 미니PC 는 "켜졌다" 가 아니라
"충전기+프로젝터가 있으면 3~14W 를 흡수한다" 가 됐고 포트 B·장소 A 가 내려갔다.
이유: **h13~h15 의 전달비가 기기마다 다르고 페이저 합은 인수분해되지 않는다** — 총전류에
거는 단일 보정으로는 못 되돌린다. 장소별 30초 녹화로도 못 푼다.
코드(`--site-transfer`, 체크포인트의 `site_transfer`)는 남겨 뒀다 — `adapt_site_s*.pt` 가 그것을 담고 있다.
⚠ `L_pref` 도 원인 치료가 아니다 (12.179.6).

**(나) 의 레시피** — 캐시의 차수별 지터에 **위상**을 켠다. 활성화당 1회 뽑으므로 창 안의
기기마다 다른 회전이 들어간다 (= 12.181.4 의 "기기별 T"). `σ_k = ph·k/9`:
```
캐시   python -m src.run_build_traincache --out cache/train60_ph --recipe-mix smps_hi_fix \
         --dither-amp 0.5 --dither-phase-deg 60 --sp-curves            (약 20분)
       ph=60 -> h3 20° / h5 33° / h7 47° / h9 60° / h11 73° / h13 87° / h15 100°
       amp=0.5 -> h9 σ 0.5, h15 σ 0.83 (장소 B |T| 0.7~3.2 = e^−0.34 ~ e^1.15 를 덮는다)
홀드아웃  python -m src.run_build_holdout --out processed_data/holdout60_ph (캐시와 같은 지터·--sp-curves, 규칙 63)
1단계   python -m src.run_train_cnn --cache cache/train60_ph --holdout processed_data/holdout60_ph \
         --epochs 300 --epoch-windows 50000 --batch 512 --w-over 0 --eval-every 10 \
         --harm-odd-only --standby-operating session --fine-channels 52 --tag cnn_ph       (27분; cnn_ac 의 config 그대로)
2단계   adapt_ac 레시피 그대로 (--init results/cnn_ph.pt --cache cache/train60_ph), 3시드 9분. **--site-transfer 없이.**
```
**판정 기준 (실행 전에 results/_criteria_ph.md 로 옮길 것)**
```
① 미니PC IDLE 전력 1.31 -> 4W 이상 **그리고** test_16/17 미니PC 유령 < 1W (흡수가 아니라 검출)
② 388~414s 세 시드 드라 > 0.9 / AC < 0.1 (12.181.2 가 h2~h9 만으로도 됨을 보였다 — 위상 지터가 그 자리를 덮는지)
③ 지킬 것: 장소A 전체F1 0.925~0.931 · 장소B 드라이기 0.93 · 포트 B 0.955~0.985 · 장소B 전체 0.89~0.915
④ 대가 측정: 합성 홀드아웃 SMPS 3종 F1 (cnn_ac 0.9706 대비) — φ3 판별력을 얼마나 잃는가
```
⚠ 위험: ph=60 은 h3 에도 20° 를 준다. φ3 는 12.34 가 프로젝터↔충전기 판별자로 넣은 것이라
(간격 27~44°) 합성 판별이 내려갈 수 있다. 내려가면 지터를 h5 부터 걸도록 증강에 최소 차수를
추가한다 (`_apply_harmonic_dither` 에 `k >= k_min` 마스크 — 코드 5줄).

---

## 1. 운영점 — **`adapt_ph5_s0`** (2026-09-04 오후, 사용자 결정 — 12.183.3 의 교환)

`src/run_live.py` 의 `--ckpt` 기본값: `adapt_ac_s0` -> **`adapt_ph5_s0`**. 장소 A 0.03 을
내주고 "포트+드라이기 강풍이 겹치면 없는 에어컨 558W" 를 없앴다. 시드 셋이 같은 답을 낸다.

**배포 묶음(`../power_monitor`)도 맞췄다** — `deploy/models/adapt_ph5_s0.pt`, `app.py` 의
`CKPT_PREFERENCE` (adapt_ph5_site > adapt_ph5_s0 > 옛 것), `deploy/nilm_runtime/inputs.py` 를
**52채널 판으로 교체**(옛 58채널 판은 ch50~57 이 홀수차 크기 블록이라 ph5 의 ch50/51 과
다르다 — `.backup_models/inputs_58ch_2026-08-26.py` 에 보관), `state_definitions.py`/
`file_registry.py` 동기화, `predictor` 에 `site_transfer` 체크포인트 거부 가드, `site_kit.py adapt`
의 레시피를 `cnn_ph5` + `cache/train60_ph5_30k`(만들어 둠) + adapt_ph5 레시피 + `--harm-offset-z`
로 교체 (학습 저장소 경로 D: -> C: 의 이 저장소), 문서 3종. 옛 `adapt_zi_*` 체크포인트는
앞 50채널이 그대로라 여전히 돈다 (재생으로 확인).

### 1.0 이전 운영점 `adapt_ac_s0` (아침 교체분, 기록용)

`src/run_live.py` 의 `--ckpt` 기본값: `adapt_zi_s0` -> `adapt_ac_s0`

```
같은 자 (병합 채점, --postproc off, 3시드 중앙)
                  장소A F1  유령W  |  장소B F1   잔차W    유령W
adapt_zi_s0 (구)    0.920   3.96  |   0.714   **146.1**  **133.3**
adapt_hwO          0.903   4.16  |   0.908      14.2       6.3
**adapt_ac_s0**    0.930   1.27  |   **0.915**  15.3      10.7
```
구 운영점은 **장소 B 가 생기기 전에 적응한 모델**이라 새 장소에서 안 돈다.

**이 모델의 계보**
```
1단계  cnn_ac.pt      cache/train60_ac (300k, smps_hi_fix, dither 0.2, --sp-curves)
       --fine-channels 52 --standby-operating session --harm-odd-only
       홀드아웃 processed_data/holdout60_ac (같은 설정으로 만든 것)
2단계  adapt_ac_s0.pt --w-res 0.1 --w-swap 2.0 --harm-offset norton_coef.npz
                      --smps-boost 4.0 --lam 0.5 --w-cons 0.1 --w-harm 4.0
```

### ⚠ 시드 표 — 12.176.2 를 정정한다 (규칙 69)

어제 표의 장소 A 약점(s1 포트 0.502, s2 프로젝터 0.769)은 `run_site_table` 이 **없는
기기의 오탐을 F1 0.0 으로 접은** 인공물이었다 (test_5 포트 오탐 **2창 = 1초**). 고쳤다.
```
수정된 자        장소A F1  포트A  프로A  |  장소B F1  프로B   미니PC B  장소B 유령W
 s0 (운영점)      0.930   0.949  0.921  |   0.915   0.948   0.664    10.67  (7.7 = test_15 에어컨)
 s1               0.925   0.920  0.920  |   0.897   0.879   0.685     1.03
 s2               0.931   0.931  0.919  |   0.890   0.871   0.619     2.51
```
**장소 A 는 세 시드가 같다.** 남는 시드 차는 장소 B 프로젝터와 유령뿐이다.

`s0` 의 유령은 `test_15` **388~414초 26초** 한 구간이다 — 포트+드라이기 강풍 겹침
(장소 B 에서 유일)에서 드라이기 930W 를 **에어컨 558W** + 잔돈으로 흩는다. 1단계가
그 창에서 드라이기 게이트 0.001 / 에어컨 0.44 로 시작시켰고, 500스텝 더 돌려도
안 움직이는 국소 최소다 (12.180.3). **원인은 12.179 와 같고, ★ 의 ③ 이 그것을
확인한다.**

```
시연에서 "없는 에어컨" 이 보이면   -> 그 순간은 포트+드라이기 강풍이 같이 켜진 때다.
   results/adapt_ac_s1.pt 로 바꾸면 사라진다 (장소B 검출 0.915 -> 0.897)
```
⚠ 세 `.pt` 를 그대로 보관한다. **★ 가 되면 시드를 고를 이유가 없어져야 한다** —
(가) 에서 실제로 그렇게 됐다 (12.181.1, 세 시드 동일). 다만 (가) 는 채택 불가라 운영점은 그대로다.

**`adapt_site_s0/s1/s2.pt` (12.181)** — 장소 B 유령 2.2~2.9W 로 깨끗하고 시드가 같지만
미니PC 를 흡수기로 쓰고(정밀도 0.36~0.39) 포트 B 0.92, 장소 A −0.01~0.02 다. 운영점 아님.
⚠ 이 체크포인트들은 `site_transfer` 를 담고 있어 `run_live` 가 자동으로 장소 B 보정을 건다.
쓰지 마라 — 쓰려면 `--no-site-transfer` 나 그 장소에서 새로 잰 `--site-transfer`.

---

## 2. 이번 세션이 바꾼 것 (12.164 ~ 12.180)

```
12.164  gt_plugged[oven] 을 "동작 세션 중"으로. 오븐 FAN_LIGHT standby
        9.25 -> 15.56W (참 14.2). 그런데 장소 B 붕괴 -> L_impl 두 판 반증
12.165  **핫플은 문제가 아니었다 — 채점이었다.** SESSION_MERGE_CYCLES 에 핫플 추가.
        핫플 F1 0.601 -> **0.908**
12.166  s(p) 부하 의존 서명 + 상시 배경 흡수 (외부 자료를 검증 후 이식)
12.167  선로 임피던스 실측. 장소 A 1.470Ω / 장소 B 0.907Ω. **장소 B 무리 추가**
12.168  셋을 묶어 실행 -> 장소별로 부호가 갈림 (귀속 불가, 규칙 54 위반)
12.169  홀드아웃을 짝 맞춰 재생성. **"1단계 악화"는 분포 어긋남이었다.**
12.170  미니PC 원인 1차: 검출은 되는데 p_raw 가 0 이다. 배분 실패
12.171  판별자는 차분에 있고 손실은 절대값을 본다
12.172  바닥을 4.9배 줄여도 안 움직인다 -> L_harm 은 애초에 구속 안 했다
12.173  합성/실측 간극: SMPS전용 창의 h3/h1. 12.166 의 배경은 이중 계상
12.174  간극의 정체는 에어컨 대기 하나. 상한 0.10 (PLUG_PROBABILITY_CAP)
12.175  실행. 인과는 실패(미니PC 1.31W)지만 세션 최고 성적
12.176  운영점 교체 (사용자 결정)
12.177  미니PC 는 적응 전부터다. 합성에서는 완벽하다 (게이트 1.000 / p_raw 11.31)
12.178  정정 — 동반 조합과 무관하다 (2종도 똑같이 실패)
**12.179  미니PC 원인: 장소 B 의 h9~h15 위상. 채널 교환 -> 전달비 T_h -> 전압 고조파 -> 인과 닫음**
**12.180  에어컨 유령·시드: 1단계가 장소 B 드라이기 강풍을 못 본다. 뿌리는 12.179.
        "시드별 약점" 은 접기 인공물 -> run_site_table 수정**
**12.181  (가) 실행. 유령·시드 ✅, 미니PC ❌ (흡수), 포트B·장소A ❌ -> 채택 불가.
        단일 전달비는 h13~h15 에서 기기별이라 다중 SMPS 창을 못 되돌린다. 다음은 (나)**
**12.182  (나) 실행 (위상 지터 60°/0.5, h3 부터). 유령·시드 ✅ 장소 보정 없이. 장소A −0.02~0.03,
        합성 미니PC −0.078, 실측 미니PC 흡수기 -> 문턱 초과. h5 부터 다시 (--dither-min-order)**
**12.183  (나′) h5 부터. 합성 미니PC −0.055 로 절반 회복, 장소A −0.03 은 그대로 — 판별자(|I9|/|I3|)와
        장소 취약 특징이 같은 차수(h9). 운영점 선택은 교환 -> 사용자 판단. 다음 (라) 장소 불변 판별자**
```

## 3. 이번에 만든 자 (전부 `python -m src.<이름>`)

```
run_site_transfer_probe   **장소 전달비 T_h** (단일 SMPS 창 실측/합성 비). --apply CKPT 로
                          실측을 1/T 로 보정하고 합성에 T 를 곱해 인과를 닫는다.  <- 12.179
run_channel_swap_probe    실측/합성 채널군 교환 — 모델이 **어디를 읽는지** (d′ 와 다르다)  <- 규칙 70
run_ghost_window_probe    유령이 **어느 창**에 있는지 10초 타임라인 + 구간 기기별 게이트/p_raw
                          + --harm 으로 그 구간의 L_harm 비교  <- 12.180
run_site_table            gate_check JSON -> 장소별 표. **없는 기기의 F1 을 이제 안 접는다**
run_oven_idle_probe       오븐 FAN_LIGHT standby (test_4)
run_minipc_idle_probe     미니PC IDLE 전력 (test_15/18, 참값 9.90W)
run_minipc_company_probe  미니PC 예측을 동반 SMPS 로 가른다 (저항 없는 창만)
run_minipc_step_probe     계단에서 그 12W 가 누구에게 가는가
run_step_attribution_probe 계단을 순방향 모형으로 직접 푼다
run_harm_landscape_probe  손실이 그 배분을 절대 창에서 가릴 수 있는가
run_synth_gap_probe       합성 h3/h1 이 실측에 붙는가 — GPU 없이 판정한다
run_line_impedance        계단에서 선로 임피던스 (60Hz 만 — 전달비는 못 본다, 규칙 71)
run_delta_thd_probe       이벤트 델타 서명의 THD
run_degeneracy_probe      저항 조합의 컨덕턴스 축퇴 목록
run_sp_verify             s(p) 곡선 주장의 독립 검증
```

## 4. 안 한 것 / 열린 것

```
0. ★ (라) 장소 불변 판별자 (위). (나)/(나′) 는 끝났다 — 12.182/12.183
0′. 배포: NILM_AI_monitor 저장소(= ../power_monitor)의 **옛 이력(LFS 모델 ~300MB)** 을 고아
    브랜치로 비울지 사용자 결정 대기. 파일은 지웠고 이력만 남았다 (클론이 무겁다).
0″. 작업 트리의 READ_ME_FIRST.txt / SMPS_PLAN_2026-08-31.md 삭제 두 건 — 내가 한 것이 아니라
    커밋에서 뺐다. 지울 것이면 `git rm`, 살릴 것이면 `git checkout --`.
0‴. 12.179 의 부수 발견 — 풀이 녹화 환경을 섞고 있다 (laptop_charger_4_fixed / hair_dryer_1 은
    234~237V, vh3 9~10V 의 세 번째 환경). 아직 안 다뤘다.
1. (나) 1단계 위상 지터 — 실행 완료, 운영점 adapt_ph5_s0 (1절)
2. (가) 는 끝났다 — 채택 불가 (12.181). 코드와 adapt_site_s* 만 남는다
3. 풀이 녹화 환경을 섞고 있다 — laptop_charger_4_fixed / hair_dryer_1 은 234~237V,
   vh3 9~10V 의 **세 번째 환경** (12.179.4)
4. 차분 항 L_step (12.171.4 의 C) — 12.179 뒤로는 우선순위가 내려갔다
5. 12.166 의 s(p) 를 L_harm 의 순방향 모형에도 (12.166.3)
6. 프로젝터 F1 장소B 0.948/0.879/0.871 시드 차 — (가) 에서는 0.92 로 같아졌다. (나) 뒤에 다시 잰다
7. 재적응이 드라강풍+핫플 창에서 포트 0.31 로 헤지한 것 (12.181.4) — 보정 자체는 그 창을
   고치는데(cnn_ac 0.30 -> 0.005) 2단계가 새 해를 찾았다. 2단계 항의 문제일 수 있다
```

## 5. 판정 기준 파일 (전부 결과 **전에** 적었다)

```
results/_criteria_hwY.md       gt_plugged 재정의
results/_criteria_hwY2x2.md    그 붕괴의 원인 가르기
results/_criteria_impl.md      L_impl 두 판
results/_criteria_h2.md        ch51 |I2| 채널
results/_criteria_tb.md        L_swap 동점깨기 두 판
results/_criteria_harmmask.md  고조파 마스크 A·B
results/_criteria_sp.md        s(p)+배경+장소B 무리
results/_criteria_ac.md        에어컨 상한  <- 채택된 것
results/_criteria_site.md      (가) 장소 전달비 입력 보정  <- 실행했고 ②④ 실패로 채택 불가
results/_criteria_ph.md        (나) 위상 지터            <- 실행했고 ② 통과, ③④ 문턱 초과. (나′) 도 같은 기준
```

## 6. 이번에 얻은 규칙 (53~71)

```
53 라벨의 뜻을 바꿨으면 그 라벨을 쓰는 항이 몇 개인지 세라
54 한 번에 셋을 바꿨으면 재설계 전에 하나씩 걷어내라
55 감시자는 고장이 난 곳에 놓아라
56 처방이 안 들면 그 항이 그 창에 닿기는 하는지 먼저 세라
57 지표가 안 움직이면 라벨의 granularity 를 먼저 의심하라
58 문턱을 넓히기 전에 그 문턱 안의 축퇴를 세라 (상태별 지문으로)
59 잔차로 판별하려면 빼는 항의 오차부터 재라
60 남이 준 처방은 우리 코드에 그 전제가 성립하는지부터 확인하라
61 실측 근거가 있는 값은 더하되 지우지 마라
62 "표본이 작다" 는 경고는 상대 비교까지 먹는다
63 합성 설정을 바꿨으면 홀드아웃도 같이 다시 만들어라
64 판별이 되는 자와 손실이 보는 자가 같은지 확인하라
65 항의 값이 크다고 그 항이 구속하는 것은 아니다
66 빠진 성분을 더하기 전에 그 자리에 이미 무엇이 있는지 세라
67 기본값을 고쳤으면 그 기본값이 실제로 쓰이는지 확인하라
68 조건을 나눌 때 나누지 않은 축이 섞여 있는지 확인하라
**69 없는 기기의 F1 을 접지 마라. 유령은 창 단위로 봐라**
**70 간극은 "어디가 다른가" 와 "모델이 어디를 읽는가" 를 따로 재라. 크기가 아니라 위상일 수 있다**
**71 장소가 바뀌면 전달비부터 재라 (단일 SMPS 창 1분)**
**72 보정은 그것을 쓸 창(다중 SMPS)에서 잔여를 재고 나서 돌려라**
**73 특징을 흔들기 전에 그 차수에 사는 판별자 목록(12.36)을 확인하라**
```

## 7. 채점할 때 주의

```
--session-merge 를 켜라. 오븐과 핫플의 라벨이 세션 단위라 안 켜면
  오븐 0.63 / 핫플 0.60 으로 나온다 (실제 0.93 / 0.91).
--postproc off 로 재라. 처방이 모델 안에 있으면 후처리가 그것을 가린다.
1단계는 **짝 맞춘 홀드아웃**으로. holdout60_ze1 은 오븐 라벨이 낡았다.
장소를 갈라 봐라. 장소 A(SMPS 위주+오븐)와 B(저항 위주)는 어려움이 정반대다.
run_site_table 은 오늘부터 없는 기기를 F1 에서 뺀다 — 옛 표와 포트A/프로A 가 다르다.
유령 W 가 크면 run_ghost_window_probe 로 어느 창인지부터 찍어라.
```
