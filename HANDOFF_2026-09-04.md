# 인수인계 2026-09-04 — 운영점을 `adapt_ac_s0` 로 교체했다

앞 문서: `HANDOFF_2026-09-03b.md`.
설계 **12.164 ~ 12.175**, 측정 규칙 **53 ~ 67**.

---

## 0. 한 줄

**합성이 에어컨을 85% 확률로 꽂고 있었고, 그 대기 전류 하나가 모든 창의
고조파 모양을 망가뜨리고 있었다.** 실측 16파일 전부에서 에어컨은 한 번도
안 켜졌고 꽂혀 있지도 않았다. 상한을 0.10 으로 내리자 합성이 실측 분포에
붙었고, 장소 B 잔차가 146 -> 15W 가 됐다.

---

# ★ 다음 수 — `L_pref` 를 켠다 (12.170.6)

⚠ 한때 "레시피의 SMPS 3종 동시 비율" 을 ★ 로 적었다가 **철회했다** (12.178).
2종일 때도 똑같이 실패한다 — 동반 조합과 무관하다.

**2단계만, 3시드 9분. 지금 유일하게 안 켜 본 지렛대다.**

```bash
python -m src.run_adapt --init results/cnn_ac.pt --cache cache/train60_ac \
  --steps 1000 --batch 256 --lr 3e-5 --lam 0.5 \
  --w-cons 0.1 --w-harm 4.0 --w-hedge 0.2 --harm-weight inv_h2 \
  --harm-offset results/norton_coef.npz \
  --w-res 0.1 --res-apps electiric_kettle,oven,hair_dryer,hotplate \
  --w-swap 2.0 --swap-tol 0.02 --swap-slack 1 \
  --w-pref 0.1 --pref-apps beam_projector,minipc \
  --smps-boost 4.0 --real-stride 30 --eval-every 250 \
  --seed 0 --tag adapt_pref_s0 --out results
```

**왜.** 12.171~12.172 가 확정했다 — **2단계에 SMPS 3종의 전력 배분을 구속하는
항이 하나도 없다.** `L_cons` 는 총합만, `L_res`/`L_swap` 은 저항만, `L_hedge` 는
게이트만이다. `L_harm` 은 값의 38%가 짝수차·56%가 h11~h15 인 바닥이라
기울기가 무의미하다 (끊어 봤더니 ±0.02 안에서 아무것도 안 변했다).

`L_pref` 는 `|p_raw − ref| · σ(on).detach()` 로 **`p_raw` 에 직접 걸고 게이트는
마스크로만 쓴다** — 지금 실패의 형태(게이트 0.97 인데 p_raw 1.3W)와 정확히 짝이다.
그리고 12.159 가 미니PC 용 창별 마스크(9.90W, test_14~18)를 이미 만들어 놨다.
**한 번도 안 켜 봤다** (`w_pref` 기본 0.0).

**판정 기준**
```
① 미니PC IDLE 전력 (test_18)  1.31W -> **4W 이상**   (참값 ~12W)
② 프로젝터 F1  장소A 0.844 / 장소B 0.879 -> 향상 (power_ref 46.9W 가 잡을 자리)
③ 지킬 것  장소A 전체F1 0.930 · 포트 0.949 · 핫플 0.991 · 유령 1.27W
           장소B 전체F1 0.915 · 드라이기 0.928 · 포트 0.985
```

⚠ **위험**: `L_pref` 는 `test_14~18` 창에만 걸린다 (적응 창의 약 30%). 그리고
프로젝터의 46.9W 는 전역이라 장소 B 실제 동작점(45.6~46.8W)과 1~3% 어긋난다.

**그 다음**은 차분 항 `L_step` 이다 (12.171.4 의 C). 판별 신호가 계단 차분에
8~18% 마진으로 있는데 손실은 절대 창만 본다. 근본이지만 설계가 필요하다.

---

## 1. 운영점 — **교체했다**

`src/run_live.py` 의 `--ckpt` 기본값: `adapt_zi_s0` -> **`adapt_ac_s0`**

```
같은 자 (병합 채점, --postproc off, 3시드 중앙)
                  장소A F1  유령W  |  장소B F1   잔차W    유령W
adapt_zi_s0 (구)    0.920   3.96  |   0.714   **146.1**  **133.3**
adapt_hwO          0.903   4.16  |   0.908      14.2       6.3
**adapt_ac_s0**    0.930   1.27  |   **0.915**  15.3      10.7
```
구 운영점은 **장소 B 가 생기기 전에 적응한 모델**이라 새 장소에서 안 돈다.

**이 모델의 계보**
```
1단계  cnn_ac.pt      cache/train60_ac (300k, smps_hi_fix, dither 0.2, --sp-curves)
       --fine-channels 52 --standby-operating session --harm-odd-only
       홀드아웃 processed_data/holdout60_ac (같은 설정으로 만든 것)
2단계  adapt_ac_s0.pt --w-res 0.1 --w-swap 2.0 --harm-offset norton_coef.npz
                      --smps-boost 4.0 --lam 0.5 --w-cons 0.1 --w-harm 4.0
```

### ⚠ 시드마다 실패 자리가 다르다 — 셋 다 남겨 뒀다 (규칙 50)

```
      장소B F1 (test_15/16/17/18)         장소B 유령W            장소A 약점
 s0   0.938 0.943 0.859 0.908  (최고)   **test_15 33.75**      —
                                        (에어컨 30.96, 나머지 1.4~7.0)
 s1   0.915 0.929 0.821 0.901           0.57~1.96  (최저)    **포트 F1 0.502**
 s2   0.910 0.928 0.807 0.893           1.40~4.58            프로젝터 0.769
                                                             미니PC 0.799
```
**`s0` 은 검출이 가장 좋은 대신 유령이 가장 크다.** 그 유령은 `test_15` 한
파일에 몰려 있고 내용은 **없는 에어컨 30.96W** 다. 사용자 판단으로 일단
`s0` 을 운영점으로 뒀다 — 시연 리허설에서 보고 정하기로 했다.

```
시연에서 "없는 에어컨이 31W" 가 보이면   -> results/adapt_ac_s1.pt 로 교체
   유령은 0.57~1.96W 로 깨끗해지고 장소B 검출이 0.912 -> 0.891 로 진다
   ⚠ 장소A 포트가 0.502 로 무너진다 (시연이 장소 B 면 무관)
```
⚠ **재학습으로는 못 고른다.** `adapt_ac_s0/s1/s2.pt` 세 파일을 그대로 써야 한다.
다중해의 원인은 아직 모른다 (열린 것 5번).

---

## 2. 이번 세션이 바꾼 것 (12.164 ~ 12.175)

```
12.164  gt_plugged[oven] 을 "동작 세션 중"으로. 오븐 FAN_LIGHT standby
        9.25 -> 15.56W (참 14.2). 그런데 장소 B 붕괴 -> L_impl 두 판 반증
12.165  **핫플은 문제가 아니었다 — 채점이었다.** refined 라벨이 통전 단위를
        세션 단위로 되돌려 놨다. SESSION_MERGE_CYCLES 에 핫플 추가.
        핫플 F1 0.601 -> **0.908**. 포트 오탐은 L_swap 허용오차 안의 축퇴다
12.166  s(p) 부하 의존 서명 + 상시 배경 흡수 (외부 자료를 검증 후 이식)
12.167  선로 임피던스 실측. 장소 A 1.470Ω / 장소 B 0.907Ω. **장소 B 무리 추가**
12.168  셋을 묶어 실행 -> 장소별로 부호가 갈림 (귀속 불가, 규칙 54 위반)
12.169  홀드아웃을 짝 맞춰 재생성. **"1단계 악화"는 분포 어긋남이었다.**
        그리고 holdout60_ze1 의 **오븐 라벨이 낡았다** (FAN_LIGHT 을 on 으로)
12.170  미니PC 원인: 검출은 되는데(게이트 0.97) p_raw 가 0 이다. 배분 실패
12.171  근본: **판별자는 차분에 있고 손실은 절대값을 본다.** 오차 바닥 1.767
        대 신호 2% — 부호까지 뒤집힌다
12.172  바닥을 4.9배 줄여도 안 움직인다 -> **L_harm 은 애초에 구속 안 했다**
12.173  합성/실측 간극: SMPS전용 창의 h3/h1 이 실측 0.83~0.90 vs 합성 0.57~0.86.
        ⚠ 12.166 의 배경은 **이중 계상**이었다
12.174  **간극의 정체는 에어컨 대기 하나** (전체 바닥 전류의 65%). 상한 0.10.
        ⚠ DEFAULT_PLUG_PROBABILITY 만 고치면 안 닿는다 -> PLUG_PROBABILITY_CAP
12.175  실행. ① 인과는 3연속 실패(미니PC 1.31W)지만 **세션 최고 성적**
```

## 3. 이번에 만든 자 (전부 `python -m src.<이름>`)

```
run_site_table            gate_check JSON -> 장소별 표 (병합 채점과 함께 쓴다)
run_oven_idle_probe       오븐 FAN_LIGHT standby (test_4)
run_minipc_idle_probe     미니PC IDLE 전력 (test_15/18, 참값 9.90W)
run_minipc_step_probe     계단에서 그 12W 가 누구에게 가는가
run_step_attribution_probe 계단을 순방향 모형으로 직접 푼다
run_harm_landscape_probe  손실이 그 배분을 절대 창에서 가릴 수 있는가
run_synth_gap_probe       **합성 h3/h1 이 실측에 붙는가 — GPU 없이 판정한다**
run_line_impedance        계단에서 선로 임피던스 (새 데이터마다 돌릴 것)
run_delta_thd_probe       이벤트 델타 서명의 THD
run_degeneracy_probe      저항 조합의 컨덕턴스 축퇴 목록
run_sp_verify             s(p) 곡선 주장의 독립 검증
```

**`run_synth_gap_probe` 가 이번 세션 최대 수확이다** — 캐시만 보고 처방을
판정한다. 12.174 는 그것으로 1분 만에 확인하고 60분을 썼다.

## 4. 안 한 것 / 열린 것

```
1. ★ L_pref (위)                                     9분
2. 차분 항 L_step (12.171.4 의 C)                     설계 필요
3. 배경을 **바꾸는** 형태 (12.173.4 의 E1)             지금은 --background off
   무리별 배경 코드(12.168.3)는 있으나 안 씀
4. 12.166 의 s(p) 를 L_harm 의 순방향 모형에도 (12.166.3)
   지금은 증강의 전력 스케일에만 쓴다
5. 시드 다중해 (장소A 포트 0.949/0.502/0.931) — 원인 미상
6. 프로젝터 F1 하락 (장소A 0.881->0.844, 장소B 0.948->0.879)
7. 12.167 의 장소 B 무리 / 12.166 의 sp_curves 개별 기여 미분리
```

## 5. 판정 기준 파일 (전부 결과 **전에** 적었다)

```
results/_criteria_hwY.md       gt_plugged 재정의
results/_criteria_hwY2x2.md    그 붕괴의 원인 가르기
results/_criteria_impl.md      L_impl 두 판
results/_criteria_h2.md        ch51 |I2| 채널
results/_criteria_tb.md        L_swap 동점깨기 두 판
results/_criteria_harmmask.md  고조파 마스크 A·B
results/_criteria_sp.md        s(p)+배경+장소B 무리
results/_criteria_ac.md        에어컨 상한  <- 채택된 것
```

## 6. 이번에 얻은 규칙 (53~67)

```
53 라벨의 뜻을 바꿨으면 그 라벨을 쓰는 항이 몇 개인지 세라
54 한 번에 셋을 바꿨으면 재설계 전에 하나씩 걷어내라
55 감시자는 고장이 난 곳에 놓아라
56 처방이 안 들면 그 항이 그 창에 닿기는 하는지 먼저 세라
57 지표가 안 움직이면 라벨의 granularity 를 먼저 의심하라
58 문턱을 넓히기 전에 그 문턱 안의 축퇴를 세라 (상태별 지문으로)
59 잔차로 판별하려면 빼는 항의 오차부터 재라
60 남이 준 처방은 우리 코드에 그 전제가 성립하는지부터 확인하라
61 실측 근거가 있는 값은 더하되 지우지 마라
62 "표본이 작다" 는 경고는 상대 비교까지 먹는다
63 합성 설정을 바꿨으면 홀드아웃도 같이 다시 만들어라
64 판별이 되는 자와 손실이 보는 자가 같은지 확인하라
65 항의 값이 크다고 그 항이 구속하는 것은 아니다
66 빠진 성분을 더하기 전에 그 자리에 이미 무엇이 있는지 세라
67 기본값을 고쳤으면 그 기본값이 실제로 쓰이는지 확인하라
```

## 7. 채점할 때 주의

```
--session-merge 를 켜라. 오븐과 핫플의 라벨이 세션 단위라 안 켜면
  오븐 0.63 / 핫플 0.60 으로 나온다 (실제 0.93 / 0.91).
--postproc off 로 재라. 처방이 모델 안에 있으면 후처리가 그것을 가린다.
1단계는 **짝 맞춘 홀드아웃**으로. holdout60_ze1 은 오븐 라벨이 낡았다.
장소를 갈라 봐라. 장소 A(SMPS 위주+오븐)와 B(저항 위주)는 어려움이 정반대다.
```
