# 측정 체크리스트 — 펌웨어 수정 후 (2026-08-24)

`MODEL_TRAINING_DESIGN.md` 12.72 가 확정한 계측 버그를 고친 뒤의 측정 계획이다.
**A 를 먼저 하고 결과를 확인한 다음 B~C 로 넘어갈 것** — A 가 실패하면 나머지
녹화가 전부 헛수고가 된다.

---

## A. 펌웨어 수정 검증 (가장 먼저, 30분)

수정이 실제로 먹었는지부터 확인한다. **셋 다 필요하다.**

### A-1. 무부하 녹화 (2분 이상)

두 증폭 경로의 오프셋이 개별 보정됐는지 본다.

- 기대: `|ΔI2|` 잡음 바닥이 수정 전 **0.013~0.036 mA (중앙)** 수준을 유지하거나 낮아진다
- 이건 원래도 작았으므로 **회귀가 없는지 확인하는 용도**다

### A-2. 충전기 단독 재녹화 (5분 이상) ← **핵심 검증**

수정 전 같은 조건의 값과 직접 비교한다.

```
수정 전 laptop_charger_1      |I2|/|I1| = 0.0288   (2,4,6,8,10차가 전부 0.029 로 평평)
수정 전 laptop_charger_2      |I2|/|I1| = 0.0285
기대                          짝수차가 크게 내려가고, 차수별로 평평하지 않게 된다
```

- **판정: `|I2|/|I1|` 이 0.01 아래로 내려가면 성공.** 0.029 근처면 수정이 안 먹은 것
- 함께 볼 것: **홀수차는 변하면 안 된다** (`|I3|/|I1| ≈ 0.93`, `|I5|/|I1| ≈ 0.81`).
  홀수차가 같이 움직이면 보정이 과했거나 다른 것을 건드린 것이다

### A-3. 헤어드라이어 단독 재녹화 (2분 이상) ← **가장 민감한 시험**

수정 전 `r(|I2|, |I1|) = −0.993` 이 나온 녹화다. 순저항이라 짝수차가 0 이어야 정상인데
레인지 전환 오차가 그것을 만들었다.

- **판정: `r(|I2|, |I1|)` 의 절대값이 0.5 아래로 떨어지면 성공.**
  −0.99 가 그대로면 단차가 남아 있다
- 가능하면 **열 단수를 바꿔 가며** 녹화할 것 (전류가 여러 값을 가져야 상관을 잰다)

> **⚠ 수정 전후 비교가 가능하려면 조건을 맞춰야 한다.** 같은 기기, 같은 콘센트,
> 가능하면 같은 시간대. 전압이 크게 다르면 비교가 흐려진다.

---

## B. 기존 결손 보완 (인수인계 4.5절, 우선순위 순)

### B-1. 저항 부하 없는 복합 녹화 2~3개 ← **1순위**

지금 `test_7` 하나뿐이라 SMPS 실패의 표본이 전이 13개다. 이번 세션의 판정이
전부 여기 걸려 있었다 (전이 귀속 n=41 중 test_7 이 12~14개).

- 구성: 프로젝터 + 충전기 + 미니PC 만. 오븐·핫플·포트는 끈다
- 각 10분 이상, ON/OFF 를 여러 번 (전이가 많아야 한다)
- **충전기는 충전 상태를 바꿔 가며** — 배터리 잔량이 다른 상태를 각각 잡을 것

### B-2. 프로젝터 소등 후 5분 이상 연속 녹화

기존 녹화가 소등 뒤 6.3초 / 10.4초 만에 끊겨 **팬 기착의 두 번째 계단(5.4→2.3W)
뒤가 없다** (12.60). 끄고 나서 최소 5분은 계속 돌릴 것.

### B-3. 전기포트가 켜지는 복합 녹화

비봉인 6파일에 하나도 없다 (12.41.3). 12.43 지속시간 규칙의 오기각 비용을 못 쟀다.

### B-4. 오븐 활성화 5개 이상 · 핫플 5개 이상

지금 4개/3개라 조합이 12가지가 천장이고, 그것이 1단계 시드 분산 21.78W 의 근원이다
(12.39.7). 각각 별개 세션으로.

### B-5. 미니PC 작업 상태 비율 올리기

지금 작업 상태가 11.5% 뿐이다. 빌드·렌더링 등으로 부하를 올린 구간을 길게.

---

## C. 녹화할 때 함께 남길 것

- **기기 상태 메모** — 충전기는 배터리 잔량(%), 프로젝터는 램프 사용시간,
  미니PC 는 무슨 작업 중인지. 12.30.6 이 "충전기 지문은 점이 아니라 궤적" 이라
  했는데 지금 풀에는 그 궤적의 어디인지 기록이 없다
- **펌웨어 버전** — A 이후 녹화는 짝수차 성질이 달라진다. 섞이면 안 된다
- 스위칭 시각은 **초 단위로** (지금 라벨이 최대 5.5초 어긋나 계단 스냅으로 보정 중)

---

## D. 돌아온 뒤 할 것

```bash
# 1) 전처리
python -m src.run_preprocess_and_label

# 2) A 검증 — 짝수차가 실제로 내려갔는지
python -m src.run_delta_feature_probe --audit

# 3) 기존 모델을 새 데이터에 채점 (재학습 전에)
python -m src.run_gate_check --ckpt results/adapt_ph1.pt \
    --ckpt-smps results/cnn_even.pt --stride 6 --out results/gate_new.json
python -m src.run_summarize_gate results/gate_new.json
```

**A 가 성공했다면 짝수차 지터·마스크가 더 이상 필요 없을 수도 있다** — 그때는
`cnn_even` 계열과 지터 없는 계열을 새 데이터로 다시 견줘야 한다.
