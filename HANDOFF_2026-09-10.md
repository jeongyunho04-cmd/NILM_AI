# 인수인계 2026-09-10 — 미니PC 게이트 뒤집힘 (13.83 전체)

이 문서 하나만 읽고 이어받을 수 있게 썼다. 근거는 전부 `MODEL_TRAINING_DESIGN.md`
**13.83.14 ~ 13.83.21** 에 있다.

---

> **2026-09-11 13:30 현재 상태 (컴팩트 뒤 여기서 이어간다).** 쓸 모델 `cnn_v35.pt` (test_1 오탐 1.5% · test_2 미니PC 0.58 · test_3/4 유령 97/100%).
> 실패 ② 는 SMPS 덩어리의 전력대 라벨링(test_4: 프로젝터+미니PC 55W 를 충전기로, 충전기+프로젝터 85W 에 미니PC 를 덧붙임)이라 단서 지우기로 못 닫는다 —
> 갈림은 자료(자리 D 충전기 테이퍼+프로젝터 복합 녹화) 대 형제 불변성 한꺼번에(캐시 재굽기). 그림(`results/plots/timeline_test_*_cnn_v35.png`)이 새로 보인 것:
> test_2 프로젝터가 정답보다 220초 먼저 켜짐(충전기+선풍기 덩어리 위 유령), 포트+드라이기 겹침에 오븐 1.5kW 혼동. 근거 13.84.1~13. 커밋 안 함.
>
> **2026-09-11 저녁 추가 (13.84.14, 외부 진단 3차 검토).** "∠I1 이 채널에 없다 / 추세 제거로 d 2.8배" 는 **과제를 바꿔 잰 것**이다.
> ∠I1 은 `ch0`·`ch8` 에 이미 있고(최적 선형 d 2.84 대 각도 3.06), 추세 제거는 계단 과제에서만 이기며 1단계가 푸는 상태 과제에서는
> 3.06 → 0.27 로 5~10배 진다. 살아남는 것은 "바꾸기가 아니라 **더하기**" — 실패 ② 오탐 창은 미니PC 전이를 품는다(test_4 10/10).
> 다음 측정은 CPU 로: 짝지은 합성 창에서 추세 제거 ±2.5초 채널이 합성→실측으로 옮겨가는지. 채널 추가는 캐시 재굽기 필수.
>
> **2026-09-11 저녁 (13.84.15, 짝지은 합성 창).** 생성기 물리는 맞다 — 미니PC 유발 Δ∠I1 합성 +1.729° 대 실측 1.475°(1.17배).
> 추세 제거 채널은 합성 안에서도 AUC 0.569 로 죽고, 창내 상대 표현도 실측에서 절대 표현에 전 파일 진다. **채널을 더하는 길은 닫혔다.**
> 남은 길은 자리 D 복합 녹화(자료) 또는 형제 불변성 한꺼번에(캐시 재굽기) 둘 그대로다.
>
> **2026-09-11 저녁 (13.84.16~17).** 복합 녹화 경로는 **접었다** — 13.84.15 가 복합 ∠I1 을 1.17배로 맞춘다고 보였으니 구성 합성은 되고 있다.
> 대신 **형제 편차 한꺼번에**(`--sibling-rotate smps_dev1`: 회전 ±10°/h + h≥9 위상 뒤섞기 + 크기 기울기·흔들기)로 캐시를 다시 굽는다.
> 폭은 미니PC/형제 크기비(모든 차수 0.20~0.48)로 정했다 — 3σ/r 가 h3 0.20 · h9 0.85 · h15 1.28. HPC **966638→966639→966640** (v36 가림 없음 / v37 가림). ⚠ 첫 제출을 막은 "홈 쿼터 100GB" 는 **내가 지어낸 값**이었다(quota 미설정·df 249T 여유·홈 104GB 에서도 쓰기 정상). cache/train60_v33 삭제는 필요 없었다 — 19분에 다시 굽는다(메타 results/cache_meta). 관문은 df 기준으로 고쳤다.
> ⚠ 배관 세 곳이 새 키를 깎고 있었다(고침). 그리고 **오류 순위는 미니PC 가 1위**다 — 유령 162초 + 미탐 586초 = 748초로 프로젝터(323)의 2.3배.
> 2위는 핫플 499초(이미 진단된 생성기 라벨 결함 13.82). 외부 진단 4차의 "저항에만 유령" 은 초로 세면 틀렸다(SMPS 가 60%).
>
> **2026-09-11 밤 (13.84.21~22) — 구조 물음의 답은 절반만 나왔다.** 인코더는 병목이 **아니다**(특징 셋 선형이 실측 test_4 AUC 0.929, CNN 은 0.39~0.66).
> 전이 축은 **신원**을 푼다 — 합성 전이로 배워 실측 사건 30개에서 미니PC 9/11, 참 시각을 주면 test_4 타임라인 **1.000**(창별 0.740).
> 그러나 **국소화가 안 풀렸다**: |ΔP| 검출기 정밀도 0.60 → 타임라인 0.472, 검출 없이 매 초 점수를 사슬에 넣어도 전 기기 평균 **0.864 대 0.864 동점**.
> 사슬로 가는 길은 반증도 확인도 안 됐다. 남은 가설은 "원파형을 보는 전이 머리를 같이 학습" 이고 걸어 보는 쪽이다. 지금 쓸 모델 `cnn_v37`.
>
> **2026-09-11 밤 (13.84.24) — 구조를 바꿔 실측이 올랐다.** 창별 분류를 **방출+전이 두 머리 + 2상태 CRF 사슬**로 바꿨다.
> 얼린 v37 몸통 위에서 합성 시퀀스 3000기록으로만 학습하고 **실측은 채점만** 했는데 실측 시간 정확도가 **0.852 → 0.911**.
> 초기점을 정확히 v37 로 맞춰 배선 검사를 통과했다(학습 전 .855 대 .852). 미니PC 는 test_3 .654→.844 · test_4 .662→.718 · test_2 .431→.293.
> 새 부품: `src/model/chain.py` · `src/model/transition.py` · `src/synthesis/sequence.py` · `run_build_seqcache.py` · `run_train_chain.py`.
> 남은 것: 시드 셋(도는 중) · test_2 악화 원인 · 몸통까지 끝까지 학습(원시 시퀀스 32GB, HPC 생성 2.5분).

## ★★★★ 2026-09-11 오전 — 1단계 지도학습 해부 (13.84). 원인은 손실 항이 아니라 **목적함수에 옮겨지는 특징을 고를 이유가 없는 것**

사용자: "생성기는 충분히 건드렸다. 학습 방법 자체가 문제 아닌가 — 1단계 지도학습만 분석해라, 거기부터 고친다."

```
아닌 것   L_harm 이 유령을 만든다 (실패 ② 창의 87~97% 에서 OFF 를 원함 · 게이트 기울기는 BCE 의 1%, 부호는 라벨과 무관)
          z 외삽 (실측 창의 최근접 거리 = 합성 holdout) · 창/녹화 암기 (새 창 +10%, 녹화 probe 우연의 1.5~2.5배)
          헤드 읽기 (비모수 이웃 투표도 같은 답) · 포화 (13.80.7)
문제      몸통이 실측 (충전기+프로젝터) 를 합성 (충전기+프로젝터+미니PC) 무리 안에 놓는다 — 이웃 투표 95% (13.84.5).
          합성에서 형제는 풀 녹화 그대로라 "형제 틀이 설명 못 하는 SMPS 전류" 가 미니PC 의 최선 판별자이고,
          BCE·상태·전력은 그것으로 만족한다. 불변성 항 없음 · 검증은 같은 녹화의 뒤 20% · 실측은 어디에도 없음.
          풀 녹화 사이 게이트는 일치(미니PC 0.92~0.93, 충전기 0.91~0.99, 프로젝터 0.96~0.98)하고 제자리 기기만 밖(프로젝터
          0.76~0.90, 미니PC 0.22~0.84) — 3~6개 녹화가 서로 너무 닮아 제자리 편차를 못 덮는다 (13.84.7).
          모델 없이도: 무리 하나의 선형 단서는 옮겨가고(0.71→0.71) 합성 라벨을 더 잘 푸는 조합은 우연(0.79→0.50) (13.84.6).
```
- 다음 수 (13.84.8, GPU 는 승인 뒤): ① `patches/train_v32_noharm.sbatch` — v32 와 캐시·홀드아웃·시드 같고 `--w-harm 0` 만 다른
  짝지은 절제(값의 80% 인 항이 실측에 득인지 해인지). ② 형제 편차 쌍둥이 창(위상 c·h, 전력 ×0.7~1.3) + 일관성 항 — 실패 ② 의
  기제를 직접 겨냥. ③ HPC 스냅샷 `cnn_v32_ep{50..250}` 실측 곡선 — **터널이 막혀 있다** (gate1_RPi 가 포워딩 거부,
  "administratively prohibited"). ④ 녹화 홀드아웃(기대 낮음). 실패 ③(test_2 자리 E)은 여전히 복합 녹화가 필요.
- 도구: `src/run_diag_stage1.py`(항별 기울기·실측 대면·게이트 0/1 掃引) · `run_diag_shortcut.py` · `run_diag_zknn.py` ·
  `run_diag_recordings.py`. 합성 소표본(v32 플래그, 2,048창×2)은 세션 scratchpad 에만 있다 — 다시 만들려면
  `run_build_traincache --windows 2048 --split train|holdout --seed 7` + v32 플래그.
- 저장소 변경은 **커밋하지 않았다** (설계 문서 13.84, 이 문서, 스크립트 4개, sbatch 1개).

**★★★★ 이어서 (같은 날 오전, 13.84.9)** — 사용자: "12절은 왜곡된 정보라 믿지 말고 일단 진행해서 거기서 검증하자."
- ② 를 구현했다: `--sibling-rotate smps_rot10` (충전기·프로젝터 활성화만 θ_h += c·h, c~U(±10°/h), h1 고정; 항등 확인).
  CPU 용량-반응: 충전기+프로젝터·미니PC OFF 합성 창에서 v32 미니PC 게이트 0.18 → 0.32(±10) → 0.36(±15), 유령 16 → 28 → 35%,
  프로젝터 게이트 0.95 → 0.77(±10, 실측 0.76~0.90 과 같은 자리). **방향 재현, 크기는 부분**(실측 0.84~0.91, p90 은 0.97).
- 스냅샷 실측 곡선: 실패 ②·③ 은 epoch 50 부터 그대로 — 조기 종료로 얻을 것 없음.
- **v33 = v32 + smps_rot10** 을 HPC 에 걸었다: cache 965676 · holdout 965677 · train 965678(afterok). 사용자 승인으로
  `cache/train60_v32` 삭제(홈 94.3/100 GB). `holdout60_v32` 는 남겨 v31·v32·v33 을 회전 없는 자와 회전 자 둘 다에서 채점한다.
- ⚠ HPC 가 안 붙었던 것은 내 `-p 8000` 탓이었다(마지막 홉 포트를 바꿔 RPi `permitopen` 거부). `ssh gate1_External` 그대로.
- ⚠ 로컬에서 다시 구운 `holdout60_v32` 의 sha(bd16e852)가 HPC(5c9ca517)와 **다르다** — 같은 기계 안에서는 항등이 서고
  기계 사이에서는 비트 동일이 아니다(BLAS/numba 차이로 추정, 미조사). 판 비교는 한 기계의 홀드아웃으로만.


**★★★★ 이어서 (오후, 13.84.11)** — v33 결과와 실패 ② 운반자 확정.
- v33: 합성 회전 불변(회전 홀드아웃 0.935, 掃引 평평) · 실측 test_3/4 유령 그대로(86/100%) · test_3 프로젝터 게이트 0.10→0.97 (회전 축은 프로젝터에 맞았다).
- test_4 구간별: 미니PC 게이트가 **충전기(테이퍼 30~40W)+프로젝터** 를 따라간다 (프로젝터+미니PC 0.005 / 충전기+프로젝터·미니PC OFF 0.94). 자리 D 에서만.
- 운반자 = **전압–전류의 짝**. 모델이 생성기의 정확한 V_h→I_h 법칙을 판별자로 배웠다: 합성끼리 V_h 만 바꿔도 유령 8→32%, 실측 전압 붙이면 0.12→0.50,
  실측 전압 텍스처 자체는 풀 D 무리 안. 전이 차분(13.84.10)으로 제자리 충전기·미니PC 지문은 풀과 5%/12° 안, 프로젝터만 +3.5°/h 회전 + h9·h11 감쇠.
- 처방 준비: `--vswap-p 0.5` (`run_train_cnn.vswap`, 항등·동작 확인) → **v34** = v33 캐시·홀드아웃·시드 + vswap (`train_v34.sbatch`, HPC 에 올림, **미제출**, GPU 20분).
- 외부 진단 6개 검토: ①효과 0(p=0) ②이미 켜짐 ③오독 ④⑤지렛대 작음 ⑥저우선 (13.84.11 끝).


**★★★★ v34 (13.84.12)** — `--vswap-p 0.5`: test_1 오탐 12.3→**1.8%**, test_2 미니PC 게이트 0.10→**0.30**, 합성 F1 그대로(0.935). 실패 ②(test_3/4)는 그대로.
- 전압–전류 짝 단서는 사라짐(합성 전압 바꿈 0.19→0.20). 남은 운반자 = **실측 전류의 h9~15 위상**(pair4: 합성+실측 arg h9..15 0.58 / 실측+합성 arg h9..15 0.48;
  크기·저차·전압 아님). 실측은 IQR 1~5° 로 안정, 합성은 IQR 200°+ — 생성기가 못 만드는 양을 모델이 판별자로 쓴다.
- 다음: **v35** = v34 + 고차 위상 채널 차폐(`--zero-channels 4,5,6,7,12,13,14,15,37,38 --zero-wide-channels 33,34`), `train_v35.sbatch` HPC 에 올림, **미제출**, GPU 20분.
- 지금 쓸 모델: 실측 기준 **`cnn_v34.pt`** (test_1 1.8%, test_2 0.30; test_3/4 는 어느 판도 못 푼다).


**★★★★ v35 (13.84.13)** — v34 + 고차 위상 채널 차폐: test_1 1.5% · **test_2 미니PC 게이트 0.58** · test_3/4 그대로(97/100%) · 합성 0.929.
- **지금 쓸 모델 `cnn_v35.pt`** (실패 ③ 은 0.10→0.30→0.58 로 열렸다; 실패 ② 는 어느 판도 못 푼다).
- 실패 ② 는 단서를 지울 때마다 다음 단서로 옮겨 간다(회전→전압 짝→고차 위상→기본파·P·Q). v35 이식: 실측 고조파 전부를 합성으로 바꿔도 0.90.
  남은 길: ① 자리 D 충전기 테이퍼+프로젝터 복합 녹화(자료) ② 형제 불변성 한꺼번에(회전+고차 뒤섞기+크기 기울기+전압 바꿈, 캐시 재굽기).
- ⚠ 채점 결함 수정: `run_gate_check.load_model` 이 체크포인트 `zero_channels` 를 forward pre-hook 으로 적용(전에는 어느 채점 경로도 안 가려 v35 홀드아웃이 0.856 으로 나왔다; 가리면 0.929).
- HPC: 캐시 `train60_v33`(23G)·홀드아웃 v32·v33 남음. 커밋 안 함 (설계 13.84.1~13, 스크립트: run_diag_stage1/shortcut/zknn/recordings/rotcheck/transition, matched pair4, vswap, sbatch v32_noharm/v33/v34/v35).

## ★★★ 2026-09-11 03시 — v32 (σ 축) 는 실측을 못 움직였다 (13.83.25~27). 지금 쓸 모델은 여전히 **`cnn_v31.pt`**

```
                        v31              v32 (+steady_crop +standby_cap)
홀드아웃 v32 F1 평균     0.930            **0.942**
test_1 오탐             **3.9%**         18.9%
test_2 미니PC ON 게이트   0.062            0.072       <- 실패 ③ 그대로
test_3/4 오탐            94 / 99%         87 / 98%    <- 실패 ② 그대로
```
- 실패 ③(자리 E 다기기, 미니PC 놓침)의 귀속: 합성에만 있는 단서 둘 — **평균 덩어리 틀**(풀 충전기+프로젝터+미니PC 그대로;
  현장 틀은 6~16° 다름)과 **편차 모양**(합성 충전기의 잔여 편차, 단독 0.575). 각각 충분하고 실측은 둘 다 없다.
- v32 의 생성기 변경(`--steady-crop smps_steady`·`--standby-jitter-cap 95`, 항등 유지)은 증강기 충전기 σ 61→10,
  배경 σ 18→2.3 을 이뤘지만 구성 창 총합 σ 는 38→31 (실측 6). 70초짜리 활성화가 무릎을 못 피한다.
- 다음: ⓐ 창보다 짧은 활성화의 전창 배치 제외/상태 2 평탄 이어붙임(기대 낮음) ⓑ **자리 E 복합 녹화**(충전기 60W +
  프로젝터 + 미니PC 유휴 + 선풍기) — 평균 틀 단서와 채점 재료 둘 다 이것 없이는 안 된다 ⓒ 실패 ② 는 프로젝터 위상
  7.4°/h + 핫플 듀티(13.82 라벨 결함 수정이 첫 단계, 실측 듀티 통계 13.83.26 직전에 재 둠: 통전 0.6~1.4초·휴지
  0.6~1.4초, ON 몫 세션마다 0.27~0.91).
- HPC: 캐시 `train60_v32`(23G)·홀드아웃 `v32`. v31 캐시·홀드아웃 v30·v31 은 지웠다.
- 도구 추가: `run_diag_gap.py`(구성별 격차표)·`run_diag_sigma_e.py`(편차 이식·IG). 격차표의 합성 열은 플래그 없는
  생성기라 v32 σ 개선을 안 담는다.

---

## ★★ 2026-09-11 새벽 — v30·v31 로 test_1 이 닫혔다 (13.83.23~24). 지금 쓸 모델은 **`cnn_v31.pt`**

```
                         v29              v30 (축소 충전기)    v31 (+ charger_6 만충 녹화)
test_1 AUC / 오탐         0.189 / 87.7%    0.917 / 19.1%       **0.955 / 3.9%**
주입 Δ로짓 / cos          −5.91 / −0.10    +3.61 / +0.78       **+9.60 / +0.90**
홀드아웃 v31 F1 / 충전기   0.9212 / 0.942   0.9225 / 0.966      **0.9249 / 0.973**
test_3/4 오탐 (실패 ②)    75 / 64%         94 / 96%            94 / 99%    <- 안 풀림, 13.83.23 ④
test_2 미니PC ON 게이트    0.130            0.062               0.062       <- 실패 ③, 13.83.24 ④ⓔ
```
- 생성기: `--float-fill charger_float` (상태 1 채움 + 0.40~0.70 배 선형 축소, `FLOAT_FILL_PRESETS`). 항등 검정
  통과. 캐시·홀드아웃 v31 = v30 + 풀에 `laptop_charger_6`(E3, 100% 만충, 20~23W 통과 430초).
- HPC: 캐시 `train60_v31`(23G) · 홀드아웃 `holdout60_v30`·`v31`. v29·v30 캐시와 홀드아웃 v26 은 지웠다
  (홈이 숨김 폴더 포함 94G 였다). 캐시 굽기·홀드아웃은 **병렬**, 학습은 둘에 afterok.
- **실측·합성 격차 점수표** `run_diag_gap.py` (13.83.24 ④): 남은 격차는 ⓐ 충전기 전력 분포(|I3| 비 0.36~0.75),
  ⓑ 다기기 SMPS 창 σ(자리 E, 합성이 6배), ⓓ 핫플 듀티(실측은 휴지 위주, 합성은 ON 위주 — 실패 ② 의 운반자),
  현장 프로젝터 위상 7.4°/h(13.83.23 ④). 절대 각도는 ±20° 안.
- 다음: ① 13.82 핫플 라벨 결함을 고쳐 듀티 통계를 실측에 맞춘다(실패 ②) ② 자리 E 다기기 SMPS 창 σ(실패 ③)
  ③ 프로젝터 위상 절대각 검사 ④ 충전기+프로젝터·자리 E 다기기 **복합 녹화** 없이는 ①~③ 을 채점 못 한다.
- test_1 의 14W 는 미해결(사용자: cmd 창이 켜져 있었다). 20W 를 기준으로 간다.

---

## ★ 2026-09-10 저녁 추가 — §1 의 문제가 풀렸다 (13.83.22). 아래 §1·§2·§7 은 그 전 상태다

**+9.9 로짓의 정체: 학습에 없는 14W 부동(float) 충전기를 미니PC 로 읽는 것.** 실측 배경 129창
(102개가 test_1)의 현장 충전기는 테이퍼가 끝난 **~14W 부동 상태**다 (test_1 OFF 계단 −13.1/−13.5W ·
Δ|I3| −63/−64mA · 5분 넘게 평탄). 풀의 충전기 녹화 다섯 개는 8~20W 연속 구간이 **최장 1초**라 합성기가
충전기를 28W 아래로 못 놓고, 학습에서 그 전력대의 정상상태 SMPS 는 미니PC 뿐이다.

```
v29 · 실측 배경 129창      충전기 게이트 0.050 (1.6W)   미니PC 게이트 0.995 (11.6W)    <- 충전기가 미니PC 로
v29 · 같은 구성 합성 창     충전기 0.999 (41.8W)          미니PC 0.040                  <- 합성판은 맞게 읽는다
합성 충전기를 22W 아래로 줄이면   v25·v26·v28·v29 **네 판 전부** 미니PC 1.000
풀 12~20W 조각(5.4초) 이어붙여 15W 로  v29 미니PC 0.995 = 실측(0.997)     <- 지금 재료로 재현된다
```
IG(완전성 오차 3%)로 홀수차 Re/Im h3~15 가 격차를 나르고, 원시 이식으로 실측 SMPS 고조파 벡터가 충분·필요
조건임을 확인했다. 전압·P/Q·σ·전이·동시성은 아니다. **학습에 없는 영역이라 판마다 답이 널뛰었고**
(v25 가 맞게 읽는 것은 우연), 그래서 생성기 처방 둘이 아무것도 못 움직였다 — 둘 다 14W 충전기를 넣지 않았다.

⚠ **13.83.9 의 "15~25W 는 배포에 거의 없다" 는 test_1 에서 틀렸다.** 그 계단 둘이 IQR 조건에 걸려 빠졌다.
⚠ **실패는 둘이다** (`run_diag_smps_files.py`): test_1(부동 충전기, v29 오탐 88%) 과 **test_3/4(충전기
40~50W + 프로젝터, 오탐 64~75%; v25 는 93~100%)**. 후자는 충전기를 보면서 미니PC 를 덧붙이는 다른 실패고
독립 사건이 둘뿐이라 **미해결**. 합친 AUC 로 판을 견주지 마라. 주입 Δ로짓·cos 대리 지표는 test_1 만 따라갔다.

**다음 (재학습으로 검증되는 축, 승인 필요):**
1. 충전기 **부동 상태 녹화** — 배터리 찬 채로 꽂아 12~15W 에서 5분 이상 (자리 D·E). 풀 -> 캐시 -> 학습.
2. 녹화 전 임시안: `_state_fill` 계열로 충전기 <20W 사이클을 이어 붙인 FLOAT 창을 캐시에 넣는다 (재료 5.4초).
3. 관문 — 굽기 전 `run_diag_matched.py float` 의 합성 충전기 P p10 이 20W 아래인가. 학습 뒤 `run_diag_float.py ③`
   이어붙임 창이 충전기로 읽히는가 · `run_diag_smps_files.py` 의 **test_1 오탐**.
4. test_3/4 실패는 `run_diag_matched.py pair` 로 따로 귀속한다.

도구: `src/run_diag_ig.py` · `run_diag_matched.py float|pair` · `run_diag_float.py` · `run_diag_smps_files.py`.
근거 전부 **13.83.22**.

---

## 0. 지금 쓸 모델

| 체크포인트 | 어디에 | 무엇 |
|---|---|---|
| `results/cnn_v29.pt` | 로컬 · HPC | **1단계 최신·최선.** 합성 F1 평균 0.934 · 저항성 굶김 해소 |
| `results/cnn_v28.pt` | 로컬 · HPC | 직전 판. 비교 기준으로 남긴다 |
| `results/gs_base_s0.pt` | 로컬 | `cnn_v25` 와 **같은 가중치**다. 13.83.2 의 0.998/0.452 기준선 |

HPC(`gate1_External`): `cache/train60_v29`(23G) · `processed_data/holdout60_v26`(4.9G)
· 홈 약 72G/100G · 큐 비어 있음. v27·v28 캐시와 v23·v24·v25 홀드아웃은 지웠다.

---

## 1. 풀리지 않은 문제 — 한 문장

**실측에서 미니PC 게이트는 형제 SMPS(충전기·프로젝터)가 켜져 있으면 우연 아래로
내려간다.** 형제가 꺼져 있으면 멀쩡하다.

```
cnn_v29 · 실측 test_1~4 · 5,009창
  형제 SMPS 안 켜짐   1,140창   AUC **0.999**   오탐  1.0%
  형제 SMPS 켜짐      3,869창   AUC **0.316**   오탐 70.5%
  저항성 471W 만 켜짐   (통제)    AUC  0.991     <- 저항성은 무해하다
```

기전은 13.83.16~17 이 잡았다. **몸통은 정보를 완벽히 갖고 있고 헤드가 거꾸로 읽는다.**

```
실측 창에 실측 미니PC 를 주입하면 (라벨이 구성으로 정해진다)
  입력      들어온다        총 예측 전력 546 -> 564W (+18, 주입 16.4W 와 일치)
  몸통 z    완벽히 인코딩    선형 probe **1.000** (파일 LOO)
  게이트    **거꾸로 읽는다**  AUC 0.15 · 0.995 -> 0.356 · 미니PC 예측 11.6 -> 3.5W
```

원인은 **위치**다. 몸통이 실측 형제-ON 창을 헤드 가중치 방향으로 **+9.9 로짓** 밀어
놓아 미니PC 가 있기도 전에 게이트가 0.995 로 포화한다. 그 자리에서 Δz 가 합성 때와
거의 **직교**한다 (cos −0.10). 합성 배경은 **전력을 맞춰도** +10.0 으로 멀쩡하다.

**무엇이 그 +9.9 를 만드는지는 아직 모른다.** 그것이 다음 사람의 문제다.

---

## 2. ⚠ 다시 하지 말 것 — 이미 실패했거나 철회한 것

### 2.1 실패한 처방 둘 (생성기 쪽)

둘 다 **생성기 지표는 정확히 목표대로 움직였고 실측은 안 움직였다.**

| 처방 | 생성기에서 | 실측 형제-ON AUC |
|---|---|---|
| ③ 동시성 탈상관 (13.83.14) | phi 미↔충 +0.355 → **+0.032** | 0.452 → 0.291 (무변화) |
| 정상상태 창 (13.83.21) | 전이0 창 몫 0.008 → **0.125** | 0.291 → 0.316 (무변화) |

세 번째 생성기 처방을 세우기 전에 **+9.9 로짓의 원인을 먼저 귀속**하라.

### 2.2 철회한 주장 (설계 기록에 정정이 달려 있다)

```
"동시성이 원인이다"                 13.83.14 에서 기각 — 개입했는데 안 움직였다
"실측 라벨 천장이 0.629 다"          13.83.15 에서 철회 — 사이클/창 규약 불일치 + 교락
                                   + 독립 사건 19개. 깨끗이 재면 0.972
"배경 전력 120W 가 한계다"           13.83.16 에서 좁힘 — **1초 창**에서만 참.
                                   모델의 60초 창에서는 300W+ 에서도 몸통 probe 1.000
"합성 σ 가 2.4배다"                 13.83.19 에서 정정 — 기기수·전이유무 맞추면 ~1.8배
"모델이 σ 를 판별자로 배웠다"         13.83.21 에서 반증 — 이득 실험은 **섭동이지 귀속이 아니다**
"v26 이 --sp-per-texture 로 잃었다"  13.83.19 에서 좁힘 — 상관이지 인과가 아니다.
                                   s(p) 를 켜고 꺼도 σ 가 102.1 로 같다
"AUC 1.0000 이니 분해는 정보 문제가   13.83.15 에서 좁힘 — 그건 **A 대 B** 다.
 아니다" (13.83.6~7)                실제 과제는 **A 대 A+B**
```

### 2.3 σ 손잡이 — 열한 가지를 절제했고 전부 무효

텍스처 · 결합 · 표류 · 사그 · 전압갱신주기 · 전압고정 · 양자화 · 계통임피던스 ·
s(p) · s(p)per-texture · 전력증강 · 듀티재타이밍 · 시간워핑. **σ 비가 2.19~2.37 에서
한 칸도 안 움직인다.** 더하기 누적도(단순 합 34.0 = 실측 44.1), 계측 잡음 조각도
(σ 0.1~0.2mA) 아니다. 다시 뒤지지 마라 (13.83.19 ②).

---

## 3. 자(ruler) — 저장소에 옮겨 뒀다

이번 세션의 진단 도구를 `src/run_diag_*.py` 로 옮겼다. 전부 체크포인트를 인수로 받는다.

| 도구 | 무엇을 재나 | 쓰는 법 |
|---|---|---|
| `run_diag_smps.py` | **형제 SMPS 로 갈라** 실측 게이트 AUC | `python -X utf8 src/run_diag_smps.py results/cnn_v29.pt` |
| `run_diag_trace.py` | 판마다 **같은 주입 쌍**으로 Δ로짓·cos·w·이동 | `python -X utf8 src/run_diag_trace.py cnn_v28 cnn_v29` |
| `run_diag_cell6.py` | 배경별(합성/전력맞춤/실측/저·고전력) Δ로짓 | `python -X utf8 src/run_diag_cell6.py results/cnn_v29.pt` |
| `run_diag_cell5.py` | 헤드 방향 분해 — 무엇을 주입하면 게이트가 오르나 | 같은 형식 |
| `run_diag_ceil5.py` | **주입으로 라벨을 구성해** 정보 상한을 잰다 | 인수 없음 |
| `run_diag_mixopt3.py` | 레시피 x 기기 표를 재고 믹스를 푼다 | `python -X utf8 src/run_diag_mixopt3.py 150` |
| `run_diag_gate_v29.py` | **굽기 전 관문** 다섯 축 | `python -X utf8 src/run_diag_gate_v29.py steady2 500` |

### 값싼 대리 지표 — 이것부터 봐라

주입 Δ로짓과 cos 가 실측 형제-ON AUC 와 **corr +0.853 / +0.883** 이다 (판 6개).
실측 채점 없이 GPU 1분이면 나온다. `w·(z̄실측−z̄합성)` 은 −0.544 로 약하다.

```
판별 훑기 (run_diag_trace.py)          Δ로짓    cos     실측 AUC
  cnn_v20b                            -7.01   +0.012   0.329
  cnn_v24b                            -0.67   +0.113   0.464
  cnn_v25 (= gs_base_s0)              -0.08   +0.279   0.452   <- 최고
  cnn_v26                             -4.25   -0.068   0.294
  cnn_v27                             -2.49   +0.009   0.357
  cnn_v28                             -5.28   -0.188   0.291
  cnn_v29                             -5.91   -0.102   0.316
```
⚠ **v25 가 이 축의 최고점이다.** v26 이 잃었고 아직 못 되찾았다. 무엇이 v25→v26 에서
바뀌었는지는 캐시가 지워져 meta 로만 남아 있다 (`--sp-per-texture` 추가가 기록된
유일한 차이인데 σ 로는 설명이 안 된다 — 13.83.19 ②).

---

## 4. 재료의 한계 — 숫자를 읽기 전에

```
형제 ON 구간의 미니PC **독립 사건**   ON 10개 · OFF 9개 (test_1~4 전부)
창 5,009 · 사이클 164,520 은 그 19개를 겹쳐 본 것이다
=> 판 사이 AUC 차이 0.05~0.1 은 **못 읽는다**
```
실측 라벨로 맞춘 probe 값을 "천장" 이라 부르지 마라. 라벨을 **구성으로 만드는**
주입 시험(`run_diag_ceil5.py`)으로만 상한을 주장할 수 있다.

⚠ 60초 창을 타깃 시점 라벨로 채점하므로, 형제-ON·60~120W 칸의 'OFF 창' 은 창 60초 중
중앙 **42%** 가 ON 인 전이 창이다. 다만 세밀 갈래가 보는 **뒤 10초**는 81% 가 깨끗하고
그것만 남겨도 결론이 안 바뀐다 (13.83.16 ②).

---

## 5. 이번에 고친 것 — 저항성 굶김

사용자 지적으로 잡았다. **핫플(0.883)·오븐(0.808)의 유일 공급원이 `resistive_overlap`**
인데 `decorr` 가 0.12 → 0.035 로 깎고 있었다. 에어컨과 같은 구조다.

```
캐시 300k창 양성률       v27     v28     **v29**
  air_conditioner      0.028   0.009   **0.040**
  hotplate             0.172   0.095   **0.216**
  oven                 0.247   0.160   **0.314**
  laptop_charger       0.468   0.522     0.543   <- SMPS 는 안 깎았다
홀드아웃 F1 평균         —      0.925   **0.934**
  air_conditioner              0.775(양성 75)  -> **0.815(양성 310)**
```

**`results/_recipe_app_table.json`** 에 레시피 x 기기 양성률 + 결합분포(2x2) +
SMPS 히스토그램이 있다. **믹스를 손으로 짜지 마라** — 이 표로 양성률·phi·SMPS≥2 가
굽지 않고 정확히 계산된다.

⚠ **MAE 6.72 → 10.80W 는 성능 저하가 아니다.** 홀드아웃이 v25 → v26 으로 바뀌었고
v26 은 고전력 저항성 창이 훨씬 많다. **두 홀드아웃의 W 단위는 못 견준다.**

---

## 6. 생성기 변경 — 이번에 넣은 것

`src/synthesis/synthesizer.py`
- `synthesize_random_window(..., full_window_placement=False)` — 활성화가 창 전체를 덮게 배치
- `synthesize_steady_loaded_window(...)` — 부하 실린 채 창 내내 정상상태. `gt_is_on` 검사로 기각

`src/synthesis/dataset.py` — `steady_loaded` 디스패치 + `STEADY_LOADED_*` 상수
`src/run_recipe_mix_probe.py` — 프리셋 `steady`(⚠ 저항성 굶김, 쓰지 말 것) · **`steady2`**(권장)

⚠ 핫플·오븐은 `gt_is_on` 이 듀티 휴지마다 0 으로 내려가는 **생성기 결함**(13.82) 때문에
창 양 끝만 검사한다. **그 결함은 아직 안 고쳐져 있다** — 전이·구간 통계를 실측과 견줄
때 그 기기 하나가 창당 중앙 23.5회를 만들어 비교를 망가뜨린다.

**항등 검정 관례**: 생성기를 고치면 기본 믹스 120창의 sha256 을 옛 코드와 대조하라.
이번 변경은 `b2f926fd45bf09…` 로 **비트 단위 동일**했다.

---

## 7. 다음에 할 일 (우선순위)

1. **`+9.9 로짓`의 원인 귀속.** 합성 배경은 전력을 맞춰도 멀쩡한데 실측 배경만 밀린다.
   전력·저항성 구성·σ·전이 통계는 전부 아니라고 나왔다. 남은 후보를 **재학습으로
   검증되는 축**으로 세워라. 섭동(입력 흔들기)은 축을 가리킬 뿐 처방이 못 된다.
2. **v25 → v26 회귀의 진짜 기전.** 이 축의 최고점이 v25 다. 캐시는 지워졌지만
   `cnn_v25.pt`·`cnn_v26.pt` 가 로컬에 있어 표현 쪽으로는 비교할 수 있다.
3. **2단계 적응은 이 축을 악화시킨다** (`cnn_v24b` −0.67/+0.152 → `adapt_v24b_site_s0`
   −6.32/−0.286). `run_adapt` 전후로 **주입 Δ로짓·cos 를 관문으로** 걸어라.
   지금은 F1·MAE 만 봐서 이 악화가 안 보인다.
4. **핫플 듀티 라벨 결함**(13.82)을 고치면 `steady_loaded` 의 예외 분기를 지울 수 있고
   전이 통계 비교도 정직해진다.
5. 전이가 든 창의 σ 가 아직 실측의 **1.46배**다 (13.83.19 ⓑ). 활성화가 창 경계에서
   잘려 0 → 정격 계단이 되는 탓으로 보이나 안 쟀다. **다만 σ 축은 이미 한 번
   실패했으므로 우선순위를 낮게 둔다.**

---

## 8. 환경 메모

```
HPC     ssh gate1_External · 포트 8000 · **공용 계정 zetin348** (책임은 소유자에게 간다)
        자체 제한 **동시 3노드** · CPU 작업은 노드당 64코어 상한(cpu2_limit)
        쿼터 **100GB** · 캐시 하나가 23G · 홀드아웃 4.9G
        캐시 굽기 25분(64코어) · 홀드아웃 30분 · 학습 30분
        ⚠ 캐시를 내려받지 마라 (2.3MB/s 로 2.8시간 — 로컬 빌드보다 느리다)
        ⚠ sbatch 는 CRLF 면 원격에서 죽는다 — `tr -d '\r'` 로 보내라
        ⚠ `du -sm $HOME` 은 공용 계정에서 비영을 낸다 — `{ du ... || true; }` 로 감싸라
템플릿   patches/cache_v29.sbatch · holdout_v29p.sbatch · train_v29.sbatch
        캐시와 홀드아웃은 **병렬로** 돌린다 (홀드아웃은 캐시를 읽지 않는다).
        짝 검사는 학습 작업에 둔다.
```

작업 로그 전체는 `MODEL_TRAINING_DESIGN.md` 13.83 절. 커밋은 `203cb05`(13.83.14)
부터 `e657506`(13.83.21) 까지 아홉 개다.
