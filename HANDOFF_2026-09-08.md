# 인수인계 2026-09-08 — Z 보조 감독 (13.55~13.58)

설계 기록 **13.55~13.58** 이 정본이다. 여기는 산출물·다음 할 일만 적는다.

## 이번에 만든 것

```
캐시   cache/train60_v24          23GB. **v22 와 창이 비트 단위로 같고** z_grid(r,x) 만 추가
       -> cnn_v23 ↔ cnn_v24a/b 는 `--w-z` 하나만 다른 대조다
코드   traincache z_grid · NILMNet(aux_z) · LossWeights.z
       run_train_cnn --w-z · run_adapt --w-z · 채점기 9개에 aux_z 적재
       (곁가지) run_build_traincache --help 의 한국어 % 이스케이프 수정
판     cnn_v24a (w_z 0.1) · cnn_v24b (w_z 0.3)
       adapt_v24{a,b}_s0~2 (2단계 Z 없음) · adapt_v24{a,b}_z_s0~2 (2단계 Z 있음)
       abl_* 8개 — 13.58 손실 항 절제 진단용. **처방 아님, 지워도 된다**
도구   src/run_scorecard.py          실측 AUC · 합성 AUC · 격차 · 전력 헤드 생사
       src/run_site_split_check.py   **자리 x 구성**으로 갈라 관문·배분·Z추정.
                                     AUC 도 run_power_check 도 못 보는 실패를 잡는다
       src/run_z_probe.py            몸통 탐침 · 실측 Z · 학습범위 안 掃引
       src/run_loss_compare.py       손실이 정답을 원하는가 (배분 A/B 직접 계산)
                                     + 차수 묶음별 정규화 오차
       (zhow·zleak 은 일회성 진단이라 안 옮겼다 — 결론은 13.56·13.58 에 있다)
```

## 운영점

```
현재 최선   cnn_v24b + adapt_v24b_z_s*        실측 AUC 평균 0.922~0.929
            충전기 +0.077 · 미니PC +0.068 (v23p 대비, 시드 3개 전부 양수)
⚠ 자리 D 프로젝터만 보면 **1단계 cnn_v24b(31.8W, 참 43)가 어떤 적응판보다 낫다**
  (적응판 2~15W). 적응은 다른 것을 벌지만 이 축에서는 잃는다
```

## 다음 (권장 순서)

### A. 2단계 지문을 Z 로 조건화 — **다음에 할 것**
13.58 이 뿌리를 확정했다. 2단계 `harm` 의 지문은 기기x상태별 **고정값**이라 Z 도 자리도
모른다. 실제로는 자리 D(Z=1.19Ω)의 고차 전류가 E(0.42Ω)의 50~75% 인데, 프로젝터 지문은
격리 녹화 3개 중 2개가 자리 E 라 자리 D 고차를 **+71% 과대예측**한다. 손실이 그것을
**프로젝터 전력을 깎아** 맞추고 그 40W 가 충전기로 간다.

재료는 이미 다 있다:
```
생성기가 그 물리를 안다   합성 D/E 비 h11 0.474 · h13 0.555  대  실측 0.490 · 0.490
모델이 Z 를 읽는다        1단계 헤드 R² 0.947 · 실측 5파일 평균오차 27.1%
```
할 일: 회로 모델로 `sig(Z, h)` 표를 만들어 `NILMLoss` 가 창별 Z 로 지문을 고르게 한다.
Z 는 (ㄱ) 모델의 `log_z` 헤드 또는 (ㄴ) `SITE_SESSIONS.z_ohm` 에서 온다. (ㄴ)가 먼저다.

⚠ 대조는 `adapt_v24b_z_s0~2` 다. 판정은 **자리 D · SMPS 전용 · 셋 다 켜진 창**에서
프로젝터 예측W (참 43) — AUC 로는 안 보인다 (`auc-hides-gate-magnitude-collapse`).

### B. harm_weight 재검토 (A 다음)
`inv_h2` 가 판별력 있는 h15 를 h1 대비 **225배** 누른다. ⚠ 12.135 가 그 값을 고른
근거부터 다시 읽을 것 — 그 주석에 이미 "어떤 가중으로도 최소는 오답 쪽에 남는다" 가 있다.

### C. 남은 것
```
· 저항 창 타깃 편향 — ch30 합성 중앙 2.5~3.1 대 실측 0.007. `resistive_overlap` 의
  "타깃 시점 통전 강제" 재시도가 원인. v20 부터 있었다 (13.55 전에 보류하기로 함)
· 13.52 의 "회로 모형이 지문보다 1.60배 나쁘다" — **미검정**. 그 측정도 test_4 의
  자리 D · SMPS 전용 사이클이라 13.57 의 교락에서 자유롭지 않다
· 포트 실측 AUC −0.02~−0.03 (세 팔 모두 음수, 부호는 섞임) — Z 감독의 대가인지 잡음인지
· deploy/nilm_runtime/ 사본이 낡았다 — wide_target 없고 P_CH_FINE=30 (v1 배치).
  채널 규약이 13.12 에서 바뀐 뒤로 안 맞췄다. 배포 전에 다시 복사할 것
```

## 이번에 정정한 것 (되풀이 방지)

```
13.54 의 Z 비불변성      분포 밖 외삽이었다. x=0.3r 이라 Z=3.0 에서 x=0.9Ω(학습 범위의 6배 밖).
                       범위 안에서는 v23 도 이미 불변 (관문 폭 0.004)
13.50 의 "장소별 지문 무용"  저차 측정이었다. h5 는 2~3도가 맞지만 h13 은 28~32도, h15 는 56도
13.50 의 "11.9도 안에 몰림"  전체 벡터 각도. h13 에서는 충전/프로 0.409 로 2.4배 갈린다
13.56.2 의 "처방 A 반증"    AUC 로만 봤다. 크기로 재면 2단계 Z 감독이 자리 D 프로젝터를 3~5배 살린다
pst 가 0 이면 죽은 헤드      아니다. 드라이기 s1 은 pst 4e-10 인데 예측 419.8W (혼합 가중을 다이얼로 쓴다)
```
