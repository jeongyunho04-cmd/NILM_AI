# 판정 기준 — `s(p)` + 상시 배경 + 장소 B 무리 (12.166 / 12.167)

**결과보다 먼저 적는다 (규칙 22). 시드 3개 (규칙 50).**

## 무엇을 바꿨나 (한 캐시에 셋이 같이 들어간다)
```
12.166 (a) 상시 배경 레이어      p_noise 2.17 -> 7.57W, |I1| +0.074A
12.166 (b) 증강 전력스케일 s(p)   I<-I·a 를 I(p·a) 로 (곡선 있는 기기, 유효범위 안)
12.166 (c) 손실 noise_sig 에 배경 (캐시 meta 를 읽어 자동)
12.167     장소 B 콘센트 무리 추가 (227.5V / 0.91Ω, 가중 0.25) + 탐색 r 0.7~2.0
```
⚠ **셋이 묶여 있다** (규칙 54 위반). 이기면 절제해서 귀속해야 한다.
근거가 가장 센 것은 (a) 다 — 배경 5W 의 |I1| 0.074A 가 미니PC 9.5W 의
0.049A 보다 크고, 실측 '전부 OFF' 창에서 k≈3.0~3.8 로 실재를 확인했다.

## 자
```
대조   cnn_h2 / adapt_h2_s{0,1,2}   (새 라벨 + ch51, 옛 무리, 배경 없음)
처방   cnn_sp / adapt_sp_s{0,1,2}   위 넷을 다 켠 캐시로 1단계부터
```

## ① 인과 — 이것이 안 움직이면 형태가 틀린 것이다
```
미니PC IDLE 전력 예측 (test_15/18, 참값 9.90W)
   h2  대조   0.88 / 0.01 W   평균 0.44W   (**−95.5%**)
   hwO 대조   0.13 / 0.04 W   평균 0.08W   (−99.2%)
   목표       평균 **4W 이상** (−60% 이내)
```
게이트는 이미 0.49~0.94 로 켜져 있다 — 모델은 뭔가 있다는 것은 안다. `p_raw`
가 0.01~3.03W 로 죽어 있는 것이 문제다. 배경을 합성에 넣으면 그 상시 전류를
설명할 자리가 생기므로 미니PC 가 자기 몫을 되찾아야 한다는 것이 이 처방의 논리다.

## ② 사는 것
```
장소B 미니PC F1   0.288 -> 0.40 이상
장소A 미니PC F1   0.868 유지 또는 향상
장소B 유령 W      12.13 -> 줄어야
```

## ③ 지켜야 하는 것 (3시드 중앙, 병합 채점)
```
장소B  드라이기 0.932 · 포트 0.935 · 핫플 0.901 · 전체F1 0.861 · 잔차 14.66
장소A  전체F1 0.912 · 드라이기 0.873 · 오븐 0.927 · 유령 3.32
1단계  홀드아웃 F1 평균 0.928 · MAE 3.81W · 잔차 23.33
```
장소 A 전체F1 이 0.88 아래로 내려가면 전압 분포 변경(12.167)이 해를 끼친 것이다.

## 미리 적어 두는 위험
```
- 전압 무리가 바뀌어 P ∝ V² 로 모든 것에 영향이 간다. 1단계 홀드아웃은
  **옛 라벨·옛 전압**으로 만든 holdout60_ze1 이므로, 처방과 분포가 어긋난다.
  그 지표가 나빠져도 그것만으로는 판정하지 않는다 — 실측이 판정이다.
- 배경을 `p_noise` 에 넣으면 `L_cons`/`L_res` 의 `p_res` 가 그만큼 줄어든다.
  저항 배분(12.165)에 영향이 갈 수 있다. ③ 의 드라이기·포트가 그 자다.
- s(p) 재스케일은 `power_scale_std` 가 ±15% 라 대부분 창에서 2~6° 회전에
  그친다. (b) 단독 효과는 작을 것으로 본다.
- 미니PC 곡선의 유효 하한이 9.5W 인데 참값 IDLE 이 9.90W 로 겨우 안쪽이다.
  경계라 곡선의 끝값 클램프가 작동할 수 있다.
```
