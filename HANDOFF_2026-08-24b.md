# 인수인계 — 학습 PC, 2026-08-24 (오후 세션)

`HANDOFF_2026-08-24.md` 다음 세션의 기록이다. 상세 근거는
`MODEL_TRAINING_DESIGN.md` **12.39 ~ 12.61** 절에 있다.

---

## 0. 한 줄 요약

**이번 세션은 새 성능을 못 냈다. 대신 앞선 세션들의 결론 여러 개를 무너뜨리고,
그 과정에서 남은 유일한 실패를 가르는 첫 특징을 찾았다.**

- 12.38 의 "커버리지가 유령을 고쳤다" **철회** — 시드 잡음(21.78W) 안이었다
- 채점 코드 결함 하나 **수정** — 핫플 F1 을 0.10 낮게 재고 있었다
- 유령·고부하 핫플 문제가 **운영 경로에는 없다**는 것을 확정 (적응이 처리한다)
- 사용자 제안 특징 8개를 심사 — **⑦ 다단 강하가 프로젝터↔충전기를 30배로 가른다**

> **⚠ 오해하지 말 것 — 유령 전력은 실제로 해결됐다.**
> 이 문서가 "12.41~12.51 이 1단계 아티팩트를 쫓았다" 고 적은 것은 **이번 세션의
> 헛수고**를 말하는 것이고, 유령 문제 자체가 안 풀렸다는 뜻이 **아니다.**
>
> ```
> 1단계 단독 (cnn_ov1)        유령 28.81W   그중 전기포트 21.7W (75%)
> 운영 조합 (adapt_ph1+ov1)   유령  7.86W   전기포트 **0W**
> ```
>
> **2단계 무라벨 적응이 유령 포트를 완전히 없앤다.** 그림으로도 보인다
> (`results/plots_solo` 의 test_4 에서 예측 합계가 관측을 800~1000W 넘어서는데,
> `results/plots_op_s6` 에서는 사라진다). `>=1300W` 구간 핫플 재현율도
> 1단계 0.413 -> 운영 **0.998** 이다.
>
> 12.39 가 철회한 것은 **"커버리지 수정이 그 개선을 만들었다" 는 인과 주장**이지
> 개선 자체가 아니다. 남은 유령 6.49W 의 72%는 충전기(5.7W)이고 포트는 0 이다.

---

## 1. 지금 상태

### 운영점 (변경 없음)

```
저항 갈래 = results/adapt_ph1.pt   (44채널, 2단계 무라벨 적응)
SMPS 3종  = results/cnn_ov1.pt     (48채널, 1단계)
run_live --ckpt results/adapt_ph1.pt --ckpt-smps results/cnn_ov1.pt
```

`run_live` / `run_plot_timeline` 기본값은 아직 `adapt_v17` / `cnn_v17` 이다.
**이번 세션에서 운영점을 바꾸지 않았다.**

### 운영점 실력 (최근접 보간 + stride 6 으로 재채점 — 12.52)

**네 조합을 나란히 쟀다. 지배하는 조합이 없다.**

| 조합 | 유령W | 잔차W | 미니PC | 프로젝터 | 충전기 | 핫플 | 오븐 |
|---|---|---|---|---|---|---|---|
| `adapt_v17` 단독 ← **`run_live` 기본값** | 9.56 | 14.50 | 0.389 | 0.792 | 0.861 | **0.980** | 0.931 |
| `adapt_v17` + `cnn_v17` | 13.05 | 17.04 | 0.767 | 0.829 | **0.955** | 0.980 | 0.931 |
| `adapt_ph1` 단독 | **6.50** | **5.86** | 0.359 | 0.830 | 0.917 | 0.980 | 0.925 |
| **`adapt_ph1` + `cnn_ov1`** (권장) | 7.90 | 11.49 | **0.805** | **0.841** | 0.937 | 0.980 | 0.925 |

- **저항 3종은 끝났다** — 핫플 **0.980**(네 조합 전부), 오븐 0.925~0.931.
  0.838 로 보였던 것은 채점 결함이었다 (2.3절)
- 유령·잔차만 보면 `adapt_ph1` 단독이 최고인데 미니PC 가 0.359 다.
  하이브리드가 잔차(5.86 -> 11.49)를 내주고 미니PC(0.359 -> 0.805)를 산다
- **충전기만은 `cnn_v17` 이 0.955 로 아직 최고다** (권장 조합 0.937)
- ⚠ **`run_live` 기본값(`adapt_v17` 단독)이 권장 조합보다 거의 다 나쁘다** —
  미니PC 0.389 vs 0.805, 유령 9.56 vs 7.90. **기본값을 바꾸는 것을 먼저 결정할 것**

남은 약점: 미니PC 0.805 · 프로젝터 0.841 · 충전기 0.937 · 유령 7.90W(대부분 충전기).
**전이 귀속 22/41 — 오귀속 15개가 전부 SMPS 3종 맞바꿈이다** (이것이 남은 유일한 실패).

### 9초 모델(`cnn_la9` / `adapt_la9`)은 채택하지 않는다 (12.61)

같은 조건(최근접 + stride 6 + 하이브리드)으로 재채점했다.

| 조합 | 유령W | 잔차W | 미니PC | 프로젝터 | 충전기 | 핫플 | 오븐 | 전이귀속 |
|---|---|---|---|---|---|---|---|---|
| `adapt_ph1`+`cnn_ov1` (6초) | 7.90 | **11.49** | **0.805** | 0.841 | **0.937** | 0.980 | **0.925** | **22/41** |
| `adapt_la9`+`cnn_la9` (9초) | **7.69** | 13.48 | 0.786 | **0.853** | 0.913 | 0.980 | 0.921 | 20/41 |

**7개 중 5개에서 6초가 앞선다.** 1단계에서는 9초가 핫플을 0.785 -> **0.907** 로
크게 올리는데 **적응 뒤 둘 다 0.980** 이 된다 — 12.47 의 결론이 재현된다.

> ⚠ `cnn_la9` 의 **1단계** 유령이 54.0W(vs `cnn_ov1` 28.9W)인데 **9초 탓이 아니다.**
> `cnn_ov1_s1`(시드만 다름)이 50.6W 다. 셋 다 유령의 75~88%가 **전기포트**이고
> `test_7`(저항 없음)은 0.0~0.6W 다. s0 은 `test_4` 에 가두고 s1·la9 는 `test_5/6`
> 으로 번진다 — 12.41.5 의 "점화율만 실행마다 다르다" 그대로다. 25.2W 차이는
> 잡음 바닥 21.78W 를 겨우 넘고 la9 는 n=1 이다. **적응하면 양쪽 다 0 이 된다.**

`cache/train60_la9`(22GB)는 지워도 된다. 체크포인트는 남겨 둘 것 (12.61.5).

### ⚠ 저장소 상태

- `src/model/inputs.py` 는 **6초 구성**(`TARGET_LOOKAHEAD=360`, `FINE_CYCLES=600`)이다
- 체크포인트가 이제 `target_lookahead` / `fine_cycles` 를 저장하고 **로더가 검사한다**.
  9초 체크포인트(`cnn_la9`, `adapt_la9`)는 상수를 540/780 으로 바꾸지 않으면 거부된다
- 9초 자산: `cache/train60_la9`(22GB), `processed_data/holdout60_la9`
- 캐시: `train60_{ov,pf,ph,la9}` 살아 있음. `train60_{,_nohp2,_v17}` 는 지워도 된다

---

## 2. 무너진 것 — 먼저 읽을 것

### 2.1 12.38 의 커버리지 효과 철회 (12.39)

같은 캐시·같은 채널·**시드만** 바꿔 한 번 더 학습했다.

```
             유령W   잔차W  미니PC 프로젝터 충전기  핫플   오븐   합성MAE 합성F1
cnn_ov1 (s0)  28.81  39.56  0.804  0.839  0.935  0.660  0.904   2.939  0.9787
cnn_ov1_s1    50.59  48.06  0.787  0.834  0.929  0.540  0.905   2.997  0.9776
실행 간 폭    21.78   8.50  0.017  0.005  0.006  0.120  0.001   0.058  0.0011
```

**12.38 이 주장한 6.07W 가 한 설정 안의 폭 21.78W 안에 있다.** 방향이 틀렸다는
것이 아니라 이 측정으로는 가릴 수 없다는 것이다. 핫플 "부분 실패"(0.694->0.660)도
같은 이유로 철회했다 (폭 0.120).

**합성 홀드아웃은 두 시드가 구분되지 않는다** (MAE 차 0.058W, F1 0.0011).
12.38.1 의 미결정 진단은 맞고, **그 처방이 미결정을 해소하지 못했다.**

### 2.2 잡음 바닥과 시드 규율 (12.39.6, 12.39.7)

```
1단계 실측 유령의 잡음 바닥 = 21.78W  (n=2 라 하한)
n=1 로 읽어도 되는 것: 합성 홀드아웃, 오븐, 프로젝터, 미니PC, 충전기 (폭 0.001~0.017)
n=1 로 못 읽는 것:     유령(21.78W), 핫플 F1(0.120)
```

**캐시를 쓰면 시드는 학습 데이터를 하나도 안 바꾼다** — 가중치 초기화와 배치 순서뿐이다.
그래서 21.78W 는 데이터 분산이 아니라 최적화 경로 분산이다. 그리고 창을 더 만들어도
안 준다 — 목표 구성 9,795창이 **오븐 활성화 4개 × 핫플 3개 = 12가지 조합의 복사본**이다.
**근본은 시드가 아니라 원본이다.**

### 2.3 채점 코드 결함 — 핫플 F1 을 0.10 낮게 재고 있었다 (12.52)

`upsample_to_cycles` 가 0.5초 예측을 **앞으로 채웠다.** 핫플 릴레이 펄스가
1.0~1.3초라 모든 펄스가 평균 0.25초 뒤로 밀리는 **편향** 오차가 났다.

```
adapt_ph1 핫플 F1   앞채움/stride30 0.838  ->  최근접/stride30 **0.933**  ->  stride6 0.965
다른 기기는 전부 불변 (유령 6.49->6.50, 오븐 0.925, 프로젝터 0.829->0.830)
```

최근접 보간으로 고쳤다. **12.38~12.51 의 핫플 F1 은 전부 약 0.10 낮다** —
같은 설정끼리 비교는 유효하나 절대값 인용 금지.

### 2.4 1단계 지표로 문제를 고르면 안 된다 (12.47, 12.51.3, 12.50)

**세 문제가 1단계에만 있고 운영 경로에는 없었다.**

```
유령 포트          1단계 28.8~53.5W  ->  운영 7.86W (포트 몫 0)
고부하 핫플 재현율   1단계 0.413        ->  운영 0.998
미설명 잔여        1단계 +286W        ->  운영 4.92W (규칙이 1창만 발화)
```

**2단계 무라벨 적응이 다 처리한다.** 12.41~12.51 이 최적화한 것이 전부 그것이었다.
`run_gate_check` 로 1단계 체크포인트만 채점하고 문제를 고르지 말 것 —
**운영 조합으로 먼저 재라.**

### 2.5 그 밖에 철회·정정된 것

| 절 | 내용 |
|---|---|
| 12.40 | **취소.** 과도 채널 재시험 불필요 (인수인계 1순위였다). 12.54 참조 |
| 12.45 | 룩어헤드 9초 — 1단계 핫플 0.660->0.782 인데 **적응 뒤 0.838->0.837 로 사라진다** |
| 12.48 | "핫플 = 리플 지문" **부분 철회** — 저부하에서만 유효. 평활 조작이 타깃 크기와 교락 |
| 12.49 | **철회.** `|r|` 로 쟀는데 부호로 재야 했다. 그리고 `P>=1300` 조건이 순환이었다 |
| 12.56 | **철회.** "충전기 48.5분 표류를 쓰자" — 60초 창에서 0.62W, `test_7` 에서 0.19W |
| 12.57.3 | **철회.** ⑦ "복합에서 관측 불가" — 12.59 가 뒤집었다 |

---

## 3. 새로 세운 것

### 3.1 특징 심사 기준 (12.53.7) — 이제 이 자를 먼저 댄다

```
그 차수의 |Δ| 절대 크기가 복합 널의 p95 보다 충분히 큰가?
   널 p95 미만        -> 격리에서 아무리 갈려도 못 쓴다. 버린다
   널 p95 의 2~5배    -> 아슬아슬. 복합에서 직접 확인해야 한다
   널 p95 의 10배 이상 -> 그 다음에 분리비를 본다

복합 널 (test_5/6/7, 426 표본):
  |ΔP| 중앙 1.65 / p95 13.02 W        |ΔQ1| 중앙 0.36 / p95 2.77 var
  |ΔI2| 1.21 / 12.26 mA               |ΔI3| 8.82 / 58.57 mA
  |ΔI9| 9.25 / 63.91 mA               |ΔI13| 6.71 / 31.74 mA
```

`run_delta_feature_probe --audit` 이 이 표를 찍는다. **널은 건물·계측기가 바뀌면
다시 재야 한다.** 차분 자체는 배경을 완벽히 지운다 (복합 전이가 격리 지문과
cos 0.95, 크기 1.0배). 못 지우는 것은 **계측 잡음(√2배로 늘어난다)과 다른 기기의
동시 움직임**이다.

### 3.2 특징 8개 심사 결과 (12.53~12.59)

| | 격리 | 복합 | 판정 |
|---|---|---|---|
| ch48~50 \|I3\| 과도 | — | AUC 0.484, 분리비<1 | **반증** (12.54) |
| ① \|ΔI2\| | 2.28 | 0.05~0.48 | 넣지 않음 |
| ② \|ΔI13\|/\|ΔI3\| | 2.12 | 0.35~1.27 | 넣지 않음 (기존 ch47 이 이미 2.07) |
| ④ ΔQ1/ΔP | 1.18~2.11 | 0.13~0.81 | 넣지 않음 |
| ⑤ Δφ3 (SMPS 내부) | 0.01~0.11 | 0.07~0.36 | 넣지 않음 |
| ⑤ Δφ3 (종족 축) | 166° / R=1.00 | — | 이미 되는 판별 |
| ⑧ ΔP/ΔS | **4.69** | 0.44 | 넣지 않음 (부하 의존 의심) |
| ③ \|ΔI3\|/ΔP | 12.52 | AUC 0.798 | **넣을 만함** |
| ⑥ σP (0.1~0.2초) | 2.29 | **AUC 0.78~0.95** | **유망** |
| **⑦ 다단 강하 2단** | 정확 | **30배, 겹침 0** | **가장 유망** |

물리 주장은 **8개 중 7개가 정확했다.** 걸러진 이유는 대부분 "물리는 맞는데 그
물리량의 절대 크기가 계측 잡음 근처" 였다.

---

## 4. 다음에 할 것 (우선순위 순)

### 4.0 파형 암기 시험 ← **1순위** (12.58, 12.60.3)

**12.60 이 순서를 바꿨다.** ⑦ 의 정보는 실측·합성 양쪽에 다 있다 (합성 창 8/8 에서
45~49W -> 4.0W 3~5초 유지 -> 0). 그런데 **합성에서 그 쌍이 이미 F1 0.985~1.000 이라
압력이 없다.** 채널만 넣으면 12.20.2 가 반복된다 (AUC 0.922 특징을 꽂아 줘도 안 썼다).

```bash
python -m src.run_build_traincache --out cache/train60_holdrec --windows 300000 \
    --split train --seed 0 --workers 8 \
    --exclude-activation-files '{"laptop_charger":["laptop_charger_2"],"beam_projector":["beam_projector_2"]}'
python -m src.run_train_cnn --epochs 300 --epoch-windows 50000 --batch 512 \
    --w-over 0 --eval-every 10 --cache cache/train60_holdrec --tag cnn_holdrec
```

뺀 녹화의 창에서 그 쌍 F1 이 0.985 아래로 크게 떨어지면 **암기 확정** ->
그러면 ⑦ 채널을 **암기를 막은 조건에서** 넣는다 (그때 압력과 경로가 함께 생긴다).
안 떨어지면 격차는 중첩·결합 모델링이다 (`apply_cross_appliance_coupling`).

약 50분 (캐시 17분 + 학습 30분).

### 4.1 ⑦ 다단 강하 채널 ← **2순위** (12.59, 12.60)

**프로젝터↔충전기를 실제로 가르는 첫 특징이다.** 계단에 스냅하고 주변이 조용한
전이만 보면:

```
기기       n   1단 중앙   2단 값들
충전기     4    44.2W    0.02  −0.02  0.01  −0.10      <- 정확히 0
프로젝터   6    41.6W    5.41  3.06  3.03  3.15  14.38  <- 3W 이상
```

1단은 41~44W 로 구별 불가인데 **2단은 30배 차이에 겹침이 0** 이다.
`>=1300W` 가 아니라 저부하 SMPS 문제이므로 **운영에 그대로 남은 실패**다.

**형태**: `ch51 = asinh((P − P를 4.5초 앞으로 민 것) / RIPPLE_SCALE)` — 전방 차분.
세밀 갈래가 `[타깃−3.98초, 타깃+6.00초]` 를 덮으므로 정보는 이미 입력에 있다.
다만 타깃 탭 수용영역이 ±1.56초라 **닿는 경로가 없다** (12.44.2). 그래서 명시 채널.

**합성 팬 기착은 확인했다 — 재현한다** (12.60.1, 창 8/8). 정보와 경로는 해결
가능하다. **문제는 압력이다** — 그래서 4.0 을 먼저 한다.

판정 기준은 12.59.5 에 적혀 있다.

### 4.2 ⑥ σP 채널 (12.57)

```
복합 "충전기 ON" 판별 AUC (창 길이별)
           0.1s   0.2s   0.5s    1s
test_7    0.935  0.945  0.947  0.943
test_6    0.919  0.923  0.891  0.851
test_5    0.812  0.778  0.673  0.598
```

**핫플 릴레이는 0.5초 창부터 폭발한다** (격리 σP 0.39 -> 83 -> 208W). 충전기는
0.1~0.2초에서 이미 그 구간 최대다. **기존 `RIPPLE_HALF_SHORT=30`(±0.5초)은
핫플을 겨냥한 값이라 충전기에는 무용하다.**

합성이 요동을 재현하는 것은 확인했다 (합성 3.03 vs 실측 3.30 @0.2초).
**다만 합성 홀드아웃에서 그 쌍이 이미 F1 0.985~1.000 이라 안 쓸 수 있다** (12.20.2 의 함정).
4.1 과 함께 넣고 4.1 의 기준으로 판정하는 것이 낫다.

### 4.3 ③ \|ΔI3\|/ΔP 채널 (12.53)

격리 분리비 12.52, 복합 AUC 0.798. 저항 3종(핫플 vs 오븐·포트)을 가른다.
**운영에서는 이미 풀린 문제라 실질 용도는 1단계 미결정(시드 폭 21.78W) 축소다.**
4.1/4.2 보다 뒤에 둔다.

### 4.4 (4.0 으로 올라감) 파형 암기 시험 — 아래는 근거 (12.58)

합성은 프로젝터↔충전기를 **어떤 조건에서도** 푼다 (동시 ON, |전력차|<5W 에서도
F1 1.000). 커버리지 네 가설이 다 반증됐다. 남은 후보가 **파형 암기**다 —
풀에 충전기 녹화 2개·프로젝터 2개뿐이다.

```bash
python -m src.run_build_traincache --out cache/train60_holdrec --windows 300000 \
    --split train --seed 0 --workers 8 \
    --exclude-activation-files '{"laptop_charger":["laptop_charger_2"],"beam_projector":["beam_projector_2"]}'
```

뺀 녹화의 창에서 그 쌍의 F1 이 0.985 아래로 떨어지면 **암기 확정**이고, 처방은
채널도 커버리지도 아니라 **녹화 수**다.

### 4.5 다음 측정 때 반드시 넣을 것

1. **프로젝터를 끄고 5분 이상 계속 녹화.** 기착 자체는 합성에 들어 있으나(12.60.1)
   녹화가 소등 뒤 6.3초 / 10.4초 만에 끝나 **두 번째 계단(5.4->2.3W) 뒤가 없다**
2. **저항 부하 없는 녹화 2~3개 더.** `test_7` 이 유일해서 SMPS 실패의 표본이
   전이 13개뿐이다. 4.1 의 판정도 여기 걸려 있다
3. **전기포트가 켜지는 복합 녹화.** 비봉인 6파일에 하나도 없다 (12.41.3).
   12.43 지속시간 규칙의 오기각 비용도 못 쟀다
4. **오븐 활성화 5개 이상, 핫플 5개 이상.** 지금 4개/3개 = 12가지 조합이 천장이다 (12.39.7)
5. (기존) 미니PC 단독 작업 구간, 오븐·핫플 동시 장시간

---

## 5. 도구 — 이번에 추가된 것

```
run_summarize_gate         gate_check 산출물 -> 인수인계 표. --by-seed 로 실행 간 폭
run_verify_cache_prefix    채널만 늘린 캐시의 앞 N채널 바이트 일치 검증
run_transition_probe       전이 귀속 (12.35.2 재현). 정확/오귀속/미검출 구분
run_seed_variance_probe    시드 분산의 소재 — 구간·구성·기기별 분해
run_ablation_probe         입력 절제 (광역=0, ch36~37=0)
run_postproc_probe         포트 지속시간 후처리
run_sumfix_probe           합 정합성 후처리 (반증)
run_fillgap_probe          미설명 잔여 메우기 (1단계에서만 듣는다)
run_subtraction_probe      뺄셈 가설 — 성분을 직접 조작
run_ripple_gap_probe       리플 대비 (12.49, 철회됨)
run_delta_feature_probe    차분 특징 ①②③ + `--audit` 로 널 표
run_dq_dphi_probe          ④ ΔQ1/ΔP · ⑤ Δφ3
run_steady_feature_probe   ⑥ σP · ⑦ 다단 강하 · ⑧ ΔP/ΔS
```

### 고쳐진 것

- `upsample_to_cycles` **최근접**으로 (12.52). 핫플 F1 이 0.838 -> 0.933
- 체크포인트에 `target_lookahead` / `fine_cycles` 저장 + 로더 가드 (12.45.3).
  `run_gate_check.assert_target_config` 를 `run_adapt --init`, `run_reeval` 도 쓴다
- `synthesizer.DEFAULT_TARGET_LOOKAHEAD_CYCLES` 가 `inputs.TARGET_LOOKAHEAD` 를
  **import** 한다. 두 곳에 따로 두고 테스트로만 묶었더니 어긋나 캐시 16분을 버렸다
- `run_train_cnn --holdout` 추가 + 홀드아웃 `target_index` 가드
- `forward_file` 이 `obs_harm` 를 함께 돌려준다

---

## 6. 방법론 — 이번 세션이 비싸게 배운 것

이번 세션에 **측정이 다섯 번 틀렸고 세 번은 사용자가 물어서 잡았다.** 공통 원인과
규칙을 적어 둔다.

1. **비율·대비를 보고하기 전에 (a) 두 집단의 표본 수, (b) 마스크 정의를 나란히
   찍는다.** 12.49 는 실측·합성에 다른 마스크를 썼고, AUC 0.568 을 표본 223개로 냈다
2. **요약 통계를 쓰기 전에 물리적 의미와 대조한다.** `|r|` 을 "판별 대비" 라 부르기
   전에 통전과 휴지가 각각 어느 **부호**인지 봤어야 했다 (12.50)
3. **인용한 수치는 내가 쓰려는 시간 규모로 환산한 뒤에 근거로 삼는다.**
   "48.5분 67->33W" 를 60초로 환산하면 0.62W 다 (12.56)
4. **"안 보인다" 고 쓸 때는 "무엇으로 어떻게 봤는데 안 보인다" 를 함께 적는다.**
   12.57.3 은 측정 설계의 결론을 현상의 결론으로 적었다 (12.59)
5. **창은 사건 간격에 맞춘다.** 핫플 펄스 1초에 ±5초 창을 쓰면 앞뒤가 둘 다 여러
   펄스를 걸친다 (12.53.2). 라벨 시각은 최대 5.5초 어긋나므로 계단에 스냅한다
6. **1단계 지표로 문제를 고르지 않는다.** 운영 조합으로 먼저 잰다 (2.4절)

---

## 7. 실행 메모

### ⚠ 시스템 — 오늘 두 번 멈췄다

```
09:02  KERNEL_SECURITY_CHECK_FAILURE (0x139)  캐시 빌드 중 (이전 세션)
13:25  하드 행. BugcheckCode=0, 덤프 없음, WHEA·디스크 오류 없음, SSD Healthy
10:10  nvlddmkm 153 오류 x2  <- CUDA 학습 프로세스를 taskkill /F 한 시각과 일치
```

- **CUDA 프로세스에 `taskkill /F` 를 쓰지 말 것.** 드라이버가 나빠진다
- **실행 중인 스크립트 파일을 덮어쓰지 말 것.** bash 가 오프셋으로 읽어 중복 실행된다
  (그 수습으로 taskkill 을 쓴 것이 위 문제의 시작이었다)
- NVIDIA 드라이버 클린 재설치, `mdsched.exe` 메모리 검사 권장
- 무거운 작업 전에 커밋. 결과가 나오는 즉시 커밋

### 자주 쓰는 명령

```bash
# 운영 조합 채점 (이것으로 문제를 고른다)
python -m src.run_gate_check --ckpt results/adapt_ph1.pt --out results/gate_check_op.json
python -m src.run_summarize_gate results/gate_check_op.json

# 전이 귀속 (SMPS 실패의 주 지표)
python -m src.run_transition_probe --ckpt results/adapt_ph1.pt --ckpt-smps results/cnn_ov1.pt

# 특징 심사 — 널 표부터
python -m src.run_delta_feature_probe --audit

# 운영 조합 그리기 (핫플은 stride 6 으로)
python -m src.run_plot_timeline --ckpt results/adapt_ph1.pt \
    --ckpt-smps results/cnn_ov1.pt --stride 6 --tag op --out results/plots_op

# 9초 체크포인트를 쓰려면 inputs.py 를 540/780 으로 바꾸고
python -m src.run_gate_check --ckpt results/adapt_la9.pt   # 안 바꾸면 가드가 거부한다
```
