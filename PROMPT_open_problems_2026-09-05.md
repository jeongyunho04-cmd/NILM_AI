# 회로 모델 / FCM — 지금 막혀 있는 것, 그 원인과 근거 (2026-09-05)

## 0. 이 문서는

60Hz 고조파 페이저로 가전을 분해하는 NILM 시스템이다. 합성 학습자료의 현실성을 올리려고
SMPS 를 **회로 모델**(PFC 없는 capacitor-input 정류기)로 시뮬레이션하고, 여러 기기가 공유
선로 임피던스로 **결합**되는 것까지 재현하려 한다.

`CIRCUIT_FCM_GUIDE.md`(2026-09 개정) · `fcm.py` · `fcm_check.py` 를 받아 저장소 규약으로 다시
만들고, 장소 C 원시 파형 스냅샷 18개(40주기 × 256표본 @ 15360Hz)로 전부 검증했다.
**가이드의 방향은 맞았고 대부분 재현됐다.** 이 문서는 그 다음에 남은 것만 적는다.

각 문제에 **증상 / 근거(어떻게 알았나) / 원인(기각한 가설 포함) / 필요한 것** 을 붙였다.
읽는 사람에게 바라는 것: **§2 의 문제 하나를 골라 §4 기준으로 풀어 달라.** P1 은 펌웨어
상수 하나 문제이고 지금 가장 크게 막고 있다.

---

## 1. 이미 선 것 — 다시 하지 말 것

**회로 모델은 잘 맞는다.** 장소 C 원시 파형(계측 배경 뺌)에 v3 토폴로지로 다점 동시 적합:

| 기기 | C_dc | R | L | Cx | rd | 훈련 RMS(h≤15) | LOO |
|---|---|---|---|---|---|---|---|
| laptop_charger | 66.6 µF | 3.57 Ω | 781 µH | 0.120 µF | 0.98 Ω | 4.6% | 5.4% |
| beam_projector | 53.1 µF | 5.23 Ω | 534 µH | 0.370 µF | 0.99 Ω | 1.4% | — |
| minipc | 46.7 µF | 2.71 Ω | 1028 µH | 0.304 µF | 6.67 Ω | 4.2% | 4.4% |

재현 바닥(주기 산포/√40)이 1.2~3.0% 이므로 바닥에 근접했다.

**적합에 반드시 필요한 네 가지** — 하나라도 빼면 무너진다. 넷 다 실측으로 확인했다:

1. **전압 반파 대칭화.** 근거: 실측 전압이 +352/−323V 로 비대칭이다. 그냥 넣으면 캡이 양의
   피크로 충전된 뒤 음 반주기(323V)에서 도통 자체가 불가능해 시뮬이 반파 전류를 낸다.
   빼고 맞추면 RMS 가 **55~95%** 에서 멈추고 R·L 이 경계(40Ω, 8mH)에 붙는다. 넣으면 5~14%.
2. **계측 RC 를 순방향 모델에.** 세 ADC 입력이 1kΩ·100nF(fc 1591.55Hz)를 지난다. 근거:
   프로젝터 실측 펄스 1.18A/35° 대 필터 없는 시뮬 2.47A/21°. 60Hz 에서는 2.16° 라 무시해도
   되지만 펄스 대역(1~2kHz)에서 크기를 0.6~0.8배로 깎는다.
3. **다점 동시 적합.** 한 점으로 맞추면 rd 가 그 점을 흡수한다 (가이드 §10.5 와 같은 관찰).
4. **계측계 배경만 뺀다** — `noise_noselfpower_C` 의 1.70W / |I1| 7.3mA ∠+10.9°.
   ⚠ "기기가 꺼진 창"을 배경으로 쓰면 안 된다. 근거:
   ```
                          전력    |I1|          판정
   noise_noselfpower_C   1.70W   7.3mA ∠+10.9°  진짜 배경 (기기 없음)
   laptop_charger_5C     2.10W   7.6mA ∠+10.2°  꺼짐 창 = 배경과 같다 (정말 빠져 있었다)
   minipc_4C             2.62W  30.4mA ∠+68.9°  배경의 4배, 강한 용량성
   beam_projector_4C     3.12W  31.1mA ∠+65.4°  같음
   ```
   미니PC·프로젝터는 꺼져도 어댑터가 꽂힌 채라 30·31mA 는 **그 기기 자신의 X-cap 전류**다 —
   원시로 잰 Cx(0.30·0.37µF)가 237V·60Hz 에서 내는 28.6·34.1mA 와 맞고 위상도 용량성이다.
   그걸 빼면 모델이 Cx 를 못 찾는다 (2Hz 적합에서 미니PC Cx 가 하한 0.010µF 로 갔다.
   안 뺀 판은 0.453µF). 제대로 고치자 저부하 잔차가 11.3% → 5.2% 로 떨어졌다.

**결합도 선다.** 조합 원시 스냅샷 6개에서 각 기기를 **조합의 단자 전압**으로 시뮬해 더하면
(조합에 맞춘 자유 파라미터 0개):

```
                단순 중첩   모델(조합 V_term)   모델(단독 V_term)
6개 조합       12.7~15.9%      3.6~8.8%          12.8~15.7%
```
**[C] 가 [A] 와 같다는 것이 핵심 근거다** — 개선이 전적으로 단자 전압에서 온다. 차수별로
단순 중첩은 h9~h11 에서 28~64% 과다하고 모델은 4~6% 다. 가이드 §6.1 의 고정점 설계가
실측으로 섰다.

**가이드 §5 도 재현됐다.** 대각 |Y_hh|·V1/|I1| 이 h1 0.09~0.19 → h7 26~37 (가이드 0.11~0.26 →
23~31), 최대 비대각이 h11~h13 (30~51, 가이드 26~37). 선형화 `I ≈ J − Y·ΔV` 는 V3 를 **1%** 만
흔들어도 h9 27~32%, h13 47~71% 틀린다 (가이드 27%/70%) → **Y 는 생성에 쓰지 않는다.**

**정정된 것 둘**: 프로젝터 Cx 는 0.240µF 이 아니라 **0.379µF** — 근거는 비도통 구간 직접측정
0.383µF(상관 0.987)이 자유 적합 0.379 와 일치하고, Cx 를 직접측정에 고정해도 훈련 RMS 가
변하지 않는다는 것(1.42% → 1.42%). 그리고 미니PC 에 v5(선로측 X-cap+NTC)는 **필요 없다** —
h15 에서도 ωL 15.8Ω 대 X_c 553Ω 이라 X-cap 위치가 자료로 구분되지 않는다. v3 로 4.2% 다.

---

## 2. 막혀 있는 것

### P1 (최우선) 전압 고조파 15개로는 결합 모델이 서지 않는다 — h21 이 필요하다

**증상.** 실시간 입력은 고조파 15개다. 그것만으로 소스 전압을 만들어 시뮬을 돌리면 결합
재현이 6.3~13.9% 로, 단순 중첩(12.7~15.9%)보다 나을 게 없어진다.

**근거.** 적합과 생성에 **같은 대역**을 걸고 훑었다 (대역마다 기기 재적합 + 조합 재현):
```
소스 대역   단독 적합 RMS (충전기/프로젝터/미니PC)   조합 재현 (6개)
  h15            7.1%   2.3%   6.1%                6.3  8.5 13.9 13.9 10.9 10.7 %
  h21            6.3%   1.3%   7.2%                3.3  2.4  4.2  4.7  7.3  7.4 %   <- 여기서 선다
  h31            4.8%   1.7%   4.3%                2.9  3.5  5.1  5.2  8.4  8.3 %
  전체 파형      4.6%   1.4%   4.2%                3.5  3.7  5.5  5.4  8.7  8.5 %
```
h15 적합은 파라미터도 망가진다 (충전기 C_dc 66.5→99.7µF, L 784→43µH; 미니PC L 1020→301µH) —
자유도가 절단 편향을 흡수한 것이다.

**원인 (기전이 확인됐다).** 장소 C 전압의 h17 이상은 V1 의 **0.27%**(실측)/0.40%(역RC 후)로
작다. 그런데 h15 로 자르면 파형 피크가 **+0.94V** 오르고 **도통각이 38.8° → 36.9°, 2°** 바뀐다.
```
  h15 까지 자른 v: peak 337.41 (전체 336.47, 차 +0.94V)  파형 RMS 차 0.94V   도통각 36.9°
  h31 까지        : peak 336.78 (차 +0.31V)              RMS 차 0.53V        도통각 39.0°
  h63 까지        : peak 336.50 (차 +0.03V)              RMS 차 0.18V        도통각 38.7°
  전체            : peak 336.47                                              도통각 38.8°
```
도통각이 전압 왜곡에 **초선형**이라 그 2° 가 h9~h15 에서 크기 14%·위상 20~37° 로 증폭된다.
이건 §1 의 선형화 실패(V3 1% → h9 27~32%)와 **같은 물리**다 — 전압의 작은 변화가 도통 창의
시작·끝 시각을 옮기고, 짧은 펄스의 에지 시각은 고차 위상에 h 배로 실린다.

**기각한 설명.** "역RC 로 벗기면서 잡음을 증폭한 것 아닌가" — 아니다. RC 를 벗기기 **전**의
실측 전압에도 h17 이상이 이미 0.269% 있다 (벗긴 뒤 0.396%). 절반 이상이 실재 신호다.

> **요청 A (펌웨어):** 홀수 고조파를 **h21(1260Hz)까지** 발행. 지금 `N_HARM 15`.
> h31 까지 갈 필요 없다 (표에서 h31 이 h21 보다 낫지 않다).
> **대안 (펌웨어를 못 바꿀 때):** V15 에서 전체 파형을 복원하는 사전분포. 계통 전압은 집합
> 정류부하 때문에 체계적으로 꼭대기가 평평하다 — 한두 파라미터로 h17~h31 을 예측할 수 있는지
> 검토해 달라. **아직 아무도 시도하지 않았다.**

### P2 두 계측 경로가 h11~h15 에서 20~30% 갈린다

**증상.** 같은 기기·같은 전력인데 원시 스트림(표본 → FFT)과 2Hz 발행 고조파가 다르다.

**근거.** 원시에 펌웨어와 같은 RC 크기 보상을 걸고, 양쪽에서 계측 배경을 빼고, 홀수차만:
```
프로젝터 49W (가장 깨끗 — 위상 오프셋 0, 레인지 혼합 없음)
  차수         h1     h3     h5     h7     h9    h11    h13    h15
  |I| 2Hz/원시 1.016  1.004  0.983  0.964  0.912  0.838  0.785  0.873
  Δ위상 [°]     0.8    0.3    1.0    0.2    0.2    0.5   13.4   11.9
충전기 70W    0.977  0.993  1.010  1.048  1.132  1.258  1.288  1.199   <- 부호가 반대
  Δ위상 [°]     0.5    1.1    2.3    2.8    2.9   −0.9   −9.8  −14.6
```
h1~h9 는 2~5%·3° 안에서 맞는다. **h11~h15 에서만 갈리고 부호가 기기마다 반대다.**

**기각한 원인 (전부 수치로 배제).**
```
표본별 중앙값이 펄스 지터를 뭉갠다   주기별 DFT 로 다시 해도 0.4~2.7% 차이뿐 (필요한 건 20~30%)
주기 간 위상 지터의 복소 평균 감쇠   스냅샷 안에서 |복소평균|/|크기평균| = 0.993~1.000
2Hz 프레임 간 위상 드리프트         복소 중앙값/크기 중앙값 = 0.94~1.00 (steady_signature 가 쓰는 값)
RC 크기 보상 불일치                 양쪽에 같은 계수 sqrt(1+(60h/1591.55)^2) 를 걸었다
```
**남은 후보.** 스트리밍되는 `i_a` 와 펌웨어 DFT 가 보는 버퍼가 다를 가능성, 레인지 이어
붙이기(P5), 그리고 두 녹화 시점의 기기 상태 차 (h11~h15 는 총전류의 2~15% 라 작다).
이 대역은 우리 모델 잔차와 운영점 판별이 사는 대역과 정확히 겹친다. 당분간 h11~h15 는
원시(표본 → FFT 로 감사 가능한 경로)를 기준으로 삼고 있다.

> **요청 B (펌웨어):** **같은 순간**의 원시 스트림과 2Hz 고조파를 함께 뽑아 비교해 달라.
> 지금은 다른 시각의 두 녹화를 비교하는 것이라 기기 상태 차를 못 배제한다.

### P3 HIGH 레인지 저항 고조파 인공물 0.5~3%

**증상.** 순저항(전기포트 1485W)을 물리면 `I_h = V_h/R` 이어야 하는데 HIGH 레인지에서
h3 에서 3%, h5~h15 에서 0.3~1% 어긋난다.

**근거·기각.** 이것이 **전압 채널의 오차가 아니라 전류 채널의 인공물**이라는 근거 둘:
(a) 포트가 말하는 비 `c_h = Y_h·R` 를 전압 보정으로 걸고 SMPS 를 재피팅하면 손실이 **2~16배
악화**된다. (b) 한 기기에서 차수별 복소 채널 보정을 피팅해 다른 기기에 고정하면 안 옮는다.
또 같은 장소·같은 날의 두 저항(포트·핫플)이 h3~h7 에서 서로 갈린다 — 저항을 전압계로 쓰는
방법 자체가 이 바닥 위에 있다.

**막는 것.** (a) 저항 부하로 전압 채널을 검정하는 방법, (b) 고차 선로 임피던스 측정(P4).

### P4 선로 임피던스 — |Z₁| 만 잰다. ∠Z₁ 도, 고차도 못 잰다

**근거.** 포트 on/off 계단 **78개**로 `Z(h) = −ΔV/ΔI` (계단 앞뒤 5초 창, ON 은 range==1,
OFF 는 range==0):
```
차수      h1      h3      h5      h7      h9     h11     h13     h15
|Z|    0.520   1.361   3.694   4.483   0.597   1.948   9.703   1.189  Ω
∠Z     −1.0   141.5    61.8    11.3  −172.8   −64.9   −65.5    95.3  °
산포     19%     37%     22%     25%    243%    332%    338%     56%
```
**|Z₁| = 0.520 Ω 는 믿는다** (78계단, 산포 19%). ΔI₁ 이 6.4A 라 신호 대 표류 비가 좋다
(조합 스냅샷 차분으로 재려던 첫 시도는 ΔI₁ 이 0.05~0.14A 뿐이라 Z·ΔI ≈ 0.05V 인데 녹화 간
3~4분의 계통 표류가 0.1~1.3V 여서 쌍마다 R 이 −1.9~2.6Ω 로 흩어졌다 — 산포 51%).

**그러나 두 가지를 못 잰다.**
- **∠Z₁.** 관례상 `vhdeg1 ≡ 0` 이라 V1 이 실수여서 ΔV₁ 도 실수다. 그래서 ∠Z₁ 은
  −∠ΔI₁(포트가 저항이라 ≈0)의 반영일 뿐이다. → **선로 인덕턴스가 미측정이다.** 생성기
  민감도에서 L=400µH 면 총전류가 13% 움직이므로(R 은 0~1.5Ω 에서 4~9%) 무시할 수 없다.
- **고차.** h3~h7 은 78계단에서 산포 22~37% 로 **재현되는데도 수동 임피던스가 아니다.**
  ∠Z₃ = 141° 는 실수부가 음수라는 뜻이라 R+jωL 이 낼 수 없다. 산수로 보면:
  ```
  ΔI₃ 실측 0.23A          (순저항 예상 V₃/R = 9.5/36 = 0.26A — 같은 크기)
  ΔV₃ = |Z₃|·|ΔI₃| = 0.31V  (V₃ 9.5V 의 3.3%)
  수동 0.52Ω 이면          0.12V  (1.3%)  -> 관측이 2.6배 크고 방향도 다르다
  ```
  둘이 섞였다. **분모**에 P3 의 HIGH 인공물 (0.5~3% × 6.4A = 32~190mA 로 ΔI₃ 0.23A 와 같은
  자릿수). **분자**에 상류 부하의 응답 — 6.4A 를 끌면 계통이 3.4V 내려가고 건물의 다른
  정류부하들이 그만큼 도통을 바꿔 V₃~V₇ 를 되민다. 즉 이 비는 "그 부하에 대한 그 장소의
  응답"이지 다른 ΔI 스펙트럼으로 옮길 수 있는 Z 가 아니다. h9 이상은 산포 240~340% 로 잡음.

> **요청 C (자료, 장소 C):** **LOW 레인지 안에 머무는 큰 비선형 부하**를 한 녹화 안에서
> 여러 번 켜고 꺼 달라 (펄스 피크 < 2.2A 여야 하므로 rms 0.5A 이하의 SMPS 를 여러 대 모아
> 한꺼번에 스위치하는 식). 포트는 HIGH 로 넘어가고 단일 SMPS 는 ΔI 가 작아 둘 다 안 된다.
> **요청 D (자료):** 포트 on/off 를 **원시 스냅샷**으로. P3 의 HIGH 인공물을 표본 수준에서
> 직접 확인할 수 있다 (지금은 2Hz 고조파로만 봤다).

### P5 LOW/HIGH 이어 붙이기가 한 파형에 두 위상 응답을 섞는다

**증상·원인.** SMPS 펄스 피크가 LOW 포화(2.2A)를 넘으면 표본마다 두 경로를 이어 붙인다.
두 경로의 위상 응답 차는 **LOW 경로의 안티앨리어싱 RC 한 극**이고 (1kΩ·100nF → 60Hz 에서
2.16°), 펌웨어가 잰 LOW/HIGH 교정값 차 2.18° 와 정확히 같다. 그 교정은 **사이클 단위**로
걸리므로 한 파형 안에 섞인 것은 못 고친다 — 피크는 HIGH 위상, 꼬리는 LOW 위상이 된다.

**근거.** 범위혼합 조합(72W 충전기, 3종 동시)의 잔차가 같은 전력대 깨끗한 점의 **1.7배**다
(5.9~6.1% 대 3.4%). 또 겹친 3종의 펄스 피크가 3.4A 로 LOW 포화를 넘는다.

> **요청 E (펌웨어):** 두 경로의 위상 응답을 **표본을 이어 붙이기 전에** 맞춰 달라.

### P6 2Hz 파일의 세션별 전류 위상 오프셋

**근거.** 원시는 v 와 i 가 같은 표본 인덱스에서 나오므로 V–I 정렬이 참값이다. 2Hz 의
`ihdeg` 와 대조하면 파일마다 **순수 지연**이 실려 있다:
```
laptop_charger_5C   −9.29 ~ −9.70 °/차수 = −6.6 ~ −6.9 표본   선형 잔차 0.3~0.7° -> 순수 지연
beam_projector_4C   +0.07 °/차수 ≈ 0                          잔차 0.7~1.0°
minipc_4C           −0.72 (22시 원시) / −2.1~2.4 (23시 원시)  잔차 1.6~2.3° -> 순수 지연이 아니다
```
충전기는 +9.5°/차수로 보정했다 (옛 값 10.8 은 대기전류 위상으로 추정한 것이라 1.3°/차수
과교정 — h15 에서 19°). **미니PC 는 원시 세션끼리도 1표본 다르고 잔차가 커서 값을 정할 수
없다** → 보정하지 않았다. h13 에서 15~30° 어긋날 수 있다.

**원인은 못 가른다.** "부하 없이 USER 버튼 위상 교정을 눌렀다" 와 "DFT 창이 k표본 어긋났다"
는 둘 다 `−k×h` 를 내므로 이 자료로 구분되지 않는다. 값만 고쳤다.

> **요청 F (자료):** `minipc_4C` 재녹화. 그리고 **모든 세션 첫 30초에 순저항 부하 on/off 1회**
> 를 넣어 `ihdeg1 ≈ 0` 으로 세션 정렬을 자가진단하는 절차 (가이드 §10.9 와 같은 취지).

### P7 남은 모델 잔차 — 저부하와 R–rd 축퇴

- **충전기 19W 가 최악으로 남는다** (훈련 5.9%, LOO 9.3% — 다른 동작점의 2~3배). 계측 배경을
  제대로 빼자 저부하 잔차가 대부분 사라졌지만(미니PC 9W 11.3→5.2%) 이것은 남았다.
  후보: 버스트/스킵 모드, 도통 표본 20개 안에서 R–L 축퇴, rd 의 부하 의존.
- **미니PC 의 R–rd 축퇴.** 배경을 빼자 R 9.88→2.71Ω, rd 1.63→6.67Ω 로 **맞바뀌었다**
  (도통 중 합은 11.5→9.4 로 거의 같다). 둘은 도통 구간에서 직렬로만 보이므로 자료가 못
  가른다. rd 6.67Ω 은 물리 범위(0~5Ω) 밖이다. 하나를 물리값에 고정해야 한다.

### P8 검정할 수 없는 것 — 장소 A·B

측정기와 기기를 다른 장소로 가져갈 수 없다(사용자 제약). 장소 A 는 학습 풀의 기준 장소인데
그 **전압 파형**(vhdeg)이 없다 — 옛 펌웨어 녹화라 크기만 있다. 그래서 장소 전이(C→A) 판정이
열려 있고, 실측 전달비의 분모가 파형 미상의 장소 A 곡선이라 시뮬(이상 정현파 분모)과 비교할
때 방향·추세만 본다.

---

## 3. 필요한 것 — 우선순위

```
펌웨어  A  홀수 고조파를 h21 까지 발행                    <- P1. 지금 가장 크게 막는다
        E  LOW/HIGH 위상 응답을 이어 붙이기 전에 맞춤     <- P5
        B  같은 순간의 원시 + 2Hz 를 함께 발행            <- P2 진단용
        (참고) RC 위상은 크기만 보상돼 있다. 관례가 arg(I_h) − h·arg(V_1) 이라
               h·δ1 − δh 가 남는다 (δh = atan(60h/1591.55); h9 +0.69°, h13 +1.95°,
               h15 +2.90°). 우리 쪽에서 후처리로 빼고 있으니 꼭 바꿀 필요는 없다.
자료    C  LOW 안에 머무는 큰 비선형 부하 on/off 반복      <- P4 고차 Z
        D  포트 on/off 원시 스냅샷                         <- P3 HIGH 인공물
        F  minipc_4C 재녹화 + 세션 자가진단 절차           <- P6
        G  백열전구 60~100W 또는 납땜인두 1분 (LOW 순저항) <- 전압 채널 홀수차 보정표
        H  프로젝터 두 번째 동작점 (밝기/에코 3분)         <- 동작점이 1개라 재피팅을 못 믿는다
분석    I  V15 에서 h17~h31 을 예측하는 전압 사전분포      <- P1 의 대안, 아직 미시도
        J  충전기 저부하(19W) 물리                          <- P7
```

---

## 4. 판정 기준 (이걸로 채점해 달라)

```
[1] 손실은 h1~h15 로 자른 **상대 RMS** (파세발로 절대 페이저 적합과 같다). 전대역 RMS 는
    참고로만 — 실측 전류의 h16 이상은 RMS 의 9~15% 이고 모델이 낼 수 없는 바닥이다.
[2] 재현 바닥은 주기 산포/√n_cyc (1.2~3.0%). 그 아래로 내려가면 잡음을 맞춘 것이다.
[3] "더 낫다" 의 정의는 훈련 손실이 아니라 **동작점 LOO 교차검증**이다.
[4] 확장 요소는 base 최적점 + 중립값에서 **겹쳐 시작**해 단조성을 보장할 것.
[5] 파라미터가 경계에 붙으면 "식별 불가" 로 적고 기각한다.
[6] 결합은 **조합에 맞춘 자유 파라미터 0개**로 판정한다. 기준선은 단순 중첩(12.7~15.9%).
```

**[4] 를 강조하는 이유** — 실제로 겪었다. 이전 판정에서 확장 모형(다이오드 무릎·초크 감쇠·
직류 부하 지수)이 전부 기각됐는데, 다시 보니 충전기 `+alpha` 의 "최적" 손실 0.008759 가
중립값(α=1)에서의 0.008725 **보다 컸다**. 겹친 모형은 기본보다 나쁠 수 없으므로 그것은
요소의 증거가 아니라 Nelder-Mead 가 못 내려간 증거였다. 잔차 벡터 최소제곱(TRF) + 겹쳐 시작
+ 다중 시작으로 바꿨다. 같은 이유로 옛 회로 파라미터 표도 폐기했다 — 저장된 par5 를 현재
규약에서 재평가하면 기록된 손실과 **5~13배** 다르다 (지금은 철회된 위상 회전이 걸려 있던
동안 만든 표였다).

---

## 5. 읽기 주의 — 실제로 빠졌던 함정

```
· 위상 기준 프레임: 시뮬의 I·V 는 **소스 V1 기준** (arg(X_h) − h·arg(V_1)) 이고 시간영역
  FFT 페이저는 절대 프레임이다. 섞으면 h 차에 h·∠V1 이 실린다 — ∠V1 2° 면 h15 에서 30°.
  이것 하나로 같은 계산의 오차가 6.3% 대 16.4% 로 갈렸다.
· 계측 RC: 시뮬은 참 전류를 내고 원시 표본에는 RC 가 걸려 있다. 한쪽만 맞춰 비교하면
  h9 5%, h13 11%, h15 15% 를 모델 탓으로 돌린다.
· "배경" 은 대개 그 기기 자신의 X-cap 이다 (§1-4).
· 전압 짝수차는 아날로그 전단의 **덧셈** 성분이다 (곱셈 비선형 가설은 2차 다항식 보정을
  걸면 펄스 폭이 35°→18° 로 모델이 붕괴해 기각됐다). 반파 대칭화로 정확히 지운다.
  홀수차는 h3~h9 는 믿고 h11 이상은 의심 (P2).
· 시뮬의 P 는 **직류 부하 전력**, 측정 p_w 는 **교류 입력 전력** — 3~5% 다르다.
· 포트·드라이기처럼 LOW 포화를 넘는 부하의 고조파는 계측 단계에서 이미 다르다 (P5).
· 원시 파형은 40주기 **중앙값**을 쓴다. 주기 산포가 7~19% 라 단일 주기로 판단하면 안 된다.
```

---

## 6. 재현용 — 자와 자료

```
자료 (전부 장소 C, 2026-09-04~05)
  data/raw_{laptop_charger×5, beam_projector×2, minipc×5}.csv   단독 원시 스냅샷 (40주기)
  data/raw_{beam_minipc×2, beam_charger×2, smps3×2}.csv          조합 원시 스냅샷
  data/{laptop_charger_5C, minipc_4C, beam_projector_4C}.csv     2Hz, vhdeg 있음
  data/electric_kettle_4C.csv                                    포트 on/off 78계단 (Z 측정)
  data/noise_noselfpower_C.csv                                   계측계 배경 (기기 없음)
  data/tesr_19.csv                                               3종 동시, 라벨 타임라인 있음

자 (python -X utf8 -m src.<이름>)
  run_raw_fit_probe    원시 다점 적합 + Cx 직접측정 + LOO + Cx 고정 (--vband 로 소스 대역)
  run_raw_combo_probe  조합 검정 [A] 단순중첩 / [B] 모델(조합 V_term) / [C] 모델(단독 V_term)
  run_fcm_probe        FCM 구조 · 선형화 한계 · Z(h) 계단 측정 · forward 고정점 · Z 민감도
  run_circuit_model_probe  2Hz 고조파 쪽 적합·요소 재검정·계측 RC 전개·절대 페이저·식별성
  synthesis/fit_raw    원시 적합 라이브러리 (반파 대칭화·RC·배경·다점·대역제한 손실)
  synthesis/fcm        HNE/FCM 추출, forward 고정점, z_from_steps
  synthesis/circuit_sim  시뮬레이터 (가이드 §3 토폴로지 v3)

기록
  MODEL_TRAINING_DESIGN.md  §12.185.1~17   (오늘 한 것 전부, 철회한 것 포함)
  results/_criteria_circuit.md              (실험 기준 — 전부 결과 **전에** 적었다)
  results/_circuit_raw_C.json, _circuit_raw_C_v15.json, _fcm_C.json, _circuit_model_C.json
```
