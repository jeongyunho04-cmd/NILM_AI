# 인계 2026-09-15c — 오탐 원인이 **컨덕턴스 축퇴**로 좁혀졌다

## ⏳ 도는 것

```
  984924  cnn_hv2   머리 배치 v2 · 복제패딩 위   짝 cnn_pad   3시드 · 약 35분
```

`--head-layout v2 --fine-pad replicate`. 관문 `run_gate_headv2`(9개)·`run_gate_finepad`(9개)
전부 통과. 판정 조건은 **"다섯 가지로 줄여도 v1 만큼 하나"** 와 **씨앗 산포**다
(어느 자든 sd 가 짝의 3배를 넘으면 배치가 해를 갈라놓은 것으로 읽는다).
통전 오탐이 나아질 것으로는 **기대하지 않는다** — 아래 ★ 참조.

---

## ★★ 오늘의 결론 — **오븐 통전 헛detect 는 시간 문제가 아니라 컨덕턴스 문제다**

```
  헛창 84개 (후보 905 · cnn_pcap_s0)
    포트가 **100.0%** · 드라이기가 **98.8%** 참으로 켜져 있다 (빔은 0.0%)
    모델이 준 전력: 오븐 **1169W**(참 0) · 드라이 472W · 포트 **74W**(참 ~1235W)

  관측 R = **25.7Ω** 을 만드는 조합
    포트 + 드라이약(반파)   26.9Ω  (+1.2)   <- **참**
    오븐 + 드라이          23.2Ω  (-2.5)   <- **모델이 고른 것**
    (포트 35.8 · 오븐 40.6 · 드라이 54.3 · 핫플 101.8Ω)

  개입 셋 **전부 무효**
    미래 지우기    6.48 -> **7.99%**  (오히려 나빠진다)
    타깃 앞5중앙   6.48 ->   6.52%   (무효)
    광역 과거 0초  6.48 ->   6.22%   (거의 무효)
```

**포트 1235W 를 오븐 1169W 로 통째로 바꿔 읽는다.** 참 조합이 관측에 **더 가까운데도**
그렇다 — `cons 0.0 · over 0.0 · proj 0` 이라 Σ를 관측에 묶는 항이 하나도 없다.
⇒ **다음 처방은 컨덕턴스 정합이다.** ⚠ 14.31 이 **후처리** 컨덕턴스 정합을 기각했다 —
그건 후처리였고 이건 손실이다. **던지기 전에 그 판을 반드시 다시 읽어라.**

---

## ★ 지표가 틀려 있었다 — 통전과 팬조명을 안 갈랐다

```
  오븐 헛detect (참OFF·큰 저항부하 창 1043개)
    게이트 `on_logit>0.5`  **20.2%**
    ├─ 통전 state2>0.5      **5.6%**   <- 그림의 파란 1100W 덩이
    └─ 팬조명만             **13.6%**  <- 16.8W. 그래프에 거의 안 보인다
  계단 거리축에서 다르게 움직인다 — 게이트 9.2->37.7%(4배) · 통전 4.9->6.7%(평평)
  전이 ±20초 **밖**은 통전도 **0.00%** (일곱 팔 전부 · 138창)
```
`src/run_diag_ovencond.py` 를 만들었다. 이름을 번호가 아니라 뜻으로 쓴다 —
**포트창 오븐통전% · 전이근처 오븐통전% · 전이밖 오븐통전% · 오븐 재현 · 오븐 헛전력W**.
⚠ ★1·★2 는 못 박힌 자라 **안 바꿨다.** 이건 통전판 **짝**이다 (모집단도 다르다:
★2 는 격자 2.0초 61창, 이건 stride 0.5초 241창 — **섞어 인용하면 안 된다**).

---

## 오늘 판 여덟 (★1 · ★2 · ★4 · 저항신원 · SMPS신원 · 판정줄 · 잔차 · 전이근처통전)

```
          ★1     ★2     ★4     저항     SMPS    판정줄   잔차   통전   짝     판정
  hzwt   2.908  33.88  0.859  0.9527  0.9237  0.8885  21.1  7.48   —    기준선
  hs3    4.499  36.61  0.881  0.9753  0.9412  0.8783  21.0  7.62  hzwt  미정
  tsp    4.435  40.98  0.850  0.9815  0.9675  0.8952  19.9  8.66  hzwt  기각
**pcap   4.462  35.52  0.893  0.9774  0.9807  0.8820  21.3  6.48  hzwt  채택**
  fseg   3.197  36.61  0.845  0.9815  0.9465  0.8889  21.2  7.37  tsp   기각
  h3cap  5.228  30.60  0.872  0.9712  0.9684  0.8756  23.0  6.59  hs3   기각
  hdrop  4.868  32.24  0.840  0.9280  0.9666  0.8902  25.7  9.02  pcap  기각
  pad  **1.902** 37.71  0.872  0.9774  0.9588  0.8813  20.7  7.88  pcap  기각
```

**채택은 `pcap` (`--p-state-cap 3`)** — SMPS 신원 0.9807(최고) · 전이근처 통전 6.48%(최저).
`pcap` 은 **저항 슬롯에 한 번도 안 걸린다**(최대 0.38배) — 효과가 **간접**이고 기전은
아직 모른다. 기울기 측정(14.125)으로 "부푼 슬롯이 몸통을 흔든다" 가설은 **반증**됐다
(빔 기울기가 오히려 x1.26). 남은 읽기: **슬롯이 오차 배출구였고 상한이 그걸 막았다** —
합성 손실·MAE 는 지고 실측 신원은 이겼다. **아직 가설이다.**

---

## 새 손잡이 (전부 기본값이면 **비트 동일**)

```
  --p-state-cap R        p_states <= R x S_STATE   **채택(R=3)**   관문 run_gate_pstatecap
  --head-drop NAMES      머리 덩이 제거              기각(14.128)   관문 run_gate_headdrop
  --head-layout v1|v2    머리 배치                  판정중          관문 run_gate_headv2
  --fine-pad zeros|repl  세밀 conv 패딩             기각(14.131)   관문 run_gate_finepad
  --fine-future-segs K   미래 토막                  기각(14.122)   관문 run_gate_futsegs
  --fine-time-split      몸통 시간분할              기각(14.116)   관문 run_gate_timesplit
```
곁들여: 체크포인트에 `fine_time_split` · `p_state_cap` · `head_drop` · `head_layout` ·
`fine_pad` · **`cache`/`holdout`** 을 적는다 (안 적혀서 sbatch 를 뒤져야 했다).

---

## 값이 거의 0인데 안 던진 것 둘

```
  --target-median 5   머리 `feats[0]` 을 앞 5사이클 중앙값으로
                      test_1 드라이 헛전력 >100W 창 **17 -> 0개** · 핫플 무손해(146.9->147.2W)
                      **캐시 재굽기 불필요** — 머리 한 줄
  광역 룩어헤드 0      `wide_target_index` 를 창 끝으로
                      AUC 변화 **0.0000** (시드별로도 동일) · 헛게이트 23.3 -> 22.4%
                      미래로 들어오는 문이 둘에서 하나로 준다
```

---

## 구조에 대해 오늘 잰 것

```
  수용영역   블록6 RF **763** — 창 600 의 1.27배 · 조각 240 의 **3.18배**
             넘치는 만큼이 전부 패딩 (탭의 18.3% / **45.7%**)
  정규화     `GroupNorm(C, T 전체)` — **RF 와 무관하게** 창 전체를 묶는다
             풀링만 토막내면 대각/비대각 **1.73배** · 따로 태우면 **0.0000**
  머리 중복  765칸의 유효 차원 **참여비 20.2** · 모든 덩이 R^2 0.905~0.982
             그런데 빼면 씨앗이 갈라진다 (hdrop 씨2: 저항 0.975 -> **0.821**)
  룩어헤드   세밀 6초 **쓰인다**(짧을수록 나쁨) · 광역 6초 **AUC 0.0000 = 낭비**
             광역 과거 54초는 **쓰인다** (자르면 AUC 0.9796 -> 0.9629)
  미래 채널  `DROP_TAPS=(180,330)` -> **ch41/42 가 RF 1 로 미래 3.0/5.5초를 담는다**
             `--fine-time-split` 이 못 막는다. 오븐에는 +1.3%p 로 작다
  씨앗 바닥  3시드 오븐 on_logit 상관 0.945~0.960 · **판정 일치 0.94~0.95**
             = 처치 효과와 **같은 자리수**. 판정이 1/3·2/3 로 나오는 진짜 이유
```

---

## test_1 스파이크 — 별개의 병이고 **고쳐졌다**

```
  드라이 헛전력 >100W 창 17개/1481 · 관측 계단까지 중앙 **0.08초** (나머지 0.36초)
  타깃이 듀티 **골짜기 바닥**에 앉는다 (관측 147W 인데 예측 745W)
  직전/직후 한 칸으로는 안 사라진다 — 과도가 **여러 사이클**
  앞 5사이클 중앙값이면 **17 -> 0개**
```
오븐↔포트와 **반대 분포**다 — 스파이크는 계단 **위**, 오븐 오탐은 계단에서 **1~10초**.
같은 고침이 오븐에는 무효(20.2 -> 20.3%). **축퇴가 공통 증폭기이고 방아쇠가 둘이다.**

---

## 죽은 손잡이 (되살리지 마라)

```
  L_gcond 14.112 · L_swap 14.35 · 위치 채널 14.42 · 광역 축 전부 14.89/14.94
  후처리 컨덕턴스 정합 14.31   <- 손실판을 짜기 전에 **반드시 다시 읽어라**
  --seg-pool 14.72  <- ⚠ **기각이 틀렸다.** 다른 자(0~20초 띠)로 채점했고,
                        못 박은 ★4 로 다시 재니 **+0.0220 ± 0.0144, 3/3** 이다.
                        다만 시간분할을 켜면 seg_pool 2 는 [(0,239),(239,240)] 로
                        거의 무의미해진다 — 살아 있는 형태는 **seg_pool 3**(과거 4초를
                        2초씩)이고 그건 아직 아무도 안 쟀다.
```

---

## 오늘 세운 규칙 (기억에 넣었다)

```
  · 0 으로 죽여 원인을 읽지 마라 — 쓸모가 아니라 분포 이탈을 잰다 (-3.6%p 가 실제 0.0%p)
  · 중복은 해를 고정한다 — R^2 0.98 이어도 빼면 씨앗 하나가 무너진다
  · 개입과 처치가 같은 물건인가 — "덮기"로 진단하고 "0으로 자르기"를 구웠다
  · 정규화가 수용영역보다 넓게 묶는다 — GroupNorm 이 실패 셋의 공통 원인
  · 지표를 먼저 갈라라 — 헛게이트 20.2% 중 통전은 5.6% 뿐
```
