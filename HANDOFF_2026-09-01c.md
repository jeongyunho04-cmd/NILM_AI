# 인수인계 2026-09-01c — 배분의 기제가 닫혔고, 처방이 하나 검증됐다

`HANDOFF_2026-09-01b.md` 다음이다. 그 문서를 받아 §1 [1] 을 하려다 **사용자가
목표를 배분으로 좁혀서** 방향을 틀었고, 그 결과 기제가 닫혔다.

설계 문서 **12.128 ~ 12.130.1**. (12.123~12.127 은 b 인수인계에 있다.)

---

## 0. 한 줄

**프로젝터 과대예측은 총전력 배출구가 아니라 충전기와의 제로섬 맞바꿈이다**
(+17.00 / −17.01W). 스냅이 그것을 되돌리고, 남은 난제는 충전기↔미니PC ±2.5W 다.

---

## 1. 슬랙 가설 반증 — 그리고 진짜 기제 (12.128)

12.126 이 미시도 처방으로 *"`L_cons` 에 슬랙을 주면 32W 가 거기로 간다"* 를
남겼다. **학습 전에 반증 조건을 적고 쟀다** (규칙 25/28). 창 11,301개에서
1단계 부족분 `gap1` 에 적응의 기기별 이동량 `dP_i` 를 회귀:

```
기기                dP 중앙W   상관    기울기 dP/gap1    몫
electiric_kettle     +0.00   0.331      0.558      49.1%
hair_dryer           -0.00   0.468      0.359      31.6%
hotplate             -0.00   0.358      0.148      13.0%
laptop_charger       -8.63   0.042      0.004       0.4%
beam_projector      +10.50   0.013      0.001       0.1%   <- 사실상 안 받는다
```

**반증됐다. 학습을 안 돌렸다.** 총전력 부족분은 저항이 받는다.

### 대신 진짜 기제가 나왔다 — 제로섬 맞바꿈

```
층                   창    |gap1| 중앙  dP 프로젝터  dP 충전기      합
프로젝터 ON        6,681      5.7W    +17.00    -17.01   **-0.01**
저항 0종          7,197      4.4W    +15.75    -15.06     +0.69
프로젝터 ON + 저항  1,371     28.7W     +4.50     -4.18     +0.32
```

**총전력을 건드리지 않는다.** 그리고 **부족분이 작은 층에서 맞바꿈이 가장 크다** —
배출 가설의 예측과 정반대다.

12.122.2 가 **모델 없이** NNLS 로 예측한 것과 같다:
*"자유해는 프로젝터를 20W 더 얹고 충전기를 16W 뺀다 (65/7/6 vs 정답 45/23/6)"*.
**+20/−16 (NNLS) 과 +17/−17 (학습된 모델) 이 일치한다. 기제가 완전히 닫혔다.**

---

## 2. 스냅이 맞바꿈을 되돌린다 (12.129)

맞바꿈이 보존적이므로 프로젝터를 참값에 못 박으면 17W 는 갈 곳이 충전기뿐이다.

⚠ **`snap_power` 는 설계상 SMPS 그룹 안에서 재배분한다.** "충전기로 가는가" 는
동어반복이다. 물어야 할 것은 **옳은 값에 착지하는가** 다. 기준을 넷 세웠다:

```
기기              격리 p5~중앙~p95     ①1단계   ②NNLS   2단계 원시   운영점+스냅
beam_projector   44.2~46.9~47.8      62.5     87.2      81.0     **46.9**
laptop_charger   33.3~48.7~68.4      31.1      1.9       8.9       30.6
minipc            8.2~12.4~22.7       5.9      7.0       4.1       25.1
```

**적응 뒤 원시는 셋 다 물리 범위 밖이다.** 운영점+스냅을 걸면 셋 다 범위 안이거나
경계 10% 안으로 들어온다. 충전기는 1단계(31.1)와 **0.5W 차**로 되돌아온다.

```
대가        유령8 +0.4W,  잔차 +0.04W
재배분 끄면  잔차 4.24 -> 7.89W  <- 옮긴 8W 의 95% 가 켜진 기기로 갔다
총합 정합   SMPS 전용 3파일에서 (배분합+대기+잡음)/관측 = 0.98~1.03
```

### ⚠ 순환을 조심할 것

**46.9 로 스냅하고 46.9 기준으로 채점하면 중앙|오차| 0.00W 는 동어반복이다.**
비순환 증거는 셋뿐이다 — 폭안(격리 p5~p95 안) 2.5%→99.9%, 충전기의 독립 기준
회복, 총합 정합.

### ⚠ 내가 기준 편향으로 한 번 틀리게 읽었다

①②만 보고 *"미니PC 25.1W 인데 기준 둘은 6~7W. 4배 과대예측"* 으로 읽었다.
③(격리 통전 8.2~22.7W)을 넣으니 뒤집혔다 — **①(5.9)과 ②(7.0)이 둘 다 p5 아래**다.
1단계는 SMPS 를 통째로 낮게 보고(실측 잔차 54W), NNLS 는 충전기를 0 으로 민다.
**기준 둘이 같은 방향으로 틀렸을 때는 셋째가 필요하다** (규칙 23 의 형태).

---

## 3. 충전기의 시간 패턴 (12.130 / 12.130.1)

> 사용자 관찰: *"충전기는 단독녹화 로그에 상황별 패턴이 있는 것 같다."*
> / *"거의 완충상태일 때"* / *"그 흔들림에 패턴이 있는지 보라고."*

남은 충전기↔미니PC 난제가 막힌 이유는 **참값이 없어서**다(폭/중앙 0.722 / 1.162).
그래서 "절대값이 아닌 제약" 을 찾았다.

```
✅ 관찰이 맞다        모든 통전 구간이 상승(0~30s) -> 고원 -> 완만한 감쇠
❌ "구간 안에서 평평"   상태를 안 가르면 틀리다. 트리클 CV 0.225 > 고속 0.178
❌ 트리클 전용 스냅    순수 상태1 60초 창이 폭/중앙 0.049 로 **기준을 통과**하는데,
                    그런 창이 4녹화 통틀어 **5개**다. 에피소드가 2~15초라 창보다 짧다
❌ 주기성            ACF(5s) = −0.016. 5초면 백색이다. 걸 것이 없다
◐ **전력 분포 모양**   양봉갭 (p95−p5)/(p75−p25):
                    미니PC 4.15/3.42  vs  충전기 2.98/2.30  vs  프로젝터 2.73
                    (정규분포면 2.6). **남은 난제를 정확히 겨눈다. 미시험**
```

부수로 나온 것: **프로젝터 CV 0.013** — 거의 완전한 정전력 부하다. 스냅이 왜
프로젝터에만 듣고 다른 둘에는 못 거는지가 이것으로 설명된다.

---

## 4. 다음에 할 것 — 순서대로

### [1] 스냅을 배포 경로에 넣는다 — ✅ **했다 (12.131)**

세 경로에 넣었다. `SNAP_TARGET_W` 47.4 -> 46.9 (튜닝값 -> 측정값),
`run_live --snap` 기본 46.9, `deploy/nilm_runtime` 동기화 + `NILMPredictor(snap=46.9)`.
저전력 상태 단서도 확인했다 — 격리 60초 창 95개 중 warmup 이 절반을 넘는 창이
**0개**, 창 최소 41.8W 라 최악 오차 +5.1W 로 유계다. 고정 목표로 안전하다.

⚠ **남은 부채 하나**: 오프라인 채점기(`run_gate_check`/`run_power_check`)의
`--snap` 기본은 **0(끔)** 으로 뒀다. 바꾸면 과거 표 전부와 비교가 깨진다(규칙 20).
**배포와 기본 채점이 다른 것을 재고 있다** — 다음 성능표를 새로 낼 때
`--snap 46.9` 로 기준선을 다시 잡고 그때 맞추는 것이 맞다.

<details><summary>당시 기록 (참고)</summary>

**측정으로는 닫혔는데 코드에는 없다.**

```
run_live.py          `--snap` 인자가 **없다**. snap_power 를 부르지도 않는다
postproc.py:122      SNAP_TARGET_W = 47.4  <- 옛 튜닝 후보 (SNAP_SWEEP_W 의 첫 항)
                     참값표는 46.9 (recompute_reference 의 측정 중앙값)
```

```
① SNAP_TARGET_W 47.4 -> 46.9 (참값표와 출처를 묶는다. 결과 차는 0.5W 로 미미)
② run_live 에 --snap 추가 + snap_power 호출 (run_gate_check 와 같은 순서)
③ 배포 경로에서 12.129 의 수치가 재현되는지 확인
```

⚠ **프로젝터 저전력 상태**(WARMUP_COOLDOWN 3.7~4.4W, 통전의 3.1%)를 스냅이
46.9 로 올린다. 60초 창 평균에서는 묻히지만(창 최소 41.8W) 확인은 하고 넣을 것.
`snap_power` 에 `state_targets` 인자가 이미 있다 — 상태별 목표를 줄 수 있다.

</details>

### [2] 양봉갭 제약 — 남은 난제를 겨눈다 (미시험)

걸림돌 둘: 표본이 작고(충전기 상태1 구간 8개), **창 하나에서는 분포를 못 본다.**
창을 잇는 제약이라 지금 손실 구조(창 독립)와 안 맞는다. 작은 일이 아니다.

### [3] b 인수인계에서 안 한 것

```
[0] 가드 수정 뒤 저항 절대값 재인용   12개 체크포인트 재측정, 15분, 재학습 없음
[1] 불감대 배수 상한 8/16           예측이 12.123.1 에 적혀 있다 (16.0 에서 붕괴)
[2] 조합 판에서 ridge 재쓸이
```

**전부 검출 축이거나 기록 정합성이다. 배분은 위 [1][2] 다.**

---

## 5. 새로 만든 것

```
src/run_dump_probe.py             창 단위 인과 분해 — 부족분이 어디로 가는가
src/run_snap_reverses_probe.py    스냅이 맞바꿈을 되돌리는가 (기준 넷)
src/run_charger_pattern_probe.py  충전기 시간 패턴 + 그림
src/run_plot_timeline.py          **후처리 옵션 추가** (--postproc/--resmatch/--rm-snap/--snap)
                                  안 걸면 그림이 운영 실물과 다르다
results/plots_op/                 11파일 타임라인 (조합 판 + 스냅 46.9)
results/dump_probe.json           results/snap_reverses.json
results/charger_pattern.json      results/forward_gap.json
results/pc_op.json / pc_op_snap.json / gc_snap.json / gc_snap_nr.json
```

⚠ `results/plots_op/` 의 그림은 **`adapt_combo_s0.pt` + 스냅 46.9** 다.
조합 판은 채택 안 했고 스냅도 배포에 없다 — **후보 구성의 그림**이다.

---

## 6. 반증·정정 목록

| 진술 | 출처 | 결과 |
|---|---|---|
| "`L_cons` 슬랙을 주면 프로젝터가 낫는다" | 12.126 | **반증.** 프로젝터 기울기 0.001 |
| "프로젝터는 총전력 부족분의 배수구" | 12.122.13 원안 | **반증 확정.** 창 단위 회귀로 |
| "구간 안에서 충전기가 평평하다" | 12.130 (내가 씀) | **정정.** 상태를 안 가르면 틀리다 |
| "미니PC 가 운영점에서 4배 과대예측" | 나 (세션 중) | **철회.** 기준 둘이 p5 아래였다 |
| "불감대는 배분을 안 고친다" | 나 (세션 중) | **정정.** 단독 −8.8W. 조합에서 추가분이 0 인 것 |

---

## 7. 환경 메모

- 학습에 `--init results/cnn_ovh.pt --cache cache/train60_ovh` 를 반드시 줄 것.
- 맨 `python -c "import torch"` 는 OMP 로 죽는다. `src.env_guard` 를 먼저 import.
- 한글 트레이스백이 안 보이면 `python -X utf8 -m ...` 로 돌릴 것.
- `run_plot_timeline` 은 **기본이 후처리 끔**이다. 운영 실물을 보려면 플래그를 줄 것.
