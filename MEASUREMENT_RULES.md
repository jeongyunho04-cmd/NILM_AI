# 측정의 규칙

**이 저장소에서 실제로 틀린 뒤에 적힌 것만 모았다.** 각 규칙 옆의 절 번호는
`MODEL_TRAINING_DESIGN.md` 에서 그 실수가 일어난 자리다. 추상적 조언이 아니라
사건 기록이다.

`MODEL_TRAINING_DESIGN.md` 6절 표는 **설계**에서 하지 말 것이고,
이 문서는 **측정**에서 하지 말 것이다.

> **새 실험을 설계할 때 이 문서를 먼저 읽는다.** 설계 문서는 10,000행이 넘어
> 규칙이 묻힌다. 그래서 따로 뺐다.

---

## 1. 격리 통계로 "모델이 무엇을 써야 한다" 를 정하지 않는다

그 양이 **복합에서도 살아 있는지** 먼저 재고, 모델이 **실제로 쓰는지**는 절제로 잰다.

**이 저장소에서 세 번 어겼다.**

| 사건 | 무엇을 했나 | 결말 |
|---|---|---|
| 12.65 | 격리에서 `\|I2\|/\|I1\|` 이 4.1배로 갈리는 것을 보고 "이것이 판별자다" 로 추론 | 12.73 에서 철회. 절제해 보니 −0.0025 였다 |
| 12.79 | `\|I3\|/\|I1\|` 이 고부하에서 66배 압축되는 산술을 보고 "이것이 미검출 원인" | 12.81 에서 철회. 저항 부하만 있으면 재현율 99% 였다 |
| 12.81.4 | 격리 위상 d′ 14.9 를 보고 "모델이 위상을 써야 한다" | 12.82 에서 철회. 복합에서 d′ 0.4 다 |

**12.81.4 는 같은 절에 이 경고를 직접 달아 놓고도 그 방향을 다음 할 일로 적었다.**

---

## 2. 절제는 두 방향으로 잰다

- **하나씩 빼기(필요성)** — 그 채널이 없으면 무너지는가
- **그것만 남기기(충분성)** — 그 채널만으로 얼마나 되는가

**하나씩 빼기만 보면 중복된 특징이 전부 "안 쓰인다" 로 보인다.**
정보가 여러 채널에 겹쳐 있으면 어느 하나를 빼도 Δ 가 작다.

> 12.76.2 — 판정 기준 ②를 "불통과" 로 읽었다가, 충분성으로 다시 재니 예측이
> 맞았다 (위상 +0.243 -> +0.321).

**충분성을 잴 때는 기저율을 먼저 잡는다.** 채널 대부분을 0 으로 만들면 모델이
한쪽으로 찍는데, 그 정확도가 바닥이다 (12.76.3 에서 0.4413).

---

## 3. 절제 그룹은 가설을 가르도록 짠다

성질이 다른 채널(짝수/홀수, 물리/인공물)을 한 그룹에 묶으면 **그룹 Δ 가 어느
쪽 것인지 알 수 없다.**

> 12.73.2 — `ch33~35,47` 에 짝수 하나와 홀수 셋을 묶어 −0.39 를 얻고
> `ch35` 를 판별자로 지목했다. 갈라 보니 `ch35` 단독은 −0.0025, 홀수 셋이
> −0.4163 이었다.

---

## 4. 절제도 운영 조합·실측에서 한 번 더 잰다

합성 절제는 모델이 그 채널을 **합성에서** 쓰는지만 말한다. 도메인이 바뀌면
의존도 바뀐다.

> 12.74.2 — 합성 홀드아웃에서 −0.011 이던 짝수차 의존이 실측에서는 0.07 이었다
> (충전기 0.937 -> 0.868).

이것은 12.47 · 12.51.3 의 "1단계 지표로 문제를 고르지 말라" 가 **절제에도**
적용된다는 뜻이다.

---

## 5. 두 집단을 견줄 때 비교하려는 변수 하나만 다른지 확인한다

**세 번 어겼다.**

| 사건 | 무엇이 섞였나 |
|---|---|
| 12.79.4 | "오븐 배경만" 을 재면서 다른 저항 부하를 안 뺐다. `test_4` 의 핫플 배경이 262mA(p10~p90 14~471)로 떴다 |
| 12.86.2 | 미니PC 가 **켜진** 창과 **꺼진** 창을 견주고 "미니PC 전력이 프로젝터에 얹힌다" 로 읽었다. 총합이 높은 것이 당연하다 |
| 12.88 초안 | 같은 부류를 한 번 더 |

---

## 6. 층화 변수가 서로 상관될 때는 교차표를 만든다

> 12.81.2 — 재현율을 **총전력**으로 층화해 "부하에 묻힌다" 로 읽었다.
> 실제 원인은 **동시에 켜진 SMPS 수**였고, 같은 표에 그 축이 이미 있었는데
> 두 축의 상관을 안 봤다.
>
> ```
> 미니PC 재현율      저부하 <200W    고부하 >600W
> 경쟁 SMPS 없음      98.2%          99.2%     <- 부하와 무관
> 경쟁 SMPS 있음      66.6%          30.2%
> ```

---

## 7. "잡음 바닥" 이라 부르기 전에 계측기만 따로 잰다

간섭이 섞인 널을 센싱 한계로 읽으면 **처방이 하드웨어로 잘못 간다.**

> 12.70.1 — 복합 널 `|ΔI2|` p95 12.26mA 를 잡음 바닥이라 부르고 "신호가
> 죽는다" 로 결론냈다. 무부하 녹화로 계측기만 재니 **0.031mA** 였다 (400배).
> 12.26mA 의 99.7% 는 다른 기기의 움직임이었다.
>
> `run_delta_feature_probe.audit` 의 독스트링이 처음부터 "계측 잡음뿐 아니라
> 다른 기기의 움직임도 들어 있다" 고 적어 놨는데 그것을 읽고도 그렇게 썼다.

---

## 8. 잡음 통계로 "신호가 죽는다" 고 주장하려면 모델의 시간 규모로 환산한다

> 12.68.2 — 10초 규모 널(pre/post 중앙값 차)을 60초 창 판단의 임계로 썼다.
> 10초 요동을 신호 위로 올려도 60초로 평균하면 신호가 다시 드러난다.
> 단서는 안 죽고 과제만 어려워졌고, 어려워지자 모델은 그 단서에 **더** 매달렸다.
>
> 12.56 의 재발이다 — 그때는 "48.5분 67->33W" 를 60초로 환산해 0.62W 임을
> 알고 철회했다.

**단서를 죽이려면 그 시간 규모에서 죽여야 한다.**

---

## 9. "잡음처럼 작다" 와 "판별에 안 쓰인다" 는 다른 진술이다

어떤 양을 산포 계산에서 빼기 전에 그 양의 **기기간 격차**를 본다.
산포가 크다는 것은 그 양이 무의미하다는 뜻이 아니라 **분포가 넓다**는 뜻일 뿐이다.

> 12.62.2 — 짝수차를 "`|I_k|/|I_1|` 이 0.4~8.9% 인 계측 바닥" 이라며
> 크기 결정에서 뺐다. **판별자가 정확히 거기 있었다** (12.65.3).

---

## 10. 합성 통계를 실측과 견줄 때는 모델이 실제로 먹는 생성 경로로 뽑는다

같은 합성기라도 레시피 믹스가 다르면 다른 분포다.

> 12.66.1 — `synthesize_random_window` 기본값으로 뽑아 `|ΔI2|` p95 10.42mA 를
> 얻었다. 캐시·홀드아웃이 쓰는 `DEFAULT_RECIPE_MIX` 로 바꾸니 5.42mA 였다.
> **이 한 번의 실수가 결론의 부호를 뒤집을 뻔했다.**

---

## 11. 요약 통계가 좋다고 그 안의 모든 성분이 좋은 것은 아니다

> 12.83.2 — 12.53.7 의 "차분이 격리 지문을 cos 0.95 로 복원한다" 를 믿고
> 차분 위상 불변량을 시도했다. cos 0.95 는 **15성분 벡터 전체**의 유사도로
> 큰 저차가 지배한다. **차수별 위상 불변량은 d′ 0.07 이었다.**

---

## 12. 분포 밖 조작의 결과는 "안 변할 때" 만 강하다

0 으로 죽이기, 범위 밖으로 밀기 등은 학습 분포 밖 입력을 만든다.
**안 변하면** 그 입력을 안 보고 있는 것이 확실하다. **크게 변했을 때**는 같은
탐침을 두 모델에 걸어 **상대 변화**만 근거로 쓴다.

> 12.76.3 · 12.79.5 — 비율 채널을 0 으로 만들자 미니PC 재현율이 38.1 -> 67.5%
> 로 뛰었으나 정밀도가 97.1 -> 78.5% 로 떨어졌다. "정보를 되살렸다" 와
> "게이트가 무차별로 켠다" 를 그 측정만으로는 못 가른다 (12.81.1 이 후자로 확정).

---

## 13. 계측이 바뀌면 모델에 넣기 전에 계측부터 잰다

새 펌웨어·새 회선·새 보드로 측정한 파일은 **모델 파이프라인에 넣기 전에**
계측 자체를 검증한다. 순서가 반대면 오염된 데이터로 학습한 모델을 운영에 올린다.

> 12.108 — 2차 펌웨어가 짝수차를 고치면서 **레인지 자동 전환을 껐다.** 옛 판은
> `|I1|` 2A 에서 HIGH 로 가는데 그 판은 8A 에서도 LOW 에 머물러, 순저항 부하가
> `PF 0.901 / |I2|/|I1| 0.46` 으로 나왔다. `over_range` 플래그는 0 이었다 —
> **보드가 자기 왜곡을 모른다.**
>
> 그때 이미 그 파일로 라벨을 붙이고 적응까지 돌려 "잔차 226 -> 13.6W" 라는
> 좋아 보이는 숫자를 얻은 뒤였다. 사용자가 펌웨어 이야기를 안 했으면 그대로
> 믿었을 것이다.

**순저항 부하가 있는 고전류 구간에서 이 셋을 본다.**

```
PF ≥ 0.99      Irms/|I1| ≤ 1.01      |I2|/|I1| ≤ 0.01
```

---

## 14. "다음에 해 볼 것" 에 적은 추측은 근거가 아니다

절 끝에 적는 다음 후보는 **아직 안 잰 것**이다. 다음 사람(또는 다음 세션의
자신)이 그것을 측정 결과처럼 읽는다.

> **2026-08-27 세션에 두 번 틀렸다.**
> 12.97.3 이 *"오염이 같은 45W 규모가 아니니 전이 차분을 다시 볼 값어치가 있다"*
> 고 적었는데, 재 보니 45W 규모에서도 39%(우연 33%)였다 (12.99).
> 12.100.5 가 *"지속 성분만 넘기면 프로젝터 전이 손실을 살릴 여지가 있다"* 고
> 적었는데, 재 보니 **반대**였다 — 재배분은 바로 그 전이 순간에 작동해서 효과를
> 낸다 (12.101.2).
>
> 둘 다 학습이 필요 없는 항목이라 값이 쌌다. 비쌌으면 두 번 다 헛돌았다.

**적을 때 "재지 않았다" 를 같이 적는다.**

---

## 15. 안전장치가 무엇인지도 측정으로 확인한다

"이런 경우는 이 장치가 막는다" 는 설명을 **검증 없이 문서에 적지 않는다.**
실제로 막고 있는 것이 그것이 아닐 수 있다.

> 12.104 — 잔차 흡수에 *"고조파가 SMPS 를 안 닮으면 가중이 0 이라 저항 잔차는
> 안 넘어간다"* 고 적었다. 단위 시험을 짜다가 드러났는데, **실제로 막고 있던 것은
> 게이트 문턱**이었다. 코사인이 기본파에 지배돼 오븐 모양 잔차의 62%가 SMPS 로
> 흘렀다. 3차 이상만 쓰도록 고치고 나서야 문서가 참이 됐다.

---

## 16. 인공물로 판정한 신호가 진짜일 수 있다 — 상태를 갈라 보라

"이 값은 계측 인공물이다" 는 판정은 **그 기기의 모든 동작 상태에서** 확인해야
한다. 상태 하나가 만든 실재 신호를 전체의 인공물로 오인할 수 있다.

> 12.72 는 헤어드라이어의 `r(|I2|,|I1|) = −0.993` 을 보고 *"순저항인데
> 결정론적 관계"* 라며 짝수차 전체를 인공물로 판정했고, 12.77 이 15채널을
> 0 으로 만들었다.
>
> 12.109.2 — 약/강 두 단계를 담은 녹화가 오자 갈렸다. **약풍은 다이오드 반파
> 정류다**: P 가 정확히 절반(466 vs 879W), `|I2|/|I1|` 0.395 (이론 0.424).
> 옛 녹화도 0.414 다. 그 상관은 인공물이 아니라 **약(반파)과 강(전파) 두 상태**가
> 만든 것이었다. 저항 3종에서 유일하게 신원이 확실한 신호를 우리가 0 으로
> 만들고 있었다.

---

## 17. 라벨 정의가 모델의 실패를 만든다 — 실패를 보면 라벨을 의심하라

모델이 한 기기를 남용하면 **그 기기의 라벨이 무엇을 허용하는지** 본다.

> 12.110 — 모델이 저항 부하를 전부 오븐으로 읽었다 (재현율 1.000, 정밀도 0.277).
> 사용자는 전력 증강이 과한 것을 의심했고, 그 방향으로 재 보니 증강은 폭의
> 1/3 만 만들었다 (`power_scale_std` 를 0 으로 해도 겹침 51 -> 45%).
>
> 진짜 원인은 라벨이었다. 오븐의 팬/조명 상태가 16.5W 라 `on_threshold_w=10` 을
> 넘어, **히터가 꺼진 25분 내내 `is_on=1`** 이었다. 그래서 "오븐 ON 인데 전력
> 0W" 인 창이 52.8% 였고, **오븐만 0~1,300W 전 구간을 덮어 공짜로 켤 수 있는
> 기기**가 됐다. 포트를 켜면 1,000W 이상을 붙여야 하는데 오븐은 0W 로 둬도 된다.
>
> 라벨을 히터 통전으로 바꾸자 포트 F1 이 0.000 -> 0.921 이 됐다 (12.111).

**같은 구조가 SMPS 에도 있다** — 프로젝터가 비용 없이 켤 수 있는 기기라
승자독식의 승자였다 (12.99.3).

---

## 18. 후처리는 '누구인지' 는 바꿔도 '몇 대인지' 는 정하지 않는다

물리 제약으로 예측을 고칠 때, **기기 수를 바꾸게 두면 없는 기기를 발명한다.**

> 12.112 — 등가저항 정합에 조합 제한을 안 뒀더니 `test_9`(드라이기 없는 파일)의
> 유령이 3.94 -> **86.98W** 로 터졌다. 컨덕턴스를 맞추려고 드라이기를 켠 것이다.
> **개수를 고정하고 맞바꿈만 허용**하자 유령이 8.90 -> 5.33W 로 줄고 F1 도 올랐다.
>
> 물리 정합이 잘하는 일은 *누구인지* 를 바꾸는 것이다. *몇 대인지* 는 모델이 안다.

반대 방향의 실패도 있다 — 게이트로 후보를 좁히면 **고쳐야 할 맞바꿈을 놓친다**
(`test3` 의 오븐 게이트가 0.09 라 후보에서 빠졌다).

---

## 19. 파일이 교체되면 라벨을 먼저 지운다

같은 이름으로 다른 녹화가 오면 옛 라벨이 남아 **엉뚱한 채점**을 한다.

> 2026-08-27 — `test_10.csv` 가 교체됐는데(952초/235V -> 240초/218V)
> `real_events.json` 의 옛 항목이 남아 있었다. 지우지 않았으면 길이도 전압도
> 다른 파일에 옛 구간을 대고 채점할 뻔했다.

---

## 20. 재학습이 필요한 실험에는 **채널이 0 인 대조 파일**을 판정 기준에 넣는다

재학습은 안착 운을 섞는다. 그 폭을 모르면 결과가 처방의 것인지 운의 것인지
가를 수 없고, **그러면 좋아져도 나빠져도 읽을 수 없다.**

> 12.114 — 반파 대비 채널 하나를 넣고 캐시부터 다시 만들어 재학습했다.
> 겨냥한 곳(드라이기)은 움직이지 않았고 기존 8파일 유령이 5.33 -> 11.25W 로
> 나빠졌다. 그대로면 "반증" 으로 닫았을 것이다.
>
> 판정 기준 ④에 *"`test_13` 은 그 파일에서 채널이 항상 ~0 이므로 예측이
> 거의 안 바뀔 것"* 을 적어 뒀는데, 파일별로 갈라 보니 이렇게 나왔다.
>
> ```
> 발화 파일 (n=2)    |Δ유령| 4.88W   |ΔF1| 0.018
> 무발화 파일(n=9)   |Δ유령| 5.87W   |ΔF1| 0.026   <- 입력이 바이트 단위로 같은 파일들
> ```
>
> **채널이 0 인 파일이 더 많이 변했다.** 유령 회귀의 정체는 `test.2` 의 충전기
> (0 -> 16.3W)와 `test_4` 의 포트(4.1 -> 31.3W)였고 둘 다 발화 0.0% 다.
> 채널의 효과가 아니라 재학습 잡음이었다.

그리고 **폭을 재고 나서 판정한다.** 12.115 가 2단계 시드만 바꿔 n=4 로 쟀더니
유령 6.05W, 전이 귀속 9건, F1 0.011 이 나왔다 — 이 저장소가 그동안 그보다 작은
차이로 여러 결론을 내려 왔다.

> 12.115.4 — 12.113 이 운영점을 바꾸며 *"판 것: 전이 귀속 44 -> 38"* 이라 적었는데,
> 같은 모델의 시드 넷이 **38 / 47 / 43 / 40** 이었다. 44 와 38 은 같은 분포에서
> 뽑은 두 점이다. **그 교체는 아무것도 팔지 않았다.**

**폭의 하한을 잴 때는 판정 방향에 주의한다.** 2단계 시드만 바꾼 폭은 하한이므로,
**하한 안이면 잡음으로 확정**되고 **하한 밖이면 아직 모른다** (진짜 폭이 더 넓다).
반대로 읽으면 결론이 뒤집힌다.

**대조 파일이 없으면 실험이 아니라 추첨이다.** 새 채널·새 라벨·새 레시피처럼
재학습을 부르는 처방에는, 그 처방이 **닿지 않는 것이 확실한 파일**을 골라
판정 기준에 함께 적는다. 그 파일이 겨냥한 파일만큼 움직이면 판정을 보류한다.

```
잡음 바닥 (2단계 시드 n=4, 12.115.6). 이보다 작은 차이는 근거가 못 된다.
  유령 (8파일 평균)   6.05W       잔차 (11파일 평균)  2.14W
  on/off F1 평균     0.011       전이 귀속 (/59)     9
  총전력 잔차 (합성)   21.78W  <- 12.39.6
```

**이 바닥의 상당 부분이 전력 증강에서 온다** (12.118.8). 기기별 σ 를 실측에
맞춰 좁히자 유령 폭 4.15 -> 2.37W, 전이 폭 9 -> 2~5 로 줄었다. 성능은 안
좋아졌지만 **판정을 쉽게 만드는 손잡이가 하나 생겼다.**

---
## 21. 판별자를 학습 분포에서 살리는 것과 모델이 그것을 쓰는 것은 다르다

**특히 '크기 사전' 은 겹침에서 반대로 작동한다.** 격리에서 옳고 복합에서도
살아 있는 양이라도, 그것을 **학습 사전으로 날카롭게 만들면** 관측이 합 하나뿐인
겹침 구간에서 모델이 **틀리게 맞추기보다 기기를 버린다.**

> 12.118 — 전력 증강이 실측과 거꾸로 걸려 있었다 (저항 4종의 실제 산포는
> 0.1~0.9% 인데 일괄 5% 를 걸고, 충전기·미니PC 는 실제 8~14% 인데 5% 를 걸었다).
> 그 결과 등가저항 판별자가 학습 분포에서 뭉개져 포트↔오븐 d' 가 **2.59** 였다
> (실측 산포로는 18).
>
> 기기별로 실측에 맞춰 다시 만드니 **d' 가 9.03 으로 올랐다.** 복합에서도 그
> 양이 산다는 것을 미리 확인했다 (격리값 대비 편차 중앙값 0.53%).
> **그런데 실측 성능은 나빠졌다.**
>
> ```
> test_12       포트 정밀/재현        핫플 정밀/재현
> 기준선        0.912 / 0.947      0.986 / 0.889
> 좁힌 판       0.940 / 0.759      0.745 / 0.914
> ```
>
> 겹침이 덜한 `test_11` 은 거의 안 변했다. **겹칠 때만 나빠진다.**

**같은 물리를 어디에 거느냐가 결과를 가른다.** 12.117 은 정확히 같은 양
(`V^2/R`)을 **추론 뒤에** 걸어 유령을 8.98 -> 5.23W 로 줄였다. 학습 사전은
겹침에서 기기를 버리게 만들고, 후처리는 이미 정한 조합의 전력만 고친다.

**그래서 크기 제약은 후처리부터 시험한다** — 값도 싸고(재학습 없음) 실패 방식도
안전하다.

---

## 22. 순서가 일치한다고 기제가 아니다 — 절제로 확인하기 전에 '원인' 이라 쓰지 않는다

> 12.120 — SMPS 3종의 편향 순서(프로젝터 > 충전기 > 미니PC)와 `L_harm` 의
> 와트당 지문 크기 순서(0.1219 < 0.1479 < 0.1697)가 **정확히 일치**했다.
> 그것을 근본 원인으로 읽고 절 제목에 그렇게 적었고, 12.87.3 의 '배분 미결정'
> 을 정정한다고까지 썼다.
>
> 그 순서를 **없애는** 조작을 만들어 걸었더니(기울기 균등화, 값은 소수점
> 10자리까지 동일하고 기울기만 1.202/0.990/0.864 로) **아무 일도 안 일어났다.**
> 프로젝터 정밀도 0.832 -> 0.833. 헤지 벌점도 껐지만 0.835. 둘 다 시드 폭 안이다.
>
> **12.87.3 이 그대로 서고 나의 정정이 철회됐다.**

규칙 1 의 사촌이다. 그쪽은 *격리 통계*로 기제를 정하지 말라는 것이고, 이쪽은
**순서·상관의 일치**로 정하지 말라는 것이다. 둘 다 처방이 "그럴듯함" 에서
나올 때 생긴다.

**절 제목과 문서의 단정문은 절제 뒤에 쓴다.** 12.120 은 값이 싸서(2단계 3분)
두 번의 시험으로 닫혔지만, 비쌌으면 그 단정이 다음 사람의 지도가 됐을 것이다.

---

## 부록 — 실험을 설계할 때의 순서

이 규칙들을 순서로 바꾸면 이렇게 된다.

1. **재고 나서 만든다.** 이번 세션에 재지 않고 만들었다가 네 번 헛돌았다
   (12.79/12.80/12.82/12.83)
2. **판정 기준을 돌리기 전에 적는다.** 12.59.5 · 12.66.5 · 12.79.6 이 그 형식이다
3. **대조군이 유효한지 확인한다.** `run_verify_cache_prefix` 로 앞 채널이 바이트
   일치하면 재학습 없이 대조군을 재사용할 수 있다 (12.62 에서 학습 한 번을 아꼈다)
4. **단일 변수로 만든다.** 여러 개를 한 번에 바꾸면 좋아져도 무엇 덕인지 모른다
5. **실패하면 그 실패가 무엇을 배제하는지 적는다.** 소진 목록이 다음 사람의 지도다
   (`HANDOFF_2026-08-25.md` 4절)

---
## 23. 판별력에는 **분모**가 있어야 한다 — 신호만 재면 결론이 뒤집힌다

두 기기의 지문이 얼마나 다른지 재서 "이 차수/특징에 정보가 많다" 고 말할 때,
**같은 기기 안에서 그 값이 창마다 얼마나 흔들리는지도 같이 재야 한다.**
분자만 재면 정보가 아니라 **크기**를 잰 것이다.

> 12.122.1 — `SMPS_PLAN` 이 차수별 판별력을 `‖sig_i − sig_j‖` 로 재라고 적었다.
> 그 자로 재면 차수별로 8~24배 갈려서 *"균등 무게가 어긋나 있다"* 가 나온다.
> 창간 산포로 나누면 h=3~15 가 1.1배로 평평해서 **정반대 판정**이 나온다.
>
> ```
> 프로젝터 vs 충전기      h1     h3     h5     h7     h9    h11    h13    h15
> 기기간 차이          14.5%  14.5%  23.9%  35.1%  47.5%  69.2% 108.9% 169.4%
> 기기 안 산포          2.5%   6.4%  10.7%  16.4%  23.1%  33.9%  61.2%  72.8%
> -------------------------------------------------------------------------
> d'                   4.59   1.61   1.58   1.55   1.48   1.47   1.41   1.52
> ```
>
> **분자와 분모가 같은 속도로 큰다.** 고차로 갈수록 기기간 차이가 커지는 것은
> 맞지만 잡음도 같이 커져서 쓸 수 있는 정보는 늘지 않는다.

같은 실수의 다른 얼굴이 **위상을 버리고 크기만 재는 것**이다. 같은 절에서
h=3 의 기기간 차이가 크기만으로는 0.83% 인데 복소로 재면 14.5% 다.
12.34 가 φ3 d′ 를 12.99 로 잰 그 정보가 크기 열에는 없다.

**규칙**: "이 차수/특징에 정보가 있다" 를 쓸 때는 반드시 셋을 함께 적는다.

    분자   기기간 거리          (크기만인지 복소인지 명시)
    분모   기기 안 창간 산포     (녹화 여러 개를 걸쳐야 한다 — 규칙 5)
    n      양쪽 표본 수

분모를 못 쟀으면 **"차이가 크다" 까지만 쓰고 "정보가 많다" 로 넘어가지 않는다.**

---
## 24. 집계 지표는 어려운 경우를 희석한다 — 층을 먼저 정하고 값을 인용한다

같은 이름의 지표가 **분모에 따라 전혀 다른 값**을 갖는다. 어느 층에서 잰
값인지 안 적으면 나중에 그 숫자를 판정 기준으로 쓸 때 엉뚱한 데를 겨냥한다.

> 12.122.5 — `SMPS_PLAN` 이 *"충전기 재현 0.641"* 을 주 판정으로 걸었다.
> 그 값의 출처(12.121.7)는 **창 층화** 값이다 — "SMPS 1대 + 저항 있음" 창으로
> 자른 재현율이다. 같은 파일들을 **파일 전체**로 재면 0.96 이라 포화로 보인다.
>
> ```
> 같은 체크포인트, 같은 다섯 파일
>   파일 전체            충전기 재현 0.96   <- 여지가 없어 보인다
>   SMPS 1대 + 저항      충전기 재현 0.685  <- 실제로 무너진 자리
> ```
>
> 층을 모르고 파일 전체 값만 보면 **"이미 포화라 실험할 가치가 없다"** 는
> 잘못된 결론에 이른다.
>
> ⚠ **그런데 그 층의 잡음도 따로 재야 한다.** 시드 0 에서 0.685 -> 0.806 이
> 움직여 "판정선 통과" 라 적었는데, 시드 1 은 0.730 -> 0.658 로 내려갔다.
> **그 층의 기준선 SD 가 0.106 이고 효과는 +0.095 로 폭 안이었다** (12.122.5).
> 층화 지표는 예민한 대신 시끄럽다 — 같은 지표의 시드 SD 가 파일 전체
> 0.013 vs 이 층 0.106 으로 **8배**다.

**규칙**: 지표를 문서에 적을 때 분모를 함께 적는다.

    "충전기 재현 0.641"                          <- 금지
    "충전기 재현 0.641 (SMPS 1대 + 저항 층, n=...)"  <- 이렇게

그리고 그 숫자를 **다른 실험의 판정 기준으로 옮길 때는 같은 층으로 다시 잰다.**
`src/run_stratified_smps.py` 가 12.121.7 의 층을 재현한다.

---
## 25. 처방을 판정하려면 **그 처방이 겨냥한 것을 재는 자**가 있어야 한다

부작용만 잴 수 있으면 모든 처방이 실패로 보인다.

> 12.122.7 — 프로젝터 스냅을 유령·잔차로만 재고 "반증" 으로 닫았다(12.122.4).
> 기기별 전력 오차 지표를 만들자 같은 처방이 **겨냥한 것을 거의 완전히
> 고치고 있었다**:
>
> ```
>                    유령      잔차   |  프로젝터 전력오차   격리폭 안
> 운영점             2.54W    4.25W  |       8.1W        0.021
> 운영점 + 스냅       2.96W    4.34W  |     **0.5W**      **0.999**
> ```
>
> 부작용만 보면 실패, 겨냥한 것을 보면 16배 개선이다. **맞바꿈이지 실패가 아니다.**

**규칙**: 새 처방을 설계할 때 판정 기준을 적기 전에 먼저 묻는다.

    이 처방이 고치려는 양을 **지금 재고 있는가?**
    못 재고 있으면 **지표를 먼저 만든다.** 그 전에는 판정하지 않는다.

지표가 없으면 "안 나빠졌다" 까지만 쓸 수 있고 "효과가 없다" 는 못 쓴다.
그리고 **이미 닫은 판정도 다시 봐야 한다** — 같은 이유로 잘못 닫혔을 수 있다.

[참값이 없다고 단정하기 전에]
4.2절이 *"실측에는 기기별 정답이 없다"* 고 적었고 그 문장이 이 저장소의 채점을
총합과 유령 둘로 묶어 놓았다. **넷은 있었다** — 격리 통전 전력 폭이 좁은 기기
(포트·핫플·오븐·프로젝터)는 "켜져 있으면 몇 W" 가 정해져 있다.
`run_power_check --recompute-ref` 가 그 판정을 자동으로 한다 (폭/중앙 0.10 초과면 제외).

---
## 26. "새 녹화가 필요하다" 고 적기 전에 **이미 있는 변동**을 먼저 훑어라

측정 요청은 비싸고 되돌릴 수 없다. 그 전에 기존 녹화가 그 변수를 이미 얼마나
덮는지 세어 본다. **의도해서 만든 변동만 변동이 아니다.**

> 12.122.9 — *"저항 부하 하에서의 충전기 지문이 필요하다"* 며 75분짜리 토글
> 녹화를 1순위로 적었다. 그런데 충전기 **격리** 녹화의 전압이 이미
> 216.4~238.3V (폭 21.9V) 였다 — 저항이 만드는 강하(4~8V)보다 넓다.
> 계통 전압이 시간에 따라 흔들린 것이 공짜 변동이었다.
>
> 전력과 전압을 동시에 회귀하니 **찾던 효과(전압)가 2%, 안 보던 효과(전력)가
> 17~95%** 였다. 새 녹화의 근거였던 가설이 부차적이었다.

**규칙**: 측정 요청서에 이 세 줄을 먼저 채운다.

    겨냥한 변수        V (전압)
    기존 자료의 폭     216.4~238.3V, n=4,126 창      <- 세어 본다
    요청이 더할 폭     -4~-8V                        <- 비교한다

기존 폭이 요청보다 넓으면 **요청을 미루고 기존 자료부터 회귀한다.**

[교락을 같이 확인한다]
공짜 변동은 대개 다른 변수와 얽혀 있다. 위 사례에서 corr(P,V)=+0.40 이라
전력 분위로 자른 `sig(P)` 에 전압 효과가 섞여 있었다. **층화가 아니라 회귀로
가른다** — 층 안에서 두 변수가 같이 움직이면 층화는 아무것도 안 가른다.

---
## 27. 비(比)가 좋아진 것과 좋아진 것은 다르다 — 분모가 같이 줄었는지 보라

두 값의 비로 판정할 때, 비가 1 에 가까워진 것이 **분자가 줄어서인지 분모가
줄어서인지** 갈라야 한다. 아무것도 설명 못 하게 만들면 비는 저절로 1 이 된다.

> 12.122.10 — 순방향 모델을 고치려고 `sig(P,V)` 를 넣었다. 판정 지표는
> "자유해 잔차 대비 정답 support 잔차의 비" 였고 1.42 -> 1.11 로 **좋아졌다.**
>
> ```
>              자유잔차   정답잔차     비      오차/판별신호
> 고정          0.1375   0.1953   1.42        3.4
> sig(P,V)     0.2106   0.2409   1.11        8.0     <- 비는 좋고 실제는 나쁘다
> ```
>
> 비가 준 것은 정답이 좋아져서가 아니라 **자유해가 같이 나빠져서**다.
> 절대 잔차는 둘 다 +23% 나빠졌다. 모형이 관측을 덜 설명하게 되자 어느 배분을
> 골라도 비슷하게 나쁜 상태가 됐을 뿐이다.

**규칙**: 비를 판정에 쓸 때는 분자·분모의 **절대값을 함께 적는다.** 그리고
가능하면 비 대신 **신호 대 잡음**으로 적는다 — 위 사례에서는

    오차 / 판별신호 = 정답잔차 / (정답잔차 − 자유잔차)

가 3.4 -> 8.0 으로 **나빠진 것**을 바로 보여준다. 비(1.42 -> 1.11)는 반대로 읽힌다.

[같은 함정의 다른 얼굴]
"차이가 사라졌다" 를 개선으로 읽는 것. 두 판이 같아진 것은 처방이 통한 것일
수도 있고 **양쪽 다 바닥을 친 것**일 수도 있다. 절대값을 보면 갈린다.

---
## 28. 한 방법이 막힌 것과 자료가 부족한 것은 다르다

"이 방법으로는 안 된다" 에서 "새 자료가 필요하다" 로 넘어가려면 **남은 방법을
다 세어 봐야 한다.** 측정 요청은 비싸고, 대개 방법이 하나 더 있다.

> 12.122.3/9/11 — 한 세션에서 토글 녹화의 우선순위를 **세 번** 바꿨다.
>
> ```
> 12.122.3   "가산성을 못 잰다"            -> 토글 1순위
> 12.122.9   "격리에 전압 변동이 이미 있다"   -> 내림
> 12.122.10  "격리->복합 전이가 실패했다"     -> 다시 올림
> 12.122.11  "복합에서 직접 풀면 된다"       -> 내림. 배분 오차 2.6배 감소
> ```
>
> **세 번 다 한 가지 방법이 막힌 것을 자료 부족으로 읽었다.** 실제로는
> 지문을 얻는 길이 셋이었다 — 격리 상수 / 격리 적합 후 전이 / **복합에서 직접**.
> 마지막을 12.122.11 까지 아무도 안 봤다.

**규칙**: 측정 요청서에 **"남은 방법" 칸**을 만들고, 비워 두지 않는다.

    막힌 방법      격리에서 sig(P,V) 적합 -> 전이 실패
    남은 방법      ① 복합에서 직접 적합 (사람 라벨이 ON 집합을 준다)
                  ② 전이 Δ 로 단일 기기 분리
                  ③ ...
    요청이 유일한 길인 이유    <- 여기를 못 채우면 요청하지 않는다

[한 방법이 막혔을 때 먼저 물을 것]
그 방법이 의존한 **가정**이 무엇이었나. 12.122.10 은 "격리에서 적합한 관계가
복합에 전이된다" 였다. 그 가정을 **안 쓰는** 방법이 곧 남은 방법이다.

---
## 29. 보존 제약이 걸린 계에서 오차는 사라지지 않고 **옮겨 다닌다**

총합을 맞추라는 항(`L_cons`)이 있으면, 어느 기기의 예측을 고쳐도 **총량 오차는
그대로고 자리만 바뀐다.** 겨냥한 기기가 좋아지고 다른 데가 나빠지면 그 기기는
**원인이 아니라 배수구**였을 수 있다.

> 12.122.12 — 지문을 고쳐 프로젝터의 과대예측을 없앴다 (평균 +30.6W -> −1.3W).
> **유령이 3.5 -> 10.8W 가 됐다.** "배수구를 막으니 옆으로 샜다" 로 읽었다.
>
> ⚠ **그 읽기는 12.122.14 에서 반증됐다.** 층을 가르니 유령 증가의 93% 가
> 적합 집합 밖 두 파일에 몰려 있었다 — **전가가 아니라 일반화 실패**였다.
> 그리고 프로젝터 과대예측은 저항이 **없을 때 오히려 더 컸다** (+31 vs +17W).
>
> **이 규칙이 틀린 것이 아니라 적용이 틀렸다.** 회계를 한 층("프로젝터 ON +
> 저항 ON")에서만 냈다. 아래 절차의 마지막 줄을 안 지켰다.

**규칙**: 어떤 기기의 예측을 겨냥하기 전에 먼저 묻는다.

    이 기기의 오차는 **자기 것**인가, 남의 오차를 받아 준 것인가?
    총량 오차가 0 이 아닌데 이 기기만 고치면 그 차액은 어디로 가는가?

가르는 법: 겨냥한 기기를 고친 판에서 **전 기기의 예측을 회계로 적는다.**
겨냥한 기기가 내놓은 만큼이 다른 데로 갔으면 배수구다. 총량 오차가 같이
줄었으면 진짜 원인이다.

**그리고 회계를 층마다 따로 낸다** (규칙 24). 한 층에서만 내면 전가와
일반화 실패를 못 가른다 — 12.122.13 이 그렇게 틀렸다. 최소한 이 셋:

    겨냥한 조건이 있는 층 / 없는 층      전가라면 없는 층에서 안 나야 한다
    적합·학습 집합 안 / 밖              일반화 실패라면 밖에서만 커진다
    파일별                            한두 파일에 몰려 있으면 전가가 아니다

[증상을 고치는 것이 무가치하다는 뜻은 아니다]
12.100 의 상한 후처리는 배수구를 막은 것인데 이 저장소 최대 이득이었다.
다만 **그것이 근본 원인이라고 쓰면 안 되고**, 그 다음 처방을 같은 자리에서
찾으면 안 된다.

---
## 30. 가드가 안 걸리면 **가드가 약한 것부터** 의심하라

이상값을 거르는 조건을 넣었는데 걸리는 것이 거의 없으면, 두 가지다.

    ① 이상값이 원래 없었다        <- 이렇게 읽고 싶어진다
    ② **가드가 그 이상값을 못 잡는다**

②를 먼저 확인한다. 가드가 약한 쪽이 훨씬 흔하다.

> 12.122.7/15 — 세션 단위 라벨(오븐)이 "ON 인데 안 흐르는" 창을 만드는 것을
> 알고 있었고, `P관측 >= 그 기기 참값 x 0.8` 가드를 넣었다. 넣기 전후로
> **n 이 35,263 -> 35,213** 이었다. 그것을 *"과소예측은 실재한다"* 의 근거로 썼다.
>
> **가드가 약했던 것이다.** 저항이 둘 켜졌다는 창에서 하나만 흐르면 그 하나의
> 참값은 넘으므로 안 걸린다. 라벨상 켜진 기기들의 참값 **합**으로 바꾸니:
>
> ```
>                  기기별 가드          합 가드
> 포트 평균오차      -73.9W            -3.4W
> 오븐 평균오차      -53.8W            -8.1W
> ```
>
> 실재한다던 40~90W 과소예측이 **없었다.**

**규칙**: 가드를 넣으면 **거른 개수와 거른 예시를 함께 적는다.**

    가드          P관측 >= 그 기기 참값 x 0.8
    거른 창        50 / 35,263  (0.14%)          <- 이 숫자를 보고
    확인          이상값 표본을 직접 열어 본다      <- 여기서 멈추지 않는다

거른 것이 예상보다 적으면 **이상값을 직접 골라 가드가 왜 안 걸었는지 본다.**
0.14% 는 "이상값이 없다" 가 아니라 "가드가 0.14% 만 잡는다" 는 뜻이다.

---
## 31. 자유도를 늘려 잔차를 줄이기 전에 **식별성이 남는지** 보라

모형에 열을 더하면 잔차는 반드시 준다. 그것이 개선인지 아닌지는
**같은 관측을 다른 답으로도 설명할 수 있게 됐는지**로 판정한다.

> 12.122.18 — "지문 형상이 틀렸으면 기기당 부분공간을 주자" 를 재 봤다.
>
> ```
> rank   자유잔차   정답잔차   오차/신호   평균 열수/16차원
> 1     0.0599   0.1643    1.57      2.6
> 2     0.0000   0.1010    1.00      5.2
> ```
>
> 잔차는 −39% 인데 **자유해가 관측을 완전히 맞춘다** (0.0000). 기기 9개 x
> rank2 = 18열이 16차원을 덮기 때문이다. 어떤 관측도 어떤 조합으로든 설명되니
> 배분이 결정 불가가 된다. **기기당 벡터 하나였던 것이 곧 식별성의 원천이었다.**

**규칙**: 자유도를 늘릴 때 셋을 함께 적는다.

    미지수 / 식             18 / 16      <- 1을 넘으면 그 자체로 경고
    자유해 잔차              0.0000       <- 0 에 가까우면 아무것도 못 가린다
    정답 대비 자유해의 이득    (t−f)/t      <- 이것이 판별 신호다. 0 이면 죽었다

잔차만 보고 판정하면 **완전히 결정 불가가 된 모형이 가장 좋아 보인다.**

[조건화는 자유도가 아니다]
`sig(state)` 처럼 **관측에서 결정되는 것**에 매인 조건화는 자유도를 안 늘린다.
`sig(P)` 처럼 **풀어야 할 미지수**에 매이면 순환이 되어 자유도와 같아진다.
그 구분이 채택 여부를 가른다.

---
## 32. 조합 실험의 가법성은 **지표마다 따로 판정한다**

두 처방을 같이 걸면 "이득이 더해지나 겹치나" 를 묻게 된다. 그 답은 **하나가
아니다.** 지표 하나로 결론내면 다른 축에서 뒤집힌다.

> 12.123 — 불감대 4.0 과 in-situ 지문을 같이 걸었다. 한 지표만 봤으면
> 정반대 두 결론 중 아무거나 적었을 것이다.
>
> ```
> 지표          불감대Δ   지문Δ   가법예측   조합Δ   판정
> 프로젝터전력W    -8.8   -12.2   -20.9   -11.9   **겹침** (지문 단독과 같다)
> F1           +0.029  +0.001  +0.030  +0.041  **초가법** (합보다 크다)
> ```
>
> 계획은 *"지문이 잔차를 줄이고 불감대가 나머지를 용서한다"* 로 **가법**을
> 예측했다. 전력에서는 겹치고 검출에서는 초가법이다. 두 처방은 **다른 것을
> 고치고 있었다** — 불감대는 검출, 지문은 배분.

**규칙**: 조합 실험에는 축마다 한 줄씩 적는다.

    지표 | 처방A Δ | 처방B Δ | A+B (가법예측) | 조합 실측 | 잡음바닥 | 판정

판정선은 **기준선 시드 SD** 다 (규칙 20). `|조합−가법예측| < SD` 면 가법,
`|조합−큰쪽| < SD` 면 겹침, `|조합| > |가법예측|+SD` 면 초가법이다. 그 폭은
**하한**이므로 안이면 확정, 밖이면 미확정이다.

---
## 33. 채점 산출물에는 **그것을 만든 명령**을 같이 남긴다

플래그가 결과 파일의 이름이나 태그에 안 붙으면, 그 파일은 나중에 아무것도
증명하지 못한다.

> 12.123 — `gc_*.json` 은 설정 메타가 없고 `--rm-snap` 은 태그에도 안 붙는다
> (`run_gate_check` 는 `+pp`, `+rm0.02`, `+snap` 만 붙인다). 그래서 전 세션이
> 낸 표를 어떤 명령으로 냈는지 파일만 보고는 알 수 없었다. 결국 **12개
> 체크포인트를 하나의 명령으로 다시 채점**해야 했다.

**규칙**: 채점기는 `_config` 키에 `argv` 를 통째로 넣는다. 넣을 수 없으면
같은 이름의 `.log` 를 남기고 첫 줄에 명령을 적는다. **다시 돌리는 값보다
"이 표가 무엇인지 모른다" 가 비싸다.**

---
## 34. 집계표를 인용하기 전에 **어떤 파일 집합의 평균인지** 적어라

규칙 24 의 따름이다. 층을 희석하는 것만 문제가 아니라, **같은 산출물에서
서로 다른 숫자가 나오는데 어느 것인지 표에 안 적혀 있는 것**이 문제다.

> 12.123 — 같은 `gc_combo4.json` 에서 기준선 on/off F1 이
>
> ```
> 겨냥 8파일      0.846   <- 12.122.16/17 이 쓴 값
> 전 11파일       0.839
> 사람기록 7파일   0.853
> 사람−대조 5파일  0.873
> ```
>
> 이다. 판정선이 "+0.029" 인데 집합을 잘못 고르면 같은 처방이 +0.012 로도
> +0.020 으로도 보인다. 표를 재현하는 데 네 조합을 끼워 봐야 했다.

**규칙**: 표 밑에 한 줄을 붙인다 — *"유령대조만 대조 3파일이고 나머지는 전부
겨냥 8파일 평균이다"* 처럼. 열마다 집합이 다르면 **그것도 적는다.**

---
## 35. 기울기를 만지는 절제는 **최적점을 못 옮긴다** — 값을 만져야 한다

"어느 기기가 싸서 오차를 받는다" 같은 가설은 **손실 지형의 최소가 어디인가**에
대한 주장이다. 그것을 시험하려면 **값**을 바꿔야 한다. 파라미터별 기울기 배율을
바꾸는 절제는 **경로만 바꾸고 도착지는 그대로 둔다.**

> 12.120.5/12.122.19 — 12.120.3 이 *"프로젝터가 와트당 지문이 가장 작아서
> `L_cons` 의 부족분을 받는다"* 를 정확히 적었다. 그것을 시험한 절제 A 는
> `p_eff = P·w + (P·(1−w)).detach()` 로 **값을 보존하고 기울기만** 균등화했다
> ("물리 보존" 이 설계 의도였다). 안 움직였고 12.120.9 가 원인 가설을 철회했다.
>
> **절제가 가설을 시험할 수 없는 설계였다. 두 이유가 독립으로 겹친다:**
> ```
> ① ∇L=0 인 자리는 파라미터별 배율과 무관하다. 최적점이 안 움직인다
> ② 그리고 **Adam 이 상쇄한다** — m/√v 에서 m ∝ w, v ∝ w² 이라
>    상수 배율이 정확히 나눠진다. run_adapt 는 AdamW 를 쓴다
> ```
> 1년 뒤 값(지문)을 바꾸자 배수구가 실제로 옮겨갔다 (12.122.17).

**규칙**: 절제를 설계하고 나서, 돌리기 전에 묻는다.

    이 조작이 **손실의 값**을 바꾸는가, **기울기만** 바꾸는가?
    가설이 "어디가 최소인가" 에 대한 것이면 **값을 바꿔야 한다**
    옵티마이저가 Adam 계열이면 파라미터별 상수 배율은 **거의 무작용이다**

[기울기 조작이 유효한 자리]
"학습이 그 축으로 잘 안 간다"(수렴·안정성) 는 가설이면 기울기 조작이 맞다.
"수렴하면 그 자리에 있다"(편향·배분) 는 가설이면 값을 바꿔야 한다.
**12.120.5 는 후자를 전자의 도구로 시험했다.**

---
## 36. 아는 결함은 **절제의 축으로 올려라**

결함이 이미 확인된 항을 켜 둔 채 새 항을 시험하면, **새 항의 효과**와 **새 항이
옛 결함을 이기는 능력**이 한 숫자에 섞인다. 켜 두는 것이 옳은 선택이더라도
끈 칸을 함께 돌려야 읽을 수 있다.

> 12.134 — 12.122.2 가 `L_harm` 의 최소는 오답 쪽이라고 확정했고 12.132.1 이
> 그것을 끄면 프로젝터 오차가 −18.9W 라고 쟀다. 그 상태에서 무효전력 항을
> `w_harm 4.0` 그대로 두고 더했다. 근거는 있었다 — 고조파를 버리면 새 기기
> 앞에서 배울 것이 없으니 **끄기는 처방이 아니다**.
>
> 결과는 실패였고 원인도 밝혀졌지만(Q 가 비가산), **"`w_harm` 을 껐으면 Q 가
> 도왔을까" 는 그 자료로 답할 수 없다.** 2×2 가 3판 더였다.

**규칙**: 새 항을 시험할 때, 이미 결함을 아는 기존 항이 있으면 그것을 **끈 칸을
함께** 돌린다. 판이 두 배가 되는 것이 아니라 **읽을 수 있는 판이 되는 것**이다.

[운영 선택과 실험 설계를 가르는 것이기도 하다]
"운영에서는 켜 둘 것" 과 "실험에서는 축으로 둘 것" 은 충돌하지 않는다.
운영점 후보를 정하는 판과 기제를 가르는 판은 원래 다른 판이다.

---
## 37. sim2real 비교는 **선택 기준을 양쪽에 똑같이** 걸어라

합성과 실측을 견줄 때, 창을 고르는 조건이 조금만 달라도 **원하는 결론이 나온다.**
합성에는 타깃 시점 라벨이 있고 실측에는 구간 라벨이 있어서, 자연스럽게 쓰는
조건이 서로 다르다는 것이 함정이다.

> 12.138 — 합성 창을 `y_on`(타깃 시점 켜짐), 실측 창을 `on.mean(0) > 0.5`(창의
> 절반 이상)로 골라 고조파 함량을 견줬다. **실측이 2.07배 크게 나왔고**
> "합성기가 비선형성을 덜 만든다" 로 읽었다.
>
> 그런데 `y_power` 도 타깃 시점 값이라 창평균 고조파와 짝이 안 맞는다. 창 일부만
> 켜진 창이 섞이면 그것만으로 관측이 예측보다 작아진다. 정상상태 창만 골라
> 다시 재니 **0.57 -> 1.04** 로 뒤집혔다.
>
> 조건을 양쪽 다 맞추자 **실측 표본이 0~2개가 되어 비교 자체가 성립하지 않았다.**

**규칙**: sim2real 비교를 적기 전에 셋을 적는다.

    합성 창을 고른 조건 / 실측 창을 고른 조건 / **그 둘이 같은가**
    라벨의 시간 해상도가 다르면(타깃 시점 vs 구간) 그것부터 맞춘다
    맞췄더니 표본이 없으면 **"반증" 이 아니라 "못 잰다"** 다 (규칙 28)

---
## 38. 자를 만들 때 **판정에 쓸 파일을 그 자에도 넣어라**

규칙 20 은 재학습 실험에 대조 파일을 요구한다. **자에도 똑같이 걸린다.**
대조가 없는 자로 고른 처방은, 대조에서 나빠져도 그 자가 알려 주지 않는다.

> 12.136 이 차수 가중 `1/h²` 을 고른 근거는 `모델오차/판별신호` 2.23 -> 1.57
> 이었다. 그 자를 만든 창은 `HUMAN_ON_DEFAULT_STEMS` — **겨냥 5파일뿐**이다.
>
> 12.142 가 대조 3파일을 넣고 다시 풀었다. 비는 대조에서도 4.41 -> 2.02 로
> 같은 방향인데, **자유해가 정답에 없는 SMPS 에 얹는 와트**는 반대다:
>
> ```
> 가중         모델오차/판별신호   유령 SMPS W
> 균등              4.41           7.2
> **1/h²**          2.02           9.4   <- 비는 좋아지고 유령은 31% 나빠진다
> h1,h3 만          1.00           9.6   <- 12.136 이 '바닥' 이라 부른 자리
> ```
>
> 그리고 그 열이 학습 결과를 예측한다 (예측 7.2 -> 9.4, 실측 유령대조
> 7.09 -> 8.22). **회귀는 재학습 잡음이 아니라 처방의 직접 귀결이었다.**

규칙 25 의 사촌이다. 그쪽은 "처방이 겨냥한 것을 재는 자가 있어야 한다" 이고,
이쪽은 **"처방이 망가뜨릴 수 있는 것을 재는 자도 있어야 한다"** 이다.
비(比)를 자로 쓸 때 특히 그렇다 — 비는 잔차의 **크기**를 재지 그 잔차가
**어디로 옮겨 가는지**를 재지 않는다 (규칙 27·29 와 같은 자리).

---
## 39. 자는 **처방이 도는 입자**로 만든다

같은 양이라도 창의 길이가 다르면 다른 것을 잰다. 후처리가 0.5초 창에서 도는데
자를 60초 창으로 만들면, 그 60초 안에서 상태가 바뀌는 것은 **정의부터 다르다.**

> 12.143 이 등가저항을 손실에 넣으면 어떻게 되는지 재려고 12.135 의 3600사이클
> (60초) 창을 그대로 썼다. **저항 조합 정답률이 6% 로 나왔다** — 그 안에서
> 핫플·오븐의 온도조절기가 껐다 켜서 '켜진 조합' 자체가 성립하지 않는다.
> `resistive_match` 가 도는 stride 30(0.5초)으로 맞추자 49~80% 가 됐다.
>
> 같은 자리에서 한 번 더 틀렸다. **조합을 고조파 잔차로 골랐는데** 저항끼리는
> h1 이 다 4.6 mA/W 라 고조파로는 구분이 안 된다. 조합을 정하는 것은
> 전력(컨덕턴스)이고, 그래서 `resistive_match` 가 `G = P/V²` 를 맞춘다.

**자가 이상한 값을 내면 처방을 의심하기 전에 자를 의심하라.** 정답률·표본 수처럼
**자 자신이 제대로 도는지 보여 주는 열**을 같이 찍어 두면 그 자리에서 잡힌다.

---
## 40. 미설명량을 새 항으로 받으려거든 **기존 항의 용량부터 세라**

"이 값이 갈 자리가 없다" 는 진술은 **자리를 다 세어 본 뒤에만** 할 수 있다.

> 12.140 이 실측 고조파 바닥(h1 39~103 mA)을 보고 *"갈 자리가 `noise_sig`
> (8.62 mA) 뿐"* 이라 적고 그것을 키우자고 제안했다. 3.4절은 배출구를 **둘**
> 마련해 뒀다 — 계측 항과 **대기 항**이고, 대기 항 용량은 h1 기준 **177 mA** 다.
>
> 그리고 모델이 이미 쓰고 있었다. `Σ idle·standby_sig` 가 test_11 에서 36.9 mA
> 인데 실측 배경이 39.3 mA 다. **키웠으면 이중 계상만 됐다.**

손실식을 **항별로 적어 놓고 각 항이 낼 수 있는 최대치를 숫자로** 쓴 다음,
**모델이 지금 얼마를 쓰고 있는지**를 순전파로 확인한다. 셋을 다 보기 전에는
새 항을 제안하지 않는다.

---
## 41. 어떤 축을 닫기 전에 **그 축의 변수가 움직이는 자료로** 재라

자료를 골라 놓고 "효과가 없다" 고 적으면, 효과가 없는 것이 아니라 **그 자료에서
그 변수가 안 움직인 것**일 수 있다. 닫기 전에 **적합에 쓴 자료의 동적 범위**를
숫자로 적어라.

> 12.147 이 Norton/교차주파수 어드미턴스를 SMPS 3파일로 적합해 홀드아웃에서
> 졌다 (30.5 vs 맹탕 전역상수 24.1). 거기서 축을 닫으려 했다.
>
> **하필 그 3파일이 전압이 가장 평평한 조합이었다:**
>
> ```
> 적합에 쓴 3파일     vh1 폭 2.1~3.4V    vh5 1.53~2.80V
> 전 11파일          vh1 209.6~223.2V   vh5 1.53~8.01V   (5.2배)
> ```
>
> 저항이 큰 파일이 전압을 끌어내린다. 전 파일로 적합하니 **39.4 -> 12.3W** 로
> 뒤집혔고, 상수로는 원리적으로 못 고친다고 적었던 `test_7` 이 +51.2 -> +4.7 이
> 됐다 (12.148).

**축을 고르는 것과 자료를 고르는 것이 얽혀 있다.** SMPS 를 겨냥하니 SMPS 파일을
골랐는데, **그 축(전압 왜곡)을 흔드는 것은 저항 부하**였다. 겨냥한 기기가 있는
파일이 그 기기를 설명하는 변수를 흔드는 파일과 **다를 수 있다.**

절차: 처방을 닫기 전에 ① 그 처방이 쓰는 변수를 적고 ② **가진 자료 전체에서**
그 변수의 폭을 재고 ③ 적합에 쓴 부분집합의 폭과 견준다. 후자가 크게 좁으면
결론은 "효과가 없다" 가 아니라 **"이 자료로는 못 잰다"** 다 (규칙 28).

---
## 42. 잔차를 설명하는 자와 **과제를 재는 자**는 다르다

`R²`·설명 에너지가 오르는 것과 과제가 풀리는 것은 별개다. 처방을 판정할 때는
**과제의 지표**로 해야 한다 (규칙 25 의 따름이지만, 회귀를 쓸 때 특히 잘 어긴다).

> 2026-09-01 세션이 같은 자리에서 **세 번** 갈렸다:
>
> ```
> 처방                     잔차/손실 설명              실제 과제
> 단일기기 지문 (12.140)     ΔL_harm 88% 제거          학습 유령 3.54 -> 20.55W  ❌
> 최소부하 오프셋 (12.147)   —                        분위가 자유 파라미터       ❌
> V_h 어드미턴스 (12.148)   잔차 76.1% (최고)         홀드아웃 30.5 > 24.1     ❌(3파일)
> ```
>
> 셋 다 전자에서 이기고 후자에서 졌다. 세 번째는 자료를 넓히니 후자에서도
> 이겼지만(12.3), **그것을 확인한 것은 배분 오차이지 설명 에너지가 아니다.**

특히 **평균 |오차| 를 볼 때 부호를 같이 보라.** 12.147 의 최소부하 35% 분위가
평균 19.6 으로 최소였는데 개별 오차가 +4.5/−18.7/−35.8 이었다 — 부호가 갈려
상쇄된 것이고, 그 지점의 오프셋은 기기 전류를 통째로 빨아들이고 있었다.

---
## 43. 지표가 세는 양과 **운영이 내보내는 양**이 같은지 먼저 맞춰라

지표를 인용하기 전에 그 값을 사용자가 실제로 보는지 확인하라. 둘이 어긋나면
**지표에만 있는 결함**을 쫓게 되고, 반대로 **지표가 못 보는 결함**을 놓친다.

> 2026-09-02. 유령 지표(`score_absent`)는 `P̂ = σ(on)·p_raw` 를 그대로 센다.
> 그런데 `run_live.render` 와 `score_on_off` 는 `σ > 0.5` 인 기기만 내보낸다.
> 두 자가 다르므로 **σ=0.008 x p_raw=592W = 4.9W** 가 지표에는 잡히고 화면에는
> 안 나온다.
>
> ```
> 복소 Z·I    유령8   소프트 6.78  /  하드 0.83     하드로 보면 기준선(1.17)보다 낮다
>            에어컨          5.59  /       0.021    on_rate 0.0001
> ```
>
> 인수인계가 *"유령8 의 82%가 에어컨"* 이라 적고 *"그 열을 빼거나 게이트를
> 막는 것이 다음 수"* 라 했다. **열은 이미 막혀 있었다.** 진짜 문제는
> "OFF 라고 보고한 기기가 와트를 낸다" 는 **정합 결함**이었고, 처방도 달랐다
> (열 절제가 아니라 게이트 정합 + 잔차 흡수. 재학습 0회).

절차: 새 지표를 만들거나 인용하기 전에 ① 그 값이 운영 경로의 어느 출력인지 적고
② 운영이 이진화·상한·후처리를 거치면 **같은 변환을 지표에도 걸어** 두 값을 나란히
남긴다 (`run_gate_check` 의 soft/hard 두 열이 그것이다).

⚠ **반대 방향도 같다.** 화면이 안 보여준다고 없는 양이 아니다 — 그 4.9W 는
`L_cons` 를 떠받치고 있었고, 그냥 지우면 잔차가 4.52 → 8.80W 로 커졌다
(규칙 29). 지표에만 있는 양이라도 **무엇을 지탱하는지는 따로 재라.**

규칙 25 의 사촌이다. 그쪽은 "처방이 겨냥한 것을 재는 자가 있어야 한다" 이고
이쪽은 **"그 자가 재는 것이 운영에 실재하는가"** 다.

---
## 44. 계수를 옮기기 전에 **장소의 값인지 기기의 값인지** 갈라라

적합한 상수를 다른 현장으로 가져갈 때, 그것이 **무엇의 속성인지** 먼저 갈라야
한다. 안 가르면 옮겨도 되는 것을 다시 재고, 옮기면 안 되는 것을 그냥 쓴다.

> 12.148 의 `Z·I` 보정은 계수가 하나로 보였지만 셋이 섞여 있었다:
>
> ```
> Z (R, L)      **장소** — 배선. 장소 간 3.2배. 라벨 0 으로 11초에 재추정
> A 기울기 (ΣY)  **기기** — 어드미턴스 합. 기기 구성이 같으면 그대로
> A 절편 (V_src) **장소·시간** — 배경 왜곡. 장소 간 2.1배. 전이 안 된다
> ```
>
> 가르기 전에는 *"장소가 바뀌면 라벨을 받아 전부 다시 학습"* 으로 보였다. 가르고
> 나니 **`Z` 만 다시 재고 2단계를 다시 돌리면 되고, 그 둘은 라벨이 필요 없다**
> (12.150.2). 재학습이 아니라 재적응이고 5분이다.

**가르는 방법은 그 값을 독립적으로 추정할 수 있는지 보는 것이다.** `Z` 를 `|V|`
와 `I` 로 따로 풀었기 때문에 회귀 기울기가 `ΣY` 로 **식별**됐다. `Z` 를 모른 채
회귀만 했다면 `A·Z` 가 곱으로만 정해져 둘이 축퇴하고, 그러면 `Z` 를 다시 재는 것이
오히려 해로웠을 것이다 (12.150.2 가 그 걱정을 정정했다).

⚠ **가르는 것과 확인하는 것은 다르다.** `ΣY` 라는 해석은 물리에서 나오지만
**다른 집 라벨이 없어 실측으로 못 쟀다.** 문서에 그렇게 적어야 한다.

---
## 45. 상수로 굳힌 측정에는 **측정 조건이 같이 굳는다** — 정의로 정해지는 성분을 나눠 내라

기기의 성질이라고 믿고 상수로 박아 둔 값이 있으면, 그 값의 성분 중 **보존식이나
정의로 이미 정해지는 것**이 무엇인지 먼저 따져라. 그 성분은 대상에 대해 아무것도
말하지 않는다 — **측정할 때의 조건**을 말한다.

> `harmonic_signatures` 는 기기별 와트당 h1 페이저를 상수로 박는다. 그런데
> 유효전력의 정의가 `P = V₁·I₁·cos φ₁` 이므로
>
> ```
>     Re(I₁)/P = 1/V₁        <- 기기와 무관하다. 측정이 아니라 정의다
> ```
>
> 그러면 `1/Re(sig[k,1,0])` 은 **그 기기 격리 녹화의 선전압**이다. 맞는다:
>
> ```
> 전기포트 212.9(녹화 213.5)  오븐 215.7(216.0)  프로젝터 224.4(223.4)
> 드라이기 234.5(228.7)      선풍기 236.2(234.0)  에어컨 238.1(232.9)
> ```
>
> 손실은 이 **11.8% 를 기기 판별자로 쓰고 있었다.** 같은 드라이기를 233.8V 와
> 217.7V 에서 찍은 두 녹화가 와트당 7.4% 다른 것이 결정적이다 (12.151.1).

**찾는 방법.** 모형의 상수마다 이렇게 물어라 — *"이 값에서 보존식·정의·항등식으로
이미 정해지는 부분이 있나?"* 있으면 그것을 나눠 낸 나머지가 진짜 측정값이다.
실측 13파일에서 `Re(I₁)·V/P` 가 0.9973 (0.983~1.001) 로 나온 것이 그 확인이다.

⚠ **나눠 냈다고 좋아진다는 보장은 없다. 이번에는 안 좋아졌다.** 정규화+추종을
넣은 판이 프로젝터 −1.98 / 유령8 6.95 / F1 0.890 으로 그냥 둔 판(−1.63 / 6.78 /
0.887)과 **시드 폭 안에서 같았다** (12.151.2). 그리고 예측했던 기전 — 에어컨 유령이
줄어든다 — 은 **반증됐다.** 배율은 크기만 바꾸고 각도를 안 바꾸는데, 유령을 정하는
것은 각도였기 때문이다.

**그래도 재는 값이 있다.** 이 규칙이 주는 것은 성능이 아니라 **그 숫자가 무엇이
아닌지에 대한 확실성**이다. 지문의 h1 차이를 앞으로 "기기 판별 근거" 로 인용하면
틀린다 — 그것을 못 박아 둔 것이 소득이다. 이득 여부는 **따로 재라** (규칙 25).

규칙 44 의 안쪽 판이다. 저쪽은 계수 **여럿 사이**에서 장소의 값과 기기의 값을
갈랐고, 이쪽은 **숫자 하나 안**에서 조건과 성질을 가른다.

---
## 46. 결함이 **떠받치고 있는 것**을 먼저 세라 — 고치면 그것이 무너진다

진단이 "여기가 틀렸다" 고 말해도, 그 틀린 것이 다른 무언가를 지탱하고 있으면
고치는 순간 그쪽이 무너진다. **결함을 찾았으면 그것을 없앤 반사실부터 재라.**

> 2026-09-02 세션이 같은 형태로 **두 번** 걸렸다.
>
> ```
> 찾은 결함                          그것이 떠받치던 것            고쳤을 때
> 에어컨 문턱 아래 누설 4.9W          겨냥 파일 L_cons 의 66.5%    잔차 4.52 -> 8.80W
> 흡수가 증거(1.18W)보다 6W 를 준다   흡수 이득의 **전부**          흡수를 끈 것과 같아짐
> ```
>
> 둘 다 진단은 옳았다 — 누설은 실재했고, 고조파는 정말 1.18W 만 지지한다.
> **그런데 첫째는 짝(흡수)을 같이 줘야 고칠 수 있었고, 둘째는 고칠 것이 아니었다.**
> `absorb_residual` 은 고조파 귀속 장치가 아니라 총전력 부족분을 되돌리는
> 장치이고, 고조파는 방향만 고른다. 그 정체를 모르고 "증거대로 고치자" 로 가면
> 도구가 꺼진다.

절차: 결함 `X` 를 처방하기 전에 ① `X` 를 **0 으로 둔 반사실**을 재고 (재학습 없이
추론에서 지우면 된다) ② 그때 무너지는 지표를 적고 ③ **그것을 대신 받을 것**을
처방에 같이 넣는다. 대신 받을 것이 없으면 그 결함은 **고칠 것이 아니라 옮길 것**이다
(규칙 29).

규칙 15 의 사촌이다. 그쪽은 "안전장치가 무엇인지도 측정하라" 이고 이쪽은
**"결함이 무엇을 하고 있었는지도 측정하라"** 다.
