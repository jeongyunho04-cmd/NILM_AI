# 측정의 규칙 (v2 — 2026-09-06, 새 계측기부터)

옛 규칙 1~82 는 옛 계측기(차동 ADC) 자료 위에서 쌓인 것이라 2026-09-06 에 문서째 지웠다. 필요하면
`git show 88ec645:MEASUREMENT_RULES.md`. **여기는 새 자료에서 새로 쌓는다.** 근거는 `READ_ME_FIRST.md`.

---

## 규칙 1 — 계측기가 바뀌면 그 전 자료의 결론은 전부 "미검증" 으로 내린다

계측기의 결함은 신호 통계로 위장한다. 옛 계측기의 ADC 공통모드 한계 하나가 짝수차 인공물·전압 h3
인공물·계측 h3 바닥·장소 지문으로 갈라져 각각 처방을 받았다. 계측기를 바꾸면: 자료를 지우고,
결론을 표로 옮기고(폐기/대체/유지/미검증), 첫 녹화는 순저항으로, 코드에 경계를 박고, 옛 문서에는
경계 절 번호(13.1)를 남긴다. `READ_ME_FIRST.md` §4·§7.

## 규칙 2 — 단독 녹화는 '꽂힌 대기' 로 시작하고 '플러그 뽑은 10초' 로 끝낸다

```
시작   기기 플러그를 꽂은 채(대기 전력) 녹화 시작  ->  켜고 끄고 단계를 바꾼다
끝     기기 플러그를 뽑고 약 10초 더 찍는다        ->  계측계만 남는다
```
끝의 꼬리가 **그 파일의 계측계 기준**이다. 계측계 자체 전류는 세션마다 다르다 — 13파일에서
1.33~1.60W / 6.2~7.5mA / 위상 −21~+13° (같은 보드, 같은 자리). 전역 노이즈 파일(`noise_*`) 하나로
빼면 그 차이가 기기 서명에 들어간다. 그래서:

- 전처리가 꼬리를 자동으로 찾아 `is_unplugged=1` 로 표시한다 (`cleaner.detect_trailing_unplugged`:
  끝에서부터 `P ≤ 바닥+0.6W` 이고 `|I1| ≤ 7.5mA×1.3`(외부전원) / `11mA×1.3`(자체전원) 인 구간, 5초 이상).
- 그 꼬리의 중앙값이 `p_target_w` 의 바닥이 되고(`noise_floor_source = trailing_unplugged`), 세그먼트 풀은
  그 파일의 활성화·대기 지문에서 **그 꼬리의 페이저**를 뺀다. 대기 지문 자체에서는 꼬리를 제외한다
  (꼬리는 기기의 대기가 아니라 계측계다).
- **대비책** — 꼬리를 빼먹은 녹화(2026-09-06: `laptop_charger_1` 충전 중 종료, `oven_1` 대기 상태로 종료)는
  등록부 바닥(1.6/2.3W)과 바닥이 같은 전역 노이즈 파일로 돌아간다. 어느 쪽을 썼는지 npz 메타
  `noise_floor_source` 와 `SegmentPool.noise_source[stem]` 에 남는다. 다시 찍을 때 꼬리를 붙이면 자동으로 바뀐다.
- 꼬리가 없다고 파일을 버리지 않는다. 다만 미니PC 7W 처럼 배경/|I1| 이 0.2 인 저부하 구간의 정확도는
  그 파일의 바닥을 얼마나 잘 아느냐가 상한이다 (옛 12.185.26 의 결론은 새 계측기에서도 선다).

## 규칙 3 — seq 0 프레임은 버린다

계측기가 측정을 시작하는 첫 0.5초(`seq == 0`)는 pll 미락 30사이클·전압 클립 창·vrms 2~3V 오차가 실린다
(13파일 전부). 보간으로 메우면 첫 0.5초를 지어낸다. `DataCleaner.drop_startup_frames` 와
`read_raw_csv(drop_startup=True)` 가 버린다. 세션 앞머리의 무효 구간(pll 미락)도 보간이 아니라 폐기다.
그래서 npz 의 `t_rel_s = 0` 은 **seq 1** 이다 — 사람 타임라인과 맞출 때 `t = (seq − seq_lo)·0.5`,
`seq_lo` 는 npz 의 `seq[0]`.

## 규칙 4 — 사람 타임라인은 ±3초다. 시각은 신호가 정하고 정체는 사람이 정한다

`user_timeline.txt` 형식: 파일 머리 `test_1.csv`, 줄마다 `seq:기기 켬/끔` (`seq0 충전기 켬` 은 시작부터
켜져 있던 것). 사람 시각은 ±3초 어긋난다 (사용자 진술). 정밀화:

```
python -X utf8 -m src.run_switch_sig                          단독 녹화 -> 전이 지문 (results/switch_sig.json)
python -X utf8 -m src.run_refine_labels --all --out results/refined_labels.json   ±3.5초 안의 그 기기다운 계단
python -X utf8 -m src.run_write_labels --refined results/refined_labels.json      -> processed_data/real_events.json
```
- 계단은 SMPS 는 h3 채널로, 저항은 P 로 찾는다. ΔP 는 **기기 몫**(SMPS 는 h3 에서 되돌린 값)이다.
- 못 맞춘 항목은 시각을 지어내지 않고 근방 ±4초를 `uncertain` 으로 둔다 (채점에서 뺀다).
- 오븐 '켬' 은 제어보드·팬·조명(≈14W)의 통전이고 히터는 서모스탯이 저 혼자 돈다. 핫플 '켬/끔' 은 릴레이
  듀티의 첫/마지막 펄스다. 채점기는 오븐 90초 / 핫플 5초 세션 병합으로 잰다.
- 라벨의 출처 표지는 `human_switching_log_signal_refined` 다. 2단계 사람 라벨 지도가 이것을 받는다.

## 규칙 5 — 릴레이 부하의 '스위치 내림' 과 '휴지' 는 바닥 준위로 가른다

핫플은 스위치를 내려도 플러그가 꽂혀 있고, 스위치를 켜도 서모스탯이 릴레이를 열어 두는 시간이 대부분이다.
둘 다 전력이 거의 0 이라 같아 보이지만 다르다 (`hotplate_1`, 2026-09-06):

```
플러그만 (스위치 내림)      P 1.54~1.56W   |I1| 7.3~7.4mA ∠+12°   |I3| 1.80mA     (바닥 뺀 값 0.15W)
스위치 켜짐·릴레이 열림     P 1.95~1.98W   |I1| 9.2mA     ∠+10°   |I3| 1.03mA     (바닥 뺀 값 0.56W)
플러그 뽑음 (꼬리)          P 1.39W        |I1| 6.7mA     ∠+15°
```
0.4W·1.9mA 차이고 휴지 바닥의 흔들림이 0.025W 라 깨끗하게 갈린다. **원시 p_w 의 절대 문턱으로는 못 가른다**
— 계측계 바닥이 파일마다 1.33~1.60W 로 달라서다. 그래서 규칙 2 의 꼬리로 뺀 `p_target_w` 의 4초 구름 10백분위
(펄스가 섞여도 바닥을 가리킨다)가 0.3W 이상이면 `ARMED_IDLE`(state 1), 아니면 `OFF_STANDBY`(state 0) 다
(`state_definitions.armed_state_id`, `annotator._apply_armed_state`). ON(is_on)은 여전히 통전(state 2)뿐이다.

이것이 세션의 경계다: 세그먼트 풀은 통전 펄스 사이에 state 0 이 **1초 이상** 끼면 사용자가 껐다 켠 것으로 보고
활성화를 가른다 (오븐의 팬·조명 상태와 같은 규칙). 릴레이 공백은 state 1 이라 잇는다. hotplate_1 은 이 규칙으로
[17, 665초] 와 [695, 795초] 두 세션이 됐다 — 뒤 세션은 플레이트가 뜨거워 릴레이가 2~5사이클(0.03~0.08초)씩만
닫히는데, 1초 중앙값·0.3초 체류의 옛 분류기는 그것을 전부 OFF 로 지웠다. 핫플의 평활 창은 0.05초, 체류는
1사이클이다 (`smooth_window_s`, `min_state_duration_s`). 2Hz 계단 검출도 같은 이유로 못 보므로 타임라인 정밀화는
펄스 시작/끝으로 구제한다 (규칙 4).

## 규칙 6 — 생성기를 바꾸면 혼합검증(mixval)으로 먼저 잰다. 교체가 아니라 델타로 넣는다

회로 모델을 생성기에 넣는 방법은 둘이다: 모델 전류로 **교체**하거나, 녹화를 재생하고 환경 차이의 몫만 모델의 **차분**으로
더하거나. 2026-09-06 test_2 로 쟀다 (`run_mixval12`, SMPS 창 216개, 실측 총전류 대 상대 RMS):

```
                     녹화 중첩 [A]   +텍스처 델타 [B]   모델 단독 [D]
test_2 (심야 2.9%)      0.248            0.111             0.116
test_1 (저녁 0.6%)      0.105            0.115             0.287     <- 녹화와 같은 텍스처면 중첩이 이미 좋고, 교체는 3배 나쁘다
```
델타는 텍스처가 다를 때 오차의 절반 이상을 닫고 같을 때 0.01 을 치른다. 교체는 모델의 단독 오차를 그대로 훈련 신호에 넣는다.
그리고 A/B 비교는 **합성기를 시드 앞에 만들고** 한다 — 뒤에 만들면 전역 난수 흐름이 밀려 다른 활성화끼리 비교하게 된다
(처음 잰 h1 14~33% 가 그것이었다).

**절차.** 생성기 변경 → `python -X utf8 -m src.run_mixval12 --stems test_2 test_1` → [A] 대비 [B] 가 나빠지는 창 조합이
있으면 그 원인을 적고 나서 캐시를 만든다. 소스가 측정 단자 전압인 검증에서는 결합 델타를 더하지 않는다 (이중 계상).

## 부록 — 새 규칙을 적을 때

한 번 잰 것을 규칙으로 올린다. 형식은 위와 같이: 무엇을 봤는가(숫자·파일), 왜 그렇게 하는가, 코드의
어디가 그것을 강제하는가. 옛 규칙의 방법론(단일 변수, 대조 파일, 시드 3개, 판정 기준을 먼저 적기)은
계측기와 무관하게 옳았다 — 필요하면 그 문장을 여기로 옮겨 오되, 새 자료로 잰 숫자를 붙여서 옮긴다.

## 규칙 7 — 원시 스냅샷은 두 시간대 모두, 순저항 하나를 끼워서 (2026-09-06, 설계 13.3.4)

회로 모델은 원시 스냅샷을 찍은 **그 텍스처**에서만 검증된다. 지금 원시는 전부 심야(229V, vh3 2.9%)라 심야 2Hz 녹화와는
1.3~5% 로 맞고 저녁(216V, vh3 <1%) 녹화와는 15~30% 어긋난다. 그래서:

- SMPS 원시 스냅샷은 **저녁과 심야 둘 다** 찍는다 (같은 기기, 같은 동작점).
- 각 원시 세션에 **순저항(핫플 LOW) 스냅샷 하나**를 끼운다 — 전압 채널의 h3~h9 를 `V_h = R·I_h` 로 검산하는 자 (원시 파일명 `raw_hotplate_*`).
- R(NTC) 상태를 재려면 **켠 뒤 시간을 기록**한다 (0/30초/1/2/5/10분). 프로젝터는 2Hz 만으로 τ 13~18초가 보였다.

## 규칙 8 — 세션마다 Z 를 잰다. 큰 저항 부하 on/off 를 세 번 넣는다 (2026-09-06, 설계 13.4.3)

같은 콘센트인데 **시간대가 선로 임피던스를 2.7배 바꾼다.**

```
심야 229V   포트 0.422Ω   드라이기 0.439Ω   test_2 0.424Ω
저녁 216V   오븐  1.138Ω                    test_1 1.214Ω
```

무부하 전압(229 vs 216V)과 같은 방향으로 움직인다 — 상류 부하가 만든 것이다. 합성기의 결합 델타는 `Z·ΣI` 에 비례하므로
Z 를 틀리게 두면 결합이 그 비만큼 틀린다 (저녁을 0.45 로 두고 있었으니 2.5배 작았다).

- **녹화마다 1kW 급 저항 부하(포트·오븐·드라이기 강)의 on/off 를 세 번 이상** 넣는다. 2분이면 그 세션의 Z 가 영구 기록된다.
- 재는 법: 계단 앞뒤 3초 창, 가드 0.75초, **두 창이 모두 조용할 때만**(P 산포 <40W) `Z1 = −ΔV1/Δ|I1|`. 창이 넓거나
  가드가 크면 다른 사건이 섞여 산포가 10배가 된다 (13.3.5 가 test_1 을 그렇게 버렸다가 13.4.3 에서 되찾았다).
- 핫플 같은 **릴레이 펄스(2~5사이클)는 못 쓴다** — 창에 안 들어온다. 통전이 몇 초 이상인 부하로.
- 값은 `grid_simulator.MEASURED_SITE_Z_OHM` 과 그 무리의 `r_grid_ohm` 에 적는다.
