# 인수인계 — 학습 PC, 2026-08-27

`HANDOFF_2026-08-25.md` 다음 세션의 기록이다. 상세 근거는
`MODEL_TRAINING_DESIGN.md` **12.89 ~ 12.112** 절에 있다.

---

## 0. 한 줄 요약

**SMPS 3종을 겨냥한 여섯 가지가 또 닫혔고, 그러다 열린 봉인과 새 실측이
`저항 4종`이 더 어렵다는 것을 드러냈다. 그리고 그 원인이 오븐 라벨 한 줄이었다 —
고치자 포트 F1 이 0.000 에서 0.921 이 됐다.**

- **오븐 `is_on` 이 팬/조명(16.5W)까지 ON 으로 잡고 있었다.** 히터 통전으로
  바꾸니 저항 전용 파일 F1 0.268 -> 0.757, 잔차 285 -> 20W — 12.110, 12.111
- **등가저항이 저항 3종을 가른다** (포트 35.8 / 오븐 40.6 / 드라이기 54.3 /
  핫플 101.8Ω, 재현성 0.1~1.3%). 후처리로 넣었다 — 12.112
- **드라이기 약풍은 반파 정류다.** 12.72 가 인공물로 판정한 짝수차가 그 상태에서는
  진짜다 (`|I2|/|I1|` 0.40) — 12.109.2
- **봉인(`test.csv`)을 열었다.** 사용자 판단 — 12.105
- **2차 펌웨어가 레인지 전환을 망가뜨렸다가 3차에서 고쳐졌다** — 12.108, 12.109

> ⚠ **이번 세션도 철회가 있다.** 12.96 을 12.97 이 통째로 뒤집었고, "다음에 볼 것"
> 에 적은 추측이 두 번 틀렸다. `MEASUREMENT_RULES.md` 에 규칙 13~19 로 넣었다.
> **새 실험 전에 그 문서를 먼저 읽을 것.**

---

## 1. 지금 상태

### 운영점 — 2026-08-27 교체했다 (12.113)

```
results/adapt_ovh.pt  단독 + 후처리 + 저항 정합
python -m src.run_live --csv data/live.csv          # 기본값이 곧 운영점
```

`run_live` 기본값이 `--ckpt results/adapt_ovh.pt --ckpt-smps "" --postproc on
--resmatch 0.02` 다.

```
                        유령W   잔차W   F1평균  전이(59)   저항전용파일 F1
adapt_smpsf + pp        4.32   58.35   0.779    44      0.268 / 0.516
**adapt_ovh + pp + rm** 5.04    9.99   0.838    38      0.782 / 0.836
```

**산 것**: 잔차 −83%, 전체 F1 +0.059, 저항 전용 파일 F1 0.27/0.52 -> 0.78/0.84.
**판 것**: 전이 귀속 44 -> 38/59, 없는 기기 전력 4.32 -> 5.04W.

근거는 크기다 — 저항은 700~1,500W 라 사용자가 보는 값이고, 잃은 전이 6건은
15~50W 대 SMPS 맞바꿈이다.

> ⚠ **전이 38 vs 44 가 실행 간 폭 안인지 아직 모른다** (5.3절의 부채).
> 시드 재실행 1회면 나온다. **폭이 크면 이 교체의 '판 것' 은 애초에 없었다.**

되돌리려면 `--ckpt results/adapt_smpsf.pt --resmatch 0`.

### 데이터

```
실측 복합 12개 (라벨 11개)
  test.2 test3 test_4          AI 추정 라벨
  test_5 test_6 test_7 test_8  사람 기록
  test_9                       AI 추정
  **test_11 test_12 test_13**  사람 기록 (2026-08-27 추가, 3차 펌웨어)
  test_10                      **라벨 없음** — 2절 참조
  test                         봉인 개봉됨 (12.105)

새 단일 기기
  hair_dryer_3           약/강 두 단계. 반파 정류 발견의 근거
  laptop_charger_4_fixed 52 -> 70W 로 **오르는** 충전 구간 (기존은 전부 테이퍼)
```

**`test_11`/`test_12` 는 저항 4종만, `test_13` 은 SMPS 3종만** 있는 첫 파일들이다.
4.3절이 "실질 난이도는 저항 3종 혼동에서 결정된다" 고 한 것을 전가 없이 잰다.

---

## 2. 사용자 확인이 필요한 것 — `test_10`

교체본(4.0분, 217.8V, 저항 4종 + SMPS 2종, P 3,088W)의 라벨을 못 붙였다.
`TEST_DATASET_TIMELINE_ANALYSIS.txt` [8]절이 두 가지를 남겼다.

1. **98.6초의 −970W 가 무엇인가.** 포트(954W)나 오븐(1,024W) 중 하나로 보이는데
   라벨은 그 둘을 118.5초 / 125.0초에 둔다 — 20초가 어긋난다
2. **'노트북 켜짐'(0 seq)과 '충전기 켜짐'(382 seq)이 같은 기기인가.**
   OFF 없이 ON 이 두 번이라 구간이 안 닫힌다

이 파일은 **3kW 저항 부하 속에서 미니PC 15W 를 찾는** 유일한 파일이라
(0.7절의 오차 전가) 라벨이 붙으면 값어치가 크다.

---

## 3. 이번 세션이 확정한 것

### 3.1 저항이 SMPS 보다 어렵다 (12.109.3) — **2026-08-31 뒤집혔다 (12.119)**

> 이 절의 결론은 **오븐 라벨을 고치기 전** 것이다. 지금 기기 단위로 재면:
>
> ```
>               옛 채점   세션 채점
> 저항 5종       0.765    0.902      <- 저항이 앞선다
> SMPS 3종      0.877    0.877
>
> 실질 최악  미니PC 0.799 (SMPS)   그 다음  드라이기 0.862
> 오븐 0.648 -> 세션 단위 0.925 (대부분 라벨 정의 차이였다)
> ```
>
> 오븐 재현율의 **천장이 0.607** 이다 — 라벨은 세션인데 모델은 히터 통전을
> 예측하기 때문이다. 실측 0.528 은 그 천장의 87% 다.
> `run_gate_check --session-merge` 로 양쪽을 세션으로 묶어 잰다.
>
> **남은 진짜 난제는 원래대로 SMPS 3종 배분이다.**


같은 모델, 같은 날 측정한 파일:

```
test_13 (SMPS 3종만)   유령 0.28W  잔차  3.3W  F1 0.924
test_11 (저항 4종만)   유령 9.71W  잔차285.4W  F1 0.268
```

그동안 SMPS 를 어렵다고 불렀는데 **저항이 더 어렵다.** 안 보였던 이유는 저항
전용 실측이 없어서다 — 섞인 파일에서는 SMPS 오차가 저항으로 전가돼 가려졌다.

### 3.2 오븐 라벨이 원인이었다 (12.110, 12.111)

```
그 기기가 ON 인 창에서 그 기기에 붙은 전력
  포트    <50W  0.0%   >1000W 99.5%
  핫플    <50W  1.9%   50~500W 68.2%
  **오븐  <50W 52.8%   >1000W 42.2%**
```

오븐만 0~1,300W 전 구간을 덮는다 → **공짜로 켤 수 있는 기기** → 승자독식.
원인은 `on_threshold_w=10W` 인데 팬/조명이 16.5W 라는 것이었다.

`ApplianceStateConfig.on_state_min_id=2` 로 히터 통전만 ON 으로 바꾼 결과:

```
test_11  포트 F1 0.000 -> 0.921   드라이기 0.009 -> 0.916   오븐 정밀도 0.277 -> 0.756
test_12  포트 F1 0.439 -> 0.850   드라이기 0.215 -> 0.843   오븐 정밀도 0.634 -> 0.723
잔차     285 / 282W -> 20.5 / 22.2W
```

딸려 온 것 둘도 같이 고쳤다 — 대기 지문을 `state_id==0` 에서만 뽑고
(안 그러면 오븐을 꽂아만 둬도 17W 유령), 주기 병합 상한을 10 -> 90초로
(오븐 서모스탯 공백이 25~31초라 세션이 펄스 70개로 쪼개졌다).

### 3.3 등가저항이 저항 3종을 가른다 (12.112)

```
포트 35.79/35.85Ω   오븐 40.41/40.90/40.45   드라이기 54.48/54.57/53.89
핫플 101.76/101.82/101.90                    (재현성 0.1~1.3%)
```

0.2절이 *"고조파 지문 거리 0.596%p"* 라고 한 세 기기가 **저항값으로는 13~180%
갈린다.** 핫플과 드라이기 약(반파, 겉보기 102.6Ω)만 겹치고 그 둘은 짝수차로 갈린다.

후처리 `--resmatch 0.02` 로 넣었다 — 컨덕턴스 정합, **맞바꿈만** (2절 규칙 18).

### 3.4 물리 상한 후처리 (12.100~12.104)

프로젝터는 격리에서 48.5~49.3W 인데 모델이 창의 74%에서 그보다 크게(중앙 73.5W,
최대 137W) 예측했다. 55W 로 자르고 초과분을 다른 SMPS 로 넘긴다.

```
adapt_smpsf 단독   전이 27/59 -> 44/59,  유령 1.46 -> 2.13W,  잔차 8.88W 유지
```

`--absorb`(잔차 흡수)도 있으나 **기본 꺼짐** — 미등록 SMPS 부하의 잔차까지
갈 수 있고 그 위험을 지금 데이터로 못 잰다.

### 3.5 봉인 파일 (12.105)

`test.csv` 20.2분 234V, 에어컨+드라이기+충전기+선풍기. 잔차 53.0W.

- **"유령 55.8W" 는 맞바꿈이었다** — 프로젝터 39.5 + 충전기 21.2 = 60.7W ≈
  충전기 실제 60W. 12.96/12.99 의 그 문제가 새 회선에서 재현됐다
- **에어컨+드라이기가 겹치면 드라이기를 놓친다** (구간 내내 0W, 600W 증발).
  학습에 그 조합이 0.21%, 적응셋에는 아예 없다

### 3.6 계측 (12.108, 12.109.1)

2차 펌웨어가 레인지 전환을 껐다가(8A 에서도 LOW) 3차에서 고쳐졌다.
새 파일 4개 모두 고전류에서 HIGH 전환 100%, PF 0.999~1.000, `|I2|/|I1|` 0.0012~0.0019.

---

## 4. 소진 목록 — 다시 하지 말 것

12.88.3 의 아홉 개에 이어서.

| # | 시도 | 결과 | 절 |
|---|---|---|---|
| 10 | SMPS 겹침 학습 창 늘리기 | 용량-반응으로 악화. 0.15 에서 24/41, 0.26 에서 21/41 | 12.90·12.91 |
| 11 | 적응 창별 가중 (고부하 기울기 25배) | 미니PC F1 +0.09, **전이 귀속 불변** | 12.94 |
| 12 | 충전기 궤적으로 식별 | 창당 이동 0.0037 vs 잡음 0.0174. 6~7분을 봐야 한다 | 12.95.1 |
| 13 | 실현가능성 제약 (이미 켜진 기기는 못 켠다) | 오귀속은 절반으로 주는데 정확은 +1. 미검출로 갈 뿐 | 12.95.2 |
| 14 | 5초 이동 PtP 특징 | 격리는 맞고 복합 AUC 0.56, 60초 창에서 0.44 | 12.98 |
| 15 | 전이 차분 특징 (45W 규모 재시도) | 39% (우연 33%). 모델의 71% 보다 약하다 | 12.99 |
| 16 | 잔차 흡수에 기본파 포함 | 저항 잔차가 SMPS 로 샌다 (오븐 모양 20W 중 12.3W) | 12.104.2 |
| 17 | 지속 성분만 재배분 | **반대로 갔다.** 재배분은 전이 순간에 작동해 효과를 낸다 | 12.101.2 |
| 18 | 프로젝터 하한(켜졌으면 45W 이상) | 44 -> 37/59. 승자독식이 복원된다 | 12.104.1 |

> **2026-08-27 재감사 (12.116.2).** 12.115 가 잡음 바닥을 세운 뒤 이 표를 다시 댔다.
> **18개 중 14개는 후처리이거나 순수 측정이라 시드와 무관하다** — 값싼 방법으로
> 닫아 온 것이 여기서 값어치를 했다. 재학습이 낀 4개 중:
>
> - **1·2 는 산다** (차이가 폭의 3.6~10배)
> - **10 은 "약하게 닫힘" 으로 낮춘다** — 24/41 은 폭(5) 안이다. 다만 12.91.2 의
>   용량-반응(0.15→24, 0.26→21)이 단조라 두 점 비교보다 강하다
> - **11 의 "전이 귀속 불변" 은 근거가 아니다** — 전이 폭이 9 라 효과 없음과
>   효과 <9 를 못 가른다. 그 항목이 확정한 것은 미니PC +0.09 뿐이다

> **2026-08-31 추가 (12.120).** 사용자가 *"프로젝터 48W · 충전기 60W+ · 미니PC
> 10~25W 로 크기가 갈리는데 왜 구분을 못하나"* 를 물어 다시 팠다.
>
> - **크기는 실제로 갈린다.** 다만 충전기의 15~76W 가 프로젝터 대역을 덮어,
>   총 SMPS 전력만으로 조합이 유일해지는 구간은 **22%** 다
> - **편향은 실재한다** — 프로젝터 유병률 65%인데 예측 ON 81%, 충전기는
>   71%를 68%로 과소. 그리고 그 편향은 **1단계에 이미 있다**
> - **원인 두 개를 시험해 둘 다 닫았다** — `L_harm` 와트당 비대칭(A),
>   헤지 벌점(B). 어느 것도 안 움직였다 (12.120.8~9)
> - **부수 소득**: 헤지 0 이 실행 간 폭을 4배 줄인다 (유령 폭 4.15 -> 0.94,
>   전이 최악 38 -> 42). 성능이 아니라 **판정 가능성**을 산다
> - **다음은 1단계다** — 12.110.2 의 진단(그 기기가 ON 인 창의 전력 분포)을
>   SMPS 3종에 대해 학습 캐시에서 돌린다. 재학습 없다

> **2026-08-31 문서 구멍 메움 (12.75).** 코드 다섯 군데가 "12.75절" 을 근거로
> 대는데 **그 절이 존재한 적이 없었다.** `--harm-odd-only` 의 유일한 실행 기록은
> 12.78 안에 묻혀 있었고, 그 판은 넷을 한꺼번에 바꿔 단일 변수가 아니었다.
> 단일 변수로 다시 재서 12.75.6 에 적었다 — **유령 5.23 -> 4.22W, 폭 4.15 ->
> 2.34W 를 사고 전이 귀속 최대 47 -> 39 를 판다. 채택 안 함.**
>
> 그리고 지금 상태: **입력은 짝수차를 0 으로 만드는데 손실(`L_harm`)과
> 후처리(`resistive_match`)는 짝수차를 본다.** 12.77 이 예고한 어긋남이다.

**SMPS 3종 맞바꿈의 뿌리** (12.97): 격리에서는 위상 불변량으로 95% 갈린다.
**중첩이 그것을 지운다** (격리 위상 d′ 14.9 -> 복합 0.4). 남은 길은
하드웨어(더 높은 차수·빠른 표본)나 분리 문제이지 특징 추가가 아니다.

---

## 5. 다음에 할 것 (우선순위 순)

### 5.1 교체의 '판 것' 을 확인한다 — 1순위, 그리고 이제 **선행조건**

> **2026-08-27 완료 (12.115). 폭이 컸고, 이 절의 전제가 뒤집혔다.**
>
> 같은 1단계 체크포인트에서 **2단계 시드만** 바꿔 n=4 로 쟀다.
>
> ```
> seed            유령W   전이/59        s0 = adapt_ovh (현행 운영점)
> s0 (운영점)      5.33    38/59
> s1              11.37   47/59         폭: 유령 6.05W, 전이 9건
> s2               9.86   43/59              F1 0.011, 잔차 2.14W
> s3               9.35   40/59
> adapt_smpsf      —      43/59         <- 직전 운영점, 범위 한가운데
> ```
>
> **① 아래의 "전이 38 vs 44" 질문은 답이 났다 — 폭 안이다.**
> 12.113 의 교체는 저항 4종을 얻고 **아무것도 팔지 않았다.** 유령
> 4.32 -> 5.04W 도 폭 6.05W 안이다. 그리고 s0 이 네 draw 중 유령이 가장
> 낮은 쪽이라, 문서의 운영점 숫자는 운 좋은 끝이다.
>
> **앞으로 시드 3개가 기본값이어야 한다** — 2단계만 3분이라 30분이면 된다.
> 이보다 작은 차이로 설정을 고른 옛 결론들은 12.115.6 에 목록이 있다.

> **2026-08-27 (12.114).**
> 반파 대비 채널을 넣어 재학습해 봤더니 **채널이 항상 0 인 파일이 발화하는
> 파일보다 더 많이 변했다** (|Δ유령| 5.87 vs 4.88W). 실행 간 폭을 모르면
> 재학습이 필요한 후보는 어느 것도 판정이 안 된다 — 5.2·5.3 과
> '상태별 전력 대역' 이 전부 여기 걸린다. **시드 3개를 먼저 돌릴 것.**
> 관측된 흔들림의 첫 실측: |Δ유령| 평균 5.87W (최대 25.3W),
> |ΔF1| 평균 0.026 (최대 0.092). n=1 쌍이라 폭이 아니라 한 점이다.


운영점은 이미 `adapt_ovh` 로 바꿨다 (12.113). 대가로 내준 둘을 확인한다.

```
① 전이 귀속 38 vs 44 가 실행 간 폭 안인가 — 시드 재실행 1회, 50분.
   **5.3절의 부채를 여기서 갚는다** (이 저장소는 그 폭을 한 번도 안 쟀다).
   폭 안이면 이 교체는 순이득이고, 밖이면 SMPS 쪽 회귀를 따로 봐야 한다
② 유령 5.04W 를 2W 대로 — 남은 것이 소프트 게이트 누수다
   (test3 포트 7.3W 는 게이트가 0.5 아래인데 전력이 샌다). 후처리, 30분
```

> **② 완료 (12.117). 진단이 맞았고 고쳤다.**
>
> `test3` 의 포트 누수는 **조합은 맞고 전력이 틀린 창**이었다 — 오븐이 옳게
> 켜져 있는데 780W(V^2/R=1130W)로 모자라고 그 차이가 게이트 0.26 을 타고
> 포트로 샌다. `resistive_match` 가 `best == cur` 이라 그냥 넘어가고 있었다.
>
> **`--rm-snap`**: 조합이 이미 맞을 때도 전력을 `V^2/R` 로 스냅한다.
> 시드 4개에서:
>
> ```
>                 8파일유령    폭     F1     잔차   test3포트   전이/59
> 기준선            8.98   6.05  0.846  10.15    16.64   38/47/43/40
> **스냅**          5.23   4.15  0.846   7.30     0.51   변화 없음
> ```
>
> 운영점에 넣었다 (`run_live` 기본 켜짐, 끄려면 `--no-rm-snap`).
> 전이 귀속은 **구조적으로** 안 변한다 — `resistive_match` 는 저항 열에만 쓴다.
>
> ⚠ **`test_9` 의 드라이기 유령 5.98W 는 남는다.** 그것은 후처리가 *만든* 것인데
> (모델 원시 출력 최대 12W), 게이트 문턱으로 막으면 `test3` 쪽 대가가 더 크다
> (12.117.8). 12.117.10 에 남은 길을 적었다.

### 5.2 짝수차 배제를 선택적으로 되돌린다 — **부분 완료 (12.114)**

> 채널을 만들어 넣고 돌렸다. **채널의 성질은 확정됐고 학습 효과는 판정 불가다.**
>
> - 인공물(평평)과 반파(가파른 감쇠)는 **차수 프로파일**에서 갈린다.
>   `|I2| − |I4|` 가 격리에서 48배로 가르고(겹침 0), 실측 13파일에서
>   정밀도 0.98/1.00, SMPS 오탐 0 이다
> - **이 절이 제안한 `|I2|/|I1| > 0.15` 문턱 형태는 쓰면 안 된다** —
>   복합에서 분모가 터져 `test_12` 의 72%, `test_10` 의 전부를 놓친다
> - 재학습 결과는 5.1 의 폭을 모르는 상태라 읽을 수 없다. 소진 목록에 올리지 않았다
> - 자산: `cache/train60_hw`, `results/cnn_hw.pt`, `results/adapt_hw.pt`.
>   폭이 나오면 재채점만으로 판정된다

원래 항목:

12.109.2 가 드라이기 약(반파)의 `|I2|/|I1|` 0.40 을 확인했는데
`ZERO_EVEN_HARMONICS=True` 가 그것까지 0 으로 만든다. 선형 부하의 바닥
(0.02~0.06)은 죽이고 그 위만 살리는 문턱이 필요하다.

**재학습이 필요하다** — 12.74.2 가 "학습과 추론이 짝을 이뤄야 한다" 를 보였다.

### 5.2b 전력 증강 폭을 실측에 맞춘다 — **시도했고 안 됐다 (12.118)**

사용자 제안. *"크기를 완전히 배제하면 안 된다."* **분석은 맞았고 결과는 아니다.**

- 증강이 실측과 **거꾸로** 걸려 있었다 — 저항 4종 실제 0.1~0.9% 인데 일괄 5%,
  충전기·미니PC 는 실제 8~14% 인데 5%
- 그 탓에 포트↔오븐 등가저항 d' 가 학습 분포에서 **2.59** (실측 산포로는 18)
- 기기별로 맞추니 d' **9.03**. 복합에서도 그 양이 산다 (편차 중앙값 0.53%)
- **그런데 실측이 나빠졌다** — 겹침에서 포트 재현 0.947 -> 0.759,
  핫플 정밀 0.986 -> 0.745. 겹침이 덜한 파일은 거의 안 변한다
- **건진 것**: 실행 간 폭이 크게 준다 (유령 4.15 -> 2.37W, 전이 9 -> 2~5)

**소진 목록에 안 올렸다** — 방향이 닫힌 것이 아니라 거는 자리가 틀렸다.
같은 물리(`V^2/R`)를 후처리에 걸면 이롭다 (12.117 의 `--rm-snap`).
기본값은 안 건드렸다. `--power-scale-std {resistive,measured}` 로만 켜진다.

### 5.3 합성에 저항 3~4종 동시 창

지금 학습 창의 저항 동시 3대가 **0.06%**, 4대는 0.00% 인데 `test_11/12` 가
그 상황을 5~6분 내내 던진다 (12.109.4).

### 5.4 에어컨·드라이기를 적응셋에

봉인 파일과 `test_10` 이 던지는 조합인데 **적응셋 어디에도 없다** (12.105.4).
`test_11/12` 에 드라이기가 있으므로 그것부터 적응셋에 넣어 볼 수 있다.

### 5.5 다음 측정 때

1. **`test_10` 라벨 확인** (2절)
2. **234V 회선의 사람 라벨 파일** — 4.3절이 요구한 것이 아직 없다.
   교체본 `test_10` 은 217.8V 다
3. 에어컨 + 드라이기 동시 구간
4. 프로젝터 소등 후 5분 이상 연속 (계속 미해결)
5. **SMPS 단독 장구간 — 2026-08-31 추가 (12.121.5). 지금 최우선이다**

   ```
   ① SMPS 3종 각각 단독, 저항 부하 없이, 기기당 5분 이상
   ② 같은 3종을 **저항 부하가 켜진 상태에서** 각각 단독으로 5분 이상  <- 핵심
   ③ 충전기는 고속·트리클 두 상태를 모두 포함
   ```

   **왜**: 홀수차 요동(`|I3|+|I5|+|I7|` 의 0.5초 블록 σ)이 프로젝터와 충전기를
   가르는데, 전력 요동과 달리 **저항 부하를 견딘다** (d′ 저항없음 2.52 →
   저항있음 2.80, 전력은 2.63 → 0.57). 저항이 3차를 안 흘리기 때문이다.

   그런데 **복합에서 SMPS 단독 구간이 128초·151초뿐**이라 60초 창이 2~4개다.
   d′ 2.80 은 방향만 믿을 수 있다. ②가 있어야 규칙 1 을 충족하고 채널을
   넣을 근거가 선다. **①만으로는 격리를 한 번 더 재는 것이다.**

   > **2026-08-31 절제 결과 (12.121.7~9)로 목적이 바뀌었다.** 모델은 그 요동을
   > **이미 쓰고 있다** — 0.5초 규모로 평활하면 충전기 재현율이 0.641 → 0.352 로
   > 무너진다 (P 를 같은 방식으로 평활하면 무변화). 크기와 위상은 **중복 통로**라
   > 한쪽만 막으면 −0.03~−0.05 뿐이고 둘 다 막아야 −0.22 다.
   >
   > 그래서 **명시 채널 추가는 근거가 없다.** 이 측정의 목적은 *"단서를 쓰는데도
   > 왜 저항이 켜지면 재현율이 0.641 인가"* 를 n=12 가 아닌 표본으로 재는 것이다.

---

## 6. 도구 — 이번에 추가·변경된 것

```
src/model/postproc.py            apply_postproc / absorb_residual / resistive_match
src/run_cap_probe.py             물리 상한 후처리 진단
src/run_adapt_weight_probe.py    적응 손실이 어느 창에서 오는가 (기울기 포함)
src/run_final_eval.py            봉인 파일 최종 평가 (개봉 1회)
deploy/                          배포 묶음 — 모델+수신기+예측기+구조 문서 (2.7MB)

run_gate_check   --postproc {off,on,sync}  --resmatch TOL  --absorb FRAC
run_transition_probe  --cap {off,on,sync}  --feasible {off,truth,model}
run_live         --postproc {off,on,sync}  --absorb FRAC
run_recipe_mix_probe  --real (실측 SMPS 동시성 기준선), 프리셋 smps/smps_mid/smps_hi/smps_hi_fix
run_adapt        --real-weight {none,inv-power,smps-boost}
```

### 고쳐진 것

- `ApplianceStateConfig.on_state_min_id` — 오븐만 히터 통전을 ON 으로 (12.111)
- `_extract_standby_profile` 이 `state_id==0` 에서만 뽑는다
- `PERIODIC_MERGE_GAP_CAP_CYCLES` 600 -> 5400 (오븐 서모스탯 공백)
- `RealWindows` 가 `assert_not_sealed` 를 쓴다 — 예전에는 `unseal()` 문맥을
  무시해 **설계된 최종 평가 경로가 아예 막혀 있었다**
- `RealWindows.v_observed` 추가 (저항 정합용)
- `gate_check` 가 기기별 오귀속(`absent`)을 json 에 저장한다
- `run_plot_real` 이 `delta_p_w` 없는 이벤트에서 안 죽고, 라벨을 세로·3층으로 놓는다
- `run_live` 기본 체크포인트가 가드에 막힌 옛 파일을 안 가리킨다

---

## 7. 실행 메모

- **1단계 기본 홀드아웃(`processed_data/holdout60`)은 8월 21일자다.** 새 녹화가
  반영되기 전 세그먼트 풀이라 지금 학습에 쓰면 안 된다. `--holdout
  processed_data/holdout60_ze1` 을 명시할 것 (12.90 에서 4분 만에 알아챘다)
- 캐시 빌드 17분(21.8GB) + 1단계 30분 + 2단계 3분. **적응만 다시 하는 실험은
  3분**이라 값이 싸다 — 12.94·12.107 이 그 형태다
- 봉인은 이미 열렸다. `processed_data/SEAL_BROKEN.json` 에 기록이 있다
- `deploy/` 는 학습 저장소의 **사본**이다. `src/model/inputs.py`·`net.py`·
  `postproc.py` 를 고치면 양쪽이 어긋난다
