# 인수인계 2026-09-06 — **계측기가 바뀌었다.** 경계·규약·라벨·회로모델·**학습 1회전**까지 섰다

**정본은 `READ_ME_FIRST.md`** 이고 이 문서는 그 요약이다. 입력 채널 목록은 **`INPUT_CHANNELS.md`**.
설계 기록은 **13절**(13.1~13.12), 측정 규칙은
`MEASUREMENT_RULES.md` **v2 규칙 1~8**. 앞 문서: `HANDOFF_2026-09-05b.md` (옛 계측기의 마지막 인수인계, 커밋에만 있다).

## 0. 한 줄

옛 계측기(차동 ADC)의 공통모드 한계가 짝수차·전압 h3·"장소 지문" 인공물의 뿌리였다. 단일 입력 ADC 로 바꿨고 옛
자료는 전부 지웠다. 새 자료로 규약·라벨·회로모델(v12g)·합성기 배선을 다시 세웠고, **계측 경로는 완전히 닫혔다**
(원시↔2Hz 전달함수가 펌웨어 소스로 확정). 남은 것은 회로 모델의 미니PC 고차 부족과 **자료 분량**이다.

## 1. 지금 상태

```
자료      data/  2Hz 17파일(교정 전 test_1.csv 포함) + 원시 15 (충전기 8 · 미니PC 5 · 프로젝터 2)
          원시 세션: 충전기 저녁+심야 둘 다 / 미니PC 저녁만 / 프로젝터 심야만  (file_registry.RAW12_IN_RECORDING)
등록부     DEVICE_FILES 11 / NOISE_FILES 3 / test_1·test_2 복합 / raw_* 는 RAW 역할(전처리 건너뜀). 옛 이름은 거부
라벨       test_1 17/17, test_2 31/31, uncertain 0  -> processed_data/real_events.json
회로모델    circuit_model/circ12_{laptop_charger,minipc,beam_projector}.pkl (v12g), fcm.load_models() 기본
합성기     텍스처 델타 + Z 결합 델타 + R 상태 (13.2). ①a(h3 왜곡) 끔. 텍스처 173개
활성화     오븐 3 · 핫플 2 · 에어컨 3 · 미니PC 4     <- **가장 큰 제약. 각 5회 이상 필요**
학습       1회전(13.10) 뒤 **결함 둘을 잡고 재설계**(13.11~13.12). 지금 v14 를 굽는 중
장소       ⚠ **둘이다 — D 저녁(216V·vh3 0.7%·Z 1.15Ω) / E 심야(229V·vh3 3.0%·Z 0.42Ω)**. 13.1 의
           '장소 아니라 시간대' 는 **틀렸다** (사용자 확인, 13.11.1). 9종 중 충전기만 두 장소에 있다
입력       세밀 **45**채널 · 광역 **35**채널 (배치 v2, 13.12). 짝수차는 **크기만** — 위상은 플러그 방향이다
운영점     없음. v13e 계열은 배치 v1 이라 이제 로더가 거부한다. **배포 금지**
테스트     137P / 0F / 5S
```

**혼합검증 (규칙 6, `results/_mixval12.json`)** — 생성기가 실제로 쓰는 것은 [B] 다.

```
              [A] 녹화중첩   [B] +텍스처델타   [D] 모델단독
test_2 심야     0.248          0.097            0.128
test_1 저녁     0.105          0.120            0.300
차수별(심야)  h1 A.05/B.02  h3 .18/.05  h5 .28/.10  h9 .47/.27  h13 .65/.40   <- 모든 차수에서 개선
```

## 2. 계측계는 닫혔다 (13.4~13.5, 펌웨어 소스 확인)

펌웨어 소스: `NILM_ECE_IF-fix-even-harmonics-offset-bug/Core/{Inc,Src}/nilm_dsp.*`

```
원시 seq == 2Hz 국소 사이클 == 2Hz.seq*30 + 2Hz.cycle   (오프셋 0. npz 의 seq 는 **블록** 번호다 — 직접 비교 금지)
전달함수 T(h) = I_2Hz / I_원시  =  |1 + j·2π·60h·100µs| · exp(−j·1.40°·h)
   크기는 펌웨어의 안티앨리어싱 RC 역보정(NILM_RC_FC_HZ 1591.55, **크기만**), 위상은 NILM_CAL_DEFAULT_*_DEG 1.40
   원시 15개 중 11개(세 기기·두 세션·10~65W)에서 소수점 넷째 자리까지 같다 -> 등록부 raw_to_2hz_transfer()
위상 교정  새 보드 LOW = HIGH = 1.40°.  이주값 migrate_cal --phase-low 0.96 --phase-high -1.22 (검산: 이주 전 두
   레인지가 2.17° 어긋남 -> 이주 후 0.01°).  PHASE_FIX_DEG_PER_ORDER 는 **비운 채로 옳다**
모델 규약  sim_harmonics(measured=True, τ=60µs) 출력이 **2Hz 영역**이다 (참 계측기 연산자를 h1~h9 에서 0.4° 안으로
   되찾는다). 바꾸지 않는다. 원시 영역과는 다르다 — 원시와 견줄 때는 T(h) 를 나눠라
선로 Z    저녁 216V **1.15Ω** (오븐 50계단 1.138, test_1 5계단 1.214) / 심야 229V **0.42Ω**. 규칙 8
배경      평소 0.028mS (1.59W) = **ZMPT101B 전압센서 + 직렬저항 28.7kΩ**. noise_selfpower_* 는 자체전원 보드를
   일부러 멀티탭에 꽂은 **실험**이라 평소가 아니다 (13.8). 전처리는 이미 옳은 값을 쓰고 있었다
```

## 3. 학습 1회전 — 전부 돌았다 (13.10)

```
①② (13.9) 참값표 재계산 · 사람 라벨 지도 갱신
③ 캐시   cache/train60_v13e  30만창 **89분** (11워커. 인수인계의 '12분' 은 13.2 이전 수치였다 — 13.10.5)
④ 홀드아웃 processed_data/holdout60_v13  8,000창 x 60초  6.5분
⑤ 1단계  cnn_v13e            합성 F1 0.934 / MAE 12.64W / 잔차 27.8W / 최악 minipc 0.758   30분
⑥ 2단계  adapt_v13e_nores_s{0,1,2}   실측 유령 0.47~1.10W · 잔차 5.9~8.1W · on/off F1 0.832~0.839
   생성기를 건드렸으면 캐시 **전에** run_mixval12 (규칙 6)
```

**`--w-res`/`--w-swap` 은 뺐다** (13.10.8). 넣으면 test_2 에 **없는 핫플 396W** 를 만든다 — 유령이 10배
(1.1 -> 10.6W), 합성 F1 0.937 -> 0.894. on/off F1 0.03 을 얻는 교환인데 값이 안 맞는다.

채점 주의: 참값 표에 **저항 3종만** 있다. 프로젝터·충전기·미니PC 는 `score_power_ref` 가 안 잰다 —
그 셋의 배분 성능은 `real_events.json` 의 on/off 균형정확도로만 본다.

생성기 스위치: `GridSimulator(texture_library=False)` 로 텍스처·결합 둘 다 끔, `use_texture`/`use_coupling`/`randomize_r` 개별.

**정밀도는 발목이 아니다** (근거는 §1 표): 델타는 두 모델 평가의 **차분**이라 공통 편향이 상쇄되고 델타 자체가
h1 에서 1~5% 다. 미니PC 모델이 h13 에서 36% 틀려도 그 36%가 5%짜리 델타에 곱해진다. 발목은 **활성화 수**다.

## 4. 알려진 결함 — 결과가 나쁘면 여기부터 본다

```
~~드라이기가 안 나온다~~  **원인 둘을 잡았다 (13.11).** 강풍은 **목적함수**였다 — 지문이 기기당 하나라
                 약풍(반파)에 앉았고 강풍 창에서 정답 배분이 오답보다 `L_harm` **27배** 비쌌다.
                 `--state-signatures` 로 고쳤다 (MAE 12.6 -> 3.9, 저항3종 0.878 -> **1.000**).
                 약풍은 **짝수차 위상이 플러그 방향**이라 합성과 실측이 180° 반대였다 (13.11.7) —
                 입력을 크기로 바꾸고(배치 v2) 손실도 크기 공간으로(`--harm-even-magnitude`).
                 ⚠ 경사 쪽 처방 둘(`--w-state-power`·`--off-detach-praw`)은 **실패했다** — 음성 대조로 남긴다
미니PC 고차 부족  자기 적합 세션에서도 2Hz 복소오차 0.107 (프로젝터 0.028, 충전기 0.036).
                 |모델/실측| 이 h7 위에서 늘 1 미만 (h7 0.94 / h9 0.91 / h11 0.84 / h13 0.64).
                 배경도 R 도 세션도 아니다 (셋 다 검정해 죽였다, 13.7.3). 가이드 §10.7 의 역회복/EMI 공진 자리.
                 **파형 RMS 2.5~4.0% 에는 안 드러난다** — 파형 잔차만 보고 모델을 받으면 이 결함을 못 본다.
                 미니PC 가 가장 작고 가장 구별하기 어려운 기기다. 성능이 기대보다 낮으면 1순위 용의자.
파라미터 축퇴     한 세션만으로 맞추면 C·L0·Isat 이 골짜기를 따라 아무 데나 앉는다 (C 15%·L0 60% 갈림).
                 골짜기는 **비대칭** — 덜 구속하는 세션(심야)으로 맞춘 값을 저녁에 쓰면 11~18% 로 터진다.
                 충전기는 두 세션이 다 있어 풀렸고, **미니PC(심야 없음)·프로젝터(저녁 없음)는 아직 한쪽뿐이다.**
                 충전기 공동 적합은 pkl 의 alt_fit_raw1to8 에 사유와 함께 보존 (혼합검증 [B] 가 거절, 13.6.4 — 사용자 선택 사항).
저녁 델타         test_1 에서 [B] 0.120 > [A] 0.105 로 본전이다. 텍스처가 같으면 델타가 할 일이 없다 (설계대로).
h13+             [A] 0.65 / [B] 0.40 로 델타가 개선은 하지만 절대값이 크다. 판별 정보의 76% 는 h3~h9 다.
```

## 5. 자료 요청 (우선순위)

```
① 활성화 더: 핫플·오븐·에어컨·미니PC 를 각 5회 이상, 설정 바꿔 가며, 파일 끝에 꼬리 10초 (규칙 2)
② 저녁(216V) 프로젝터 원시 · 심야(229V) 미니PC 원시 — 각 기기의 축퇴를 푸는 유일한 자료 (13.6.2)
③ 원시 세션마다 순저항(핫플 LOW) 스냅샷 하나 (규칙 7), 오븐 꼬리, 긴 무부하 배경
④ NTC 워밍업 시리즈: 충전기 65W 고정, 켠 뒤 0/30초/1/2/5/10분 (13.3.8 ③)
⑤ 세션마다 1kW 급 저항 on/off 3회 — 그 세션의 Z 가 영구 기록된다 (규칙 8)
```

하드웨어 점검 하나: ZMPT101B 직렬저항이 28.7kΩ이면 거기서 **1.65W** 가 난다 (2~3W 정격이 아니면 뜨겁다).
1차 정격 2mA 대비 7.6mA = 3.8배. 다만 전압 측정 자체는 멀쩡하다 (순저항에서 |I_h/I_1|/|V_h/V_1| h3 1.01 · h5 1.00).

## 6. 커밋 상태

13.2 까지 커밋 `0bf210f`. **13.3~13.10 은 커밋 전이다.** 바뀐 것: 설계 13.3~13.10, 규칙 8, READ_ME_FIRST,
이 문서, `circuit_model/{fcm12.py, README_v12.md, circ12_*.pkl}`, `src/synthesis/grid_simulator.py`(저녁 Z),
`src/preprocessing/file_registry.py`(계측 상수·원시 지도), `src/evaluation/power_ref.py`(참값 표 재계산),
`src/model/realdata.py`(사람 라벨 지도), `results/_mixval12.json`,
그리고 13.10 의 것: `src/model/inputs.py`(짝수차 켬), `src/run_train_cnn.py`(캐시 짝수차 가드),
`src/run_adapt.py`(`--holdout` 인자).

산출물(커밋 안 함, 용량): `cache/train60_v13e` 19.6GB · `processed_data/holdout60_v13` 3.8GB ·
`results/{cnn_v13e,adapt_v13e_s*,adapt_v13e_nores_s*}.pt` · 채점 `results/_gc_v13e*.json`,
`results/_pc_v13e*.json`, 짝수차 측정 `results/_even_harm_probe.txt`.

## 7. 읽기 주의 — 실제로 빠진 함정

```
· npz 의 seq 는 **블록**(30주기), 원시의 seq 는 **사이클**이다. 이걸 헷갈려 "원시와 2Hz 가 안 겹친다" 는 오진을 했다
· 조건별 적합이 갈리면 **축퇴부터** 배제한다 (공동 손실에 넣고 하나씩만 조건별로 풀어 개선폭을 잰다).
  세션 의존 물리를 찾아 두 번 헛돌았다 (주기 교대·계측 τ, 둘 다 기각)
· 파형 RMS 와 고조파 복소오차는 **다른 것을 잰다**. 미니PC 는 파형 3% 인데 고조파 0.107 이다
· A/B 비교는 합성기를 **시드 앞에** 만든다 (생성자가 전역 난수를 한 칸 먹는다)
· 갓 쓴 파일(PNG·py)이 Errno 22 로 잠깐 잠긴다. 저장·패치에 재시도를 넣었다
· bash heredoc 안 파이썬 문자열의 \n 이 실제 줄바꿈이 된다. 패치는 파일로 쓰고 실행한다
· 핫플 짧은 펄스(2~5사이클)는 1초 중앙값 분류기·2Hz 계단 검출 둘 다 못 본다. 릴레이는 +3.5초 뒤에 붙는다
· 오븐 스위치 끔은 히터(−1100W, 서모스탯)가 아니라 팬·조명(−13W)이고 핫플 펄스 사이 바닥에서만 보인다
· 같은 콘센트가 시간대로 갈린다: 저녁 216V·vh3 0.6% / 심야 229V·vh3 3.2%, 그리고 **Z 가 2.7배 다르다**
· 집계 F1 은 크기 결함을 숨긴다. 드라이기는 합성 F1 0.999 인데 편향(on) −276.5W 이고 실측에서 0W 다.
  기기별 표의 **편향(on)** 열을 F1 과 같이 봐야 한다 (13.10.9)
· `ZERO_EVEN_HARMONICS` 는 캐시에 **구워진다.** 캐시를 굽고 나서 바꾸면 체크포인트가 거짓을 적는다 —
  `run_train_cnn` 에 가드를 넣었다. 대조는 같은 캐시에 `--zero-channels` 로 낸다 (반대 방향은 불가능)
```
