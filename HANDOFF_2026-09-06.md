# 인수인계 2026-09-06 — **계측기가 바뀌었다. 옛 자료·옛 결론의 경계를 그었고, 새 규약과 첫 라벨이 섰다**

앞 문서: `HANDOFF_2026-09-05b.md` (옛 계측기의 마지막 인수인계). **정본은 `READ_ME_FIRST.md` 다** — 이 문서는
그 요약이다. 설계 **12.186**, 측정 규칙 **v2 1~4** (`MEASUREMENT_RULES.md`, 새로 시작).

## 0. 한 줄

옛 계측기(차동 ADC)의 공통모드 한계가 짝수차·전압 h3·장소 지문 인공물의 뿌리였다. 계측기를 단일 입력 ADC 로
바꿨고, 옛 자료는 전부 지웠고, 그 자료로 세운 결론은 전부 `READ_ME_FIRST.md` §4 표에서 폐기/대체/유지/미검증으로
갈랐다. 새 녹화 규약(꽂힌 대기로 시작, 플러그 뽑은 10초로 끝, seq 0 폐기)을 전처리·풀에 넣었고, 사용자
타임라인을 신호로 정밀화해 첫 라벨(`test_1`, 17/17)을 냈다.

## 1. 지금 상태

```
자료         data/ 새 계측기 15파일 + 원시 4 (9종 전부 있다). processed_data/ 는 전처리 결과, real_events.json 은 test_1 라벨
등록부        DEVICE_FILES 11 / NOISE_FILES 3 / test_1 복합 / raw_* 는 RAW 역할(건너뜀). 옛 이름은 LEGACY_*_OLD_ADC (거부)
규약          규칙 2 꼬리: 9/11 파일이 자기 꼬리(1.33~1.58W/6.2~7.4mA)를 노이즈 기준으로, 충전기_1·오븐_1 은 전역 파일로 대체
             규칙 3 seq 0: 전처리·read_raw_csv 둘 다 버린다 (npz t_rel 0 = seq 1)
라벨          test_1: 17/17 (구제 3), test_2: 31/31 (선풍기는 h3 부호를 안 쓰게 고침). uncertain 0
회로 모델     circuit_model/circ12_{laptop_charger,minipc,beam_projector}.pkl (v12g). fcm.load_models() 기본
합성기        **회로 모델 v12g 배선** (12.187): 2Hz vh 텍스처 라이브러리(173개) + 텍스처 델타 + Z 결합 델타 + R 상태. ①a 끔.
             혼합검증 test_2: 녹화중첩 0.248 → 0.111 (모델단독 0.116); test_1: 0.105 → 0.115 (모델단독 0.287). 9종. 활성화 오븐 3·핫플 2·에어컨 3·미니PC 4
             (펄스 사이에 state 0 이 1초 이상 = 사용자 끔 으로 세션을 가른다. 핫플은 규칙 5 의 ARMED 상태가 그것을 만든다)
운영점        없음. results/adapt_ph5_s0.pt 등은 옛 자료 학습분 — 배포 금지
테스트        137P / 0F / 5S
삭제(사용자)   READ_ME_FIRST.txt, SMPS_PLAN, 옛 MEASUREMENT_RULES.md(-> v2 새로), MEASUREMENT_CHECKLIST.md, OPERATING_POINT.txt
```

## 2. 새 자료에서 확인한 것

옛 규칙 74 통과 (순저항 ihdeg1 −0.05~+0.06°, LOW·HIGH), 크기비 |I_h/I_1|/|V_h/V_1| 0.8~1.0 (h3·h5), 짝수차 0.03%
(전류·전압), 등가저항 35.61/40.09/53.22/100.83Ω · 반파 106.0 (옛 값 2.4% 안, 드라이기 약풍 반파 0.431 유지),
배경 1.59/2.3W **파일마다 다름(규칙 2 의 근거)**, 프로젝터 46.2/45.9W, 선풍기 22.9/30.8/39.0W. 같은 콘센트가
저녁(216V, vh3 0.6~0.85%)과 심야(229V, vh3 3.2%)로 갈린다 — "장소" 는 "세션" 이 됐다.
정밀화에서 새로 배운 것: 핫플 약한 단은 0.5초 펄스라 2Hz 계단 검출이 못 보고 릴레이가 +3.5초 뒤에 붙는다;
오븐 스위치는 핫플 펄스 사이 바닥(−13W)에서만 보이고 히터(−1100W)는 서모스탯이다 → `run_refine_labels` 구제.

## 3. 다음 수 / 필요한 것

`READ_ME_FIRST.md` §8·§9. 순서:
```
① 세션 더 (핫플 2·오븐 3·에어컨 3·미니PC 4 -> 각 5회 이상, 설정 바꿔 가며, 꼬리 10초). 오븐 꼬리, 긴 배경
② python -m src.run_power_check --recompute-ref          REFERENCE_W / SNAP_TARGET_W (저항은 V²/R 로 고칠 것)
③ (선택) fit12 재적합에 raw_laptop_charger_5~8 (충전기 32~65W) 넣기;  test_1/test_2 저항 계단으로 Z1 재측정
④ 2단계 사람 라벨 지도를 쓰려면 realdata.HUMAN_ON_DEFAULT_STEMS 를 ("test_1", "test_2") 로
⑤ 캐시(30만 창 ≈ 12분/11워커)·1단계·2단계 — GPU. **돌리기 전에 묻는다.** 머리 수 9. 캐시 전에 run_mixval12 (규칙 6)
```
생성기 스위치: `GridSimulator(texture_library=False)` 둘 다 끔, `use_texture`/`use_coupling`/`randomize_r` 개별.

## 4. 이 세션이 바꾼 코드

`READ_ME_FIRST.md` §5 + 설계 12.187.5. **커밋 전이다** (27파일 수정 + 신규 vtexture.py, run_mixval12.py, circuit_model/,
READ_ME_FIRST.md, MEASUREMENT_RULES.md v2, HANDOFF_2026-09-06.md, user_timeline.txt, processed_data/real_events.json).

## 5. 읽기 주의 — 이 세션에서 실제로 빠진 함정

```
· 갓 쓴 파일(PNG·py)이 Errno 22 로 잠깐 잠긴다 (세 번). 저장·패치에 재시도를 넣었다 (savefig_retry, 패치 스크립트 write 재시도)
· bash heredoc 안의 파이썬 문자열 \n 이 실제 줄바꿈이 돼 f-string 이 깨졌다 (run_synthesis). 패치는 파일로 쓰고 실행한다
· A/B 비교는 합성기를 **시드 앞에** 만든다. 뒤에 만들면 GridSimulator 생성자가 전역 난수를 한 칸 먹어 다른 창끼리 비교한다
  (델타 h1 14~33% 라는 가짜 결과가 그렇게 나왔다). 텍스처·R 난수는 창의 (V, R, X) 에서 파생시켜 워커 수와 무관하다
· 핫플 짧은 펄스(2~5사이클)는 1초 중앙값 분류기·2Hz 계단 검출 둘 다 못 본다. 라벨러는 평활 0.05초, 정밀화는 펄스 구제
· 오븐 스위치 끔은 히터(−1100W, 서모스탯)가 아니라 팬·조명(−13W)이고 핫플 펄스 사이 바닥에서만 보인다
· 사용자 자리 한 콘센트가 시간대로 vh3 0.6% ↔ 3.2% 갈린다 — 단독 녹화(저녁)와 복합(심야)이 다르면 중첩 오차 0.25
```
