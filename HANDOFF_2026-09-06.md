# 인수인계 2026-09-06/07 — **생성기를 E 에 맞추다 D1 을 잃었다.** 검증 관문이 그것을 못 봤다

**정본은 `READ_ME_FIRST.md`**, 입력 채널은 **`INPUT_CHANNELS.md`**, 설계 기록은 **13절**(13.1~13.24),
측정 규칙은 `MEASUREMENT_RULES.md` **v2 규칙 1~8**. 앞 문서: `HANDOFF_2026-09-05b.md` (커밋에만 있다).

## 0. 한 줄

옛 계측기(차동 ADC)의 공통모드 한계가 짝수차·전압 h3·"장소 지문" 인공물의 뿌리였다. 단일 입력 ADC 로
바꾸고 옛 자료를 전부 지웠다. 계측 경로는 닫혔고, 배분 결함 셋을 뿌리까지 고쳤다(저항3종 혼동
0.878 → 1.000, 포트 배분 453 → 77W, 드라이기 약풍 게이트 0.155 → 1.000).

2026-09-06/07 에 생성기를 두 자리에 맞추려 여섯을 한꺼번에 바꿨고(13.24.1) **v15 는 반쪽만 성공했다**:
E 에서 충전기 0.882 → **0.999**, 프로젝터 0.811 → **0.976** 인데, D1 에서 충전기가
0.857 → **0.283** 으로 무너졌다. 라벨은 같고, **적응 이전부터** 갈려 있다(생성기다). 예산·회로모델은
대조로 배제했다. **근본 문제는 검증 관문이다** — `run_mixval12` 는 결합 경로를 아예 안 부르고
(`hits=0`), 기본값이 E 파일 하나이며, 13.22 의 검정은 D→E 한 방향뿐이었다. 그 관문이 통과시킨
변경들이 D 를 어떻게 바꾸는지 아무도 안 봤다. 자세한 것은 **13.24**.

## 1. 지금 상태

```
자료      data/  2Hz 25파일 + 원시 22 (충전기 8 · 미니PC 10 · 프로젝터 4)
자리      **글자로만 부른다.** 자리 둘 D·E, 세션 넷 D1·E1·D2·E2
          판별은 **vh7** 이 가장 깨끗하다 — D 0.86~1.28% / E 0.08~0.30% (겹침 없음).
          vh3 은 4배, 전압 크기는 겹친다 (D2 가 210.7V 까지 내려간다)
          D1 09-05 21:26~23:50  216.5V Z 1.15Ω   charger_1 oven_1 minipc_1 hotplate_1 noise 3종 test_1
          E1 09-06 00:30~02:55  229.0V Z 0.42Ω   kettle_1 projector_1·2 fan_1 dryer_1 AC_1 charger_2 test_2
          D2 09-06 18:55~20:20  215.8V Z 1.19Ω   projector_3 hotplate_2 oven_2 test_3 test_4 test_5
          E2 09-06 21:16~21:38  230.7V Z 미측정  minipc_2 minipc_3
          양쪽 자리에 다 있는 기기 **셋** (충전기·프로젝터·미니PC)
라벨      test_1 17 · test_2 31 · test_3 28 · test_4 12 · test_5 16 사건, uncertain 0
          재현: run_switch_sig -> run_refine_labels --all --max-dt 6 -> run_write_labels
입력      세밀 **45**채널 · 광역 **35**채널 (배치 v2, 13.12). 짝수차는 **크기만**
회로모델   circ12_{laptop_charger,minipc,beam_projector}.pkl — **두 자리 공동 적합, --site-ratio 1.0**
합성기     텍스처 **개방 전압** 델타 + Z 결합(단독 SMPS 포함) + R 상태. ①a 끔. 지터 전부 0
          텍스처 표본: 자리 명시 · 세션 균등 · 최근접 낙하 (13.19)
운영점     **없음.** v13 계열은 배치 v1 이라 로더가 거부한다. 배포 금지
테스트     140P / 0F / 4S
캐시       30만창 **40분** (124 win/s, 13.23). 굽는 시간의 85%가 결합 델타다
```

**판 `v15`** (2026-09-07 완료) — 캐시 `cache/train60_v15`(300k, 52.8분) · 홀드아웃 8,000창 ·
1단계 `cnn_v15` · 2단계 `adapt_v15_s{0,1,2}` · 자리 전이 `adapt_v15_xsiteD`(E 만 적응, D 넷으로 채점) ·
`adapt_v15_xsiteE`(D 만 적응, E 로 채점) · 예산 대조 `adapt_v15_b{0,1,2}`(steps 1781).

## 2. 성적 — **v15 는 E 를 얻고 D1 을 잃었다** (13.24)

채점 대상이 둘에서 **다섯**으로 늘었다. v14 와의 델타는 **겹치는 test_1·test_2 로만** 낸다 —
다섯 평균과 둘 평균을 견주면 파일 구성 차이가 결론처럼 보인다. **`test_1`·`test_2` 의 라벨이
v14 채점 때와 소수점까지 같음을 확인했다.**

**기기별 (3시드, `--session-merge`, 후처리 끔, soft)**

```
test_1 (D1)         v14 -> v15        test_2 (E1)         v14 -> v15
  **충전기**      0.857 -> **0.283**    **충전기**      0.882 -> **0.999**
  미니PC          0.800 ->   0.728      **프로젝터**    0.811 -> **0.976**
  핫플·오븐       규약 포화, 정보 없음   **미니PC**      0.516 -> **0.338**
                                        포트·드라이기   0.993 -> 0.993 (그대로)
                                        선풍기          0.968 -> 0.959
```

**핫플·오븐은 네 판에서 소수 셋째까지 같다** — 세션 병합 규약이 포화한 것이라 모델과 무관하다.
정보가 있는 것은 SMPS 넷뿐이다.

**네 갈래 (잔차W / on-off F1)**

```
                       1(D1)         2(E1)         3(D2)         4(D2)         5(D2)
cnn_v15 (적응 없음)  22.20/0.763  16.98/0.852  72.69/0.753   7.99/0.815 320.49/0.492
xsiteD (E로만 적응)  20.33/0.774   4.44/0.880  75.47/0.763   7.10/0.849 330.08/0.492
xsiteE (D넷 적응)     6.31/0.726  30.46/0.865   9.43/0.703   1.78/0.654  27.31/0.514
adapt_v15_s0          6.83/0.734   7.76/0.866  14.50/0.695   1.53/0.645  38.20/0.513
```

**적응 이전부터 갈려 있다** — `cnn_v14` test_1 F1 0.8591 대 `cnn_v15` 0.763. 범인은 생성기다.
`test_4`(D2 순수 SMPS)가 잔차 1.5W 로 가장 좋고, `test_5`(저항 4종)가 38W 로 가장 나쁘다.

**배제한 것 둘** — (a) 적응 예산: 창이 3,089 -> 5,502 인데 steps 가 1000 그대로였다. `steps 1781`
대조는 잔차만 내리고(−0.5~−12.5W) F1 은 ±0.013, 충전기는 0.283 -> 0.307 에 그친다.
(b) 회로모델 공동 적합: 옛·새 모델이 D 실측 홀수차에서 −1.7~−15.0% 대 −2.1~−11.6% 로 같다.

**찾은 결함** — 결합 고정점이 Z 가 크면 안 잠긴다. 잠근 값 대비 전류 오차가
**Z 0.42Ω(E1) 0.9~3%, Z 1.15Ω(D1) 26~50%, Z 2.00Ω 169~204%** 다. 감쇠(w=0.5)면 4~6회에 1% 아래로
잠긴다. **인과는 아직 미증명** — 감쇠를 넣고 다시 구워야 갈린다.

## 2b. v14 성적 (13.13 · 13.14) — 옛 기준

**합성 홀드아웃 8,000창 / 1단계 300ep**

```
판                        MAE    F1평균   최악 F1        저항3종   잔차W   드라 편향(on)
cnn_v13e (옛 기준)        12.64   0.934   minipc 0.758   0.878   27.76   −276.5
cnn_v14                   5.47   0.923   에어컨 0.779  **1.000**  27.54    −34.3
```

**실측 / 2단계 / 후처리 끔 / `--session-merge`**

```
판                   오귀속W 소프트/하드   최악 FA       잔차W       on/off F1
adapt_v14 3시드     0.93~1.41 / **0.000** 0.011~0.014 **4.94~5.40** 0.836~0.905
저항 3종 배분(후처리 끔)  포트 68~77W · 핫플 3.7~5.7W · 오븐 17.0~24.0W, 검출률 1.000
```

**자리 전이 (같은 규약으로)**

```
판                  test_1(D)  잔차W  망설임%   test_2(E)  잔차W
cnn_v14 (적응 없음)    0.8591   22.41   33.6      0.8544   23.86
adapt_v14_xsiteD      0.8624   22.28   35.9      0.8667    4.75   <- E 에서만 적응
adapt_v14_s0          0.9360    4.72   22.2      0.8583    5.16
```

처음 보는 자리의 손해는 **F1 −0.074, 잔차 4.7배**다. F1 은 이 결함을 거의 안 보여 준다.

## 3. 고친 것

```
① 드라이기 강풍이 0W 로 나오고 985W 가 포트로 갔다 (13.11)
   L_harm 이 기기당 페이저 하나를 써서 강풍 창에서 **정답이 오답보다 27배 비쌌다**
   -> --state-signatures (상태별 지문, 18개 상태)
   ⚠ 경사 쪽 처방 둘(--w-state-power · --off-detach-praw)은 실패했다 — 음성 대조로 남겼다
② 드라이기 약풍 게이트가 0.155 로 안 열렸다 (13.11.7) — **짝수차 위상은 플러그 방향이다**
   -> 입력 짝수차를 크기로 (배치 v2) + 손실도 크기 공간으로 (--harm-even-magnitude)
   -> 확인됨 (13.14.1): 다섯 구간 전부 게이트 1.000, 단독 461·464W (참 491·495W)
③ **생성기를 두 자리에 맞췄다 (13.17~13.23)**
   · 텍스처 표본: 자리 명시 · 세션 균등 · 최근접 낙하 (13.19)
   · 회로모델 3종을 두 자리 공동 적합 + 자리 비를 목적함수에 (13.20, fit12 --site-ratio)
   · 텍스처를 **개방 전압**으로 되돌리고 단독 SMPS 자기 결합을 켰다 (13.22)
   -> D 녹화에서 E 녹화를 만들어 내는 오차: 충전기 0.227 -> 0.166, 프로젝터 0.137 -> 0.117
```

## 4. 알려진 결함 — 결과가 나쁘면 여기부터

```
**검증 관문**  **1순위.** `run_mixval12` 가 **결합 경로를 한 번도 안 부른다** (`circuit hits=0`) —
              창의 측정 단자 전압을 소스로 써서 결합이 이미 들어 있기 때문이다. 기본값도 `--stems test_2`
              (E 하나)이고, 13.22 의 검정은 **D→E 한 방향**뿐이었다. 그래서 13.22·13.23 이
              "[B] 가 소수점까지 같다" 로 통과시킨 변경들이 D 를 어떻게 바꾸는지 아무도 안 봤다 (13.24.14).
              **생성기를 바꾸기 전에 이 관문부터 고쳐라**
**D1 충전기**  **2순위.** F1 0.857 -> 0.283 (재현 0.795 -> 0.166), 적응 이전부터. D2 는 0.94~0.98 로
              멀쩡하다 — **자리가 아니라 세션(D1) 문제다.** mixval 도 D1 에서만 [B] 0.200 > [A] 0.169
              (다른 파일은 전부 [B] < [A])로 같은 곳을 가리킨다 (13.24.13·13.24.14)
**저항 3종**   **3순위.** `test_5`(포트+오븐+핫플+드라이기)에서 포트를 **통째로 놓치고**(검출 0.000)
              그 전력이 오븐으로 간다(+518~566W). 합성에서는 혼동 정확도 **1.000** 이다 —
              생성기가 가르는 판을 안 만들고 있다 (13.24.15)
자리 전이      처음 보는 자리에서 F1 −0.074 · 잔차 4.7배 (v14 기준).
              장소가 고차 지문을 크게 바꾼다 — 같은 충전기를 58~66W 로 좁혀 재면 h13·h15 가 2.5~2.8배다.
              원인은 임피던스가 아니라 **전압 텍스처**가 거의 전부다 (13.15.2)
              ⚠ **전달비는 (자리, 전력) 의 함수다** — 충전기 h13 이 23W 0.535 -> 63W 2.474 로 단조
              상승하고 42~51W 에서 1 을 지난다. 그런데 **곡선의 높이는 기기마다 다르다** —
              미니PC 는 전 구간 1 위다 (13.18.2). 자리당 전달비 하나는 원리적으로 안 된다
자리 감도      회로모델이 아직 짧다. W=1 공동 적합 뒤에도 자리 비 오차가 충전기 0.143 · 미니PC 0.096 이다
              (프로젝터 0.035 는 거의 닫혔다). 충전기가 가장 나쁜데 저전력 짝(32~39W)이 CV 테이퍼라
              동작 모드가 다를 수 있다 (13.18.2)
결합 고정점    **전류 오차로 쟀다** (13.24.13): 잠근 값 대비 Z 0.42Ω(E1) 0.9~3%, **Z 1.15Ω(D1) 26~50%**,
              Z 2.00Ω 169~204%. 감쇠(w=0.5)면 **n=4 에 ~1%, n=6 에 ~0.1%** 로 잠긴다.
              대가는 결합 반복 수 그대로다 (n=6 이면 캐시 52.8 -> 약 98분). 기본값 제안은 n=6
미니PC        E 에서 0.516 -> **0.338** (재현 0.205). E 녹화 둘을 넣었는데 **더 나빠졌다** —
              자리 자료 가설은 기각됐다 (13.24.16). 남은 가설은 **동작점**이다: 복합 시험은
              미니PC 를 100% IDLE(5.8~11.4W)로 쓰는데 단독 녹화는 72~84% 가 ACTIVE(18~20W)고,
              새로 넣은 E 녹화가 IDLE 을 **가장 적게**(16~22%) 담았다
회로모델 없는 넷  선풍기(유도 전동기)·드라이기 약풍(반파)·포트·핫플은 자리 반응이 1/V 뿐이다
선풍기        광역 확장의 손해가 **진짜다** (13.16). 시드 3개 대응비교로 Δ −0.037±0.009, t −7.05.
              합성 집계는 무승부(+0.0003)이고 12 -> 35 채널의 근거는 실측이다 (0.897 대 0.829)
에어컨·핫플     13.13.4 의 −0.063 / −0.045 는 **시드 산포였다** (3시드 −0.030±0.029 / −0.004±0.037).
              243·275창으로는 ±0.03 을 못 가른다
프로젝터       E 에서 0.811 -> **0.976** (정밀 0.683 -> 0.955) 로 풀렸다. 그런데 **D2 에서 0.215~0.250**
              (재현 0.12~0.15) 이다 — `beam_projector_3` 이 바로 그 D2 녹화인데도 그렇다
활성화 수      프로젝터 15 · 충전기 12 · 드라이기 11 · 포트 10 · 미니PC 7 · 핫플 6 · 오븐 5 · 선풍기 4 · 에어컨 2
```

## 5. 자료 요청 (우선순위)

```
① **교차 녹화 — 전력을 훑으면서.** 자리 반응이 전력의 함수다 (13.15.3). 한 전력점만 찍으면
   그 점의 전달비밖에 안 나온다. 기기마다 저·중·고 세 점 이상, 양쪽 자리에서 **같은 전력대로**.
   ✔ E 미니PC · D 프로젝터는 들어왔다. 남은 것은 **D 의 선풍기·드라이기·포트·에어컨**과
   **E 의 핫플·오븐** 이다 — 그 여섯은 아직 한 자리뿐이다
② 활성화 더: 에어컨·핫플·오븐·미니PC 를 각 5회 이상, 설정 바꿔 가며, 파일 끝에 꼬리 10초 (규칙 2)
③ 복합 녹화 — 저항 4종만 켜는 판이면 규칙 20 의 대조가 되살아난다
④ 세션마다 1kW 급 저항 on/off 3회 (규칙 8) · 순저항 스냅샷 (규칙 7)
⑤ **꼬리를 계속 붙여 달라 (규칙 2).** 지금 14개 녹화에 16~41초씩 있고 그것이 개방 전압 기준이라
   텍스처를 되돌리는 근거다 (13.22). `laptop_charger_1` · `oven_1` 에는 없다. 길수록(30초+) 좋다
⑥ **플러그 방향을 기록해 달라** (13.11.7). 반파 부하는 꽂는 방향으로 짝수차가 180° 돈다
```

하드웨어 점검 하나: ZMPT101B 직렬저항이 28.7kΩ이면 거기서 **1.65W** 가 난다 (2~3W 정격이 아니면 뜨겁다).

## 6. 커밋 상태

`c590334` 까지 푸시했다 (13.3~13.23). 그 뒤 이 문서와 v15 결과가 남아 있다.

## 7. 읽기 주의 — 실제로 빠진 함정

```
· **관문이 그 경로를 부르는지 확인하라.** `run_mixval12` 를 생성기 변경의 관문으로 삼아 왔는데
  결합 델타를 **한 번도 안 부른다** (로그의 `circuit hits=0` 이 매번 찍혀 있었다). 13.22.4 에
  "혼합검증은 이 변경을 못 본다" 고 내가 직접 적어 놓고도 같은 관문을 계속 썼다 (13.24.14)
· **검정은 양방향으로 하라.** 13.22.4 는 D→E 만 쟀고 거기서 충전기가 −27% 좋아졌다.
  실제로 E 에서 0.882→0.999 가 됐고 **D1 에서 0.857→0.283 이 됐다.** 안 잰 방향에서 잃었다
· **평균 말고 줄을 읽어라.** [B]가 [A]보다 나쁜 유일한 파일이 test_1(D1)이었고 (0.200 대 0.169)
  충전기가 죽는 유일한 파일도 D1 이다. 13.20 부터 그 표를 찍고 있었는데 평균만 봤다
· **파일 이름에 자료 모양을 걸지 마라.** 계측기 교체로 test_1~5 가 전부 다른 녹화가 됐는데
  이름은 그대로다. 같은 부류의 결함이 이번 판에만 셋이다 (13.19 둘, 13.24.10 하나)
· **반복 간 변화가 아니라 결과 오차를 재라.** 13.23.4 는 "3회 뒤 3.8% 가 남는다" 로 끝냈는데
  그것이 전류로는 26~50% 였다 (13.24.13). 잠근 기준을 만들어 견줘야 크기를 안다
· **벤치는 production 규모·설정으로, 명령과 함께 적어라.** 24k 의 124 win/s 가 300k 에서 95 였고
  레시피를 안 적어 사후에 가를 수도 없다 (13.24.6)
· **규약이 포화한 지표는 정보가 없다.** 세션 병합에서 핫플·오븐 F1 이 네 판에 걸쳐 소수 셋째까지
  같다. 그 숫자로는 어떤 판도 못 고른다 (13.24.11)
· **사용자가 이미 준 것을 먼저 찾아라 (13.22).** "무부하 전압을 받아야 풀린다" 고 자료를 요청했는데
  규칙 2 의 꼬리가 이미 있었고 전처리 로그가 매 파일마다 찍고 있었다
· **중앙값을 내기 전에 부류를 갈라라 (13.22).** 순저항 6파일이 SMPS 8파일의 신호를 지웠다.
  물리적으로 반응이 없어야 하는 표본을 섞으면 있는 효과도 사라진다
· **검정의 잡음 바닥을 같이 재라 (13.22).** 전압 고조파는 h9 위에서 0.3~0.5V 라 신호/바닥이 2 안팎이다.
  그 차수의 "안 됐다" 는 "못 잰다" 는 뜻이었다
· **예측이 어긋나면 준 입력부터 의심하라 (13.18.3).** 2.3배 틀린 예언의 범인은 모델이 아니라
  대표 프로파일(−14%)과 **세션 선택**(−46%)이었다
· **`circuit12` 를 최상위로 임포트하지 마라 (13.23.5).** `_core12` 가 `@njit(cache=True)` 라 numba 가
  디스크 캐시에 모듈 이름을 박는다. `circuit12` 와 `circuit_model.circuit12` 로 섞으면 그 뒤
  **모든 회로 호출이 조용히 실패**한다. 걸리면 `circuit_model/__pycache__` 를 지운다
· **옛 산출물이 같은 이름의 새 파일을 덮는다 (13.17.4).** `results/seq_time_map.json` 의 옛 test_4
  항목이 새 test_4 라벨을 통째로 뭉갰다. `results/` 의 옛 json·상수표는 시대를 확인하고 쓴다
· **두 실측 파일의 on/off F1 을 그냥 견주지 마라 (13.14.2).** `run_adapt` 는 `session_merge` 를 안 쓴다.
  핫플·오븐은 듀티 부하라 그러면 F1 이 0.63 으로 눌리는데 test_1 에만 그 둘이 있다
· **입력 채널 분포로 합성 간극을 재지 마라 (13.17.5).** 파일마다 켜진 기기가 달라 조합이 안 고정된다.
  혼합검증(`run_mixval12`)이 자다 — 다만 텍스처를 뽑지 않으므로 **13.19·13.22 의 변경은 못 본다**
· **오븐의 스위치는 히터가 아니라 15W 제어보드다.** 신원은 크기가 아니라 비(|ΔI2|/|ΔI1| ≈ 0.07)다
· **`|ΔI_h|` 로 잰 계단 지문은 고차가 부풀려진다.** 남의 자리 지문은 격리 녹화로만 잰다
· **짝수차 위상을 쓰는 특징은 만들지 마라.** 플러그 방향이다. 크기는 안전하다
· 지문이 기기당 하나면 **상태가 여럿인 기기를 못 담는다.** `--state-signatures` 를 항상 켠다
· 후처리로 좋아진 숫자는 **덮은 것일 수 있다.** 포트 453 -> 8.2W 가 그랬다 (평균 +159.5W 가 남았다)
· 집계 F1 은 크기 결함을 숨긴다. **기기별 표의 편향(on) 열을 F1 과 같이 본다**
· 채널 배치를 바꾸면 `fine[:, :N]` 슬라이스가 조용히 틀린다. 가드가 안전장치다 (두 번 일했다)
· 2단계 체크포인트의 배치 키는 부모에서 물려받지 않고 **현재 코드 값**을 적는다
· npz 의 seq 는 **블록**(30주기), 원시의 seq 는 **사이클**이다
· A/B 비교는 합성기를 **시드 앞에** 만든다 (생성자가 전역 난수를 한 칸 먹는다)
· bash heredoc 안 파이썬 문자열의 \n 이 실제 줄바꿈이 된다. 패치는 파일로 쓰고 실행한다
· 핫플 짧은 펄스(2~5사이클)는 1초 중앙값 분류기·2Hz 계단 검출 둘 다 못 본다
· 오븐 스위치 끔은 히터가 아니라 팬·조명(−13W)이고 핫플 펄스 사이 바닥에서만 보인다
```
