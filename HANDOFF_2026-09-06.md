# 인수인계 2026-09-06 — 계측계는 닫혔고, **배분은 풀렸다. 남은 것은 장소 전이와 자료다**

**정본은 `READ_ME_FIRST.md`**, 입력 채널은 **`INPUT_CHANNELS.md`**, 설계 기록은 **13절**(13.1~13.14),
측정 규칙은 `MEASUREMENT_RULES.md` **v2 규칙 1~8**. 앞 문서: `HANDOFF_2026-09-05b.md` (커밋에만 있다).

## 0. 한 줄

옛 계측기(차동 ADC)의 공통모드 한계가 짝수차·전압 h3·"장소 지문" 인공물의 뿌리였다. 단일 입력 ADC 로
바꾸고 옛 자료를 전부 지웠다. 계측 경로는 닫혔고(원시↔2Hz 전달함수가 펌웨어 소스로 확정),
**배분 결함 셋을 뿌리까지 고쳤다** — 저항3종 혼동 0.878 → 1.000, 포트 배분 오차 453 → 77W(후처리 없이),
드라이기 약풍 게이트 0.155 → **1.000**. **남은 것은 장소 전이와 자료 분량이다.**

## 1. 지금 상태

```
자료      data/  2Hz 17파일 + 원시 15 (충전기 8 · 미니PC 5 · 프로젝터 2)
장소      **둘이다.** D 저녁 216V·vh3 0.7%·Z 1.15Ω  /  E 심야 229V·vh3 3.0%·Z 0.42Ω
          D: test_1 · hotplate_1 · laptop_charger_1 · oven_1 · minipc_1 · noise 3종
          E: test_2 · hair_dryer_1 · electiric_kettle_1 · air_conditioner_1 · beam_projector_1·2 · fan_1 · laptop_charger_2
          ⚠ 9종 중 **충전기만** 두 장소에 있다 (13.11.1)
라벨      test_1 17/17, test_2 31/31, uncertain 0
입력      세밀 **45**채널 · 광역 **35**채널 (배치 v2, 13.12). 짝수차는 **크기만**
회로모델   circuit_model/circ12_{laptop_charger,minipc,beam_projector}.pkl (v12g)
합성기     텍스처 델타 + Z 결합 델타 + R 상태 (13.2). ①a 끔. 지터 전부 0
운영점     **없음.** v13 계열은 배치 v1 이라 로더가 거부한다. 배포 금지
테스트     137P / 0F / 5S
```

**최신 판 `v14`** — 캐시 `cache/train60_v14` · 홀드아웃 `processed_data/holdout60_v14` ·
1단계 `cnn_v14`(+`cnn_v14_nowide`) · 2단계 `adapt_v14_s{0,1,2}` · 전이 `adapt_v14_xsiteD`.

## 2. v14 성적 (13.13 · 13.14)

**합성 홀드아웃 8,000창 / 1단계 300ep**

```
판                        MAE    F1평균   최악 F1        저항3종   잔차W   드라 편향(on)
cnn_v13e (옛 기준)        12.64   0.934   minipc 0.758   0.878   27.76   −276.5
cnn_v14                   5.47   0.923   에어컨 0.779  **1.000**  27.54    −34.3
cnn_v14_nowide (광역 절제) 5.30   0.931   minipc 0.768  **1.000**  26.36    −41.3
```

**실측 test_1·test_2 / 2단계 / 후처리 끔 / `--session-merge`**

```
판                     오귀속W 소프트/하드     최악 FA        잔차W        on/off F1
adapt_v13e_nores      0.47~1.10 / 0.0~0.1     0.001      5.92~8.09    0.832~0.839
adapt_v14 3시드       0.93~1.41 / **0.000**   0.011~0.014  **4.94~5.40**  **0.836~0.905**
adapt_v14_nowide      0.95                    0.019       10.86        0.829
```

**저항 3종 배분 (중앙|오차|)**

```
                     포트 (1476.7W)   핫플 (446.8W)   오븐 (1107.0W)   검출률
adapt_v13e 후처리 끔    321~453W        5.9~10.8W      11.1~17.3W     1.000
adapt_v14  후처리 끔   **68~77W**       3.7~5.7W       17.0~24.0W     1.000
adapt_v14  후처리 켬       8.2W           0.2W          13.1W         1.000
```

**장소 전이 (13.14.2, 같은 규약으로 다시 잰 값)**

```
판                  test_1(D)  잔차W  망설임%   test_2(E)  잔차W
cnn_v14 (적응 없음)    0.8591   22.41   33.6      0.8544   23.86
adapt_v14_xsiteD      0.8624   22.28   35.9      0.8667    4.75    <- E 에서만 적응
adapt_v14_s0          0.9360    4.72   22.2      0.8583    5.16
```

처음 보는 장소의 손해는 **F1 −0.074, 잔차 4.7배**다. 심야에서 적응해도 저녁은 F1 이 0.003 움직인다.

## 3. 고친 것 셋 — 뿌리는 둘이다

**① · ② 는 "기기당 스칼라 하나로는 상태나 극성을 표현할 수 없다" 였다.**

```
① 드라이기 강풍이 0W 로 나오고 985W 가 포트로 갔다 (13.11.4~13.11.6)
   L_harm 이 기기당 페이저 하나를 쓰는데 지문 문턱 482.5W 에 약풍(487W)이 걸려 중앙값이 반파에 앉았다
   (h2/h1 0.4290). 강풍 창에서 정답 22.792 대 "포트로 넘기기" 0.855 — **정답이 27배 비쌌다.**
   와트당 |I1| 이 드라이기 4.3990 / 포트 4.3872 로 0.3% 차이라 h1 으로는 못 가른다.
   -> **--state-signatures** (상태별 지문). 18개 상태를 따로 맞춘다
   ⚠ 경사 쪽 처방 둘(--w-state-power · --off-detach-praw)은 **실패했다** — 음성 대조로 남겼다

② 드라이기 약풍 게이트가 0.155 로 안 열렸다 (13.11.7)
   **짝수차 위상은 플러그 방향이다** — 플러그를 뒤집으면 I_h -> −(−1)^h I_h 라 짝수차만 180° 돈다.
   격리 대 복합에서 h2 −6.63° 대 +172.60°, h4 −177.52° 대 −0.74°, 홀수차는 10.7° 차, |I2| 는 같다.
   합성은 격리 페이저를 재생하므로 실측 창이 Re I2 에서 82 IQR 밖으로 떨어졌다.
   -> 입력 짝수차를 **크기로** (배치 v2) + 손실도 크기 공간으로 (**--harm-even-magnitude**)
   -> **확인됨 (13.14.1):** 약풍 다섯 구간 전부 게이트 1.000, 단독 구간 461·464W (참 491·495W)

③ (아직 안 고침) 반파+순저항 겹침 배분 (13.14.1)
   약풍이 포트와 겹치면 드라이기 −200W · 포트 +59W 다. 게이트도 지문도 아니고 **배분**이다
```

## 4. 알려진 결함 — 결과가 나쁘면 여기부터

```
**장소 전이**  **1순위 (13.13.5 · 13.14).** 뿌리는 **기기마다 장소 반응이 다르다**는 것이다.
              두 장소에 다 있는 유일한 기기(충전기)를 58~66W 로 좁혀 재면 h1 은 1/V(0.930) 인데
              h13·h15 가 **2.5~2.8배** 다르다. 원인은 전압 고조파가 아니라 **선로 임피던스**다
              (D 가 vh7·vh11~vh15 는 오히려 3~8배 큰데 전류 고차는 작다 -> 선형 어드미턴스 기전 사망.
              E 는 Z 0.42Ω 이라 정류 펄스가 좁고 높다).
              ⚠ **장소당 전달비 하나(12.179 식)는 원리적으로 안 된다** — 충전기의 T 를 test_1 에
              걸면 충전기가 0.781 -> 0.918 로 살고 미니PC 가 0.731 -> 0.404 로 죽는다 (13.14.5).
              합성은 이미 기기별로 반응하는데(회로모델), **검증된 것은 충전기 하나뿐**이다.
              **자료로만 풀린다** (§5 ①).
미니PC        **두 곳에서 최악이고 둘은 같은 뿌리일 수 있다.**
              (a) 남의 장소(test_2, E)에서 F1 **0.525** · 재현 0.356 · 게이트 중앙 0.003.
                  전체 평가에서 가장 나쁘다. 적응 전에도 그렇다 (13.14.3)
              (b) 합성의 장소 반응이 **거꾸로**다 (E/D 가 h9~h15 에서 0.76~0.80. 충전기는 1.3~2.0).
                  13.7.3 이 잰 pkl 고차 부족(|모델/실측| h13 **0.64**)과 같은 자리다 (13.14.6)
회로모델 없는 여섯  선풍기(유도 전동기)·드라이기 약풍(반파)은 장소 반응이 있어야 하는데 합성이
              1/V 만 준다 (13.14.6). 순저항 셋과 드라이기 강풍은 1/V 가 맞다
선풍기·에어컨   광역 확장에서 −0.034 / −0.063 (2.7~2.8σ). 서로 헷갈리는 것은 **아니다** —
              와트당 지문 거리가 선풍기↔에어컨 0.70~1.20 인데 선풍기 자기 3단끼리는 0.20~0.50 이다.
              손해가 ON 창이 가장 적은 셋(243·275·856)에만 갔으므로 시드 산포일 수 있다.
              가리려면 1단계 시드 3개씩 두 팔 (2.7시간). 미결
프로젝터       test_2 에서 F1 0.804 인데 **정밀도 0.673** 이다 (재현 0.999). 장소가 아니라 대기·유휴
              구분 문제로 보인다. 아직 안 팠다
활성화 수      여전히 발목. 에어컨 ON 창 243 · 핫플 275 (합성 홀드아웃 기준)
```

## 5. 자료 요청 (우선순위)

```
① **교차 녹화 — 같은 전력대로.** 장소 반응은 기기마다 다르고 동작점도 타는 것으로 보이므로
   (13.14.5), 한쪽 65W · 다른 쪽 28W 로 찍으면 못 쓴다 (지금 충전기가 그렇다).
   1순위는 **심야(E)의 미니PC** — 무너지는 그 기기이고 회로모델의 방향을 검정할 유일한 자료다
   (13.7.5 ①의 "심야 미니PC 원시"와 같은 자료. 2Hz 도 같이).
   그다음 저녁(D)의 프로젝터·선풍기·드라이기, 심야(E)의 핫플
② **충전기 확인 하나:** `laptop_charger_1`(저녁) 과 `laptop_charger_2`(심야) 가 **같은 물리 어댑터**인가?
   13.14.4 의 결론 전체가 그 가정 위에 있다. 다른 것이었다면 다시 재야 한다
③ 활성화 더: 에어컨·핫플·오븐·미니PC 를 각 5회 이상, 설정 바꿔 가며, 파일 끝에 꼬리 10초 (규칙 2)
④ 복합 녹화 하나 더 — 지금 둘뿐이라 유보 실측이 하나밖에 없다. 저항 4종만 켜는 판이면 규칙 20 의
   대조도 되살아난다
⑤ 세션마다 1kW 급 저항 on/off 3회 (규칙 8) · 순저항 스냅샷 (규칙 7)
⑥ **플러그 방향을 기록해 달라** (13.11.7). 반파 부하는 꽂는 방향으로 짝수차가 180° 돈다.
   지금은 크기만 써서 피하고 있지만, 방향을 알면 위상도 쓸 수 있다
```

하드웨어 점검 하나: ZMPT101B 직렬저항이 28.7kΩ이면 거기서 **1.65W** 가 난다 (2~3W 정격이 아니면 뜨겁다).

## 6. 커밋 상태

`d803264` 까지 푸시했다 (13.3~13.13 + 원격의 원시 수신·교정 도구 병합).
그 뒤 13.14(약풍 확인 · 장소 전이 원인)와 이 문서가 남아 있다.

## 7. 읽기 주의 — 실제로 빠진 함정

```
· **두 실측 파일의 on/off F1 을 그냥 견주지 마라 (13.14.2).** `run_adapt` 의 내부 채점은
  `session_merge` 를 안 쓴다. 핫플·오븐은 듀티 부하라 그러면 F1 이 0.63 으로 눌리는데
  **test_1 에만 그 둘이 있다.** 13.13.5 의 "0.686 대 0.854" 는 대부분 이 규약 차이였다.
  판끼리 견줄 때는 `run_gate_check --session-merge` 로 다시 잰다
· **`|ΔI_h|` 로 잰 계단 지문은 고차가 부풀려진다 (13.14.9).** 잡음이 크기에 양수로 더해진다 —
  ΔP 6~12W 계단은 h13 이 2배 뜬다. 남의 장소 지문은 **격리 녹화로만** 잰다
· **저부하 창을 `p_observed < 60W` 로 고르면 핫플 펄스의 틈을 고른다.** 추세 채널(29·30)이
  95 IQR 튄다. 창의 10초 전체가 조용한 것으로 골라야 한다 (13.14 의 probe_b3b)
· **짝수차 위상을 쓰는 특징은 만들지 마라.** 플러그 방향이다 (13.11.7). 크기는 안전하다
· 지문이 기기당 하나면 **상태가 여럿인 기기를 못 담는다.** 드라이기가 그 극단이었고 오븐·선풍기·
  에어컨도 상태별로 크게 다르다. `--state-signatures` 를 항상 켠다
· 후처리로 좋아진 숫자는 **덮은 것일 수 있다.** 포트 453 -> 8.2W 가 그랬다 — 평균 +159.5W 가 남아 있었다
· 집계 F1 은 크기 결함을 숨긴다. 드라이기는 합성 F1 0.999 인데 편향(on) −276.5W 였다.
  **기기별 표의 편향(on) 열을 F1 과 같이 본다.** 장소 전이도 F1 은 −0.074 뿐인데 잔차는 4.7배다
· 채널 배치를 바꾸면 `fine[:, :N]` 슬라이스가 **조용히 틀린다.** 가드(`fine_layout`·`wide_channels`)가
  안전장치다 — 이번에 두 번 일했다 (v13f 2단계, v14 관문)
· 2단계 체크포인트의 배치 키는 **부모에서 물려받지 않고 현재 코드 값을 적는다.** 2단계는 실측 입력을
  코드로 만들므로 그때 배치가 그 체크포인트의 배치다
· npz 의 seq 는 **블록**(30주기), 원시의 seq 는 **사이클**이다
· 조건별 적합이 갈리면 **축퇴부터** 배제한다 (공동 손실에 넣고 하나씩만 조건별로 푼다)
· A/B 비교는 합성기를 **시드 앞에** 만든다 (생성자가 전역 난수를 한 칸 먹는다)
· 캐시 30만창은 **89분**이다 (13.2 회로모델 델타 때문. 옛 10~12분은 그 이전 수치다)
· bash heredoc 안 파이썬 문자열의 \n 이 실제 줄바꿈이 된다. 패치는 파일로 쓰고 실행한다
· 핫플 짧은 펄스(2~5사이클)는 1초 중앙값 분류기·2Hz 계단 검출 둘 다 못 본다
· 오븐 스위치 끔은 히터가 아니라 팬·조명(−13W)이고 핫플 펄스 사이 바닥에서만 보인다
· 같은 콘센트가 아니다 — D 와 E 는 **다른 장소**다 (13.11.1). vh3 0.7% 대 3.0%, Z 2.7배
```
