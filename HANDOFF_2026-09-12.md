# 인수인계 2026-09-12 — 3차원 자기보정으로 실패 ③ 이 열렸고, 표류의 정체가 밝혀졌다 (13.84.38~59)

이 문서 하나만 읽고 이어받을 수 있게 썼다. 근거는 전부 `MODEL_TRAINING_DESIGN.md`
**13.84.38 ~ 13.84.74** 에 있다. 앞선 상태는 `HANDOFF_2026-09-10.md`.

---

## 0. 지금 당장 알아야 할 것 여섯

**⓪ ★★ 먼저 읽어라 — 우리 지표가 한쪽 눈이었다 (13.84.74, 사용자 지적).**
기기별 on/off 평균만 봐 왔는데, 그 사이 **전력 재구성이 무너졌다**. 잔차를 넣고 재면:
```
체크포인트                    전체 W   오븐/핫플 켜짐   그 밖
cnn_v37 (2단계 없음)          40.7      **59.3**     14.4
seq_h38_base (물려받음)       54.1      **82.1**     15.2
v36p_mask (scratch)         69.5     **104.6**     17.7
```
· **큰 부하에서만 터진다** — test_1 오븐/핫플 구간 11.2 -> 65.1W (5.8배), 그 밖은 2.5 -> 2.5W.
  SMPS 뿐인 test_4 는 3.5 -> 7.5W 로 거의 안 움직인다.
· **`--init scratch` 교락이 아니다** — 물려받은 판도 +38% 다.
· 2단계는 **작은 부하 on/off 를 얻고 큰 부하 전력을 잃는** 맞바꿈인데, 기기마다 한 표인
  우리 지표는 **얻는 쪽만 센다**. test_3 의 "그 밖" 잔차는 25.7 -> 9.1 로 오히려 좋아졌다.
⇒ **13.84.69·13.84.72 의 판정(가림 +0.0251 · 창별 −0.027)은 전부 그 한쪽 눈으로 낸 것이다.**
⇒ 채점기에 넣었다: `run_score_seq.py` 가 잔차를 파일별·오븐핫플/그밖으로 찍는다.
⇒ **가장 먼저 볼 자리**: `run_train_seq` 가 `LossWeights(... cons=0.0 ...)` 로 **박아 놨다**
  (`src/run_train_seq.py:322`). 재구성을 손실에 넣은 적이 **한 번도 없다.**

---
## 0. 지금 당장 알아야 할 것 다섯 (아래는 종전 목록)

**① 접속 포트가 8000 이다. 25565 가 아니다.**
`gate1-access/` 꾸러미(2026-08-26)가 25565 로 틀려 있었고 그대로 깔면 1홉에서 막힌다.
이 저장소 사본은 고쳤다 — **꾸러미를 새로 받아 덮어쓰면 되살아난다.**
새 PC 에서는 `bash gate1-access/install.sh` 뒤 `grep -A2 gate1_Oracle ~/.ssh/config` 로 `Port 8000` 확인.
진단 절차는 `HPC_RULES.md` §10 (신설).

**② 지금 쓸 것은 학습이 아니라 채점 시점 절차다.**
```bash
python -X utf8 src/run_build_drift.py --block 600 --k 3      # 한 번 (results/drift_basis.npz)
python -X utf8 src/run_diag_selfcal2.py results/<ck>.pt --k 3 --anchor hard
```
어느 체크포인트에도 붙는다. 재학습 없음. 미니PC 시간 정확도 **0.8046 -> 0.9170** (13.84.53).

**③ HPC 는 **비어 있다**. `973597`(빈 슬롯 고침)은 끝났고 **채점도 끝났다** (13.84.72).**
판정은 **맞바꿈**이다: 상태별 전력 오차 중앙(15개 상태 전부) 10.4% -> **4.3%**, 그 대신
실측 on/off 창별 0.8466 -> **0.8197** (산포의 3~4배라 실재한다).
⚠ **① 만으로는 전력이 안 좋아진다** (11.8%) — ②까지 켜야 한다 (13.84.73 이 정정).
**기본값은 안 올렸다** —
`--w-state-power` 는 0, `state_power_init` 만 True(새 판 전용·무영향).
⚠ 빈 슬롯은 `cnn_v37` 에서 **물려받은** 것이었다 — `--init scratch` 판들엔 원래 없었다.
⇒ 이 맞바꿈을 결정하려면 **실측에서 전력을 잴 자**가 먼저 필요하다 (§6-10).

**③-2 앞 판 `972758`/`972759` 는 정상 종료했고 채점도 끝났다 (13.84.69).**
판정: 실측 표류 증강(v36dp)은 손 맞춤(v36p)을 **못 이긴다** — 접는다. 가림은 **유지**가 낫다
(창별 +0.0251, 칸 안 산포의 7배). ⚠ 기준선 `seq_h38_base` 와의 차는 **읽지 마라** —
그 판은 `--init cnn_v37 --keep-on 0` 이고 새 넷은 `scratch`/`keep_on 1` 이다 (교락 둘).
`972281`(standby_per_record)도 도움이 안 됐다 (§6-1).

**④ 학습기 주 루프 고침은 **끝났고 실이득도 쟀다** (13.84.63 + 13.84.71).**
`crf_nll` 이 CPU 에서 돈다 (기본값, 값 동일) + `DataLoader(pin_memory=True)`.
**같은 노드 n071 에서 378초 -> 319초/epoch, 처리량 +18.3%.** 이득은 GPU(34~42 -> 42~47%)가
아니라 **load(37.4 -> 48.4/48코어)** 에서 왔다 — 목을 넓히니 워커가 다 돌았다.
⚠ 노드가 다르면 속도도 다르다 (n071 Ada 대 n055 A10 470초). **같은 노드끼리** 견줘라.
⚠ 아직 병목은 CPU 다. 다음 이득은 GPU 가 아니라 **워커 공급**(창 만들기)에서 온다.

**⑤ ★ 추가 측정 요청 — 사용자가 나중에 녹화해 준다고 했다 (2026-09-12).**
아래 다섯이 지금 자료의 **구멍**이고, 각각이 막고 있는 것이 무엇인지까지 적는다.
새로 재면 `HPC_RULES` 나 학습을 건드릴 필요 없이 바로 값이 나온다.

```
[A] 자리 D — 형제 고정 · 미니PC 만 토글            몇 분      막고 있는 것: 실패 ②(유령)
    같은 자리에서 프로젝터·충전기 상태를 **안 바꾸고** 미니PC 만 몇 번 껐다 켠다.
    지금 test_2·3·4 어디에도 그 짝이 없어서, "자리 D 에서 진짜 미니PC 도 지문과 72~75°"
    라는 13.84.59 의 진단을 **모델 없이 확정하지 못한다.** 확정되면 자리별 보정이 선다.

[B] 새 펌웨어(96열)로 **미니PC** 대역별                각 3분+   막고 있는 것: 전압 꼬리 (13.84.66[7])
    8~14 / 14~19 / 19~24 / 24~30 W 를 각각 3분 이상.
    지금 꼬리 가진 미니PC 파일이 `minipc_4` 하나뿐이고 8~14W 에 1,774 사이클 —
    블록 600 문턱(1,800)에 **26 사이클** 모자라 표에서 통째로 빠진다.

[C] 새 펌웨어로 **프로젝터**                          3분+      막고 있는 것: 같은 것
    꼬리 가진 프로젝터 녹화가 **0개**다. 통전(45W)과 램프-꺼짐(1.5~5W) 둘 다 있으면 좋다.

[D] 새 펌웨어로 **복합 한 판**                        보통 길이  막고 있는 것: 채점 경로 전체
    **가장 중요하다.** test_1~5 는 전부 옛 펌웨어(79열)라 채점 시점에 꼬리가 없다.
    지금은 `run_build_vtail` 의 세션 중앙으로 메워야 하는데 그것은 "평균이지 그 창의
    값이 아니다". 복합이 하나라도 새 펌웨어면 13.84.66 의 예측 보정이 **채점까지** 이어진다.

[E] (있으면 좋다) 미니PC·프로젝터의 **대기 파형** 스냅샷   각 1개   막고 있는 것: 13.84.64 의 3°
    충전기는 `raw_laptop_charger_9` (1.0W) 가 있는데 나머지 둘은 없다. 프로젝터와 미니PC
    대기 페이저가 **3° · 크기비 1.02** 로 사실상 같은데, 그것이 필연인지 이 두 개체의
    우연인지 회로가 답할 수 있다.
```
⚠ **펌웨어 판을 헷갈리지 마라.** 96열(`vhhi17~31` 있음) 이 새 것이고 79열이 옛 것이다.
  `head -1 <파일>.csv | tr ',' '\n' | grep -c .` 로 센다.
⚠ 받은 뒤: npz 재생성은 **꼬리를 버린다** (`voltage_harmonics_complex` 가 (N,15) 고정).
  꼬리는 CSV 에서 따로 읽어야 한다 — `run_diag_driftpred.load_tail` 이 그 규약이다.

---

## 1. 오늘의 줄기 — 무엇이 죽고 무엇이 섰나

```
13.84.38  pow-sig / bg-head 2x2 (970310)   실측 안 움직임. bg 사슬 +0.0101 은 1mA 짜리 섭동
13.84.39  bg-head 는 **죽은 항**          합성이 준 기록당 배경이 1.3~1.7mA (실측 4.5~19.2)
13.84.40  대기 지문을 **기록당 하나씩**    v36r. h9~h15 미모형 배경 1.5 -> 3.5~5.0mA. 972281 이 시험 중
13.84.41  사슬 오류 **전파 실재**          >60초 오류가 오류시간의 56% (창별은 0%)
          ⚠ 정정: 방출이 약한 게 아니라 **틀린 쪽을 가리키는데 너무 세다**
13.84.42  전이 짝 제약                     실측 거짓 전이 2개 — 못 닫음
13.84.43  같은 것을 합성 2,817전이로       미니PC AUC 0.557 — **큰 기기에만 듣는다**
13.84.44  방출 배율 α 훑기                 실측 최적 0.25 · 합성 최적 2.00 (**영역 의존**)
13.84.45  형제 결합 복호                   맞바꿈 **0.0%** — 형제는 항상 맞다. 자리 없음
13.84.46  자기보정 EM (자유도 16)          1회 +0.12, 2회부터 **붕괴**
13.84.47  파형 도통각                      d' 1.26 이었으나 **전력 교락** — 죽음
13.84.48  격리 녹화 상대위상               LORO **AUC 1.000** (전력 19~28W 맞춤)
13.84.50  ⚠ 복합에서는 **안 선다**          전이 Δ 에서 충전기와 3° 안에 겹침
13.84.51  ★ 원인: **미니PC 대기가 ON 의 53%**  Δ = ON−대기라 방향이 달라진다
          ⚠ 13.84.31 의 요구 정밀도 정정: 5.5% -> **2.7%**
13.84.52  ★ 표류는 **3차원**(94%)          PC1(분산 50%)이 미니PC 와 **89° 직교**
13.84.53  ★★ 보정을 3차원으로 묶음         **0.8046 -> 0.9170**, 신탁 없이. 붕괴 사라짐
13.84.54  α 와 **겹친다**                  보정 걸면 최적 α 가 1 로 복귀. α 손잡이 불필요
13.84.55  재현 · 사정거리                  네 판 재현. **유령 파일은 원리적으로 못 연다**
13.84.56  ★ 표류에 **규칙이 있다**         58%가 [ΔP·Δ|V₁|·ΔV₃·ΔV₅·ΔV₇] 로 설명. PC1=ΔP·PC2=ImV₃·PC3=Δ|V₁|
13.84.57  ★★ 그 규칙은 **회로가 계산한다**  실측 PC1~3 이 해석 감도 span 안에 주각 **0°/3°/13°**
          (영가설 p<0.0005)             그런데 채점은 **PCA 기저를 못 이긴다** — 대체하지 않는다
13.84.58  ★ 유령 — **안 쓰는 축**을 찾음   잔차 정렬도 cos, 파일 안 AUC 0.71~0.77. 모델은 불일치를 안 잰다
13.84.59  그 축도 **유령은 못 연다**       자리 D 에서 **진짜** 미니PC 도 지문과 72~75° 다
13.84.60  ★★ ⓐ 고정 사영 **구현·기각**      표류 PC1 이 판별축과 24~25°. 배분 산포 4.11->7.87W
13.84.61  ★★ 증강을 **실측 표류로**          모수 9개 -> 3차원 기저에서 뽑아 더한다. 972758/972759 가 시험 중
13.84.62  13.84.59 **정정**                  정렬도 항은 과적합이 아니다. 여섯 판에서 test_2 가 6/6 로 0.990
13.84.63  ★★ 워커가 노는 이유는 **소비자 1개**  `crf_nll` 의 T 루프가 배치와 무관하게 30ms. CPU 로 옮김
13.84.64  ★★ 대기는 **있다/없다**만 말한다     프로젝터와 미니PC 의 대기 페이저가 크기비 1.02 · **3°**. 갈림은 원리적으로 없다
13.84.65  ★★★ 표류의 PC2·PC3 는 **계통**이다    원시 파형에 계통이 둘. 계통차의 75~81% 가 표류 부분공간 안. PC3 는 **형제 전용**
13.84.66  ★★★★ 회로가 표류를 **예측한다**       자유 파라미터 0개로 R² +0.66(미니PC)/+0.65/+0.42. 셋째 축은 **고차 전압 응답**이었다 (h15 절단이 원인)
13.84.67  ★★★ 저항성 부하를 **처음 채점**했다   켜짐은 0.982 로 쉬운데 **신원이 0.741** 이다. 드라이기가 포트로 읽힌다
13.84.68  ★★★★ 상태 분류가 **정확해질수록 전력이 틀린다**  혼합이 **빈 슬롯**을 가리킨다. 드라이기 약풍 466W -> 3W (1단계 -> 2단계)
13.84.69  ★★ 2x2 채점 — **실측 표류는 손 맞춤을 못 이긴다**  가림은 **유지**가 낫다(창별 +0.025, 산포의 7배). 기준선 대비는 교락돼 못 읽는다
13.84.70  ★★★ 빈 슬롯 고침 — **예방은 되고 치료는 안 된다**  초기화가 예방, `state_power` 가 유지. 죽은 슬롯은 σ(−30)=9e-14 라 못 살린다
13.84.71  ★★ 주 루프 고침 **HPC 실이득 +18.3%**   같은 노드에서 378초 -> 319초/epoch. 이득은 GPU 가 아니라 load 37.4 -> 48.4 에서 왔다
13.84.72  ★★★ 빈 슬롯 고침 **채점** — 맞바꿈  상태별 전력 11.9% -> 2.5%, 실측 창별 0.8466 -> 0.8197. ⚠ 빈 슬롯은 cnn_v37 에서 **물려받은** 것이었다
13.84.73  ★★★ 오차 예산 한 장 — **미니PC 하나**  그리고 **충전기 전력을 넷 다 −18%** 로 읽는다. ⚠ 13.84.72 의 전력 수치를 정정했다
13.84.74  ★★★★★ **F1 만 올랐고 전력은 무너졌다**  2단계가 큰 부하 잔차를 59 -> 82~105W 로. scratch 탓 아니다. **잔차를 채점에 넣었다**
```

---

## 2. 오늘의 핵심 — 3차원 자기보정

**무엇을 고쳤나.** 13.84.46 의 EM 은 차수마다 복소 이득 = **자유도 16** 이라 미니PC 를 빨아들여
붕괴했다. 13.84.52 가 표류를 **3차원**으로 재고 나니, 보정을 그 안으로 묶는 것이 정확한 정칙화였다.

```
모형   obs ≈ noise + Σ_{j≠미니PC} P̂_j·sig_j + Vᵀa      (a ∈ R^k, 파일당 하나)
E단계  보정 뒤 잔차를 미니PC 지문에 사영 -> 와트 -> 방출로 갈아 끼우고 Viterbi
M단계  미니PC 가 **OFF 라고 풀린** 단계에서만 a 를 최소제곱
기저   results/drift_basis.npz — **형제 녹화로만** 만든다 (미니PC 넣으면 자기 신호를 지운다)
```

```
체크포인트        test_1          test_2          test_3      test_4
seq_h38_base   .834 -> .940    .657 -> 1.000   .872(막힘)  .856(막힘)   평균 .8046 -> **.9170**
seq_h38_bg     .826 -> .965    .657 -> .925
seq_h38_pow    .826 -> .888    .511 -> .932                              .7555 -> .8765
seq_h38_both   .826 -> .984    .657 -> .910
견줌 자유도 16  .834 -> .891    .657 -> .496                              .8046 -> .7788 (붕괴)
견줌 신탁 천장 (13.84.35⑥)  0.747 -> 0.937
```

**사정거리 — 여기가 중요하다.**
```
실패 ③ (형제 ON 에서 미탐 · test_1·test_2)   **연다**   +0.11~+0.34, 네 판 재현
실패 ② (유령 · test_3·test_4)                못 연다   앵커가 **원리적으로** 없다
```
보정이 서려면 "미니PC 가 꺼진 구간" 이 필요한데 유령의 정의가 모델이 그 구간을 못 알아보는 것이다.
관문을 열려고 `--anchor soft`(사후확률 가중) · `--anchor quant`(하위 25%) 를 시도했고 **둘 다
유령 파일을 더 망가뜨렸다** (test_4 .856->.787 · test_3 .872->.739). **딱딱한 관문이 보호 장치다.**

⚠ **남은 의심 하나.** 보정 크기 |a| 가 111~140mA 로 단독 녹화에서 잰 표류 RMS 28.9mA 보다 크다.
"표류 보정" 이라는 이름보다 넓은 것을 흡수하고 있다. 다만 13.84.54 에서 **보정을 걸면 최적 α 가
정확히 1 로 복귀**한 것이 "미니PC 방출 자체를 고친다" 쪽의 증거다 (아무 오차나 흡수하면 그럴 이유가 없다).

---

## 3. 진단이 바뀐 곳 — 앞 문서를 읽을 때 주의

```
13.84.31 ④  "요구 5.5%"        -> **2.7%** 다 (13.84.51). 미니PC 대기 30.8mA 가 늘 흐른다
13.84.31 ③  "편차가 판별 방향에 60~75%"  -> **전체 편차** 기준이다. 주성분으로 가르면
                                          PC1(분산 절반)은 미니PC 와 89° 직교다 (13.84.52)
13.84.41     "방출이 25배 약하다"  -> 방향이 반대다. **틀린 쪽을 가리키는데 세다** (13.84.44)
13.84.48     "정보는 충분하다"     -> **격리 녹화에서만** 참이다 (13.84.50)
```

---

## 4. HPC 상태 (2026-09-12 17:00 기준)

```
972758  nilm_v36dp  **R**  n071(gpu3)  48코어  v36dp = V36 + smps_driftp  가림 A/B  <- 13.84.61
972759  nilm_v36p   **R**  n055(gpu2)  48코어  v36p  = V36 + smps_dev2    가림 A/B  <- 13.84.61
972281  nilm_sbpr   COMPLETED 1:40:13  v36r(standby_per_record) A/B. **채점 끝 — 도움 안 됨** (§6-1)
972468  nilm_phase  FAILED(8초)  관문 버그. `seq_phase.sbatch` 는 고쳤으나 **`seq_gen.sbatch` 로 대체됐다**
970310  nilm_harm38 TIMEOUT  11/12 epoch 저장. 13.84.38 의 자료
972350  nilm_pairsyn COMPLETED  13.84.43 · 972386 nilm_balsyn COMPLETED  13.84.44
```

**자원 모양 — 앞 판(972281)의 기록을 정정한다.**
```
972281 (옛)  -c 38 · 판당 워커 18 · 판당 4,180창/초 · 413초/epoch · GPU 32~43% · VRAM 5% · load 36/38
972758/9(새) -c 48 · 판당 워커 23 · 캐시 7분 · **2분 안에 잡힘** · -t 02:30 · -p gpu3,gpu2,gpu4,gpu6,gpu1
```
⚠ **"gpu6·gpu1 의 QOS 상한 40" 은 없는 제약이었다.** 실제는 `node10_cpu640_gpu12` =
사용자당 **640코어·12GPU·10노드**이고 파티션별 코어 상한 항목은 비어 있다. 한계는 **노드 크기**뿐
(gpu1·gpu6 48 · gpu2·gpu3·gpu4 56 · gpu5 64). `-c 38` 로 묶여 있던 것이 그 오기 때문이다.

**코어를 최대로 잡는 법**: 상한이 아니라 **지금 비는 자리**를 세라. `HPC_RULES.md` §2 에
`sinfo`+`awk` 한 줄을 넣었다 (HPC 에서 실행 확인). 2026-09-12 16:40 실황은
`>=52 4노드 · >=48 9노드 · >=44 9노드 · >=40 16노드 · >=56 0노드` 였고, 48 과 44 의 후보가
같으므로 **48 이 공짜**였다. 그리고 한 노드에 두 판을 얹는 대신 **작업을 나누면 코어가 곱절**이다
(여기서는 48x2 = 96코어). CPU 가 병목이라 그것이 유일한 증속 수단이다.

`HPC_RULES.md` 갱신: §2 에 `CpusPerTask`(취소 없이 코어 줄이기) + **코어 상한 정정** ·
§8 에 CRLF/md5 함정 · §10 신설(접속 진단).

---

## 5. 새 도구

| 도구 | 무엇을 |
|---|---|
| `src/run_build_drift.py` | 표류 부분공간을 굳힌다 -> `results/drift_basis.npz` |
| `src/run_diag_selfcal2.py` | **3차원 자기보정 EM** (오늘의 핵심). `--k` `--anchor` |
| `src/run_diag_driftdim.py` | 표류의 차원·사영 SNR·전이성 (`DD_BLOCK` 환경변수) |
| `src/run_diag_sepfloor.py` | 전력 맞춘 LORO 판별 (격리 녹화) |
| `src/run_diag_phasereal.py` | 복합 실측 전이 Δ 의 상대위상 |
| `src/run_diag_phasewhy.py` | 왜 복합에서 안 서나 — 동작점·회전·템플릿 |
| `src/run_diag_chainprop.py` | 사슬 오류 전파·주변확률 복호·되돌릴 힘 |
| `src/run_diag_balance.py` / `run_diag_balsyn.py` | 방출 배율 α 훑기 (실측 / 합성) |
| `src/run_diag_joint.py` | 형제 결합 복호에 자리가 있나 |
| `src/run_diag_combine.py` | α 와 보정이 겹치나 |
| `src/run_diag_pair.py` / `run_diag_pairsyn.py` | 전이 짝 제약 (실측 / 합성) |
| `src/run_diag_bghead.py` | bg-head 가 무엇을 배웠나 |
| `src/run_gate_phase.py` | 형제 위상 증강 폭이 실측과 맞나 (관문) |
| `src/run_gate_sbpr.py` | `standby_per_record` 항등·효과 관문 |
| `src/run_diag_waveform.py` | 파형 특징의 안정성·분리도 (13.84.47, 결론은 음성) |

생성기 변경: `genopts` 에 `V36R`(standby_per_record) · `V36P`(smps_dev2) ·
**`V36D`/`V36DP`/`V36D47`**(실측 표류, 13.84.61), `augmentor` 에 `smps_dev2` ·
**`smps_drift`/`smps_driftp`/`smps_drift47`** + `_drift_dirs`, `segment_pool` 에
`standby_per_record`·`new_record()`.
학습기: `--keep-last N` · **`--no-crf-cpu`**(13.84.63, 기본은 CPU) · `--drift-proj`(**켜지 마라**).

**저녁에 더한 도구**
```
src/run_diag_driftlaw.py     표류를 물리 공변량에 회귀 — 규칙이 있나 (13.84.56)
src/run_diag_driftcirc.py    그 규칙을 회로에서 **계산** — 주각·관문·보폭 (13.84.57)
src/run_diag_ghost.py        유령 잔차의 크기·전력·**정렬도** (13.84.58)
src/run_diag_ghostgate.py    정렬도를 방출에 넣고 채점 (13.84.59·62)
src/run_gate_drift.py        고정 사영 관문 — 항등·동작·비용·기울기 (13.84.60)
src/run_gate_driftaug.py     증강 관문 `--check <프리셋>` 종료코드 판정 (13.84.61)
src/run_gate_mainloop.py     주 루프 관문 — CRF 항등 + 속도 A/B (13.84.63)
src/run_bench_batch.py       주 루프 창/초 천장을 배치 크기로 훑는다 (13.84.63)
patches/seq_gen.sbatch       2x2 요인설계. `GEN=v36p|v36dp` 만 바꿔 두 번 낸다
patches/gpu_probe.sh         도는 작업의 GPU 점유 (⚠ 예열·표본 간격이 있어 6분 걸린다)
patches/cpuprobe.sh          도는 작업이 **실제로 쓰는 코어 수** (uptime 의 load 는 노드 전체다)
results/drift_basis_circ.npz 해석 기저 — **견줌용. 쓰지 마라** (13.84.57 에서 붕괴)
src/run_diag_affine.py      지문이 원점을 지나나 — 조건수·한계기울기·LORO·NNLS (13.84.64)
src/run_diag_presence.py    전부-OFF 기준선으로 **있다/없다** 를 묻는다 (13.84.64)
src/run_diag_plugged.py     plugged 머리 — 수준·흔들림·상관·**무리 합** (13.84.64)
src/run_diag_rawgrid.py     원시 파형 재고·계통 짝·표류 부분공간·성분 분해 (13.84.65)
src/run_diag_driftcirc12.py v12g 야코비 — 보폭·절대·계급·주각·설명력·**계통짝** (13.84.66)
src/run_diag_driftpred.py   **예측해서 빼기** — 블록마다 실측 전압으로 푼 표류 (13.84.66)
src/run_diag_states.py      격리 녹화에 모델을 돌려 **상태별 전력 슬롯**을 편다 (13.84.68)
src/run_gate_states.py      빈 슬롯 고침 관문 — 항등(체크포인트/손실)·동작(초기화/기울기) (13.84.70)
src/run_score_factorial.py  2x2 요인설계를 짝지어 채점 — 주효과·상호작용·epoch 산포
```

---

## 6. 다음에 할 일 (2026-09-12 저녁 갱신)

1. ~~972281 을 받아 채점한다~~ **했다. `standby_per_record` 는 도움이 안 됐다.**
   ```
                     실측 사슬   창별     3차원 보정 뒤
   seq_h38_base      0.9493   0.8482   0.8046 -> 0.9170
   seq_sbpr_base     0.9452   0.8324   0.7766 -> 0.8387
   seq_sbpr_bg       0.9489   0.8326   0.7738 -> 0.8845
   ```
   보정과 **겹치지 않고 그 아래로 깎는다.** 게다가 EM 을 불안정하게 만든다 — `seq_h38_base` 는
   단조 수렴(.834 .875 .902 .940 .940)인데 `seq_sbpr_base` 는 test_1 에서 0.834->**0.992**->
   0.926->0.888->0.880 으로 되돌아간다 (|a| 가 90->146mA 로 계속 커진다). 얻은 것 하나:
   test_4 의 보정 관문이 열렸다 (OFF단계 13 -> 88). `sbpr_bg` 는 거기서 0.787->0.856 회복.
   ⇒ **v36r(standby_per_record)은 접는다.** ep9~12 가 남아 있으니 짝지은 채점은 가능하다.
2. ~~실패 ② (유령) — Δ 의 크기·전력 축~~ **팠다. 13.84.58~59.** 결론:
   - 새 축을 찾았다 — 잔차 **정렬도** cos. 모델은 미니PC 와의 일치도만 재고 **불일치는 안 잰다**.
     파일 안 AUC 0.714(test_3) · 0.766(test_4). 자: `run_diag_ghost.py` · `run_diag_ghostgate.py`
   - **그런데 유령은 안 열린다.** (q,lam) 격자 어디서도 test_3·test_4 가 기준선을 못 넘는다.
   - **이유를 찾았다**: 자리 D 에서는 **진짜** 미니PC 가 켜져도 잔차가 지문과 **72~75°** 고
     사영이 8~14W 기기에서 **2.2W** 밖에 못 건진다. 축이 아니라 **지문이 안 옮겨간 것**이다.
   - ⇒ **지금 가장 값싼 다음 수는 녹화다.** 필요한 것은 "자리 D 복합 녹화" 가 아니라
     **자리 D 에서 형제를 고정한 채 미니PC 만 토글하는 몇 분**이다. 지금 자료에는 그 짝이
     test_2·3·4 어디에도 **없어서** 위 72~75° 를 모델 없이 확정하지 못한다 (13.84.59 끝).
3. ~~보정을 학습 안으로 — 고정 사영 (13.84.52 ⓐ)~~ **했다. 기각. 13.84.60.**
   구현했고 (`--drift-proj`, `model/losses.py`) 항등·동작 관문을 통과하는데 **값이 진다.**
   배분 모의(실측 표류 + 2mA 잡음, NNLS 4,000회)에서 미니PC 오차 산포가 **4.11 -> 7.87W**.
   이유: **표류 PC1 이 미니PC-형제 판별축과 24~25°** 다 — 표류하는 방향이 곧 판별 방향이라
   버리면 둘 다 간다. "미니PC 1.5% 비용" 은 미니PC 만 본 것이고, 실제로는 k=1 에서
   프로젝터 −24%·충전기 −20% 다. `--drift-proj` 를 **켜지 마라** (기본 꺼짐).
   ⇒ **추정해서 빼기 ≠ 사영해서 버리기.** 13.84.53 이 서는 이유가 여기서 선명해진다.
4. ⚠ **미니PC 대기가 ON 의 53%** 라는 사실이 손실·라벨·사슬 어디에도 안 쓰이고 있다 (13.84.51).
5. ~~정렬도 항 재현~~ **했다. 13.84.62.** 체크포인트 **여섯 판**에서:
   `test_2` 가 6/6 로 0.657 -> **0.990** (출발이 0.511 인 판도 같은 곳에 닿는다),
   `test_3` 이 6/6 로 **망가지고**, `test_4` 는 6/6 로 안 움직인다.
   ⇒ **과적합이 아니다** — 과적합인 것은 "평균 0.9025" 라는 머릿수뿐이다 (인용하지 마라).
   효과는 재현되는데 **부호가 파일 종류로 갈린다.** 쓰려면 13.84.53 의 OFF-앵커 관문을
   반드시 달아라 (그 관문이 정확히 둘을 가른다). 그래도 기본 절차는 3차원 보정이다 —
   다만 `seq_sbpr_*` 처럼 **보정이 발산하는 판에서는 정렬도가 낫다** (0.877 대 0.990).
6. ~~972758 · 972759 를 받아 채점한다~~ **했다. 13.84.69.** v36dp 기각 · 가림 유지.
   **다음 판은 `--init cnn_v37 --keep-on 0` 으로 v36 대 v36p 를 돌려라** — 지금 넷은
   `scratch`/`keep_on 1` 이라 기준선과 못 견준다. 생성기만 바꾼 판이 아직 없다.
   그리고 유령↔미탐 맞바꿈이 네 칸 모두에서 같은 방향이다 (test_3·4 오르고 test_1·2 떨어짐) —
   진짜라면 **한 판에 담지 말고 나눠라**: 증강은 유령 쪽, 자기보정은 미탐 쪽이 이미 듣는다.
   아래는 옛 절차 (다음 판에도 그대로 쓴다).
   ```
   972758  nilm_v36dp  n071(gpu3)  48코어  v36dp = V36 + smps_driftp  가림 A/B
   972759  nilm_v36p   n055(gpu2)  48코어  v36p  = V36 + smps_dev2    가림 A/B
   ```
   칸 넷 = 생성기(**실측 표류** / 손 맞춤) × 가림(유지 / 해제). 견줌 `seq_h38_base` 0.9493/0.8482.
   태그: `seq_v36dp_mask` · `seq_v36dp_open` · `seq_v36p_mask` · `seq_v36p_open` (+ `_ep9~12`).
   받는 법: `scp gate1_External:/home1/zetin348/NILM_AI/results/seq_v36*.pt results/`
   ⚠ `gate1_Oracle` 은 **점프 호스트**다 (nologin 이 정상). HPC 는 `gate1_External` 이다.
7. **★★ 잔차를 손실에 넣는다 (`--w-cons`).** 13.84.74 — **지금 1순위**.
   `run_train_seq` 가 `LossWeights(harm=.., cons=**0.0**, over=.., z=..)` 로 박아 놨다
   (`src/run_train_seq.py:322`). 3.3절은 2단계에서 `cons` 를 0.4 로 올리라고 적었는데
   시퀀스 학습기는 그 손잡이를 **안 내놓는다**. 재구성을 벌한 적이 한 번도 없고,
   그 사이 큰 부하 잔차가 59 -> 105W 가 됐다.
   순서: (a) `--w-cons` 를 인수로 내고 기본 0 (항등 관문), (b) 0.1/0.4 두 팔로 한 판,
   (c) **잔차와 on/off 를 같이** 본다 — 어느 쪽도 혼자 보면 안 된다 (13.84.74 [4]).
   ⚠ 13.84.74 [1] 이 안 가른 것: CRF 가중·keep_on·학습률 중 무엇이 망가뜨리는지.
   `cons` 로 되돌려지면 원인 규명 없이도 값을 얻는다 — 그래서 이것이 먼저다.
8. **표류 예측 보정을 `L_harm` 에 붙인다.** 13.84.66. (13.84.65 의
   "야코비를 v12g 로" 는 **끝났다** — 가설은 기각, 그러나 더 좋은 것이 나왔다.)
   회로가 그 블록의 **실측 전압**만 받아 표류를 R² +0.654(충전기) / +0.417(프로젝터) 로
   예측한다. **자유 파라미터 0개.** 전압 고조파는 외생이라 채점 시점에 바로 쓸 수 있다.
   ⚠ **예측해서 빼기**지 사영해서 버리기가 아니다 (13.84.60 이 가른 그 둘).
   붙일 자리는 `harm_offset` (13.84.38) 인데 거기는 창당 하나이고 이것은 **기기당**이라
   구조를 늘려야 한다. 순서:
   (a) **관문 먼저** — 예측 보정이 미니PC 를 얼마나 먹나 (`run_gate_drift.py` [3][4] 를 본떠라)
   (b) **복합 5파일에서 재현** — 지금 표는 격리 녹화다. 실측 파일에도 전압이 있다
       ⚠ 다만 복합 5파일은 **옛 펌웨어(79열)** 라 전압 꼬리가 없다 (아래)
9. **전압 꼬리(h17~31)를 파이프라인에 잇는다.** 13.84.66 [7].
   새 펌웨어(96열)가 `vhhi17~31` 을 60초에 한 벌 준다. **npz 가 그것을 버린다**
   (`voltage_harmonics_complex` 가 전부 (N,15) 다). 넣으면:
   · 계통 짝 예측 각도 **14~24° -> 5~10°** (원시 파형에서 h23 까지만 가져와도 대부분)
   · 충전기 표류 예측 R² 0.794 -> **0.835** (꼬리 있는 칸)
   ⚠ **정렬**: 펌웨어는 60초를 모아 **끝에** 낸다 — 블록의 꼬리는 *다음* seq 값이다.
     그대로 붙이면 0.759 로 **없느니만 못하다**.
   ⚠ **자료 구멍**: 꼬리 가진 파일이 `laptop_charger_3~6` · `minipc_4` 뿐이다.
     프로젝터는 0개, 미니PC 는 8~14W 에 1,774 사이클(문턱 1,800 에 26 모자람),
     복합 5파일은 옛 펌웨어다. **다음 녹화 요청**: 새 펌웨어로 미니PC 대역별 3분 이상 ·
     프로젝터 · **복합 한 판**.
   딸린 것: `raw_laptop_charger_9` 는 **1.0W 대기 파형**이다 — 13.84.64 의 "프로젝터·미니PC
   대기 3°" 가 필연인지 우연인지 회로가 답할 수 있다 (아직 안 했다).
   ⚠ `circ12_*.pkl` 은 numpy 2.x 피클이다 — `run_diag_driftcirc12.load12` 의 shim 을 써라.
   ⚠ `--npc 3072`. ⚠ 중심차분 보폭은 dV <= 0.3V.
10. ~~빈 상태 슬롯을 고친다~~ **고쳤다. 13.84.70.** 관문 `src/run_gate_states.py` **합격**.
   `NILMNet(state_power_init=True)`(기본) 이 슬롯을 잰 값에서 출발시키고,
   `run_train_seq --w-state-power W`(기본 0) 가 혼합이 안 고르는 슬롯을 붙잡는다.
   ⚠ **예방이지 치료가 아니다** — 이미 죽은 슬롯은 `σ(−30) ≈ 9e-14` 라 기울기가
   2e-18 이고 **학습으로 못 되돌린다.** 그래서 **다음 판은 `--init scratch` 여야 한다.**
   ⚠⚠ 이것이 §6-6 의 "다음 판은 `--init cnn_v37` 로 교락을 없애라" 와 **충돌한다.**
   물려받으면 드라이기가 안 고쳐진다. 둘 중 하나를 골라라:
   **(가)** scratch 로 가고 기준선도 scratch 로 다시 구워 견준다 — 교락 제거 + 고침 둘 다
   **(나)** 물려받고, **자료로** 슬롯 출력을 재서 세우는 절차를 먼저 만든다 (미착수).
       ⚠ 바이어스 크기로 판정하면 **안 된다** — `cnn_v37` 에 걸면 잘 도는 슬롯 16개가
       걸린다 (13.84.70 [4]). 값을 내는 것은 가중치다.
   남은 일: **값이 좋아지는지는 아직 안 쟀다.** 12.35 가 `state_power` 로 유령을
   42.1 -> 86.9W 로 키웠다고 쟀다 (옛 판). `run_diag_states.py` + `run_score_seq.py` 로 재라.
11. **채점에 전력을 넣는다.** 13.84.68 [7] — 위와 한 묶음이다.
    `run_score_seq` 는 **on/off 만** 낸다. 실측 파일에 기기별 참 전력이 없어서 전력 오차를
    잰 적이 **한 번도 없다.** 그 사이 1단계->2단계에서 전력이 위처럼 무너졌는데 지표가
    안 봤다. 최소한 **격리 녹화의 상태별 전력**을 관문으로 걸어라 (`run_diag_states.py`
    가 그 표를 낸다 — 참 상태를 아는 유일한 자료다).
    딸린 것: `build_loss` 가 `res_ohm`/`power_ref` 를 **안 넘긴다** (2단계 `run_adapt` 만
    쓴다). 포트가 919W 로 도는 답을 막을 항이 1·2단계에 없다.
12. **`plugged` 를 SMPS **무리 하나**로 묶는 관문을 세운다.** 13.84.64.
   프로젝터·미니PC 의 대기가 3° 라 개별 `plugged` 는 **자료가 못 정한다**. 그런데 손실은
   그 방향을 자유로 둔다 — 미니PC 대기를 지우는 방향이 **미니PC 켜기의 0.36배**,
   형제끼리 맞바꾸는 방향이 0.13배다 (표류 바닥은 0.10~0.23배).
   ⚠ **사영해 버리지 마라** — 13.84.60 이 같은 모양으로 실패했다 (추정해서 빼기 != 사영해서 버리기).
   관문부터: 묶으면 미니PC 를 얼마나 잃나. `src/run_gate_drift.py` 의 [3][4] 를 본떠라.
13. ~~주 루프 실이득을 HPC 에서 잰다~~ **쟀다. 13.84.71.** 같은 노드 n071 에서
   **378초 -> 319초/epoch · 처리량 +18.3%.** 이득은 GPU(34~42 -> 42~47%)가 아니라
   **load(37.4 -> 48.4/48코어)** 에서 왔다 — 주 프로세스 목을 넓히니 워커가 다 돌았다.
   ⚠ `--no-crf-cpu` 로 되돌릴 이유는 **없다.** 13.84.63 의 경고("GPU 가 포화되면 다시
   재라")는 GPU 47% 인 지금 체제에서는 안 걸린다.
   ⇒ **다음 속도 이득은 GPU 가 아니라 워커 공급에서 온다** (창 만들기 · `build_inputs`).
   지금 load 가 48/48 로 포화다 — 코어를 더 받거나 창 만들기를 싸게 해야 움직인다.

---

## 7. 오늘 저녁에 더 판 것 — 표류의 해석적 정체 (13.84.56~57)

사용자: *"회로모델이 있고 표류라고는 하지만 회로가 달라지는 건 아닐 테고, 3차원이라니 규칙이
있다는 뜻이다."* — **맞았다.**

```
표류 = 동작점의 함수      ΔI = (∂I/∂P)·ΔP − Y·ΔV      자유 파라미터 **0개**
실측 PC1..PC3 이 해석 span 안에   충전기 0°/3°/13°  ·  프로젝터 1°/11°/15°
영가설(무작위 8차원, 2000회)       중앙값 29°/45°/61°  ·  관측보다 작을 확률 0.0000
**왜 3차원인가**                  손잡이 8개의 응답이 실효 3차원 (누적 0.999, 다음이 10배 절벽)
```

**그러나 채점은 대체를 거부했다** — 회로 기저 k=2 → 0.8710, k=3 → 0.7885(붕괴) 대 PCA 0.9170.
어긋나는 것은 **셋째 축 하나**(주각 81°)이고 그것이 미니PC 와 46° 라 신호를 먹는다.
셋째 축 후보는 미결이다 (`alpha` = 충전 테이퍼가 유일한 생존 후보지만 시간 상관이 안 선다).

**자 두 개 — 다음 사람이 같은 데서 넘어진다 (13.84.57 끝).**
1. **유한차분 보폭.** 2%(=4.3V)로 야코비를 잡으면 크기가 **2배**, 방향이 **16°** 틀린다.
   보폭을 훑어 고원을 찾아라 (여기선 0.3V 아래). 이 실수가 "선형화가 깨진다" 는 **가짜 물리
   결론**을 한 번 만들었다.
2. **span 증폭.** 이미 있는 span 과 7° 인 방향을 직교화·정규화하면 남은 9% 짜리 잔차가
   무엇이든 설명하는 것처럼 보인다. 반드시 (ⓐ) 잔차 크기를 **모형 자신의 정확도**와 견주고
   (ⓑ) 칸마다 따로 구한 잔차가 **같은 방향인지** 봐라. 이 검사가 L 을 떨어뜨리고 alpha 를 남겼다.

**새 도구 (5)**
```
src/run_diag_driftlaw.py    표류를 물리 공변량에 회귀 — 규칙이 있나
src/run_diag_driftcirc.py   그 규칙을 회로에서 **계산** — 주각·관문·보폭
src/run_diag_ghost.py       유령의 잔차 크기·전력·정렬도 (AUC)
src/run_diag_ghostgate.py   정렬도를 방출에 넣고 채점 (--calib 로 보정과 함께)
src/run_gate_drift.py       고정 표류 사영 관문 (항등·동작·비용·기울기). 13.84.60 의 자
model/losses.py --drift-proj  (2H,2H) 고정 사영. **기본 꺼짐. 켜지 마라**
results/drift_basis_circ.npz  해석 기저 — **견줌용. 쓰지 마라**
```

---

## 8. 저녁에 더 한 것 — ⓐ 기각, 증강 교체, 코어 상한 정정

**ⓐ 고정 사영은 구현했고 기각했다 (13.84.60).** `--drift-proj` (기본 꺼짐, **켜지 마라**).
항등·동작 관문은 통과하는데 배분 모의에서 미니PC 오차 산포가 **4.11 -> 7.87W** 다.
대조가 모의를 검증한다 — PC1 위에만 표류를 넣으면 2.654W -> **0.000W** 로 정확히 지우지만,
표류를 빼고 잡음 2mA 만 넣으면 1.317W -> **4.908W** 다 (한 차원 버린 값이 3.7배).
이유: **표류 PC1 이 미니PC-형제 판별축과 24~25°** — 표류하는 방향이 곧 판별하는 방향이다.
⇒ **추정해서 빼기 ≠ 사영해서 버리기.** 13.84.53 이 서는 이유가 여기서 선명해진다.

**증강을 모수에서 실측 표류로 바꿨다 (13.84.61).** 지울 수 없으면 가르쳐야 한다.
```
프리셋         폭(실측=1.00)   표류 부분공간 밖   으뜸축-미니PC    판정
smps_dev1      4.8~13.0배        52.4%           70°        불합격
smps_dev2      0.61~1.11         23.5%           58°        합격  (손 맞춤 = c_max 0.9)
smps_driftp    0.89~0.94       **3.3%**        **85°**      합격  (기저에서 뽑아 더함)
(실측 표류)       1.00             0.0%            89°
```
`scramble_from` 은 13.84.49 가 이미 고쳤는데 **적용이 안 돼 있었다** (972468 이 관문 버그로 죽음).
이번에 걸었다. 관문 `src/run_gate_driftaug.py --check <프리셋>` 이 종료코드로 판정한다.
⚠ 합격 기준은 `[[augmentation-can-erase-the-discriminant]]` 의 3σ 규칙이 **아니다** —
실측 표류 자체가 h15 에서 미니PC 기여의 0.74배라 정직한 증강은 그 규칙을 만족할 수 없다.
기준은 **실측 대비 폭(0.5~1.5배)과 부분공간 모양(밖에 35% 미만)** 이다.

**⚠ HPC 코어 상한 기록이 틀렸었다.** "gpu6·gpu1 의 QOS 상한 40" 은 **없는 제약**이다.
실제는 `node10_cpu640_gpu12` = 사용자당 640코어·12GPU·10노드이고, 한계는 **노드 크기**뿐이다
(gpu1·gpu6 48 · gpu2·gpu3·gpu4 56 · gpu5 64). `-c 38` 로 묶여 있던 것이 그 오기 때문이었다.
`HPC_RULES.md` §2 에 정정과 "지금 비는 자리를 세는" `sinfo`+`awk` 한 줄을 넣었다 (HPC 에서 확인함).
이번엔 **-c 48 짜리 두 작업 = 96코어**를 2분 안에 잡았다 (판당 워커 23, 캐시 7분).

**새 도구·바뀐 코드**
```
src/run_gate_drift.py        고정 사영 관문 (항등·동작·비용·기울기) — 13.84.60 의 자
src/run_gate_driftaug.py     증강 관문 (--check 로 합격/불합격) — 13.84.61 의 자
src/synthesis/augmentor.py   smps_drift / smps_driftp / smps_drift47 + _drift_dirs
src/synthesis/genopts.py     v36d · v36dp · v36d47
src/run_build_drift.py       comp_sd (성분 표준편차) 를 npz 에 같이 저장
src/model/losses.py          drift_proj 버퍼 + _harm_err 사영 (**기본 꺼짐**)
src/model/lossbuild.py       build_drift_proj(path, k, n_ord)
src/run_train_seq.py         --drift-proj / --drift-k (**켜지 마라**, 도움말에 판정 적힘)
patches/seq_gen.sbatch       2x2 요인설계. GEN 만 바꿔 두 번 낸다
patches/gpu_probe.sh         도는 작업의 GPU·CPU 점유 측정 (§5 절차)
```
