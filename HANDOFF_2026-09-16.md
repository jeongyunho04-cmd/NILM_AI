# 인계 2026-09-16 — **병이 하나가 아니라 셋이었다**

어제(`HANDOFF_2026-09-15c.md`)는 "오븐 통전 헛detect" 하나를 쫓았다. 오늘 그것을
**서로 다른 기전 셋**으로 갈랐다. 처방도 셋이 다르다.

```
  읽는 순서
   1. ⚠ 오늘 내가 틀린 것 (아래)
   2. ★★★ 세 병 (§1)
   3. 자의 구멍 — `present` 사각지대 (§2)
   4. 새로 지은 자 일곱 (§3)
   5. 도는 것과 판정표 (§4)
   6. 어제 것은 `HANDOFF_2026-09-15c.md` — 게이트 줄기는 **닫혔다**
```

## ⚠ 오늘 내가 틀린 것

```
  · 전이 근처 헛detect 를 "전이가 원인" 으로 읽었다 -> **개입으로 반증**
    (창에서 전이를 지워도 80~85% 가 그대로 선다. 위약과 구별 안 됨)
  · test_1 드라이 유령을 `--gate-free-power` 탓으로 돌릴 뻔했다
    -> 짝(`cnn_pcap`)에 **같은 시각 같은 스파이크**가 있다. 경로만 다르다
  · "세밀 −2~+0초가 답을 정한다" 라고 5토막으로 읽었다
    -> 3,600자리를 훑으니 **타깃 0.05초(3사이클)** 였다. 2초는 그걸 품고 있었을 뿐
  · "합은 맞고 배분만 틀렸다" 라고 빠르게 읽었다
    -> 컨덕턴스로 계산하니 **합도 137~190W 어긋난다.** 그게 유일한 판별 축이었다
  · ② 를 "SMPS 합이 관측보다 4배 흔들린다" 로 읽었다
    -> 안정 창만 제대로 고르니 **1.0~1.1배**. 합은 멀쩡하고 **배분이 떠돈다** (14.165)
  · 채점기 셋이 창을 **한 번만** 지었다 — `EVEN_MEDIAN` 은 모듈 전역인데
    -> `cnn_pcap`(k=0)과 `cnn_em5`(k=5)를 한 표에 놓자 한쪽이 **분포 밖**으로 갔다.
       `load_model` 의 경고가 잡았다. 안 잡혔으면 "em5 는 효과 없다" 로 갔다 (14.164)
  · 14.84 ⓑ(`g_need` 에서 `p_other` 빼기)를 그대로 쓸 뻔했다
    -> SMPS 93W 가 컨덕턴스로 0.00176 S 인데 오븐↔포트 간극이 0.00330 S 다. **53% 치우친다**
```

---

## §1 ★★★ 세 병

### ① 오븐↔포트 신원 축퇴 — **수백 W · 합 제약이 가른다**

```
  증상   오븐의 와트가 포트로, 또는 포트의 와트가 오븐으로 통째로 이사한다
         test_5  오븐(참ON) -> 포트   9초    방아쇠 핫플 on
         test_2  포트(참ON) -> 오븐  27초    방아쇠 드라이 on   (**오븐은 test_2 에 없다**)
  기전   신원이 입력에 **없다**. 되돌리기로 세 창에서 확인 —
         **기본파(I1)가 답을 정하고** 고조파 모양·전압·Q·짝수차는 **전부 무효**
  서명   두 안정점이 **정확히 두 컨덕턴스 답** (226V 에서 오븐 1,259W · 포트 1,428W)
         학습된 오븐 s2 슬롯이 **1,258W** 다 (S_STATE 의 1,357 이 아니라 **V²/R**)
  판별   관측 총합이 **포트 답에 1~6W · 오븐 답에 140~190W** 어긋난다 (8/8 창)
         치환 방향에 따라 **부호가 뒤집힌다** (test_5 +97W 과잉 · test_2 −174W 과소)
         -> `L_over`(과잉만)로는 못 잡는다. **양방향**이라야 한다
  ★ 스위치는 게이트가 아니라 **상태 머리**다
         test_2 에서 게이트는 212.5초에 이미 0.996 인데 **mix[s1]=1.000 (팬·조명 17W)** 이다.
         231.4초에 **mix 가 s1 -> s2(0.986)** 로 뒤집히며 1,256W 가 된다.
         게이트 작업(14.147~152)이 전부 `on_logit` 을 겨눴는데 방아쇠는 여기 있었다
  처방   **죽은구역 보존 손실** `relu(|Σp̂+Σŝ+p_noise − P관측| − δ)/max(P관측,10W)`, δ=100W
         -> `cnn_dzn` (985155)
```

### ② SMPS 표류 — **합은 멀쩡하고 배분이 떠돈다 · 처방 없음**

★ 이 절은 14.165 에 **통째로 고쳐 썼다.** 전에 적은 "관측 σ 4.6W · SMPS 합 sd 20.0W
= 4배" 와 "쌍 상관 −0.20~+0.26, 2자 축퇴 아님" 은 **창을 좁게 고른 탓**이었다.

자: `run_diag_smpsdrift.py` — 모든 전이에서 `--guard` 초(기본 8초) 밖이고 SMPS 가
참으로 켜진 **안정 창**만 본다 (test_4 · 216창 중 132창).

```
  증상   test_4 미니PC 게이트가 0.99 <-> 0.14 로 깜빡인다 (정확도는 82% 대 참 79% 로 멀쩡)

  [2] 합    관측 sd **16.7W** (평균 85.9W)
            SMPS 3종 합 sd  pcap 17.4 · 18.8 · 17.9W    = **1.0~1.1배**
                            em5  17.5 · 18.8 · 17.6W    = **1.0~1.1배**
            ⇒ **합은 멀쩡하다.** 자연의 폭 그대로다

  [4] 배분  빔<->충전     pcap −0.40 · −0.50 · −0.51    **3/3 음수**
                        em5  −0.41 · −0.50 · −0.59    **3/3 음수**
            충전<->미니PC  +0.38 ~ +0.49
            ⇒ 빔과 충전이 **와트를 주고받는다.** ① 과 같은 2자 축퇴 꼴이다

  [3] 기기별 빔 31±16W · 충전 40±16W · 미니PC 4~10±3~6W
            ⚠ 이 폭에는 **진짜 변동이 섞인다** — SMPS 지문은 점이 아니라 범위다.
              [3] 으로 판정하지 마라. 읽을 것은 [2](합 대 관측)와 [4](부호)다
```

**처방이 달라졌다.** "합이 시끄럽다" 면 합 제약(`L_cons`)이 길이었을 텐데, 합은
이미 맞다. 그러니 `cnn_dzn` 은 이 병을 **원리상 못 건드린다** (그리고 20W 규모가
δ=100W 죽은구역 **안**이기도 하다). 빔과 충전을 가르려면 **합이 아닌 다른 축**이
있어야 한다. 598자리 sweep 이 아무것도 못 찾았다는 것은 그 축이 **입력에 없다**는 뜻이다.

전체 결손의 주인도 여기다: 미니PC −0.60 · AC −0.46 · 선풍 −0.40 · 빔 −0.28
(저항 4종은 −0.01 ~ −0.21)

### ③ 스위칭 과도 오귀속 — **사전에 없는 항목 · 처방 있음**

```
  증상   test_1 에서 **드라이기 유령** 208~402W 가 7번 (test_1 에 드라이기는 **없다**)
  기전   릴레이가 반주기에서 끊기면 **그 한 사이클이 물리적으로 반파**다
         |I2| 4.9 -> **483mA** (h2/h1 0.0018 -> **0.27**, 구간 최대 **0.96**)
         사전에서 반파는 **드라이기 s1 (h2/h1 0.4326)** 하나뿐이라 그리로 간다
  ⚠ 두 판 다 걸린다. 경로만 다르다 —
         `cnn_pcap` 는 **게이트가 속고**(0.43~0.71) `cnn_gfp` 는 **상태 머리가 속는다**(게이트 0.002)
  ★ 되돌리기가 자리를 정확히 짚는다
         ch43/44 (이미 61주기 **평균**)  402 -> **398.6W**   <- 아무 일도 안 일어난다
         ch16~22 (**순시** 짝수차 크기)   402 -> **67.5W**    <- 모델이 읽는 곳은 여기다
  처방   `mag` 의 **짝수 행**에 **5탭 이동중앙값** (`--even-median 5`)
         |I2| 최대  과도 1176.7 -> **12.8mA** (92배) · 진짜 반파 891.5 -> 884.4 (**0.8% 손실**)
         순시값에서는 과도가 진짜보다 **커서** 분리가 안 된다. k=5 에서 **69배** 갈린다
         비용 0.2초/250창 (k=61 이면 3.9초 — 옛 주석의 "25분" 이 그것이다)
  ★ 추론에만 걸어 미리 잰 것 (`cnn_pcap` 3시드, 학습 안 함 = 분포 밖)
         드라이 유령 스파이크 **7개 -> 0개** · 절대 잔차 21.3 -> **18.5W** (씨앗마다 개선)
         판정줄 .8820 -> .8774 · SMPS .9807 -> .9693  <- 분포 밖 손해로 본다
  -> `cnn_em5` (985148), `cnn_dzn` 은 그 위에 얹었다
```

---

## §2 ⚠ 자의 구멍 — `present` 사각지대

```python
  # run_baseline_nilmtk.score_arm
  for k in np.nonzero(d["present"])[0]:     # <- 그 파일에 **없는** 기기는 한 칸도 안 센다
```

**그 파일에 안 꽂힌 기기의 유령이 판정 줄에 전혀 안 나타난다.** 실제로:

```
  test_2  오븐 유령 **46초 연속 · 1,254W**   (오븐 없음)
  test_1  드라이 유령 208~402W 7번          (드라이기 없음)
```

둘 다 그림 5번 칸에만 보이고 숫자에는 없었다. `score_arm` 은 **안 건드리고**(로그의 옛
숫자와 이어져 있다) 독립된 자 `run_diag_ghost.py` 를 세웠다 — 없는 기기는 참값이
**OFF 확정·0W 확정**이라 라벨 애매함이 0 인 가장 깨끗한 모집단이고 **5,699칸**이다.

첫 수확: `--w-harm 0.03`(`cnn_wh03`)이 **유령을 두 배**로 만든다 (0.96% -> 2.01%, 3/3,
범위 안 겹침). 판정 줄만 봤으면 못 읽었다. **wh03 기각.**

---

## §3 새로 지은 자

```
  run_diag_spike.py       **0단계.** 어느 기기가 언제 튀나 + 합 잔차 순위 (`present` 안 거른다)
  run_diag_ghost.py       유령 채점 (없는 기기). **판정에 상시로 넣어야 한다**
  run_diag_flipedge.py    헛ON 경계 찾기 + 원시 45채널 되돌리기 (양방향)
  run_diag_finewindow.py  세밀 10초의 시간 해상 Δ/σ + 구간별 되돌리기 + 그림
  run_diag_finesweep.py   창 60초를 폭 0.05~0.3초로 **전부 훑는다** + 채널x시간 지도
  run_diag_transient.py   Chen et al. 의 병(전이 과도 보상 스파이크)이 있나 -> **없다**
  run_diag_transedge.py   창 안의 전이를 지우는 개입 + 위약
  run_diag_deadzone.py    δ 를 재고 치환 비용과 나란히 (죽은구역 설계 검산)
  run_diag_gateconv.py    전력에 게이트를 어떻게 쓸까 (raw/hard/soft) -> **raw**
  run_gate_deadzone.py    죽은구역 관문 6줄
  run_gate_gfcheck.py     gate_free 분리 확인 5줄
```

### ★★ 경계 하나를 판정하는 절차 — **네 단계**

오늘 세 병을 전부 이 순서로 찾았다. 그대로 따라 하면 된다.

```
  [0] 어디가 튀나 — 기기·시각·크기를 먼저 잡는다
      python -X utf8 -m src.run_diag_spike --stem test_1 --t0 530 --t1 580 \
          --ckpt results/cnn_gfp_s0.pt --only-absent
      -> **없는 기기**를 맨 위로 올린다. 듀티 기기의 진짜 펄스에 목록이 덮이면 `--only-absent`

  [1] 경계를 찾는다 — 게이트가 0.5 를 넘는 자리와 **연속 구간**
      python -X utf8 -m src.run_diag_flipedge --stem test_2 --t0 130 --t1 290 \
          --ckpt results/cnn_pcap_s0.pt
      -> "헛ON 이 서 있는 구간" 이 진짜 크기다 (46.2초 · 1,254W 가 이렇게 나왔다)
      ⚠ 참값이 **안 바뀌는** 경계를 골라라. 기기가 실제로 켜지는 자리는 교란이다

  [2] 10초를 편다 — 시간 해상 Δ/σ · 5토막 되돌리기 · 그림
      python -X utf8 -m src.run_diag_finewindow --stem test_2 --at 231.4 --gap 2.5 \
          --app oven --also electiric_kettle --ckpt results/cnn_pcap_s0.pt
      ⚠ **5토막으로 판정하지 마라.** 오늘 "세밀 −2~+0초" 로 읽은 것이 실제로는
        **타깃 0.05초** 였다. 2초 토막이 그것을 품고 있었을 뿐이다

  [3] 전부 훑는다 — 여기서 판정한다
      python -X utf8 -m src.run_diag_finesweep --stem test_2 --at 231.4 --gap 2.5 \
          --width 0.3 --step 0.1 --app oven --also electiric_kettle \
          --ckpt results/cnn_pcap_s0.pt
      사이클 단위까지 가려면  --width 0.01667 --step 0.01667   (3,600자리)
```

**읽는 법** — `[A] 시간 sweep` 과 `[B] 채널x시간` 을 같이 본다.

```
  국소 자리가 나온다      -> **반응**. 그 자리·그 채널이 원인이다
  전 구간이 평평하다      -> **표류**. 입력에 근거가 없다 -> 바깥 제약이 유일한 길
  광역 −54~−4초가 무효   -> 오늘 네 칸 중 셋에서 그랬다. 광역 갈래는 이 결정에 기여 안 한다
  Q·실효전압·전압고조파   -> 오늘 **모든** 창에서 무효였다. 움직이는 것은 언제나 전류 크기다
```

**짝을 고르는 법** (여기서 제일 많이 틀린다)

```
  · `--gap` 이 크면 뒤 창이 **참값이 바뀐 구간**으로 넘어간다.
    오늘 `test_5 --at 81.8 --gap 2.5` 가 그래서 무효였다 (82.5초에 포트가 진짜 켜진다)
  · 두 창은 `τ ∈ (−gap, +gap)` 에서만 내용이 갈린다. sweep 의 민감도가 거기 묶인다
  · 1사이클 짝(`--gap 0.00833`)까지 갈 수 있다 — 3,599/3,600 을 공유하는 두 창이다
```

### ★ 방법론 — **반응과 표류를 구분하라**

같은 sweep 을 여러 경계에 걸었더니 **두 체제**가 나왔다.

```
  (가) 입력에 **큰 계단**이 있을 때
       test_2 231.4 포트 on    -> **타깃 3사이클(0.05초)** 이 1,252W 를 정한다
       test_2 259.7 드라이 off -> **+0.32~+0.62초**(미래) 를 되돌리면 975 -> **107W**
  (나) 입력이 **정상상태**일 때
       test_2 233.5 / 250.0 · test_4 미니PC -> **3,600자리 어디도 안 바꾼다**
```

(나)가 축퇴다. **경계에 특징이 없다** — 모델이 무언가에 반응하는 게 아니라 **칼날 위에서
표류**한다. 1사이클(16.7ms) 차이 두 창이 3,599/3,600 을 공유하는데 답이 **184W** 움직이고,
그 184W 를 되돌리는 사이클이 **없다**.

⇒ (가)면 입력/특징을 고치고, (나)면 **바깥 제약**(합·컨덕턴스)이 유일한 길이다.

---

## §4 도는 것 · 판정표

```
  985148  cnn_em5   v1 + `--even-median 5`                        짝 cnn_pcap
  985155  cnn_dzn   v1 + `--even-median 5` + 죽은구역 보존         짝 cnn_em5
                    `--w-cons 1.0 --cons-deadzone 100`
```

사다리로 한 표에 찍는다: `cnn_pcap -> cnn_em5 -> cnn_dzn`

```
  판정줄 7종 · 저항 4종 · SMPS 3종 · 절대 잔차 · 혼자켜짐 · **유령(게이트율·최장초)**
  + em5:  test_1 드라이 스파이크 개수 (7 -> 0 이어야 한다)
  + dzn:  ① **수백 W 치환** — test_2 오븐 유령 46초 · test_5 포트 유령 9초
          ② **수십 W 표류** — δ=100W 안이라 **안 움직이는 것이 정상**. 갈라서 읽어라
  ⚠ 붕괴 감시 (12.9) — "합만 맞추고 배분 포기" 가 나면 **혼자켜짐**과 **저항 4종**이 무너진다
```

### 안 던진, 등록된 손잡이

```
  --w-gate-cond   14.106. 게이트를 **통전**에 묶는다. ①의 **점화**(팬·조명 17W로 슬롯이
                  먼저 열리는 것)를 겨냥한다. 대가는 "팬·조명 창을 놓쳐 오븐 재현율 하락" 이고,
                  오늘 그 반대편(유령 1,256W x 27초)이 처음 숫자로 나왔다
  14.84 ⓑ         `L_swap` 의 `g_need` 오염 제거 — **그대로는 안 된다** (위 ⚠ 참조)
  --target-median 5 · 광역 lookahead 0 · --seg-pool 3   (여전히 안 던짐)
```

### 안 쓰는 것으로 확정

```
  게이트 결합 계열 (A · B=Masked MSE · 게이트경화 · 완전분해)  — 어제 6판으로 닫힘
  Chen et al.(TSG 2020) 의 과도 보상 스파이크                 — 우리 병이 아니다.
      전이에서 문이 **더 확신**하고(중간값 4.0% -> 1.3%), p̂ 폭증은 **+2%**,
      결손은 전이에서 **좋아진다**(−0.24 -> −0.14). 부호가 전부 반대다
  --w-harm 0.03                                              — 유령 2배, 3/3 기각
  게이트를 전력에 곱하기 (hard/soft)                            — raw 가 네 판 모두 최고
```

---

## §5 ★★★ 땜질 구조 다섯 (2026-09-16 오후)

사용자: *"코드에 땜질이 많아 … 땜질이 모여 문제가 된 곳을 찾아봐"*. 모양으로 찾았다.

```
  ① 재고 만들어 놓고 **한 경로에만** 달았다
  ② 같은 것을 짓는 자리가 여럿이고 **갈라졌다**
  ③ 모르는 칸을 **틀린 값으로 채웠다**
  ④ **같은 축**을 두 기구가 재고 곱한다
  ⑤ 땜질이 **서로를 무효화**했다   <- 가장 찾기 어렵고 가장 해롭다
```

| | 기구 | 근거 | 병 | 상태 |
|---|---|---|---|---|
| ① | `companion_sig` 12.156 | 오븐 팬·조명 14.6W | adapt 에만 배선 → 1단계 버퍼 전부 0 | 14.167 우회 |
| ② | `build_loss` 13.84.26 | "두 학습기 같은 모형" 약속 | **18개 갈라짐** → 2단계가 다른 물리로 경사 | **14.171 고침+관문** |
| ③ | `power_gain` 13.84.38 | 잔차 13~34%↓ | `sig_state` 와 같은 축, 곱하면 **x2,226** | **14.172 차단+관문** |
| ④ | `sig_site` 13.59 | 자리별 지문 | 자리 하나뿐인 기기는 되돌아간다 → 포트·오븐 못 고침 | **열림** |
| ⑤ | `standby_sig` 덮어쓰기 12.163 | "10배 어긋난다" | 13.40 이 전제를 바꿈 → 반대로 **x12.6** + **축퇴 2.6%** | **14.173 관문** |

### ⑤ 가 왜 제일 해로운가

```
  12.163  "y_standby 15.0W 인데 지문은 6.44mA — 10배" -> standby_sig[oven] 을 67.91mA 로 덮어씀
  13.40   carrier_apps 가 FAN_LIGHT 를 **활성 층**으로 -> y_standby 15.0W -> **1.156W**
  결과    어긋남이 반대 방향으로 **x12.6** · 그리고 **축퇴**:
            A 켜짐  sig_state[oven][1] x 14.95W = 69.73 mA
            B 대기  idle x standby_sig          = 67.91 mA   차 **2.6%**
```

오늘 경계에서 잰 *"오븐 게이트가 104~129초 내내 0.89~0.97 인데 내는 건 13~20W"* 의
**구조적 이유**다. 처방 한 줄 — `carrier_apps` 캐시에서는 **`--standby-operating off`**.

### ★ 자리(site)가 사전에 박혀 있다

```
  자리 D V3/V1 **0.0071**  핫플·오븐·충전기 녹화   |  자리 E **0.0327**  포트·드라이·AC·빔
  사전 h3/h1  핫플 0.0053 · 오븐 0.0051           |  **포트 0.0326** · 드라이 0.0271
                                                     ^^ 전압 0.0327 과 넷째 자리까지 같다
  실측 파일   test_1 D · **test_2 E** · test_3 D · test_4 D · test_5 D
```

⇒ ① 오븐<->포트가 **한 병이 아니라 둘**이다:
```
  D 파일(test_5)  h3 자리 인공물이 구동 — 포트 사전 5배 과대 -> 손실이 오븐으로 민다 (격차의 94%)
  E 파일(test_2)  손실은 **포트를 고른다**(h1~h9 잔차 857 대 1,232) 그런데 유령은 오븐
                  -> **상태 머리**가 구동자 (게이트 0.996 인 채 mix s1->s2)
```
처방이 갈린다 — D 계열은 가짜 판별자 제거(vhrel/sig_site), E 계열은 오븐 s1 발판 제거
(14.167 · `--w-gate-cond` · ⑤ 의 `--standby-operating off`).

### 새 관문 넷

```
  run_gate_statesig     상태 지문의 분모 (14.167)          8줄
  run_gate_lossparity   세 자리의 손실 인자 면 (14.171)     6줄  <- 10분 만에 새 구멍을 잡았다
  run_gate_powsig       전력대 지문의 이중계산 (14.172)     8줄  <- 굽기 전에 막았다
  run_gate_standby      대기 층 전력 대 지문 (14.173)       5줄
```

---

## §6 오늘 고친 것 / 기각한 것 / 아직 안 던진 것 (2026-09-16 저녁)

### ▣ 버그 고침 — 들어갔다 (동작이 옳아졌거나 비트 동일)

| | 무엇이 틀렸나 | 얼마나 | 고침 |
|---|---|---|---|
| **14.164/168** | `EVEN_MEDIAN` 이 모듈 전역인데 채점기가 창을 한 번만 지었다 | 하루에 **세 번** 무효 판정 (spike·rollback·vblock) | `load_model` 이 **멈춘다**. 도구 100개가 대상 |
| **14.171** | `build_loss` 가 1단계보다 **18개 적게** 넘긴다. 문서는 "같아야 한다" 고 약속 | 2단계(`run_train_seq`)가 **전압앵커·죽은구역 없이** 경사를 받음 | 18개 뚫고 `stage1_physics(ck)` 로 체크포인트에서 상속 + 관문 |
| **14.172** | `pow_gain` 과 `sig_state` 가 **같은 축** (대역경계 = 상태전력) | 곱하면 드라이 s2 h2/h1 **x0.000**, 최악 **x2,226** | 조합 자체를 **차단** + 관문 |
| **14.174** | `NILMNet.prior_kappa` 기본 **0.0** 대 트레이너 **8.0** | 관문 **5/7 이 프라이어 블록을 한 줄도 안 돌림** (`raise` 를 심어 확인) | 기본 8.0 (한 줄) → 다시 심으니 **5/5 잡음** |

### ▣ 기각 — 되돌렸다

```
  14.167 ✗ 오븐 s1 지문 (내 고침)   오븐 유령 6.8/5.0/3.4% -> **24.2/23.9/19.7%** (3/3)
                                   수백W 띠 13.9 -> **40.7초 (x3.9)** · p99 16~1046 -> 1232~1250W
     기전: FAN_LIGHT 지문이 너무 특이(h2/h1 0.0667 = 다른 저항의 35배) ->
           `L_harm` 이 "짝수차 보이면 오븐 휴지 15W" 로 싸게 설명 -> 게이트가 열리고
           그 위에서 상태 머리가 s1->s2 로 미끄러진다. **내 처치가 발판을 튼튼하게 했다**
     ★ 교훈: **"사전이 틀렸으니 고친다" 가 곧 좋아진다는 뜻이 아니다.**
             틀린 지문이 *억제* 노릇을 하고 있었다
  14.170 ✗ 파형 앵커 f=1.0        짝(osig) 대비 −11.0/+0.9/+5.1 초 = **널** (네 번째)
```

### ▣ 진단만 했다 — **아직 안 던졌다**

```
  14.173  오븐 **대기 축퇴**   관문 `run_gate_standby` 가 FAIL
          12.163 이 standby_sig 를 67.91mA 로 덮었는데 13.40 이 y_standby 를 1.156W 로 옮김
          -> 어긋남 **x12.4** · 그리고 A(게이트 70.98mA) 대 B(대기 67.91mA) **차 4.3%**
          처방 `--standby-operating off` (끄면 x1.3)
          ⚠ 14.167 과 **같은 위험** — B 가 게이트를 눌러 주고 있었다면 오히려 오를 수 있다
  ④       `sig_site` 가 자리 하나뿐인 기기(포트 E · 오븐 D)를 **못 고친다** (비가 1.0000)
          출구는 `harm_vhrel` 하나인데 네 번 널
```

### ▣ 만들어 둔 팔 두 개 — **안 던졌다** (올리다 멈춤)

```
  patches/cnn_sbo_seeds.sbatch    dzn + `--standby-operating off`     짝 dzn
  patches/cnn_seg2_seeds.sbatch   dzn + `cache/train60_v32hs3`        짝 dzn
     seg 를 다시 굽는 이유: 14.169 의 `cnn_seg` 는 osig(=회귀 포함) 위에 얹혔다.
     짝 비교는 유효했고 **좋았다** (osig 대비 3/3, 평균 −21.5초 = −48%).
     그런데 **dzn 보다 나은가는 아무도 안 쟀다** — 출하 바닥이 dzn 이다
  미리 등록한 판정법: 이득 = 수백W 띠·오븐 유령율 / 비용 = 절대잔차·판정줄·혼자켜짐·전체유령율
     ⚠ `sbo` 는 **오븐 유령 게이트율이 오르면 즉시 기각** (14.167 이 무너진 방식)
```

### ▣ 오늘 세운 관문 여섯 (sbatch 29개에 걸었다)

```
  run_gate_statesig    상태 지문의 분모 · **기본이 꺼짐**인지    9줄
  run_gate_lossparity  세 자리의 손실 인자 면                   6줄  <- 10분 만에 새 구멍을 잡았다
  run_gate_powsig      전력대 지문의 이중계산                   8줄  <- 굽기 전에 막았다
  run_gate_standby     대기 층 전력 대 지문 · 축퇴               5줄  <- **지금 FAIL** (미처치)
  run_gate_ctordefault 생성자 기본값 함정                       4줄  <- 눈먼 관문 5개를 찾았다
  (+ 14.168 load_model 이 짝수차 규약 불일치에 **멈춘다**)
```

### ★ 방법 — 땜질 충돌을 찾는 다섯 모양

```
  ① 재고 만들어 놓고 **한 경로에만** 달았다      버퍼가 전부 0 인 것을 세라
  ② 같은 것을 짓는 자리가 여럿이고 **갈라졌다**   AST 로 인자 집합을 비교하라
  ③ 모르는 칸을 **틀린 값으로 채웠다**            `if 표본부족: continue` 뒤에 뭐가 남나 보라
  ④ **같은 축**을 두 기구가 재고 곱한다           두 표의 축이 물리적으로 같은 양인가
  ⑤ 땜질이 **서로를 무효화**했다                  땜질 주석의 **전제**를 다시 재라
  ⊕ 의심되는 블록에 **`raise` 를 심고** 관문을 돌려라 — grep 은 7 중 2 를 잘못 고발했다
```
