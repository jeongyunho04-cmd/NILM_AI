# 인수인계 — 시연 노트북에서 한 것과 학습 PC 로 넘길 것 (2026-08-23)

이 문서는 **시연 노트북(RTX 2050)** 에서 실시간 추론 환경을 세우고 실측 라벨을
정비하면서 나온 결과와, **학습 PC(RTX 5070)** 에서 해야 할 일을 나눠 적은 것이다.

상세 근거는 `MODEL_TRAINING_DESIGN.md` 12.30~12.33 절에 있다.

---

## 0. 한 줄 요약

**저부하 실패의 원인이 갈렸다.** 모델은 SMPS 3종을 가를 수 있다(미니PC F1 0.957).
못 가르는 게 아니라 **고부하가 켜지면 그 능력이 무너진다**(같은 모델이 0.574).
0.7절의 오차 전가가 원인으로 확정됐고, 재학습 없이 부분적으로 되돌리는 방법을
찾아 `run_live` 에 넣었다(`--ckpt-smps`).

---

## 1. 시연 노트북 환경 (완료)

| | |
|---|---|
| torch | 2.6.0+cu124 (드라이버 551.86 이라 cu128 은 못 씀) |
| 추론 | **4.64 ms/창 = 215 추론/초** — 60Hz 요건의 3.6배 |
| 두 체크포인트 동시 | 120 추론/초 — 여전히 요건의 2배 |
| 전처리 | 개별 가전 npz 20개 생성 완료. `composite_eval` 은 미완 |

> `composite_eval` 을 만들려면 수신기를 멈추고 `python -m src.run_preprocess_and_label`
> 을 다시 돌릴 것. 스캔 도중 `data/` 의 파일이 사라지면 멈춘다.

---

## 2. 코드 변경 (이 노트북에서 완료, 학습 PC 에 병합 필요)

### 2.1 `src/run_live.py` — 결함 둘을 고쳤다

**① `CycleRing` — 프레임 순서 뒤바뀜**

수신기는 프레임을 **도착 순서 그대로** CSV 에 쓰고, 펌웨어는 선택적 재전송을 한다.
옛 코드는 파일 순서대로 deque 에 append 했다.

실측 원데이터(24 파일, 153만 행)의 실태:

| | |
|---|---|
| 프레임 역전 폭 | **최대 7.98초**, 중앙값 3~6초 |
| `test3.csv` 프레임 기준 | **815개 중 225개(27.6%)** 가 순서를 벗어남 |
| 세션 이어붙임 (보드 리셋) | `test.2` 2회, `test3` 1회, `oven`·`hotplate_1` 등 |

10.9% 역전을 주입해 잰 효과:

| | 전력 MAE | 최대 오차 | 게이트 판정 뒤집힘 |
|---|---|---|---|
| 순서 보정 ON | **0.001W** | 0.5W | **0개** |
| 보정 OFF (옛 동작) | 0.818W | **1,182.6W** | 7개 |

`k = round(t_s * 60)` 으로 제자리에 꽂는다. **지연 0** (버퍼링해 정렬하는 게 아니라
자리를 지정할 뿐). 창 밖 행이 연달아 오면 세션 리셋으로 보고 버퍼를 비운다.

> 설계 문서 12.29.4 의 "재생 검증에서만 걸린다" 는 **틀렸다.** 실시간도 같은
> 도착 순서를 읽는다. 12.30.2 에서 정정했다.

**② `--ckpt-smps` — 2단계 적응이 SMPS 를 부수는 것을 되돌린다**

`adapt_v17` 은 총전력 잔차를 145.8W → 28.9W 로 줄이고 유령 포트를 166.6W → 3.7W 로
없앤다. 그 대가로 SMPS 3종을 부순다.

`test_5`(사람 기록 라벨) 기준:

| | `cnn_v17` | `adapt_v17` | **하이브리드(`--ckpt-smps`)** |
|---|---|---|---|
| 총전력 잔차 MAE | 145.8W | **28.9W** | **30.0W** |
| 유령 포트 | 166.6W | **3.7W** | **3.7W** |
| 충전기 F1 / ON평균(참 68W) | **0.954** / 36.3 | 0.876 / 11.6 | **0.954 / 36.3** |
| 프로젝터 ON평균(참 46W) | **50.2** | 72.6 | **50.2** |
| 미니PC F1 | **0.574** | 0.137 | **0.574** |
| 핫플 F1 | **0.603** | 0.527 | 0.527 ← 유일한 손해 |

**재학습 없이 검증했다** (12.23.5 의 규율. 예측 4/4 적중). 저항 무리는 2단계,
SMPS 무리는 1단계에서 가져온다.

```bash
python -m src.run_live --csv data/live.csv \
    --ckpt results/adapt_v17.pt --ckpt-smps results/cnn_v17.pt
```

**③ 봉인 가드** — `--replay data/test.csv` 를 `SealedDatasetError` 로 막는다.
재생으로 들여다보는 것도 봉인을 깨는 것이다.

### 2.2 `src/run_score_live.py` (신규) — 4.3절 채점기

`run_live` 는 기록만 하고 채점을 안 했다. 라벨 없이 나오는 것과 확정이 있어야
되는 것을 나눠 찍는다.

**⚠ 6초 어긋남을 반드시 보정해야 한다.** 로그의 `t_s` 는 **창의 끝**이고 모델의
타깃은 `inputs.TARGET_LOOKAHEAD = 360` 사이클 = **6초 전**이다. 설계 문서 5.1절의
"1초" 는 12.9.12 이후 갱신 안 된 낡은 서술이다. 보정 없이 채점하면 포트·드라이기
(동작 중앙값 9~10초)에서 정답과 오답이 통째로 뒤바뀐다.

새 지표 하나 — **게이트 채터(0.5 교차/분)**. 합성 홀드아웃은 창을 무작위로 섞어
뽑으므로 이 축이 원리적으로 없다. 실시간에서만 보인다.

```bash
python -m src.run_score_live --log results/live_log.jsonl --json results/live_score.json
python -m src.run_score_live --log results/t7_cnn.jsonl --truth-events test_7
```

### 2.3 `tests/test_live_ring.py` (신규) — 회귀 테스트 15개

순서 뒤바뀜/세션 이어붙임/유실 보간/체류시간. 전부 통과.
데이터가 필요 없으므로 학습 PC 에서도 그대로 돈다.

---

## 3. 실측 정답 정비 (완료)

### 3.1 라벨 출처가 파일마다 다르다 — 3등급을 박았다

`real_events.json` 에 `_label_provenance` 필드를 추가했다.

| 파일 | 길이 / 전압 | 등급 | 근거 |
|---|---|---|---|
| `test.2` | 466.0s / 220.9V | `ai_inferred_signal_checked` | AI 추정 + 이번에 미니PC 정정 |
| `test3` | 413.0s / 219.8V | `ai_inferred_signal_checked` | AI 추정 + 이번에 미니PC 정정 |
| `test_4` | 701.5s / 218.4V | `ai_inferred_signal_checked` | AI 추정 + npz 재대조 3곳 정정(이전 세션) |
| **`test_5`** | **569.5s / 218.4V** | **`human_switching_log`** | 스위치를 누른 사람이 기록 |
| **`test_6`** | **790.5s / 220.3V** | **`human_switching_log`** | 〃 |
| **`test_7`** | **675.5s / 222.6V** | **`human_switching_log`** | 〃. **저항 부하 없음** |
| `test.csv` | 1212s / 233.6V | (없음) | 봉인 (4.3절) |

**사람 기록 2,035.5s (33.9분) / AI 추정 1,580.5s (26.3분).**

> **4.3절의 표를 갱신해야 한다.** 그 표는 `test.2`/`test3`/`test_4` 를 "검증(튜닝
> 허용)" 으로 지정했는데, 라벨이 모델과 독립이 아니면 그 자격이 없다. 그 파일들로
> 잰 수치(저항3종 1.2~5.8%, 미니PC F1 ~0.10 등)는 모델 성능인지 라벨 오류인지
> 분리되지 않는다. **적응(2단계)은 무라벨이라 영향 없다 — 오염된 것은 측정이다.**

### 3.2 사람 기록 3파일을 병합했다

시각 변환식이 **파일마다 다르다.** 수신기는 `t_s = (seq − 첫seq) × 0.5` 로 쓰는데
첫 seq 는 보드가 언제 켜져 있었느냐에 따라 달라진다.

```
test_5   seq  0 부터  ->  t_s = seq x 0.5
test_6   seq 37 부터  ->  t_s = (seq - 37) x 0.5      <- x0.5 만 하면 18.5초 어긋남
test_7   seq 16 부터  ->  t_s = (seq - 16) x 0.5
```

정밀화 결과 (SMPS 전이를 I3 계단으로):

| | 전이 수 | 정밀화 | 오차 중앙 | 미검증 |
|---|---|---|---|---|
| `test_5` | 17 | 12/12 SMPS | ±2s | 0 |
| `test_6` | 24 | 15/18 SMPS | 2.7s | 3 (오븐+핫플 동시 통전에 가림) |
| `test_7` | 17 | 13/14 SMPS | 2.2s | 0 (파일 시작 1건은 레벨로 확인) |

`test_5` 에서는 사용자 txt 에 누락됐던 `seq 840`(충전기 OFF)을 신호가 독립
확인했다(421.5s, ΔI3 −275.7mA — 다른 충전기 OFF 2회와 4% 이내).

### 3.3 `test.2` / `test3` 의 미니PC 라벨을 고쳤다

test_5 에서 얻은 SMPS 레벨로 재분해했다. **세 가지가 맞아야 된다** (12.32.1):
후보를 그 파일에 실제 있는 기기로 좁히고, 저항 통전 블록을 버리고, **기기를 점이
아니라 범위로 둔다.**

검증: test_5 의 충전기 꺼짐 구간 208블록에서 **2기기 동시 정확도 1.000**
(사전 기준 0.95). 독립 검증 — 이 분해가 읽은 프로젝터가 라벨과 **0.3%p / 1.2초**
차이로 일치한다.

```
test3   미니PC  on [[140.0,413.0]] + uncertain[[63,140]]  ->  on [[80.0,413.0]]
                채점 가능 81% -> 96.6%
test.2  미니PC  on [[33.6,450.0]] (끊김 없음)  ->  on [[33.6,208.0],[248.0,450.0]]
                223~235s 에 I3 210mA = 프로젝터 단독. 약 38초 OFF 를 놓치고 있었다
```

12.26.3 의 "미니PC 는 여러 번 껐다 켜진다" 가 `test.2` 에서 확인됐다.
`test3` 에는 해당하지 않는다.

---

## 4. 발견 (중요도 순)

### ① 저부하 실패의 원인 = 저항 부하 전가 (확정)

`test_7` 은 **저항 부하가 없는 유일한 복합 부하 실측**이다 (P 최대 367.6W).
지금까지 못 갈랐던 두 가설을 분리한다.

| 기기 | `test_5` (오븐+핫플) | **`test_7` (저항 없음)** |
|---|---|---|
| **미니PC** `cnn_v17` | 0.574 | **0.957** (재현율 .933) |
| **미니PC** `adapt_v17` | 0.137 | **0.553** |
| 충전기 `cnn_v17` | 0.954 | 0.948 |
| 프로젝터 `cnn_v17` | 0.862 | 0.857 |

`cnn_v17` 의 유령 포트도 같다 — `test_5` 166.6W vs `test_7` **0.02W**.
그래서 잔차 MAE 도 145.8W → **9.5W**. **1kW 급 부하가 실재해야 헛것을 본다.**

> **우선순위가 바뀐다.** "SMPS 3종을 어떻게 가를 것인가"(12.23~12.26 의 기기별
> 처방 탐색)는 이미 풀려 있다. 진짜 문제는 고부하가 켜졌을 때 무너지는 것이다.

### ② SMPS 지문은 점이 아니라 범위다

| 기기 | I3 범위 | 배율 | 원인 |
|---|---|---|---|
| 빔프로젝터 | 195~204 mA | **1.05×** | 사실상 점 |
| 미니PC | 41~120 mA | **2.9×** | CPU 부하 |
| 노트북충전기 | 95~301 mA | **3.2×** | 배터리 잔량 |

**충전기는 다른 두 기기의 지문 위를 지나간다** — 급속 충전 중이면 I7/I3 0.640 으로
프로젝터(0.629)와 같고, 완충되면 0.785 로 미니PC(0.769)와 같다.
`laptop_charger_1.csv` 는 48.5분짜리 완전한 충전 곡선이다(67W → 33W 단조 감소).

`run_label_audit.py` 가 쓰는 점 지문 176.2mA 는 급속(256)과 완충(140) **사이의
빈 자리**라 최악이다.

`test_7` 이 미니PC 의 CPU 부하를 사람 라벨로 확인했다 (구성 고정, n=77 vs 178):

```
유휴   P 59.8W / I3 245.6mA        작업중 P 78.3W / I3 324.7mA
차이   +18.5W / +79.1mA            미니PC 단독으로는 7.9W/34.1mA -> 26.4W/113mA (3.3배)
```

### ③ ⚠ 모델이 동적 범위를 못 쓴다 (새 발견)

| | 유휴 예측 | 작업 예측 | 차이 | 참 차이(+18.5W) 대비 |
|---|---|---|---|---|
| `cnn_v17` | 9.4W | 8.0W | **−1.5W** | **−8%** |
| `adapt_v17` | 5.9W | 2.5W | −3.4W | −18% |

**부호가 반대다.** on/off 는 F1 0.957 로 맞히면서 크기는 오히려 낮춘다.
모델이 미니PC 를 20W 이상으로 예측한 적이 **한 번도 없다** (4개 로그 전부 0.0%,
최대 16.6W).

대조: 프로젝터(참 고정 1.0W 폭)는 예측 폭 5.9W 로 정확히 고정, 충전기(참 폭 38W)는
27.7W 로 따라간다. **미니PC 만 안 된다.**

풀을 확인한 결과 **축은 있다.** 다만 위쪽 끝이 얇다:

```
미니PC 활성화 9개 중 p90 이 20W 를 넘는 것: 1개 (#8 = minipc_3, CPU 부하 녹화)
풀 전체에서 >=20W 인 사이클: 11.5%
```

11.5% 노출이 적긴 하지만, **부호가 반대인 것은 이것만으로 설명되지 않는다.**
남은 후보는 복합 창에서 미니PC(34mA)와 충전기(95~301mA)를 못 가르는 것이다.
**재학습이 필요한 검증이라 학습 PC 몫이다.**

### ④ 충전기 → 프로젝터 전가는 절반만 고쳐졌다

`test_7` 272~378s (충전기+프로젝터만, 미니PC 꺼짐. 관측 98.5W = 바닥 4.8 +
충전기 44.9 + 프로젝터 48.8):

| | 충전기 (참 44.9W) | 프로젝터 (참 48.8W) | 충전기 오차 |
|---|---|---|---|
| `adapt_v17` | **18.1W** | **73.3W** | **−60%** |
| `--ckpt-smps` | **37.0W** | **50.7W** | **−18%** |

파일 전체 프로젝터 ON 평균: `adapt_v17` 65.6W(+35%) vs `cnn_v17` 47.8W(**−2%**).

**프로젝터 과대예측은 해소. 충전기 과소예측은 −60% → −18% 로 3배 개선됐지만 남음.**
on/off 판정은 충전기 F1 0.948 로 안정적이다 — 켜진 건 아는데 와트를 18% 덜 센다.

### ⑤ 프레임 유실의 원인 (참고)

`live.csv` 한 세션에서 유실 5.67% 가 났다. 26개 녹화 중앙값은 **0.00%** 다.

- 유실의 73% 가 "도착했으나 깨짐" = TCP 이전 바이트 손실 (STM32↔ESP UART)
- 펌웨어가 이 실패 모드를 문서화해 뒀다: *"ESP-01S 는 흐름제어 핀이 없고 내부
  버퍼가 프레임보다 작아서, WiFi 가 잠깐만 밀려도 바이트를 버린다"*
- 도착 지터가 유실률과 함께 움직인다 (σ 400ms 정상 → 750~890ms 유실 세션)
- **구조적 원인**: 노트북의 Wi-Fi 라디오 하나가 5GHz 외부망과 2.4GHz 핫스팟을
  시분할한다. ESP-01S 는 2.4GHz 전용이다

**처방**: 측정 중 외부 Wi-Fi 연결을 끊을 것(유선 랜 사용). 펌웨어 시리얼 로그로
baud 상향(460800) 성공 여부 확인. 배터리로 돌리면 무선 절전이 "보통 절전" 으로
떨어지니 AC 필수.

---

## 5. 학습 PC 로 넘길 목록

### A. 병합만 하면 되는 것 (코드/데이터)

- [ ] `src/run_live.py` — `CycleRing`, `--ckpt-smps`, `--no-reorder`, 봉인 가드
- [ ] `src/run_score_live.py` (신규)
- [ ] `tests/test_live_ring.py` (신규, 15개 통과)
- [ ] `processed_data/real_events.json` — test_5/6/7 병합, test.2/test3 정정,
      `_label_provenance` 3등급. 백업 `.bak` ~ `.bak6` 도 함께
- [ ] `TEST_DATASET_TIMELINE_ANALYSIS.txt` — [6-1] [6-2] [6-3] [7] 절
- [ ] `MODEL_TRAINING_DESIGN.md` — 12.30 ~ 12.33 절
- [ ] `data/test_5.csv` `test_6.csv` `test_7.csv` (gitignore 라 따로 옮겨야 함)

> `data/beam_projector_1.csv` → `beam_projector.csv`, `oven_1.csv` → `oven.csv` 로
> 이름을 되돌렸다 (레지스트리와 학습 PC 산출물 이름에 맞춤).

### B. 재학습이 필요한 것 (우선순위 순)

**B-1. 적응에서 SMPS 게이트를 얼린다** — `--ckpt-smps` 의 정식화

재학습 없이 검증은 끝났다(4/4 예측 적중). `run_adapt` 에서 SMPS 3종의 게이트를
1단계 값으로 고정하고 `L_cons` 가 저항 부하만 조정하게 한다.
필요한 것: `cache/train60` (13GB), `processed_data/holdout60/*.npy` (3.6GB).

판정 기준 (돌리기 전에 적을 것): `test_5`/`test_6` 에서 충전기 F1 ≥ 0.94 **그리고**
미니PC F1 ≥ 0.55 **그리고** 유령 포트 ≤ 10W. 셋 다 넘어야 `--ckpt-smps` 를 대체한다.

**B-2. 미니PC 의 작업 상태 비율을 올린다**

풀에서 ≥20W 인 사이클이 11.5% 뿐이고 활성화 9개 중 1개에만 있다.
`minipc_3.csv` 가 CPU 부하 측정용이므로 그 활성화의 가중치를 올리거나,
CPU 부하 녹화를 1~2개 더 추가한다.

판정: `test_7` 의 `_workload_periods` 에서 유휴↔작업 예측 차이가 **양수**가 되면
성공. 지금은 −1.5W 다.

**B-3. 충전기의 충전 상태 궤적을 세그먼트 풀에 넣는다**

`laptop_charger_1.csv` 에 급속(67W) → 완충(33W) 전 곡선이 있으므로 재측정은 필요
없다. `get_steady_power_w` 가 p90 이라 급속 쪽으로 편중돼 있을 가능성을 먼저 확인.

판정: `test_7` 272~378s 에서 충전기 오차가 −18% 보다 나아지면 성공.

**B-4. 데이터 양 스윕** (12.28.4 가 "안 해본 진단" 으로 지목. 90분)

캐시 25/50/100% 로 학습해 곡선을 본다. 일찍 포화하면 합성 데이터를 더 만들어도
소용없다는 것이 확정된다.

**B-5. `test_5`/`test_6`/`test_7` 로 재채점**

사람 기록 33.9분이 생겼다. 지금까지의 실측 수치는 전부 AI 추정 라벨 기준이다.
`run_reeval` / `run_gate_check` 를 이 세 파일로 다시 돌려 기준선을 갈아야 한다.
`test_7` 은 저항 부하가 없어 **SMPS 전용 회귀 시험대**로 쓸 수 있다.

### C. 코드로 할 수 있는 작은 개선

- [ ] `run_synthesis` 의 다양성 경고에 **동적 범위** 축을 추가한다.
      지금은 활성화 **개수**만 센다(5개 미만이면 경고). 미니PC 는 9개라 경고가 안
      뜨는데 범위의 위쪽은 1개에 의존한다
- [ ] `run_label_audit.py` 의 충전기 점 지문(176.2mA)을 범위/궤적으로 바꾼다
- [ ] 설계 문서 5.1절의 "지연 1초" 를 6초로 정정 (12.9.12 이후 낡은 서술)
- [ ] 4.3절의 검증 파일 표에 `_label_provenance` 등급을 반영

---

## 6. 다음 측정 때 지켜야 할 것

1. **`--ckpt-smps` 를 쓸 것.** 안 쓰면 충전기가 −60% 로 읽힌다
2. **측정 중 외부 Wi-Fi 를 끊을 것.** 라디오 시분할이 프레임 유실을 만든다
3. **측정 중 이 노트북에서 무거운 작업 금지** (전처리·pytest 포함)
4. **라벨링 시 `0`(전부 꺼짐)을 먼저 누를 것.** 안 누른 기기는 OFF 가 아니라 '모름'
   이라 채점에서 빠진다. `u` 는 "전부 꺼짐" 이 아니라 상태를 **비운다**
5. **seq 를 적을 때 첫 seq 를 함께 기록할 것.** 파일마다 오프셋이 다르다
6. **저항 부하 없는 녹화를 더 만들 것.** `test_7` 같은 파일이 전가와 분해 능력을
   분리해 준다 — 지금 1개뿐이다
