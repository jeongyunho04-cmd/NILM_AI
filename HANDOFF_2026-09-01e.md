# 인수인계 2026-09-01e — 프로젝터 배분을 닫았다. 뿌리는 **계통 임피던스**였다

`HANDOFF_2026-09-01d.md` 다음이다. d 는 처방을 찾았고, 이 반쪽은 **왜 그것이
들었는지**를 찾아 뿌리까지 갔다. 설계 문서 **12.139 ~ 12.148.3**, 규칙 **38 ~ 42**.

---

## 0. 한 줄

**12.87.3 이래의 SMPS 오배분이 닫혔다.** 프로젝터 +30.0 → **−1.63W**, F1 0.846 →
**0.887**, 미니PC F1 0.779 → **0.892**. 유령대조는 기준선보다 낮다 (7.09 → 5.14).

원인은 **계통 임피던스**였다. `harmonic_signatures` 는 문헌이 오래 전에 실패를
적어 둔 *fixed current injection* 모형이라 기기 전류가 계통 조건과 무관하다고
본다. 실제로는 부하가 전압을 왜곡하고 그것이 되먹임된다. 보정을 넣으니 풀렸다.

---

## 1. 최종 표 — 전부 같은 명령으로 채점 (후처리 켬 / 운영점)

```
판              프로젝터W       유령8       유령대조      잔차     F1     프로젝정밀 충전재현 미니정밀
기준선 w4.0     29.99±3.58  3.54±1.59   7.09±3.24  4.24  0.846   0.822  0.919  0.820
가중 1/h²       23.47±1.70  4.67±0.63   8.22±0.66  4.55  0.865   0.859  0.963  0.873
in-situ 지문    17.73±2.57  6.04±3.96   7.31±0.50  4.56  0.846   0.859  0.942  0.813
가중+지문         7.32±1.58  4.52±0.53  11.51±1.66  4.51  0.877   0.860  0.960  0.864
1/tau           29.26±1.42  3.73±0.59   4.91±1.06  4.24  0.852   0.841  0.917  0.820
전력사전          1.71±0.18  4.83±1.01   8.37±5.34  4.38  0.826   0.865  0.984  0.973
Norton 크기만   -0.98±0.58  7.68±0.25  11.37±3.96  4.40  0.887   0.865  0.982  0.849
**복소 Z·I**    -1.63±0.41  6.78±0.49   5.14±1.74  4.52  0.887   0.867  0.978  0.860
--- 참고 (반증됨) ---
단일기기(청정)    -0.33±4.53 20.55±8.32  12.90±7.39  4.83  0.874   0.862  0.956  0.837
단일기기(전파일)   -1.84±3.46 18.81±1.13  14.04±4.57  4.59  0.869   0.861  0.950  0.834
```

**표를 손으로 옮기지 말 것.** `run_summarize_pan` 이 만든다 (12.137.1 을 숫자
하나까지 재현한다).

```bash
python -X utf8 -m src.run_summarize_pan --pc results/pc_zi.json --gc results/gc_zi.json
```

### 채택 후보 — `복소 Z·I`

```bash
python -X utf8 -m src.run_norton_probe --save-coef results/norton_coef.npz
python -X utf8 -m src.run_adapt --init results/cnn_ovh.pt --cache cache/train60_ovh \
    --steps 1000 --seed {0,1,2} --harm-weight inv_h2 \
    --harm-offset results/norton_coef.npz --tag adapt_zi_s{0,1,2} --out results
```

```
기기별 F1        minipc  프로젝터  충전기   핫플    오븐
기준선            0.779  0.898  0.937  0.915  0.745
**복소 Z·I**      0.892  0.925  0.959  0.914  0.744    <- 다섯 중 넷이 최고/동률
```

⚠ **남은 결함 하나** — 유령8 6.78 중 **5.59 가 에어컨**이다 (82%). 에어컨은
11파일 어디서도 켜진 적이 없어 제약이 0 인 배출구다. **다음 수가 명백하다.**

---

## 2. 확정된 것

### (1) 프로젝터 과대예측은 **구조**였다 (12.145)

저항이 하나도 안 켜진 창(`P관측 = SMPS 3종 합`, 3,034개)에서 배분을 직접 읽었다.

```
모델                    프로젝터W   충전기   미니PC     P관측
1단계 (합성만)             62.5     25.9     5.7     100.1   <- d 의 §5[2] +15.6W 가 여기다
2단계 기준선               82.8      5.0     6.5     100.1   <- 적응이 +20.3W 를 더 얹는다
참값                     46.9   <--- 54.3 --->      101.2
실측 손실만 보면 최적       60.0 (균등) / 50.0 (1/h²)         <- 최소점부터 틀렸다
```

원인: **SMPS 3종 지문이 11.9도 안에 몰려 있다** (cos 0.9786, 조건수 16.7). 그 안에서
프로젝터가 와트당 전류가 가장 작아(8.038 < 8.301 < 9.332 mA/W) ① 같은 전류에 가장
많은 와트가 들고 ② 와트당 벌금이 가장 싸다. 둘 다 프로젝터로 몰아넣는다.

**못 박으면 한 칸 내려간다** — 프로젝터를 46.9W 로 고정하면 충전기가 새 최소값이
되고 NNLS 가 거기 붓는다 (모델 없이 42.1, 학습 모델 42.7).

### (2) 뿌리는 **계통 잔차**이고 그것은 계통 임피던스다 (12.146 ~ 12.148.2)

고조파에 정보는 **있다** — 36W 맞바꿈의 신호/오차가 홀수차 전부에서 **1.55** 다.
막는 것은 계통 잔차이고, 그 크기가 판별신호의 **0.42~1.15배**라 배분이 그 폭 안에서
미끄러진다.

그 잔차가 **부하가 만든 전압 왜곡**이다:

```
V_h  =  V_src,h  −  Z_h·I_h
        ^^^^^^^     ^^^^^^^
        그 집·그 시간대의 상수    변하는 부분
        -> 회귀 절편이 흡수       -> 위상이 **이미 있다** (ihdeg)
```

계통 임피던스를 물리 제약 `Z_h = R + j·h·ωL` 로 풀면 **미지수 둘**이고 14초다:

```
R = 1.953 Ω    L = 446 µH     |Z_h|: h1 1.96 -> h15 3.19 Ω
V_src,1 파일별 220.9~225.0V                    <- 같은 장소답게 안정적
```

```
                     중앙 |잔차| mA      배분 |오차| W   (파일 홀드아웃, 대조 3 제외)
(보정 없음)               109.8            39.4
전역 상수                 112.7            23.9
|V_h| 크기만              142.8            14.3   <- 잔차는 나쁜데 배분만 좋다
**복소 Z·I**               86.5 (7/8 개선)   6.7
```

### (3) `--sig-insitu` 는 지문을 고친 게 아니다 — **눈금을 비틀었다** (12.145)

판별이 사는 h3·h5 는 그대로 두고 **h1 만 12.5% 올렸다.** 그리고 12.140 의 실측은
프로젝터 h1 을 4.523 mA/W 라 했는데 `sig_insitu` 는 5.157 — **실측보다 14% 높다.**
물리적으로 더 틀린 값을 넣어서 좋아졌다 (규칙 35 — 값을 만져야 최적점이 옮겨진다).

### (4) 12.136 의 자는 **반쪽이었다** (12.142)

`1/h²` 를 고른 근거는 **겨냥 5파일**에서만 쟀다. 대조를 넣으면 두 열이 반대로 단조다:

```
가중         모델오차/판별신호   유령 SMPS W   (모델도 손실도 학습도 안 쓴다)
균등              4.41           7.2
1/tau             3.12           6.9
**1/h² (채택)**    2.02           9.4      <- 비는 좋아지고 유령은 31% 나빠진다
```

그 열이 학습을 예측한다 (예측 7.2->9.4, 실측 유령대조 7.09->8.22).

### (5) 대조 유령의 55% 는 프로젝터였다 (12.139)

유령은 **`L_harm` 을 줄인다** — 네 판 전부, 두 파일 전부. 손실이 요구하는 것이다.
그 이득의 **84%가 h1** 에서 나온다 (`inv_h2` 일 때. 균등은 26%).

---

## 3. 닫은 갈래

```
❌ 단일기기 지문 처방    유령8 3.54 -> 20.55W. 오염된 지문이 3차 판별자를 죽인다 (12.140)
❌ `noise_sig` 교체     대기 항이 이미 배경을 덮는다 (36.9 vs 39.3 mA). 이중 계상 (12.140)
❌ 등가저항을 손실에     유령 2.5~20배. 후처리는 거부권이 있고 손실에는 없다 (12.143)
❌ 반파 h2 가 유령 원인   손실값 63배지만 **기울기 0** (12.141)
❌ 파일별 오프셋 ALS     26.8 (상한 +0.9 미달). 편향된 배분으로 잔차를 재서 자기일관된
                      오답에 수렴한다 (12.147)
❌ 최소부하 창 오프셋     **분위가 자유 파라미터**다. 평균 최소는 부호 상쇄의 가짜 (12.147)
❌ |V_h| 크기만 쓰기     잔차 6/8 악화. 위상이 있어야 한다 (12.148.2)
◐ 1/tau               유령대조 4.91 (최저) 인데 프로젝터·F1 은 안 산다 (12.144)
◐ --w-pref            프로젝터 1.71W 인데 **미니PC 재현 0.834 -> 0.532** (12.145.1)
```

---

## 4. ⚠ 이 세션이 **철회한** 것 — 다시 파지 말 것

```
"지문이 실측 고조파를 못 만든다"    철회. **배경 부하 오염**이었다. 제대로 빼면 −4.5~+0.9%
"오븐 h3 이 합성의 19배"          철회. 저항은 3차를 안 흘려 분모가 0 근처다
"미설명 전류가 갈 자리가 없다"      철회. 대기 항 용량이 177 mA 이고 이미 쓰고 있다
"고조파 안에는 답이 없다"          철회. 신호/오차 1.55 다. 각도로 편 추론이 틀렸다
"모델이 자기 손실을 잘 최소화한다"   정정. 최적 60.0W, 모델 82.8W. 22.8W 는 손실 탓이 아니다
"전압도 12.72 와 같은 오염"       철회. **전압 경로엔 레인지 전환이 없다** (사용자 지적)
"펌웨어가 vhdeg 를 내보내야 한다"   철회. 배경은 절편이 먹고 변하는 부분의 위상은 이미 있다
"교차주파수 결합이 일한다"          정정. 크기만 쓴 76.1% 설명력은 홀드아웃에서 안 산다.
                              일하는 것은 **부하가 만든 복소 왜곡**이다
"헤어드라이어 반파가 새 발견"       정정. 12.109.2 가 이미 `HALFWAVE_OHM 108.6Ω` 로 적어 뒀다
```

### ⚠ 미결로 남긴 것 — 전압 짝수차

vh2 가 **2.07~2.26% 로 부하 595배 변화에 불변**이고 무부하 녹화에서도 같다. 우리
부하 것이 아니다 (IR 강하려면 582Ω). **상류 계통인지 전압 채널 자체인지 이 자료로는
못 가른다** — 깨끗한 전원에서 한 번 녹화하거나 기준 계측기와 비교해야 한다.

그것이 `corr(계통잔차, thd_v) = −0.206` 을 설명한다. 홀수차만 보면 **+0.001** 이다.
현행 `thd_v` 는 에너지의 **69% 가 짝수차**다.

### ⚠ `noise_signature` 가 배타적 두 조건을 평균한다 (12.147)

`noise_selfpower`(계측기가 측정 선로에서 급전)와 `noise_noselfpower` 가 h3 에서
4.283 vs 0.930 로 **4.6배** 다른데 `np.mean` 으로 섞는다. 이 문제에는 영향이 없지만
(계측기 7~11 mA vs 오프셋 30~160 mA) 사실 관계는 녹화 때 어떻게 급전했는지로 정해진다.

---

## 5. 다음에 할 것

### [1] **에어컨 열** — 유령8 의 82%다

```
유령8 기기별 W    air_conditioner   나머지 합
기준선                  2.11         <0.5
**복소 Z·I**           5.59         변화 없음
```

에어컨은 **11파일 어디서도 켜진 적이 없다** (`appliances_present` 에 없다). 학습
자료에 없으므로 그 열은 원래 쓸모가 없고 지금은 **제약이 0 인 배출구**다.

⚠ 이것은 **다른 축**이라 Norton 과 섞으면 귀속이 안 된다 (규칙 3). 따로 절제할 것.

### [2] 2단계가 왜 손실 최적점에서 22.8W 벗어나는가 — **아직 모른다**

가장 그럴듯한 것은 12.94 다 — 창당 기울기 노름이 고부하 창에서 25배 커서 SMPS
전용 창이 개수로 58% 인데 기울기로는 10% 미만이다. **확인 안 했다.**
`--smps-boost` 로 SMPS 창 가중을 올려 보는 것이 그 시험이다.

### [3] 임피던스를 새 집에서 어떻게 얻나

`fit_impedance` 는 **라벨을 안 쓴다** — `|V_h|` 와 복소 `I_h` 만 쓴다. 새 집에서도
부하가 변하는 녹화만 있으면 14초에 풀린다. 다만 계수 `A`(어드미턴스)는 라벨 있는
파일에서 적합했으므로 **그것이 집을 옮겨 사는지는 안 쟀다.**

### [4] `--w-pref` 와의 관계

`Norton + 사전` 이 Norton 단독과 사실상 같다 (12.148.1). **직교하지 않는다** —
Norton 이 앵커 없이 같은 일을 한다. `--w-pref` 는 미니PC 를 뭉개므로(0.532)
Norton 이 있으면 안 쓰는 것이 낫다.

---

## 6. 새 도구 / 플래그

```
run_adapt --harm-offset NPZ                **채택 후보** 교차주파수 보정 (12.148)
run_adapt --w-pref W --pref-apps LIST      전력 사전 (12.145). 미니PC 를 뭉갠다
run_adapt --harm-weight inv_tau            유령대조 4.91 (12.144) ◐

src/run_norton_probe.py         계통 임피던스 적합 + Z·I 계수. `--save-coef`
src/run_summarize_pan.py        pc_* + gc_* -> 인수인계 표 한 장 (규칙 34)
src/run_ctrl_ghost_probe.py     대조 유령을 판x파일x기기로 + ΔL_harm 차수 분해
src/run_harm_weight_recheck.py  차수 가중의 자를 **대조를 넣고** 다시 푼다 (규칙 38)
src/run_resmatch_loss_probe.py  등가저항을 손실에 (닫힘)
src/run_offset_fit_probe.py     파일별 오프셋 ALS (상한 미달)
src/run_fit_single_sig.py       단일기기 창 지문 (`--subtract-baseline` 필수)
src/run_sig_calib_probe.py      실측 vs 합성 지문 진단
src/run_sig_mode_probe.py       기기 하나에 지문이 둘인가 (전력 층화)

realdata.harmonic_offset(...)   창별 보정. **원자료 CSV 의 ih/ihdeg 를 읽는다**
run_norton_probe.fit_impedance  R + j·h·ωL, 라벨 안 씀
losses.NILMLoss(power_ref=)     참값 전력 벡터. 0 = 모름
losses.unlabeled(w_pref=)       전력 사전 항
run_gate_check.forward_file     `idle` 을 같이 돌려준다
```

---

## 7. 환경 메모

- 학습에 `--init results/cnn_ovh.pt --cache cache/train60_ovh` 를 반드시 줄 것.
- 한글 트레이스백이 안 보이면 `python -X utf8 -m ...`.
- 채점 산출물의 `_config` 에 명령이 들어 있다 (규칙 33).
- 학습 로그 6번째 줄에 손실 가중이 찍힌다. 체크포인트에는 없다.
- **표를 손으로 옮기지 말 것** — `run_summarize_pan` 이 만든다.
- **`data/test.csv` 는 다른 장소다** (사용자 확인). 임피던스를 공유하는 적합에서
  반드시 뺀다. 나머지 11파일은 같은 곳이다.
- `vh1~vh15` 는 원자료 CSV 에만 있다 — 전처리가 `power_features` 에 `vrms` 만
  남긴다. `harmonic_offset` 이 CSV 를 직접 읽는 이유다.
