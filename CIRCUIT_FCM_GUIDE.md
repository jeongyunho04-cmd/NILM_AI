# 회로 모델과 FCM 구축 가이드

NILM 합성 데이터 생성기를 **전류원 모델**에서 **회로 기반 모델**로 전환하기 위한
작업 문서. `README_sp_model.md` 와 `APPENDIX_A_line_impedance.md` 의 후속이다.

---

## 0. 요약

지금 생성기는 각 기기를 "전력을 주면 정해진 전류를 뽑는 것"으로 다룬다.
이 모델로는 **기기끼리 서로 영향을 주는 현상**을 표현할 수 없고, 그것이
SMPS 3종 분리가 막혀 있는 원인 중 하나로 확인되었다.

회로 모델을 만들면 두 가지가 동시에 해결된다.

1. **FCM(전압 결합)** — 기기 간 상호작용을 물리적으로 생성
2. **장소 이식** — 임의의 선로 임피던스에서 서명을 재생성

해야 할 일은 크게 셋이다.

```
[A] 시뮬레이터 완성    — 현재 미완. §3 의 두 요소를 추가해야 함
[B] 파라미터 피팅      — 기기별 5개. §4
[C] 생성기 통합        — 고정점 반복. §6
```

---

## 1. 왜 필요한가

### 1.1 관측된 문제

기기들을 독립 전류원으로 보면 KCL 에 의해 전류가 정확히 더해져야 한다.
그런데 실측에서 안 더해진다.

```
                h13 전달비 (실측/모델)
충전기 단독          0.83      <- 각각은 설명됨
프로젝터 단독        1.24
충전기+프로젝터      0.64      <- 합치면 안 맞음
미니PC+충전기+프로   0.60
```

경로는 하나뿐이다.

```
A 기기의 도통 펄스 → 선로 임피던스 Z 를 통한 전압 강하
                   → 단자 전압 왜곡
                   → B 기기의 도통 시점 변화
                   → B 의 전류 변화
```

전류원 모델에는 이 되먹임을 표현할 언어가 없다.

### 1.2 입력 보정은 원리적으로 불가능하다

측정 신호에 보정 계수를 곱해 되돌리는 접근이 시도되었으나 실패했다.
이유는 수식으로 명확하다.

```
측정:      I(h) = Σᵢ Tᵢ(h) · Iᵢ(h)
입력보정:  I(h) / T(h) = Σᵢ [Tᵢ(h)/T(h)] · Iᵢ(h)
```

`Tᵢ = T` 가 모든 기기에 대해 성립할 때만 원래대로 돌아온다.
**그런데 실측에서 기기마다 다르다.** 장소 A→B 위상 회전:

```
             h3    h5    h7    h9   h11   h13   h15
충전기       +8°  +19°  +38°  +65° +109° +157° +184°
프로젝터     -1°   -1°   +2°  +14°  +29°   +2°   +7°
기기 간 차   10°   19°   36°   51°   80°  154°  177°
```

같은 배선인데 충전기는 h13 에서 157° 돌고 프로젝터는 2° 다.
어떤 단일 `T(h)` 를 골라도 한쪽은 반드시 틀린다.

그리고 각 기기의 몫을 알아야 보정할 수 있는데, 그게 지금 풀려는 문제다. 순환이다.

### 1.3 순방향으로 뒤집으면 잘 정의된 문제가 된다

```
역방향(실패):  Σ Tᵢ Iᵢ  →  Σ Iᵢ      분해가 필요 → 부정문제
순방향(제안):  Σ Iᵢ      →  Σ Tᵢ Iᵢ  각 항에 적용 → 정확
```

**입력을 고치지 말고 생성기를 고친다.** 합성 데이터를 만들 때 각 기기가
공유 임피던스를 통해 서로 영향을 주도록 시뮬레이션하면, 실측과 같은
구조의 데이터가 나온다. 분해가 필요 없으니 인수분해 문제가 사라진다.

### 1.4 새 장소 문제도 함께 풀린다

```
기기별 Tᵢ(h) 방식:  3기기 × 15차수 × 복소 = 90 파라미터, 장소마다 재측정
회로 모델 방식:     (R_line, L_line) 스칼라 2개, 자동 추정 가능
```

`Z_line` 은 대전력 부하의 on/off 계단에서 라벨 없이 추정된다
(`meas_z.py` 참조). 어떤 기기가 켜졌는지 몰라도 `ΔV/ΔI` 만 있으면 된다.

기기별 반응 차이(충전기 157° vs 프로젝터 2°)는 **회로 모델이 알아서 만들어낸다.**
`C_dc` 가 작은 충전기는 도통이 짧아 `di/dt` 가 크고, 그래서 같은 `Z` 에도
크게 반응한다. 따로 잴 필요가 없다.

---

## 2. HNE 와 FCM

용어를 정리한다. 문헌에서 혼용되므로 이 문서의 정의를 따른다.

**HNE (Harmonic Norton Equivalent)** — 모델 전체

```
I(h) = J(h) − Σₖ Y(h,k)·V(k)
       └──┘   └────────────┘
      전류원      결합항
```

**FCM (Frequency Coupling Matrix)** — 위 식의 `Y(h,k)` 행렬

```
        k=1    k=3    k=5    k=7
h=1   [ Y11    Y13    Y15    Y17 ]
h=3   [ Y31    Y33    Y35    Y37 ]
h=5   [ Y51    Y53    Y55    Y57 ]
h=7   [ Y71    Y73    Y75    Y77 ]
```

- **대각 항** `Y(h,h)`: h차 전압이 h차 전류를 바꾼다. 선형 회로도 하는 일
- **비대각 항** `Y(h,k)`, h≠k: **3차 전압이 7차 전류를 바꾼다.** 정류기 특유

비대각 항이 생기는 이유는 도통 시점이 전압 파형 **전체**에 의존하기 때문이다.

### s(p) 와의 관계

세 개념의 자리를 테일러 전개로 정리하면:

```
δI(h) = (∂I/∂p)·δp   +   Σₖ (∂I/∂V_k)·δV_k
         └─ s(p) ─┘        └─── Y = FCM ───┘
         부하축                 전압축
```

**대체재가 아니라 같은 전개의 두 항이다.** 현재 첫 항만 구현되어 있다.

### 우선순위 정정

이전 문서(`README §7.4`)에서 전압축을 낮은 우선순위로 두었는데, **그 판단은
틀렸다.** 근거였던 "계통 THD 2~3% 라 효과가 작다" 는 추정이 저차 고조파
기준이었기 때문이다. THD 는 h3~h5 가 지배하므로 고차 결합을 구조적으로
과소평가한다.

실측을 보면 고차에서 지배적이다.

```
장소 효과 신호/잡음비 (충전기)
  h5  1.0    h9  3.4    h11  5.6    h13  5.2
```

그리고 SMPS 신원 정보도 h11~h15 에 있다. **판별 정보와 결합 효과가 같은
대역에 있는 것이 문제의 구조다.**

---

## 3. 시뮬레이터

### 3.1 토폴로지

대상 3종 모두 **PFC 없는 capacitor-input rectifier** 로 확인되었다
(측정 THD 1.36~1.86, h3/h1 0.83~0.94, 정격 65W 로 EN 61000-3-2 Class D
적용 하한 75W 미만).

```
  v_src ──[ R ]──[ L ]──┬──────────────┐
                        │              │
                       C_x         브리지 (Vf + rd·i)
                        │              │
                        │         ┌────┴────┐
                        │        C_dc    정전력 부하 P
                        │         └────┬────┘
                        └──────────────┘
```

**정전력 부하가 중요하다.** SMPS 후단 DC-DC 가 출력을 조정하므로 벌크 캡
입장에서는 `i = P/v_c` 로 보인다(음의 증분 저항). 저항 부하 `v/R` 로 두면
리플 파형이 달라져 고조파가 틀어진다.

파라미터 5개:

| 기호 | 의미 | 비고 |
|---|---|---|
| `C_dc` | 벌크 커패시터 | 기기 고유 |
| `R` | 직렬 저항 | `R_line + R_dev` 합쳐서 나옴 |
| `L` | 직렬 인덕턴스 | `L_line + EMI 초크` |
| `C_x` | X-커패시터 | 기기 고유 |
| `rd` | 다이오드 동적저항 | NTC 포함 가능 |

`Vf = 1.4V` (브리지 2개 직렬) 는 고정, 피팅하지 않는다.

### 3.2 작동하는 구현

아래는 검증된 코드다. **X-cap 노드를 반음해(semi-implicit)로 적분하는 것이
핵심**으로, 명시적 적분은 `R·C_x` 시정수가 `dt` 보다 훨씬 짧아 발산한다.

```python
import numpy as np
from numba import njit

@njit(cache=True)
def _core(vsrc, dt, P, C_dc, R, L, Cx, Vf, rd, vc0):
    N = vsrc.size; iac = np.zeros(N)
    vc = vc0; iL = 0.0; vn = vsrc[0]
    a = dt/(rd*Cx) if Cx > 1e-12 else 0.0
    b = dt/Cx      if Cx > 1e-12 else 0.0
    for k in range(N):
        # 소스 -> 노드 (R, L 직렬)
        if L > 1e-9:
            iL += dt*(vsrc[k] - vn - iL*R)/L
        else:
            iL = (vsrc[k] - vn)/R
        if Cx > 1e-12:
            # 노드 전압 반음해 갱신
            if vn >= 0.0:
                vn = (vn + b*iL + a*(Vf+vc))/(1.0+a) if vn-Vf-vc > 0.0 else vn + b*iL
            else:
                vn = (vn + b*iL - a*(Vf+vc))/(1.0+a) if -vn-Vf-vc > 0.0 else vn + b*iL
            drive = abs(vn) - Vf - vc
            ib = drive/rd if drive > 0.0 else 0.0
        else:
            drive = abs(vsrc[k] - iL*R) - Vf - vc
            ib = drive/rd if drive > 0.0 else 0.0
            iL = ib if vsrc[k] >= 0.0 else -ib
        vc += dt*(ib - P/max(vc, 1.0))/C_dc      # 정전력 부하
        iac[k] = iL
    return iac


F = 60.0; NPC = 3072; NCYC = 24; NDFT = 4

def sim(P, C_dc, R, L, Cx=0.0, rd=1.0, V_rms=222.0, Vf=1.4, vsrc=None):
    """h1~h15 복소 서명 (h1 정규화). 펌웨어 위상 관례와 동일."""
    N = NCYC*NPC; dt = 1/(F*NPC)
    if vsrc is None:
        t = np.arange(N)*dt
        vsrc = np.sqrt(2)*V_rms*np.sin(2*np.pi*F*t)
    I = _core(vsrc, dt, P, C_dc, max(R,1e-3), L, Cx, Vf, max(rd,1e-3),
              np.sqrt(2)*V_rms - Vf)
    if not np.all(np.isfinite(I)):
        return np.full(15, np.nan, complex)
    sl = slice(N - NDFT*NPC, N)                  # 앞 20주기는 과도 안정화용
    Ih = np.fft.rfft(I[sl])[NDFT:NDFT*16:NDFT]
    Vh = np.fft.rfft(vsrc[sl])[NDFT:NDFT*16:NDFT]
    h = np.arange(1, 16)
    s = np.abs(Ih)*np.exp(1j*(np.angle(Ih) - h*np.angle(Vh[0])))
    return s/s[0]
```

**위상 관례 주의.** 펌웨어의 `ihdeg_h = arg(I_h) − h·arg(V_1)` 와 반드시
일치시켜야 한다. 이 항을 빼먹으면 크기는 맞는데 위상이 전부 어긋난다.
(초기 구현에서 실제로 겪은 함정이다.)

성능: numba 컴파일 후 1회 약 2.3ms. `NPC=3072` 와 `NPC=6144` 의 결과 차이는
h15 에서 0.7% 로 무시할 수준이니 3072 로 충분하다.

### 3.3 아직 미완인 부분

현재 잔차에 구조가 남아 있다. 다음 두 가지를 추가 검토할 것.

**(a) NTC 비선형성** — `rd` 를 상수로 두었는데 인러시 제한용 서미스터는
전류 의존이다. 고부하에서 잔차가 커지는 패턴이 이를 시사한다.

```
충전기 68.6W 에서   h13 |비| 0.82,  h15 |비| 0.27   (시뮬이 과소)
```

**(b) 2단 구조 가능성** — 미니PC 어댑터의 `R` 이 격자 상한에 고착된다
(5.95Ω). 선로 0.9Ω + 기기 내부로는 설명이 안 되는 크기다. 부스트
프리레귤레이터가 있을 가능성이 있다.

---

## 4. 파라미터 피팅

### 4.1 절차

`sp_curves_v2.npz` 의 곡선을 목표로 5차원 격자 탐색 후 국소 정밀화.

```python
W  = np.r_[1,1,1,1,1,.8,.8,.6,.6,.45,.45,.35,.35,.3]   # h2..h15 가중
WW = np.r_[W, W]
def ri(s): return np.r_[s[1:].real, s[1:].imag]

def score(pts, Vr, par):
    e = 0.0
    for p, s_meas in pts:
        s = sim(p, *par, V_rms=Vr)
        if not np.isfinite(s[1]): return 1e9
        d = (ri(s) - ri(s_meas))*WW
        e += float(d @ d)
    return e/len(pts)
```

핵심 세 가지.

- **h1 정규화 후 비교한다.** 안 하면 h1 이 손실을 지배해 모양이 안 맞춰진다
- **고차 가중치를 낮춘다.** h13~h15 는 SNR 이 나쁘다 (미니PC h11 변동계수 0.155)
- **전력 3~4점 이상**을 동시에 맞춘다. 한 점만 맞추면 축퇴가 심하다

### 4.2 현재까지의 결과

장소 A 곡선에 대한 피팅 결과다. **출발점으로 쓰되 §3.3 개선 후 재피팅할 것.**

| 기기 | C_dc | R | L | C_x | rd | 손실 |
|---|---|---|---|---|---|---|
| laptop_charger | 53.1 µF | 2.10 Ω | 2500 µH | 0.28 µF | 5.04 Ω | 0.0091 |
| beam_projector | 116.0 µF | 4.59 Ω | 32 µH | 0.51 µF | 8.00 Ω | 0.0020 |
| minipc | 108.0 µF | 5.95 Ω | 1000 µH | 0.44 µF | 8.00 Ω | 0.0259 |

X-cap 과 `rd` 를 추가하기 전(3파라미터) 대비 손실이 8~41배 개선되었다.

**정합성 확인:** 두 어댑터 모두 정격 65W 다.

```
LG gram ADT-65FSU-D03-EPK (USB-C PD, 2021+)   C_dc/P = 0.82 µF/W
Prime Direct SPL19342B (AC220V 전용, 2023)    C_dc/P = 1.66 µF/W
```

2배 차이인데, 독립적으로 측정한 "같은 파형 모양에 도달하는 전력이 1.5배 다름"
(`README §1(c)`) 과 방향과 크기가 일치한다.

**경계 고착 주의:** `rd` 가 두 기기에서 격자 상한(8.0Ω)에 붙어 있고,
`R` 도 미니PC 에서 상한이다. 물리적으로 과한 값이므로 §3.3 의 누락 요소를
흡수하고 있을 가능성이 높다. **이 값들을 최종으로 신뢰하지 말 것.**

### 4.3 검산 범위

| 항목 | 220V 기준 |
|---|---|
| `C_dc` | 0.3 ~ 2 µF/W |
| `R` (선로+기기) | 1 ~ 3 Ω |
| `L` | 20 ~ 500 µH (EMI 초크 있으면 더 클 수 있음) |
| `C_x` | 0.1 ~ 1 µF |
| `rd` | 0.3 ~ 3 Ω |

크게 벗어나면 토폴로지 오류이거나 누락 요소가 있다는 신호다.

---

## 5. FCM 추출

시뮬레이터가 완성되면 `J` 와 `Y` 는 유한 차분으로 나온다. **별도 프로젝트가
아니라 부산물이다.**

```python
def norton(par, p, V_base, h_max=15, rel=0.01):
    """J(h) 와 Y(h,k) 를 유한 차분으로 추출"""
    J = sim(p, *par, vsrc=to_wave(V_base))
    Y = np.zeros((h_max, h_max), complex)
    dV = rel*abs(V_base[0])
    for k in range(1, h_max+1):
        for ph in (0.0, np.pi/2):                 # 실수부·허수부 각각
            Vp = V_base.copy()
            Vp[k-1] += dV*np.exp(1j*ph)
            dI = sim(p, *par, vsrc=to_wave(Vp)) - J
            Y[:, k-1] += -(dI/(dV*np.exp(1j*ph)))/2
    return J, Y
```

15×15 에 2회씩 = 450 회 시뮬레이션, 약 1초다.

**`Y` 는 동작점 의존이다.** `Y(p)` 로 전력별로 뽑아 보간해야 한다.
`s(p)` 와 정확히 같은 구조다.

### 선형화의 한계

`Y` 는 전압 왜곡에 대한 1차 근사다. 왜곡이 크면 도통각이 크게 변해 깨진다.
계통 THD 3.7% 에서는 대체로 안전하지만, 전기포트가 켜져 `Z·I` 강하가 큰
구간(예: `test_15` 의 388~414초)에서는 위험하다.

**대안: 시뮬레이터를 고정점 안에 직접 넣는다.**

```python
I_tot = sum(sim(p, *par[d], vsrc=v_term) for d, p in powers.items())
```

정확하지만 느리다(프레임당 3회 × 3기기 × 2.3ms ≈ 21ms).
오프라인 데이터 생성이면 이쪽이 안전하고, 학습 중 실시간 생성이 필요하면
`Y` 를 쓴다.

---

## 6. 생성기 통합

### 6.1 고정점 반복

```python
def forward(powers, R_line, L_line, V_src, par, n_iter=3):
    """
    powers  : {device: watts}
    V_src   : (15,) complex 원천 전압 고조파 (또는 이상 정현파)
    반환    : (15,) complex 총 전류
    """
    h = np.arange(1, 16)
    Z = R_line + 1j*2*np.pi*60*h*L_line
    I_tot = sum(J[d](p) for d, p in powers.items())        # 초기 추정
    for _ in range(n_iter):
        V_term = V_src - Z*I_tot                           # 단자 전압
        I_tot = sum(J[d](p) - Y[d](p) @ V_term for d, p in powers.items())
    return I_tot
```

3회면 수렴한다. 이 구조에서는 **분해가 개입하지 않으므로** §1.2 의
인수분해 문제가 원천적으로 사라진다.

### 6.2 장소를 조건으로

입력 보정도, 장소별 모델도 필요 없다. `(R_line, L_line)` 을 모델 입력에
조건으로 넣는다.

```python
# 훈련: Z 를 물리적으로 반응하는 형태로 무작위화
R_line = rng.uniform(0.3, 2.5)          # 주택 배선 실측 0.88~1.56 Ω
L_line = rng.uniform(20e-6, 400e-6)
I_tot  = forward(powers, R_line, L_line, V_src, par)

# 20% 확률로 조건 마스킹 -> "Z 모름" 상태도 학습
z_cond = ZERO_TOKEN if rng.random() < 0.2 else encode(R_line, L_line)
```

배포 시:

```
설치 직후   Z 기본값 + 불변 모델로 동작
수 시간 후  meas_z.py 가 Z 확정 -> 조건 입력으로 정확도 회복
지속        Z 갱신 (계절·배선 변화 추적)
```

### 6.3 기존 지터를 대체한다

현재 h≥7 에 전역 지터를 넣어 고차를 불신하게 만드는 방식은 **판별 정보까지
같이 버린다.** 실측 신호/잡음비를 보면 h11 에서 충전기가 5.6, 프로젝터가
0.7 로 정반대인데, 전역 지터는 이 구분을 못 한다.

Z 무작위화가 물리적으로 올바른 대체다.

| | 전역 지터 | Z 무작위화 |
|---|---|---|
| 교란 방향 | 임의 (30차원 전방향) | 물리적으로 가능한 저차원 다양체 |
| 학습 결과 | "고차를 무시하라" | "Z 방향에 직교한 성분을 봐라" |
| 신원 정보 | 소실 | 보존 |

Z 변동은 `(R, L)` 두 파라미터가 만드는 부분공간만 훑는다. 나머지 방향의
기기 신원 정보는 온전히 남는다.

---

## 7. 검증 게이트

각 단계를 통과해야 다음으로 넘어간다.

| # | 게이트 | 기준 | 실패 시 |
|---|---|---|---|
| 1 | 시뮬레이터 정상성 | 도통각 30~40°, THD 90~130% (전대역 기준) | 코드 버그 |
| 2 | 파라미터 상식 범위 | §4.3 표 | 토폴로지 오류 |
| 3 | 잔차 구조 | 특정 차수·전력에 몰리지 않음 | 누락 요소 |
| 4 | **s(p) 재현** | 손실 < 0.01 | 재피팅 |
| 5 | **장소 회전 재현** | §8 참조 | 아래 |
| 6 | 실측 총전력 MAE | `eval_real.py` 로 2W 이하 | |

### 게이트 5 가 관문이다

같은 `(R_line, L_line)` 변화로 **충전기는 크게 돌고 프로젝터는 안 도는 것**이
재현되어야 한다.

```
h9~h15 평균 |회전|      충전기    프로젝터    비
관측                     127°       13°      9.8
현재 모델 예측            31°       11°      2.7
```

프로젝터는 맞는데 충전기가 4배 부족하다. **현재 미통과 상태다.**

---

## 8. 알려진 미해결 문제

### 8.1 충전기 회전 크기 (게이트 5)

직렬 임피던스 논리로는 `L` 이 작은 프로젝터(32µH)가 `ΔL` 에 더 민감해야
하는데 관측은 정반대다. **원인이 직렬 `Z` 가 아니라 전압 파형 왜곡일
가능성이 높다.**

검증하려면 이상 정현파가 아니라 **실측 전압 파형**을 `vsrc` 로 넣어야 한다.
그런데 현재 데이터에 전압 고조파 **위상이 없다**(`vh1~15` 크기만 존재).

→ 펌웨어에 `vhdeg1~15` 추가 요청이 진행 중. 이것이 도착해야 게이트 5 를
   제대로 재설계할 수 있다.

### 8.2 전압 데이터 품질

```
vh2/vh1 = 2.21%    (실계통 예상치의 4배 이상)
vh1 과의 상관 = +0.787
```

스펙트럼 누설 또는 500ms 창 평균화가 원인으로 의심된다. 펌웨어 쪽에
DFT 창 축소(500ms → 4~8사이클)를 함께 요청 중이다.

**`vhdeg` 가 오더라도 이 문제가 남아 있으면 FCM 입력이 오염된다.**

### 8.3 기각된 가설 (재시도 금지)

**측정계 시간지연 `tau`.** 3파라미터 모델 피팅에서 기기 공통으로
`-17°/h` 가 나와 측정 아티팩트로 의심했으나, 순수 저항 부하(전기포트,
핫플레이트) 10개 이벤트의 기본파 위상이 **0.63° ± 1.26°** 로 측정되어
기각되었다.

```
직접 측정   Δτ = 0.029 ms   →  +0.6 °/h
피팅 추정   Δτ = 0.79 ms    →  -17.0 °/h      28배 차이, 부호 반대
```

`tau` 는 회로 모델의 누락 요소(X-cap, 다이오드 저항)를 흡수한 인공
파라미터였다. **피팅에 `tau` 자유 파라미터를 넣지 말 것.** 넣으면 진짜
결함이 가려진다.

교훈: "기기 공통이므로 측정 아티팩트" 라는 추론은 성립하지 않는다.
같은 토폴로지의 기기들은 같은 모델 결함을 공유한다. 반드시 **물리적으로
다른 부하**(저항성)를 대조군으로 쓸 것.

### 8.4 선로 임피던스 실측값

`meas_z.py` 로 83개 이벤트에서 측정됨.

```
장소 1 (test_11, 12)      1.53 Ω
장소 2 (test_15~18)       0.922 ± 0.037 Ω        비 1.66배
장소 A (test_8, 13)       미측정 (대전력 계단 없음)
```

**앞으로 모든 측정 세션에 전기포트 on/off 를 3회 이상 포함할 것.**
비용 2분에 그 세션의 `Z` 가 영구히 기록된다.

---

## 9. 작업 순서

```
1. §3.3 의 누락 요소 검토 (NTC 비선형, 2단 구조)      반나절
2. 재피팅 -> 게이트 2,3,4 통과 확인                    1일
3. vhdeg 도착 대기 / 도착 후 실측 전압 파형으로 게이트 5 재설계
4. 게이트 5 통과 시 J(p), Y(h,k,p) 추출                2시간
5. 생성기 고정점 통합 (§6)                             반나절
6. Z 무작위화로 지터 대체 (§6.3)                       2시간
7. 재학습 -> eval_real.py 로 평가                      
```

3번이 외부 의존이므로, 그동안 1~2 를 진행하면 된다.

`vhdeg` 없이도 §1.3 의 순방향 전환과 §6.3 의 Z 무작위화는 가능하다.
전압축(FCM)만 대기 상태다.

---

## 10. 참고 파일

```
sp_curves_v2.npz            피팅 목표 곡선 (기기 3종 + 배경)
sp_model.py                 곡선 로더
meas_z.py                   선로 임피던스 추정
delta_sig.py                이벤트 델타 서명 추출
eval_real.py                실측 혼합 데이터 평가
README_sp_model.md          s(p) 모델 전체 문서
APPENDIX_A_line_impedance.md  장소 간 임피던스 영향 측정 결과
```

**채점 지표는 두 개만 쓸 것** — 총전력 재현 MAE 와 구간 on/off 균형정확도.
GT JSON 의 `delta_p_w` 로 채점하면 모델이 아니라 라벨 오차를 재게 된다
(누적하면 종료 시점에 −60W 가 나온다. `README §6.4` 참조).
