# 회로 모델과 FCM 구축 가이드

NILM 합성 데이터 생성기를 **전류원 모델**에서 **회로 기반 모델**로 전환하기 위한
작업 문서. `README_sp_model.md` 와 `APPENDIX_A_line_impedance.md` 의 후속이다.

---

## 0. 요약

지금 생성기는 각 기기를 "전력을 주면 정해진 전류를 뽑는 것"으로 다룬다.
이 모델로는 **기기끼리 서로 영향을 주는 현상**을 표현할 수 없고, 그것이
SMPS 3종 분리가 막혀 있는 원인 중 하나로 확인되었다.

회로 모델을 만들면 두 가지가 동시에 해결된다.

1. **FCM(전압 결합)** — 기기 간 상호작용을 물리적으로 생성
2. **장소 이식** — 임의의 선로 임피던스에서 서명을 재생성

해야 할 일은 크게 셋이다.

```
[A] 시뮬레이터 완성    — X-cap, 다이오드 저항 추가 완료. §3
[B] 파라미터 피팅      — 원시 파형 파이프라인 `fit_raw.py`. §10  ← 2026-09 갱신
[C] 생성기 통합        — `fcm.py forward()`. 시뮬레이터 직접 호출. §5, §6
```

**FCM 검증 (2026-09):** `Y` 행렬 추출은 되지만 선형화 `J − Y·ΔV` 가 V3 1%
섭동에서도 h9 이상 25~70% 틀린다. 생성기는 시뮬레이터를 고정점 안에서
직접 호출하는 것으로 확정(0.1초/프레임). `Y` 는 진단용.

**2026-09 갱신:** 원시 파형 스냅샷이 확보되어 피팅 방법이 격자 탐색(30분)에서
`Cx 직접측정 → Z변환 초기값 → 시간영역 정밀화`(4~20초)로 바뀌었다. 3종 모두
14개 동작점에서 파형 RMS 6~17%로 검증됨. 설계 근거는 §10.

---

## 1. 왜 필요한가

### 1.1 관측된 문제

기기들을 독립 전류원으로 보면 KCL 에 의해 전류가 정확히 더해져야 한다.
그런데 실측에서 안 더해진다.

```
                h13 전달비 (실측/모델)
충전기 단독          0.83      <- 각각은 설명됨
프로젝터 단독        1.24
충전기+프로젝터      0.64      <- 합치면 안 맞음
미니PC+충전기+프로   0.60
```

경로는 하나뿐이다.

```
A 기기의 도통 펄스 → 선로 임피던스 Z 를 통한 전압 강하
                   → 단자 전압 왜곡
                   → B 기기의 도통 시점 변화
                   → B 의 전류 변화
```

전류원 모델에는 이 되먹임을 표현할 언어가 없다.

### 1.2 입력 보정은 원리적으로 불가능하다

측정 신호에 보정 계수를 곱해 되돌리는 접근이 시도되었으나 실패했다.
이유는 수식으로 명확하다.

```
측정:      I(h) = Σᵢ Tᵢ(h) · Iᵢ(h)
입력보정:  I(h) / T(h) = Σᵢ [Tᵢ(h)/T(h)] · Iᵢ(h)
```

`Tᵢ = T` 가 모든 기기에 대해 성립할 때만 원래대로 돌아온다.
**그런데 실측에서 기기마다 다르다.** 장소 A→B 위상 회전:

```
             h3    h5    h7    h9   h11   h13   h15
충전기       +8°  +19°  +38°  +65° +109° +157° +184°
프로젝터     -1°   -1°   +2°  +14°  +29°   +2°   +7°
기기 간 차   10°   19°   36°   51°   80°  154°  177°
```

같은 배선인데 충전기는 h13 에서 157° 돌고 프로젝터는 2° 다.
어떤 단일 `T(h)` 를 골라도 한쪽은 반드시 틀린다.

> **주의 (2026-09 정정):** 위 표는 장소 A→B 이벤트 델타(파일당 1~5개, 잡음 큼)
> 기준이다. 더 깨끗한 장소 A→C 단독 곡선 비교에서 충전기 회전이 "4배 크다"고
> 나온 것은 `laptop_charger_5C.csv` 의 **전류 위상 7샘플 오류** 때문이었고,
> 원시 스냅샷으로 정정하면 충전기와 프로젝터는 거의 같게 돈다(§10.8).
> A→B 의 157° 도 같은 종류의 정렬 오류일 수 있으므로 재검증 전까지 **인용
> 금지**. 다만 "기기별 `T` 가 다르다"는 논지 자체는 §10.8 의 정정 데이터에서도
> 유지된다(미니PC 가 나머지 둘보다 2배 돈다).

그리고 각 기기의 몫을 알아야 보정할 수 있는데, 그게 지금 풀려는 문제다. 순환이다.

### 1.3 순방향으로 뒤집으면 잘 정의된 문제가 된다

```
역방향(실패):  Σ Tᵢ Iᵢ  →  Σ Iᵢ      분해가 필요 → 부정문제
순방향(제안):  Σ Iᵢ      →  Σ Tᵢ Iᵢ  각 항에 적용 → 정확
```

**입력을 고치지 말고 생성기를 고친다.** 합성 데이터를 만들 때 각 기기가
공유 임피던스를 통해 서로 영향을 주도록 시뮬레이션하면, 실측과 같은
구조의 데이터가 나온다. 분해가 필요 없으니 인수분해 문제가 사라진다.

### 1.4 새 장소 문제도 함께 풀린다

```
기기별 Tᵢ(h) 방식:  3기기 × 15차수 × 복소 = 90 파라미터, 장소마다 재측정
회로 모델 방식:     (R_line, L_line) 스칼라 2개, 자동 추정 가능
```

`Z_line` 은 대전력 부하의 on/off 계단에서 라벨 없이 추정된다
(`meas_z.py` 참조). 어떤 기기가 켜졌는지 몰라도 `ΔV/ΔI` 만 있으면 된다.

기기별 반응 차이(충전기 157° vs 프로젝터 2°)는 **회로 모델이 알아서 만들어낸다.**
`C_dc` 가 작은 충전기는 도통이 짧아 `di/dt` 가 크고, 그래서 같은 `Z` 에도
크게 반응한다. 따로 잴 필요가 없다.

---

## 2. HNE 와 FCM

용어를 정리한다. 문헌에서 혼용되므로 이 문서의 정의를 따른다.

**HNE (Harmonic Norton Equivalent)** — 모델 전체

```
I(h) = J(h) − Σₖ Y(h,k)·V(k)
       └──┘   └────────────┘
      전류원      결합항
```

**FCM (Frequency Coupling Matrix)** — 위 식의 `Y(h,k)` 행렬

```
        k=1    k=3    k=5    k=7
h=1   [ Y11    Y13    Y15    Y17 ]
h=3   [ Y31    Y33    Y35    Y37 ]
h=5   [ Y51    Y53    Y55    Y57 ]
h=7   [ Y71    Y73    Y75    Y77 ]
```

- **대각 항** `Y(h,h)`: h차 전압이 h차 전류를 바꾼다. 선형 회로도 하는 일
- **비대각 항** `Y(h,k)`, h≠k: **3차 전압이 7차 전류를 바꾼다.** 정류기 특유

비대각 항이 생기는 이유는 도통 시점이 전압 파형 **전체**에 의존하기 때문이다.

### s(p) 와의 관계

세 개념의 자리를 테일러 전개로 정리하면:

```
δI(h) = (∂I/∂p)·δp   +   Σₖ (∂I/∂V_k)·δV_k
         └─ s(p) ─┘        └─── Y = FCM ───┘
         부하축                 전압축
```

**대체재가 아니라 같은 전개의 두 항이다.** 현재 첫 항만 구현되어 있다.

### 우선순위 정정

이전 문서(`README §7.4`)에서 전압축을 낮은 우선순위로 두었는데, **그 판단은
틀렸다.** 근거였던 "계통 THD 2~3% 라 효과가 작다" 는 추정이 저차 고조파
기준이었기 때문이다. THD 는 h3~h5 가 지배하므로 고차 결합을 구조적으로
과소평가한다.

실측을 보면 고차에서 지배적이다.

```
장소 효과 신호/잡음비 (충전기)
  h5  1.0    h9  3.4    h11  5.6    h13  5.2
```

그리고 SMPS 신원 정보도 h11~h15 에 있다. **판별 정보와 결합 효과가 같은
대역에 있는 것이 문제의 구조다.**

---

## 3. 시뮬레이터

### 3.1 토폴로지

대상 3종 모두 **PFC 없는 capacitor-input rectifier** 로 확인되었다
(측정 THD 1.36~1.86, h3/h1 0.83~0.94, 정격 65W 로 EN 61000-3-2 Class D
적용 하한 75W 미만).

```
  v_src ──[ R ]──[ L ]──┬──────────────┐
                        │              │
                       C_x         브리지 (Vf + rd·i)
                        │              │
                        │         ┌────┴────┐
                        │        C_dc    정전력 부하 P
                        │         └────┬────┘
                        └──────────────┘
```

**정전력 부하가 중요하다.** SMPS 후단 DC-DC 가 출력을 조정하므로 벌크 캡
입장에서는 `i = P/v_c` 로 보인다(음의 증분 저항). 저항 부하 `v/R` 로 두면
리플 파형이 달라져 고조파가 틀어진다.

파라미터 5개:

| 기호 | 의미 | 비고 |
|---|---|---|
| `C_dc` | 벌크 커패시터 | 기기 고유 |
| `R` | 직렬 저항 | `R_line + R_dev` 합쳐서 나옴 |
| `L` | 직렬 인덕턴스 | `L_line + EMI 초크` |
| `C_x` | X-커패시터 | 기기 고유 |
| `rd` | 다이오드 동적저항 | NTC 포함 가능 |

`Vf = 1.4V` (브리지 2개 직렬) 는 고정, 피팅하지 않는다.

### 3.2 작동하는 구현

아래는 검증된 코드다. **X-cap 노드를 반음해(semi-implicit)로 적분하는 것이
핵심**으로, 명시적 적분은 `R·C_x` 시정수가 `dt` 보다 훨씬 짧아 발산한다.

```python
import numpy as np
from numba import njit

@njit(cache=True)
def _core(vsrc, dt, P, C_dc, R, L, Cx, Vf, rd, vc0):
    N = vsrc.size; iac = np.zeros(N)
    vc = vc0; iL = 0.0; vn = vsrc[0]
    a = dt/(rd*Cx) if Cx > 1e-12 else 0.0
    b = dt/Cx      if Cx > 1e-12 else 0.0
    for k in range(N):
        # 소스 -> 노드 (R, L 직렬)
        if L > 1e-9:
            iL += dt*(vsrc[k] - vn - iL*R)/L
        else:
            iL = (vsrc[k] - vn)/R
        if Cx > 1e-12:
            # 노드 전압 반음해 갱신
            if vn >= 0.0:
                vn = (vn + b*iL + a*(Vf+vc))/(1.0+a) if vn-Vf-vc > 0.0 else vn + b*iL
            else:
                vn = (vn + b*iL - a*(Vf+vc))/(1.0+a) if -vn-Vf-vc > 0.0 else vn + b*iL
            drive = abs(vn) - Vf - vc
            ib = drive/rd if drive > 0.0 else 0.0
        else:
            drive = abs(vsrc[k] - iL*R) - Vf - vc
            ib = drive/rd if drive > 0.0 else 0.0
            iL = ib if vsrc[k] >= 0.0 else -ib
        vc += dt*(ib - P/max(vc, 1.0))/C_dc      # 정전력 부하
        iac[k] = iL
    return iac


F = 60.0; NPC = 3072; NCYC = 24; NDFT = 4

def sim(P, C_dc, R, L, Cx=0.0, rd=1.0, V_rms=222.0, Vf=1.4, vsrc=None):
    """h1~h15 복소 서명 (h1 정규화). 펌웨어 위상 관례와 동일."""
    N = NCYC*NPC; dt = 1/(F*NPC)
    if vsrc is None:
        t = np.arange(N)*dt
        vsrc = np.sqrt(2)*V_rms*np.sin(2*np.pi*F*t)
    I = _core(vsrc, dt, P, C_dc, max(R,1e-3), L, Cx, Vf, max(rd,1e-3),
              np.sqrt(2)*V_rms - Vf)
    if not np.all(np.isfinite(I)):
        return np.full(15, np.nan, complex)
    sl = slice(N - NDFT*NPC, N)                  # 앞 20주기는 과도 안정화용
    Ih = np.fft.rfft(I[sl])[NDFT:NDFT*16:NDFT]
    Vh = np.fft.rfft(vsrc[sl])[NDFT:NDFT*16:NDFT]
    h = np.arange(1, 16)
    s = np.abs(Ih)*np.exp(1j*(np.angle(Ih) - h*np.angle(Vh[0])))
    return s/s[0]
```

**위상 관례 주의.** 펌웨어의 `ihdeg_h = arg(I_h) − h·arg(V_1)` 와 반드시
일치시켜야 한다. 이 항을 빼먹으면 크기는 맞는데 위상이 전부 어긋난다.
(초기 구현에서 실제로 겪은 함정이다.)

성능: numba 컴파일 후 1회 약 2.3ms. `NPC=3072` 와 `NPC=6144` 의 결과 차이는
h15 에서 0.7% 로 무시할 수준이니 3072 로 충분하다.

### 3.3 아직 미완인 부분

현재 잔차에 구조가 남아 있다. 다음 두 가지를 추가 검토할 것.

**(a) NTC 비선형성** — `rd` 를 상수로 두었는데 인러시 제한용 서미스터는
전류 의존이다. 고부하에서 잔차가 커지는 패턴이 이를 시사한다.

> **정정 (2026-09):** 원시 파형 다점 시간영역 피팅에서 미니PC의 NTC 항이
> 축퇴했다(`I0 → ∞`, 즉 상수 저항). 고조파 격자 피팅에서 NTC가 도움됐던 것은
> 다른 결함(`Cx` 2배 오류, 오염된 전압)을 흡수한 것이었다. §10.6 참조.

**(b) 2단 구조 가능성** — 미니PC 어댑터의 `R` 이 격자 상한에 고착된다
(5.95Ω). 선로 0.9Ω + 기기 내부로는 설명이 안 되는 크기다. 부스트
프리레귤레이터가 있을 가능성이 있다.

---

## 4. 파라미터 피팅

### 4.1 절차

`sp_curves_v2.npz` 의 곡선을 목표로 5차원 격자 탐색 후 국소 정밀화.

```python
W  = np.r_[1,1,1,1,1,.8,.8,.6,.6,.45,.45,.35,.35,.3]   # h2..h15 가중
WW = np.r_[W, W]
def ri(s): return np.r_[s[1:].real, s[1:].imag]

def score(pts, Vr, par):
    e = 0.0
    for p, s_meas in pts:
        s = sim(p, *par, V_rms=Vr)
        if not np.isfinite(s[1]): return 1e9
        d = (ri(s) - ri(s_meas))*WW
        e += float(d @ d)
    return e/len(pts)
```

핵심 세 가지.

- **h1 정규화 후 비교한다.** 안 하면 h1 이 손실을 지배해 모양이 안 맞춰진다
- **고차 가중치를 낮춘다.** h13~h15 는 SNR 이 나쁘다 (미니PC h11 변동계수 0.155)
- **전력 3~4점 이상**을 동시에 맞춘다. 한 점만 맞추면 축퇴가 심하다

### 4.2 현재까지의 결과

**원시 파형 파이프라인(§10) 결과 — 이것을 쓴다.**

| 기기 | 토폴로지 | C_dc | R | L | C_x | rd | 동작점 | 파형 RMS |
|---|---|---|---|---|---|---|---|---|
| laptop_charger | v3 | 66.3 µF | 5.06 Ω | 988 µH | 0.164 µF | 0.43 Ω | 8 (19~73W) | 7.0~15.6% |
| beam_projector | v3 | 53.3 µF | 7.18 Ω | 711 µH | 0.240 µF | 0.30 Ω | 2 (49W) | 6.3~6.5% |
| minipc | v5 | 36.6 µF | 11.70 Ω | 2785 µH | 0.337 µF | ~0 | 6 (9~29W) | 7.0~16.6% |

`R` 은 `R_line(0.48Ω) + R_dev` 합이다. 미니PC의 `R2 + R_cold` 는 상수 11.7Ω으로
축퇴(NTC 불필요).

**이전 격자 탐색 결과 (참고용, 사용 금지):**

| 기기 | C_dc | R | L | C_x | rd |
|---|---|---|---|---|---|
| laptop_charger | 86.8 µF | 1.10 Ω | 2372 µH | 0.44 µF | 1.75 Ω |
| beam_projector | 58.9 µF | 1.85 Ω | 147 µH | 0.44 µF | 4.42 Ω |
| minipc | 62.4 µF | 1.86 Ω | 60 µH | 0.46 µF | 4.50 Ω |

`C_x` 가 세 기기 모두 0.44µF 로 같은 것이 축퇴의 증거였다. 직접 측정하니
0.16~0.34µF 로 2배 작았고, 그것을 고정하자 `L` 이 2372/147/60 에서
988/711/2785 로 재배치됐다. 고조파 30개만으로는 5개 파라미터가 분리되지 않는다.

**정합성:** 두 어댑터 모두 정격 65W.

```
LG gram ADT-65FSU-D03-EPK (USB-C PD, 2021+)   C_dc/P = 1.02 µF/W
Prime Direct SPL19342B (AC220V 전용, 2023)    C_dc/P = 0.56 µF/W
```

### 4.3 검산 범위

| 항목 | 220V 기준 |
|---|---|
| `C_dc` | 0.3 ~ 2 µF/W |
| `R` (선로+기기) | 1 ~ 3 Ω |
| `L` | 20 ~ 500 µH (EMI 초크 있으면 더 클 수 있음) |
| `C_x` | 0.1 ~ 1 µF |
| `rd` | 0.3 ~ 3 Ω |

크게 벗어나면 토폴로지 오류이거나 누락 요소가 있다는 신호다.

---

## 5. FCM 추출 — 구현 및 검증 완료 (2026-09)

`fcm.py` 가 `circ_<device>.pkl` 을 읽어 유한 차분으로 `J(h)`, `Y(h,k)` 를 뽑는다.
15×15 에 실·허 섭동 2회 = 홀수 열 8개 × 2 = 16회 시뮬레이션, 기기당 0.1~0.8초.

```python
from fcm import FCM
f = FCM.from_pickle('circ_laptop_charger.pkl')
J, Y = f.extract(p=45.0, V_base=V15)     # V15: (15,) complex, 홀수만
```

### 5.1 추출된 구조 (장소 C 전압, vh3 = 4.2%)

```
|Y_hh|·V1/I1        h1     h3     h5     h7        (대각: h차 전압 1% → h차 전류가 I1 의 몇 %)
충전기 45W         0.11    8.5   20.8   30.8
프로젝터 49W       0.26    6.7   15.9   23.3
미니PC 18W         0.16    8.2   20.1   30.0

최대 비대각        충전기 Y[11,13] = 36.5,   프로젝터 Y[9,11] = 26.4,   미니PC Y[9,11] = 35.7
```

기본파는 거의 반응하지 않고(0.1~0.3), 고차로 갈수록 결합이 폭발적으로 커진다.
§2 의 "판별 정보와 결합 효과가 같은 대역(h11~h15)에 있다" 가 수치로 확인됨.

### 5.2 선형화는 쓸 수 없다

`I ≈ J − Y·ΔV` 의 정확도를 시뮬레이터와 비교:

```
섭동            변화량 (h3 / h9)     선형화 오차 (h3 / h9 / h13)
V3 +1%          15% / 64%           7% / 27% / 70%
V3 +3%          39% / 164%          18% / 67% / 98%
V3 위상 +20°    10% / 39%           13% / 36% / 49%
```

**V3 를 1% 만 바꿔도 h9 이상에서 25~70% 틀린다.** 도통각이 전압 왜곡에
초선형으로 반응하기 때문이다. Xie 2024 §I 가 경고한 "전압 왜곡이 크면 Norton
모델 오차가 커진다" 가 우리 조건(계통 vh3 4%, 장소 간 차이 2%)에 해당한다.

**따라서 생성기는 `Y` 를 쓰지 않는다.** 시뮬레이터를 고정점 안에서 직접
호출한다(§6). 3기기 × 3반복이 0.1초라 빠르기 손해도 없다.

`Y` 의 용도는 두 가지로 한정된다.
- 감도 구조 분석 (어느 차수 전압이 어느 차수 전류를 흔드는가)
- 판별력 진단: `gain = (d @ (Y_laptop − Y_minipc) @ dV) / (d @ d)`

## 6. 생성기 통합

### 6.1 고정점 반복 — 시뮬레이터 직접 호출

```python
from fcm import FCM, forward
M = {d: FCM.from_pickle('circ_%s.pkl' % d) for d in devices}
I_total, V_term = forward({'laptop_charger': 45, 'minipc': 18, 'beam_projector': 49},
                          R_line=0.48, L_line=100e-6, V_src=V15, models=M, n_iter=3)
```

내부:

```
I_tot = Σᵢ simᵢ(pᵢ, V_src)
반복 3회:
    V_term = V_src − Z(h)·I_tot          Z(h) = R + jωhL
    I_tot  = Σᵢ simᵢ(pᵢ, V_term)
```

2회면 수렴한다(fcm_check.py: n=2 와 n=5 의 I13 차이 0.3%). 분해가 개입하지
않으므로 §1.2 의 인수분해 문제가 원천적으로 없다.

**결합 효과 크기** (장소 C, Z=0.48Ω+100µH, 3기기 112W, 독립 합 대비):

```
         h7      h9     h11     h13
|비|   0.970   0.927   0.772   1.067
Δ위상   -4°     -7°    -11°    +25°
```

h11 에서 23%, h13 위상 25°. 12.181.4 의 "합이 인수분해 안 됨" 이 모델에서
재현된다. h7 이하는 3% 미만이라 무시해도 된다.

### 6.2 장소를 조건으로

입력 보정도, 장소별 모델도 필요 없다. `(R_line, L_line)` 을 모델 입력에
조건으로 넣는다.

```python
# 훈련: Z 를 물리적으로 반응하는 형태로 무작위화
R_line = rng.uniform(0.3, 2.5)          # 주택 배선 실측 0.88~1.56 Ω
L_line = rng.uniform(20e-6, 400e-6)
I_tot  = forward(powers, R_line, L_line, V_src, par)

# 20% 확률로 조건 마스킹 -> "Z 모름" 상태도 학습
z_cond = ZERO_TOKEN if rng.random() < 0.2 else encode(R_line, L_line)
```

배포 시:

```
설치 직후   Z 기본값 + 불변 모델로 동작
수 시간 후  meas_z.py 가 Z 확정 -> 조건 입력으로 정확도 회복
지속        Z 갱신 (계절·배선 변화 추적)
```

### 6.3 기존 지터를 대체한다

현재 h≥7 에 전역 지터를 넣어 고차를 불신하게 만드는 방식은 **판별 정보까지
같이 버린다.** 실측 신호/잡음비를 보면 h11 에서 충전기가 5.6, 프로젝터가
0.7 로 정반대인데, 전역 지터는 이 구분을 못 한다.

Z 무작위화가 물리적으로 올바른 대체다.

| | 전역 지터 | Z 무작위화 |
|---|---|---|
| 교란 방향 | 임의 (30차원 전방향) | 물리적으로 가능한 저차원 다양체 |
| 학습 결과 | "고차를 무시하라" | "Z 방향에 직교한 성분을 봐라" |
| 신원 정보 | 소실 | 보존 |

Z 변동은 `(R, L)` 두 파라미터가 만드는 부분공간만 훑는다. 나머지 방향의
기기 신원 정보는 온전히 남는다.

---

## 7. 검증 게이트

각 단계를 통과해야 다음으로 넘어간다.

| # | 게이트 | 기준 | 실패 시 |
|---|---|---|---|
| 1 | 시뮬레이터 정상성 | 도통각 30~40°, THD 90~130% (전대역 기준) | 코드 버그 |
| 2 | 파라미터 상식 범위 | §4.3 표 | 토폴로지 오류 |
| 3 | 잔차 구조 | 특정 차수·전력에 몰리지 않음 | 누락 요소 |
| 4 | **s(p) 재현** | 손실 < 0.01 | 재피팅 |
| 5 | **장소 회전 재현** | §8 참조 | 아래 |
| 6 | 실측 총전력 MAE | `eval_real.py` 로 2W 이하 | |

### 게이트 5 가 관문이다

같은 `(R_line, L_line)` 변화로 **충전기는 크게 돌고 프로젝터는 안 도는 것**이
재현되어야 한다.

```
h9~h15 평균 |회전|      충전기    프로젝터    비
관측                     127°       13°      9.8
현재 모델 예측            31°       11°      2.7
```

프로젝터는 맞는데 충전기가 4배 부족하다. **현재 미통과 상태다.**

---

## 8. 알려진 미해결 문제

### 8.1 충전기 회전 크기 (게이트 5) — 해결됨

**정정 (2026-09):** "충전기 회전이 모델 예측의 4배" 라는 문제는 존재하지
않았다. 장소 C 의 충전기 2Hz 파일(`laptop_charger_5C.csv`)에 전류 DFT 창이
전압 창보다 **7샘플(0.46ms) 늦게 정렬된 오류**가 있었고, 이것이 모든 고조파
위상에 `−10°×h` 를 더해 회전을 크게 보이게 했다.

발견 경로: 원시 스냅샷 5개(두 세션)의 FFT 고조파가 이 2Hz 파일과 정확히
같은 `+7` 샘플 오프셋으로 어긋났다. 프로젝터·미니PC 의 2Hz 파일(`_4C`)은
원시와 일치한다. 계측기가 기기별로 다른 타이밍을 가질 수 없으므로 2Hz 파일
하나의 세션 오류로 확정.

정정 후 A→C 회전 (원시 스냅샷 기준):

```
             h3    h5    h7    h9   h11   h13
충전기      -11   -22   -31   -40   -47   -49
프로젝터    -13   -24   -34   -39   -39   -39
미니PC      -20   -37   -51   -70   -79   -96
```

충전기와 프로젝터가 같은 크기로 돈다. 회로 모델의 예측은 충전기 12.3°,
프로젝터 16.1°, 미니PC 27.5° (RMS). **게이트 통과.**

교훈: 2Hz 고조파 세션마다 V–I 정렬이 다를 수 있다. 세션에 저항 부하
이벤트(전기포트) 한 번을 넣어 `ihdeg1 ≈ 0` 을 확인하는 것을 절차에 넣을 것.

### 8.2 전압 데이터 품질 — 해결됨

`vh2/vh1 ≈ 2.8%` 는 **아날로그 전단의 덧셈 짝수 성분**으로 확정(§10.3).
DFT 누설이 아니다. 홀수 고조파는 오염되지 않았고, 짝수는 반파 대칭화로
정확히 제거된다. FCM 은 홀수만 쓰므로 영향 없음.

### 8.3 기각된 가설 (재시도 금지)

**측정계 시간지연 `tau`.** 3파라미터 모델 피팅에서 기기 공통으로
`-17°/h` 가 나와 측정 아티팩트로 의심했으나, 순수 저항 부하(전기포트,
핫플레이트) 10개 이벤트의 기본파 위상이 **0.63° ± 1.26°** 로 측정되어
기각되었다.

```
직접 측정   Δτ = 0.029 ms   →  +0.6 °/h
피팅 추정   Δτ = 0.79 ms    →  -17.0 °/h      28배 차이, 부호 반대
```

`tau` 는 회로 모델의 누락 요소(X-cap, 다이오드 저항)를 흡수한 인공
파라미터였다. **피팅에 `tau` 자유 파라미터를 넣지 말 것.** 넣으면 진짜
결함이 가려진다.

교훈: "기기 공통이므로 측정 아티팩트" 라는 추론은 성립하지 않는다.
같은 토폴로지의 기기들은 같은 모델 결함을 공유한다. 반드시 **물리적으로
다른 부하**(저항성)를 대조군으로 쓸 것.

### 8.4 선로 임피던스 실측값

`meas_z.py` 로 83개 이벤트에서 측정됨.

```
장소 1 (test_11, 12)      1.53 Ω
장소 2 (test_15~18)       0.922 ± 0.037 Ω        비 1.66배
장소 A (test_8, 13)       미측정 (대전력 계단 없음)
```

**앞으로 모든 측정 세션에 전기포트 on/off 를 3회 이상 포함할 것.**
비용 2분에 그 세션의 `Z` 가 영구히 기록된다.

---

## 9. 작업 순서

```
1. §3.3 의 누락 요소 검토 (NTC 비선형, 2단 구조)      반나절
2. 재피팅 -> 게이트 2,3,4 통과 확인                    1일
3. vhdeg 도착 대기 / 도착 후 실측 전압 파형으로 게이트 5 재설계
4. 게이트 5 통과 시 J(p), Y(h,k,p) 추출                2시간
5. 생성기 고정점 통합 (§6)                             반나절
6. Z 무작위화로 지터 대체 (§6.3)                       2시간
7. 재학습 -> eval_real.py 로 평가                      
```

3번이 외부 의존이므로, 그동안 1~2 를 진행하면 된다.

`vhdeg` 없이도 §1.3 의 순방향 전환과 §6.3 의 Z 무작위화는 가능하다.
전압축(FCM)만 대기 상태다.

---

## 10. 참고 파일

```
sp_curves_v2.npz            피팅 목표 곡선 (기기 3종 + 배경)
sp_model.py                 곡선 로더
meas_z.py                   선로 임피던스 추정
delta_sig.py                이벤트 델타 서명 추출
eval_real.py                실측 혼합 데이터 평가
README_sp_model.md          s(p) 모델 전체 문서
APPENDIX_A_line_impedance.md  장소 간 임피던스 영향 측정 결과
```

**채점 지표는 두 개만 쓸 것** — 총전력 재현 MAE 와 구간 on/off 균형정확도.
GT JSON 의 `delta_p_w` 로 채점하면 모델이 아니라 라벨 오차를 재게 된다
(누적하면 종료 시점에 −60W 가 나온다. `README §6.4` 참조).

---

## 10. 원시 파형 파이프라인 — 설계 근거

`fit_raw.py` 가 왜 이 순서로 만들어졌는지, 각 단계가 어떤 실패에서 나왔는지
기록한다. 이 절은 **다른 사람이 같은 실패를 반복하지 않도록** 쓴 것이다.

```
python fit_raw.py <device> raw_1.csv raw_2.csv ...  [--topo v3|v5]

 [1] Cx 직접측정     비도통 구간 선형 회귀           1 ms
 [2] Z변환 초기값     동작점별 선형 회귀 → 중앙값     1 ms
 [3] 시간영역 정밀화   Nelder-Mead 1회               4~20 s
 [4] 검증            동작점별 파형 RMS, 고조파 비
```

### 10.1 왜 원시 파형인가

고조파 15차 복소값(30개 실수)은 파형 256샘플의 **주기 전체 적분**이다.
도통 구간(약 37°)과 비도통 구간(약 323°)이 섞여 있어, 도통 중에만 성립하는
선형 관계가 지워진다. h15 재구성으로 되살리려 하면 900Hz 대역제한과
Gibbs 링잉으로 펄스 에지가 뭉개진다 — 펄스 폭 37° 를 h15 분해능 12° 로
보면 3칸이다.

실측 결과: h15 절단 데이터로 Z변환을 돌리면 `R_ac = 1870Ω` 처럼 무의미한
값이 나온다. 원시 파형에서는 1.5Ω 근처로 복원된다.

**단, 추론에는 원시 파형이 필요 없다.** 모델을 만들 때(기기당 1회, 벤치)만
필요하고, 실시간 입력은 지금처럼 고조파다.

### 10.2 왜 Cx 를 먼저 직접 재는가

5차원 고조파 격자 피팅에서 `C_x` 가 세 기기 모두 0.44µF 로 같게 나왔다.
축퇴의 증거다. `C_x` 는 다른 파라미터(특히 `L`)와 트레이드오프 관계라
고조파만으로 분리되지 않는다.

원시 파형에서는 물리적으로 분리된다. 비도통 구간에서 정류기 전류는 0 이고
남는 것은 `Cx·dV/dt` 뿐이다.

```
                격자(고조파)     직접측정 (상관)
충전기          0.44 µF        0.164 µF (0.954)
프로젝터        0.44 µF        0.240 µF (0.961)
미니PC          0.46 µF        0.337 µF (0.365)   ← 9W 라 신호 약함
```

표준 X-cap 값(0.1, 0.22, 0.33, 0.47µF)에 훨씬 가깝다. 이것을 고정하니 `L`
이 제자리를 찾았다(충전기 2372→988, 프로젝터 147→711µH).

미니PC 저전력에서는 X-cap 전류(0.04A)가 정류 펄스(0.26A)의 15% 라
비도통 구간 판정이 흔들린다. 그래서 `measure_cx()` 는 고전력 동작점을
우선 쓰고, 실패하면 펄스 밖 전 구간으로 회귀한다.

### 10.3 왜 전압을 반파 대칭화하는가

측정된 전압에 `vh2 ≈ 2.8%∠-12°` 가 세 장소 모두 일정하게 있었다. 원인
규명 과정:

| 가설 | 검증 | 결과 |
|---|---|---|
| DFT 스펙트럼 누설 | 원시 파형에서 직접 DFT | 그대로 2.8% → **기각** |
| ADC 비선형 | 두 ADC 읽기 `v_r1`, `v_r2` 비교 | 8카운트 이내 일치 → **기각** |
| 전류 채널도 오염 | CT 두 게인 경로 | 양/음 비 0.994 → 대칭, **깨끗** |
| 곱셈 비선형 `v + a·v²` | 2차 다항식 보정 후 정류기 모델 | 펄스 폭 35°→18° 로 **모델 붕괴** |
| 조각별 이득 | 음/양 비의 진폭 의존 | 저진폭 1.12, 고진폭 0.935 → **기각** |
| **덧셈 짝수 성분** | 반파 대칭화 후 정류기 모델 | 시작·피크·폭 **소수점까지 일치** |

결정적 증거는 정류기 물리다. 전압이 정말 352V/323V 로 비대칭이면 캡이
350V 로 충전된 뒤 음의 반주기(323V)에서는 도통이 불가능하다 — 전류가
반파여야 한다. 실측 전류는 대칭이다. 따라서 **실제 전압은 대칭이고
비대칭은 계측 아티팩트**다.

덧셈 짝수 성분 `A·cos(2ωt+φ)` 는 양·음 피크에 같은 부호를 더하고 양쪽
영교차에 반대 부호를 더한다. 관측된 "저진폭에서 음이 크고 고진폭에서
양이 큰" 교차 패턴을 정확히 만든다.

반파 대칭화 `v_sym = [v(t) − v(t+T/2)] / 2` 는 짝수 고조파를 **정확히**
제거하고 홀수를 **정확히** 보존한다(실측: h3 위상 변화 0.01°). 다항식
보정과 달리 부작용이 없다.

> **폐기:** `vcorr.json` 의 2차 계수. 전압 피크를 21V 깎아 모델을 망가뜨렸다.

### 10.4 왜 Z변환을 초기값으로만 쓰는가

Xie 2024 의 Z변환 선형 추정은 도통 중 회로가 선형(R-L-C-R)이라는 사실을
이용해 최소제곱 한 번으로 파라미터를 낸다. 시뮬레이션 검증에서는 1~2%
정확도였다.

**논문 오타:** 식 (22) 의 `b2` 와 식 (26) 전체가 틀렸다. 참값을 넣으면
`b2 = 2319` (안정 시스템은 |b2|<1)가 나온다. `fit_raw.py` 는 쌍선형
역변환을 직접 유도해서 쓴다.

실물에서의 결과:

```
                  시간영역(참조)      Z변환 중앙값       동작점별 산포
프로젝터   C/L/R   53.3/711/7.18   →  50.9/542/7.23     CV 1%    ✓ 일치
미니PC            36.6/2785/11.7   →  39.1/1427/11.7    CV 13~39%
충전기            66.3/988/5.06    →  58.6/444/3.43     CV 49~51% ✗
```

충전기는 동작점마다 값이 매끄럽게 흐른다(19W→73W 에서 R 4.4→1.9,
L 933→207). 잡음이 아니라 구조적이다. 원인 후보는 저항 부하 가정
(실제는 정전력), 20샘플 도통 구간의 R–L 축퇴, 혹은 실제 NTC. 정전력
선형화(상수항 추가)를 시도했으나 20샘플 안에서 상수항과 `R_dc` 가
분리되지 않아 실패했다.

**그러나 초기값으로는 충분하다.** 30~50% 틀려도 올바른 골짜기 안이다.

```
                          C_dc    R      L     파형RMS     시간
격자 16개 시작             64.6   4.15   1063   7~16%     600초
Z변환 초기값 → NM 1회      64.3   5.06   1076   7~16%       4초   ← 같은 답
임의 초기값 → NM 1회      103.3   1.00    150  22~42%       6초   ← 국소 최소
```

임의 초기값은 잘못된 골짜기에 떨어진다. Z변환은 항상 올바른 골짜기에
떨어진다. 그래서 Z변환의 역할은 **최종값이 아니라 초기값**이다.

### 10.5 왜 다점 시간영역 피팅인가

단일 동작점 스냅샷으로 피팅하면 그 점에서는 9.5% 로 맞지만 다른 전력에서
틀린다. 실제로 충전기를 73W 한 점으로 피팅한 파라미터로 43W 회전 관문을
돌리자 38°→66° 로 악화됐다. `rd` 같은 파라미터가 동작점을 흡수하기
때문이다.

기기당 3~4 동작점(요동하는 파일은 전력 분위로 쪼개 더 늘림)을 **하나의
손실 함수**에 넣으면 전 구간에서 7~16% 가 나온다. 손실은 동작점별
상대오차 `mean((Is−I)²)/mean(I²)` 의 평균 — 절대오차를 쓰면 고전력
동작점이 지배한다.

### 10.6 미니PC 토폴로지와 NTC 재판정

고조파 격자에서는 미니PC에 선로측 X-cap + NTC(v5)가 필요했고 NTC 가
손실을 28% 줄였다. 원시 파형 시간영역에서는 `I0 → 612` (사실상 ∞) 로
NTC 항이 상수 저항으로 축퇴했다. **NTC 가 실제로 없거나, 있어도 동작
전류 범위에서 이미 예열 상태**다.

격자에서 NTC 가 도움됐던 것은 `Cx=0.46µF`(실제 0.34) 과 오염된 전압이
만든 잔차를 전류 의존 저항이 흡수한 것으로 본다. 원인을 고치면 증상이
사라지는 전형적 사례다.

선로측 X-cap 위치(v5) 자체는 유효하다. 미니PC 9W 에서 기본파가 30° 앞서는
것은 선로측 X-cap 으로만 설명된다.

### 10.7 남은 결함

| 항목 | 값 | 판단 |
|---|---|---|
| 미니PC 저부하 h3~h9 | 모델/측정 1.15~1.18 | Cx 상관 0.37 — 저전력에서 Cx 불확실 |
| h15 전반 | 0.74~0.87 | 다이오드 역회복 또는 EMI 필터 공진 미모델링 |
| 측정 노이즈 바닥 | h15 에서 4~5% | 물리적 한계. 이 아래는 의미 없음 |

h15 잔여 20% 는 FCM 에 큰 문제가 아니다. `Y = ∂I/∂V` 는 미분량이라
절대값 오차가 곱셈 계수 형태면 상쇄된다(§9 대안 구조).

### 10.8 A→C 회전 관문 — 정정 후 통과

원래 이 절은 "장소 A 에 `vhdeg` 가 없어 판정 불가" 였다. 그러나 실제 문제는
장소 C 쪽 충전기 2Hz 파일의 위상 오류였다(§8.1). 원시 스냅샷 고조파로
관측값을 교체하고 원시 파이프라인 파라미터로 예측하면:

```
                 관측 (정정)              예측               RMS
충전기    -11 -22 -31 -40 -47 -49   -16 -31 -45 -57 -62 -39   12.3°
프로젝터  -13 -24 -34 -39 -39 -39   -19 -36 -51 -60 -48 -16   16.1°
미니PC    -20 -37 -51 -70 -79 -96   -22 -42 -63 -89-106-153   27.5°
                                                    전체    19.7°
```

정정 전 37~44° 에서 19.7° 로. 장소 A 를 이상 정현파로 가정한 상태에서
이 정도면, A 의 `vhdeg` 가 확보되면 더 내려갈 여지가 있다.

미니PC 가 가장 나쁜 이유: `minipc_4C.csv` 에도 +1~2 샘플 오프셋이 있고
(h13 에서 15~30°), 9~29W 의 저전력이라 Cx 상관이 0.37 로 낮다.

### 10.9 새 기기 추가 절차 (확정)

```
0. 세션 자가진단  전기포트 on/off 1회 → ihdeg1 이 ±2° 안이면 정상   2분
                 (10° 근처면 그 세션의 V–I 정렬 오류, 재부팅 후 재시도)
1. 원시 스냅샷   3~4 동작점, 각 20주기 이상, 레인지 고정        약 1시간
   - 충전기류: 방전→완충 중 여러 시점
   - PC류: stress-ng 단계별
   - 고정전력 기기: 1점이면 충분
2. python fit_raw.py <dev> raw_*.csv                          4~20초
   - 파형 RMS 가 20% 넘으면 --topo v5 시도
   - Cx 상관이 0.5 미만이면 더 높은 전력 동작점 추가
3. circ_<dev>.pkl 을 생성기에 등록
```

격자 탐색은 원시 스냅샷을 못 얻을 때의 대안으로만 남긴다.

### 10.10 검증 데이터 요약

| 기기 | 파일 | 동작점 | 주기 | 파형 RMS | Cx 상관 |
|---|---|---|---|---|---|
| laptop_charger | 4 | 8 (19.3~73.0W) | 155 | 7.0~15.6% | 0.954 |
| beam_projector | 2 | 2 (49.3W) | 78 | 6.3~6.5% | 0.961 |
| minipc | 4 | 6 (9.0~28.9W) | 156 | 7.0~16.6% | 0.365 |

장소 C, 2026-09, `vhdeg` 있는 펌웨어. 전압 채널 아날로그 아티팩트는
반파 대칭화로 처리.

**알려진 데이터 오류:**

| 파일 | 오류 | 영향 | 조치 |
|---|---|---|---|
| `laptop_charger_5C.csv` | 전류 위상 +7샘플 | 모든 ihdeg 에 −10°×h | 곡선·회전 관측을 원시로 대체 |
| `minipc_4C.csv` | 전류 위상 +1~2샘플 | h13 에서 15~30° | 미보정. 재측정 권장 |
| `beam_projector_4C.csv` | 없음 (0샘플) | — | — |

---

## 11. Claude Code 미해결 문제(2026-09-05)에 대한 답

`PROMPT_open_problems_2026-09-05.md` 의 P1~P8 에 대해 지금 데이터로 할 수 있는 것.

### 11.1 P1 — 전압 h15 절단: 펌웨어 없이 해결 (`vtemplate.py`)

**함수형 사전분포는 안 된다.** 장소 C 의 h17~h31 은 단조 감쇠가 아니다:

```
      h17   h19   h21   h23   h25   h27   h29   h31     (V1 대비 %)
      0.01  0.03  0.07  0.16  0.07  0.10  0.15  0.07
```

h23·h29 에 봉우리가 있고 h17 이 가장 작다. 지수 외삽은 h21 을 0.005% 로 예측(실측 0.069%),
평탄화 모델 `A·cos−B·cosⁿ` 은 h3~h15 조차 못 맞추고, Lanczos σ 는 12% 로 붕괴시킨다.

**선로 결합도 아니다.** 혼합 단자 전압의 h17+ 를 `Z·I_pulse` 로 복원하면 R=0.52Ω 에서
0.31% 중 0.02% 만 돌아온다. 단독 파일에도 0.27% 가 있으니 계통 자체의 텍스처다.

**경험적 템플릿은 된다.** 같은 장소 18개 파일(2일, 여러 시간대)에서:

```
CV     h19 0.56  h21 0.36  h23 0.33  h25 0.42  h27 0.37  h29 0.38  h31 0.33
위상   h23 +19°  h29 +94°  h25 −149°  h31 −110°  (일관)
```

h11~h13 (CV 0.33~0.46) 과 같은 안정성. 프로젝터 단독 스냅샷 하나에서 h17~h127 을 뽑아
혼합 V15 에 붙이면:

```
                V전체    V15     V15+템플릿   h21절단(펌웨어)
smps3_1         5.5%    9.0%      6.3%         6.6%
beam_charger_1  4.9%    9.2%      6.7%         5.3%
beam_minipc_1   6.2%    4.6%      5.4%         5.6%
```

**펌웨어 h21 발행과 같은 수준을 지금 얻는다.** 18파일 평균 템플릿은 더 나쁘다(7.5%, 8.1%)
— 세션 오프셋 있는 파일이 위상을 흐린다. 정렬 0 이 확인된 스냅샷 하나가 낫다.

```python
from vtemplate import VTemplate
T = VTemplate.load('vtemplate_C.npz')      # 장소당 1회 생성
X_full = T.extend(V15)                       # (129,) complex, h배 관례
```

**관례 주의:** `VTemplate` 은 `arg(V_h) − h·arg(V_1)`. 절대 위상 스펙트럼에 붙일 때
`exp(+j·h·φ₁)` 로 되돌려야 한다. `exp(+j·φ₁)` 만 곱하면 6.3% 가 8.6% 로 나빠진다
(`vtemplate_check.py` 주석).

**한계:** 충전기 단독 73W 는 V전체 11.4% / V15 12.5% / 템플릿 11.7% — 절단 손실 1.1%
보다 모델 오차가 훨씬 크다. 충전기는 P1 이 아니라 P7 문제.

**정정 (회신 반영): 템플릿은 장소가 아니라 세션에 고정된다.** 18개 파일을 녹화 시각으로
묶으면 같은 세션은 켜진 기기와 무관하게 같은 텍스처(00:36 세션 6개 h17 전부 ∠−64°),
1시간 떨어진 세션은 거리 0.7~1.1 로 참조 크기와 같다. 세션 내 0.33~0.41.

```
                  V전체   V15    다른세션템플릿   같은세션LOO   h23·h29만
6개 혼합 평균     5.5%   7.6%      5.9%          5.8%         6.5%
```

같은 세션이면 전체 파형에 거의 도달. 안정 차수만 남기는 건 **기각** (6.5%) — h17~h21 이
흔들려도 피크 형상 정보를 갖고 있다.

**요청 A′ (요청 A 대체):** 10~15분마다 원시 스냅샷(20주기, ≈2초)을 발행. 호스트가
템플릿을 갱신한다. h21 상시 발행보다 가볍고, 세션 고정 문제를 정면으로 푼다.

**RC 규약:** 저쪽 파이프라인은 전압에 역RC 를 건다. `VTemplate.from_raw(deembed=True)`
가 그 규약으로 변환한다 (회신 §2 의 식, 배율 소수점까지 일치). 효과는 0.3%p.

### 11.2 P2 — 두 계측 경로의 h11~h15 불일치: 가설

부호가 기기마다 반대(프로젝터 −16~21%, 충전기 +26~29%)라는 게 열쇠다. 계통적 경로
차이면 부호가 같아야 한다. 충전기 70W 는 레인지 혼합이 있고 프로젝터는 없다 →
**P5 (이어붙이기)가 h11~h15 크기를 키우는 방향**이면 충전기 쪽은 설명된다. 프로젝터의
−20% 는 별개 원인(기기 상태 차이 가능). 요청 B 없이는 여기까지.

### 11.3 P4 — ∠Z₁ (선로 인덕턴스) 측정법

`vhdeg1≡0` 이라 ΔV₁ 이 실수인 것이 문제. **용량성 부하를 스위칭하면 된다:**

```
ΔV₁ ≈ Re(−Z₁·ΔI₁) = −|ΔI|·(R·cos φ − X·sin φ)
저항 부하 φ=0    →  ΔV = −R·|ΔI|      (포트: R 이 나온다 — 지금 하고 있는 것)
용량 부하 φ=+90° →  ΔV = +X·|ΔI|      (X = ωL 이 나온다)
```

모터 런 커패시터 10~50µF (237V 에서 0.9~4.5A 용량성)를 포트처럼 수십 번 on/off.
L=400µH 면 X=0.15Ω, 4.5A 에서 ΔV=0.68V. 커패시터는 선형이라 P3 의 HIGH 인공물이
h1 에서는 덜 문제. 고차 Z 는 문서 분석대로 상류 응답이 섞여 수동 Z 가 아니다.

### 11.4 P7 — R–rd 축퇴: rd 를 물리값에 고정

`rd` 는 브리지 다이오드 2개의 동적 저항. `r_d = n·V_T/I ≈ 2×0.026/0.5 ≈ 0.1Ω`, 도통
피크에서 0.05Ω. 실리콘 정류 다이오드 rd 의 물리 범위는 **0.1~0.5Ω** 이고 6.67Ω 은
아니다. **rd = 0.3Ω 고정** 권장. R 이 나머지를 흡수하고 자유도가 하나 줄어 LOO 개선 기대.

충전기 19W 는 별개. LOO 9.3% 가 다른 점의 2~3배인 건 **버스트 모드**가 유력 — USB-C PD
브릭은 저부하에서 스킵 사이클을 돌리고 40주기 중앙값이 두 상태를 섞는다. 확인: 19W
원시의 주기별 |I₃| 히스토그램이 쌍봉이면 버스트. 그러면 주기별 분류 후 각각 피팅.

### 11.5 P3 · P5 · P6 · P8

데이터(요청 C·D·F) 또는 펌웨어(요청 E) 없이는 못 한다. P8 은 측정기를 못 옮기니 받아들인다.

### 11.6 판정 기준 [4] 에 대해

"확장 모형이 기본보다 나쁠 수 없다" 는 원칙은 옳고, 이 대화에서도 같은 함정을 두 번 밟았다
(Z변환 정전력 선형화, 2차 전압 보정). 겹쳐 시작 + 다중 시작 + TRF 로 바꾼 것에 동의한다.
