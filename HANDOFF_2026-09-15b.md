# 인계 2026-09-15 (저녁) — 오븐↔포트 혼동의 **원인을 찾았다**

## ⏳ 지금 도는 것

```
984668  nilm_hs3t  3시드  캐시 **train60_v32hs3** (텍스처 96%)   -> cnn_hs3_s{0,1,2}
984682  nilm_tsp   3시드  **--fine-time-split** (구조)            -> cnn_tsp_s{0,1,2}
   둘 다 대조는 로컬 `cnn_hzwt_s{0,1,2}` · BASE+WTAP 글자 동일 확인함
   포장: 갈래당 **1GPU · 3코어** (CPU 2.44/3.00 · GPU 100%)
```

## ★ 오늘의 결론 — **미래 누수**다

세밀 창 600 · 타깃 239 · **미래 361사이클 = 6.02초**.
`--fine-extra-dilations 32,64`(wtap)를 켜면 깊은 탭 RF 가 187 -> **763**(±6.36초)이라
`h[:,:,239]` 라는 "지금의 특징" 안에 **미래 6초가 타임스탬프 없이 섞인다.**

개입으로 확인 (미래를 타깃값으로 덮는다, `cnn_hzwt_s0`):
```
test_5 260.0초    핫플 헛게이트 **0.878 -> 0.004**  (참 통전 263.0초 · 3초 빠른 오탐)
test_5 128~138초  **포트 게이트 0.003 -> 0.938** · 오븐 0.998 -> 0.431
                  -> 사용자가 열 번 짚은 오븐<->포트 혼동이 **미래 누수**다
전체 5파일        오븐 **헛detect 의 22.0%** 가 미래에 기댄다 (참detect 는 **0.8%**)
                  핫플 참detect 의 **30.1%** 는 미래가 필요하다 (듀티 위상)
```
⇒ 미래를 **없애면 안 되고 시간 부호를 붙여야** 한다 = `--fine-time-split` (14.116).

**그 구간의 컨덕턴스는 처음부터 포트를 가리켰다** — 참 포트 **35.3Ω** · 표 포트 35.8 ·
오븐 40.6 · 문턱 38.2. 정보가 없던 게 아니라 미래가 뒤집은 것이다.

## 이 병에 대해 **죽은** 손잡이 (되살리지 마라)

```
L_gcond  게이트를 통전에 묶기   14.112 반증 — B 팬조명 196->0 인데 A 축퇴 57->59 그대로
                                오븐을 듀티 추종기로 만들어 참 ON 채움률 98%->53% (조각 2.1->4.6)
L_swap   컨덕턴스 손실          14.35  반증 — 오븐 정밀 +0.004 ± 0.010 · SMPS −0.043 · 판정줄 −0.0395
위치 채널                        14.42  — 모델은 위치를 넷으로 안다. 처방 아님
--seg-pool 2                    14.72  기각 (−0.9 ± 2.0). ⚠ **RF 187 시절 자**라 지금과 구조가 다르다
광역 셋(풀링·수용영역·타깃탭)     14.89 / 14.94 전부 기각
후처리 컨덕턴스 정합             14.31  판정 줄 −0.005~−0.016
```

## 판정 자 (`src/run_diag_ovenstar.py --f1`)

```
대조 cnn_hzwt   ★1 2.908 ± 1.271 · ★2 33.9 ± 3.9 %
                ★3 현행 F1 0.914 (재현 .985/정밀 .853) · ★4 마스크 F1 0.859 (.988/.761)
대조 cnn_v32h_on ★1 4.454 ± 1.005 · ★2 30.6 ± 1.5   (자 검정용으로 PINNED 에 박혀 있다)
오븐 헛게이트 10.1% (A 축퇴 57 · B 팬조명 196 · C 초과 6) — `run_diag_ovenfp`
```

**984682 의 통과 조건**: `test_5` 128~138초에서 **포트 게이트 > 0.5**, 3/3.

## 캐시

```
cache/train60_v32hs3            30만창 23G · step=seg=1.25 · coarse=25 · harmonic_z
processed_data/holdout60_v32hs3  8천창 4.9G · 처치 넷이 학습 캐시와 같음(관문 확인)
텍스처 회수율  복합 실측 기준 **96%** (대조 v32h 는 22%)   — 14.111
굽기 비용     **1.41 코어·초/창** = 8토막 x 40코어로 **22분**. 옛 "4.3시간" 추정은 20배 틀렸다
```

## 새 도구 (오늘)

```
run_diag_ovenstar   ★1~★4. `--f1` 로 현행/물리마스크 두 자 (12.162)
run_diag_ovenfp     오븐 헛게이트를 A축퇴/B팬조명/C초과 로 가른다 + 전이 바닥값
run_diag_stepohm    참 전이의 `R = V^2/dP` — 계단이 누구를 가리키나
run_diag_texreal    캐시 대 실측 창-안 텍스처 변동 (`--npz-dir` 로 단일/복합 선택)
run_diag_bakecheck  구운 캐시 검증 [A]토막 [B]회로 호출 [C]창당 비용
run_gate_cachemeta  캐시 meta 관문 (새 키는 **값을 검사한 뒤** 제외)
run_gate_bakeseg    굽기 경로의 비균일 토막
run_gate_timesplit  `--fine-time-split` 8개 (특히 [4] 미래 바꿔도 타깃 탭 최대차 0)
run_gate_gatecond   `--w-gate-cond` 9개
```

## 오늘 세운 규칙 (기억에 넣었다)

- **던지기 전에 문서를 뒤져라** — 죽은 손잡이를 되살릴 뻔한 게 하루 세 번
- **분모를 먼저 확인하라** — 표본편향·모집단·수준. 255% -> 121% -> 96%
- **진단은 구조가 바뀌면 만료된다** — RF 187 시절 진단을 RF 763 에서 그대로 믿었다
- **지목한 창을 직접 펴라** — 집계는 "얼마나" 만 말한다
- HPC 포장: **1GPU·3코어 x N** 이 2GPU·8코어 한 덩이보다 낫다 (GPU 70% -> 100%,
  빈틈에도 잘 들어간다). 병목은 시시각각 바뀌니 `-c` 고정값을 믿지 마라
- `run_gate_help.py` 를 CLI 건드린 뒤 **반드시** 돌려라 (argparse 도움말의 맨 `%`)
- `sys.path.insert` 없는 관문은 `-m` 으로만 돈다 (`run_gate_swap1`)
