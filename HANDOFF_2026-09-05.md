# 인수인계 2026-09-05 — 원시 파형이 왔고, **회로 모델이 섰다**

앞 문서: `HANDOFF_2026-09-04b.md` (장소 C 첫 자료, 계측 판정, 철회 목록).
설계 **12.185.1 ~ 12.185.19**, 측정 규칙 **77 ~ 79**. 커밋 `bfb2b5c`.

---

## 0. 한 줄

**원시 파형 스냅샷 18개가 들어와 회로 모델이 h≤15 에서 1.4~4.6% 로 서고, 결합까지
조합에 맞춘 자유 파라미터 0개로 3.5~8.3% 로 재현된다** (단순 중첩은 12.7~15.9%).
막고 있던 것은 모델이 아니라 셋 다 계측 규약이었다 — 전압 반파 대칭화, 계측 RC 를
순방향 모델에 넣기, "배경" 의 정의. 어제까지의 회로 수치는 전부 폐기했다.

## 1. 지금 상태

```
운영점            results/adapt_ph5_s0.pt (변경 없음). 배포 묶음 ../power_monitor 그대로
펌웨어            v5, LOW 교정 0.44 / HIGH 2.62 (원래 값). 소스 ..\NILM_ECE_IF-fix-even-harmonics-offset-bug
정본 파라미터     results/_circuit_raw_C.json 의 `fit_fixrd` (rd 를 물리값 0.3Ω 에 고정)
                  laptop_charger  C_dc 66.9µF R 4.20Ω L  766µH Cx 0.120µF   훈련 4.6% LOO 5.4%
                  beam_projector  C_dc 53.0µF R 5.93Ω L  544µH Cx 0.370µF   훈련 1.4%
                  minipc          C_dc 46.2µF R 9.05Ω L 1048µH Cx 0.304µF   훈련 4.2% LOO 4.4%
                  재현 바닥(주기산포/√40) 1.2~3.0%. `fcm.load_models()` 기본이 이것
자료 규약         PHASE_FIX laptop_charger_5C = **9.5°/차수** (원시로 직접 측정, 옛 10.8 은 과교정)
                  minipc_4C 는 값을 못 정해 미보정 — h13 에서 15~30° 어긋날 수 있다
선로 임피던스     |Z₁| = 0.520Ω (포트 계단 78개). ∠Z₁·선로 L 은 미측정. 고차 Z 는 수동 임피던스가 아니다
커밋              master 4a908e2 f78046e 70b0bbb bfb2b5c — **푸시함**. power_monitor main 2959992 는 로컬
```

## 2. 오늘 밝힌 것 (전부 GPU 없이)

```
12.185.1   저장된 회로 수치 폐기 — 현 규약 재평가에서 5~13배 어긋난다 (2.62 회전 시절 표)
           요소 기각(12.184.10)도 철회 — 확장 모형 "최적"이 중립값보다 나빴다 = 최적화 실패
12.185.2   펌웨어 RC 는 크기만 보상. 관례가 arg(I_h)−h·arg(V_1) 이라 h·δ1−δh 가 남는다
           (h9 +0.69° h13 +1.95° h15 +2.90°). 원시에서는 펄스 대역 크기를 0.6~0.8배로 깎는다
12.185.3   **전압 반파 대칭화가 없으면 적합이 무너진다** (RMS 55~95%, R·L 경계 고착)
12.185.4   손실은 h1~h15 로 자른다 (h16+ 는 RMS 의 9~15%, 모델이 못 낸다). 파세발로 절대 페이저 적합과 같다
12.185.5   세 기기 §4.3 범위 안, 경계 없음. **미니PC 에 v5(선로측 X-cap+NTC) 불필요**
           프로젝터 Cx 는 0.240 이 아니라 **0.379µF** (직접측정 0.383, 상관 0.987)
12.185.6   2Hz 파일의 세션별 전류 위상 오프셋을 원시로 직접 쟀다 (5C −9.5°/차수 = −6.8표본)
12.185.9   **"배경" 은 대개 그 기기 자신의 X-cap 이다.** 진짜 배경은 7.3mA ∠+10.9° 하나
           고치자 저부하 잔차 11.3% -> 5.2%
12.185.10  정본 파라미터. 남은 잔차: 충전기 19W (훈련 5.9% LOO 9.3%), 미니PC R–rd 축퇴
12.185.11  두 계측 경로가 h11~h15 에서 20~30% 갈린다. h1~h9 는 2~5%·3° 안에서 맞는다
12.185.12  **결합 검정** — 조합 V_term 으로 돌려 더하면 3.6~8.8%, 단독 V_term 이면 12.8~15.7%
           (= 단순 중첩). 개선이 전적으로 단자 전압에서 온다. 가이드 §6.1 이 실측으로 섰다
12.185.13  FCM 구조·선형화 한계가 우리 파라미터로 재현. V3 1% 에 h9 27~32% 틀림 -> **Y 를 생성에 쓰지 마라**
12.185.14  Z(h): 포트 계단 78개로 |Z₁| 0.520Ω. ∠Z₁ 은 vhdeg1≡0 규약상 못 잰다.
           h3~h7 은 재현되는데도 ∠Z₃ 141° (실수부 음수) — 분모에 HIGH 인공물, 분자에 상류 응답
12.185.15  forward() 고정점은 수렴한다 (V_term 0.02%). 함정 둘: 계측 RC, **위상 기준 프레임**
12.185.16  h15 절단이 피크를 +0.94V 올리고 도통각을 38.8°->36.9° 바꾼다 -> h9~h15 가 14%·20~37° 흔들린다
12.185.18  **그런데 합성은 오프라인이라 소스 전압을 우리가 고른다** — 원시 전대역을 넣으면 3.5~8.3%.
           `fcm` 이 전대역을 받게 했다. 펌웨어 h21 요청의 우선순위를 내린다
12.185.19  rd = 0.3Ω 고정이 **공짜** (훈련 +0.0~0.1%, LOO 불변). 미니PC R 2.71->9.05, rd 6.67->0.30
```

## 3. 철회한 것 — 다음 세션이 같은 함정에 빠지지 않게

```
"§3.3 요소(다이오드 무릎·초크 감쇠·부하 지수) 기각"   최적화 실패였다. 겹쳐 시작으로 다시 세워야 한다
"_circuit_params_C.json / _circuit_elements_C.json"  옛 규약 수치. 폐기 (재평가에서 5~13배)
"기기 꺼진 창 = 배경"                                그 기기의 X-cap 이다 (규칙 78)
"PHASE_FIX 5C = 10.8°/차수"                          실측 9.5. 옛 값은 h15 에서 19° 과교정
"Z_C = 0.45Ω"                                        계단 78개로 0.520Ω
"조합 차분으로 Z 를 재려면 대전력 계단이 필요하다"     그 계단(포트)이 이미 있었다. 접지 말았어야 했다
"고조파 15개로는 결합 모델이 안 선다 -> 펌웨어 h21"    합성 소스는 우리가 고른다. 원시 전대역이면 된다
"미니PC 는 v5 토폴로지"                              자료로 구분되지 않는다. v3 로 4.2%
```

## 4. 자 (전부 `python -X utf8 -m src.<이름>`, GPU 없음)

```
run_raw_fit_probe    원시 다점 적합 + Cx 직접측정 + rd/Cx 고정 + LOO   (--vband 로 소스 대역)   3분
run_raw_combo_probe  결합 검정 [A] 단순중첩 / [B] 모델(조합 V_term) / [C] 모델(단독 V_term)     1분
run_fcm_probe        FCM 구조 · 선형화 한계 · Z(h) 계단 · forward 고정점 · Z 민감도            3분
run_circuit_model_probe  2Hz 쪽 적합·요소 재검정·RC 전개·절대 페이저·식별성·원시 대조          5분
synthesis/fit_raw    원시 적합 라이브러리 (대칭화·RC·배경·다점·대역제한 손실·LOO)
synthesis/fcm        HNE/FCM, 전대역 소스 고정점 생성기, z_from_steps, source_from_raw
synthesis/circuit_fit  2Hz 고조파 쪽 잔차 최소제곱 (겹쳐 시작 단조 보장, RC 전개, 식별성)
synthesis/circuit_sim  시뮬레이터 (가이드 §3 토폴로지 v3)
```
결과: `results/_circuit_raw_C.json`(정본) · `_circuit_raw_C_v15.json` · `_fcm_C.json` ·
`_circuit_model_C.json`. 기준: `results/_criteria_circuit.md` ([R1]~[R7], [F1]~[F5], 전부 결과 전에 적었다).

## 5. 자료

```
원시 스냅샷 (장소 C, 9/04 22시 ~ 9/05 00시, 40주기 × 256표본 @ 15360Hz)
  단독  laptop_charger×5 (17~73W) · beam_projector×2 (47W) · minipc×5 (7~25W)
  조합  beam_minipc×2 · beam_charger×2 · smps3×2      <- 결합 검정의 정본
  등록부 RAW_SNAPSHOT_FILES / RAW_COMBO_FILES / RAW_COMBO_SOLO
2Hz (vhdeg 있음)  laptop_charger_5C · minipc_4C · beam_projector_4C · electric_kettle_4C(포트 78계단)
                  hair_dryer_4C · noise_noselfpower_C(진짜 배경) · tesr_19(⚠오타, 라벨 타임라인 있음)
tesr_19 타임라인  등록부 TEST19_TIMELINE. seq -> t_s = (seq−60)/2 초.
                  프로젝터 −46/+45W, 미니PC −9/+16W 는 깨끗한 계단.
                  ⚠ 충전기 OFF(seq 430)만 −11W 뒤 완만한 하강 — 계단이 아니다. 델타 서명에 쓰지 마라
```
장소 C 파일은 전부 QUARANTINE (풀 혼입 보류). `data/` 는 gitignore 라 커밋에 없다.

## 6. 다음 수 (순서)

> **① 은 끝났다 (2026-09-05 저녁, 12.185.20~22).** 아래 원문은 기록으로 남기고
> 실제로 무엇이 됐는지는 §6.1 에 적었다. **셋 다 원래 처방과 다른 답이 나왔다.**

```
① 생성기 (지금 할 수 있는 것)   <- 완료. §6.1 참조
   a. 저항 부하를 I_h = V_site,h / R 로 (12.184.15a). 장소 C 포트가 포트로 안 보이는 이유
   b. `fcm.forward` 를 생성기에 붙인다 — 소스는 `fcm.source_from_raw` 의 전대역 파형 18개
      Z 무작위화는 부록A §5 의 0.7~2.0Ω (총전류에 4~9% 다양성. L 은 미측정이라 0~400µH 로)
   c. 요소 재검정 (12.184.10 철회분) — 겹쳐 시작으로 nvt / Gp / alpha 를 원시 자료에서 다시
② 남은 모델 잔차
   a. 충전기 19W (LOO 9.3%, 다른 점의 2~3배). 버스트는 기각됨(주기별 |I3| 이 쌍봉 아님).
      다음 후보: 도통 표본 20개 안의 R–L 축퇴, rd 의 부하 의존
   b. 미니PC 기기 R 8.53Ω 이 크다 — 가이드 §3.3(b) 부스트 프리레귤레이터 가능성
③ 계측 (펌웨어·자료 요청. PROMPT_open_problems_2026-09-05.md 에 정리)
   E  LOW/HIGH 위상 응답을 이어 붙이기 **전에** 맞춤 (P2 의 절반이 이것이다)
   B  같은 순간의 원시 + 2Hz 를 함께 발행 (P2 나머지 진단)
   C  LOW 안에 머무는 큰 비선형 부하 on/off 반복 (고차 Z)
   D  포트 on/off 원시 스냅샷 (HIGH 인공물)
   F  minipc_4C 재녹화 + 세션 자가진단 (저항 부하로 ihdeg1≈0 확인)
   G  백열전구/납땜인두 1분 (전압 채널 홀수차 보정표)
   H  프로젝터 두 번째 동작점
   A′ (저쪽 제안) 10~15분마다 20주기 원시 스냅샷 발행 — 배포 현장의 전압 텍스처용
④ 장소 A·B 는 갈 수 없다. 전이·분모 편향은 열린 채
GPU 는 게이트 5 통과 전엔 돌리지 않는다. 돌리기 전에 묻는다.
```

## 6.1 ① 의 결과 (12.185.20~22, 커밋 0d591c2 fd9f1b2 edfbcfa aebab60)

```
c 요소 재검정   **nvt(다이오드 무릎) 채택.** 훈련 −45~50%, 충전기 LOO 5.4->3.3%,
                19W 점 9.1->6.0% (②a 해결), 유보 자료(조합 6개) 5.99->2.69%.
                Vf 대조군이 +0.00% 라 이득은 강하의 크기가 아니라 **전류 의존성**에서 온다.
                Gp 기각 (물리값 33Ω 강제하면 오히려 나빠진다). alpha 는 직류 리플이
                1~6% 뿐이라 **구조적 미식별** — 자료를 더 모아도 안 된다, 다시 시험 마라.
                미니PC 는 셋 다 안 먹는다: 잔차가 h3·h5 와 비도통 구간(5.5%)에 있다 (덧셈 성분).
                ⚠ nvt 0.018~0.023V 는 물리값의 1/6~1/2 이고 L 을 33~38% 끌어내린다 — 자리표다.
                **정본(fcm.load_models 기본)은 fit_fixrd 그대로 두었다. 교체는 사용자 결정.**
                자: run_element_raw_probe / results/_circuit_elements_raw_C.json

a 저항 부하     처방대로 `V_site,h/R` 은 **안 된다** — 전압 채널의 vh 를 쓰면 장소 A 에서
                h3 을 1.80% 로 예측하는데 저항 부하는 0.36% 를 낸다. 절대값으로 갈라 보면
                `|I_h| = a_h + d_h·|I_1|` 에서 **h3 은 덧셈 33.5mA∠+164°(계측기), h5~h15 는
                비례**다. 장소축이 서는 것은 h3 하나: d_3 = A 0.000 / B 0.003 / **C 0.031**.
                생성기: VoltageCluster.v_distortion_h3 + apply_site_distortion (저항만).
                234.7V/0.45Ω 클러스터가 곧 장소 C 인데 고조파가 없었다 = 12.184.15(a) 의 원인.
                검정: 같은 서명이 콘센트별로 0.49% -> 3.34%∠−141° (실측 C 3.60%∠−137°).
                ⚠ 장소 **안**에서는 어느 모형도 지금 생성기보다 낫지 않다 (홀드아웃 62.7 대 52.9%).
                자: run_resistive_site_probe / results/_resistive_site.json

b 결합         처방의 Z 무작위화는 **지배 축이 아니다.** 조합 실측 V 와 단독 녹화 V 의 차가
                V1 의 0.87~1.03% 인데 Z·I_total 은 0.05~0.08% (1/12) 다. 남는 것은
                **세션이 다르다** 는 사실이고, 그 0.9% 가 도통각 초선형으로 증폭된다.
                전력 재배분이 없는 두 조합에서: 단순중첩 13.5% -> 결합만 12.7% -> **텍스처만 2.7%**.
                12.185.12 의 "[B] 가 [C] 보다 4배" 는 결합이 아니라 텍스처였다.
                생성기: synthesis/coupling.TextureModel + VoltageEnvironment.texture_stem +
                apply_voltage_texture (SMPS 만, 기본 켬). CouplingModel 은 만들었지만
                **안 붙였다** — 소스가 측정 파형이면 결합이 이미 그 안에 있어 이중 계상이다.
                창당 60ms -> 3.1ms (캐시 적중률 89%), 기본파 변화 최대 4.2%.
                자: run_coupling_probe / results/_coupling.json
```

**① 이 만든 새 관문 (다음 수)**
```
1. 정본 교체 여부 — nvt 를 켠 par5 를 fcm.load_models 기본으로 할지 (사용자 결정).
   얻는 것 LOO −2.1%p·조합 −55%. 잃는 것 L 이 물리 인덕턴스가 아니게 된다
2. 텍스처 12개가 전부 **장소 C, 2시간 안**이다 — 세션 다양성이지 장소 다양성이 아니다 (P8).
   장소 A·B 는 원시 파형이 없고 2Hz 에 vhdeg 도 없다. 저쪽 요청 A′ 가 겨누는 자리
3. 풀의 서명은 장소 A 인데 텍스처 기준은 장소 C 다 — 상대 다양성은 맞지만 절대 앵커가 없다.
   장소 C 파일 풀 편입(사용자 결정)이 있어야 선다
4. nvt 가 무엇의 자리표인지 — 배제한 것: 레인지 이어붙이기(원시 18개 전부 LOW),
   상수 Vf, 초크 감쇠. 남은 후보: 계측 대역 모형(1극 1591.55Hz)이 실제보다 넓다,
   초크 포화, 다이오드 역회복
5. 미니PC ②b 는 회로가 아니다 — 7W 의 |I1| 36mA 에 계측 배경 7.3mA 가 20% 다. 배경·Cx 로 간다
6. 게이트: 생성기가 바뀌었으니 게이트 5 를 다시 통과해야 GPU 로 간다
```

## 7. 미결 결정 (사용자)

```
삭제 두 건   READ_ME_FIRST.txt / SMPS_PLAN_2026-08-31.md (작업 트리 삭제, 커밋에서 계속 뺐다)
풀 혼입      장소 C 파일을 풀(장소 A)에 넣을지 — ① 이 되기 전엔 넣지 않기를 권함
power_monitor  main 2959992 (수신기 v5) 아직 로컬
정본 교체    nvt 를 켠 par5 를 정본으로 할지 (§6.1 의 새 관문 1)
GitHub main  옛 계보 정리
```

## 8. 읽기 주의 — 실제로 빠졌던 함정

```
· 위상 기준 프레임: 시뮬의 I·V 는 소스 V1 기준, np.fft 페이저는 절대 프레임. ∠V1 2° 면 h15 에서 30°.
  이것 하나로 같은 계산이 6.3% 대 16.4% 로 갈렸다 (규칙 79)
· 계측 RC: 시뮬은 참 전류, 원시 표본에는 RC 가 걸려 있다 (규칙 79 뒷단)
· 배경: 규칙 78
· `fcm.to_spectrum` 은 길이로 관례를 가른다 — 129면 차수 색인 X[h], 그 밖이면 V[k−1]=h(k).
  섞으면 기본파가 h2 로 가서 짝수차 소거에 지워지고 발산한다
· 시뮬의 P 는 직류 부하 전력, 측정 p_w 는 교류 입력 전력 — 3~5% 다르다 (`match_power`)
· 전압 짝수차는 아날로그 전단의 덧셈 성분. 반파 대칭화로 정확히 지운다. 홀수차는 h3~h9 만 믿는다
· 원시는 40주기 중앙값을 쓴다. 주기 산포가 7~19% 라 단일 주기로 판단하지 마라
· 레인지 혼합 파일(raw_latop_charger_1, raw_laptop_charger_2, raw_beam_charger_2, raw_smps3_*)의
  h11~h15 는 계측 단계에서 다르다 — 2Hz/원시 비의 부호까지 뒤집는다
```

## 9. 규칙 (77~79)

```
77 원시 적합은 넷을 동시에 켠다 (반파 대칭화·계측 RC 순방향·다점 동시·계측계 배경만)
78 "기기 꺼진 창" 을 배경으로 쓰지 마라 — 어댑터가 꽂혀 있으면 그건 그 기기의 X-cap 이다
79 시뮬 페이저(소스 V1 기준)와 시간영역 FFT 페이저(절대 프레임)를 섞지 마라
```

## 10. 외부 협업

`docs/external/` 에 받은 가이드·코드와 **우리 검증 결과 요약표**가 있다.
우리가 보낸 것: `PROMPT_open_problems_2026-09-05.md` (P1~P8, 증상·근거·원인·요청),
`REPLY_vtemplate_rc_2026-09-05.md` (템플릿 RC 규약 + 세션 의존성).
저쪽이 답한 것: 가이드 §11 (P1 템플릿, P2 가설, P4 용량성 부하로 ∠Z₁, P7 rd 고정).
우리가 검정한 결과 — §11.2 가설 **맞다**(레인지 혼합이 부호를 뒤집는다), §11.4 버스트 **기각**
(주기별 |I3| 이봉계수 0.41~0.51), §11.4 rd 고정 **채택**(공짜), §11.1 템플릿은 세션 의존.
