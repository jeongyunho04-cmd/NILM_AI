# 인수인계 — 학습 PC, 2026-08-24

이 문서는 `HANDOFF_2026-08-23.md`(시연 노트북 -> 학습 PC) 다음 세션의 기록이다.
상세 근거는 `MODEL_TRAINING_DESIGN.md` **12.34 ~ 12.38** 절에 있다.

---

## 0. 한 줄 요약

**저부하 SMPS 실패의 원인이 셋으로 갈렸고, 그중 둘은 순시 지문으로 못 푼다는 것이
확정됐다.** 남은 하나(학습 커버리지)를 고쳐 유령을 9.22 -> 7.86W, 미니PC F1 을
0.740 -> 0.804 로 올렸다. **다만 이번 세션의 모든 설정 비교가 n=1 이라,
시드를 반복하기 전에는 어느 것이 실재하는 효과인지 확정할 수 없다.**

---

## 1. 지금 상태

### 운영점 (아직 `run_live` 기본값에는 반영 안 함)

```
저항 갈래 = results/adapt_ph1.pt   (44채널, 2단계 무라벨 적응)
SMPS 3종  = results/cnn_ov1.pt     (48채널, 1단계, 커버리지 수정 캐시)
```

`run_live --ckpt results/adapt_ph1.pt --ckpt-smps results/cnn_ov1.pt` 로 쓴다.
`run_live` / `run_plot_timeline` 의 기본값은 아직 `adapt_v17` / `cnn_v17` 이다 —
**바꿀지는 결정 안 했다.**

| | 유령 | 잔차 | 미니PC | 프로젝터 | 충전기 | 핫플 | 오븐 |
|---|---|---|---|---|---|---|---|
| 12.34 운영점 (SMPS 38ch) | 9.22 | 10.07 | 0.740 | 0.814 | **0.956** | 0.838 | 0.925 |
| 12.36 (SMPS 48ch) | 8.98 | **8.93** | 0.759 | 0.834 | 0.935 | 0.838 | 0.925 |
| **현재 (48ch + 커버리지)** | **7.86** | 10.69 | **0.804** | **0.839** | 0.935 | 0.838 | 0.925 |

(유령·잔차는 6파일 평균 W, F1 은 사람 기록 3파일 평균)

### 체크포인트

| 이름 | 채널 | 캐시 | 비고 |
|---|---|---|---|
| `cnn_ph0` / `cnn_ph1` | 38 / 44 | `train60_ph` | 12.34 위상 채널 대조·처치 |
| `adapt_ph0` / `adapt_ph1` | 38 / 44 | 〃 | 2단계 적응 |
| `cnn_pf0` / `cnn_pf1` | 44 / 48 | `train60_pf` | 12.36 PF·고차비·φ9 |
| `cnn_sp1` | 44 | `train60_ph` | 12.35.3 state_power (반증) |
| `cnn_tr1` | 51 | (삭제됨) | 12.37 과도 채널 (반증, 재검토 필요) |
| **`cnn_ov1`** | **48** | **`train60_ov`** | **12.38 커버리지 수정 (현재 최선)** |

캐시는 `cache/train60_{ph,pf,ov}` 가 살아 있다. `train60_{,_nohp2,_v17}` 는
이전 세션 것이고 지금 비교에 안 쓴다 (42GB, 지워도 된다).

### 입력 채널 (48)

```
0~29   Re/Im 고조파 15차     30 P   31 Q   32 V
33~35  |I3|/|I1|, |I5|/|I1|, |I2|/|I1|
36~37  추세 제거 전력 (±0.5초 / ±2.5초)
38~45  고조파 위상 불변량 φ3 φ5 φ7 φ9 (cos/sin 쌍)      <- 12.34, 12.36
46     PF = P / (V·sqrt(Σ|I_h|²))                      <- 12.36
47     |I9|/|I3|                                       <- 12.36
48~50  |I3| 과도 서술자 3개   **`>= 51` 가드 뒤에 비활성**   <- 12.37
```

새 채널은 **반드시 뒤에 붙인다.** 옛 체크포인트는 `NILMNet(fine_channels=N)` 이
입력을 슬라이스해 그대로 돈다. 체크포인트에 `fine_channels` 가 저장되고, 없으면
`LEGACY_FINE_CHANNELS = 38` 로 본다. 검증: 38채널 체크포인트로 `run_gate_check`
재실행 시 **180개 값 전부 0 차이**.

---

## 2. 확정된 것

### 2.1 유령(오귀속)은 관측 ~1300W 를 넘을 때 터진다

인수인계 4절 ①의 "1kW 급 부하가 실재해야 헛것을 본다" 는 부족한 서술이다.

```
cnn_v17 유령W    <300W  300-800W  800-1300W  >1300W
test_4            2.92     7.18      61.71   699.22
test_5            1.51     3.06     232.75   593.83
test_6            1.03     1.67     107.91   979.54
```

`test.2`/`test3` 는 1178W 오븐이 있는데 유령이 7~16W 다. 셋 다(test_7 포함)
**>1300W 구간에 진입한 창이 0개**라 효과가 나타날 수 없었다.

따라서 인수인계의 `test_5` vs `test_7` 대조는 **교락돼 있다** — test_7 은 관측
최대가 144.2W 다. 교락 없는 형태는 파일 안에 있다: test_5 하나에서 미니PC F1 이
0.824(<300W) vs 0.409(>=300W).

### 2.2 SMPS 3종이 안 갈리는 이유 — 기기 자신이 기기 사이 차이보다 더 변한다

와트당 고조파 벡터의 기기 간 차이 방향으로 기기 **내부** 변동을 투영하면:

```
미니PC vs 프로젝터 2.15   미니PC vs 충전기 1.44   프로젝터 vs 충전기 1.12
```

(2 미만이면 겹침) **부하를 고정하면 2.2~4.2 로 갈린다.**

`test_7` 전이를 다 재보면 실패가 **전부 프로젝터↔충전기 맞바꿈**이고, 급속충전 중
충전기의 I7/I3 가 프로젝터 지문(0.626~0.639) 위에 올라앉는 구간에 몰려 있다.

```
 145.2  충전기 off   관측 I7/I3 0.630->0.657   프로젝터를 -50W 껐다   실패
 175.2  충전기 on    관측 I7/I3 0.656->0.637   프로젝터를 +52W 켰다   실패
 457.7  충전기 on    관측 I7/I3 0.862->0.751   정확
 600.7  충전기 off   관측 I7/I3 0.739->0.848   정확
```

**38 / 48 / 51채널 세 모델이 145.2초에서 전부 똑같이 프로젝터를 -50W 끈다.**

### 2.3 오븐 히터는 3차를 거의 안 흘린다 — 복합 창 분해의 핵심 사실

```
오븐 히터 1156W:  |I1| 5.34A   |I3| 0.0124A   |I5| 0.0572A   |I7| 0.0725A
                 I3/I1 = 0.0023 (거의 없음),  I7/I3 = 5.85 (매우 특이)
```

복합 창에서 오븐이 차지하는 몫: I3 의 6~10%, **I5 의 38~97%, I7 의 40~72%**.
그래서 SMPS 의 I7/I3 가 히터 통전 중 0.75~0.86 으로 뭉치는데, 그것은 **전압 효과가
아니라 오븐 고조파의 선형 중첩**이다. (SMPS 단독)+(오븐 단독) 을 복소로 더하면
실제 합계가 ±10% 안에서 재현된다.

### 2.4 손실은 수렴한다. 유령이 손실이 안 건드리는 방향이다 (12.38)

```
합성 홀드아웃 ep100 이후   F1 폭 ±0.007~0.017   MAE 폭 ±0.28~0.38W   <- 수렴
실측 유령    ep100 이후   33~87W 를 헤맨다                          <- 안 수렴
```

원인: **실측 test_4/5/6 의 >1300W 구간 구성(포트 없음 + 오븐·핫플 동시 통전)이
학습 창의 1.001%** 였다. 99% 에서 답이 확정되니 손실은 수렴하고, 그 1% 방향은
미결정으로 남는다.

`resistive_overlap` 레시피(5%)가 저항 4종에서 무작위 쌍을 뽑아 오븐+핫플이 1/6.
**총량은 그대로 두고 쌍 선택만 기울여** 고쳤다 (`RESISTIVE_OVERLAP_PREFER_P = 0.6`,
`RESISTIVE_OVERLAP_EXCLUDE = ("electiric_kettle",)`):

```
목표 구성  1.001% -> 3.265%    포트 양성률 11.36% -> 9.68%
오븐/핫플 양성률 11.9/7.4% -> 13.0/8.7%    >=1300W 창 비율 12.95% -> 12.84%
```

편향 0.85 도 쟀다 — 목표를 +1.1%p 더 올리는 대가로 오븐 +1.1%p 를 더 미는데
포트는 -0.18%p 뿐이라 **0.60 이 무릎**이다.

### 2.5 실측 정답 결함 두 개를 고쳤다

| | 이전 | 재유도 | 검증 |
|---|---|---|---|
| test_5 오븐 히터 | 9개 112.5초 | 8개 178.5초 | 관측>=900W 설명률 62.6% -> **100%** |
| test_5 핫플 | 124개 160.0초 | 192개 251.5초 | 세션 안 3초 넘는 공백 **없음** |
| test_6 핫플 | 82개 159.0초 | 104개 151.9초 | 라벨 ON 의 300W 미만 7.06% -> **0.00%** |

`test_4` 를 대조군으로 썼다 — 같은 방법이 그 파일의 기존 라벨을 그대로 재현한다.
백업 `processed_data/real_events.json.bak_20260824`.

> **핫플 라벨은 통전 단위다.** 스위치는 켜져 있어도 서모스탯이 히터를 끊는다.
> 오븐 히터가 없는 test_5 세션 구간에서 라벨 ON 은 관측 P 중앙 569W, OFF 는 109W
> 이고, 모델 게이트도 1.000 / 0.000 으로 따라간다. 세션 단위로 채점하면 모델이
> 옳게 껐다고 답한 67.4초가 전부 미탐이 된다 (12.13.1 이 기록한 왜곡).

---

## 3. 반증된 것 — 다시 시도하지 말 것 (단, 3.4 는 예외)

### 3.1 "전압강하가 SMPS 판별자를 무너뜨린다"

오븐 고조파의 **선형 중첩**이었다 (2.3절). `apply_cross_appliance_coupling` 에
5·7차 항이 없다는 관찰은 사실이지만, 그것이 틀렸다는 증거는 없다.

### 3.2 "부하 상태를 조건으로 쓰게 하면 된다" (12.35.3)

라벨도 구조도 이미 있었다 (`p_raw = Σ softmax(state)·p_states`). 상태별 전력 출력만
붕괴해 있어 `state_power` 항으로 고쳤더니 **항은 의도대로 동작하는데 목적은
달성 못 했다** (전이 8/13 -> 7/13, 유령 42.1 -> 86.9W).

**부하 상태는 독립적인 정보원이 아니다** — 조건으로 삼아야 할 그 상태를 모델이
스스로 추론해야 한다. **회귀는 돕고 귀속은 못 돕는다.**
`--w-state-power` 기본값 0. 기능은 남아 있다.

### 3.3 "순시 특징을 더하면 프로젝터↔충전기가 갈린다" (12.36)

후보 60개를 훑어 `|I9|/|I3| + φ3 + PF` 셋이 격리 녹화에서 최악 쌍 d' 를
1.97 -> **3.79** 로 벌린다. 넣었더니:

- **미니PC 는 실제로 좋아졌다** (같은 캐시 대조군 대비 F1 0.638 -> 0.759, +0.121)
- **프로젝터↔충전기는 전혀 안 움직였다** (145.2초 맞바꿈 동일)

미니PC 는 크기가 다르지만(8~26W) 프로젝터(47W)와 급속충전 충전기(45W)는 크기까지
같다. 크기가 같고 형상이 근접하면 합에서 바꿔 넣어도 관측이 거의 안 변한다.

### 3.4 스위칭 과도 채널 (12.37) — **재검토 대상**

`|I3|` 기반 과도 서술자 3개를 만들고 사전 기준 4개를 다 못 넘어 되돌렸다
(전이 8/13, 유령 83.5W, 미니PC 0.724, 합성 F1 0.975).

**그런데 그 83.5W 는 12.38 이 밝힌 미결정 상태에서 나온 값이다.** 커버리지를
고친 뒤 같은 채널을 다시 시험해야 판정이 성립한다. **인수인계 1순위다** (4.1절).

만드는 과정에서 확정된 것들은 유효하다:

- **창을 줄이는 것은 지렛대가 아니다.** 병목은 창 길이가 아니라 **깊은 층 타깃
  슬라이스의 수용영역 187 사이클(±1.56초)** 이고 커널·dilation 이 정한다.
  앞쪽을 2.8초 아래로 줄이면 충전기 돌입 2.76초가 오히려 잘린다
- **P 가 아니라 |I3| 로 만들어야 한다.** 오븐 히터가 켜진 창에서 충전기 돌입이
  차지하는 비중이 P 2.5% vs |I3| **57.5%** 다. P 기반은 기존 추세제거 ch37 과
  r = 0.82~0.91 로 중복이기도 하다
- **최대값이 아니라 중앙값이어야 한다.** 저항 부하도 통전 순간엔 3차를 흘리는데
  (포트 +0.174A) **0.02초**뿐이고 충전기는 **2.68초**다. 최대값을 쓰면 포트가
  충전기의 9.2배가 되어 'SMPS 돌입' 대신 '큰 저항 부하가 켜졌다' 를 잰다
- **모델은 그 채널을 확실히 쓴다** — 절제 민감도 2위(0.249), 위상(0.064)의 3.9배

---

## 4. 다음에 할 것 (우선순위 순)

### 4.1 과도 채널 × 커버리지 수정 재시험 ← **1순위**

3.4 의 이유다. 12.37 의 반증이 오염된 조건에서 나왔다.

```bash
# inputs.py 의 FINE_CHANNELS 를 51 로 되돌린다 (코드는 가드 뒤에 그대로 있다)
python -m src.run_build_traincache --out cache/train60_tr2 --windows 300000 \
    --split train --seed 0 --workers 8
python -m src.run_train_cnn --epochs 300 --epoch-windows 50000 --batch 512 \
    --w-over 0 --eval-every 10 --snapshot-every 50 --cache cache/train60_tr2 --tag cnn_tr2
```

**대조군**: 새 캐시에서 `--fine-channels 48` 로 한 번 더 학습하거나, 앞 48채널이
`cache/train60_ov` 와 바이트 일치하면 `cnn_ov1` 을 그대로 쓴다 (확인 필수).

판정: `test_7` 전이 8/13 초과, 유령 28.8W 이하, 미니PC 0.804 이상.

### 4.2 분산 확정 — 이 세션의 최대 부채

**설정당 실행이 1회씩이라 이번 세션의 모든 비교가 확정되지 않았다.**
위상 채널 42 vs 87W, 과도 채널 35 vs 84W, 커버리지 34.9 vs 28.8W — 전부 n=1 이다.
게다가 채널 수를 바꾸면 텐서 모양이 달라져 초기화 난수도 바뀌므로 **설정과 시드가
교락**돼 있다.

```bash
for S in 1 2; do
  python -m src.run_train_cnn --epochs 300 --epoch-windows 50000 --batch 512 \
      --w-over 0 --eval-every 10 --snapshot-every 50 --cache cache/train60_ov \
      --seed $S --tag cnn_ov1_s$S
done
```

같은 캐시·같은 채널에서 시드만 바꾼다. 유령의 실행 간 분산이 나오면 그때부터
설정 비교가 신뢰할 만해진다. **이것을 먼저 하면 4.1 의 판정도 단단해진다.**

### 4.3 프로젝터↔충전기 — 시간 연속성

3.3 이 확정한 대로 순시 특징으로는 끝났다. 남은 정보는 시간축이다:

```
프로젝터  전 녹화 폭 1.02배 (47.2~48.4W)     <- 미동도 없다
충전기    48.5분에 걸쳐 67W -> 33W 단조 감소  <- 계속 흐른다
```

지금 모델은 60초 창을 **독립으로** 본다 — 순환 구조도 창 사이 상태 전달도 없다.
145.2초에 45W 가 사라졌을 때 "직전 2분간 충전기가 45W 를 끌었다" 를 못 쓴다.

**재학습 없이 후처리로 먼저 값어치를 잰다** — 기기별 상태 유지 필터를 예측
시계열에 걸어 전이 시점의 귀속을 직전 이력으로 교정한다.

### 4.4 남은 작은 것들

- **`run_live` / `run_plot_timeline` 기본 체크포인트**가 아직 `adapt_v17`/`cnn_v17`
  이다. 운영점을 `adapt_ph1` + `cnn_ov1` 로 옮길지 결정
- **B-1 판정 기준이 깨져 있다** (12.34 이전부터). 인수인계 5절이 "유령 <= 10W" 로
  적었는데 현직 하이브리드가 test_6 에서 15.16W 다. 그 유령은 저항 갈래에서 오므로
  SMPS 게이트 동결로는 원리적으로 못 고친다
- **미니PC 부하 범위**가 여전히 모자란다. 창 계층화로 >=20W 가 6.0% -> 9.4% 가
  됐지만 실측 30.3W 는 합성 60초 창의 최대(27.9W) 밖이다. **재측정이 필요하다** —
  4.5절
- **B-3(충전 궤적)은 실체가 거의 없다.** 궤적은 이미 풀에 있고(활성화 #0/#1 이
  길이 가중 추첨의 97%), 실측 test_6 43.7W 는 합성 분포의 40 백분위다.
  급속충전 72.6W 만 99.9 백분위로 바깥이다

### 4.5 다음 측정 때 반드시 넣을 것

1. **미니PC 만 켜 놓고 CPU 부하를 1~2분 연속으로 거는 구간.** `test_7` 에는
   미니PC 단독 + 작업중 구간이 0초라(작업 때 항상 다른 SMPS 가 같이 켜져 있었다)
   φ3 지문과 부하 범위를 실측 복합에서 직접 검증하지 못했다
2. **저항 부하 없는 녹화를 더.** `test_7` 같은 파일이 전가와 분해 능력을 분리해
   준다 — 지금 1개뿐이다
3. **오븐·핫플을 동시에 오래 켠 구간.** 학습에서 그 구성을 3.265% 로 올렸지만
   실측 검증 표본은 test_4/5/6 뿐이다

---

## 5. 도구 / 실행 메모

### 이번에 추가·수정된 것

- `run_sim_to_real` — `select=` 로 **라벨 기반 구간 선택**, `I5/I3`·`I7/I3` 지표,
  `[4]` 저항 부하가 SMPS 지문을 덮는 정도, `[5]` 결합 함수 직접 검사.
  **`n_seeds` 5 -> 20** (5시드는 충전기에서 부호까지 뒤집혔다)
- `run_train_cnn --fine-channels N` — 같은 캐시에서 채널 수만 다른 대조군 학습
- `run_train_cnn --w-state-power W` — 12.35.3 의 항. 기본 0 (반증됨)
- `run_plot_timeline --ckpt-smps` — 운영 조합 그대로 그린다
- `DataAugmentor(load_stratified=)` — 창 자르는 지점을 전력으로 계층화.
  미니PC >=20W 가 6.0% -> 9.4%. **활성화 단위 계층화는 반증됐다** (저부하 계층이
  14.9% -> 33.3% 로 올라 오히려 나빠진다)
- `score_events` — `delta_p_w` 없는 이벤트를 건너뛴다 (test_6/test_7 은 사람이
  전이 시각만 적었다)
- 라벨 물리 검사에 **0.25초 가드** (라벨 시각이 seq 기준 0.5초 양자화인데 핫플
  통전 펄스가 중앙 1.5초다)

### ⚠ 시스템

**2026-08-24 09:02 에 `KERNEL_SECURITY_CHECK_FAILURE` (0x139, 하위코드 3 =
LIST_ENTRY 손상) 로 다운됐다.** 캐시 빌드 중이었다 (워커 11, 18GB memmap 쓰기).

- 최근 14일 중 처음이고 덤프 수집이 꺼져 있어 원인 미확정
- **워커를 8로 낮춘 뒤에는 재발하지 않았다** (`--workers 8`, 빌드 13분 -> 15분)
- 권장: 백신 실시간 검사에서 `cache/` 제외, 크래시 덤프를 "작은 메모리 덤프" 로

무거운 작업 전에 커밋해 둘 것. 캐시는 재생성되지만 학습 결과는 아니다.

### 자주 쓰는 명령

```bash
# 실측 채점 (6파일, 자동으로 비봉인 전부)
python -m src.run_gate_check --ckpt results/cnn_ov1.pt --out results/gate_check.json

# 운영 조합 그대로 그리기
python -m src.run_plot_timeline --ckpt results/adapt_ph1.pt \
    --ckpt-smps results/cnn_ov1.pt --tag mixed

# sim-to-real (라벨 기반 구간 포함, 20시드)
python -m src.run_sim_to_real

# 2단계 적응 — **캐시를 반드시 지정할 것** (채널 수가 안 맞으면 즉시 멈춘다)
python -m src.run_adapt --init results/cnn_ov1.pt --cache cache/train60_ov --tag adapt_ov1
```

> **`run_adapt` 는 이제 실측 6파일로 학습한다** (전처리로 test_5/6/7 이
> `composite_eval` 에 들어가면서 `RealWindows` 가 자동으로 가져간다). 3파일 26.3분
> -> 6파일 60.2분. `adapt_v17` 과는 비교할 수 없다.
