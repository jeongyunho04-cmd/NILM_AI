# 인수인계 2026-09-09 — 붕괴의 뿌리를 찾았다 (13.59~13.68)

설계 기록 **13.59~13.68** 이 정본이다. 여기는 산출물·다음 할 일만 적는다.
직전 인수인계는 `HANDOFF_2026-09-08.md` (13.55~13.58).

## 운영점 — 바뀌었다

```
새 최선   cnn_v24b + `--no-standby-harm --harm-weight h1`
          자리 D 프로젝터 9.2 -> **39.7W** (4시드 평균, 참 44.6) · 대조 대비 4.3배
          배분 총오차 63.6 -> **18.4W** · 실측 on/off F1 0.716 -> 0.780
          프로젝터 실측 AUC +0.028 (3/3) · 죽은 헤드 없음
⚠ 반드시 `cnn_v24b`(보조 Z 헤드) 에서 출발할 것 — `cnn_v23` 출발은 프로젝터 11W (13.65.3)
⚠ 후처리는 켜지 않는다 — F1 +0.033 을 벌지만 test_2 에 없는 핫플을 23.8W 켠다 (13.65.2)
⚠ 결과가 **두 갈래**다 (프로 44 / 프로 ~20). 시드 하나로 읽지 말 것
잃은 것  오귀속 101 -> 115W. 다만 그중 87% 는 이번 세션 이전 문제다 (13.65.2)
```

## 채택 (검정 끝)

```
--no-standby-harm     `L_harm` 에서 대기 지문을 끈다. `L_cons` 의 대기 전력은 그대로다
판     adapt: nosbharm_s0~2 (+ s3·s4)
근거   13.61.3 기울기 귀속 — 자리 D·SMPS 전용 창 압력의 60%가 이 통로였다
결과   프로젝터 15.1/4.5/7.9 -> 43.8/16.6/17.3 (참 44.6)   3/3 양수, 평균 +16.7W
       배분 총오차 63.6 -> 42.7W (−33%) · SMPS 합 82.0 -> 88.8 (참 92.4)
부작용  실측 AUC 프로젝터 0.931 -> 0.959 (3/3) · 나머지 |Δ|<=0.03 · 죽은 헤드 없음
       선풍기 s2·s3 와 오븐 대기 개선(3/3) · 충전기 s1 악화(3/3)
⚠ 시드 0 에서만 미니PC 가 0.2W 로 죽는다 (참 8.9). 시드 1·2 는 6.6 으로 멀쩡
```

## 기전 (13.60~13.61)

```
2단계는 `y_standby`(규약: 활성 중이면 0) 라벨을 놓는다. 그러면 두 통로가 열린다

① 전력  out["standby"] 가 게이트 없이 L_cons 의 recon 에 들어간다
        1단계 1.06W -> 2단계 6.93W (조합 차분 예산 0.6W)
        초과 8W 가 SMPS 에서 빠진다 — 92.4 − 83.8 = 8.6, 정확히 일치
② 지문  idle = σ(plugged)(1−σ(on)) 이 standby_sig 를 켠다
        게이트를 내리면 그 기기의 대기 고조파가 공짜로 들어온다
        h11 절대 크기: 에어컨 8.83 · 미니PC 5.03 · 프로젝터 4.71 mA

②가 지렛대다. ①만 막는 `--w-standby` 는 안 먹었고 ②를 막으니 먹었다
```

## 반증·정정한 것

```
자리/Z 로 조건화 (13.59)      6판 중 5판에서 프로젝터가 더 깎였다. 순방향 모형은
                            정확해졌는데(h13 과대 +92% -> −7%) 결과는 나빠졌다
회로 모델로 sig(Z,h) (13.59.6) 프로젝터·충전기가 h11 부터 무너진다 (자리 D h13 2.25배)
축은 Z 다 (13.59.6)          아니다. 임피던스 축은 h11 에서 0.91~0.93 만 움직인다.
                            그 콘센트의 **전압 고조파**가 축이고, 그건 이미 입력이다
L_sb 대기 닻 (13.60.7)       전력 통로만 막아 효과 없음. 오히려 착취를 싸게 만든다
--w-cons 0                   효과 없음 (9.0W)
--w-real-on 1.0              효과 없음. 판정 파일 test_3/4/5 에 사람 로그가 없다
대기 지문을 저항만 끔         **더 나쁘다** (5.3W). 지렛대는 와트당 비가 아니라 절대 크기
inv_h2 가 판별 차수를 누른다  **철회.** 학습이 반대를 말한다 (아래)
TRUE_W = 43/39/8             조합 차분으로 다시 재니 **44.6/38.9/8.9W** (±2W 안)
```

## `harm_weight` — 다시 쟀다 (13.63)

`inv_h2` 는 12.135, 옛 계측기 값이고 **새 자료에서 학습으로 견준 적이 없었다.**

```
가중 (시드 0, 대기지문끔)     프로     배분 총오차      시드 평균 (프로 / 총오차)
off (균등)                  13.2       68.1
inv_h                       36.4       32.0
inv_h2                      43.8       16.5          28.3 / ~39   (5시드)
inv_h3                      44.4        5.1          34.8 / 24.1  (3시드)
inv_h4                      44.3        7.1          38.1 / 19.5  (3시드)
**h1 만**                   45.4        6.0        **39.7 / 18.4** (4시드)
```

**단조이고 `inv_h4` 에서 포화한다.** `h1` 만 써도 안 무너지는 이유는 `L_cons` 가
유효전력만 보고 **h1 의 위상**은 못 보기 때문이다 — 두 SMPS 가 7° 차이고 그것이
총전력 고정 조건에서 d′ 3.30 으로 가장 센 판별자다 (13.62).

⚠ 인수인계 이전 판의 B("inv_h2 가 판별 차수를 225배 눌러서 문제")는 **철회**한다.

## `L_cons` 손잡이 — 채택 안 함 (13.64)

```
팔                 프로 평균(4시드)   test_5 잔차|W|
h1+nosb              39.7              26.3
+ --cons-asinh 10    39.3             102.0        <- 4배 나빠진다
+ --cons-deadzone 1  (시드1만) 40.3     —
```
어려운 시드(23.4W)를 넷 다 구하지만 **시드를 붙이면 2승 2패**이고 총전력 잔차가
전 파일에서 나빠진다. ⚠ `--smps-boost` 는 `real_weight="none"` 이라 애초에 안 걸린다.

## SMPS 배분은 무엇으로 가리나 (13.62)

```
프로젝터<->충전기 10W 이동    d′(무조건부)   d′(총전력 고정)
|I1|                          0.160          **3.301**   <- 위상 7°
|I3|                          0.146            1.614
|I13|                       **1.189**          1.344
Q                             0.228            0.958      <- 복소 h1 과 같은 정보
P                             0                  —        <- 맞바뀜은 합을 안 바꾼다
```

**정보는 있다** (577창 누적 d′ 28~54). 못 가른 이유는 ① 그 차수의 순방향 모형 오차
(13.59.7 격리->복합 전이) ② 손실의 더 싼 우회로(13.61 대기 지문)였다. **②는 막았다.**

⚠ `--w-consq`(Q 보존)를 켤 이유가 약하다 — `Q ≈ V·|I1|·sinφ` 라 복소 h1 과 같은 정보다.
  12.133 의 "Q/P 가 고조파보다 2.2~2.5배" 는 옛 계측기 결론이다.

## 다음 (권장 순서)

### A. 1단계의 포트↔오븐 축퇴 — **다음에 할 것** (13.66)
```
증상   test_5 포트 F1 **0.000**, 잔차 +346W (관측 − 예측; 과소예측이다)
정보   포트 순위 AUC **0.959** — 있다. 게이트가 p50 0.002 로 0.5 를 안 넘을 뿐
귀속   오븐이 **꺼진** 59창에서 오븐 게이트 1.000 · 1378W  (포트 1456W 를 오븐으로)
분포   캐시의 포트+오븐 동시 6.39% · 총전력 최대 3795W -> **동작점 부족이 아니다**
반증   --w-res 1.0 은 전면적으로 나쁘다 (잔차 8.9->24.5 · 오귀속 115->207)
        더 낮은 가중은 안 재 봤다
```
1단계 게이트 보정만 고치면 적응이 AUC 를 0.959 -> 0.794 로 깎는 것도 안 해도 된다.

### A2. 생성기 — 위상 기준을 고쳤다 (13.68). 회로 재적합은 여전히 막혔다 (13.67)

**2026-09-09 적용됨.** `sim_harmonics` 가 계측 영역 전류를 **참** 전압에 기준했다 —
펌웨어 규약은 **계측** 전압이라 h 에 비례해 1.2958°/h 씩 돌아갔다 (h15 에서 19.44°).

```
판정   run_mixval12 짝지은 477창
       [A] 녹화 중첩   0.2490 -> 0.2490  ±0.0000   ← 회로 안 타는 경로. 통제군
       [B] +텍스처 델타 0.1404 -> **0.1152**  ← 생성기가 타는 길. 431/477창 개선
       [D] 모델 단독   0.2068 -> 0.1769
원시 28구간 복소오차  h3 0.132->0.074 · h5 0.189->0.083 · h7 0.292->0.134 · h9 0.352->0.173
재적합   **불필요.** fit12 는 sim_harmonics 를 안 부르고 위상을 목적함수에 넣은 적이 없다
```

⚠ **`cache/train60_v24` 는 이제 낡았다.** 1단계를 다시 받으려면 캐시 재구축(~3h25m) +
재훈련이 필요하다. **2단계는 영향 없다** — `losses.py` 가 회로 모델을 안 쓴다.

⚠ 안 한 것 둘. ① **전압 h17+** (`wave_from_harmonics` 의 `X[1:16]` 절단). h11+ 의 남은
오차는 전부 이것이지만 텍스처 라이브러리에 h15 까지밖에 없어 먹일 자료가 없다.
그래서 "h9 제한 철회" 는 아직 이르다. ② **`sim_wave` 의 블록평균 29.84µs**
(`Ic.reshape(npc, up).mean(1)` 이 평균을 블록 첫 표본에 놓는다, +0.6447°/h). 별개 결함이고
고치려면 **재적합을 세트로** 해야 한다. ③ `circuit_model/sp_model_v12/` 는 굳은 배포 묶음이라
버그를 그대로 갖고 있다 (src 가 임포트 안 함).

#### A2-이전. 회로 재적합은 막혔다 (13.67)
```
합성/실측 현장지문 비   h9 에서 프로 +56% · 충전 −33% · 미니 −49% (h1 은 0.90~1.03)
혼합검증 [B] h13       자리 D 1.36~1.60 (단독 SMPS 창도 44~116%)
시도                   fit12 에 `--harm-mag` (고차 크기 로그비) 추가 -> **거의 안 움직임**
                       넷 중 셋이 조금 나빠짐. 원본 pkl 복원 (circuit_model/_bak/)
결론                   회로 위상(v12g)이 그 고차를 못 만든다. [A]·[B]·[D] 어느 경로도 못 만든다
남은 길                **측정한 자리별 지문**(13.59.1 sig_site)을 생성기 고차에 쓰기
                       ⚠ 자리가 둘뿐 — r 을 연속으로 훑는 생성기와 어떻게 맞출지가 설계 문제
```

### A3. 격리->복합 전이 (13.59.7)
조합 홀드아웃에서 격리 0.4021 · 자리 보정 0.2866 · **현장 적합 0.1241**.
맞바뀜 창에서는 0.4425 / 0.4134 / **0.0912**.
`--sig-insitu` 를 새 자료로 다시 세우는 길. ⚠ 대조는 이제 `nosbharm_s0~2` 다.

### B. 미니PC 붕괴 (시드 0 에서만)
`--no-standby-harm` 시드 0 에서 미니PC 가 0.2W (참 8.9). 13.58.8 의 `--w-pref 2.0` 과
같은 자리(프로 43.8 · 미니 0.2)라 끌개가 있어 보인다. `--pref-apps` 에 미니PC 를
넣거나 `power_ref` 를 주는 것이 후보 — ⚠ 미니PC 는 녹화마다 전력이 다르다(9~25W)라
`REFERENCE_W` 채택 문턱을 통과 못 한다 (규칙 14).

### C. 남은 것
```
· 대기 지문을 **절대 크기**로 가르는 판 (--standby-harm-max-ma) 은 아직 안 만들었다.
  지금은 전부 끄기(--no-standby-harm)와 와트당 비(--standby-harm-max-ratio, 반증됨)뿐
· pref 가 프로젝터를 끌어내린다 (13.61.1, 코사인 −0.259). 미해명, 노름이 작다
· 복합에서 고차가 격리보다 크다 (13.59.7). 선로 초크 직관과 반대. 미해명
· 저항 창 타깃 편향 · 포트 실측 AUC · deploy/nilm_runtime 사본 낡음 (이월)
```

## 이번에 만든 도구

```
src/run_alloc_landscape.py   진짜 `unlabeled()` 로 배분 격자를 훑는다
                             ⚠ `모델 그대로` 줄이 격자 최소보다 낮으면 반사실이
                               도달 가능 집합을 못 덮는 것이다 — 13.60 이 그걸로 답을 찾았다
src/run_grad_attrib.py       항별·창묶음별 기울기 귀속 (⟨−g_T, ĝ_S⟩)
                             ⚠ 묶음별은 `--repeat 6` 이상. 배치 하나면 잡음이 2.6배
src/run_smps_identify.py     특징별 판별력 d′ (무조건부 · 총전력 고정)
src/model/sig_site.py        자리별 지문 (**처방은 반증됐다**, 측정 도구로만)
src/run_real_score.py        실측 지표 넷을 **후처리 전후로**. `--detail` 이 오귀속·검출을
                             파일 x 기기로 쪼갠다 — 13.66 이 그걸로 포트 F1 0.000 을 찾았다
run_plot_real --postproc {off,cap,full}
run_adapt --no-standby-harm · --standby-harm-max-ratio · --w-standby ·
          --cons-deadzone · --cons-asinh · harm-weight {inv_h3,inv_h4,h1}
circuit_model/fit12.py --harm-mag   (고차 크기 항. **효과 없었다** — 13.67.2)
NILMLoss(standby_w=..., signatures_site=...) + `L_sb`
```

## 되풀이 방지 — 이번에 내가 낸 오류

```
잔차 부호            `run_plot_real` 은 **관측 − 예측**이다. +346W 는 과소예측
기기 열 순서          캐시·모델은 **알파벳순**, `list(S_I)` 와 다르다. 그 탓에
                     "포트 라벨 285W" · "포트+오븐 0.41%" 를 냈다 (참 1276W · 6.39%)
시드 하나로 판정      `--cons-asinh 10` 을 "유일하게 안 다치는 팔" 이라 적었다가
                     4시드에서 2승 2패로 철회
와트당 비 대 절대     대기 지문 지렛대를 와트당 비로 골라 저항만 껐다가 더 나빠졌다
보조 Z 헤드 무용론    掃引 폭이 안 줄어 "되먹임 부재" 로 기울었는데, cnn_v23 출발로
                     갈아 보니 프로젝터가 11W 로 무너진다 — 공유 몸통으로 되먹인다
```

⚠ **정적 반사실 지형은 학습이 가는 자리를 예측하지 못한다.** 이번에 두 번 데었다
  (자리 조건화 · 대기 닻). 기울기 귀속으로 바꾸자 바로 답이 나왔다.
