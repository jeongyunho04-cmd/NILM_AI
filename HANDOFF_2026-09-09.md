# 인수인계 2026-09-09 — 자리 조건화 반증, 축은 격리->복합 전이 (13.59)

설계 기록 **13.59** 가 정본이다. 여기는 산출물·다음 할 일만 적는다.
직전 인수인계는 `HANDOFF_2026-09-08.md` (13.55~13.58).

## 한 줄

`HANDOFF_2026-09-08.md` 의 **다음 할 일 A(2단계 지문을 Z/자리로 조건화)를 끝까지
하고 반증했다.** 순방향 모형은 확실히 정확해졌는데(자리 D h13 과대 +92% -> −7%)
자리 D 프로젝터는 **짝지은 6판 중 5판에서 더 나빠졌다**. 그 과정에서 진짜 축이 나왔다:
**격리 지문이 복합으로 전이되지 않는다** (조합 홀드아웃에서 자리 보정 −7%, 현장 적합 −79%).

## 이번에 만든 것

```
코드   src/model/sig_site.py         자리별 지문 묶음 (D/E + 뭉친 줄). **기본 꺼짐**
       NILMLoss(signatures_site, signatures_state_site) + tgt["site_idx"]
       run_adapt --sig-site
       run_loss_compare --sig-site {D,E}
검정   tests/test_baseline.py::test_site_signature_bank_falls_back_exactly_and_keeps_states
       뭉친 줄로 보내면 **비트 단위로 같다** (차 0.00e+00) + 상태 축 보존
판     adapt_v24b_zsite_s0~2   ⚠ **결함판** (13.59.5 의 상태 문턱 결함). 지워도 된다
       adapt_v24b_site_s0~2    고침판. 반증의 증거로 남긴다
```

## 운영점 — **안 바뀌었다**

```
현재 최선   cnn_v24b + adapt_v24b_z_s*        (13.55~13.58 그대로)
⚠ 자리 D 프로젝터는 여전히 **1단계 cnn_v24b(31.8W, 참 44.6)가 어떤 적응판보다 낫다**
```

## 반증·정정한 것

```
"자리/Z 로 조건화하면 고쳐진다"   반증. 6판 중 5판에서 프로젝터가 더 깎였다 (13.59.3)
  이유: 참 배분의 잔차는 h13 **과대**가 아니라 h9·h11 **과소**다. 손실은 부족한
  h11 을 가장 싸게 내는 쪽으로 옮기는데 자리 D 와트당 h11 이 프로 0.651 · 충전
  0.789 · 미니 1.564 mA/W 라 프로젝터가 제일 비싸다. 자리 보정은 그 순위를 **벌린다**

"축은 Z 다"                     아니다. 2x2 로 가르면 임피던스 축은 h11 에서 0.91~0.93
  밖에 안 움직인다 (실측 0.52~0.55). 움직이는 것은 그 콘센트의 **전압 고조파**다.
  ⚠ 그런데 그것은 이미 입력이다 (채널 45~56). 못 보는 것은 **손실의 순방향 모형**

"회로 모델로 sig(Z,h) 를 짓는다"  못 쓴다. 격리 녹화로 직접 검정하니 미니PC 는 h13 까지
  ±2% 인데 **프로젝터·충전기는 h11 부터 무너진다** (자리 D h13 에서 2.25배·1.72배).
  하필 맞바뀌는 두 기기, 하필 판별 차수다. 13.52 의 "회로가 1.60배 나쁘다" 가
  교락 없이 확인된 셈 (13.59.6)

TRUE_W = 43/39/8               유도가 기록에 없었다. 조합 차분으로 다시 재니
  **44.6 / 38.9 / 8.9W** (대기 되돌림 포함, 조합 6개 잔차 0.63W). ±2W 안이라 결론은
  안 바뀐다. ⚠ 미니PC 는 녹화마다 다르다 — test_1 에서는 같은 계산이 24.9W 다

inv_h2 가 판별 차수를 누른다     지금은 그것이 **프로젝터를 지켜 주고 있다**. 균등
  가중으로 자유 최소를 훑으면 뭉친·격리 지문이 프로젝터를 0~2W 로 민다 (13.59.8).
  인수인계의 B 를 하려면 **지문을 먼저** 고쳐야 한다 — 순서가 거꾸로였다
```

## SMPS 아닌 기기에 미치는 영향 (사용자 질문)

```
지문      SMPS 아닌 기기는 **한 자리도 안 바뀐다** (|Δsig| = 0.000000 mA/W).
          한 자리에서만 녹화된 기기는 그 자리의 사이클이 곧 전체 사이클이라 정의상 같다
관문      안 움직인다. 짝지은 실측 AUC 차가 전부 |Δ| <= 0.024, 부호 섞임 (문턱 0.03)
전력 헤드  **움직인다.** 3시드 전부 같은 방향:
            선풍기 s2 −1.6W · s3 −1.4W · 드라이기 s1 −13W · 오븐 s1 +1.3W  (−3~5%)
          `L_cons` 가 기기를 합으로 묶고 가중이 공유되므로 SMPS 배분이 흔들리면
          잔차가 다른 기기로 흐른다. 합성 홀드아웃에서도 보인다 = 가중이 움직였다
죽은 헤드  여섯 판 모두 없음
```

**규칙: 지문 불변은 필요조건이지 충분조건이 아니다.** 한 기기군만 건드리는 처방이라도
`run_scorecard` 표 ③ 으로 **전 기기 전력 헤드를 짝지어** 다시 재라.

## 다음 (권장 순서)

### A. 현장 지문 — **다음에 할 것**
13.59.7 이 축을 확정했다. 복합 파일의 SMPS 켜짐 조합 6개로 현장 지문을 풀면
조합 홀드아웃 오차가 **0.4021 -> 0.1241**, 맞바뀜이 일어나는 (1,1,1) 창에서
**0.4425 -> 0.0912** 다. 자리 보정(0.4134)과 비교가 안 된다.

```
할 일   `--sig-insitu` (12.122.11, `run_fit_insitu_sig` 의 산출물) 를 **새 자료로
        다시 세운다**. 지금 것은 옛 계측기 시대다. 자리별로 따로 세울지는 그 다음
        (자리 D 는 조합 6개, 자리 E 는 4개뿐이다)
⚠ 대조는 `adapt_v24b_z_s0~2`. 판정은 `run_site_split_check` 표 ② 프로젝터 예측W
⚠ ALS 는 앵커 없이 돌리면 편향된 초기해에 자기강화한다 (`run_fit_insitu_sig` 문서)
```

### B. 자유 최소와 학습이 가는 자리가 다르다 — 미해명
`inv_h2` 로 `L_harm` 자유 최소를 훑으면 세 지문 전부 프로젝터를 **32~38W** 로 원한다
(참 44.6). 그런데 학습은 **3~15W** 로 간다. `L_harm` 만으로는 붕괴가 설명되지 않는다 —
`cons`·`hedge`·`pref`·합성 replay·게이트 동역학 중 어디인지 안 갈랐다.
13.58.8 의 절제가 "옮길 뿐" 이었던 것과 같은 자리다. **A 보다 이것이 먼저일 수도 있다.**

### C. 복합에서 고차가 **더 크다** — 기전 미해명
현장/격리 비가 자리 D 에서 h11 1.23~1.52 다. 임피던스가 커지면 도통각이 넓어져 THD 가
내려간다는 직관과 반대이고, 회로 모델도 이 방향을 못 맞힌다.

### D. 남은 것 (13.55~13.58 에서 이월)
```
· 저항 창 타깃 편향 — ch30 합성 중앙 2.5~3.1 대 실측 0.007 (`resistive_overlap`)
· 포트 실측 AUC −0.02~−0.03
· deploy/nilm_runtime/ 사본이 낡았다 (wide_target 없고 P_CH_FINE=30)
· harm_weight 재검토는 **A 다음** — 위 정정 참조
```

## 도구

`HANDOFF_2026-09-08.md` 의 네 개(`run_scorecard` · `run_site_split_check` ·
`run_z_probe` · `run_loss_compare`)를 그대로 쓴다. 이번에 더한 것:

```
run_loss_compare --sig-site {D,E}   그 자리 지문으로 손실을 다시 계산
⚠ 이 도구는 **모델의 상태 배정이 맞을 때만** 읽는다 (13.59.9). A/B 는 `power` 만
  바꾸므로 모델이 프로젝터를 저전력 상태에 두면 B 의 43W 가 그 상태 지문을 타고
  흘러 h13 을 과하게 낸다 — 상태 지문이 정확해질수록 이 편향이 커진다
```
