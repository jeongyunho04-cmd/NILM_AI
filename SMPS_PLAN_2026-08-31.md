> ⚠ **이 계획은 2026-09-01 에 실행됐고 절반이 반증됐다.**
> 문서 뒤쪽 "실행 결과" 절과 `HANDOFF_2026-09-01.md`, 설계 문서 12.122 를 볼 것.
> **아래 본문은 실행 전 원문 그대로 둔다** — 무엇을 왜 틀렸는지 읽으려면 필요하다.

# SMPS 3종 — 지문은 흐려지지 않았다. 무게가 어긋나 있다 (2026-08-31)

`OPERATING_POINT.txt` 7절의 *"고조파는 격리에서 위상 d′ 12.99 인데 중첩에서
0.4 로 죽는다"* 를 다시 판 결과다.

> **규칙 14 — 이 문서에서 잰 것과 안 잰 것**
>
> | 값 | 출처 | 상태 |
> |---|---|---|
> | kappa 8.2 | 12.120.3 의 노름·쌍거리에서 **복원** | 미확인 |
> | 와트당 A/W 표 | 12.15 의 PF·THD·`I_h/I_1` 에서 **복원**. 1·3·5·7차 **크기만** | 미확인 |
> | 차수별 판별력 | 위 표에서 나온 것. **9~15차와 위상이 빠져 있다** | 미확인 |
> | 그 밖의 인용 수치 | 저장소가 실제로 잰 것 | 측정 |
>
> `run_sig_conditioning` 이 실제 `sig` 행렬로 위 셋을 5분 안에 확인한다.
> **그 전에는 전부 후보다.**

---

## 0. 한 줄

**지문은 안 흐려진다 (kappa 8.2). 흐려지는 것은 특징이고, 어긋난 것은 손실의
차수 무게다.**

SMPS 배분을 정하는 항은 `L_harm` 하나뿐인데, 그 항은 **차수마다 같은 무게**를
준다. 그런데 차수마다 판별 정보가 **17배** 다르고, 무게를 가장 크게 받는 축
(`ch33 = |I3|/|I1|`, 3차)이 **SMPS 끼리는 가장 안 갈리는 차수**다.

---

## 1. "d′ 14.9 → 0.4" 는 관측이 아니라 특징의 성질이다

무너진다고 잰 그 양은 **위상 불변량** `φ_h − h·φ_1` 이다 (ch38~45).

```
격리      φ_1 = 그 기기 자신의 기본파 위상
복합      φ_1 = **합**의 기본파 위상. 1.2kW 오븐이 I1 5.4A 를 끌면 그것이 지배한다
          -> 불변량이 φ_h − h·φ_1(자기것) 에서 φ_h − h·0 으로 끌려간다
          -> 끌려가는 양이 저항 부하 크기에 따라 연속으로 변한다   = 이것이 '흐려짐'
```

**분모와 기준을 다른 기기와 공유하는 특징은 중첩에서 비선형이다.**
`ch33 = |I3|/|I1|` 도 같다 — 12.79 가 고부하에서 66배 압축을 쟀다.

**관측 자체는 선형이다.** 전류는 더해진다:

```
y = Σ_i P_i · sig_i          15차 x Re/Im = 실수식 30개, 미지수 3개 (과결정계)
```

저장소의 측정이 이 구분을 이미 지지한다:

```
                   격리   복합(저항X)  복합(저항O)
전력 P             2.79     2.63      0.57   <- 저항이 지운다        12.121.1
|I3|+|I5|+|I7|    2.62     2.52      2.80   <- **안 지워진다**
절대 |I3|                      60초 창 d′ 2.54  <- 2배             12.121.2
|I3|/|I1| (ch33)               60초 창 d′ 1.26  <- 비율이 가장 나쁘다
```

**같은 물리량인데 절대값은 살고 비율은 죽는다.**

---

## 2. 두 가지를 계산했다

### 2.1 조건수 — 8.2. 이 계는 풀린다

12.120.3 이 세 지문의 노름과 쌍거리를 적어 놨다. 그람 행렬을 복원하기에
충분하다 (`⟨a,b⟩ = (‖a‖²+‖b‖²−‖a−b‖²)/2`).

```
‖sig‖  프로젝터 0.1219  충전기 0.1479  미니PC 0.1697      (손실 척도 정규화)
‖차‖   프-충 0.0561     프-미 0.0654   충-미 0.0540

쌍별 각도   프-충 21.3°  프-미 17.9°  충-미 17.9°   (cos 0.93 / 0.95 / 0.95)
특이값      0.2516  0.0362  0.0305
** kappa = 8.2 **       쌍별  프-충 5.4  프-미 6.7  충-미 6.4
```

> **자기정합 확인**: 12.120.3 의 '비' 열(0.42/0.45/0.34)이 `‖차‖ ÷ 쌍평균` 과
> 소수 둘째 자리까지 맞는다.

**kappa 8.2 는 잘 조건화된 값이다.** 고조파 1% 오차 → 전력 8% 오차.
세 벡터가 18~21° 벌어져 있다. **12.87.3 의 '배분 미결정' 은 dictionary 의
성질이 아니다.**

### 2.2 차수별 정보 — 17배 갈린다

12.15 의 실측표(P, PF, THD, `I_h/I_1`)에서 **와트당 전류**를 복원했다.
`I1 = (P/PF/V)/√(1+THD²)`, `I_h = I1·(I_h/I_1)`.

```
와트당 A/W        h=1        h=3        h=5        h=7
프로젝터        0.004582   0.004118   0.003434   0.002564
충전기         0.004443   0.004084   0.003591   0.002957
미니PC         0.004839   0.004177   0.003682   0.003366
오븐          0.004500   0.000015   0.000005   0.000002
핫플          0.004505   0.000067   0.000014   0.000005
포트          0.004505   0.000022   0.000007   0.000003
```

여기서 두 가지가 나온다.

**① h=1 은 신원 정보가 0 이다.** 아홉 기기가 전부 0.0044~0.0048 이다.
물리적으로 당연하다 — 유효분 `I1/P ≈ 1/V` 이고 V 는 공통이다.
**즉 `L_harm` 의 기본파 행은 `L_cons`(총전력)의 중복이다.**

**② SMPS 끼리의 판별력은 차수와 함께 커진다.**

```
                      h=1      h=3      h=5      h=7
SMPS 대 저항          1배   61~287배  254~818배  569~1496배
프로젝터 vs 충전기    3.06%    0.83%    4.46%   **14.25%**
미니PC vs 프로젝터    5.46%    1.44%    6.96%   **27.06%**
미니PC vs 충전기      8.52%    2.27%    2.51%   **12.93%**
```

**h=3 이 SMPS 끼리 가장 안 갈린다 (0.83%). h=7 이 17배 낫다.**

그런데:

```
입력    ch33 = |I3|/|I1| 이 "저항성 무리를 가르는 주 축" 이다 (2절)  <- 3차
손실    harm_scale 가 차수마다 같은 무게를 준다 (net.py:366)        <- 균등
```

**3차는 SMPS 대 저항에는 훌륭하고(61~287배) SMPS 끼리는 최악이다.**
지금 구조는 그 3차를 주 축으로 삼고, 손실은 14.25% 짜리 7차와 0.83% 짜리
3차에 같은 무게를 준다.

### 2.3 ⚠ 앞 판(이 세션 초안)의 진술 하나를 정정한다

초안에 *"기본파는 1.5kW 저항 오차가 SMPS 예산으로 새는 통로"* 라고 적었다.
**틀렸다.** 위 표가 보이는 대로 h=1 에서는 모든 기기가 같으므로 **기기 사이에
샐 것이 없다**. 그리고 총량 오차는 `L_cons` 가 이미 막는다.

기본파의 실제 문제는 다르다 — **정보가 0 인데 손실의 1/8(홀수차만) 또는
1/15 를 가져간다.** 새는 통로가 아니라 **희석**이다. 처방은 같은 방향이지만
근거가 다르고, 기대 효과도 더 작다.

> 규칙 22 의 형태다. 순서(저항이 크다 → 기본파에 있다 → 그래서 샌다)가
> 그럴듯해서 기제로 읽었다. 표를 만들자 갈렸다.

---

## 3. 그래서 진단은 이렇게 선다

```
① dictionary 가 나란하다                        kappa 8.2  -> **아니다**
② 저항 오차가 SMPS 로 샌다 (손실 층위)           h=1 에서 기기가 동일  -> **아니다**
                                              (④ 가 이것을 W/W 로 확인한다)
③ **정보가 있는 차수와 무게를 받는 차수가 다르다**  판별력 0.83% vs 14.25%,
                                              무게는 균등        <- **여기다**
```

③이 설명하는 것:

- **왜 특징 추가가 24번 다 실패했나** — 정보를 더 넣는 문제가 아니었다.
  이미 있는 정보에 무게가 안 실려 있다
- **왜 12.120 의 기울기 균등화(A)가 안 움직였나** — 그것은 **기기별** 기울기를
  맞췄다. 어긋난 축은 기기가 아니라 **차수**다. 다른 축을 만졌다
- **왜 프로젝터↔충전기가 최악인가** — 그 쌍이 3차에서 0.83% 로 가장 안 갈린다
- **왜 미니PC 는 다른 병인가** — 미니PC 는 7차에서 27% 로 가장 잘 갈린다.
  혼동이 아니라 검출 문제라는 12.120.14 와 맞는다

⚠ **그런데 이것도 아직 후보다.** 위 표는 크기만이고 9~15차가 빠져 있다.
12.120.15 는 *위상* d′ 가 3차 12.99 에서 15차 0.01 로 **단조 하강**한다고 쟀다.
**크기는 오르고 위상은 내린다면 합쳐서 어떻게 되는지 모른다.**
`run_sig_conditioning` 이 복소 `sig` 로 그것을 가른다. **그것이 1번인 이유다.**

---

## 4. 실행 — 단계별

### 4.1 [1번] `run_sig_conditioning` — 5분, 재학습 없음

```
python -m src.run_sig_conditioning
```

내는 것:

```
① harm_scale        차수별 정규화 값이 무게를 실제로 어떻게 바꾸는가
② 차수별 판별력      ‖sig_i−sig_j‖ (복소 — 크기와 위상이 **둘 다** 들어간다)
③ kappa             차수 부분집합별 조건수
④ leak W/W          저항 1W 오예측이 SMPS 에 붙이는 W
⑤ 제안 무게          ②에 비례하는 harm_mask 벡터
```

**판정 (돌리기 전에 적는다)**

```
②가 3~5배 이상 갈린다          -> 균등 무게가 어긋나 있다. 4.2 로 간다
③ kappa 가 어디서든 20 아래     -> 식별가능성 문제가 아니다. 12.87.3 을 정정한다
③ 기본파를 빼면 kappa 가 는다   -> 마스크(0)가 아니라 무게(0<w<1)로 간다
④ leak 이 어디서도 0.05 아래    -> 2.3 의 정정이 맞다. ②만 남는다
② 가 평평하다                  -> **이 방향을 닫는다.** 4.4·4.5 와 계측만 남는다
```

②가 평평하게 나올 실제 가능성이 있다. 위상이 크기와 반대로 움직이면 복소
판별력은 평평할 수 있다. **그때 이 절의 처방은 죽고, 그것도 5분에 안다.**

### 4.2 [3번] `L_harm` 의 차수 무게 — 2단계 3분 x 시드 4개

4.1 의 ②가 통과했을 때만. 코드 변경은 **버퍼 하나**다.

```python
# losses.py __init__  (지금)
mask = torch.ones(h)
if harm_odd_only:
    mask[1::2] = 0.0
self.register_buffer("harm_mask", mask)

# 바꾸는 것 — 값은 CLI 로만 들어오고 기본값은 지금과 같다
if harm_w is not None:                       # (H,) 차수 무게
    mask = mask * torch.as_tensor(harm_w, dtype=mask.dtype)
```

`losses.py:261` 의 `/ self.harm_mask.mean()` 이 이미 손실 규모를 지켜 준다 —
`w_harm=4.0` 의 뜻이 안 바뀐다.

**돌릴 판 (부록 규칙 4 — 단일 변수)**

```
uniform   현행                                    <- 기준선
nofund    기본파만 0                               2.3 의 정정 뒤 기대는 작다
disc      4.1 ⑤ 가 낸 판별력 비례 무게              <- 주 후보
hi        5차 이상만 (h=1,3 을 0)                  극단. disc 의 방향 확인용
```

**판정 기준 (돌리기 전에 적는다)** — 기준선은 운영점 시드 4개:
8파일 유령 **5.23W**(폭 4.15), F1 **0.846**(폭 0.011), 잔차 **7.30W**,
전이 **38~47/59**, 프로젝터 정밀 **0.832**, 충전기 재현(저항 있음) **0.641**.

```
① 충전기 재현율 (저항 있음)  0.641 -> 0.72 이상          <- 주 판정
② 프로젝터 정밀도           0.832 -> 오를 것
③ 전이 귀속 38~47/59 안 또는 위
④ 8파일 유령 5.23W 이하, F1 0.846 이상                  (다른 데를 안 부술 것)
⑤ **대조 (규칙 20)** `test_9`(SMPS 0종)와 `test_11/12`(저항 4종만)는
   거의 안 변할 것. 이 처방은 SMPS 배분만 겨냥하므로 SMPS 가 없는 파일에는
   닿을 데가 없다. 그 파일들이 SMPS 파일만큼 움직이면 **판정 보류**
⑥ `disc` 와 `hi` 가 **같은 방향**일 것. 반대면 튜닝 잔향이다
```

**1단계를 안 건드리므로 12.115 의 폭 문제가 없다.** 순수 단일 변수, 시드 4개.

⚠ **이것은 손실 수정이다.** `OPERATING_POINT.txt` 8절이 *"특징 추가와 손실
수정은 24번 시도해 0번 성공"* 이라 적은 그 부류다. 다른 점은 하나뿐이다 —
**앞의 24개는 정보를 더 넣거나 손실 모양을 바꾼 것이고, 이것은 이미 있는
정보에 대한 무게가 정보량과 어긋난 것을 되돌린다.** 그 어긋남을 4.1 이
숫자로 보이지 못하면 이 항목은 돌리지 않는다.

### 4.3 [2번] 프로젝터 스냅 — 후처리, 가장 값싸다

4.1 과 독립이라 **병렬로 돌릴 수 있다.**

창의 **87.1%** 가 55W 상한에 붙어 있는데 격리값은 47.4W(폭 1.9W)다 (12.120.1).

```python
# postproc.py — 지금은 상한만 있다
excess = np.clip(out[:, j] - lim, 0.0, None)      # 넘는 만큼만, **한 방향**

# 스냅 — 12.117 의 --rm-snap 과 같은 형태
delta = out[:, j] - TARGET_W[app]                 # **양방향**. 음수면 받아온다
```

세 가지가 지금 것과 다르다:

```
① 값      55W(상한) -> 47.4W(격리 중앙)
② 방향    한 방향 -> **양방향**. 모델이 40W 로 낮게 붙였으면 되받아 온다
③ 상대    게이트 가중 -> **3차 이상 고조파 코사인** (absorb_residual 의 기계 재사용)
          게이트가 이미 편향돼 있으면(충전기 재현 0.641) 그 편향대로 나눈다
```

⚠ **12.102 의 "52W 로 내렸더니 42/59 로 나빠졌다" 와 다른 조작이다.**
그것은 clip 이고 이것은 snap 이다. 다만 **그 결과가 이 항목의 가장 강한
반증 후보**이므로 판정 기준에 값 둔감성을 넣는다.

⚠ **프로젝터는 상태가 여럿이다** (OPERATING_POINT 1절, 상태수 3).
단일 상수 대신 **모델의 상태 헤드 argmax 에 맞는 격리 중앙값**으로 스냅하는
판도 같이 돌린다. 그쪽이 물리적으로 옳다.

```
① 충전기 재현율 (저항 있음) 0.641 -> 오를 것       <- 주 판정
② 전이 귀속 38~47/59 안 또는 위
③ 8파일 유령 5.23W 이하                          (미니PC 로 새면 유령이 는다)
④ 값 둔감성: 47.4 / 48.5 / 50.0 이 **같은 방향**일 것.
   한 점만 좋으면 튜닝 잔향이다 (12.102 가 상한에 쓴 그 검사다)
⑤ 양방향 판과 한 방향 판을 갈라 잰다 — ②가 어느 쪽에서 오는지 알아야 한다
```

⚠ **반증 후보**: 12.87.2 는 프로젝터 초과분을 **미니PC 로** 옮기면 고조파가
나빠진다고 쟀다 (0.0222 → 0.0252). 그 절은 상대를 미니PC 로 고정했고,
지금 미검출인 쪽은 충전기다. ③이 그것을 잡는다.

### 4.4 [5번] 저항 고조파를 빼고 SMPS 를 **역산**한다 — 후처리

**이 저장소는 `Σ P̂·sig` 를 만들기만 하고 한 번도 역산한 적이 없다.**
`absorb_residual` 은 잔차를 기기 하나에 코사인으로 투영한다 — 1차원 사영이지
결합 해가 아니다. 프로젝터+충전기가 **함께** 만드는 모양은 어느 하나와도 다르다.

이제 저항 전력을 `V²/R` 로 0.53% 안에서 안다 (12.112·12.117). 그러면 그
**고조파 기여**도 뺄 수 있다:

```
1. resistive_match 가 저항 조합 b_res(t) 와 P_res(t)=V²/R 을 준다  (이미 있다)
2. y_smps = y_관측 − Σ_저항 P_res·sig_res − 대기 − 계측계
3. 모델 게이트가 켠 SMPS 개수 n 을 읽는다                        (규칙 18)
4. 개수가 n 인 조합만 훑어 각각 NNLS:  min_{P≥0} ‖y_smps − A_S·P‖   (h≥3 만)
5. 잔차가 가장 작은 조합을 고르고, 기기별 물리 범위로 클램프
```

**규칙 18 을 지킨다** — 개수는 모델이 정하고 정합기는 신원·배분만 바꾼다.
4.1 의 ③이 어느 차수 집합에서 kappa 가 가장 낮은지 말해 주고, 5번의 `h≥3` 을
그 집합으로 바꾼다.

**4.1 이 끝나야 설계가 정해진다.** 그래서 순서가 뒤다.

### 4.5 [4번] 사람 라벨 58분 — 2절과 완전히 독립인 축

```
test_5 / 6 / 7 / 8 / 13  =  3,499초 (58.3분), 전부 SMPS 3종 포함
  real_events.json 의 intervals[app]["on"] = 사람 스위칭 로그
  -> **채점에만 쓰고 학습에는 한 번도 안 넣었다** (run_adapt 는 crit.unlabeled 만)
```

4.2절의 전제(*"실측에 기기별 정답이 없다"*)가 이 다섯 파일에는 거짓이다.
⚠ **전력 정답은 여전히 없다** — 로그는 on/off 만 준다. 그러니 걸 수 있는 것은
`on_logit` 하나뿐이고, 마침 지금 무너지는 지표가 정확히 on/off 다.

**배관이 이미 다 있다.** `RealWindows` 가 창마다 `self.stem` 과
`self.target_cycle` 을 들고 있고(realdata.py:116-117), `run_hmm_probe` 가
그것으로 정답 벡터를 만드는 코드를 이미 갖고 있다:

```python
t = target_cycle / 60.0
on = np.zeros(len(t), bool)
for x, y in ev[stem]["intervals"][app]["on"]:
    on |= (t >= x) & (t <= y)
```

```
L_adapt += w_real_on · BCE(on_logit, human_on)      라벨 있는 창에만
```

- `intervals[app]["uncertain"]` 구간은 **감독에서 뺀다** (양쪽 다 안 센다)
- `--holdout-real` 이 이미 파일 단위 제외를 한다 — **그대로 LOFO 다**
- 다섯 폴드 x 3분 = **15분**. `w_real_on` 은 {0.1, 0.3, 1.0} 세 점

**어느 쪽이 나와도 결정적이다.**

```
충전기 재현 0.641 -> 0.85 이상   12.87.3 이 틀렸다. 배분은 미결정이 아니라
                               **학습 신호가 없었을 뿐**이다.
                               -> 처방은 커미셔닝 녹화이고, 이미 하고 있다
거의 안 움직인다                 12.87.3 이 **가장 강한 형태로** 확정된다.
                               기기별 정답을 줘도 못 가른다면 관측에 없는 것이다
                               -> 계측 축(4.6)만 남고, 그 판단이 확실해진다
```

**반칙이 아니다** — NILM 커미셔닝은 표준 절차이고, `MEASUREMENT_RULES.md` 가
요구하는 녹화 절차가 이미 그것이다. 사용자는 이미 그 로그를 남기고 있다.

**대조 (규칙 20)**: `test_9`(라벨 없음)와 `test_11/12`(SMPS 0종)가 거의 안 변할 것.

### 4.6 [7번] 앞이 다 닫히면 — 계측 축은 '차수' 가 아니라 '스위칭 주파수'

12.88.4 의 3번이 *"더 높은 차수나 더 빠른 표본"* 이라 적었는데 **앞은 저장소가
이미 닫았다** — 위상 d′ 가 3차 12.99 에서 15차 0.01 로 단조 하강한다.

```
지금        세 SMPS 의 3차가 **같은 180Hz 빈**에 떨어진다. 관측은 벡터 합 하나
스위칭 대역  SMPS 마다 스위칭 주파수가 다르다 (대개 30~150kHz).
            **서로 다른 빈**에 떨어지므로 합쳐도 안 겹친다
```

**중첩이 지문을 지우는 것은 지금 특징공간의 성질이지 물리의 성질이 아니다.**
ElectriSense 계열이 SMPS 를 93.8% 로 가른 근거가 이것이다.

**사기 전에 규칙 1 을 건다** — 오실로스코프 + 전류 프로브로
① 세 기기 격리 FFT (10kHz~1MHz), ② 세 피크 주파수를 적는다,
**③ 셋을 동시에 켜고 같은 FFT — 세 피크가 다 보이는가.** ③이 관문이다.
①만 보고 사면 12.65·12.79·12.81 의 실수를 하드웨어 값으로 하는 것이다.

**안 잰 것**: 이 절에는 저장소가 잰 수치가 하나도 없다.
지금 계측기가 원시 ADC 를 노출하는지도 안 봤다 (노출하면 ①이 공짜다).

---

## 5. [6번] 걸려 있는 측정 요청(인수인계 5.5-5)을 이렇게 바꾼다

지금 안은 *기기당 5분 연속* 이라 60초 창 5개, 전이 2건이다. **표본이 안 는다.**

```
저항 부하 하나를 계속 켜 둔 채 (전압·회선 고정), SMPS 를 하나씩:
    90초 ON -> 60초 OFF 를 10번         기기당 25분, 3종 75분

지금 안(5분)   깨끗한 ON 창 5개,  전이 2건
토글 안(25분)  ON 창 10개 + OFF 창 10개 + **전이 20건**
```

- 90초인 이유: 60초 창이 상태 안에 **온전히** 들어가야 정상 구간 표본이 나온다
- 같은 세션 안의 ON/OFF 쌍이라 **세션 간 표류가 d′ 에 안 섞인다** —
  규칙 5 가 이 저장소에서 세 번 물었던 자리다
- 충전기는 고속·트리클 두 상태를 모두 포함할 것 (15~76W 를 만드는 원인)

**그리고 이 녹화 하나가 4.1·4.4·4.5 에 전부 들어간다.** 토글 시각이 곧 사람
스위칭 로그이므로 4.5 의 지도 신호가 20배로 는다. 지금 안은 4.5 에 아무것도 안 준다.

---

## 6. 순서와 비용

```
    무엇                          비용     재학습    막히면
1   run_sig_conditioning          5분      없음     4.2 를 취소한다
2   프로젝터 스냅 (4.3)            탐침     없음     1 과 병렬. 독립이다
3   L_harm 차수 무게 (4.2)         12분    2단계만   1 의 ②가 통과했을 때만
4   사람 라벨 LOFO (4.5)           15분    2단계만   **무조건 돌린다. 독립 축이다**
5   저항 빼고 NNLS (4.4)           반나절   없음     1 이 차수 집합을 정한 뒤
6   토글 녹화 (5절)                75분     —       1·5·4 에 동시에 들어간다
7   스위칭 대역 (4.6)              부품값    —       위가 다 닫혔을 때
```

**오늘 안에 끝나는 것: 1, 2, 3, 4 (합쳐 40분).**
1 과 2 는 병렬이고, 4 는 1 의 결과와 무관하게 돌린다.

---

## 7. 무엇이 이 이야기를 무너뜨리는가

돌리기 전에 적는다. 규칙 20 이 요구하는 형태다.

```
4.1 의 ②가 평평하다                     3절의 진단이 죽는다. 4.2 를 안 돌린다
4.1 의 ③ kappa 가 100 을 넘는다          2.1 이 죽는다. 식별가능성 문제가 맞다
                                        -> 4.5 와 4.6 만 남는다
4.2 에서 대조 파일(test_9, test_11/12)이
  SMPS 파일만큼 움직인다                  재학습 잡음이다. 판정 보류 (12.114 의 형태)
4.2 의 disc 와 hi 가 반대 방향            튜닝 잔향이다. 채택하지 않는다
4.3 에서 유령이 는다                     12.87.2 의 경고가 맞았다. 상대 선택을 다시 본다
4.5 가 안 움직인다                       12.87.3 이 확정된다. **좋은 결과다** —
                                        24개의 실패가 하나로 설명되고 계측 축이 선다
충전기 재현은 그대로인데 F1·유령만 좋아진다  다른 것을 고친 것이다. 기제를 주장하지 않는다
```

---

## 8. 자가 점검

```
규칙 1   격리 통계로 처방하지 않았나
         -> 2절은 격리 지문의 **대수적 성질**이지 판별력 주장이 아니다.
            판정은 전부 실측 재채점이다. 4.6 은 그 위험이 있어 ③을 관문으로 박았다
규칙 3   절제 그룹이 가설을 가르는가
         -> 4.2 의 네 판(uniform/nofund/disc/hi)이 '기본파' 와 '차수 기울기' 를 가른다.
            4.3 의 양방향/한방향 판이 스냅의 어느 쪽이 효과를 내는지 가른다
규칙 14  안 잰 것을 측정처럼 쓰지 않았나
         -> 머리말 표가 그것이다. 2.2 의 와트당 표는 **1·3·5·7차 크기만**이고
            9~15차와 위상이 빠져 있다고 3절 끝에 다시 적었다
규칙 20  재학습이 필요한 것에 대조 파일을 넣었나
         -> 4.2 의 ⑤, 4.5 의 대조. 4.3·4.4 는 후처리라 해당 없음
규칙 22  절제 전에 '원인' 이라 쓰지 않았나
         -> 3절 제목이 '진단은 이렇게 선다' 이고 본문이 '여기다' 로 끝나지만,
            바로 아래에 "이것도 아직 후보다" 를 달았다. 2.3 은 이 세션의 초안이
            같은 실수를 한 것을 스스로 정정한 기록이다
§8 경험칙  라벨 문제인가 / 추론 뒤 물리 제약인가
         -> 4.5 는 라벨, 4.3·4.4 는 추론 뒤 물리 제약.
            4.2 만 손실 수정이고, 그것이 왜 앞의 24개와 다른지 4.2 안에 적었다.
            **판별 특징을 새로 넣는 항목은 하나도 없다**
```

---
---

# 실행 결과 (2026-08-31 밤) — 이 계획의 절반이 반증됐다

위 계획을 검토하고 순서를 바꿔 실행한 기록이다. 자세한 것은
`MODEL_TRAINING_DESIGN.md` **12.122절**, 방법론은 `MEASUREMENT_RULES.md`
**규칙 23·24**.

## 무엇이 반증됐나

| 이 문서의 진술 | 결과 |
|---|---|
| 4.1 의 ② 를 `‖sig_i−sig_j‖` 로 잰다 | **자가 틀렸다.** 분모 없이는 24.5배, d′ 로는 2.06배 → **정반대 판정** |
| §2.2 "차수별 정보가 17배 다르다" | 크기만 쓴 값. 위상 넣으면 h=3 이 h=1 과 같다 (14.5%) |
| §3 "정보 있는 차수와 무게 받는 차수가 다르다" | **죽었다.** 7절의 자기 판정대로 4.2 를 취소 |
| §2.3 "누설은 없다 (h=1 에서 기기가 동일)" | **철회를 철회한다.** 누설은 h≥3 로 흐른다. 저항 전용 파일에서 유령 프로젝터 13.3W |
| 4.3 프로젝터 스냅 | **반증.** 시드 4/4 에서 유령 +0.44W. 주 판정(재현율)은 원리상 못 움직인다 |
| 4.3/4.5 "충전기 재현 0.641" | 층을 안 적었다. 파일 전체로는 0.96, 그 층에서는 0.685 (규칙 24) |
| §1 제목 "d′ 14.9 → 0.4" | 12.99 다 (OPERATING_POINT 7절, 이 문서 §0·§3 과도 불일치) |
| §2.2 출처 "12.15 의 실측표" | 12.23.3 이다 |
| 1번 "`run_sig_conditioning` 5분, 재학습 없음" | **그 스크립트가 없었다.** 짜야 했다 |

## 무엇이 확인됐나

- **§2.1 kappa 는 맞다.** 복소 지문·손실 좌표계로 재면 전 차수 5.28, 홀수차 8.56.
  12.87.3 의 '배분 미결정' 이 dictionary 의 성질이 아니라는 판정은 유효하다.
  다만 차수 부분집합에 민감하다 — `h=3,5,7` 만 쓰면 **125** 로 거의 특이해진다.
  4.4 의 `h≥3` 은 h=3..15 를 뜻해야 한다.
- **4.5 는 맞았다.** 아래.

## 진짜 원인 — 이 문서가 못 본 것

**`L_harm` 을 어떻게 최소화할지가 아니라, 그 항이 최소화하는 대상이 문제다.**

모델도 손실도 없이 NNLS 로만 풀어도 다섯 파일 전부에서 **관측이 오답을 선호한다**
(정답 support 잔차가 1.05~1.79배 나쁘다). 순방향 모델 오차가 판별 신호의 2.8배다.

원인은 지문이 상태를 뭉갠 것이다 — `harmonic_signatures` 가 통전 구간 중앙값
하나를 쓰는데, 충전기는 **고전력에서 프로젝터와 형상이 같아지고**(h5 0.3% 차)
저전력에서 갈린다(h5 15.5% 차). 지문은 겹치는 쪽 상태의 것이고, 문턱이
저전력 표본을 충전기 15%·미니PC 46% 잘라낸다.

**그래서 §5 토글 녹화의 값어치가 바뀌었다** — "표본 20배" 가 아니라
**전력이 통제된 단독·쌍 구간을 만드는, 순방향 모델을 고칠 유일한 입구**다.
지금 자료로는 가산성조차 못 잰다 (단독 구간과 동시 구간에서 기기 전력이 달라 교란).

## 4.5 사람 라벨 LOFO — 이 계획에서 유일하게 이긴 항목

배관을 만들고(`--w-real-on`, `run_real_on_lofo.py`) 5폴드 LOFO 를 돌렸다.
**귀무 대조**로 라벨 시간축을 37% 순환 이동한 판을 같이 돌렸다 (ON 비율·구간
길이 보존, 시각 대응만 파괴).

**시드 3개로 확인한 뒤의 결과다** (시드 1개로는 다른 결론이 나왔다 — 아래 ⚠).

```
파일 전체, 홀드아웃, 시드 3개        w=0            w=0.3         Δ / 기준선 SD
beam_projector.precision      0.792 ±0.001   0.825 ±0.001   +0.034 (44.8x)  <- 확정
minipc.precision              0.772 ±0.002   0.876 ±0.009   +0.104 (49.1x)
minipc.recall                 0.955 ±0.009   0.866 ±0.018   -0.089
laptop_charger.recall         0.960 ±0.013   0.970 ±0.004   +0.010 ( 0.8x)  <- 무의미
```

프로젝터 정밀은 **5파일 x 3시드 = 15/15 전부** 같은 방향이고 귀무 대조는 0.523
으로 크게 나쁘다 — 이득이 BCE 의 정규화가 아니라 **라벨 내용**에서 온다.

⚠ **이 문서의 주 판정(충전기 재현)은 살아남지 못했다.** 12.121.7 의 층에서:

```
SMPS 1대 + 저항 층, 시드 3개      w=0            w=0.3          Δ / SD     순환이동
laptop_charger.recall       0.648 ±0.106   0.742 ±0.076   +0.095 (0.9x)   0.679
beam_projector.precision    0.659 ±0.017   0.702 ±0.010   +0.043 (2.5x)   0.523
```

시드 0 만 보면 0.685 -> 0.806 이라 판정선(0.72)을 넘는다. **시드 1 은
0.730 -> 0.658 로 내려간다.** 이 층의 기준선 SD 가 0.106 이라 효과가 폭 안이다.
**층화 지표는 예민한 대신 시끄럽다** — 같은 지표의 시드 SD 가 파일 전체
0.013 vs 이 층 0.106 으로 8배다.

**그런데 전력 층위는 안 움직인다:**

```
                        w=0    순환이동   정상라벨
전이 ΔP |상대오차| 평균   0.754    0.776    0.754
잔차 |W|               7.638    7.267    7.623
```

`P̂ = σ(on)·p_raw` 인데 BCE 는 `on_logit` 만 건드린다. `p_raw` 를 정하는 것은
여전히 `L_harm` 이고, 그 항은 오답을 선호한다.

### 그래서 12.87.3 이 둘로 갈린다

```
검출 (누가 켜져 있나)   **틀렸다.** 학습 신호가 없었을 뿐이다
                      단, 확정된 것은 프로젝터 정밀도이고 충전기 재현은 보류다
배분 (몇 W 씩인가)      **확정.** 기기별 정답을 줘도 안 움직인다
```

## 배분을 고칠 길 — 셋을 쟀다 (12.122.6)

프로젝터는 켜지면 항상 45.9~48.8W 다. **정답을 아는 기기가 하나 있다.**

```
프로젝터 ON 창에서 프로젝터에 주는 W  (참값 45.9~48.8)
  고정 지문 (현행)              74.5 ~ 99.3W
  상태별 지문 + 전역 격자 탐색     65.4 ~ 71.0W    <- 과잉의 30% 축소
  전이 기반 신원 정확도           0.718 (무작위 0.333, h3..9)
```

- **상태별 지문의 첫 실패는 최적화 실패였다.** 전역 탐색으로 풀면 개선된다
- **스냅 실패의 2/3 는 재배분 탓이다** — 재배분을 끄면 Δ유령 +0.438 -> +0.138.
  다만 30~50W 를 버리는 셈이라 잔차가 +3.19W 폭발한다

⚠ **지금 지표는 배분 개선을 볼 수 없다.** 스냅은 프로젝터를 88 -> 47W 로
정답에 맞추는데, 저장소가 재는 것은 잔차와 유령뿐이라 **개선은 안 잡히고
부작용만 잡힌다.** 프로젝터 전력 오차 27~52W 를 아무도 안 재고 있다.

## 남은 순서

```
0  ⚠ **§5 토글 녹화는 1순위가 아니다** (12.122.9). 겨냥한 가설(전압강하)이
   실측 2% 수준이고, 진짜 문제(전력 상태)는 격리 녹화만으로 잰다.
   그리고 규약이 실행 불가능하다 — 핫플 8초·오븐 108초만에 끊긴다.
   저항 배경으로 쓸 수 있는 것은 전기포트뿐이다 (198초 연속)
1  프로젝터 전력 오차 지표를 만든다          **완료** (12.122.7, run_power_check)
1b ~~sig(P,V) 를 회귀로 만들어 L_harm 에~~  **반증 (12.122.10).** 순방향은 10.4%
   좋아지는데 배분은 안 고쳐진다 (자유해 프로젝터 83.9 -> 86.1W, 참 47).
   전압항은 격리/복합 전압대가 달라(겹침 0~31%) +23% 나빠진다.
   -> §5 토글 녹화를 1순위로 되살린다. 배경은 전기포트
1b w_real_on 을 운영점에 넣을지 판정        프로젝터 정밀 +0.034 는 확정.
                                        미니PC 재현 -0.089 를 어떻게 볼지가 관건
2  토글 녹화 (§5)                        순방향 모델을 고칠 유일한 입구
3  상태별 지문 (단순 치환은 실패했다)         2번 자료가 있어야 제대로 된다
4  스위칭 대역 (§4.6)                     위가 다 막히면. 근거는 오히려 세졌다
X  4.2 L_harm 차수 무게                   취소
X  4.3 프로젝터 스냅                       반증
?  --harm-odd-only                      `L_harm` 최소화 방법을 만지는 부류라 기대값 하락
```
