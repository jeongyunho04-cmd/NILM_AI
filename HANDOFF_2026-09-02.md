# 인수인계 2026-09-02 — 유령 에어컨은 **문턱 아래 누설**이었다. 재학습 0회로 닫았다

`HANDOFF_2026-09-01e.md` 다음이다. e 가 남긴 결함 하나(*"유령8 의 82%가 에어컨"*)를
닫았고, **그 결함의 정체가 e 가 적은 것과 달랐다.** 설계 문서 **12.149 ~ 12.149.4** + **12.152 ~ 12.154.1**, 규칙 **43 / 46**.

⚠ **12.150 ~ 12.151.3 은 다른 세션이 같은 날 쓴 것이다** (계통 임피던스 전이 /
저항 `V_h/R` / 현장 꾸러미). 번호가 겹치지 않게 12.152 로 비켜 붙였다.

---

## 0. 한 줄

**운영점이 바뀌었다.** 모델을 `복소 Z·I` 로 올리고, 후처리에 스켈치+흡수를 넣고,
프로젝터 스냅을 뺐다. **재학습은 한 번도 안 했다** — 체크포인트는 12.148.3 이
만든 `adapt_zi_s{0,1,2}.pt` 그대로다.

```
run_live 기본값      전                후
--ckpt          adapt_ovh.pt   **adapt_zi_s0.pt**   12.148.3 을 이제야 채택한다
--squelch       (없음)          **0.1**             12.149.1
--absorb        0.0            **1.0**             12.104 + 12.149
--absorb-mode   (없음)          **pq**              12.153 — 무효전력 방정식
--absorb-wq     (없음)          **1.0**             12.153
--snap          46.9           **0**               12.149.2 (사용자 판단)
```

사람 라벨 5파일에서 검출 F1 **0.873 → 0.892**, 유령 **2.13 → 0.27W**,
잔차 **3.70 → 1.62W**. 에어컨 유령 4.37W 는 **0.024W** 가 됐다.

⚠ **e 를 읽고 배포 상태를 오해했다** — e 는 `복소 Z·I` 를 "채택 후보" 로만
적었고 `run_live --ckpt` 기본값은 여전히 `adapt_ovh.pt` 였다. 이 인수인계의
"현행" 은 **그 기준선 배포본**을 말한다.

---

## 1. e 가 틀린 것 — 에어컨 게이트는 **한 번도 안 켜졌다**

e 의 §5[1] 이 *"에어컨 열을 빼거나 게이트를 막는 것. 다음 수가 명백하다"* 라
적었다. `gc_zi.json` 의 `absent.air_conditioner` 를 열면 **`on_rate` 가 0.0001**
이고 하드 게이트 기여가 **0.021W** 다. 열은 이미 막혀 있었다.

새는 것은 `P̂ = σ(on)·p_raw` 의 **문턱 아래 누설**이다:

```
에어컨    σ 0.0080  x  p_raw 592W  =  4.9W    게이트>0.5 인 창 0.01%
전기포트  σ 0.0048  x  p_raw 973W  =  5.9W    <- 더 크다. resistive_match 가 이미 지운다
```

**`w_hedge` 는 이것을 볼 수 없다.** 이진 엔트로피 `H(0.008)=0.046` 이라 손실이
보기에 σ=0.008 은 이미 "결정했다" 인데 와트로는 4.9W 다. **손실에 "꺼진 게이트가
내는 와트" 를 보는 항이 없다.** 에어컨이 특별히 나쁜 열이라서가 아니라
`SMPS_GROUP` 도 `RESISTIVE_OHM` 도 아니라 **어느 후처리도 안 건드려서** 눈에 띈
것이다 (규칙 30).

⚠ 그래도 **빈 것이 아니라 실린 것**이었다. 에어컨만 0 으로 두면 겨냥 파일에서
`L_cons` 가 **+66.5%** 오른다. 그냥 지우면 잔차가 4.52 → 8.80W 로 커진다 (규칙 29).

---

## 2. 처방 — **게이트 정합 스켈치 + 잔차 흡수.** 둘은 짝이다

```
① 스켈치   P̂_k = 0  if σ_k < τ                       postproc.squelch (신규)
② 흡수     빠진 와트를 고조파가 닮은 SMPS 로 되돌린다   postproc.absorb_residual (12.104)
```

**운영점이 바뀌었다.** `run_live` 기본값이 곧 운영점이라 인자가 필요 없다.

```
--squelch 0.1     신규
--absorb  1.0     0.0 -> 1.0
```

⚠ **하나만 켜면 반쪽이다.** 흡수만: 유령 4.68W 그대로. 스켈치만: 잔차 7.27W.
끄려면 둘 다 0 으로.

---

## 3. 표 — 사람이 스위치를 적은 7파일 (겨냥 5 + 대조 2)

`run_summarize_pan --human-only` 가 만든다. **손으로 옮기지 말 것** (규칙 34).

⚠ **이 쓸기는 상한 버그(12.149.3) 이전에 돌렸다.** `프로젝터W`·`잔차겨냥` 열은
그 영향을 받는다 (흡수가 프로젝터를 상한 위로 밀었다). **τ 선택의 근거인
유령·유령대조·대조잔차 열은 그 영향을 안 받는다** — 흡수는 게이트를 안 건드리고
상한은 프로젝터 하나뿐이다. 그래서 τ=0.1 결론은 유지된다.

```
판                  프로젝터W   유령5   유령대조  잔차겨냥  잔차대조    F1    에어컨W
현행 Z·I            -2.91   4.68   3.64   3.88  13.38  0.892   4.369
흡수만 1.0             ―     4.68   3.95   0.64  12.99  0.892   4.369
스켈치0.5만          -4.70   0.25   0.51   7.27  21.50  0.892   0.000
τ0.05 + 흡수1.0     -0.37   0.28   1.12   0.89    ―    0.892   0.024
**τ0.1 + 흡수1.0**  -0.37   0.27   1.13   0.90  14.44  0.892   0.024
τ0.2  + 흡수1.0     -0.34   0.27   1.20   0.90    ―    0.892   0.024
τ0.5  + 흡수0.5     -2.48   0.25   0.98   4.08    ―    0.892   0.000
τ0.5  + 흡수1.0     -0.25   0.25   1.46   0.91  20.54  0.892   0.000
```

**귀속이 깨끗하다** — 유령은 스켈치가, 잔차는 흡수가 고친다. 서로 다른 축이다.

**값 둔감성** — τ 0.05~0.2 가 평지고 0.5 만 절벽이다 (대조 잔차 20.54).
τ=0.5 는 저항 3종의 **진짜 헤지**(σ 0.1~0.5)까지 지운다 — test_12 에서 오븐
28.2W + 포트 13.5W. 저항만 있는 파일에는 흡수가 되돌려 줄 SMPS 가 없다.
**0.1 은 측정된 누설대(<0.05)와 측정된 헤지대(0.1~0.5) 사이의 골이다.**

### 귀속 대조 — 이 처방은 **Z·I 위에서만 산다**

같은 후처리를 기준선에 걸면 유령대조가 **7.18 → 10.46W 로 나빠진다.** 기준선은
대조 파일에서 프로젝터 게이트가 2.84W 새고 있어 흡수의 `min_gate=0.1` 관문을
통과한다. Z·I 는 0.137W 라 관문이 닫혀 있다. **Z·I 가 게이트를 깨끗하게 만들어
놓아서 흡수가 안전해졌다.** 초가법이고 반대 방향으로도 확인된다.

---

## 4. 새 운영점의 성능 — **옛 배포본과 나란히** (사람 라벨 7파일, 시드 3 평균)

```
A = 옛 배포본  adapt_ovh + 상한 + 스냅46.9 + 저항정합0.02
B = 새 운영점  adapt_zi  + 상한 + 저항정합0.02 + 스켈치0.1 + 흡수1.0(pq, w_q=1), 스냅 끔
Z = 귀속 대조  기준선 adapt_ovh 에 **같은 후처리**

기기            A F1    정밀    재현  |  B F1    정밀    재현  |   ΔF1
노트북충전기     0.937  0.964  0.919  | 0.959  0.943  0.978  | +0.022
전기포트       0.952  0.947  0.958  | 0.950  0.947  0.954  | -0.002
빔프로젝터      0.869  0.774  1.000  | 0.897  0.819  0.997  | +0.028
미니PC        0.841  0.765  0.942  | 0.870  0.820  0.935  | +0.028
핫플레이트      0.866  0.973  0.800  | 0.864  0.948  0.808  | -0.002
헤어드라이어     0.861  0.895  0.849  | 0.845  0.893  0.819  | -0.016
오븐          0.619  0.928  0.475  | 0.624  0.922  0.481  | +0.004

기기 전력        참값W    A 평균오차  B 평균오차   A 중앙|오차|  B 중앙|오차|  B p90
빔프로젝터        46.9    -0.03     -1.42       0.00      7.09   13.35
전기포트       1309.3   -26.26    -27.59       9.23      9.23   54.43
핫플레이트       450.4    +2.71     +4.28       6.26      6.28   11.19
오븐         1142.0    +1.71     +4.89       3.65      3.66   17.11

집계                        A       B        Δ      Z 기준선+같은후처리
검출 F1 평균 (겨냥5)        0.873   0.892   +0.020        0.873
검출 F1 평균 (대조2)        0.804   0.800   -0.003        0.804
유령 W (겨냥5)              2.13    0.27    -1.85         0.27
유령 W (대조2)              7.18    1.41    -5.77       **10.21**
총전력 잔차 W (겨냥5)         3.70    1.62    -2.08         2.81
총전력 잔차 W (대조2)        12.35   14.71   +2.36        14.03
```

**귀속이 유지된다** — 같은 후처리를 기준선에 걸면 유령대조가 7.18 → **10.21W** 로
나빠진다. 이 처방 묶음은 **복소 Z·I 위에서만 산다** (12.153).

⚠ **이 표는 12.149.3 이 정정한 뒤 값이다.** 그 앞 판은 흡수가 물리 상한(55W)을
뚫고 있어서 프로젝터가 −0.37W / 잔차 0.90W 로 **더 좋아 보였다.** 창의 32.7%가
55W 를 넘었고 최대 344W 였다. `absorb_residual` 이 상한을 지키도록 고쳤다.

**얻은 것** — SMPS 3종이 전부 오른다 (충전기 +0.022, 프로젝터 +0.028, 미니PC
+0.028). 유령이 1/8 로 준다.

**판 것 — 셋 다 저항 쪽이다.**
① 프로젝터 **창별** 중앙|오차| 0.00 → 7.09W. 스냅을 버려서다. 평균은 −1.42W.
   스냅의 0.00 은 모델이 맞춘 증거가 아니라 후처리가 덮은 결과다.
② 드라이기 F1 −0.016.
③ 대조 파일 잔차 +2.36W. 저항만 있는 파일에는 흡수가 되돌려 줄 SMPS 가 없다.

- **오븐 0.624 는 이 세션이 만든 것이 아니다.** 12.119 가 라벨 정의 차이로
  확정했다 — 재현율 천장 0.607, 세션 단위로는 0.925.
- **전기포트 −27.6W 는 1309W 의 2.1%** 이고 중앙 |오차| 는 9.23W (0.7%) 다.
  평균을 끄는 것은 소수의 전이 창이다.
- 대조 2파일 F1 0.800 은 저항 3종만 있는 파일이다 (12.119 의 그 층).

---

## 4-1. 흡수의 **배분 규칙**도 바꿨다 — `Q` 를 방정식으로 (12.152 / 12.153)

> 사용자: *"흡수시키는 매커니즘을 더 개선할 여지가 없나?"* → *"6W를 맞게 배분해야지.
> 모델을 한번 더 돌릴 수도 없고 뭘 써야 되는 거지?"*

**12.152 — 세 갈래를 재고 셋 다 접었다.** Norton 보정을 흡수에 넣기 / NNLS 배분 /
고조파가 지지하는 만큼만 주기. 진단은 셋 다 "고칠 것이 있다" 였는데 절제하면
전부 나빠진다. 특히 `증거제한만` 이 **흡수를 아예 끈 것과 같아진다** (−4.53/6.62 vs
−4.70/7.27). 그래서 정체가 드러났다:

> `absorb_residual` 은 **고조파 귀속 장치가 아니다.** 총전력 부족분을 SMPS 로
> 되돌리는 장치이고, 고조파는 **방향만** 고른다.

**12.153 — 그러면 방정식을 하나 더 주면 된다.** 12.133 이 이미 잰 `Q/P` 다
(SMPS 쌍 d′ 2.31~4.64 vs 고조파 0.91~1.85).

```
Σ x_k            = 총전력 잔차      하드 (6W 는 그대로 다 나간다)
Σ (Q/P)_k · x_k  = 무효 잔차        연성  <- 새로 쓰는 것
Σ x_k · sig_k    ~ 고조파 잔차      연성
```

```
판           프로젝평균      중앙|e|         유령대조       잔차겨냥     잔차대조
A 코사인    -1.95±0.92  7.31±0.06   1.13±0.38  2.12±0.13  14.45
pq w_q=1   -1.42±1.14  7.09±0.28   1.41±0.94  **1.62±0.13**  14.71
pq w_q=3   -1.62±1.11  7.02±0.28   1.46±1.01  2.11±0.23  14.79
pq w_q=9   -1.65±1.10  7.00±0.30   1.46±1.01  2.29±0.25  14.79
```

`w_q` 1→9 가 **단조**다 (프로젝터 ↔ 잔차의 절충). **`w_q=1` 을 쓴다** — 잔차겨냥
2.12 → 1.62W 로 시드 범위가 분리된다 ([2.06,2.19] vs [1.56,1.69]).

⚠ **가산성이 경험적이다.** 이 `Q` 는 `sign(phase)·sqrt(S²−P²)` 라 원리적으로
가산이 아니고, 12.133 이 66창 중 62창에서 성립을 확인했을 뿐이다.
⚠ 대조 파일에서 무효 잔차가 −78 VAR 로 기대(−17)의 4.6배다. 저항의 `Q` 모형이
부정확하다는 뜻이고 잔차대조가 +0.26W 나빠진다. **안 쟀다.**

---

## 5. 남은 것 — 다음 세션이 볼 자리

### [1] 미니PC 유령은 **진짜 오검출**이고 **오븐이 켤 때만 난다** (12.149.4)

겨냥 파일 유령의 나머지가 사실상 전부 여기다. 미니PC 가 없는 겨냥 파일은
`test_4` 하나이므로 그 파일이 전부다:

```
test_4 (642초)        평균W    게이트 on율
옛 배포본 adapt_ovh   10.72     0.397
Z·I 만               2.54     0.354
새 운영점              3.41     0.354      <- 231초 동안 "미니PC 켜짐"
```

**전력은 1/3 로 줄었는데 게이트는 그대로다.** 후처리로는 못 고친다 (게이트가
진짜로 켜져 있어 스켈치 사정권 밖).

⚠ **여기 적었던 "오븐이 켤 때만 난다" 는 12.154 가 철회했다** — `test_4` 는 오븐이
파일 100% 내내 켜져 있어 초과율이 1.000, 즉 정보가 0 이었다 (규칙 23). 진짜
구분자는 **프로젝터**다:

```
test_4, 미니PC 게이트 ON 창 (36%)    기저율   유령창중   초과율
프로젝터 게이트 ON                   0.722    0.994    1.376
충전기 게이트 ON                    0.358    0.465    1.299
오븐 게이트 ON                     0.340    0.398    1.173
r(프로젝터 예측W, 미니PC 게이트) = +0.642   |  r(오븐 예측W, ...) = +0.092
```

즉 **12.87.3 이래의 SMPS 3종 내부 오배분**이다 (지문 셋이 11.9도 안, cos 0.9786).
미니PC 는 h1 결손을 갚고 있고(미니PC 를 0 으로 두면 ΔL_harm 의 **79%가 h1**),
그 결손은 **SMPS 가 있을 때만** 생긴다 (저항 전용 파일 셋에서는 유령 0.000).

**①은 해 봤다 — 본전이다** (12.154.1). `adapt_zv*` 를 새 운영점에서 재채점했다:

```
                    미니PC유령W        잔차겨냥        프로젝터평균
현행 Z·I         [4.52, 5.65]   [1.54, 1.68]   [-2.15, -1.01]
+ h1 추종만       [2.97, 4.04]   [1.93, 2.17]   [-3.20, -2.62]
                   ✅ −1.49W       ❌ +0.45W      ❌ 1.5W 더 과소
```

**미니PC 유령과 프로젝터 과소예측은 같은 손잡이다** — 12.128 의 제로섬 맞바꿈
(프로젝터 +17.00 / 충전기 −17.01)의 미니PC 판이다. F1 은 어느 쪽도 안 움직인다.
채택 안 했다.

**②가 남은 길이다** — 옮겨 다니는 것을 멈추려면 판별자가 더 있어야 하고, 안 쓴
것은 `Q/P` 뿐이다 (프로젝터 vs 미니PC d′ **4.64** vs 고조파 1.85). 12.153 이
**흡수의 배분에만** 넣었고 게이트·손실에는 아직 안 쓴다.
⚠ 후처리로 게이트를 끄는 것은 규칙 18 에 정면으로 걸린다. **손실 항**으로
넣는 쪽(`w_consq`, 12.134 계열)이 규칙과는 맞는데 그 계열은 한 번 실패했다.

### [2] 손실에는 여전히 누설을 보는 항이 없다 — `--w-leak` (미시도)

후처리가 증상을 지웠을 뿐 2단계 적응은 아직 그 열을 배출구로 쓸 수 있다.
그 자유를 뺏으면 **배분 자체가 달라질** 수 있다.

```
L_leak = mean_k[ relu(τ − σ_k)/τ · σ_k·p_raw_k ] / max(P관측, 10W)
```

⚠ 반증 조건을 먼저 적을 것 — 부족분(+5.7W)은 갈 곳이 있어야 하므로, 누설을
막으면 **켜진 기기로 간다.** 프로젝터가 다시 오를 수 있다. 3판 × 3시드, 30분.

### [3] 부족분 +5.7W 의 정체는 아직 모른다

`gap = P관측 − 재구성` 을 회귀하면 절편 **5.72W**, 기울기 −0.02%. 전부 OFF 창의
`P관측` 은 4.5~5.5W 인데 대기항(5.33)+계측(1.40)이 이미 그만큼이라 **배경이
아니다.** 켜진 기기의 과소예측인데 어느 기기인지 안 갈랐다.

### [4] 저항 정합과 스켈치의 상호작용

대조 파일의 드라이기 유령이 1.93 → 3.78W 로 늘었다 (τ0.5 판). 스켈치가 저항 열의
문턱 아래 전력을 지우면 `resistive_match` 의 입력이 바뀐다. **안 쟀다.**

### [5] `absorb_residual` 의 수신자가 SMPS 뿐이다 — **가장 값진 다음 수**

저항만 있는 파일에서 잔차가 갈 곳이 없다 (대조 14.71W, 옛 배포본 12.35W).
이번에 판 셋이 전부 저항 쪽인 것도 여기서 온다. 저항 수신자를 열려면 3차 이상
코사인이 저항에 대해 무의미하다는 것부터 풀어야 한다 (12.104) — 저항은 3차를
거의 안 흘린다. 후보는 등가저항 정합(12.112)이 이미 쓰는 `P = V²/R` 이다.

### [6] 프로젝터 창별 산포 — 중앙|오차| 7.09W, 그리고 **창의 15%가 상한 위**

스냅을 버려서 드러났다. 격리 통전 폭이 44.2~47.8W(폭/중앙 0.077)뿐인데 모델은
창의 **15.1% 에서 55W 를 넘고 최대 95W** 다 (12.149.3). 상한이 그것을 자르므로
지표에는 안 보이지만 **모델 자체의 분산 문제**다. 옛 모델은 52.6% / 129W 였으니
Z·I 가 8분의 1로 줄인 것이고, 남은 15%가 다음 결함이다.

---

## 6. 새 도구 / 플래그

```
postproc.squelch(P, gate, tau)          게이트 정합 스켈치 (12.149)
postproc.absorb_residual(exclude=[..])  못 박은 기기는 잔차를 안 받는다 (12.149.2)
postproc.ABSORB_CAP_W                   **흡수의 기기별 천장** (12.149.3/.4)
run_gate_check/power_check --absorb-cap-scale   그 천장 배수 (값 둔감성용)
run_live (인자 없이)                     **새 운영점** — 기본값이 곧 운영점이다
run_plot_timeline (인자 없이)            그림도 새 운영점이 기본값이다
run_gate_check --squelch TAU            채점 쪽 같은 순서
run_power_check --squelch TAU --absorb F  기기별 전력 오차를 같은 파이프라인에서
run_summarize_pan --human-only          사람 라벨 층으로 표를 다시 낸다 (규칙 24)
run_summarize_pan --suffix S            후처리 접미사. 안 주면 산출물에서 찾는다
src/run_ghost_absorb_probe.py           유령이 무엇을 흡수하는가 (재학습 없음)
```

## 7. 산출물

```
results/gc_sqab.json      pc_sqab.json       τ0.5 + 흡수1.0 (기준선 팔 포함)
results/gc_cap10.json     pc_cap10.json      **운영점** τ0.1 + 흡수1.0 (천장 기기별)
results/gc_cap125 / cap150                     천장 배수 쓸기 (12.149.4)
results/gc_cap.json       pc_cap.json        같은 설정, 천장이 프로젝터뿐 (12.149.3)
results/gc_sq10_ab1.json  pc_sq10_ab1.json   같은 설정, **상한 버그 판** (12.149.3 전)
results/gc_sq05_ab1 / sq20_ab1 / sq50_ab0 / sq50_ab05   쓸기
results/gc_absorb.json    흡수만
results/gc_op_now.json    pc_op_now.json     **옛 배포본** (스냅 켬)
results/gc_op_new.json    pc_op_new.json     스냅 + 흡수(프로젝터 포함)
results/gc_op_new2.json   pc_op_new2.json    스냅 + 흡수(프로젝터 제외)
results/ghost_absorb.json 진단
results/_sweep_sq.sh      쓸기를 만든 스크립트
```

## 7-1. 배포 꾸러미(`deploy/`)도 같이 갱신했다

`deploy/` 는 런타임을 **복사해 둔 별도 꾸러미**라 `src/` 변경을 자동으로 못 받는다.
넷을 맞췄다:

```
deploy/models/adapt_zi_s0.pt            새로 넣음 (옛 adapt_ovh.pt 도 남겨 뒀다)
deploy/nilm_runtime/signatures.npz      **신규 3KB** — 와트당 고조파 지문.
                                        `absorb_residual` 이 쓰는데 배포에는
                                        `SegmentPool`(processed_data/npz)이 없다
deploy/nilm_runtime/postproc.py         `squelch` + `absorb_residual(exclude=)`
deploy/nilm_runtime/predictor.py        기본값 snap 46.9 -> 0, squelch 0.1, absorb 1.0
```

확인 (test_7 재생, 2.8초):

```bash
cd deploy && python -X utf8 run_predict.py --replay ../data/test_7.csv --speed 0
```

⚠ `signatures.npz` 의 기기 목록이 체크포인트와 다르면 `NILMPredictor` 가 즉시
`ValueError` 를 낸다. 기기 목록을 바꾸면 **다시 구워야 한다**:

```python
from src.run_gate_check import load_model, _signatures
m, apps, _ = load_model("results/adapt_zi_s0.pt", "cpu")
sig, sb, nz = _signatures(list(apps))
np.savez_compressed("deploy/nilm_runtime/signatures.npz", appliances=np.array(list(apps)),
                    sig=sig, standby_sig=sb, noise_sig=nz)
```

## 8. 환경 메모 (e 에서 그대로)

- 학습에 `--init results/cnn_ovh.pt --cache cache/train60_ovh` 를 반드시 줄 것.
- 한글 트레이스백이 안 보이면 `python -X utf8 -m ...`.
- **`data/test.csv` 는 다른 장소다.** 임피던스를 공유하는 적합에서 반드시 뺀다.
  그리고 **그 파일에는 에어컨이 실제로 있다** — 에어컨 열을 지우면 안 되는 이유다.
- 표를 손으로 옮기지 말 것 — `run_summarize_pan` 이 만든다.
