# 인수인계 2026-09-01d — 손실 수정이 25전 1승에서 **27전 3승**이 됐다

`HANDOFF_2026-09-01c.md` 다음이다. c 는 배분의 **기제**를 닫았고, 이 반쪽은
**처방**을 찾았다. 설계 문서 **12.131 ~ 12.138.1**, 규칙 **36 · 37**.

---

## 0. 한 줄

**`L_harm` 의 결함은 세기가 아니라 차수 배분에도 있었다.** `harm_scale` 이
15차수를 균등화하는데 실측에서는 고차가 신호가 아니라 모델오차다. 차수 가중
`1/h²` + in-situ 지문이 **초가법**으로 프로젝터 +30.0 → **+7.3W**.

그리고 프로젝터 편향은 **`L_harm` 과 `L_cons` 의 상호작용**이다 — 각각은
+8.5 / −4.5 인데 둘 다 있으면 +14.4 다 (초가법).

---

## 1. 오늘의 표 — 전부 같은 명령으로 채점 (후처리 끔 / 운영점)

```
판             프로젝터W     유령8       유령대조      잔차       F1       프로젝정밀 충전재현 미니정밀
1단계 cnn_ovh   +15.6       7.36        4.29      13.83  0.843    0.881  0.986  0.941
기준선 w4.0    +30.0 ±1.8  3.54 ±0.87  7.09 ±1.63  4.24 ±0.13  0.846 ±0.008  0.822  0.919  0.820
w_harm 1.0     +26.1 ±0.8  5.92 ±0.20  9.21 ±2.11  4.68 ±0.23  0.860 ±0.006  0.853  0.943  0.835
w_harm 0       +11.1 ±2.4  7.11 ±0.15 11.69 ±0.21  5.22 ±0.09  0.883 ±0.004  0.860  0.973  0.847
w_cons 0       +24.1      34.23      17.76       8.96       0.769         —      —      —
가중 1/h²      +23.5 ±0.9  4.67 ±0.32  8.22 ±0.35  4.55 ±0.10  0.865 ±0.006  0.859  0.963  0.873
in-situ 지문   +17.7 ±1.3  6.04 ±2.04  7.31 ±0.25  4.56 ±0.15  0.846 ±0.020  0.859  0.942  0.813
**가중+지문**  **+7.3 ±0.8** 4.52 ±0.28 11.51 ±0.94 4.51 ±0.08  0.877 ±0.005  0.860  0.960  0.864
consQ 0.1      +27.3      7.95      31.04       5.42       0.862   (반증됨)
consQ 0.3      +26.2      9.79      52.38       5.63       0.865   (반증됨)
```

⚠ **저항 열은 이 세션 중반에 가드가 바뀌었다** (병렬 세션의 `EDGE_GUARD_FRAC`).
프로젝터 열은 안 바뀐다. 12.123/12.125 의 저항 값은 옛 가드다.

---

## 2. 확정된 것

### (1) `L_harm` 의 결함은 **차수 배분**에도 있다 (12.135/12.136)

차수 부분집합마다 12.122.2 의 자를 다시 풀면 **모델오차/판별신호가 단조**다:

```
h9~h15 고차만 3.68  >  짝수차 2.74  >  **전 15차(현행) 2.23**  >  홀수차 2.17
   >  1/tau 2.00  >  1/h 1.74  >  **1/h² 1.57**  >  h1,h3 만 1.41 (바닥)
```

`harm_scale` 의 전제 — *"판별 정보는 높은 차수에 있다"*(0.2절) — 는 **합성에서는
맞아도 실측에서는 뒤집힌다.** 바닥이 1.41 이고 거기서는 유효차원이 4 라 더
낮추면 식별성이 죽는다 (규칙 31). `1/h²` 이 유효차원 4.6 을 남기고 **튜닝 상수가
없다**.

**학습으로 확인**: 같은 유령에서 `w_harm` 곡선보다 프로젝터가 4.6W 낮고, 같은
프로젝터에서 유령이 1.46 싸다. **곡선 밖 = 세기와 독립인 축이다.**

### (2) 가중과 지문은 **초가법** (12.137)

```
프로젝터전력W   가중 −6.5 + 지문 −12.3 = 예측 −18.8   vs   실측 **−22.7**
```

12.123 의 불감대x지문은 **겹쳤는데**(둘 다 "못 줄이는 잔차의 압력"), 가중은
`L_harm` 의 **차수 무게**를, 지문은 `sig_i` 의 **값**을 바꾼다.
⚠ **왜 합보다 큰지는 못 밝혔다.**

### (3) `L_cons` 는 원인이 아니라 **조건**이다 (12.138.1)

```
1단계 기준선          +15.6
L_cons 만 더하면      **−4.5**   <- 줄인다
L_harm 만 더하면      **+8.5**   <- 늘린다
가법 예측             +19.6
둘 다 (실측)          **+30.0**  <- 합보다 10.4W 크다
```

`L_cons` 가 총합을 못 박으므로 `L_harm` 의 선호가 **제로섬 재배분**이 될 수밖에
없다 (12.128 의 +17.00/−17.01). 끄면 제로섬이 아니라 전부 부풀린다
(유령 3.54 -> 34.23W).

> 12.128 은 "프로젝터가 총전력 **부족분**을 안 받는다"(기울기 0.001)로 `L_cons`
> 를 원인에서 뺐다. **"원인이 아니다" 와 "무관하다" 는 다르다.**

### (4) 라벨 의존도

```
처방                  적용에 필요한 것            라벨      프로젝터Δ
--harm-weight inv_h2  **없음** (차수의 역제곱)     **0**     −6.5W
--snap 46.9           REFERENCE_W (격리 녹화)     0         (후처리)
--sig-insitu          복합 파일 적합              **상시**   −12.3W
둘 다                                                     **−22.7W**
```

**차수 가중만 라벨이 0 이다.** 그것만으로는 −6.5W 이고 지문을 얹으면 −22.7W —
**라벨이 3.5배를 산다.** 그리고 **채점에는 원리적으로 라벨이 필요하다.**

---

## 3. 닫은 갈래 (전부 재학습 없이 또는 절제로)

```
❌ 무효 채널 (N, Q₁)   **분해능** — 21.7~49.1W < 결함 17W (12.134.4)
                      학습으로도 시험했고 유령대조 52.38 참사 (12.134.1)
❌ 전이(轉移)          정상상태보다 판별력이 나쁘다 (d′ 0.5~0.8배, 12.135)
❌ 상태 조건 지문       판별비 2.23 -> 2.19 (12.124)
❌ L_cons 슬랙        프로젝터가 총전력 부족분을 안 받는다 (12.128)
❌ 충전기 시간 패턴     완충 근처는 짧은 버스트, 흔들림에 주기 없음 (12.130.1)
❌ w_cons 0           프로젝터 −5.9W 인데 유령 10배·F1 붕괴 (12.138.1)
◐ 합성기 상향         12.125 는 닫았고 12.138 이 다시 열려다 **잴 수 없어** 실패
```

---

## 4. ⚠ 이 세션이 **철회한** 것 — 다시 파지 말 것

```
"순수한 sim2real 전이 문제"        철회. `L_cons` 가 조건으로 관여한다 (12.138.1)
"합성이 고조파를 2배 덜 만든다"      철회. **선택 기준 불일치**였다 (12.138)
"합성에 위상 상쇄 (0.47배)"         철회. 정상상태 창만 보면 1.04 (격리 실물 0.98~1.01)
"Q 는 비가산이라 불가"              정정. N 은 맞고 Q₁ 은 틀리다. 진짜 이유는 **분해능** (12.134.4)
"h1 이 91% 미설명"                정정. 상대값 오독. 절대 15.3 VAR, 보정하면 2.9
"전역 Q 보존 항"                   반증. 검증 안 된 열이 배출구가 된다 (12.134.1)
```

**12.138 의 교훈**: 합성에는 타깃 시점 라벨, 실측에는 구간 라벨이 있어 자연스럽게
쓰는 조건이 다르다. **맞추지 않으면 sim2real 비교는 무엇이든 나온다** (규칙 37).
맞췄더니 표본이 0~2개면 **"반증"이 아니라 "못 잰다"** 다 (규칙 28).

---

## 5. 다음에 할 것

### [1] **대조 파일 유령을 가른다** — 가중+지문 채택 전 필수

`유령대조 11.51 ±0.94` 가 네 판 중 최악이고 **초가법**이다 (겨냥 +0.98 vs
대조 +4.42). 저항 전용 파일에 SMPS 를 붙인다. **지문 단독은 대조 +0.22 로
깨끗했으므로 가중과 겹칠 때 생긴다.** 재학습 없이 파일·기기별로 가를 수 있다.

### [2] 1단계의 +15.6W — **아직 설명 못 했다**

`L_cons` 도 `L_harm` 도 아니다 (둘 다 없는 모델이다). 합성에서는 −1.59W 로
정확한데 실측에서만 틀리고, **프로젝터만이 아니라 전반적이다** (포트 +13.3,
오븐 +14.2, 실측 잔차 절대 53.96W).

⚠ 12.138 이 이것을 재려다 세 번 실패했다. **조건 맞춘 실측 창이 더 필요하다** —
창 내내 켜진 SMPS 전용 창이 지금 0~2개다 (규칙 37/28).

### [3] 학습 체크포인트에 `_config` 를 넣는다

`cnn_ovh` 의 손실 가중이 체크포인트에 없어서 1단계 재현이 원리적으로 불가능했다.
로그(`results/_cnn_ovh.log:6`)에는 있어서 이번엔 닫혔지만 운에 기댄 것이다.
규칙 33 을 학습 쪽으로 확장한다.

### [4] 운영점 후보로 굳히기

`가중+지문` 시드를 늘리고 스냅(12.131)과 겹쳐 본다. 스냅이 프로젝터를 0.00W 로
닫으므로 **모델 개선의 값어치는 충전기·미니PC 쪽에서 나온다** — 그쪽은 참값이
없어 경계 비교(12.129)가 전부다.

---

## 6. 새 플래그 / 도구

```
run_adapt --harm-weight {off,inv_h,inv_h2,inv_tau}   차수 신뢰도 가중 (12.136) ✅
run_adapt --w-consq                                 무효전력 보존 (12.134) ❌ 반증
run_live  --snap (기본 46.9)                         프로젝터 참값 스냅 (12.131) ✅ 채택
losses.NILMLoss(harm_weight=, reactive_qp=, noise_q=)
net.reactive_signatures / noise_reactive             Q/P 상수 (**녹화 간 가드 포함**)
src/run_second_judge_probe.py     차수 부분집합 + Q/P 판별력
src/run_lharm_alt_probe.py        차수 가중 상한 + 전이 판별력
src/run_dump_probe.py             창 단위 인과 분해
src/run_snap_reverses_probe.py    스냅이 맞바꿈을 되돌리는가
src/run_charger_pattern_probe.py  충전기 시간 패턴
src/run_forward_gap_probe.py      순방향 오차의 층 + sim2real
src/run_state_sig_probe.py        상태 조건 지문
```

---

## 7. 환경 메모

- 학습에 `--init results/cnn_ovh.pt --cache cache/train60_ovh` 를 반드시 줄 것.
- 한글 트레이스백이 안 보이면 `python -X utf8 -m ...`.
- 채점 산출물의 `_config` 에 명령이 들어 있다 (규칙 33). **세션 중 가드가 바뀐
  것을 그것으로 잡았다.**
- **학습 로그 6번째 줄에 손실 가중이 찍힌다** (`results/_cnn_*.log`). 체크포인트에
  없으므로 1단계 설정을 확인하려면 거기를 봐야 한다.
