# 인수인계 2026-09-13 — 재설계 시도와 **네 번의 자기반증**

앞 문서: `HANDOFF_2026-09-12.md`. 이 세션의 상세는 설계 문서 **13.85 ~ 13.95 와 §14** 에 있다.

---

## 0. 지금 당장 알아야 할 것

### ⓪ ★★★★★ 이 세션은 **내가 낸 결론을 네 번 스스로 뒤집었다**

```
13.84.74  "2단계가 **큰 부하에서만** 터진다"          -> 13.91 반증. 절대 W 눈금 착시였다
13.91 [5] "상태 헤드가 듀티를 못 가른다"              -> 13.92 반증. 혼합 엔트로피 0.000
13.89 [1] "충전기가 미니PC 를 덮는다 (비대칭 가림)"    -> 13.90 반증. rho 전부 ~1.00
13.93 [1] "alloc 이 저항 신원을 +0.148 올린다"        -> 13.94 반증. 버그 기준선이었다
```

**네 번 다 같은 모양이다** — 한 번 재고 결론을 냈다가, ① 지표를 바꾸거나(절대->상대)
② 표본을 늘리거나(12k->40k) ③ 기준선을 고치니 뒤집혔다. 그리고 **세 번은 사용자가
물어서** 뒤집혔다. 새 결론을 내기 전에 그 셋을 먼저 하라.

### ① ★★★★★ **`run_plot_real` 이 가림을 안 걸고 있었다** (13.95) — 고쳤다

옛 CNN 갈래가 `NILMNet` 을 직접 지으며 `zero_channels` 를 안 읽었다.
**`cnn_v37` 만 물린다** (v33~v36 은 가림이 없고, 시퀀스 판은 다른 갈래로 간다).
```
v37 그림 잔차 평균   85.5W  ->  **40.0W**      (v36 은 40.3W 로 **불변** = 확증)
```
⚠ 그 전에 만든 `cnn_v37` 그림은 전부 무효다. `_v37fix` 로 다시 그렸다.

### ② ★★★★ 잔차 정의가 **둘**이다. 섞어 쓰지 마라 (13.95 [5])
```
run_plot_real   관측 − (예측합 + **모델이 낸 대기**)      양수 = 과소예측
score_arm       (예측합 + **라벨 기준선 p_base**) − 관측   부호 반대
```

### ③ ★★★★★ 지표를 **절대 W 로 판정하지 마라** (13.91)
오븐/핫플 창은 관측이 수백~1500W 라 **같은 상대오차가 절대 W 로 10배**가 된다.
cnn_v37 은 오븐핫플 **4.1%** · 그밖 **4.1%** 로 정확히 같다. 사슬 학습은 둘 다 약 2배로
**고르게** 나빠진다. "큰 부하에서만" 은 없었다.
채점기에 `rel_heavy`/`rel_light` 를 넣었고 경고도 찍는다.

### ④ ★★★★ on/off 평균은 **핫플 라벨 규약에 44% 지배된다** (사용자 지적)
핫플 `is_on` 이 **ARMED(휴지 포함)** 다. 실제 통전은 41.7% 뿐(456W), 나머지는 2W.
```
cnn_v37 핫플  재현율 0.409 (= 실제 듀티 0.417) · **정밀도 0.995**  <- 통전을 맞히고 있다
사슬    핫플  재현율 0.996                                        <- 라벨을 맞히고 있다

on/off 평균   전부 0.8647 대 0.9322   (사슬 +0.068)
             핫플 빼고 0.9093 대 0.9238   (+0.015)
             핫플·오븐 빼고 0.9042 대 0.9141  (**+0.010**)  <- 씨앗 잡음 이하
```
**사슬의 on/off 이득은 사실상 핫플 하나다.** 채점기에 듀티 기기 분리가 **아직 안 들어갔다** (§6-1).

### ⑤ ★★★ **용어** — `seq_*` 는 **사슬 학습**이지 4.2절의 실측 적응(2단계)이 아니다
후자는 12.31 이 미니PC 0.574->0.137 로 되돌렸고 6절이 금지한다.
`run_train_seq` 의 `real` 은 **채점에만** 쓰이고 손실에 안 들어간다 (grep 확인).
13.84.74 부터 내가 "2단계" 로 잘못 불렀다.

---

## 1. HPC 에서 도는 것 (2026-09-13 09:34 기준)

```
976560  seq_fix_ctl_s1   n057 gpu2 A10 48코어   씨앗 1 · 기준선      ep 9/12
976561  seq_fix_attn_s1  n016 gpu6 A10 40코어   씨앗 1 · 어텐션      ep 8/12
976720  seq_fix_nochain  n026      A10 40코어   **--w-crf 0**       ep 1/12
```

**`nochain` 이 이 세션 최대 미결의 시험이다.** 생성기(v36p)·`S_STATE`·초기화는 다른 팔과
같고 **CRF 항만 뺐다**. 손실이 `창 손실 + 0·CRF` 라 정확히 1단계 학습이다.
```
nochain 이 cnn_v37 수준(상대잔차 ~4.6%)이면  -> **CRF 항이 몸통을 망친다.** v36p 로 롤백 완료
nochain 이 여전히 ~8.8% 면                  -> 범인은 생성기 v36p / S_STATE / scratch 초기화
```

씨앗 2판의 판정 기준(**돌리기 전에 적었다**):
```
① ctlfix 두 판(s0,s1) 폭이 attn−ctlfix 차이(창별 +0.014 · SMPS신원 +0.051 · 저항신원 −0.037)
   보다 크면 -> **이번 판의 어떤 차이도 효과로 못 읽는다**
② 그 폭을 앞으로의 **잡음 바닥**으로 채택 (12.39.2 ④ 규약)
③ 폭(max−min)을 쓰고 표준편차는 안 쓴다 (n=2 에 자유도가 없다)
```

---

## 2. 채점 정본 — **사슬 이전이 사슬보다 낫다**

`src/run_diag_rollback.py` (네 판을 **한 채점기**로. `--no-chain` 으로 사슬만 떼기도 된다):
```
                   v33     v34     v35     v36     v37   | seq_fix_ctl(사슬)
on/off            0.8478  0.8422  0.8343  0.8437 0.8520  |  0.9219
저항 4종 신원       0.9815  0.9753  0.9815  0.9753 0.9815  |  0.9691
SMPS 3종 신원      0.9316  0.9316  0.9658  0.8947 0.9579  |  **0.8605**
상대 잔차           5.4%    5.1%    5.0%    4.8%   4.6%   |  **8.8%**
혼자켜짐 전력오차    24.2%   24.0%   28.8%   27.6%  23.5%  |  26.7%
```
**계보 다섯 판 전부**가 사슬 판보다 잔차·SMPS 신원에서 낫다. 한 판의 우연이 아니다.
`cnn_v37` 이 계보 1위 (6개 중 4개).

**⚠ 그런데 사슬을 떼도 신원·잔차는 안 돌아온다** (`--no-chain` 으로 확인):
```
seq_fix_ctl  사슬 있음   on/off 0.9219 · 저항 0.9691 · SMPS 0.8605 · 잔차 8.8%
             사슬 뗌     on/off 0.8538 · 저항 0.9691 · SMPS 0.8605 · 잔차 8.8%
                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ **불변**
```
`power = σ(on_logit)·Σ mix·p_states` 이고 사슬은 **on/off 판정만** 낸다. 전력을 안 건드린다.
⇒ 차이는 **몸통**에 있고, 몸통이 다른 이유는 넷이다: 생성기(v36 대 v36p) · `S_STATE` ·
  CRF 항 · 초기화. `nochain` 이 그 중 CRF 항을 가른다.

**전이 뒤 감쇠** (`src/run_diag_lag.py`) — 큰 변화(|ΔP|>=200W) 뒤 |잔차|:
```
경과        cnn_v37   seq_fix_ctl
0~2s        63.6W     119.8W
64초 이상    **16.8W**   **93.4W**       -> cnn 은 −74% 로 가라앉고 사슬은 −22% 뿐
```
⚠ 다만 **전이가 하나도 없던 구간에서도 이미 14.9W 대 36.2W (2.4배)** 다.
   전이 구조 하나로 귀속할 근거는 **없다**. 부호 있는 잔차는 세 판이 비슷하니
   편향이 아니라 **분산**이다.

---

## 3. 고친 것

| | 무엇 | 절 |
|---|---|---|
| 1 | **`run_plot_real` 가림 누락** — `run_gate_check.load_model` 에 위임 | 13.95 |
| 2 | **`S_STATE["hotplate"]` 슬롯** `{1:549.6}` -> `{1:10.0, 2:549.6}`. 통전 전력이 휴지 슬롯에 얹혀 있었다 | 13.92 |
| 3 | **슬롯 정합 관문** `run_gate_states` [5] — 풀 p90 과 대조. 오탐 0, 옛 값 되살리면 양쪽 다 잡는다 | 13.92 |
| 4 | **상대 잔차** `rel_heavy`/`rel_light` + 별도 표 + 경고 | 13.91 |
| 5 | **합 정합성 사영** `--proj/--proj-cap/--proj-floor/--proj-resp`, 관문 7개 | 13.87 |
| 6 | **기기 축 어텐션** `--appl-attn 64`. 0 초기화가 지금 모델과 **정확히 동일**, +50,304 파라미터 | 13.93 |
| 7 | **`--w-cons`** 를 인수로 (시퀀스 학습기가 0.0 으로 박아 놨던 것) | 13.87 |
| 8 | **`--dry-run`** 연기 시험 (974261 이 옵티마이저 조립에서 죽었다) | 13.88 |
| 9 | **numpy2 피클 shim** (`circuit_model/fcm12.py`) — 테스트 44개가 이것 때문에 죽고 있었다 | — |
| 10 | **옵티마이저 중복 파라미터** — `proj_head` 가 `model.parameters()` 에 이미 있었다 | 13.88 |
| 11 | **`--seed` 가 꼬리표에** — 씨앗 반복이 씨앗 0 판을 덮어쓰던 것 | — |
| 12 | **HPC_RULES §2 재정정** — 노드당 상한은 `MaxTRESPerNode` 에 있다 | — |
| 13 | **HPC_RULES §5 체제 구분** — 워커 44면 GPU 병목, 14면 CPU 병목 | — |

새 도구: `run_baseline_nilmtk` · `run_diag_dictcond` · `run_diag_coswitch` · `run_diag_masking`
· `run_diag_heavy` · `run_diag_rollback` · `run_diag_lag` · `run_gate_proj`

---

## 4. 측정으로 **죽은** 것 — 되살리지 마라

| 죽은 주장 | 측정 |
|---|---|
| 사전 조건수가 계획 B 를 막는다 | 등장 활성집합 **50종 전부 cond <= 299** (13.85 [2]) |
| 기기 간 비대칭 가림이 있다 | `rho` 9x9 **전부 ~1.00**. n=12k 의 이탈 두 칸은 n=40k 에서 사라졌다 (13.90) |
| A-크기 (`proj_resp=power`) | 창별 곱셈 재조정과 같아 **크기만** 고친다. 학습에 넣으면 SMPS 신원 −0.069 (13.87 [2] · 13.93) |
| 계획 E (시간축 트랜스포머) | §10(40h 로도 CNN<RF) + 12.8(120초가 더 나쁨) + **COLD 자신이 시간축을 첫 층에서 버린다** (13.89 [2]) |
| 정보가 합에 없어서 못 가른다 | 13.85 [2] · 13.90 **두 독립 측정**이 정보가 있다고 말한다 |

---

## 5. 문헌 (13.89) — 우리 병목을 다룬 2026 논문

**Entropy 28(3) 334 (2026-03)** *An Information-Theoretic Analysis of High-Frequency Load Disaggregation*
· 가림을 **조건부 상호정보 보존비**로 잰다 (우리 13.90 이 그 방법을 우리 자료에 적용했다)
· **충전기가 세션 간 분포이동 최대** (JS 거리 ~1.0) — 우리 12.35 와 독립적으로 같은 결론
· mRMR 특징 순위 **1위 h1, 2~10위가 h29·h23·h13·h31·h19·h25·h26·h21** —
  **우리는 h15 까지만 본다.** 13.84.66 [7] 의 전압 꼬리에 외부 근거가 생겼다
· NMI 가 분해오차를 예측 (Spearman −0.81)

**COLD** (arXiv 2106.02352) · 동시 부하 전용 트랜스포머 · **50차까지**
· 합 제약을 **공유율 softmax** 로 넣는다 (합=1 을 먼저 내고 총량을 곱한다)
  -> 우리 사영의 **대안 형식**. `resp=head` 가 실패하면 다음 후보
· 시간축을 **첫 층에서 1 로 뭉갠다** — 트랜스포머인데 시간 어텐션을 안 쓴다

---

## 6. 다음에 할 것

**6-1. 채점기에 듀티 기기 분리** (⑤ 참조). on/off 평균이 핫플 라벨 규약에 44% 지배된다.
  핫플·오븐을 따로 찍어야 앞으로 같은 착시가 안 난다. **싸다. 먼저 하라.**

**6-2. `nochain` 결과로 롤백 확정.** CRF 항이 범인이면 v36p 를 지킨 채 롤백된다.
  아니면 `cnn_v37`(생성기 v36 = 형제 위상 산포 **실측의 12~42배** 과장)을 감수해야 한다.

**6-3. 씨앗 2판으로 잡음 바닥 확정.** 이 세션의 모든 효과(0.010~0.051)가 12.39 가 잰
  씨앗 잡음(핫플 F1 **0.120**)과 같은 자리다. **그 전에는 어떤 팔도 채택 못 한다.**

**6-4. 계획 D (h17~h31 + 드리프트).** 13.89 의 mRMR 이 그 대역을 판별력 2~10위로 꼽고,
  13.84.66 [7] 이 이미 쟀다(예측 각도 14~24° -> 5~10°). 13.85·13.90 이 "병목은 관측
  가능성이 아니라 **사전 충실도**" 로 수렴한다.

**6-5. 13.86 기준선 재실행.** `S_STATE` 고침이 CO/FHMM 사전을 바꾼다
  (핫플 `[0,549.6]` -> `[0,10,549.6]`).

**6-6. 저항 4종이 여전히 미해결.** 13.86 [3](가): 학습이 무매개변수 CO-H(0.7143) 위에
  **0.03 밖에 못 얹는다**. 13.85 [2] 의 최소사이각 6.39°(포트~핫플) · 9.03°(핫플~오븐)가
  같은 자리를 가리킨다. test_5(저항 4종만)가 늘 최악이다 (잔차 −110.9W).

---

## 7. 재현

```bash
python -X utf8 src/run_diag_rollback.py                      # 사슬 이전 대 사슬, 한 채점기
python -X utf8 src/run_diag_rollback.py --no-chain results/seq_fix_ctl.pt
python -X utf8 src/run_diag_lag.py                           # 전이 뒤 감쇠
python -X utf8 src/run_score_seq.py results/seq_fix_ctl.pt   # 상대 잔차 포함
python -X utf8 -m src.run_gate_states                        # [5] 슬롯 정합
python -X utf8 src/run_gate_proj.py                          # 사영·어텐션 관문
python -X utf8 -m src.run_plot_real --ckpt results/cnn_v37.pt --tag v37fix
```

체크포인트: `cnn_v33~v37`(사슬 이전) · `seq_fix_{ctl,attn,alloc}`(고친 S_STATE, seed 0)
· `seq_proj_{ctl,size,alloc}`(**버그 기준선**, 참고용만)
