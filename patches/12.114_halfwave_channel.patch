--- a/src/model/inputs.py
+++ b/src/model/inputs.py
@@ -39,8 +39,24 @@
 # **번호를 옮긴 이유**: `out` 이 `np.empty` 라 FINE_CHANNELS 아래의 모든 칸은
 # 반드시 채워져야 한다. 반증된 블록을 48~50 에 둔 채 새 채널을 51~52 에
 # 놓으면, FINE_CHANNELS=53 이 되어 반증된 채널 3개가 학습에 되살아난다.
-FINE_CHANNELS = 58           # 36 + 추세 2 + 위상 8 + 역률·고차비 2
-                             #   + 다단강하 2 (12.62) + 홀수차 크기 8 (12.80)
+FINE_CHANNELS = 51           # 36 + 추세 2 + 위상 8 + 역률·고차비 2
+                             #   + 다단강하 2 (12.62) + 반파 대비 1 (12.114)
+
+#: 반파 정류 대비 채널의 자리 (12.114).
+#
+# **50~57 의 홀수차 크기 블록(12.80)은 은퇴시키고 그 자리를 쓴다.** 그 블록은
+# 소진 목록 2번이다 — 미니PC 0.763 -> 0.658 로 반증됐고, 운영점 계보
+# (`cnn_ovh`/`adapt_ovh`/`cnn_smpsf`/`adapt_smpsf`) 가 전부 `fine_channels=50`
+# 이라 애초에 안 쓰고 있다.
+#
+# 뒤에 붙여 FINE_CHANNELS=59 로 두면 **반증된 8채널이 학습에 같이 되살아나
+# 단일 변수가 깨진다** — 40행 주석이 경고하는 바로 그 함정이다. 그래서 슬롯을
+# 물려받는다. 0~49 는 그대로라 옛 체크포인트는 `fine[:, :50]` 으로 계속 돈다
+# (`build_fine` 산출물의 0~49 가 바이트 일치함을 확인했다).
+#
+# ⚠ 50~57 블록을 되살리려면 이 채널을 먼저 옮겨야 한다. 지금은 `>= 53`·`>= 58`
+# 가드가 FINE_CHANNELS=51 에서 안 걸려 충돌하지 않는다.
+HALFWAVE_CH = 50
 WIDE_CHANNELS = 12
 
 # 고조파 위상 불변량의 크기 게이트 (A). |I_h| 가 이 값 근처 아래로 내려가면
@@ -111,6 +127,14 @@
 # 1.2절의 asinh 스케일 상수. 배치마다 재계산하지 않는다.
 RATIO_SCALE = 50.0           # 고조파비
 CURRENT_SCALE = 20.0         # 절대 고조파 전류
+
+# 반파 대비 채널의 평활 반폭 (12.114). ±0.5초 = 61주기.
+# **중앙값이 아니라 평균이다.** `_movavg` 독스트링대로 (B,600,61) 정렬은 캐시
+# 생성을 25분 넘게 늘린다. 재 보니 평균으로도 전이 오탐이 다 죽는다 —
+# 핫플 릴레이(0.9초 펄스)의 원시 오탐 2.58/3.32/4.33% 가 셋 다 0.00% 가 되고,
+# 포트·오븐·선풍기·에어컨·프로젝터·충전기·미니PC 도 전부 0.00% 다.
+# 드라이기 약풍은 평균에서 오히려 더 잘 산다 (99.68 -> 100.00%).
+HALFWAVE_HALF = 30
 POWER_SCALE = 100.0          # P, Q
 V_CENTER, V_SPAN = 222.0, 10.0
 
@@ -360,6 +384,37 @@
         for slot, h in enumerate((1, 3, 5, 7, 9, 11, 13, 15)):
             out[:, 50 + slot] = np.arcsinh(mag[:, h - 1] * CURRENT_SCALE)
 
+    # ── 반파 정류 대비 |I2| − |I4| (2026-08-27, 12.114절) ────────────────
+    # 12.109.2 가 드라이기 약풍을 **다이오드 반파 정류**로 확정했다 (P 가 정확히
+    # 절반, `|I2|/|I1|` 0.395, 이론 0.424). 그런데 12.77 의 짝수차 배제가 그것까지
+    # 0 으로 만들고 있었다 — 저항 3종에서 **유일하게 신원이 확실한 신호**를.
+    #
+    # 짝수차 전체를 되살리면 12.72 의 계측 인공물이 함께 돌아온다. 그럴 필요가
+    # 없다. **둘은 차수 프로파일의 모양에서 갈린다.**
+    #
+    #   반파 이론  |I2|:|I4|:|I6|:|I8| = 0.424 : 0.085 : 0.036 : 0.020   가파르다
+    #   인공물                          평평 (12.71.2: 0.0288/0.0292/0.0290/0.0289)
+    #
+    # 그래서 `|I2| − |I4|` 는 평평한 성분을 상쇄하고 반파만 남긴다. 격리에서
+    # (61주기 평활 후) 드라이기 약풍 0.670A vs **나머지 전 기기·전 상태 ≤0.014A**,
+    # 겹침 0 이다. 핫플(101.8Ω)과 드라이기 약(겉보기 102.6Ω)은 12.112.3 이
+    # 저항 정합의 유일한 모호쌍으로 남긴 것인데, 여기서 0.0001 vs 0.670 으로 갈린다.
+    #
+    # **비율이 아니라 절대량이다.** `|I2|/|I1|` 문턱은 복합에서 분모가 터져 죽는다
+    # (12.79 가 미니PC 에서 겪은 그것). 실측 13파일에서 재면 `test_12`(|I1| 9.15A)
+    # 에서 비율 형태는 발화의 72% 를, `test_10` 은 전부를 놓친다. 절대 차분은 안 그렇다.
+    #
+    # 실측 검증: `test_11` 정밀도 0.981 / `test_12` 1.000 (드라이기 ON 중 재현율
+    # 0.196·0.279 로, 격리에서 잰 약풍 비중 31.4% 와 맞는다). SMPS 전용 `test_13`
+    # 과 `test_7`·`test_8` 은 0.0% — **오탐이 없다.**
+    #
+    # ⚠ `ZERO_EVEN_HARMONICS` 는 True 로 둔다. 이 채널은 `EVEN_FINE_CHANNELS`
+    # 목록에 없고, 드라이기 외 8종에서 수치적으로 ~0 이라 그 창들의 입력이
+    # 사실상 안 바뀐다. 그래서 12.77 을 되돌리지 않고도 신호만 얻는다.
+    if FINE_CHANNELS > HALFWAVE_CH:
+        out[:, HALFWAVE_CH] = np.arcsinh(
+            _movavg(mag[:, 1] - mag[:, 3], HALFWAVE_HALF) * CURRENT_SCALE)
+
     if ZERO_EVEN_HARMONICS:
         out[:, [c for c in EVEN_FINE_CHANNELS if c < FINE_CHANNELS]] = 0.0
     return np.nan_to_num(out, nan=0.0, posinf=0.0, neginf=0.0)
