# 인수인계 2026-09-01 — SMPS_PLAN 실행과 그 절반의 반증

`HANDOFF_2026-08-27.md` 다음 세션의 기록이다. 그 사이 2026-08-31 세션이
`SMPS_PLAN_2026-08-31.md` 를 남겼고(12.119~12.121), 이 세션이 그 계획을
검토하고 실행했다.

**계획의 절반이 반증됐고, 실행 중에 내가 세운 인과 가설 셋 중 둘도 반증됐다.**
규칙이 9개 늘었는데 다섯은 내 잘못된 결론에서 나왔다.

자세한 것은 설계 문서 **12.122.1 ~ 12.122.18**, 방법론은 **규칙 23~31**.

---

## 0. 한 줄

**남은 난제는 처음 그대로 SMPS 3종 내부 오배분이다** (12.87.3). 이 세션이
더한 것은 **정량화·판정할 자·검증된 처방 둘**이고, 그 셋이 다음 세션의 출발점이다.

---

## 1. 다음에 할 것 — 순서대로

### [1] 불감대 x in-situ 지문 조합 (아직 안 함. 가장 값지다)

```bash
python -m src.run_fit_insitu_sig --ridge 0.1                    # 5분
python -m src.run_adapt --init results/cnn_ovh.pt --cache cache/train60_ovh \
    --steps 1000 --seed 0 --sig-insitu results/sig_insitu.npz \
    --harm-deadzone 4.0 --tag adapt_combo_s0 --out results      # 3분 x 시드 3
python -m src.run_power_check --ckpt results/adapt_combo_s*.pt  # 후처리 끄고
python -m src.run_gate_check --ckpt results/adapt_combo_s0.pt --postproc on \
    --resmatch 0.02 --rm-snap
```

**근거**: 정답 잔차의 51% 가 아직 고칠 수 있는 **구조적** 오차다 (12.122.18).
지문이 그것을 줄이고(−18% 확인) 불감대가 못 고치는 부분만 용서한다.
**순서가 중요하다** — 불감대를 먼저 키우면 고칠 수 있는 것까지 용서한다.

**판정 기준 (돌리기 전에 적는다)**
```
① 프로젝터 전력오차 +29.7W 에서 줄 것            불감대 단독 −8.8, 지문 단독 −12.2
② F1 0.846 에서 오를 것                        불감대 단독 +0.029 (SD 0.008)
③ 유령: 대조 3파일과 함께 볼 것 (규칙 20)        불감대는 귀속 불가, 지문은 귀속됨
④ 두 축의 이득이 더해지는가, 겹치는가            겹치면 같은 것을 두 번 고친 것이다
```

### [1b] `p_noise` 를 자유 항으로 여는 것 — 이번 세션에서 접었는데 다시 볼 것

2단계에 슬랙이 없어 총전력 오차 32W 가 반드시 기기로 배분된다 (12.122.19).
**"총합이 맞으니 슬랙은 무관하다" 며 접었는데 저항 없는 층만 보고 한 판단이다.**
저항이 낀 층에서는 1단계가 32.56W 틀리고 2단계가 그것을 기기에 얹는다.

⚠ 4.2절이 상수로 둔 것은 의도였다 — 슬랙이 있으면 모델이 아무것도 설명 안 하고
거기 버린다. 그래서 **벌점 설계가 필요하고**, `--w-over` 처럼 한 방향만 벌하는
형태나 L1 벌점을 얹은 자유 항을 생각해 볼 수 있다.

### [2] 불감대 배수 상한

`0.5 / 1 / 2 / 4` 에 전부 단조였고 4.0 에서도 안 무너졌다. **8.0 / 16.0 이
어디서 무너지는지 안 봤다.** 12.12.2 가 `L_cons` 만 남으면 "합만 맞추는 해" 로
간다고 쟀으므로 붕괴 지점이 있어야 한다.

### [3] tau 재보정

`HARM_DEADZONE_PROFILE` 은 **격리 지문**으로 뽑았다. in-situ 지문을 쓰면
잔차가 달라지므로 tau 도 다시 뽑아야 한다. 둘이 결합돼 있다.

### [4] 운영점 채택 판단 — 측정이 아니라 판단이다

```
--harm-deadzone 4.0   F1 +0.029, 충전기 재현 +0.037, 프로젝터 전력 −8.8W
                      유령 +2.5W 인데 **대조가 더 움직여 귀속 불가**
--sig-insitu          프로젝터 전력 −12.2W
                      유령 +2.5W 이고 **대조가 안 움직여 귀속됨 = 실제 비용**
프로젝터 스냅          프로젝터 전력 8.1 -> 0.5W (16배), 유령 +0.44W
                      **맞바꿈이지 실패가 아니다** (12.122.7 이 12.122.4 를 정정)
```

셋 다 "겨냥한 것은 고치고 유령이 는다" 형태다. **무엇이 사용자에게 중요한가**가
정해져야 채택이 정해진다. 그건 측정으로 안 나온다.

---

## 2. 확정된 진단

### 관측이 오답을 선호한다 (12.122.2)

모델도 손실도 없이 NNLS 로만 풀어도 다섯 파일 전부에서 **사람이 적은 정답
구성이 오답보다 잔차가 1.05~1.79배 나쁘다.** 순방향 모델 오차가 판별 신호의
2.8배다. 그래서 고조파 잔차를 줄이는 절차는 무엇이든 같은 자리로 간다.

### 잔차의 해부 (12.122.18)

```
‖y‖ 14.104 (홀수 8차 x Re/Im = 16차원)
  ① 지문 span 밖   0.0599 (6.0%)   어떤 지문 조합으로도 불가
  ② 정답 support   0.1953 (19.5%)
  ③ 자유           0.1375 (13.8%)

정답 잔차 0.1953 의 내역
  31%  지문 span 밖 — 못 넘는 바닥
  18%  지문 형상 오차 — in-situ 적합이 실제로 회수
  51%  아직 설명 못 함. **구조적이다** (같은 구성끼리 잔차 코사인 0.560)
```

### 적응이 프로젝터 오차를 만든다 — 기제가 닫혔다 (12.122.19)

```
                    프로젝터 평균오차   중앙|오차|   총전력 잔차
1단계 (cnn_ovh)        +15.4W       15.0W      32.56W
2단계 (adapt_ovh)      +31.6W       30.9W       7.30W
```

**적응이 프로젝터 과대예측을 2배로 만들고 그 대가로 총전력 잔차를 4.5배 줄인다.**

```
① 2단계가 하는 일이 "실측 총전력을 맞추는 것" 이다 (4.2절). 1단계는 32.56W 틀린다
② 그런데 2단계에 **기기별 전력 감독이 없다** — L_cons 는 합만 알고 누구인지 모른다
③ 그리고 **슬랙이 없다** — p_noise 는 실측에서 상수 1.4W (realdata.py:141),
   자유 흡수원인 standby 는 관측의 1.0%. 잔차가 −0.4~−7.3W 이므로
   **그 32W 는 반드시 기기 어딘가로 배분된다**
④ 어디로 갈지는 L_harm 이 정하고 **와트당 노름이 작을수록 싸다** (12.120.3):
   프로젝터 0.1215 < 충전기 0.1354 < 미니PC 0.1680
⑤ 그래서 프로젝터가 받는다
```

**⑤가 직접 증명됐다** — in-situ 적합이 앵커 없이 돌아 충전기 노름을 ×0.69 로
줄이자(프로젝터보다 작아짐) **유령이 그대로 충전기로 옮겨갔다**
(`test.2` 0.16→22.94W, 전부 충전기). 노름 순서를 바꾸면 배수구가 따라 바뀐다.

**12.120.9 의 철회는 절반만 맞았다.** 와트당 비대칭이 원인인 것은 맞고,
**기울기가 아니라 값이다.** 12.120.5 의 A 는 `.detach()` 로 값을 안 건드려서
(설계상 "물리 보존") 재구성이 안 바뀌었고 배수구도 안 바뀌었다.

**처방이 셋으로 갈린다**
```
값을 바꾼다   in-situ 지문        +29.7 -> +17.5W          검증됨
밀지 않는다   불감대              +29.7 -> +20.9W, F1 +0.029  검증됨
슬랙을 준다   **안 해봤다**        p_noise 를 자유 항으로 여는 것.
                              이번 세션에서 "총합이 맞으니 무관" 이라며
                              접었는데 **저항 없는 층만 보고 한 판단이다.**
                              저항이 낀 층에서는 1단계가 32W 틀린다.
                              다시 볼 값어치가 있다 (12.122.19)
```

### 지문이 상태를 뭉갠다 (12.122.3)

`harmonic_signatures` 는 통전 구간 중앙값 하나를 낸다. 그런데 형상이 전력에
따라 변하고, **충전기는 고전력에서 프로젝터와 형상이 같아진다** (h5 차 0.3%.
저전력에서는 15.5%). 지문이 하필 겹치는 상태의 것이다.

### 12.87.3 은 둘로 갈렸다 (12.122.5)

```
검출   **틀렸다.** 학습 신호가 없었을 뿐이다. 사람 로그를 걸면 라벨을 못 본
      파일에서 프로젝터 정밀 0.792 -> 0.825 (5파일 x 3시드 = 15/15).
      귀무 대조(라벨 순환 이동)는 −0.079 로 반대 방향 — 이득이 라벨 내용에서 온다
      ⚠ 충전기 재현은 **판정 보류** (시드 0 통과, 시드 1 하락, 층 SD 0.106)
배분   **확정.** 기기별 정답을 줘도 전이 ΔP 상대오차가 0.754 로 안 움직인다
```

---

## 3. 새로 만든 것

### 도구

```
src/evaluation/power_ref.py   기기별 전력 오차 — **참값을 아는 넷**
src/run_power_check.py        위 채점 + --recompute-ref
src/run_fit_insitu_sig.py     복합에서 지문 적합 + LOFO 검증
src/run_sig_conditioning.py   지문 조건화 진단 (판별력·kappa·누설·식별가능성)
src/run_stratified_smps.py    12.121.7 의 창 층으로 채점
src/run_real_on_lofo.py       사람 라벨 LOFO 5폴드 드라이버
src/model/sig_model.py        sig(P,V) — **채택 안 함.** 토글 녹화가 오면 그릇이 된다
```

### 플래그 (전부 기본 꺼짐 — 기본값이면 이전과 글자 그대로 같다)

```
run_adapt  --harm-deadzone      L_harm 불감대 (12.122.16)          <- 25전 1승
           --sig-insitu         L_harm 지문 교체 (12.122.11)
           --w-real-on          사람 라벨 on/off 지도 (4.5절)
           --real-on-scope      smps|present|all (규칙 3)
           --human-label-shuffle  귀무 대조
run_gate_check  --snap / --snap-oneway / --snap-share / --snap-no-redist
run_fit_insitu_sig  --ridge / --free-norm / --all-appliances / --no-anchor
```

### 참값표 (`power_ref.REFERENCE_W`)

```
electiric_kettle 1276.9W   hotplate 460.1W   oven 1143.2W   beam_projector 46.9W
제외: 에어컨·미니PC·충전기·선풍기·드라이기 (폭/중앙 0.41~1.45)
```

⚠ **가드 둘이 필수다** — 라벨상 켜진 저항의 **합**을 관측이 감당하는지, 그리고
참값의 `V^2` 전압 보정. 없으면 저항 과소예측이 3배 부풀려진다 (12.122.15).

---

## 4. 반증·철회 목록

### 계획이 틀린 것

| SMPS_PLAN 의 진술 | 결과 |
|---|---|
| ② 를 `‖sig_i−sig_j‖` 로 잰다 | 분모가 없다. d′ 로는 정반대 판정 (12.122.1) |
| §2.2 "차수별 정보 17배" | 크기만 쓴 값. 위상 넣으면 h=3 이 h=1 과 같다 |
| §3 "정보 있는 차수와 무게가 다르다" | 죽었다. 4.2 취소 |
| §2.3 "누설은 없다" | h≥3 로 흐른다. 저항 전용 파일에서 유령 13.3W |
| 4.3 프로젝터 스냅 | 유령 +0.44W 로 '반증' 했다가 **12.122.7 이 정정** — 겨냥한 것은 16배 고친다 |
| 4.3/4.5 "충전기 재현 0.641" | 층을 안 적었다. 파일 전체 0.96, 그 층 0.685 |
| 1번 "5분, 재학습 없음" | **그 스크립트가 없었다** |
| §5 "저항을 계속 켜 둔 채" | 불가능. 핫플 8초·오븐 108초에 끊긴다. 전기포트만 198초 |

### 내가 틀린 것

| 내 진술 | 결과 |
|---|---|
| "프로젝터는 저항 결손의 배수구다" (12.122.13) | **철회** (12.122.14). 층을 안 갈랐다 |
| "저항이 40~90W 과소예측된다" | **지표 버그** (12.122.15). 실제 0.3~3.4% |
| "토글 녹화가 유일한 입구" (세 번 바꿈) | in-situ 적합이 새 녹화 없이 된다 (12.122.11) |
| "sig(P,V) 로 된다" (12.122.9) | 격리→복합 전이 실패 (12.122.10) |
| "충전기 재현 0.685→0.806 통과" | 시드 1 에서 하락. 폭 안 (12.122.5) |

### 24전 0승 재감사 (12.122.8)

```
확실히 뒤집힘   1개  항목 8 (물리 전력 범위, 12.87.2) — 고조파 잔차로 닫았는데
                    그 자가 오답을 선호한다. 전력오차로는 원본이 5.6배 나쁘다
원 판정 유지    3개  기울기 균등화 / 헤지 0 / 짝수차 제외 — 새 자로도 안 움직인다
규칙 25 무관    8개  순수 측정
미확인 위험군   5개  후처리 6·13·16·17·18. **전부 재학습 없이 각 15초면 본다**
```

---

## 5. 새 규칙 (23~31)

```
23  판별력에는 **분모**가 있어야 한다 — 신호만 재면 결론이 뒤집힌다
24  집계 지표는 어려운 경우를 희석한다 — 층을 적고 인용하라
    (따름) 층화 지표는 예민한 대신 시끄럽다. 그 층의 잡음 바닥을 따로 재라
25  처방을 판정하려면 **그 처방이 겨냥한 것을 재는 자**가 있어야 한다
26  "새 녹화가 필요하다" 고 적기 전에 이미 있는 변동을 먼저 훑어라
27  비(比)가 좋아진 것과 좋아진 것은 다르다 — 분모가 같이 줄었는지 보라
28  한 방법이 막힌 것과 자료가 부족한 것은 다르다 — "남은 방법" 칸을 채워라
29  보존 제약이 걸린 계에서 오차는 사라지지 않고 옮겨 다닌다
    (단 회계를 **층마다** 내라 — 한 층만 보면 전가와 일반화 실패를 못 가른다)
30  가드가 안 걸리면 **가드가 약한 것부터** 의심하라
31  자유도를 늘려 잔차를 줄이기 전에 **식별성이 남는지** 보라
```

---

## 6. 환경 메모

- **`processed_data/holdout60/` 을 재생성했다.** 커밋 2139c6e 의 캐시 정리에서
  `X.npy` 가 지워져 있었다. 같은 씨앗(20260821)인데 생성기에 옵션 3개가 추가돼
  **레시피 분포가 ~5% 다르다.** 옛 로그의 "합성 홀드아웃 MAE/F1" 과 비교하면 안 된다.
- LOFO 체크포인트 `.pt` 는 커밋하지 않았다 (112MB, 재생성 가능). 재생성 명령은
  각 결과 JSON 의 `config` 에 있다.
- 학습 명령에 `--init results/cnn_ovh.pt --cache cache/train60_ovh` 를 반드시 줄 것.
  `run_adapt` 기본값은 `cnn_v17.pt` / `cache/train60` 이라 운영점과 다르다.

---

## 7. 라벨이 정말 필요한가 (12.122.18)

```
용도                 라벨 없이           대가
채점                  불가 (원리적)       일회성 검증. 배포 비용 아님
--w-real-on 검출 지도   뺄 수 있다         +0.034 (작다)
in-situ 지문          모델 게이트로 40%   정답잔차 −7% vs −18%
불감대 tau            게이트로 대체 가능   기제 자체는 라벨 무관
총전력 유일해           **죽음**          저항 없는 39창 중 1창 (3%)
```

**수기 기록이 부담이면 정답을 없앨 게 아니라 수집을 자동화한다.** 격리 녹화는
이미 무기록 정답이고(한 번에 한 기기), 복합 정답이 필요하면 설치 마법사가
"지금 켜세요" 하고 앱이 시각을 기록하면 같은 정보가 나온다.
