# 인수인계 2026-09-05b — **계측 규약이 정리됐다.** 그리고 철회가 둘 있다

앞 문서: `HANDOFF_2026-09-05.md` (원시 파형 도착, 회로 모델 수립, ① 생성기 세 항목).
설계 **12.185.20 ~ 12.185.26**, 측정 규칙 **80 ~ 82**. 커밋 `2eb7181` (푸시함).

---

## 0. 한 줄

**오늘 얻은 것은 파라미터가 아니라 방법이다.** "잔차를 줄이는 것" 후보가 아홉 개 나왔고
**여덟이 자리표로 기각**됐다. 살아남은 하나는 회로가 아니라 **계측 규약 결함**이었다 —
펌웨어의 위상 교정이 2Hz 블록에만 걸리고 원시 파형에는 안 걸려 있었다. 장소 C 포트 원시
2개(순저항)가 그것을 모델 없이 잡았다.

**⚠ 어제 저녁 보고한 "nvt 채택" 을 철회했다.** 적분기 인공물이었다 (§3).

---

## 1. 지금 상태

```
운영점        results/adapt_ph5_s0.pt (변경 없음). 배포 묶음 ../power_monitor 그대로
정본 파라미터  results/_circuit_raw_C.json 의 `fit_fixrd`  (rd 0.3Ω 고정, 원시 위상교정 0.44°/차수)
              laptop_charger  C_dc 67.3µF R 4.27Ω L  846µH Cx 0.118µF   훈련 5.26% LOO 6.18%
              beam_projector  C_dc 53.8µF R 6.06Ω L  616µH Cx 0.367µF   훈련 2.06%
              minipc          C_dc 46.5µF R 9.05Ω L 1245µH Cx 0.304µF   훈련 4.48% LOO 4.61%
              재현 바닥 1.2~1.8%.  `fcm.load_models()` 기본이 이것
              ⚠ 훈련·LOO 가 어제보다 나쁘다. **그게 옳은 방향이다** — §2 의 12.185.25
원시 규약     `RAW_SKEW_SAMP_LOW` = −0.313 표본 (0.44°/차수). `load_raw` 가 기본으로 건다
              펌웨어 위상 교정이 원시에 없어서 우리가 건다. 등록부에 상수와 근거
시뮬레이터    노드를 **음해 뉴턴**으로 푼다 (선형화 버림). nvt 를 켜면 4배 느리다 (0.9->3.5ms).
              안 켜면 전과 같다. `i0`(지수항 기준전류)가 파라미터가 됐다
생성기        ①a `VoltageCluster.v_distortion_h3` + `apply_site_distortion` (저항만)
              ①b `coupling.TextureModel` + `VoltageEnvironment.texture_stem` +
                 `apply_voltage_texture` (SMPS 만). `CouplingModel`(Z) 은 **일부러 안 붙였다**
커밋          master 2eb7181 까지 **푸시함**. power_monitor main 2959992 는 아직 로컬
```

## 2. 오늘 밝힌 것 (전부 GPU 없이)

```
12.185.20  요소 재검정 (12.184.10 철회분) — ⚠ 이 절의 nvt 채택은 12.185.23 에서 철회
12.185.21  ①a 저항 부하. `V_site,h/R` 은 그대로는 안 된다 — 전압 채널이 h3 을 5배 과다.
           절대값으로 가르면 h3 은 **덧셈 33.5mA∠+164°(계측기)**, h5~h15 는 |I1| 비례.
           장소축이 서는 건 h3 하나: d_3 = A 0.000 / B 0.003 / **C 0.031**
           ⚠ 장소 **안**에서는 어느 모형도 지금 생성기보다 못하다 (홀드아웃 62.7 대 52.9%)
12.185.22  ①b 결합. 지배 축은 공유 임피던스가 **아니라 세션의 전압 텍스처**다.
           조합 V 와 단독 V 의 차 0.87~1.03% 중 Z·I 는 0.05~0.08% (1/12).
           전력 재배분 없는 조합에서: 단순중첩 13.5% -> 결합만 12.7% -> **텍스처만 2.7%**
12.185.23  ⚠ **철회** — nvt 의 이득은 다이오드가 아니라 **적분기**였다. §3
12.185.24  계측 모형. 사용자가 준 LTspice 넷리스트가 판정을 끝냈다 — **RC 1극이 맞다**.
           LOW 는 `N002`(U1 출력)에서 갈라지므로 세 채널이 1k·100n 을 하나씩 지난다.
           GBW 까지 넣으면 60Hz LOW−HIGH 위상차 −0.055°, 1극 모형 대비 h15 에서 0.5° 안
12.185.25  **포트 원시 2개가 규약 결함을 찾았다.** 순저항은 `i(t)=v(t)/R` 이 모든 표본에서
           성립한다. ∠I₁−∠V₁ = +2.864°/+2.871° (두 파일 0.007° 안, 펌웨어 HIGH 2.62°).
           펌웨어 위상 교정은 2Hz 블록에만 걸린다 — 원시에는 남아 있다. LOW 상수 0.44° 를 건다.
           유보 조합 5.92 -> 4.21%. 부수: 전압 채널 |A₃| = 2.62% of V₁ 직접 풀이,
           짝수차 확정(전류 h2 0.091% 대 전압 2.587%), h17+ 는 양 채널 잡음 바닥
12.185.26  잔차 진단. **②a 는 도통 *상승* 에지**(하강의 1.5~3.6배, 전 기기·전 동작점),
           |I1| 비례, 펄스 무게중심이 전력 의존(+3.2 -> −2.0 표본).
           **②b 는 닫는다** — 기기 독립 덧셈 2.6~4.0mA. dv/dt 로 R² 0.13 뿐이라 Cx 가 아니다
```

## 3. 철회한 것 — **다음 세션이 같은 함정에 빠지지 않게**

```
"nvt(지수 다이오드 무릎) 채택. 훈련 −50%, 충전기 LOO 5.4->3.3%, 조합 −55%"
   -> **전부 적분기 인공물.** 반음해 노드 갱신의 선형화 지점 때문이고, 노드를 음해
      뉴턴으로 제대로 풀면 같은 파라미터가 2.30% 가 아니라 **24%** 다 (nsub 48배 수렴 확인).
      정정 후: nvt 가 충전기·미니PC 에서 정확히 0, Shockley(rd·Vf 제거)도 기본으로 복귀,
      물리값(nVt 0.076) 강제하면 세 기기 다 나빠진다, 충전기 LOO 5.4->6.8%.
      저쪽 가이드 §12.1 이 독립적으로 같은 결론. **다시 시도하지 마라**

"12.184.16: LOW 는 HIGH 의 ADC 노드 뒤라 RC 극이 하나 더 있다 (2.18° 의 설명)"
   -> **회로도와 다르다.** LOW 는 `N002`(U1 **출력**)에서 갈라진다. 세 채널이 1k·100n 을
      하나씩만 지난다. (LOW 교정 0.44 라는 **결론 자체는** 직접 측정 근거라 유효 —
      틀린 것은 그 **설명**이다. 2.18° 의 정체는 ADC 랭크 스큐다 — §5 사용자 정보)

"②a = 충전기 19W 이상점"
   -> 19W 만의 문제가 아니다. 충전기 다섯 점이 전부 **상승 에지 19~29%** 다.
      19W 는 신호가 작아 비율이 커 보였을 뿐

"alpha(직류 부하 지수)를 다시 시험해 볼 만하다"
   -> **구조적 미식별.** 직류 리플이 1~6% 뿐이라 −3~+1 전 구간에서 손실이 0.4% 안에서만
      움직이고 단조도 아니다. **자료를 더 모아도 안 된다**
```

**기각된 아홉 — 다시 꺼내기 전에 왜 죽었는지 읽어라.**
```
회로 요소   nvt(다이오드 무릎) · Gp(초크 감쇠) · alpha(부하 지수) · Shockley(rd·Vf 제거)
계측 모형   전류 2극 · 전압 2극 · fc 자유 · CT/ZMPT 고역통과 · 배경 RC 규약
```

## 4. 판별 서명 — **이것이 오늘의 진짜 소득이다**

같은 자리(주파수에 따라 커지는 위상 지연)를 서로 다른 이름으로 옮기는 후보가 계속 나왔다.
**서명으로 가른다:**

```
              훈련·LOO   유보 조합    L        판정
자리표          좋아짐     나빠짐    무너짐    nvt · 전류2극 · fc상향 · 양의 스큐
옳은 보정       나빠짐     좋아짐    올라감    원시 위상교정 (−0.313 표본)
```

**훈련이 나빠지는 것이 오히려 증거다.** 자유 파라미터가 흡수하던 계통 오차를 없애면
파라미터가 물리값으로 밀리고, 대가는 자기 자료에서 치르되 **다른 녹화에서 갚는다.**

그리고 **`L` 이 진짜 오차를 흡수하는 자리다** — 기각된 넷이 전부 L 을 건드렸다
(536 -> 23µH 까지). "왜 L 이 계통적으로 큰가" 가 본 질문이고, 답은 계측 모형에 없었다.

## 5. 계측계 — 이제 아는 것 (사용자·넷리스트 제공)

```
아날로그 (LTspice 넷리스트, 사용자 제공. `docs/external/code/afe_netlist_check.py` 로 계산)
  바이어스  R2/R3(3.3V->1.65V) -> U2 팔로워 -> N005 = 세 채널 공통 기준
  전류      CT(SCT-013-100) -> 버든 R8 27Ω -> U1 비반전 ×3.478 -> **N002**
    HIGH    N002 -> R7 1k -> C2 100n -> ADC_high                    RC 1극 (전체 ×3.5)
    LOW     N002 -> R4 5.04k -> U3 반전 ×−14.84 -> R6 1k -> C5 100n  RC 1극 (전체 ×51.7)
  전압      ZMPT-101B -> 버든 R12 220Ω -> U4 비반전 ×13.27 -> R11 1k -> C4 100n  RC 1극
  -> 세 채널이 1k·100n(fc 1591.55Hz)을 **정확히 하나씩**. GBW 로 인한 차이는 h15 에서 0.5° 안

디지털 (사용자 제공)
  12비트, 16배 하드웨어 오버샘플 + 우측시프트 2 -> 14비트 코드(0~16383)
  ADC_OFFSET_NONE (ADC 가 빼는 값 없음), HAL_ADCEx_Calibration_Start
  **표본 하나 = 연속 16회 변환의 하드웨어 평균** -> 유효 표본 시각이 ~20µs **폭**을 갖는다
  랭크 스큐 18~21µs = **0.39~0.45°/차수**  <- 펌웨어 LOW 상수 0.44° 의 정체
                                              (HIGH 2.62° = 121µs ≈ 랭크 6칸? 미확인)

CSV 열의 정체 (사용자 제공)
  high  v_r1  low  v_r2      손 안 댄 ADC 코드
  i_a  v_v  i_low_a  i_high_a  range   수신기 파이썬이 (코드−오프셋)×LSB/감도 + 레인지 판정
  off_high  off_low  off_volt         펌웨어가 그때 쓰던 오프셋 (참고)
  -> 파생 열에도 **위상 교정·회전·지연 보정은 한 줄도 없다** (사용자 확인)

⚠ 원시에는 RC 감쇠 보상(`s_rc_gain`)이 없다. 2Hz 의 `ih*`/`vh*` 는 보상된 값이다.
   두 경로의 고조파를 직접 비교하려면 `sqrt(1+(h·60/1591.55)²)` 를 곱해 맞춘다
   (h1 0.07% / h15 13%). 우리 배경 빼기가 이 불일치를 안고 있는데 [M6] 에서 영향 없음 확인
```

## 6. 자 (전부 `python -X utf8 -m src.<이름>`, GPU 없음)

```
run_raw_fit_probe          원시 다점 적합 + Cx 직접측정 + rd/Cx 고정 + LOO         5분
run_element_raw_probe      요소 재검정 [1]재현 [2]겹쳐시작 [2b]효과크기 [2c]Shockley
                           [3]겨냥 [4]LOO [6]**유보 조합**                        15분
run_meas_rc_probe          계측 모형 M0~M6 + fc 훑기 + 유보 조합                  15분
run_resistive_raw_probe    포트 원시 — v–i 정렬·채널 전달·h17+·짝수차 (모델 없음)   10초
run_resistive_raw_lowhigh  같은 자료를 LOW/HIGH 로 갈라서                         10초
run_residual_probe         잔차 진단 — 시간 구조·비도통 회귀·에지·전력 의존        1분
run_resistive_site_probe   ①a 저항 부하의 장소 의존 (2Hz, 11개 녹화)              1분
run_coupling_probe         ①b 결합/텍스처 [B1]~[B5]                             10분
run_raw_combo_probe        결합 검정 [A]단순중첩 [B]조합V_term [C]단독V_term       1분
synthesis/fit_raw          원시 적합 (대칭화·RC·배경·다점·대역제한·LOO·확장요소·스큐)
synthesis/coupling         TextureModel(지배, 생성기 기본) / CouplingModel(미배선)
synthesis/circuit_sim      시뮬 (음해 뉴턴 노드, i0 파라미터)
```
결과: `_circuit_raw_C.json`(정본) · `_circuit_elements_raw_C.json` · `_meas_rc.json` ·
`_residual.json` · `_resistive_site.json` · `_coupling.json`.
기준: `results/_criteria_circuit.md` — [E1]~[E8] [A1]~[A5] [B1]~[B5] [M1]~[M7] [N1]~[N4].
**사후에 적은 것은 그렇게 표시돼 있다.**

## 7. 자료

```
원시 스냅샷 (장소 C, 40주기 × 256표본 @ 15360Hz)
  단독  laptop_charger×5 (17~73W) · beam_projector×2 (47W) · minipc×5 (7~25W)  전부 LOW
  조합  beam_minipc×2 · beam_charger×2 · smps3×2      <- **유보 자료. 판정은 이걸로**
  순저항 raw_elcetric_kettle_1/2 (1485W, HIGH 86%)    <- **계측 판정의 정본** ⚠ 파일명 오타
  등록부 RAW_SNAPSHOT_FILES / RAW_COMBO_FILES / RAW_COMBO_SOLO / RAW_RESISTIVE_FILES
2Hz  laptop_charger_5C · minipc_4C · beam_projector_4C · electric_kettle_4C(포트 78계단)
     hair_dryer_4C · noise_noselfpower_C(배경) · tesr_19(⚠오타, 타임라인 등록부에)
     저항 11개(장소 A 8 · B 2 · C 1)가 ①a 의 근거
```
장소 C 파일은 전부 QUARANTINE (풀 혼입 보류). `data/` 는 gitignore.

## 8. 다음 수

```
① 게이트 5   **생성기가 ①a·①b 로 바뀌었으니 다시 통과시켜야 GPU 로 간다.**
             잔차를 더 깎는 것보다 이게 먼저일 수 있다 — 지금 상태로도 생성기는 돈다
             (단독 2.1~5.3%, 조합 3.4%, 단순중첩 13.5% 대비 4배)
② ②a 도통 상승 에지 (열려 있음)
   후보 (a) 도통 개시의 전력 의존 — 다이오드 역회복, 초크의 피크 근처 비선형
        (b) 소스 전압의 h17+ — 상승 에지가 거기 가장 민감한데(12.185.16) 12.185.25 가
            h17+ 는 두 채널 다 잡음 바닥이라고 했다
   ⚠ 아홉이 이미 기각됐다. **유보 조합으로 먼저 걸러라** (규칙 81)
③ ②b 미니PC — **닫혔다.** 계측 바닥 2.6~4.0mA. 다음 수는 회로가 아니라 **더 나은 배경**
   (무부하 창을 길게 재녹화). 미니PC 7W 는 배경/|I1| = 0.201 이라 그게 정확도 상한
④ 계측 요청 (우선순위 순)
   G′ **LOW 에 머무는 순저항 원시** (백열전구 60~100W / 납땜인두, 0.3~1A) — 최우선.
      원시 위상교정의 정확한 값을 가른다 (유보 최적 0.70° 대 펌웨어 상수 0.44°, §9)
      포트는 86%가 HIGH 라 LOW 경로를 못 본다
   F′ 무부하 배경 재녹화 (길게) — ②b 의 바닥을 낮춘다
   A′ (저쪽 제안) 10~15분마다 20주기 원시 스냅샷 — 배포 현장의 전압 텍스처 (P8)
   H  프로젝터 두 번째 동작점 (지금 1개라 LOO 판정 불가)
⑤ 펌웨어
   · **원시 스냅샷 발행 시 위상 교정을 적용하거나 적용 여부를 헤더에 명시** (12.185.25)
   · ADC 스캔 시퀀스의 채널 수·순서 — HIGH 2.62° 가 랭크 6칸인지 확인
   · LOW/HIGH 위상 응답을 이어 붙이기 **전에** 맞춤 (P5. 이제 정체가 랭크 스큐로 밝혀졌다)
⑥ 장소 A·B 는 갈 수 없다. 전이·분모 편향은 열린 채
GPU 는 게이트 5 통과 전엔 돌리지 않는다. 돌리기 전에 묻는다.
```

## 9. 미결 (사용자 결정 / 미상)

```
삭제 두 건     READ_ME_FIRST.txt / SMPS_PLAN_2026-08-31.md (작업 트리 삭제, 커밋에서 계속 뺐다)
풀 혼입        장소 C 파일을 풀(장소 A)에 넣을지 — ①a·①b 의 절대 앵커가 여기 걸려 있다
power_monitor  main 2959992 (수신기 v5) 아직 로컬
GitHub main    옛 계보 정리 (실제 작업은 master)
원시 위상교정   유보 최적 −0.500 표본(0.70°/차수) 대 펌웨어 상수 −0.313(0.44°). 상수를 채택했다
               (독립 측정 근거, 자유 파라미터 0). 0.19 표본 차이는 **미상** — G′ 가 가른다
HIGH 2.62°     랭크 스큐 18~21µs 로는 0.4°/차수뿐이다. 2.62° = 121µs ≈ 6칸인지 **미확인**
```

## 10. 읽기 주의 — 실제로 빠졌던 함정

```
· **적분기 수렴을 먼저 확인해라** (규칙 80). 중립값에서 비트 단위로 같아도 요소를 켠 자리에서
  발산할 수 있다. nvt 가 그래서 18배 갈렸다
· **LOO 는 계통 오차를 못 잡는다** (규칙 81). 모든 폴드가 같은 오차를 공유한다.
  판정 순서: **유보 자료 > LOO > 훈련**
· **계측을 의심하면 순저항 원시부터** (규칙 82). HIGH·LOW 둘 다 찍어야 한다
· 위상 기준 프레임: 시뮬은 소스 V1 기준, np.fft 는 절대 프레임 (규칙 79)
· 계측 RC: 시뮬은 참 전류, 원시에는 RC 가 걸려 있다. **그리고 위상 교정은 없다** (12.185.25)
· 배경: 규칙 78. 그리고 2Hz 배경을 원시에서 뺄 때 RC 규약이 어긋난다 ([M6], 영향은 없었다)
· `fcm.to_spectrum` 은 길이로 관례를 가른다 — 129면 X[h], 그 밖이면 V[k−1]=h(k)
· 시뮬의 P 는 직류 부하 전력, 측정 p_w 는 교류 입력 전력 (`match_power`)
· 전압 짝수차는 아날로그 전단의 덧셈 성분 — 반파 대칭화로 지운다. 12.185.25 가 재확인
· 원시는 40주기 중앙값. 73W 충전기에 30Hz 교대가 있으나(lag-1 −0.64/−0.74) 홀짝 서명이
  0.33° 안이라 "두 펄스 모양" 설명은 우리 자료에서 안 선다
· 텍스처 라이브러리의 **h17+ 는 차수별로 믿지 마라** — 양 채널 잡음 바닥. 총량으로만
· ⚠ 폴링 루프(`until grep ...; do sleep; done`)를 백그라운드로 띄우지 마라. 감시 대상이
  죽으면 무한히 돈다. 완료 알림이 자동으로 온다
```

## 11. 규칙 (80~82)

```
80  확장 요소를 켜기 전에 **적분기가 그 자리에서 수렴하는지** 먼저 확인한다
    (a) 중립값에서 비트 단위 일치 (b) nsub 16배까지 손실 수렴 (c) 다른 도식과 5% 안
81  LOO 는 계통 오차를 못 잡는다 — 판정은 **다른 녹화**로. 유보 > LOO > 훈련
82  계측을 의심하면 **순저항 원시부터** 찍는다. 큰 것(포트 6A, HIGH)과 작은 것(전구 0.3~1A, LOW) 둘 다
```

## 12. 외부 협업

`docs/external/` 에 받은 가이드·코드. 오늘 받은 것: `circuit6.py`(Shockley v6),
`vtexture_lib.py`+`vtexture_C.npz`(세션 텍스처 라이브러리), 가이드 §12(우리 ①a·b·c 보고에
대한 검정), `circ_rc_laptop_charger.pkl`.

**저쪽과 독립적으로 같은 결론에 도달한 것**
- §12.1 nvt 는 다이오드가 아니다 (그쪽: nVt 하한·Is 상한 / 우리: 적분기 인공물)
- §12.4 시뮬 전류에 계측 RC 를 걸어야 한다 (그쪽 L 988->750µH 로 우리 값에 수렴)
- §12.7 `CouplingModel` 미부착이 옳다 — 소스가 실측 파형이면 결합은 이미 안에 있다

**우리가 더 밝힌 것 (보내야 할 것)**
- 원시에 위상 교정이 없다 (12.185.25). 그쪽 파이프라인에도 걸린다
- 넷리스트로 RC 1극 확정 — §11.2 의 "LOW 2극" 추론 폐기
- §12.3 의 73W 주기 교대는 우리 자료에서 "두 펄스 모양" 이 아니다 (홀짝 서명 0.33° 안)
- §12.5 텍스처의 h17+ 는 차수별로 못 믿는다 (순저항으로 확인)
- 잔차의 자리는 도통 **상승** 에지다 (12.185.26)
