# 인계 2026-09-15 — 진행 중 상태

## ⚠⚠ 지금 손에 들고 있는 것 (검증 안 끝남)

`src/model/traincache.py` 에 **방금 넣은 고침 둘**을 아직 **확인 못 했다**:

```
① initargs 에 `float(vtex_coarse_s or 0.0)` 를 `vtex_seg_s` 다음 자리에 끼워 넣었다
② meta 에 `"vtex_coarse_s"` 를 적게 했다
```

**반드시 이것부터 확인할 것** — `initargs` 는 **위치 인자**라 한 칸만 밀려도
`harmonic_z` 가 `vtex_coarse_s` 자리로 들어가 **조용히 다른 캐시를 굽는다** (14.62 와 같은 부류):

```bash
python -X utf8 -c "
import inspect, sys, re, io; sys.path.insert(0,'.')
from src.model import traincache as T
sig=list(inspect.signature(T._init).parameters)
s=io.open('src/model/traincache.py',encoding='utf-8').read()
m=re.search(r'initargs=\((.*?)\),\s*\n\s*chunksize', s, re.S)
args=[x.strip() for x in re.sub(r'#[^\n]*','',m.group(1)).split(',') if x.strip()]
print(sig[14:19]); print(args[14:19])
print('OK' if len(sig)==len(args) else '**개수 안 맞다**')
"
```
그리고 `compile()` 로 전체 검사 (`ast.parse` 는 **중복 인자를 안 잡는다** — 오늘 당했다).

---

## 지금 도는 작업

```
984520  굽기 (seg_s=3 균일, 옛 설정)  8토막 x 40코어   <- **멈추고 ⓐ 로 다시 던져야 한다**
```
ⓐ 로 바꾸는 작업이 끝나면: `scancel 984520` → `rm -rf ~/NILM_AI/cache/train60_v32hs3_s*`
→ `sbatch patches/cache_v32hs3_par.sbatch`

## 오늘 확정된 굽기 설정 (ⓐ)

```
patches/cache_v32hs3_par.sbatch
TREAT="--harmonic-z --vtex-step-s 1.25 --vtex-seg-s 1.25 --vtex-coarse-s 25"
8토막 x 40코어 (한 파도) · -t 10:00:00 · 결과 cache/train60_v32hs3
관문 ① Z표 · ①-b 캐시모양 · ①-c 텍스처경계 · ② 굽기경로 · ③ 끄면 v32 비트동일 · ④ meta 넷만 다름
```

**예상**: 균일 20토막(약 7시간) → 비균일 12토막 → **~4.3시간**, 세밀 해상도 3.33초 → **1.11초**

---

## 오늘 만든 것 (전부 커밋됨, 마지막 커밋 `b0690da` 이후 미커밋 있음)

| 번호 | 내용 | 상태 |
|---|---|---|
| 14.84 | `--w-harm` 가설 **반증** · `L_harm` 이 자리 D 에서 부호가 반대 | 기록됨 |
| 14.85 | 오귀속 표를 평균으로 읽어 없는 병을 지어냄 → 중앙값으로 고침 | 기록됨 |
| 14.86 | `run_diag_fptrig` — 전이 정체별 헛detect. (전이 없음) 칸 **0.0%** | 기록됨 |
| 14.87 | 광역에 "계단이 몇 초 전"을 담을 수 있나 — 담기는데 모델이 안 씀 | 기록됨 |
| 14.88 | `--wide-seg-pool` 구현 + 관문 (14.87 ②를 정정) | 기록됨 |
| 14.89 | `--wide-seg-pool` **기각** · 광역 축을 닫는다 | 기록됨 |
| 14.90 | **`wtap` 은 창 전체를 읽는다** — 계단이 아니라 *평평함*이 증거 | 기록됨 |
| 14.91 | `--wide-extra-dilations` + 관문 | 기록됨 |
| 14.92 | 광역 `p_std` 는 입력엔 1등인데 머리가 안 씀 (SMPS 전용) | 기록됨 |
| 14.93 | 캐시 **광역 모양** 관문 (세밀만 막혀 있었다) | 기록됨 |
| 14.94 | `--wide-extra-dilations` **기각** — 광역에 용량을 주면 분산만 는다 | 기록됨 |
| 14.96 | 굽기 비용 측정 · 한도는 노드 10 · 코어 640 | 커밋됨 |
| 14.99 | `current()` 메모이즈 — **바이트 동일**, 1.21배 | 커밋됨 |
| 14.100 | 8토막/8노드 (토막 수 = 노드 수여야 파도가 하나) | 커밋됨 |
| 14.101 | 텍스처 보간 **기각** (자료·회로 두 자가 모두 반증) | 커밋됨 |
| 14.102 | 토막 경계 **위상 무작위화** — 세밀 창 안 3자리 → 533자리 | 커밋됨 |
| **14.103** | **비균일 토막** (세밀 촘촘 + 바깥 성김) | **미완** |

---

## 판정이 안 끝난 것

**`cnn_hzwt_*` (hz + wtap, `984489`) 체크포인트 6개를 받아만 놓고 판정을 못 했다.**
`results/cnn_hzwt_s{0,1,2}.pt` · `cnn_hzwt_wt_s{0,1,2}.pt`

미리 적어 둔 자 (14.95):
```
대조  cnn_v32h_on (hz 단독)   ★1 면적 4.454 ± 1.005 · ★2 축퇴 30.6 ± 1.5%
      (참고) v32 off 3.814 ± 0.402 / 42.1 ± 5.6%  ·  wtap A 1.529 ± 1.257 / 22.4 ± 2.0%
예측  ① 가산적이면 ★1 이 약 2.2, 짝차 **3/3 음수**
      ② ★2 는 **25% 밑**, 3/3
      ③ 둘 다 안 움직이면 두 처치가 **같은 자리를 다투는** 것이다
지킴  저항 4종 신원 0.97 · SMPS · 판정 줄 · 절대 잔차 (v32h_on 과 **짝으로**)
```
★1 재는 법: `test_5` 128~142초 오븐 통전확률(`state[:,oven].softmax[2]`)의 **면적**
★2 재는 법: 참 포트 ON · 참 오븐 OFF 창(61개)에서 오븐 게이트가 서는 비율

---

## 오늘 세운 규칙 (기억에 넣을 것)

- **토막 수는 노드 수로 나누어떨어져야 한다** — 안 그러면 마지막 하나가 파도를 통째로 쓴다 (6.8h → 13.6h)
- **`ast.parse` 는 중복 인자를 안 잡는다** — `compile()` 을 써라
- **`initargs` 는 위치 인자다** — 서명에 끼워 넣으면 같은 자리에 넣어라
- **굽기가 도는 중에 코드를 동기화하지 마라** — `spawn` 풀은 시작 때만 뜨므로 도는 것은 안전하지만, **대기 중인 토막이 새 코드를 읽는다**
- **쿼터는 `/usr/lpp/mmfs/bin/mmlsquota` 만 정본** — `du $HOME` 도 `df` 도 아니다
- **"특징에 정보가 있나" 를 복호기로 묻지 마라** — 복호기의 실패는 정보의 부재와 구별되지 않는다. 입력을 흔들고 특징이 움직이는지 봐라
- **혼동표를 평균으로 읽지 마라** — 쌍봉에서 평균은 어느 창도 대표하지 않는다. 그리고 `라벨 != 예측` 은 "모델이 틀렸다" 가 아니다

---

## 저장소

```
57.07G / 100G 소프트 / 110G 하드   (오늘 32G 회수)
남은 것  train60_v32h 23G · _v32_head512 40M(관문 ③④용 슬라이스) · holdout60_v32/v32h 각 4.9G
지운 것  train60_v32 본체 · holdout60_v33/v36 · 로컬에도 있는 .pt 170개 · 끝난 .out
```
⚠ `cache/_v32_head512` 를 지우면 굽기 관문 ③④ 가 못 돈다.
