# -*- coding: utf-8 -*-
"""새 계측기(단일 입력 ADC, 2026-09-06)로 만든 회로 모델 v12g 와 그 코드 (사용자 제공, README_v12.md).

    from circuit_model.fcm12 import FCM          # 패키지로
    python circuit_model/fit12.py minipc ...     # 단독 실행도 그대로 된다 (circuit12 를 평면 임포트로도 찾는다)

저장소 쪽 어댑터는 `src/synthesis/fcm.py` 의 `DeviceModel12` / `load_models()` 다.
"""
