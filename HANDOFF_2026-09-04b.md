# 인수인계 2026-09-04b — 회로 모델 검토, 장소 C 첫 자료, 그리고 **계측이 먼저였다**

**다음 문서: `HANDOFF_2026-09-05.md`** (원시 파형으로 회로 모델이 섰다. 아래 회로 수치는 그 문서가 폐기했다.)

앞 문서: `HANDOFF_2026-09-04.md` (아침·오후: 운영점 교체, 12.179~12.183. 저녁분도 그 문서 ★★ 블록에 누적돼 있으나
정정이 섞여 있다 — **이 문서가 정본**). 설계 **12.184.1 ~ 12.184.16**, 측정 규칙 **74 ~ 76**.

---

## 0. 한 줄

**회로 모델(캡 입력 정류기)은 장소 C 실측 파형으로 두 어댑터를 h11 까지 5° 안에 재현한다 — 실패한 것은 모델이 아니라
그 입력·비교의 자(전압 채널 고차, HIGH 레인지 전류 인공물, 장소 A 파형 부재)다.** 그리고 장소 C 에서 운영점은
SMPS 3종 동시에 없는 에어컨 40~100W 를 세우고, 포트를 포트로 못 본다 (저항 부하의 고조파 = 그 장소의 전압 고조파).

---

## 1. 지금 상태

```
운영점            results/adapt_ph5_s0.pt (변경 없음). 배포 묶음 ../power_monitor 도 그대로 (수신기만 v5)
펌웨어            v5 (vh_cdeg[15], 공통부 116B, 프레임 3243B). LOW 교정 0.44 / HIGH 2.62 — **원래 값으로 복원됨**
                  소스: ..\NILM_ECE_IF-fix-even-harmonics-offset-bug (v5) · Documents\NILM_ECE_IF-main (v4) · ~\NILM_ECE_IF_clone (v4)
자료 규약         전 파일·npz·캐시·체크포인트·sp_curves 가 한 규약(LOW 0.44). 읽기 경로 회전은 laptop_charger_5C 의 PHASE_FIX(+10.8°×h)
                  하나뿐. LOW_CAL_FLASHED_ACTIVE=False (2.62 로 찍힌 파일 없음)
커밋              NILM_AI master 4a908e2, f78046e (+이 문서). power_monitor main 2959992. **전부 로컬 — 푸시 안 함**
GitHub main       사용자의 v5 수신기 커밋(f544a21)이 옛 계보 `main` 에 갔다. 기본 가지 정리는 사용자 결정
```

## 2. 오늘 밝힌 것 (전부 GPU 없이)

```
12.184.1   가이드 판정: 방향(생성기를 회로 물리로) 채택. 장소 기술자는 (R,L) 이 아니라 전압 페이저 15개. §1.1 결합은 2차.
           §5 norton 정규화 결함 -> 절대판으로 고침. §6.3 지터 서술 틀림. 장소 이름 불일치 (가이드 1/A = 우리 A)
12.184.2   장소 C = 12.179.4 의 세 번째 환경 (237V, vh3 9V). 게이트 5 전압축: vh3(직교) -> 펄스 이동 -> h 선형 회전·크기 불변
12.184.3   충전기 5C 의 −10.8°×h = 부하 없는 USER 버튼 교정 (대기전류 위상 +10.8 -> +0.6). PHASE_FIX 로 복원
12.184.7   드라이기 강 HIGH 기본파 −0.01° (채널 지연 없음). 불감대 라벨 교대 = 같은 파형에 두 교정값 (±1° 얼룩, 불감대에서만)
12.184.8   미니PC 4C: 게이트 5 기기·부하 축 통과 (h3~h11 10~15°, 부하 추세). 충전기·프로젝터는 시뮬이 1.5~2배 과다
12.184.9   포트 전압계 불합격 (같은 장소 두 저항이 h3~h7 에서 갈림). 장소 C 실측 파형 재피팅: 충전기 0.0088 통과, 미니PC 0.016.
           장소 B 는 h9,11,13 만으로 프로젝터 위상·크기 추세 재현. 전이(C->A)는 V_A 없이 못 잰다
12.184.10  §3.3 요소(다이오드 무릎·초크 감쇠·부하 지수) 기각. 잔차(h13~h15 중부하)는 h9~h15 전압 입력. k_hi 0.73~0.89
12.184.11  포트 ①: 두 채널이 h3 에서 3%, h5~h15 에서 0.3~1% 어긋남. 짝수차는 전압 채널 인공물 확정. Z_C 0.45Ω
12.184.12  E5: 포트 비를 전압 보정으로 걸면 SMPS 피팅 2~16배 악화 -> 포트 비는 HIGH 전류 인공물. E6: 채널 보정은 기기 간 안 옮음
12.184.14  프로젝터 4C: T_C 가 충전기와 같다 (회전은 부하의 함수). ON 49W 한 점이라 재피팅 못 믿음
12.184.15  test_19/20: 운영점이 3종 동시에 에어컨 40~100W, 포트를 포트/드라이기/핫플/오븐으로 나눔. 중첩은 h7 까지, h9↑ 는 동반
           기기 의존 — 겹친 펄스 피크 3.4A 가 LOW 포화 2.2A 를 넘어 HIGH 경로로 (계측). 포트 창의 SMPS h5~h11 20~45% 오독
12.184.16  **정정**: LOW 0.44 가 옳다. 2.18° = LOW 경로 안티앨리어싱 RC 한 극 (1kΩ·100nF, 60Hz 2.16°). 2.62 권고 철회, 보드 복원
```

## 3. 철회한 것 — 다음 세션이 같은 함정에 빠지지 않게

```
"LOW 교정 = HIGH(2.62)"              불감대 자료는 이어 붙인 한 파형이라 경로 지연을 안 잰다. 회로 추론은 RC 를 빠뜨렸다. 규칙 76
"정본 규약(−2.18°×h)에서 미니PC 손실 개선"   회전된 입력을 모델이 선호한 것. 물리 판정 아님
"옛 포트 녹화로 장소 A·B 전압 위상 복원"      HIGH 레인지 저항 고조파 0.3~1% 는 바닥. 규칙 75
"4차 펌웨어 0.5ms 채널 지연"                 버튼 교정이었다. 규칙 74
"장소 회전은 기기 정체성"                    부하(도통각)의 함수 — 충전기 70W = 프로젝터 49W, 미니PC 9W 만 3~4배
```

## 4. 자 (전부 `python -X utf8 -m src.<이름>`, GPU 없음)

```
run_circuit_gate_probe     게이트 1·4·5 + 장소 C 전달비 + 위상 회전 검정 + 짝수차 + 미니PC 기기·부하 축      5초
run_site_voltage_probe     포트 전압계 검정 + 장소 전압 페이저 A/B/C + 장소 C 실측 파형 재피팅(3종) + 전이·장소 B    1분 (--no-fit 10초)
run_circuit_element_probe  §3.3 요소 E0~E6 (입력 차수 제한, 무릎, 감쇠, 부하 지수, k_hi, 포트 보정, 채널 보정 교차)   --variants, 3분
run_vh_calib_probe         순저항으로 전압 채널 검정 (I_h/V_h = 1/R), 짝수차 인공물 표, 계단 Z. --low 로 전구가 오면 보정표  5초
run_combo_probe            동시 녹화: 계단 Δ vs 단독 서명(중첩), 포트 창 (총전류−포트) vs SMPS 만                 10초
synthesis/circuit_sim      시뮬레이터 (vsrc·절대 페이저·도통각·norton 절대판·nvt/Rp/alpha 요소)
preprocessing/raw_phasors  전류/전압 페이저, 정상 구간 서명, 순수 회전 검정, apply_phase_rotation(mask=)
```
결과 파일: `results/_circuit_params_C.json` (장소 C 재피팅), `_circuit_elements_C.json`, `_site_voltage.npz`,
`_vh_check_electric_kettle_4C.npz`. 기준: `results/_criteria_circuit.md` (세 번 추가, 전부 결과 전에 적음).

## 5. 자료 (전부 장소 C, 9/04, v5, LOW 0.44 규약)

```
laptop_charger_5C    96분 30~97% 충전 완주. ⚠ PHASE_FIX +10.8°×h (버튼 교정). 격리
minipc_4C            19분 IDLE 9W + 12~30W 계단. 기본 교정. 격리                       <- 게이트 5 통과 자료
beam_projector_4C    16분 ON 49W ×4 + 대기. 옛 규약(OVERRIDE). 격리. 동작점 1개
electric_kettle_4C   6분 1485W on/off 3회. 순저항 검정용. 격리
hair_dryer_4C        5분 강/약 교대. 격리
noise_noselfpower_C  5분 외부 전원 배경 1.7W. 격리
test_19 / test_20    3종 동시 / 3종+포트 on/off. 라벨 없음 (COMPOSITE_EVAL, 옛 규약 OVERRIDE)
electric_kettle_3_new / hotplate_4_new   9/02 장소 B, 지금까지 미등록이던 것 -> 등록·격리
```
격리(QUARANTINE)는 **풀 혼입 결정 보류**다 — 풀은 장소 A. 넣을지는 사용자 결정 (0‴).

## 6. 다음 수 (순서)

```
① 펌웨어 (사용자·펌웨어 작성자)
   a. 두 경로의 위상 응답 차(LOW 의 RC 한 극)를 표본을 이어 붙이기 **전에** 맞춘다 — SMPS 펄스 피크(>2.2A)가 HIGH 로 넘어가
      한 파형에 두 위상이 섞인다 (12.184.15b, 12.184.16). 겹친 3종 피크 3.4A 도 같은 문제
   b. HIGH 레인지 홀수차 인공물 0.5~3% (포트 ≠ V/R, 이득·오프셋 이음매로는 안 나옴) 원인 (12.184.11~12)
   c. 전압 채널 짝수차 인공물 (ZMPT) — 홀수차도 h11 이상 20~40% 과다 의심 (12.184.10)
② 생성기, GPU 전 (싸고 확실한 것부터)
   a. **저항 부하를 I_h = V_site,h / R 로 만든다** — 장소 C 포트가 포트로 안 보이는 이유가 이것 하나다 (12.184.15a). 고정 서명 대신
      장소 전압 페이저(A/B/C 실측 포락, 홀수차)에서 계산. 계산 하나
   b. V_term 조건 입력 + V_src 무작위화 합성 (12.184.6) — SMPS 쪽. 게이트 5 는 미니PC 만 통과했고 충전기·프로젝터는 분모(장소 A 파형)
      편향 때문에 판정 불가 -> ④ 없이는 회로 시뮬 고정점(가이드 §6.1)까지 가지 않는다. 당장은 s(p)+FCM 1차(norton) 로
③ 자료 (장소 C 안에서)
   a. 백열전구 60~100W / 납땜인두 1분 (LOW 순저항) — 전압 채널 홀수차 보정표. 없으면 h11↑ 0.75 배 가정으로 감
   b. 프로젝터 두 번째 동작점 (밝기·에코 모드 3분) — 재피팅이 서려면 필요
   c. 펌웨어 ①a 뒤 test_20 재녹화 — 포트 켜진 창의 SMPS 오독(20~45%)이 얼마나 주는지
④ 장소 A·B 는 갈 수 없다 (사용자). 전이·분모 편향은 그때까지 열린 채로
GPU 는 게이트 5(위상+크기, 충전기·프로젝터 포함)가 통과하기 전엔 돌리지 않는다. 돌리기 전에 묻는다.
```

## 7. 미결 결정 (사용자)

```
풀 혼입      장소 C 파일 8개·장소 B 저항 2개를 풀(장소 A)에 넣을지 — ②a/②b 가 되기 전엔 넣지 않기를 권함
삭제 두 건   READ_ME_FIRST.txt / SMPS_PLAN_2026-08-31.md (작업 트리 삭제, 커밋에서 뺐다)
GitHub       main(옛 계보) 정리, 오늘 커밋 푸시
```

## 8. 채점·읽기 주의

```
원본 CSV 는 read_raw_csv / pipeline 이 등록부 PHASE_FIX·LOW_CAL 을 건다. 원시가 필요하면 phase_fix=False
5C 만 +10.8°×h. 다른 회전은 없다 (LOW_CAL_FLASHED_ACTIVE=False)
전압 채널: 짝수차 쓰지 마라 (인공물). 홀수차 h3~h9 는 믿고 h11↑ 는 의심
HIGH 라벨 창(포트·드라이기 켜짐)의 SMPS 고조파는 계측 단계에서 20~45% 다르다 — 그 창의 배분 실패를 모델 탓으로만 읽지 마라
실측 T_C/T_B 의 분모는 장소 A 곡선(파형 미상) — 시뮬(이상 정현파 분모)과 비교할 땐 방향·추세만
USER 버튼은 순저항이 물린 채로만. 녹화 첫 30초 무부하(대기전류 지문, 장소 C +10.2°)
```

## 9. 규칙 (74~76)

```
74 펌웨어·수신기·교정이 바뀌면 저항 부하로 전류–전압 위상 기준부터 재라 (옛 녹화 대조 쌍: 크기 1·위상 h 선형 = 교정/채널)
75 저항 부하를 전압계로 쓰려면 같은 장소·같은 날의 두 저항으로 먼저 검정하라 — HIGH 저항 고조파는 바닥이다
76 교정값을 바꾸기 전에 그 값을 잰 조건을 재현하라. 이어 붙인 파형의 라벨 교대는 경로 지연을 안 재준다
```
